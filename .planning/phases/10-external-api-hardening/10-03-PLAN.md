---
phase: 10-external-api-hardening
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - frontend/src/pages/ApiKeyManagement.tsx
  - frontend/src/pages/WebhookManagement.tsx
  - frontend/src/pages/ApiDocs.tsx
  - frontend/src/services/apiKeyService.ts
  - frontend/src/services/webhookService.ts
  - frontend/src/types/api.ts
  - frontend/src/App.tsx
  - frontend/src/utils/roleAccess.ts
autonomous: true
requirements:
  - API-03
  - API-05

must_haves:
  truths:
    - "管理员可在 UI 中创建 API Key 并看到一次性明文弹窗"
    - "管理员可在 UI 中查看所有 Key 的名称、创建日期、最后使用日期和过期状态"
    - "管理员可在 UI 中轮换和撤销 Key"
    - "管理员可在 UI 中注册 Webhook URL 并查看投递日志"
    - "API 使用指南页面包含认证说明、分页教程和快速开始步骤"
  artifacts:
    - path: "frontend/src/pages/ApiKeyManagement.tsx"
      provides: "API Key 管理页面"
      contains: "ApiKeyManagementPage"
      min_lines: 100
    - path: "frontend/src/pages/WebhookManagement.tsx"
      provides: "Webhook 管理页面"
      contains: "WebhookManagementPage"
      min_lines: 80
    - path: "frontend/src/pages/ApiDocs.tsx"
      provides: "API 使用指南增强版"
      contains: "ApiDocsPage"
    - path: "frontend/src/services/apiKeyService.ts"
      provides: "API Key 服务层"
      exports: ["createApiKey", "listApiKeys", "rotateApiKey", "revokeApiKey"]
    - path: "frontend/src/services/webhookService.ts"
      provides: "Webhook 服务层"
      exports: ["registerWebhook", "listWebhooks", "unregisterWebhook", "getWebhookLogs"]
  key_links:
    - from: "frontend/src/pages/ApiKeyManagement.tsx"
      to: "frontend/src/services/apiKeyService.ts"
      via: "createApiKey/listApiKeys/rotateApiKey/revokeApiKey calls"
      pattern: "import.*apiKeyService"
    - from: "frontend/src/pages/WebhookManagement.tsx"
      to: "frontend/src/services/webhookService.ts"
      via: "registerWebhook/listWebhooks calls"
      pattern: "import.*webhookService"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/ApiKeyManagement.tsx"
      via: "Route path='/api-key-management'"
      pattern: "ApiKeyManagement"
---

<objective>
前端 + 文档：创建 API Key 管理页面（创建/列表/轮换/撤销，含一次性明文弹窗）、Webhook 管理页面（注册/列表/投递日志）、增强 API 使用指南页面（认证说明、分页教程、快速开始），并注册路由和角色权限。

Purpose: 让管理员通过 UI 管理 API Key 和 Webhook，让外部开发者通过指南页面快速上手对接。
Output: 2 个新页面、2 个服务模块、1 个页面增强、类型定义更新、路由注册
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-external-api-hardening/10-CONTEXT.md
@.planning/phases/10-external-api-hardening/10-02-SUMMARY.md

@frontend/src/App.tsx
@frontend/src/pages/FeishuConfig.tsx
@frontend/src/pages/ApiDocs.tsx
@frontend/src/services/api.ts
@frontend/src/types/api.ts
@frontend/src/utils/roleAccess.ts
</context>

<interfaces>
<!-- Backend API endpoints created in Plan 02 -->

API Key Management endpoints (admin-only, JWT auth):
```
POST   /api/v1/api-keys/              → ApiKeyCreateResponse { key: ApiKeyRead, plain_key: string }
GET    /api/v1/api-keys/              → ApiKeyRead[]
GET    /api/v1/api-keys/{key_id}      → ApiKeyRead
POST   /api/v1/api-keys/{key_id}/rotate  → ApiKeyRotateResponse { key: ApiKeyRead, plain_key: string, old_key_id: string }
POST   /api/v1/api-keys/{key_id}/revoke  → ApiKeyRead
```

Webhook Management endpoints (admin-only, JWT auth):
```
POST   /api/v1/webhooks/              → WebhookEndpointRead
GET    /api/v1/webhooks/              → WebhookEndpointRead[]
GET    /api/v1/webhooks/{webhook_id}  → WebhookEndpointRead
DELETE /api/v1/webhooks/{webhook_id}  → 204
GET    /api/v1/webhooks/{webhook_id}/logs → WebhookDeliveryLogRead[]
```

TypeScript interfaces needed:
```typescript
interface ApiKeyRead {
  id: string;
  name: string;
  key_prefix: string;
  is_active: boolean;
  rate_limit: number;
  expires_at: string | null;
  last_used_at: string | null;
  last_used_ip: string | null;
  created_at: string;
  updated_at: string;
  created_by: string;
}

interface ApiKeyCreateResponse {
  key: ApiKeyRead;
  plain_key: string;
}

interface ApiKeyRotateResponse {
  key: ApiKeyRead;
  plain_key: string;
  old_key_id: string;
}

interface WebhookEndpointRead {
  id: string;
  url: string;
  is_active: boolean;
  description: string | null;
  events: string[];
  created_by: string;
  created_at: string;
  updated_at: string;
}

interface WebhookDeliveryLogRead {
  id: string;
  webhook_id: string;
  event_type: string;
  payload: Record<string, unknown>;
  response_status: number | null;
  response_body: string | null;
  attempt: number;
  success: boolean;
  error_message: string | null;
  created_at: string;
}
```

From frontend/src/services/api.ts:
```typescript
import api from './api';
// api is an axios instance with JWT interceptor, base URL from VITE_API_BASE_URL
```

From frontend/src/utils/roleAccess.ts:
```typescript
export function getRoleModules(role?: string): { title: string; description: string; href: string }[];
// Admin modules include system management items
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 类型定义 + 服务层 + API Key 管理页面 + Webhook 管理页面</name>
  <files>
    frontend/src/types/api.ts
    frontend/src/services/apiKeyService.ts
    frontend/src/services/webhookService.ts
    frontend/src/pages/ApiKeyManagement.tsx
    frontend/src/pages/WebhookManagement.tsx
  </files>
  <read_first>
    frontend/src/types/api.ts
    frontend/src/services/api.ts
    frontend/src/pages/FeishuConfig.tsx
    frontend/src/pages/AuditLog.tsx
    frontend/src/services/evaluationService.ts
  </read_first>
  <action>
**1. 追加 `frontend/src/types/api.ts`** — 新增类型定义：

在文件末尾追加以下接口（保持项目惯例：集中类型定义）：

```typescript
// === API Key Management ===

export interface ApiKeyRead {
  id: string;
  name: string;
  key_prefix: string;
  is_active: boolean;
  rate_limit: number;
  expires_at: string | null;
  last_used_at: string | null;
  last_used_ip: string | null;
  created_at: string;
  updated_at: string;
  created_by: string;
}

export interface ApiKeyCreatePayload {
  name: string;
  rate_limit?: number;
  expires_at?: string | null;
}

export interface ApiKeyCreateResponse {
  key: ApiKeyRead;
  plain_key: string;
}

export interface ApiKeyRotateResponse {
  key: ApiKeyRead;
  plain_key: string;
  old_key_id: string;
}

// === Webhook Management ===

export interface WebhookEndpointRead {
  id: string;
  url: string;
  is_active: boolean;
  description: string | null;
  events: string[];
  created_by: string;
  created_at: string;
  updated_at: string;
}

export interface WebhookEndpointCreatePayload {
  url: string;
  description?: string;
  events?: string[];
}

export interface WebhookDeliveryLogRead {
  id: string;
  webhook_id: string;
  event_type: string;
  payload: Record<string, unknown>;
  response_status: number | null;
  response_body: string | null;
  attempt: number;
  success: boolean;
  error_message: string | null;
  created_at: string;
}
```

**2. 创建 `frontend/src/services/apiKeyService.ts`** — API Key 服务层：

```typescript
import api from './api';
import type { ApiKeyRead, ApiKeyCreatePayload, ApiKeyCreateResponse, ApiKeyRotateResponse } from '../types/api';

export async function createApiKey(payload: ApiKeyCreatePayload): Promise<ApiKeyCreateResponse> {
  const { data } = await api.post<ApiKeyCreateResponse>('/api-keys/', payload);
  return data;
}

export async function listApiKeys(): Promise<ApiKeyRead[]> {
  const { data } = await api.get<ApiKeyRead[]>('/api-keys/');
  return data;
}

export async function rotateApiKey(keyId: string): Promise<ApiKeyRotateResponse> {
  const { data } = await api.post<ApiKeyRotateResponse>(`/api-keys/${keyId}/rotate`);
  return data;
}

export async function revokeApiKey(keyId: string): Promise<ApiKeyRead> {
  const { data } = await api.post<ApiKeyRead>(`/api-keys/${keyId}/revoke`);
  return data;
}
```

**3. 创建 `frontend/src/services/webhookService.ts`** — Webhook 服务层：

```typescript
import api from './api';
import type { WebhookEndpointRead, WebhookEndpointCreatePayload, WebhookDeliveryLogRead } from '../types/api';

export async function registerWebhook(payload: WebhookEndpointCreatePayload): Promise<WebhookEndpointRead> {
  const { data } = await api.post<WebhookEndpointRead>('/webhooks/', payload);
  return data;
}

export async function listWebhooks(): Promise<WebhookEndpointRead[]> {
  const { data } = await api.get<WebhookEndpointRead[]>('/webhooks/');
  return data;
}

export async function unregisterWebhook(webhookId: string): Promise<void> {
  await api.delete(`/webhooks/${webhookId}`);
}

export async function getWebhookLogs(webhookId: string, limit = 50): Promise<WebhookDeliveryLogRead[]> {
  const { data } = await api.get<WebhookDeliveryLogRead[]>(`/webhooks/${webhookId}/logs`, { params: { limit } });
  return data;
}
```

**4. 创建 `frontend/src/pages/ApiKeyManagement.tsx`** — API Key 管理页面（per D-11, D-12, API-03）：

参考 FeishuConfig.tsx 的页面结构和 AppShell 布局模式。

页面结构：
- 使用 `AppShell` 作为布局容器
- 顶部：标题「API Key 管理」 + 「创建新 Key」按钮
- 表格列：名称、Key 前缀（key_prefix）、状态（活跃/已撤销/已过期）、每小时限流、创建时间、最后使用时间、最后使用 IP、操作按钮（轮换/撤销）
- 状态显示逻辑：
  - `is_active == false` → 红色「已撤销」badge
  - `expires_at && new Date(expires_at) < now` → 橙色「已过期」badge
  - 否则 → 绿色「活跃」badge

创建弹窗（per D-12）：
- 表单字段：名称（必填）、每小时限流（默认 1000）、过期时间（可选 datetime picker）
- 提交后显示明文 Key 弹窗：
  - 警告文字：「请立即复制此 Key，关闭后将无法再次查看」
  - Key 明文文本框（readonly, 全选）
  - 「复制」按钮（使用 navigator.clipboard.writeText）
  - 「我已复制，关闭」按钮
- 关闭弹窗后刷新列表

轮换操作：
- 确认弹窗：「确定要轮换此 Key 吗？旧 Key 将立即失效。」
- 轮换后显示新 Key 明文弹窗（同创建弹窗）

撤销操作：
- 确认弹窗：「确定要撤销此 Key 吗？撤销后立即失效，无法恢复。」
- 撤销后刷新列表

使用 `useState` 管理表格数据、弹窗状态、loading 状态。`useEffect` 加载列表。

**5. 创建 `frontend/src/pages/WebhookManagement.tsx`** — Webhook 管理页面（per D-13, D-14）：

页面结构：
- 使用 `AppShell` 布局
- 顶部：标题「Webhook 管理」 + 「注册新 Webhook」按钮
- 表格列：URL、描述、事件类型（badge list）、状态（活跃/已停用）、创建时间、操作（查看日志/停用）

注册表单：
- URL（必填）
- 描述（可选）
- 事件类型（checkbox list，目前仅 `recommendation.approved`）

投递日志查看：
- 点击「查看日志」展开或跳转子面板
- 列表显示：事件类型、投递时间、HTTP 状态码、尝试次数、成功/失败、错误信息
- 成功条目绿色背景，失败条目红色背景

停用操作：
- 确认弹窗：「确定要停用此 Webhook 吗？」
- 停用后刷新列表
  </action>
  <verify>
    <automated>cd D:/wage_adjust/frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>
    - ApiKeyManagement.tsx 页面存在，包含列表/创建/轮换/撤销全部功能
    - 创建弹窗展示一次性明文 Key + 复制按钮 + 关闭警告（per D-12）
    - WebhookManagement.tsx 页面存在，包含注册/列表/日志/停用功能
    - apiKeyService.ts 和 webhookService.ts 完成全部 API 调用
    - api.ts 类型定义完整
    - TypeScript 编译无错误
  </done>
</task>

<task type="auto">
  <name>Task 2: API 使用指南增强 + 路由注册 + 角色权限</name>
  <files>
    frontend/src/pages/ApiDocs.tsx
    frontend/src/App.tsx
    frontend/src/utils/roleAccess.ts
  </files>
  <read_first>
    frontend/src/pages/ApiDocs.tsx
    frontend/src/App.tsx
    frontend/src/utils/roleAccess.ts
  </read_first>
  <action>
**1. 增强 `frontend/src/pages/ApiDocs.tsx`** — API 使用指南（per D-10, API-05）：

在现有 ApiDocs 页面基础上，添加以下内容模块（参考技术文档风格）：

**认证说明模块：**
- 标题「认证方式」
- 说明所有 `/api/v1/public/` 端点需要 `X-API-Key` header
- 代码示例（curl）：
  ```
  curl -H "X-API-Key: your_api_key_here" https://your-domain/api/v1/public/...
  ```
- 说明 Key 需要由管理员在「API Key 管理」页面创建
- 错误码说明：401 = Key 无效/撤销/过期，429 = 超过限流

**分页教程模块：**
- 标题「游标分页」
- 说明 cursor-based pagination 的工作方式
- 参数说明：`cursor`（分页游标）、`page_size`（每页条数，默认 20，最大 100）
- 响应结构：`items`、`next_cursor`、`has_more`
- 代码示例（Python）：
  ```python
  import requests
  cursor = None
  while True:
      params = {'page_size': 50}
      if cursor:
          params['cursor'] = cursor
      resp = requests.get(url, headers={'X-API-Key': key}, params=params)
      data = resp.json()
      process(data['items'])
      if not data['has_more']:
          break
      cursor = data['next_cursor']
  ```

**快速开始模块：**
- 标题「快速开始」
- 3 步流程：
  1. 联系管理员获取 API Key
  2. 调用 salary-results 端点拉取已审批调薪数据
  3. 使用游标分页遍历全部结果
- 完整 curl 示例

**端点参考模块：**
- 列出全部 `/api/v1/public/` 端点的 URL、方法、参数、响应结构
- 每个端点配 example request 和 example response

**Webhook 说明模块：**
- 标题「Webhook 通知（推送模式）」
- 说明：管理员可注册 Webhook URL，系统在 recommendation.approved 事件时自动 POST
- 签名验证说明：`X-Signature-256: sha256=hmac_hex`
- 代码示例（Python Flask 接收端）：
  ```python
  import hmac, hashlib
  def verify_signature(payload, signature, secret):
      expected = 'sha256=' + hmac.new(secret.encode(), payload, hashlib.sha256).hexdigest()
      return hmac.compare_digest(expected, signature)
  ```

页面保持公开可访问（无需登录），样式使用项目现有的 surface/eyebrow/text-ink/text-steel 类。代码块使用 `<pre><code>` 标签，背景 `var(--color-bg-subtle)`。

**2. 注册路由到 `frontend/src/App.tsx`**：

添加 import：
```typescript
import { ApiKeyManagementPage } from './pages/ApiKeyManagement';
import { WebhookManagementPage } from './pages/WebhookManagement';
```

在 admin-only 路由区域添加：
```tsx
<Route element={<ProtectedRoute allowedRoles={["admin"]} />}>
  {/* existing admin routes... */}
  <Route element={<ApiKeyManagementPage />} path="/api-key-management" />
  <Route element={<WebhookManagementPage />} path="/webhook-management" />
</Route>
```

放在现有 admin-only `<Route>` 组内（与 `/cycles/create`、`/audit-log` 平级）。

**3. 更新 `frontend/src/utils/roleAccess.ts`** — 添加模块入口：

在 admin 角色的模块列表中添加：
```typescript
{ title: 'API Key 管理', description: '创建、轮换和撤销外部 API 访问密钥。', href: '/api-key-management' },
{ title: 'Webhook 管理', description: '注册回调 URL，查看通知投递日志。', href: '/webhook-management' },
```

确保这两个模块仅在 admin 角色中出现。
  </action>
  <verify>
    <automated>cd D:/wage_adjust/frontend && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - ApiDocs.tsx 增强版包含认证说明、分页教程、快速开始、端点参考、Webhook 说明
    - /api-key-management 路由注册成功（admin-only）
    - /webhook-management 路由注册成功（admin-only）
    - roleAccess.ts admin 模块列表包含 API Key 管理和 Webhook 管理
    - TypeScript 编译无错误
    - 前端 dev server 可正常启动
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npm run dev` 启动成功
2. `cd frontend && npx tsc --noEmit` 无错误
3. admin 登录后工作区显示「API Key 管理」和「Webhook 管理」入口
4. `/api-key-management` 页面展示 Key 列表和创建按钮
5. `/webhook-management` 页面展示 Webhook 列表和注册按钮
6. `/api-docs` 页面包含认证、分页、快速开始、Webhook 说明
7. 非 admin 角色访问 `/api-key-management` 被重定向
</verification>

<success_criteria>
- API Key 管理页面完整：列表 + 创建（一次性明文弹窗）+ 轮换 + 撤销
- Webhook 管理页面完整：注册 + 列表 + 投递日志 + 停用
- API 使用指南页面包含认证、分页、快速开始、端点参考、Webhook 说明
- 新路由注册到 App.tsx（admin-only）
- roleAccess.ts 包含新模块入口
- TypeScript 编译通过
</success_criteria>

<output>
After completion, create `.planning/phases/10-external-api-hardening/10-03-SUMMARY.md`
</output>
