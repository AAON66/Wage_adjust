---
phase: 10-external-api-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/api_key.py
  - backend/app/models/webhook_endpoint.py
  - backend/app/models/webhook_delivery_log.py
  - backend/app/schemas/api_key.py
  - backend/app/schemas/webhook.py
  - backend/app/schemas/public.py
  - alembic/versions/xxxx_add_api_key_webhook_tables.py
  - backend/tests/test_api/test_public_hardening.py
  - backend/tests/test_services/test_api_key_service.py
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04

must_haves:
  truths:
    - "ApiKey ORM 模型存在且 key_hash 使用 SHA-256 存储"
    - "WebhookEndpoint ORM 模型存在且包含 HMAC 签名密钥"
    - "WebhookDeliveryLog ORM 模型存在且可记录每次投递结果"
    - "游标分页 wrapper schema 存在且包含 next_cursor + has_more 字段"
    - "ApiKey CRUD schema 完整定义（Create/Read/RotateResponse）"
    - "RED 测试桩覆盖 API-01 到 API-04 全部需求"
  artifacts:
    - path: "backend/app/models/api_key.py"
      provides: "API Key ORM 模型"
      contains: "class ApiKey"
    - path: "backend/app/models/webhook_endpoint.py"
      provides: "Webhook 端点 ORM 模型"
      contains: "class WebhookEndpoint"
    - path: "backend/app/models/webhook_delivery_log.py"
      provides: "Webhook 投递日志 ORM 模型"
      contains: "class WebhookDeliveryLog"
    - path: "backend/app/schemas/api_key.py"
      provides: "API Key Pydantic schema"
      exports: ["ApiKeyCreate", "ApiKeyRead", "ApiKeyCreateResponse"]
    - path: "backend/app/schemas/webhook.py"
      provides: "Webhook Pydantic schema"
      exports: ["WebhookEndpointCreate", "WebhookEndpointRead", "WebhookDeliveryLogRead"]
    - path: "backend/app/schemas/public.py"
      provides: "游标分页 wrapper schema"
      contains: "CursorPaginatedResponse"
    - path: "backend/tests/test_api/test_public_hardening.py"
      provides: "公开 API 加固测试桩"
      contains: "test_approved_only_filter"
    - path: "backend/tests/test_services/test_api_key_service.py"
      provides: "API Key 服务测试桩"
      contains: "test_revoked_key_returns_401"
  key_links:
    - from: "backend/app/models/api_key.py"
      to: "backend/app/models/user.py"
      via: "ForeignKey('users.id') on created_by"
      pattern: "ForeignKey.*users"
    - from: "backend/app/models/webhook_endpoint.py"
      to: "backend/app/models/user.py"
      via: "ForeignKey('users.id') on created_by"
      pattern: "ForeignKey.*users"
    - from: "backend/app/models/webhook_delivery_log.py"
      to: "backend/app/models/webhook_endpoint.py"
      via: "ForeignKey('webhook_endpoints.id') on webhook_id"
      pattern: "ForeignKey.*webhook_endpoints"
---

<objective>
数据基础：创建外部 API 加固所需的全部 ORM 模型（ApiKey、WebhookEndpoint、WebhookDeliveryLog）、Pydantic Schema（Key 管理、Webhook 管理、游标分页 wrapper）、Alembic 迁移和 RED 测试桩。

Purpose: 为 Plan 02 服务层和 API 端点提供稳固的数据层基础和类型契约。
Output: 3 个 ORM 模型、3 个 Schema 模块更新、1 个 Alembic 迁移、2 个测试文件
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-external-api-hardening/10-CONTEXT.md

@backend/app/models/mixins.py
@backend/app/models/audit_log.py
@backend/app/models/salary_recommendation.py
@backend/app/schemas/public.py
@backend/app/core/config.py
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From backend/app/models/mixins.py:
```python
class UUIDPrimaryKeyMixin:
    id: Mapped[str] = mapped_column(String(36), primary_key=True, default=generate_uuid)

class CreatedAtMixin:
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False, default=utc_now)

class UpdatedAtMixin:
    updated_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False, default=utc_now, onupdate=utc_now)
```

From backend/app/core/database.py:
```python
from backend.app.core.database import Base
```

From backend/app/models/salary_recommendation.py:
```python
class SalaryRecommendation(UUIDPrimaryKeyMixin, CreatedAtMixin, Base):
    __tablename__ = "salary_recommendations"
    status: Mapped[str] = mapped_column(String(32), nullable=False, default="draft", index=True)
    # status values: "draft", "pending", "approved", "rejected"
```

From backend/app/schemas/public.py (existing — will be extended):
```python
class PublicSalaryResultsResponse(BaseModel):
    cycle_id: str
    cycle_name: str
    cycle_status: str
    items: list[PublicSalaryResultItem]
    total: int
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: ORM 模型 + Alembic 迁移</name>
  <files>
    backend/app/models/api_key.py
    backend/app/models/webhook_endpoint.py
    backend/app/models/webhook_delivery_log.py
    alembic/versions/xxxx_add_api_key_webhook_tables.py
  </files>
  <read_first>
    backend/app/models/mixins.py
    backend/app/models/audit_log.py
    backend/app/core/database.py
    backend/app/utils/helpers.py
  </read_first>
  <action>
**1. 创建 `backend/app/models/api_key.py`** — ApiKey ORM 模型（per D-01, D-02, D-04）：

```python
from __future__ import annotations

from datetime import datetime

from sqlalchemy import Boolean, DateTime, ForeignKey, Integer, String
from sqlalchemy.orm import Mapped, mapped_column, relationship

from backend.app.core.database import Base
from backend.app.models.mixins import CreatedAtMixin, UUIDPrimaryKeyMixin, UpdatedAtMixin


class ApiKey(UUIDPrimaryKeyMixin, CreatedAtMixin, UpdatedAtMixin, Base):
    __tablename__ = 'api_keys'

    name: Mapped[str] = mapped_column(String(128), nullable=False)
    key_hash: Mapped[str] = mapped_column(String(64), nullable=False, unique=True, index=True)
    key_prefix: Mapped[str] = mapped_column(String(8), nullable=False)
    is_active: Mapped[bool] = mapped_column(Boolean, nullable=False, default=True)
    expires_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)
    last_used_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)
    last_used_ip: Mapped[str | None] = mapped_column(String(45), nullable=True)
    rate_limit: Mapped[int] = mapped_column(Integer, nullable=False, default=1000)
    created_by: Mapped[str] = mapped_column(ForeignKey('users.id'), nullable=False)

    creator = relationship('User', backref='api_keys')
```

字段说明：
- `key_hash`: SHA-256 hex digest (64 chars)，用于验证 `X-API-Key` header
- `key_prefix`: 明文 Key 的前 8 位，用于管理界面识别
- `is_active`: False = 已撤销
- `rate_limit`: 每小时请求上限，默认 1000（per D-04）
- `last_used_ip`: IPv4 (15 chars) 或 IPv6 (45 chars)

**2. 创建 `backend/app/models/webhook_endpoint.py`** — WebhookEndpoint ORM 模型（per D-13, D-14）：

```python
from __future__ import annotations

from sqlalchemy import Boolean, ForeignKey, JSON, String, Text
from sqlalchemy.orm import Mapped, mapped_column, relationship

from backend.app.core.database import Base
from backend.app.models.mixins import CreatedAtMixin, UUIDPrimaryKeyMixin, UpdatedAtMixin


class WebhookEndpoint(UUIDPrimaryKeyMixin, CreatedAtMixin, UpdatedAtMixin, Base):
    __tablename__ = 'webhook_endpoints'

    url: Mapped[str] = mapped_column(Text, nullable=False)
    secret: Mapped[str] = mapped_column(String(128), nullable=False)
    is_active: Mapped[bool] = mapped_column(Boolean, nullable=False, default=True)
    description: Mapped[str | None] = mapped_column(String(256), nullable=True)
    created_by: Mapped[str] = mapped_column(ForeignKey('users.id'), nullable=False)
    events: Mapped[dict] = mapped_column(JSON, nullable=False, default=list)

    creator = relationship('User', backref='webhook_endpoints')
    delivery_logs = relationship('WebhookDeliveryLog', back_populates='webhook', cascade='all, delete-orphan')
```

字段说明：
- `secret`: HMAC-SHA256 签名密钥（明文存储，仅服务端使用）
- `events`: JSON list，如 `["recommendation.approved"]`

**3. 创建 `backend/app/models/webhook_delivery_log.py`** — WebhookDeliveryLog ORM 模型（per D-14）：

```python
from __future__ import annotations

from sqlalchemy import Boolean, ForeignKey, Integer, JSON, String, Text
from sqlalchemy.orm import Mapped, mapped_column, relationship

from backend.app.core.database import Base
from backend.app.models.mixins import CreatedAtMixin, UUIDPrimaryKeyMixin


class WebhookDeliveryLog(UUIDPrimaryKeyMixin, CreatedAtMixin, Base):
    __tablename__ = 'webhook_delivery_logs'

    webhook_id: Mapped[str] = mapped_column(ForeignKey('webhook_endpoints.id'), nullable=False, index=True)
    event_type: Mapped[str] = mapped_column(String(64), nullable=False)
    payload: Mapped[dict] = mapped_column(JSON, nullable=False)
    response_status: Mapped[int | None] = mapped_column(Integer, nullable=True)
    response_body: Mapped[str | None] = mapped_column(Text, nullable=True)
    attempt: Mapped[int] = mapped_column(Integer, nullable=False, default=1)
    success: Mapped[bool] = mapped_column(Boolean, nullable=False, default=False)
    error_message: Mapped[str | None] = mapped_column(Text, nullable=True)

    webhook = relationship('WebhookEndpoint', back_populates='delivery_logs')
```

**4. 创建 Alembic 迁移** — 使用 `alembic revision --autogenerate -m "add_api_key_webhook_tables"`。

迁移创建 3 张表：`api_keys`、`webhook_endpoints`、`webhook_delivery_logs`。使用 `batch_alter_table` 确保 SQLite 兼容。验证 downgrade 正确 drop 3 张表。

运行 `alembic upgrade head` 确认迁移成功。
  </action>
  <verify>
    <automated>cd D:/wage_adjust && .venv/Scripts/python -m alembic upgrade head 2>&1 | tail -5</automated>
  </verify>
  <done>3 个 ORM 模型文件存在，Alembic 迁移成功执行，api_keys/webhook_endpoints/webhook_delivery_logs 表已创建</done>
</task>

<task type="auto">
  <name>Task 2: Pydantic Schema + RED 测试桩</name>
  <files>
    backend/app/schemas/api_key.py
    backend/app/schemas/webhook.py
    backend/app/schemas/public.py
    backend/tests/test_api/test_public_hardening.py
    backend/tests/test_services/test_api_key_service.py
  </files>
  <read_first>
    backend/app/schemas/public.py
    backend/app/schemas/feishu.py
    backend/tests/test_api/test_attendance.py
  </read_first>
  <action>
**1. 创建 `backend/app/schemas/api_key.py`** — API Key 管理 schema（per D-01, D-02, D-12）：

```python
from __future__ import annotations
from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field


class ApiKeyCreate(BaseModel):
    name: str = Field(..., min_length=1, max_length=128, description='Key 名称')
    rate_limit: int = Field(1000, ge=1, le=100000, description='每小时请求上限')
    expires_at: datetime | None = Field(None, description='可选过期时间')


class ApiKeyRead(BaseModel):
    model_config = ConfigDict(from_attributes=True)

    id: str
    name: str
    key_prefix: str
    is_active: bool
    rate_limit: int
    expires_at: datetime | None
    last_used_at: datetime | None
    last_used_ip: str | None
    created_at: datetime
    updated_at: datetime
    created_by: str


class ApiKeyCreateResponse(BaseModel):
    """创建后一次性返回明文 Key，后续不可再查看（per D-02, D-12）"""
    key: ApiKeyRead
    plain_key: str = Field(..., description='完整 API Key 明文（仅此一次展示）')


class ApiKeyRotateResponse(BaseModel):
    """轮换后返回新 Key 明文"""
    key: ApiKeyRead
    plain_key: str
    old_key_id: str
```

**2. 创建 `backend/app/schemas/webhook.py`** — Webhook 管理 schema（per D-13, D-14）：

```python
from __future__ import annotations
from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, HttpUrl


class WebhookEndpointCreate(BaseModel):
    url: str = Field(..., description='Webhook 回调 URL')
    description: str | None = Field(None, max_length=256)
    events: list[str] = Field(default_factory=lambda: ['recommendation.approved'], description='订阅事件类型')


class WebhookEndpointRead(BaseModel):
    model_config = ConfigDict(from_attributes=True)

    id: str
    url: str
    is_active: bool
    description: str | None
    events: list[str]
    created_by: str
    created_at: datetime
    updated_at: datetime


class WebhookDeliveryLogRead(BaseModel):
    model_config = ConfigDict(from_attributes=True)

    id: str
    webhook_id: str
    event_type: str
    payload: dict
    response_status: int | None
    response_body: str | None
    attempt: int
    success: bool
    error_message: str | None
    created_at: datetime
```

**3. 更新 `backend/app/schemas/public.py`** — 添加游标分页 wrapper（per D-05, D-06）：

在文件末尾追加：

```python
from typing import Generic, TypeVar

T = TypeVar('T')


class CursorPaginatedResponse(BaseModel, Generic[T]):
    """游标分页通用 wrapper（per D-05, D-06）"""
    items: list[T]
    next_cursor: str | None = None
    has_more: bool = False
    total: int | None = None


class PaginatedSalaryResultsResponse(BaseModel):
    """游标分页版调薪结果（替代原 PublicSalaryResultsResponse 用于分页端点）"""
    cycle_id: str
    cycle_name: str
    cycle_status: str
    items: list[PublicSalaryResultItem]
    next_cursor: str | None = None
    has_more: bool = False
    total: int | None = None  # 保留向后兼容，可选
```

注意：保留原有所有 schema 不变，只追加新类。

**4. 创建 `backend/tests/test_api/test_public_hardening.py`** — RED 测试桩，覆盖 API-01 到 API-04：

测试函数签名（全部 `assert False, 'RED: ...'`）：

```python
# API-01: 仅返回 approved 记录
def test_approved_only_filter_excludes_draft():
    """salary-results 端点不返回 status=draft 的记录"""
    assert False, 'RED: approved-only filter not implemented'

def test_approved_only_filter_excludes_pending():
    """salary-results 端点不返回 status=pending 的记录"""
    assert False, 'RED: approved-only filter not implemented'

def test_approved_only_returns_approved():
    """salary-results 端点返回 status=approved 的记录"""
    assert False, 'RED: approved-only filter not implemented'

# API-02: 游标分页
def test_cursor_pagination_first_page():
    """首页请求返回 items + next_cursor + has_more"""
    assert False, 'RED: cursor pagination not implemented'

def test_cursor_pagination_no_duplicates():
    """翻页不丢失不重复"""
    assert False, 'RED: cursor pagination not implemented'

def test_cursor_pagination_max_page_size():
    """page_size > 100 被截断为 100（per D-06）"""
    assert False, 'RED: cursor pagination not implemented'

# API-03: 多 Key 管理
def test_create_api_key_returns_plain_key():
    """创建 Key 返回明文（仅一次）"""
    assert False, 'RED: api key management not implemented'

def test_rotate_api_key():
    """轮换 Key 撤销旧 Key 并返回新 Key"""
    assert False, 'RED: api key rotation not implemented'

def test_revoke_api_key():
    """撤销 Key 设置 is_active=False"""
    assert False, 'RED: api key revocation not implemented'

# API-04: 撤销/过期 Key 立即 401
def test_revoked_key_returns_401():
    """已撤销的 Key 请求返回 401"""
    assert False, 'RED: revoked key check not implemented'

def test_expired_key_returns_401():
    """已过期的 Key 请求返回 401"""
    assert False, 'RED: expired key check not implemented'
```

**5. 创建 `backend/tests/test_services/test_api_key_service.py`** — ApiKeyService RED 测试桩：

```python
# 单元测试桩
def test_create_key_stores_sha256_hash():
    """创建 Key 时存储 SHA-256 hash 而非明文"""
    assert False, 'RED: ApiKeyService.create not implemented'

def test_validate_key_matches_hash():
    """验证时比较 SHA-256 hash"""
    assert False, 'RED: ApiKeyService.validate not implemented'

def test_validate_key_updates_last_used():
    """验证成功后更新 last_used_at 和 last_used_ip"""
    assert False, 'RED: ApiKeyService.validate not implemented'

def test_per_key_rate_limit_uses_key_id():
    """限流使用 key_id 而非 IP"""
    assert False, 'RED: per-key rate limiting not implemented'
```

所有测试函数必须以 `assert False` 结尾以确保 RED 状态。使用 `pytest.mark.xfail(reason='RED: ...')` 可选。
  </action>
  <verify>
    <automated>cd D:/wage_adjust && .venv/Scripts/python -m pytest backend/tests/test_api/test_public_hardening.py backend/tests/test_services/test_api_key_service.py --no-header -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    - api_key.py schema 文件存在且 exports ApiKeyCreate/ApiKeyRead/ApiKeyCreateResponse/ApiKeyRotateResponse
    - webhook.py schema 文件存在且 exports WebhookEndpointCreate/WebhookEndpointRead/WebhookDeliveryLogRead
    - public.py 追加了 CursorPaginatedResponse 和 PaginatedSalaryResultsResponse
    - 15 个 RED 测试桩全部 FAILED（预期行为）
  </done>
</task>

</tasks>

<verification>
1. `alembic upgrade head` 成功执行，3 张新表已创建
2. `python -c "from backend.app.models.api_key import ApiKey; from backend.app.models.webhook_endpoint import WebhookEndpoint; from backend.app.models.webhook_delivery_log import WebhookDeliveryLog; print('OK')"` 输出 OK
3. `python -c "from backend.app.schemas.api_key import ApiKeyCreate, ApiKeyRead, ApiKeyCreateResponse; from backend.app.schemas.webhook import WebhookEndpointCreate, WebhookEndpointRead; from backend.app.schemas.public import CursorPaginatedResponse; print('OK')"` 输出 OK
4. 全部 RED 测试桩运行且 FAILED
</verification>

<success_criteria>
- 3 个 ORM 模型文件存在且可 import
- Alembic 迁移成功，数据库包含 api_keys, webhook_endpoints, webhook_delivery_logs 表
- 3 个 Schema 模块可 import，类型定义完整
- 15 个 RED 测试桩全部 FAILED
</success_criteria>

<output>
After completion, create `.planning/phases/10-external-api-hardening/10-01-SUMMARY.md`
</output>
