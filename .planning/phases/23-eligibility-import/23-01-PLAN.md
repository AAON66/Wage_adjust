---
phase: 23-eligibility-import
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/non_statutory_leave.py
  - backend/app/core/rate_limiter.py
  - backend/app/services/import_service.py
  - backend/app/services/feishu_service.py
  - alembic/versions/xxxx_add_non_statutory_leave.py
autonomous: true
requirements: [ELIGIMP-02, FEISHU-01]

must_haves:
  truths:
    - "NonStatutoryLeave 模型存在且可通过 Alembic 迁移创建表"
    - "ImportService 支持 hire_info 和 non_statutory_leave 两种新导入类型"
    - "InMemoryRateLimiter 提取为共享模块，FeishuService 使用限流器控制请求频率"
    - "FeishuService 支持 4 种资格数据类型的同步方法和字段列表获取"
  artifacts:
    - path: "backend/app/models/non_statutory_leave.py"
      provides: "NonStatutoryLeave ORM 模型"
      contains: "class NonStatutoryLeave"
    - path: "backend/app/core/rate_limiter.py"
      provides: "共享的 InMemoryRateLimiter 类"
      contains: "class InMemoryRateLimiter"
    - path: "backend/app/services/import_service.py"
      provides: "hire_info 和 non_statutory_leave 导入支持"
      contains: "_import_hire_info"
    - path: "backend/app/services/feishu_service.py"
      provides: "限流器集成 + 新同步方法 + 字段列表 API"
      contains: "list_bitable_fields"
  key_links:
    - from: "backend/app/services/import_service.py"
      to: "backend/app/models/non_statutory_leave.py"
      via: "import NonStatutoryLeave for upsert"
      pattern: "from backend.app.models.non_statutory_leave import NonStatutoryLeave"
    - from: "backend/app/services/feishu_service.py"
      to: "backend/app/core/rate_limiter.py"
      via: "import InMemoryRateLimiter"
      pattern: "from backend.app.core.rate_limiter import InMemoryRateLimiter"
---

<objective>
构建 Phase 23 后端数据层基础：新建 NonStatutoryLeave 模型，扩展 ImportService 支持 hire_info 和 non_statutory_leave 两种新 Excel 导入类型，提取 InMemoryRateLimiter 为共享模块，为 FeishuService 添加 RPM 限流和新数据类型同步方法。

Purpose: 为调薪资格统一导入管理页面提供所有后端数据处理能力
Output: NonStatutoryLeave 模型 + ImportService 扩展 + 限流器共享模块 + FeishuService 扩展
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-eligibility-import/23-CONTEXT.md
@.planning/phases/23-eligibility-import/23-RESEARCH.md

<interfaces>
<!-- 现有接口，executor 需要了解 -->

From backend/app/models/mixins.py:
```python
class UUIDPrimaryKeyMixin: ...
class CreatedAtMixin: ...
class UpdatedAtMixin: ...
```

From backend/app/models/performance_record.py (模型模式参考):
```python
class PerformanceRecord(UUIDPrimaryKeyMixin, CreatedAtMixin, UpdatedAtMixin, Base):
    __tablename__ = 'performance_records'
    __table_args__ = (UniqueConstraint('employee_id', 'year', name='uq_performance_employee_year'),)
    employee_id: Mapped[str] = mapped_column(String(36), ForeignKey('employees.id'), nullable=False, index=True)
    employee_no: Mapped[str] = mapped_column(String(64), nullable=False, index=True)
    year: Mapped[int] = mapped_column(Integer, nullable=False, index=True)
    grade: Mapped[str] = mapped_column(String(8), nullable=False)
    source: Mapped[str] = mapped_column(String(32), nullable=False, default='manual')
```

From backend/app/services/llm_service.py (InMemoryRateLimiter 原始位置):
```python
class InMemoryRateLimiter:
    def __init__(self, limit: int, *, window_seconds: int = 60, clock: Callable[[], float] | None = None) -> None:
        self.limit = max(limit, 1)
        self.window_seconds = window_seconds
        self.clock = clock or time.monotonic
        self.events: deque[float] = deque()
    def acquire(self) -> None:
        now = self.clock()
        while self.events and now - self.events[0] >= self.window_seconds:
            self.events.popleft()
        if len(self.events) >= self.limit:
            raise RuntimeError('DeepSeek rate limit reached for the current minute window.')
        self.events.append(now)
```

From backend/app/services/import_service.py:
```python
class ImportService:
    SUPPORTED_TYPES = {'employees', 'certifications', 'performance_grades', 'salary_adjustments'}
    REQUIRED_COLUMNS = { ... }
    COLUMN_ALIASES = { ... }
    def _dispatch_import(self, import_type, dataframe) -> list[dict]: ...
    def _import_performance_grades(self, dataframe) -> list[dict]: ...  # 参考模式
```

From backend/app/services/feishu_service.py:
```python
class FeishuService:
    def _ensure_token(self, app_id, app_secret) -> str: ...
    def _fetch_all_records(self, token, app_token, table_id, field_mapping, since=None) -> list[dict]: ...
    def sync_performance_records(self, *, app_token, table_id, field_mapping=None) -> dict: ...
    def get_config(self) -> FeishuConfig | None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NonStatutoryLeave 模型 + Alembic 迁移 + ImportService 扩展</name>
  <files>
    backend/app/models/non_statutory_leave.py
    backend/app/services/import_service.py
    alembic/versions/xxxx_add_non_statutory_leave.py
  </files>
  <read_first>
    backend/app/models/performance_record.py
    backend/app/models/salary_adjustment_record.py
    backend/app/services/import_service.py
    backend/app/models/mixins.py
  </read_first>
  <behavior>
    - Test 1: ImportService.SUPPORTED_TYPES 包含 'hire_info' 和 'non_statutory_leave'
    - Test 2: _import_hire_info 接受 DataFrame(employee_no, hire_date)，找到 Employee 记录后更新 hire_date，返回 success
    - Test 3: _import_hire_info 对不存在的 employee_no 返回 failed + 错误信息
    - Test 4: _import_non_statutory_leave 接受 DataFrame(employee_no, year, total_days)，创建 NonStatutoryLeave 记录，返回 success
    - Test 5: _import_non_statutory_leave 对重复 (employee_id, year) 执行 upsert（更新 total_days），不报错
    - Test 6: _import_non_statutory_leave 对 leave_type 可选字段正确处理空值
    - Test 7: build_template 和 build_template_xlsx 支持 hire_info 和 non_statutory_leave 类型
  </behavior>
  <action>
    1. **创建 NonStatutoryLeave 模型** (`backend/app/models/non_statutory_leave.py`):
       - 遵循 PerformanceRecord 模式（per D-12）
       - 继承 `UUIDPrimaryKeyMixin, CreatedAtMixin, UpdatedAtMixin, Base`
       - `__tablename__ = 'non_statutory_leaves'`
       - `UniqueConstraint('employee_id', 'year', name='uq_leave_employee_year')`
       - 字段: `employee_id` (String(36), FK employees.id, index), `employee_no` (String(64), index), `year` (Integer, index), `total_days` (Numeric(6,1)), `leave_type` (Optional[str], String(32), nullable, comment='事假/病假/其他'), `source` (String(32), default='manual')
       - 注意: Python 3.9 兼容，使用 `Optional[str]` 而非 `str | None`（项目约定 per Phase 18）
       - 在 `backend/app/models/__init__.py` 中添加导入（如果存在统一注册）

    2. **生成 Alembic 迁移**:
       - 运行 `alembic revision --autogenerate -m "add non_statutory_leaves table"`
       - 验证迁移脚本包含 `create_table('non_statutory_leaves', ...)` 和 unique constraint
       - 运行 `alembic upgrade head` 确认迁移执行无错误

    3. **扩展 ImportService** (`backend/app/services/import_service.py`):
       - 在 `SUPPORTED_TYPES` 中添加 `'hire_info'` 和 `'non_statutory_leave'`
       - 添加 `REQUIRED_COLUMNS`:
         - `'hire_info': ['employee_no', 'hire_date']`
         - `'non_statutory_leave': ['employee_no', 'year', 'total_days']`
       - 添加 `COLUMN_ALIASES`:
         - `'hire_info': {'员工工号': 'employee_no', '入职日期': 'hire_date'}`
         - `'non_statutory_leave': {'员工工号': 'employee_no', '年度': 'year', '假期天数': 'total_days', '假期类型': 'leave_type'}`
       - 添加 `COLUMN_LABELS` 条目: `'hire_date': '入职日期'`, `'total_days': '假期天数'`, `'leave_type': '假期类型'`
       - 在 `_dispatch_import` 中添加分支:
         - `if import_type == 'hire_info': return self._import_hire_info(dataframe)`
         - `if import_type == 'non_statutory_leave': return self._import_non_statutory_leave(dataframe)`
       - **实现 `_import_hire_info`** (per D-11, Pitfall 2):
         - 只需 employee_no + hire_date
         - 查找 Employee by employee_no，不存在则报错 "未找到员工工号"
         - `pd.to_datetime(row['hire_date']).date()` 解析日期
         - 更新 `employee.hire_date = parsed_date`
         - 返回 success 结果，每行用 SAVEPOINT
       - **实现 `_import_non_statutory_leave`**:
         - 查找 Employee by employee_no，不存在则报错
         - 解析 year (int), total_days (float)
         - Upsert on (employee_id, year): 存在则更新 total_days/leave_type/source，不存在则创建
         - source = 'excel'
         - leave_type 可选，空值则为 None
         - 每行用 SAVEPOINT
       - 添加 `_detect_hire_error_column` 和 `_detect_leave_error_column` 方法
       - 在 `build_template` 和 `build_template_xlsx` 中添加 hire_info 和 non_statutory_leave 的模板:
         - hire_info: headers=['员工工号', '入职日期'], example=['EMP-1001', '2024-03-15']
         - non_statutory_leave: headers=['员工工号', '年度', '假期天数', '假期类型'], example=['EMP-1001', '2025', '5.5', '事假']

    4. 添加 `from backend.app.models.non_statutory_leave import NonStatutoryLeave` 到 import_service.py 顶部
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust && python -m pytest backend/tests/ -x -q --timeout=30 && alembic upgrade head && python -c "from backend.app.models.non_statutory_leave import NonStatutoryLeave; print('Model OK')" && python -c "from backend.app.services.import_service import ImportService; assert 'hire_info' in ImportService.SUPPORTED_TYPES; assert 'non_statutory_leave' in ImportService.SUPPORTED_TYPES; print('ImportService OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "hire_info" backend/app/services/import_service.py` 返回 >= 5
    - `grep -c "non_statutory_leave" backend/app/services/import_service.py` 返回 >= 5
    - `grep "class NonStatutoryLeave" backend/app/models/non_statutory_leave.py` 匹配
    - `grep "uq_leave_employee_year" backend/app/models/non_statutory_leave.py` 匹配
    - `grep "_import_hire_info" backend/app/services/import_service.py` 匹配
    - `grep "_import_non_statutory_leave" backend/app/services/import_service.py` 匹配
    - Alembic migration 文件存在且包含 `non_statutory_leaves`
  </acceptance_criteria>
  <done>
    NonStatutoryLeave 模型创建并通过迁移，ImportService 支持 6 种导入类型（原 4 + hire_info + non_statutory_leave），模板下载覆盖新类型
  </done>
</task>

<task type="auto">
  <name>Task 2: InMemoryRateLimiter 提取 + FeishuService 限流集成 + 新同步方法</name>
  <files>
    backend/app/core/rate_limiter.py
    backend/app/services/llm_service.py
    backend/app/services/feishu_service.py
  </files>
  <read_first>
    backend/app/services/llm_service.py
    backend/app/services/feishu_service.py
    backend/app/core/config.py
  </read_first>
  <action>
    1. **提取 InMemoryRateLimiter 到共享模块** (`backend/app/core/rate_limiter.py`):
       - 从 `llm_service.py` 复制 `InMemoryRateLimiter` 类到新文件
       - 修改错误消息为通用的: `'Rate limit reached ({count}/{limit} requests per {window_seconds}s window).'`
       - 添加 `wait_and_acquire` 方法: 当达到限制时 sleep 到最旧事件过期，然后 acquire（用于飞书同步的阻塞等待模式）
       - 在 `llm_service.py` 中改为: `from backend.app.core.rate_limiter import InMemoryRateLimiter`
       - 确保 llm_service.py 的现有行为不变

    2. **FeishuService 限流集成** (per D-08, FEISHU-01):
       - 在 `FeishuService.__init__` 中初始化: `self._rate_limiter = InMemoryRateLimiter(60)` (60 RPM)
       - 在 `_fetch_all_records` 的 `while True` 循环中，每次 `httpx.post` 前调用 `self._rate_limiter.wait_and_acquire()`
       - 添加指数退避重试: 当 httpx 响应 code=99991400 或 HTTP 429 时，等待 `2^attempt` 秒后重试（最多 3 次）
       - 在 `list_bitable_fields` 方法中同样使用 `self._rate_limiter.wait_and_acquire()`

    3. **添加 list_bitable_fields 方法** (per D-07):
       ```python
       def list_bitable_fields(self, *, app_token: str, table_id: str) -> list[dict]:
       ```
       - 获取 config + token（复用 get_config + _ensure_token 模式）
       - 调用 `GET /open-apis/bitable/v1/apps/{app_token}/tables/{table_id}/fields`
       - 分页拉取（page_size=100, page_token），每次请求前 `_rate_limiter.wait_and_acquire()`
       - 返回 `[{'field_id': ..., 'field_name': ..., 'type': ..., 'ui_type': ...}]`

    4. **添加 parse_bitable_url 静态方法**:
       ```python
       @staticmethod
       def parse_bitable_url(url: str) -> tuple:
       ```
       - 支持 2 种 URL 格式:
         - `https://xxx.feishu.cn/base/{app_token}?table={table_id}`
         - `https://xxx.feishu.cn/base/{app_token}/{table_id}`
       - 返回 `(app_token, table_id)` 元组
       - 解析失败 raise `ValueError('无法解析飞书多维表格 URL...')`
       - 注意: Python 3.9 返回类型写 `Tuple[str, str]`（从 typing import）

    5. **添加 sync_salary_adjustments 方法** (复制 sync_performance_records 模式):
       - 默认 field_mapping: `{'员工工号': 'employee_no', '调薪日期': 'adjustment_date', '调薪类型': 'adjustment_type', '调薪金额': 'amount'}`
       - Upsert 逻辑参考 ImportService._import_salary_adjustments

    6. **添加 sync_hire_info 方法**:
       - 默认 field_mapping: `{'员工工号': 'employee_no', '入职日期': 'hire_date'}`
       - 查找 Employee，更新 hire_date

    7. **添加 sync_non_statutory_leave 方法**:
       - 默认 field_mapping: `{'员工工号': 'employee_no', '年度': 'year', '假期天数': 'total_days', '假期类型': 'leave_type'}`
       - Upsert on (employee_id, year)
       - source = 'feishu'

    所有新同步方法返回 `{'synced': int, 'skipped': int, 'failed': int, 'total': int}` 格式。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust && python -c "from backend.app.core.rate_limiter import InMemoryRateLimiter; rl = InMemoryRateLimiter(10); rl.acquire(); print('RateLimiter OK')" && python -c "from backend.app.services.feishu_service import FeishuService; assert hasattr(FeishuService, 'list_bitable_fields'); assert hasattr(FeishuService, 'parse_bitable_url'); assert hasattr(FeishuService, 'sync_salary_adjustments'); assert hasattr(FeishuService, 'sync_hire_info'); assert hasattr(FeishuService, 'sync_non_statutory_leave'); print('FeishuService OK')" && python -c "from backend.app.services.llm_service import InMemoryRateLimiter; print('LLM backward compat OK')" && python -m pytest backend/tests/ -x -q --timeout=30</automated>
  </verify>
  <acceptance_criteria>
    - `grep "class InMemoryRateLimiter" backend/app/core/rate_limiter.py` 匹配
    - `grep "wait_and_acquire" backend/app/core/rate_limiter.py` 匹配
    - `grep "_rate_limiter" backend/app/services/feishu_service.py` 匹配
    - `grep "list_bitable_fields" backend/app/services/feishu_service.py` 匹配
    - `grep "parse_bitable_url" backend/app/services/feishu_service.py` 匹配
    - `grep "sync_salary_adjustments" backend/app/services/feishu_service.py` 匹配
    - `grep "sync_hire_info" backend/app/services/feishu_service.py` 匹配
    - `grep "sync_non_statutory_leave" backend/app/services/feishu_service.py` 匹配
    - `grep "from backend.app.core.rate_limiter import" backend/app/services/llm_service.py` 匹配
    - 所有现有 pytest 通过
  </acceptance_criteria>
  <done>
    InMemoryRateLimiter 提取为共享模块且 LlmService 保持向后兼容，FeishuService 集成 60 RPM 限流 + 指数退避，支持 4 种资格数据类型的飞书同步 + 字段列表获取 + URL 解析
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Excel 文件 → ImportService | 不可信用户上传文件，含潜在恶意内容 |
| 飞书 API → FeishuService | 外部 API 返回数据需校验 |
| 前端 URL 输入 → parse_bitable_url | 用户输入的 URL 可能包含恶意内容 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering | ImportService._import_hire_info | mitigate | per-row SAVEPOINT + employee_no 存在性校验，防止篡改非自有数据 |
| T-23-02 | Denial of Service | ImportService | mitigate | MAX_ROWS=5000 限制已有，新类型同样受限 |
| T-23-03 | Denial of Service | FeishuService._fetch_all_records | mitigate | InMemoryRateLimiter 60 RPM 限制 + Celery task time_limit |
| T-23-04 | Spoofing | parse_bitable_url | mitigate | 正则只匹配 feishu.cn 域名，拒绝其他域名 URL |
| T-23-05 | Information Disclosure | FeishuService | mitigate | app_secret 通过 AES-256-GCM 加密存储（已有），API 返回 masked |
</threat_model>

<verification>
1. `python -m pytest backend/tests/ -x -q` 全部通过
2. `alembic upgrade head` 无错误
3. NonStatutoryLeave 表在数据库中存在
4. ImportService.SUPPORTED_TYPES 包含 6 种类型
5. FeishuService 有限流器且 _fetch_all_records 每次请求前 acquire
</verification>

<success_criteria>
- NonStatutoryLeave 模型完整创建，Alembic 迁移可执行
- ImportService 支持 hire_info 和 non_statutory_leave 的 Excel 导入
- InMemoryRateLimiter 在 core/rate_limiter.py 共享，LlmService 和 FeishuService 均使用
- FeishuService 新增 4 种资格数据同步方法 + list_bitable_fields + parse_bitable_url
- 所有现有测试通过，无回归
</success_criteria>

<output>
After completion, create `.planning/phases/23-eligibility-import/23-01-SUMMARY.md`
</output>
