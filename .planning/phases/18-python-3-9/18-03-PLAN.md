---
phase: 18-python-3-9
plan: 03
type: execute
wave: 2
depends_on: ["18-01", "18-02"]
files_modified: []
autonomous: true
requirements:
  - DEPLOY-01
  - DEPLOY-02
  - DEPLOY-05

must_haves:
  truths:
    - "uvicorn backend.app.main:app 可在 Python 3.9 下正常启动，无 ImportError 或 SyntaxError"
    - "pytest 测试套件全部通过"
    - "pandas 批量导入功能在 numpy 2.0.2 下正常工作"
    - "图片解析功能在 Pillow 10.4.0 下正常工作"
    - "SQLite cascade delete 操作实际触发级联删除"
  artifacts: []
  key_links:
    - from: "backend/app/main.py"
      to: "all models and schemas"
      via: "import chain at startup"
      pattern: "Application startup complete"
---

<objective>
验证 Plan 01 和 Plan 02 的类型降级、依赖锁定和 SQLite FK 修复后，整个应用可正常启动并通过测试。

Purpose: 确保所有降级变更没有引入回归，系统在 Python 3.9 环境下完全可用
Output: 验证通过的确认，或发现的问题清单
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-python-3-9/18-CONTEXT.md
@.planning/phases/18-python-3-9/18-01-SUMMARY.md
@.planning/phases/18-python-3-9/18-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 全量导入验证与应用启动测试</name>
  <files></files>
  <read_first>
    backend/app/main.py,
    backend/app/models/__init__.py,
    backend/app/schemas/__init__.py
  </read_first>
  <action>
执行以下验证步骤，确认 Plan 01 和 Plan 02 的变更不引入回归：

**步骤 1：全量模块导入验证**
```bash
python -c "
from backend.app.main import create_app
app = create_app()
print('App created successfully')
"
```
这会触发所有 model 和 schema 的导入链。如果任何文件有未降级的 PEP 604/585 运行时语法，会在此步骤报 TypeError。

**步骤 2：SQLite FK 验证**
```bash
python -c "
from backend.app.core.database import engine
conn = engine.raw_connection()
cursor = conn.cursor()
cursor.execute('PRAGMA foreign_keys')
result = cursor.fetchone()[0]
assert result == 1, f'FK not enabled: {result}'
print('SQLite FK enabled: OK')
cursor.close()
conn.close()
"
```

**步骤 3：依赖版本验证**
```bash
python -c "
import numpy; assert numpy.__version__ == '2.0.2', f'numpy version: {numpy.__version__}'
import PIL; assert PIL.__version__ == '10.4.0', f'Pillow version: {PIL.__version__}'
print(f'numpy=={numpy.__version__}, Pillow=={PIL.__version__}')
"
```
注意：如果当前 venv 中未安装降级版本，需先执行 `pip install numpy==2.0.2 pillow==10.4.0`。

**步骤 4：运行 pytest 测试套件**
```bash
cd /Users/mac/PycharmProjects/Wage_adjust && python -m pytest backend/tests/ -x -q
```
所有测试必须通过。如果有因 numpy API 差异导致的测试失败（STATE.md 提到的 numpy 2.0.2 vs 2.2.1 API differences），需要修复相关代码。

**步骤 5：关键功能快速验证**
```bash
# 验证 pandas 导入功能（numpy 2.0.2 兼容性）
python -c "
import pandas as pd
import numpy as np
df = pd.DataFrame({'a': [1,2,3], 'b': ['x','y','z']})
print(f'pandas {pd.__version__} + numpy {np.__version__}: DataFrame OK')
"

# 验证 Pillow 图片操作（10.4.0 兼容性）
python -c "
from PIL import Image
import io
img = Image.new('RGB', (100, 100), color='red')
buf = io.BytesIO()
img.save(buf, format='PNG')
print(f'Pillow image create+save: OK ({len(buf.getvalue())} bytes)')
"
```

如果发现问题，修复相关文件并记录修复内容。常见问题：
- numpy 2.0.2 中移除了某些 deprecated API（如 `np.bool` → `np.bool_`）
- Pillow 10.4.0 中某些 11.0 新增 API 不可用
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust && python -c "from backend.app.main import create_app; create_app(); print('OK')" && python -m pytest backend/tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from backend.app.main import create_app; create_app()"` 退出码为 0
    - `python -m pytest backend/tests/ -x -q` 退出码为 0，无 FAILED 测试
    - `python -c "from backend.app.core.database import engine; c=engine.raw_connection(); cur=c.cursor(); cur.execute('PRAGMA foreign_keys'); print(cur.fetchone()[0]); cur.close(); c.close()"` 输出 1
    - `python -c "import numpy; print(numpy.__version__)"` 输出 2.0.2
    - `python -c "import PIL; print(PIL.__version__)"` 输出 10.4.0
  </acceptance_criteria>
  <done>应用完整导入链无错误，pytest 全部通过，SQLite FK 启用，numpy 2.0.2 和 Pillow 10.4.0 功能正常</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无新增信任边界 | 本计划仅做验证，不引入新代码 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06 | Elevation of Privilege | numpy 2.0.2 | accept | numpy 2.0.2 是 2024 年 6 月发布的稳定版本，无已知高危 CVE |
</threat_model>

<verification>
1. `python -c "from backend.app.main import create_app; create_app()"` 成功
2. `python -m pytest backend/tests/ -x -q` 全部通过
3. SQLite PRAGMA foreign_keys 返回 1
4. numpy.__version__ == '2.0.2'
5. PIL.__version__ == '10.4.0'
</verification>

<success_criteria>
- 应用可正常启动，无 ImportError、SyntaxError、TypeError
- 现有 pytest 测试套件全部通过
- SQLite cascade delete 通过 FK 约束正常工作
- pandas + numpy 2.0.2 批量操作正常
- Pillow 10.4.0 图片操作正常
</success_criteria>

<output>
After completion, create `.planning/phases/18-python-3-9/18-03-SUMMARY.md`
</output>
