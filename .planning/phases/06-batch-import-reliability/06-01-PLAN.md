---
phase: 06-batch-import-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/import_service.py
  - backend/app/api/v1/imports.py
  - backend/app/schemas/import_job.py
  - requirements.txt
autonomous: true
requirements: [IMP-01, IMP-02, IMP-03, IMP-04, IMP-05]

must_haves:
  truths:
    - "包含 10 条无效行和 90 条有效行的 CSV 导入后，90 条有效行被提交，10 条错误被收集返回"
    - "导入同一工号员工文件两次，第二次更新已有记录而非创建重复"
    - "导入响应包含 total_rows、success_rows、failed_rows 和逐行错误"
    - "上传 xlsx 文件不再被拒绝，可正常解析导入"
    - "上传 GBK 编码的 CSV 文件不出现乱码"
    - "上传超过 5000 行的文件被拒绝并返回明确提示"
    - "部分失败时 API 返回 HTTP 207 而非 201"
    - "员工更新操作写入审计日志"
  artifacts:
    - path: "backend/app/services/import_service.py"
      provides: "SAVEPOINT 逐行处理、xlsx 读取、5000 行限制、审计日志、xlsx 模板和错误报告"
      contains: "begin_nested"
    - path: "backend/app/api/v1/imports.py"
      provides: "HTTP 207 Multi-Status 条件响应"
      contains: "JSONResponse"
    - path: "backend/app/schemas/import_job.py"
      provides: "ImportJobRead schema（无需改动，已包含所有字段）"
  key_links:
    - from: "backend/app/services/import_service.py"
      to: "SQLAlchemy Session"
      via: "db.begin_nested() SAVEPOINT"
      pattern: "begin_nested"
    - from: "backend/app/api/v1/imports.py"
      to: "backend/app/services/import_service.py"
      via: "ImportService.run_import() 返回 ImportJob"
      pattern: "JSONResponse.*status_code=207"
    - from: "backend/app/services/import_service.py"
      to: "backend/app/models/audit_log.py"
      via: "AuditLog 写入（员工 upsert 更新时）"
      pattern: "AuditLog"
---

<objective>
改造后端批量导入服务，实现逐行 SAVEPOINT 部分成功提交、xlsx 文件读写支持、5000 行限制校验、员工 upsert 审计日志、HTTP 207 Multi-Status 响应。

Purpose: 让批量导入从"全成功或全失败"变为"部分成功 + 逐行错误报告"模式，满足 HR 实际使用场景。
Output: 改造后的 import_service.py、imports.py API 层、xlsx 模板和错误报告生成能力。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-batch-import-reliability/06-CONTEXT.md
@.planning/phases/06-batch-import-reliability/06-RESEARCH.md

<interfaces>
<!-- 执行器需要的关键类型和接口 -->

From backend/app/models/audit_log.py:
```python
class AuditLog(UUIDPrimaryKeyMixin, CreatedAtMixin, Base):
    __tablename__ = "audit_logs"
    operator_id: Mapped[str | None]  # ForeignKey("users.id")
    action: Mapped[str]              # e.g. 'employee_import_update'
    target_type: Mapped[str]         # e.g. 'employee'
    target_id: Mapped[str]           # employee.id
    detail: Mapped[dict]             # JSON with old_value/new_value
    operator_role: Mapped[str | None]
    request_id: Mapped[str | None]
```

From backend/app/models/import_job.py:
```python
class ImportJob(UUIDPrimaryKeyMixin, CreatedAtMixin, Base):
    __tablename__ = "import_jobs"
    file_name: Mapped[str]
    import_type: Mapped[str]
    status: Mapped[str]          # 'pending'|'processing'|'completed'|'failed'|'partial'
    total_rows: Mapped[int]
    success_rows: Mapped[int]
    failed_rows: Mapped[int]
    result_summary: Mapped[dict]  # JSON: {rows: [...], supported_types: [...]}
```

From backend/app/schemas/import_job.py:
```python
class ImportJobRead(BaseModel):
    model_config = ConfigDict(from_attributes=True)
    id: str
    file_name: str
    import_type: str
    status: str
    total_rows: int
    success_rows: int
    failed_rows: int
    result_summary: dict
    created_at: datetime
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ImportService 核心改造 — SAVEPOINT、xlsx、5000 行、审计日志、xlsx 模板/报告</name>
  <files>backend/app/services/import_service.py, requirements.txt</files>
  <read_first>
    - backend/app/services/import_service.py（当前完整实现）
    - backend/app/models/audit_log.py（AuditLog 模型字段）
    - backend/app/models/employee.py（Employee 模型字段）
    - backend/app/models/import_job.py（ImportJob 模型字段）
    - backend/app/services/identity_binding_service.py（auto_bind 签名）
    - requirements.txt（当前依赖列表）
  </read_first>
  <action>
对 `backend/app/services/import_service.py` 进行以下改造（按 D-01 至 D-08）：

**1. 添加导入（文件顶部）：**
```python
from backend.app.models.audit_log import AuditLog
```
以及在方法内部使用时：
```python
from openpyxl import Workbook
from openpyxl.styles import Font, PatternFill, Alignment
```

**2. 类常量：**
```python
MAX_ROWS = 5000  # D-06: 单次导入最大行数
```

**3. `_load_table` 方法改造（IMP-04）：**
将现有 xlsx/xls 分支从 `raise ValueError` 改为：
```python
if suffix in {'xlsx', 'xls'}:
    return pd.read_excel(io.BytesIO(raw_bytes), engine='openpyxl').fillna('')
```
保留 CSV 编码回退链不变（D-05 已实现）。

**4. `run_import` 方法添加 5000 行限制（D-06）：**
在 `dataframe = self._load_table(...)` 之后、`self._dispatch_import(...)` 之前插入：
```python
if len(dataframe) > self.MAX_ROWS:
    raise ValueError(f'单次导入不能超过 {self.MAX_ROWS} 行，请分批导入。')
```

**5. `_import_employees` 改造为 SAVEPOINT 模式（IMP-01, IMP-02）：**
将整个逐行循环体用 `try: with self.db.begin_nested():` 包裹。当前代码中 `self.db.add(employee)` 和 `self.db.flush()` 保留在 `begin_nested()` 块内。如果任何异常发生，`begin_nested()` 的 `__exit__` 会自动 rollback 到 SAVEPOINT。
- 第一遍循环：每行用 `try: with self.db.begin_nested():` 包裹
- 第二遍 manager 绑定循环：每个 manager 绑定也用 `try: with self.db.begin_nested():` 包裹
- 循环结束后调用 `self.db.commit()` 提交所有成功的行

**6. 员工 upsert 审计日志（D-03, IMP-05）：**
在 `_import_employees` 中，当检测到 `employee is not None`（即更新而非新建）时，在更新字段前记录旧值：
```python
if employee is not None:
    old_values = {
        'name': employee.name,
        'department': employee.department,
        'job_family': employee.job_family,
        'job_level': employee.job_level,
        'status': employee.status,
    }
    # ... 更新字段 ...
    audit_entry = AuditLog(
        operator_id=None,  # 批量导入无特定操作人，由 API 层注入
        action='employee_import_update',
        target_type='employee',
        target_id=employee.id,
        detail={'old_value': old_values, 'new_value': {
            'name': name, 'department': department,
            'job_family': job_family, 'job_level': job_level, 'status': status,
        }},
    )
    self.db.add(audit_entry)
```
注意：`AuditLog` 的 `operator_id` 在服务层设为 None，因为 ImportService 当前不接收 current_user。为了支持审计，给 `__init__` 添加可选参数 `operator_id: str | None = None` 和 `operator_role: str | None = None`，在创建 AuditLog 时使用。API 层在构造 ImportService 时传入。

**7. `_import_certifications` 改造为 SAVEPOINT 模式（IMP-01, IMP-02）：**
同员工导入，将逐行循环体用 `try: with self.db.begin_nested():` 包裹。循环结束后 `self.db.commit()`。

**8. `run_import` 中 status 字段扩展：**
当 `failed_rows > 0 and success_rows > 0` 时，`job.status = 'partial'`（部分成功）。
当 `failed_rows > 0 and success_rows == 0` 时，`job.status = 'failed'`。
当 `failed_rows == 0` 时，`job.status = 'completed'`。

**9. `build_template` 方法扩展为支持 xlsx（IMP-06, D-04）：**
添加新方法 `build_template_xlsx(self, import_type: str) -> tuple[str, bytes, str]`：
- 使用 openpyxl 创建 Workbook
- 表头行加粗、蓝底白字（`PatternFill(start_color='4472C4', end_color='4472C4', fill_type='solid')`，`Font(bold=True, color='FFFFFF')`）
- 列宽设为 20
- 添加与 CSV 模板相同的示例行
- 返回 `('{import_type}_template.xlsx', bytes, 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet')`

**10. `build_export_report` 方法扩展为 xlsx（D-02）：**
添加新方法 `build_export_report_xlsx(self, job: ImportJob) -> tuple[str, bytes, str]`：
- 使用 openpyxl 创建 Workbook，sheet 标题 '导入结果报告'
- 表头：行号、结果、错误字段、错误原因
- 仅输出 status='failed' 的行
- 返回 xlsx 格式

**11. requirements.txt 添加 openpyxl：**
在 requirements.txt 中添加 `openpyxl==3.1.5`（如果尚未存在）。

**错误行数据结构改进（IMP-03）：**
在 `_import_employees` 和 `_import_certifications` 中，错误行结果增加 `error_column` 字段。例如必填字段为空时：
```python
results.append({
    'row_index': int(index) + 1,
    'status': 'failed',
    'message': '员工工号、员工姓名、所属部门、岗位族、岗位级别不能为空。',
    'error_column': '必填字段',
})
```
部门不存在时 `error_column` 为 `'所属部门'`，身份证号冲突时为 `'身份证号'`，日期格式错误时为 `'发证时间/到期时间/补贴比例'`。
  </action>
  <verify>
    <automated>cd D:/wage_adjust && python -c "from backend.app.services.import_service import ImportService; print('ImportService imports OK')" && python -c "from openpyxl import Workbook; print('openpyxl OK')" && grep -q "openpyxl" requirements.txt && echo "requirements.txt OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "begin_nested" backend/app/services/import_service.py` 返回 >= 3（员工第一遍、员工第二遍 manager 绑定、认证导入）
    - `grep -c "AuditLog" backend/app/services/import_service.py` 返回 >= 2（import 和使用）
    - `grep -q "MAX_ROWS = 5000" backend/app/services/import_service.py` 成功
    - `grep -q "pd.read_excel" backend/app/services/import_service.py` 成功
    - `grep -q "build_template_xlsx" backend/app/services/import_service.py` 成功
    - `grep -q "build_export_report_xlsx" backend/app/services/import_service.py` 成功
    - `grep -q "openpyxl" requirements.txt` 成功
    - `grep -q "status = 'partial'" backend/app/services/import_service.py` 或 `grep -q "'partial'" backend/app/services/import_service.py` 成功
    - `grep -q "error_column" backend/app/services/import_service.py` 成功
    - `grep -q "operator_id" backend/app/services/import_service.py` 成功（__init__ 参数）
    - `python -c "from backend.app.services.import_service import ImportService"` 无报错
  </acceptance_criteria>
  <done>ImportService 完成 SAVEPOINT 逐行处理、xlsx 读取、5000 行限制、员工 upsert 审计日志、xlsx 模板生成、xlsx 错误报告生成。所有异常被 try/except 捕获不冒泡。</done>
</task>

<task type="auto">
  <name>Task 2: API 层 HTTP 207 响应 + 模板/报告端点扩展</name>
  <files>backend/app/api/v1/imports.py, backend/app/schemas/import_job.py</files>
  <read_first>
    - backend/app/api/v1/imports.py（当前 API 路由）
    - backend/app/schemas/import_job.py（当前 schema）
    - backend/app/services/import_service.py（改造后的版本，Task 1 产出）
    - backend/app/dependencies.py（get_current_user 返回类型）
  </read_first>
  <action>
**1. `create_import_job` 端点改造（IMP-02, IMP-03）：**
在 `backend/app/api/v1/imports.py` 中：
- 添加导入：`from fastapi.responses import JSONResponse`
- 修改 `create_import_job` 函数：
  - 移除 `response_model=ImportJobRead, status_code=status.HTTP_201_CREATED`（因为 status_code 变为动态）
  - 将 `_: object = Depends(require_roles(...))` 改为 `current_user = Depends(require_roles('admin', 'hrbp', 'manager'))`
  - 构造 ImportService 时传入 `operator_id=current_user.id, operator_role=current_user.role`
  - 返回时根据 `job.failed_rows` 决定状态码：
    ```python
    data = ImportJobRead.model_validate(job).model_dump(mode='json')
    if job.failed_rows > 0:
        return JSONResponse(content=data, status_code=207)
    return JSONResponse(content=data, status_code=201)
    ```

**2. 模板下载端点扩展（IMP-06, D-04）：**
修改 `download_template` 端点，添加 `format` query 参数：
```python
@router.get('/templates/{import_type}')
def download_template(
    import_type: str,
    format: str = 'xlsx',  # 'xlsx' or 'csv'
    db: Session = Depends(get_db),
    _: object = Depends(get_current_user),
):
    service = ImportService(db)
    try:
        if format == 'xlsx':
            file_name, content, media_type = service.build_template_xlsx(import_type)
        else:
            file_name, content, media_type = service.build_template(import_type)
    except ValueError as exc:
        raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail=str(exc)) from exc
    return StreamingResponse(
        iter([content]),
        media_type=media_type,
        headers={'Content-Disposition': f'attachment; filename={file_name}'},
    )
```

**3. 错误报告导出端点扩展（D-02）：**
修改 `export_import_job` 端点，默认导出 xlsx 格式：
```python
@router.get('/jobs/{job_id}/export')
def export_import_job(
    job_id: str,
    format: str = 'xlsx',
    db: Session = Depends(get_db),
    _: object = Depends(get_current_user),
):
    service = ImportService(db)
    job = service.get_job(job_id)
    if job is None:
        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail='Import job not found.')
    if format == 'xlsx':
        file_name, content, media_type = service.build_export_report_xlsx(job)
    else:
        file_name, content, media_type = service.build_export_report(job)
    return StreamingResponse(
        iter([content]),
        media_type=media_type,
        headers={'Content-Disposition': f'attachment; filename={file_name}'},
    )
```

**4. ImportJobRead.result_summary 类型不变（dict 已足够）。** 无需改动 schema 文件，但确认 `result_summary` 的 `rows` 子元素现在包含 `error_column` 字段。如需要可在 schema 中添加注释说明。
  </action>
  <verify>
    <automated>cd D:/wage_adjust && python -c "from backend.app.api.v1.imports import router; print('API imports OK')" && grep -q "JSONResponse" backend/app/api/v1/imports.py && grep -q "status_code=207" backend/app/api/v1/imports.py && echo "207 OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "JSONResponse" backend/app/api/v1/imports.py` 成功
    - `grep -q "status_code=207" backend/app/api/v1/imports.py` 成功
    - `grep -q "format.*xlsx" backend/app/api/v1/imports.py` 成功（模板端点 format 参数）
    - `grep -q "build_template_xlsx" backend/app/api/v1/imports.py` 成功
    - `grep -q "build_export_report_xlsx" backend/app/api/v1/imports.py` 成功
    - `grep -q "operator_id" backend/app/api/v1/imports.py` 成功（传入 ImportService）
    - `python -c "from backend.app.api.v1.imports import router"` 无报错
  </acceptance_criteria>
  <done>API 层在部分失败时返回 HTTP 207，模板下载支持 xlsx/csv 双格式参数，错误报告默认导出 xlsx。</done>
</task>

</tasks>

<verification>
```bash
# 确认后端可启动
cd D:/wage_adjust && python -c "from backend.app.main import create_app; app = create_app(); print('App created OK')"

# 确认关键改造点
grep -c "begin_nested" backend/app/services/import_service.py  # >= 3
grep -q "MAX_ROWS = 5000" backend/app/services/import_service.py
grep -q "status_code=207" backend/app/api/v1/imports.py
grep -q "openpyxl" requirements.txt
```
</verification>

<success_criteria>
- ImportService 使用 SAVEPOINT 逐行处理，单行失败不影响其他行
- xlsx 文件可正常读取导入
- 超过 5000 行的文件被拒绝
- 员工 upsert 更新时写入 AuditLog
- API 部分失败返回 HTTP 207
- 模板下载支持 xlsx 和 csv 双格式
- 错误报告支持 xlsx 格式导出
- 后端服务可正常启动
</success_criteria>

<output>
After completion, create `.planning/phases/06-batch-import-reliability/06-01-SUMMARY.md`
</output>
