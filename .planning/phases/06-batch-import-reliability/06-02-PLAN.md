---
phase: 06-batch-import-reliability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_services/test_import_partial_success.py
  - backend/tests/test_api/test_import_207.py
  - backend/tests/test_services/test_import_xlsx.py
  - backend/tests/test_services/test_import_upsert_audit.py
  - backend/tests/test_services/test_import_certification.py
  - backend/tests/fixtures/import_gbk.csv
  - backend/tests/fixtures/import_utf8bom.csv
  - backend/tests/fixtures/import_valid_employees.csv
  - backend/tests/fixtures/import_mixed_employees.csv
autonomous: true
requirements: [IMP-01, IMP-02, IMP-03, IMP-04, IMP-05, IMP-06]

must_haves:
  truths:
    - "所有 6 个 IMP 需求都有对应的行为测试用例，而非空桩"
    - "测试使用真实夹具文件（GBK CSV、UTF-8 BOM CSV），不仅是内存构造"
    - "SAVEPOINT 部分提交测试直接断言 DB 中的行数，不仅断言响应"
    - "upsert 幂等测试断言 Employee 表无重复行且 AuditLog 有记录"
    - "认证导入有独立测试覆盖：缺失员工引用、重复处理"
    - "API 集成测试覆盖 207/201 状态码和响应结构"
    - "测试可通过 pytest 独立运行"
  artifacts:
    - path: "backend/tests/test_services/test_import_partial_success.py"
      provides: "IMP-01 惰性验证 + IMP-02 SAVEPOINT 部分提交的行为测试"
      min_lines: 80
    - path: "backend/tests/test_api/test_import_207.py"
      provides: "IMP-02 HTTP 207 + IMP-03 响应结构的 API 集成测试"
      min_lines: 80
    - path: "backend/tests/test_services/test_import_xlsx.py"
      provides: "IMP-04 xlsx/GBK 编码 + IMP-06 xlsx 模板生成的行为测试"
      min_lines: 60
    - path: "backend/tests/test_services/test_import_upsert_audit.py"
      provides: "IMP-05 upsert 幂等 + 审计日志的 DB 状态断言测试"
      min_lines: 60
    - path: "backend/tests/test_services/test_import_certification.py"
      provides: "认证导入 SAVEPOINT + 缺失员工引用 + 重复处理测试"
      min_lines: 50
    - path: "backend/tests/fixtures/import_gbk.csv"
      provides: "GBK 编码的中文员工 CSV 夹具文件"
    - path: "backend/tests/fixtures/import_utf8bom.csv"
      provides: "UTF-8 BOM 编码的员工 CSV 夹具文件"
  key_links:
    - from: "test files"
      to: "backend/app/services/import_service.py"
      via: "ImportService 直接实例化 + DB 状态断言"
      pattern: "ImportService"
    - from: "test files"
      to: "backend/app/models/employee.py"
      via: "直接查询 Employee 表验证行数和字段值"
      pattern: "select.*Employee"
---

<objective>
为批量导入的全部 6 个 IMP 需求创建完整的行为测试和集成测试，使用真实夹具文件、DB 状态断言和 API 集成验证。覆盖员工导入和认证导入双路径。

Purpose: 提供可靠的自动化验证基线，确保 SAVEPOINT 部分成功、编码兼容、upsert 幂等等核心行为可被客观检验。解决评审指出的"RED 测试桩过于薄弱"问题。
Output: 5 个测试文件 + 4 个夹具文件，覆盖所有 IMP 需求，包含真实行为断言。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-batch-import-reliability/06-CONTEXT.md
@.planning/phases/06-batch-import-reliability/06-RESEARCH.md

<interfaces>
<!-- 已有测试模式参考 -->

From backend/tests/conftest.py — fixture 模式：
```python
# 使用 SQLAlchemy Session + SQLite 内存数据库（StaticPool）
# db fixture 提供 Session 实例
# 参考 backend/tests/conftest.py 中的 db fixture
```

From backend/app/services/import_service.py — 关键方法签名（改造后）：
```python
class ImportService:
    MAX_ROWS = 5000
    def __init__(self, db: Session, *, operator_id: str | None = None, operator_role: str | None = None): ...
    def run_import(self, *, import_type: str, upload: UploadFile) -> ImportJob: ...
    def build_template(self, import_type: str) -> tuple[str, bytes, str]: ...
    def build_template_xlsx(self, import_type: str) -> tuple[str, bytes, str]: ...
    def build_export_report(self, job: ImportJob) -> tuple[str, bytes, str]: ...
    def build_export_report_xlsx(self, job: ImportJob) -> tuple[str, bytes, str]: ...
```

From backend/app/models/employee.py:
```python
class Employee(UUIDPrimaryKeyMixin, CreatedAtMixin, UpdatedAtMixin, Base):
    employee_no: Mapped[str] = mapped_column(String(64), nullable=False, unique=True, index=True)
    name: Mapped[str]
    department: Mapped[str]
    job_family: Mapped[str | None]
    job_level: Mapped[str | None]
    status: Mapped[str]
```

From backend/app/models/certification.py:
```python
class Certification(UUIDPrimaryKeyMixin, CreatedAtMixin, Base):
    employee_id: Mapped[str]           # FK -> employees.id
    certification_type: Mapped[str]
    certification_stage: Mapped[str | None]
    bonus_rate: Mapped[float]
    issued_at: Mapped[datetime]
    expires_at: Mapped[datetime | None]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 夹具文件 + SAVEPOINT 部分成功行为测试 + API 集成测试（IMP-01, IMP-02, IMP-03）</name>
  <files>
    backend/tests/fixtures/import_gbk.csv,
    backend/tests/fixtures/import_utf8bom.csv,
    backend/tests/fixtures/import_valid_employees.csv,
    backend/tests/fixtures/import_mixed_employees.csv,
    backend/tests/test_services/test_import_partial_success.py,
    backend/tests/test_api/test_import_207.py
  </files>
  <read_first>
    - backend/tests/test_services/test_import_service.py（现有测试模式、fixture 用法）
    - backend/tests/test_services/test_import_idempotency.py（现有幂等测试模式）
    - backend/tests/conftest.py（db fixture 定义、TestClient 模式）
    - backend/app/services/import_service.py（改造后的签名和行为）
    - backend/app/api/v1/imports.py（改造后的 API）
    - backend/app/models/employee.py（Employee 字段）
    - backend/app/models/department.py（Department 字段，导入依赖部门存在）
  </read_first>
  <action>
**Step 0: 创建夹具文件目录和文件**

创建 `backend/tests/fixtures/` 目录（如不存在）。

**`backend/tests/fixtures/import_valid_employees.csv`** — UTF-8 编码，5 行全部有效：
```csv
员工工号,员工姓名,所属部门,岗位族,岗位级别,在职状态,身份证号
EMP-001,张三,产品技术中心,技术,P6,active,
EMP-002,李四,产品技术中心,技术,P7,active,
EMP-003,王五,产品技术中心,产品,P5,active,
EMP-004,赵六,产品技术中心,设计,P4,active,
EMP-005,孙七,产品技术中心,技术,P8,active,
```

**`backend/tests/fixtures/import_mixed_employees.csv`** — UTF-8 编码，3 行有效 + 2 行无效（部门不存在）：
```csv
员工工号,员工姓名,所属部门,岗位族,岗位级别,在职状态,身份证号
EMP-101,张混合一,产品技术中心,技术,P6,active,
EMP-102,李混合二,不存在的部门,技术,P7,active,
EMP-103,王混合三,产品技术中心,产品,P5,active,
EMP-104,赵混合四,另一个不存在部门,设计,P4,active,
EMP-105,孙混合五,产品技术中心,技术,P8,active,
```

**`backend/tests/fixtures/import_gbk.csv`** — GBK 编码，3 行有效员工数据（用 Python codecs 写入 GBK 编码）：
必须使用代码生成此文件，因为它需要真正的 GBK 编码。在测试的 conftest 或 fixture 函数中生成：
```python
@pytest.fixture
def gbk_csv_path(tmp_path):
    content = '员工工号,员工姓名,所属部门,岗位族,岗位级别,在职状态,身份证号\nEMP-G01,张国标,产品技术中心,技术,P6,active,\nEMP-G02,李国标,产品技术中心,产品,P5,active,\n'
    p = tmp_path / 'gbk_employees.csv'
    p.write_bytes(content.encode('gbk'))
    return p
```

**`backend/tests/fixtures/import_utf8bom.csv`** — UTF-8 BOM 编码，2 行有效：
同样在 fixture 中生成，写入时添加 BOM 头 `b'\xef\xbb\xbf'`。

**文件 1: `backend/tests/test_services/test_import_partial_success.py`**

创建完整的行为测试（不是空桩），每个 test 方法都有实际的断言逻辑：

```python
from __future__ import annotations
import io
import pytest
from unittest.mock import MagicMock
from sqlalchemy import select, func
from backend.app.services.import_service import ImportService
from backend.app.models.employee import Employee
from backend.app.models.department import Department


def _make_upload(filename: str, content: bytes) -> MagicMock:
    """构造 UploadFile mock。"""
    upload = MagicMock()
    upload.filename = filename
    upload.file = io.BytesIO(content)
    upload.file.read = lambda: content
    return upload


def _ensure_department(db, name: str = '产品技术中心') -> None:
    """确保测试用部门存在。"""
    existing = db.scalar(select(Department).where(Department.name == name))
    if existing is None:
        db.add(Department(name=name, status='active'))
        db.commit()


class TestLazyValidation:
    """IMP-01: 惰性验证——收集所有行级错误后一次性返回，不在第一个错误时中断。"""

    def test_all_row_errors_collected_not_stopped_at_first(self, db):
        """准备包含 3 行全部无效的 CSV（部门不存在），验证所有 3 行都出现在 results 中。"""
        csv_content = (
            '员工工号,员工姓名,所属部门,岗位族,岗位级别,在职状态,身份证号\n'
            'BAD-001,张一,不存在部门A,技术,P6,active,\n'
            'BAD-002,李二,不存在部门B,技术,P7,active,\n'
            'BAD-003,王三,不存在部门C,产品,P5,active,\n'
        ).encode('utf-8')
        upload = _make_upload('bad.csv', csv_content)
        service = ImportService(db)
        job = service.run_import(import_type='employees', upload=upload)
        assert job.failed_rows == 3, f'Expected 3 failures, got {job.failed_rows}'
        failed_rows = [r for r in job.result_summary.get('rows', []) if r['status'] == 'failed']
        assert len(failed_rows) == 3, 'All 3 invalid rows must be collected'

    def test_mixed_valid_and_invalid_rows(self, db):
        """准备 5 行 CSV：3 行有效、2 行无效。验证 success_rows=3, failed_rows=2。"""
        _ensure_department(db)
        csv_content = (
            '员工工号,员工姓名,所属部门,岗位族,岗位级别,在职状态,身份证号\n'
            'MIX-001,张有效一,产品技术中心,技术,P6,active,\n'
            'MIX-002,李无效一,不存在部门X,技术,P7,active,\n'
            'MIX-003,王有效二,产品技术中心,产品,P5,active,\n'
            'MIX-004,赵无效二,不存在部门Y,设计,P4,active,\n'
            'MIX-005,孙有效三,产品技术中心,技术,P8,active,\n'
        ).encode('utf-8')
        upload = _make_upload('mixed.csv', csv_content)
        service = ImportService(db)
        job = service.run_import(import_type='employees', upload=upload)
        assert job.success_rows == 3
        assert job.failed_rows == 2


class TestSavepointPartialCommit:
    """IMP-02: 每行独立 savepoint，有效行在部分失败时仍然提交。"""

    def test_valid_rows_committed_despite_failures(self, db):
        """导入 5 行：3 有效 + 2 无效。验证数据库中有 3 条 Employee 记录。"""
        _ensure_department(db)
        csv_content = (
            '员工工号,员工姓名,所属部门,岗位族,岗位级别,在职状态,身份证号\n'
            'SP-001,张存活一,产品技术中心,技术,P6,active,\n'
            'SP-002,李失败一,不存在部门,技术,P7,active,\n'
            'SP-003,王存活二,产品技术中心,产品,P5,active,\n'
            'SP-004,赵失败二,不存在部门,设计,P4,active,\n'
            'SP-005,孙存活三,产品技术中心,技术,P8,active,\n'
        ).encode('utf-8')
        upload = _make_upload('savepoint.csv', csv_content)
        service = ImportService(db)
        service.run_import(import_type='employees', upload=upload)
        # DB 状态断言：3 条有效记录被提交
        count = db.scalar(select(func.count()).select_from(Employee).where(
            Employee.employee_no.in_(['SP-001', 'SP-003', 'SP-005'])
        ))
        assert count == 3, f'Expected 3 committed employees, got {count}'
        # DB 状态断言：无效行未被提交
        bad_count = db.scalar(select(func.count()).select_from(Employee).where(
            Employee.employee_no.in_(['SP-002', 'SP-004'])
        ))
        assert bad_count == 0, f'Expected 0 failed employees in DB, got {bad_count}'

    def test_savepoint_failure_does_not_corrupt_session(self, db):
        """导入 3 行：第 2 行失败。验证第 3 行（在失败行之后）仍成功提交，会话未被污染。"""
        _ensure_department(db)
        csv_content = (
            '员工工号,员工姓名,所属部门,岗位族,岗位级别,在职状态,身份证号\n'
            'SESS-001,第一行正常,产品技术中心,技术,P6,active,\n'
            'SESS-002,第二行失败,不存在部门,技术,P7,active,\n'
            'SESS-003,第三行也正常,产品技术中心,产品,P5,active,\n'
        ).encode('utf-8')
        upload = _make_upload('session.csv', csv_content)
        service = ImportService(db)
        job = service.run_import(import_type='employees', upload=upload)
        assert job.success_rows == 2
        assert job.failed_rows == 1
        # 关键断言：第 3 行在第 2 行失败后仍被提交（会话清理生效）
        third = db.scalar(select(Employee).where(Employee.employee_no == 'SESS-003'))
        assert third is not None, 'Row after failure must be committed (session cleanup works)'
```

**文件 2: `backend/tests/test_api/test_import_207.py`**

创建 API 集成测试（使用 TestClient + create_app）：

```python
from __future__ import annotations
import io
import pytest
from fastapi.testclient import TestClient
from backend.app.main import create_app
# ... fixture imports, dependency overrides ...


class TestHttp207Response:
    """IMP-02: 部分失败时返回 HTTP 207 Multi-Status。"""

    def test_partial_failure_returns_207(self, client, auth_headers, seed_department):
        """上传包含有效行和无效行的 CSV，断言 response.status_code == 207。"""
        csv = (
            '员工工号,员工姓名,所属部门,岗位族,岗位级别,在职状态,身份证号\n'
            'API-001,有效行,产品技术中心,技术,P6,active,\n'
            'API-002,无效行,不存在部门,技术,P7,active,\n'
        ).encode('utf-8')
        files = {'file': ('test.csv', io.BytesIO(csv), 'text/csv')}
        response = client.post('/api/v1/imports/jobs?import_type=employees', files=files, headers=auth_headers)
        assert response.status_code == 207

    def test_all_success_returns_201(self, client, auth_headers, seed_department):
        """上传全部有效的 CSV，断言 response.status_code == 201。"""
        csv = (
            '员工工号,员工姓名,所属部门,岗位族,岗位级别,在职状态,身份证号\n'
            'OK-001,全成功,产品技术中心,技术,P6,active,\n'
        ).encode('utf-8')
        files = {'file': ('test.csv', io.BytesIO(csv), 'text/csv')}
        response = client.post('/api/v1/imports/jobs?import_type=employees', files=files, headers=auth_headers)
        assert response.status_code == 201

    def test_all_failure_returns_207(self, client, auth_headers):
        """上传全部无效的 CSV，断言 response.status_code == 207。"""
        csv = (
            '员工工号,员工姓名,所属部门,岗位族,岗位级别,在职状态,身份证号\n'
            'FAIL-001,全失败,不存在部门,技术,P7,active,\n'
        ).encode('utf-8')
        files = {'file': ('test.csv', io.BytesIO(csv), 'text/csv')}
        response = client.post('/api/v1/imports/jobs?import_type=employees', files=files, headers=auth_headers)
        assert response.status_code == 207


class TestResponseSummary:
    """IMP-03: 响应包含 total_rows, success_rows, failed_rows + 逐行错误。"""

    def test_response_contains_summary_fields(self, client, auth_headers, seed_department):
        """断言响应 JSON 包含 total_rows, success_rows, failed_rows 字段。"""
        csv = (
            '员工工号,员工姓名,所属部门,岗位族,岗位级别,在职状态,身份证号\n'
            'SUM-001,汇总测试,产品技术中心,技术,P6,active,\n'
            'SUM-002,汇总失败,不存在部门,技术,P7,active,\n'
        ).encode('utf-8')
        files = {'file': ('test.csv', io.BytesIO(csv), 'text/csv')}
        response = client.post('/api/v1/imports/jobs?import_type=employees', files=files, headers=auth_headers)
        data = response.json()
        assert 'total_rows' in data
        assert 'success_rows' in data
        assert 'failed_rows' in data
        assert data['total_rows'] == 2
        assert data['success_rows'] == 1
        assert data['failed_rows'] == 1

    def test_failed_rows_have_error_messages(self, client, auth_headers):
        """断言每条失败行都有 row_index、status、message 字段。"""
        csv = (
            '员工工号,员工姓名,所属部门,岗位族,岗位级别,在职状态,身份证号\n'
            'ERR-001,错误行,不存在部门,技术,P7,active,\n'
        ).encode('utf-8')
        files = {'file': ('test.csv', io.BytesIO(csv), 'text/csv')}
        response = client.post('/api/v1/imports/jobs?import_type=employees', files=files, headers=auth_headers)
        rows = response.json()['result_summary']['rows']
        failed = [r for r in rows if r['status'] == 'failed']
        assert len(failed) >= 1
        for row in failed:
            assert 'row_index' in row
            assert 'status' in row
            assert 'message' in row

    def test_failed_rows_have_error_column(self, client, auth_headers):
        """断言失败行包含 error_column 字段，标识具体失败的列。"""
        csv = (
            '员工工号,员工姓名,所属部门,岗位族,岗位级别,在职状态,身份证号\n'
            'COL-001,错误列测试,不存在部门,技术,P7,active,\n'
        ).encode('utf-8')
        files = {'file': ('test.csv', io.BytesIO(csv), 'text/csv')}
        response = client.post('/api/v1/imports/jobs?import_type=employees', files=files, headers=auth_headers)
        rows = response.json()['result_summary']['rows']
        failed = [r for r in rows if r['status'] == 'failed']
        assert len(failed) >= 1
        for row in failed:
            assert 'error_column' in row, 'Failed rows must include error_column field'

    def test_mixed_100_rows_90_success_10_failure(self, client, auth_headers, seed_department):
        """IMP-01/IMP-02 门控集成测试：上传 100 行 CSV（90 有效 + 10 无效），
        断言 207 响应，success_rows=90，failed_rows=10。"""
        lines = ['员工工号,员工姓名,所属部门,岗位族,岗位级别,在职状态,身份证号']
        for i in range(1, 91):
            lines.append(f'BULK-{i:03d},有效员工{i},产品技术中心,技术,P6,active,')
        for i in range(91, 101):
            lines.append(f'BULK-{i:03d},无效员工{i},不存在部门{i},技术,P7,active,')
        csv = '\n'.join(lines).encode('utf-8')
        files = {'file': ('bulk100.csv', io.BytesIO(csv), 'text/csv')}
        response = client.post('/api/v1/imports/jobs?import_type=employees', files=files, headers=auth_headers)
        assert response.status_code == 207
        data = response.json()
        assert data['success_rows'] == 90
        assert data['failed_rows'] == 10
        assert data['total_rows'] == 100
```

**注意事项：**
- 所有测试文件必须以 `from __future__ import annotations` 开头
- API 测试需要 auth_headers fixture（admin 用户 JWT），参考项目已有 TestClient 模式
- 创建 `seed_department` fixture 确保"产品技术中心"部门存在
- 上述代码为完整的行为测试模板，执行器根据实际 conftest.py 中的 fixture 命名进行适配
- 100 行门控测试是评审要求的关键集成测试——必须实现
  </action>
  <verify>
    <automated>cd D:/wage_adjust && python -m pytest backend/tests/test_services/test_import_partial_success.py backend/tests/test_api/test_import_207.py --collect-only 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" backend/tests/test_services/test_import_partial_success.py` 返回 >= 4
    - `grep -c "def test_" backend/tests/test_api/test_import_207.py` 返回 >= 7（包含 100 行门控测试）
    - `grep -q "assert.*count.*==" backend/tests/test_services/test_import_partial_success.py` 成功（DB 状态断言）
    - `grep -q "expire_all\|session" backend/tests/test_services/test_import_partial_success.py` 成功（会话清理验证）
    - `grep -q "status_code == 207" backend/tests/test_api/test_import_207.py` 成功
    - `grep -q "status_code == 201" backend/tests/test_api/test_import_207.py` 成功
    - `grep -q "error_column" backend/tests/test_api/test_import_207.py` 成功
    - `grep -q "100\|BULK" backend/tests/test_api/test_import_207.py` 成功（100 行门控测试）
    - `python -m pytest --collect-only` 可以收集到所有测试用例
  </acceptance_criteria>
  <done>SAVEPOINT 部分成功和 HTTP 207 响应的完整行为测试已创建，包含 DB 状态断言、会话清理验证、100 行门控集成测试，覆盖 IMP-01、IMP-02、IMP-03。</done>
</task>

<task type="auto">
  <name>Task 2: xlsx/编码行为测试 + upsert 审计 DB 断言 + 认证导入独立测试（IMP-04, IMP-05, IMP-06）</name>
  <files>
    backend/tests/test_services/test_import_xlsx.py,
    backend/tests/test_services/test_import_upsert_audit.py,
    backend/tests/test_services/test_import_certification.py
  </files>
  <read_first>
    - backend/tests/test_services/test_import_service.py（现有测试模式）
    - backend/tests/test_services/test_import_idempotency.py（现有幂等测试模式）
    - backend/tests/conftest.py（fixture 定义）
    - backend/app/services/import_service.py（改造后版本）
    - backend/app/models/certification.py（Certification 模型字段）
  </read_first>
  <action>
**文件 1: `backend/tests/test_services/test_import_xlsx.py`**

完整行为测试，每个 test 方法包含实际断言：

```python
from __future__ import annotations
import io
import pytest
from unittest.mock import MagicMock
from backend.app.services.import_service import ImportService
from backend.app.models.employee import Employee
from backend.app.models.department import Department
from sqlalchemy import select, func


def _make_upload(filename: str, content: bytes) -> MagicMock:
    upload = MagicMock()
    upload.filename = filename
    upload.file = io.BytesIO(content)
    upload.file.read = lambda: content
    return upload


def _ensure_department(db, name='产品技术中心'):
    existing = db.scalar(select(Department).where(Department.name == name))
    if existing is None:
        db.add(Department(name=name, status='active'))
        db.commit()


class TestXlsxRead:
    """IMP-04: xlsx 文件可正常读取导入。"""

    def test_xlsx_file_parsed_successfully(self, db):
        """创建一个真实的 xlsx 文件（用 openpyxl），上传导入，验证成功。"""
        _ensure_department(db)
        from openpyxl import Workbook
        wb = Workbook()
        ws = wb.active
        ws.append(['员工工号', '员工姓名', '所属部门', '岗位族', '岗位级别', '在职状态', '身份证号'])
        ws.append(['XLSX-001', '张Excel', '产品技术中心', '技术', 'P6', 'active', ''])
        output = io.BytesIO()
        wb.save(output)
        xlsx_bytes = output.getvalue()

        upload = _make_upload('test.xlsx', xlsx_bytes)
        service = ImportService(db)
        job = service.run_import(import_type='employees', upload=upload)
        assert job.total_rows == 1
        assert job.success_rows == 1
        # DB 断言
        emp = db.scalar(select(Employee).where(Employee.employee_no == 'XLSX-001'))
        assert emp is not None
        assert emp.name == '张Excel'

    def test_gbk_csv_no_garbled_characters(self, db):
        """创建 GBK 编码的 CSV，导入后验证中文字段正确存储（不乱码）。"""
        _ensure_department(db)
        csv_text = '员工工号,员工姓名,所属部门,岗位族,岗位级别,在职状态,身份证号\nGBK-001,张国标编码,产品技术中心,技术,P6,active,\n'
        gbk_bytes = csv_text.encode('gbk')
        upload = _make_upload('gbk.csv', gbk_bytes)
        service = ImportService(db)
        job = service.run_import(import_type='employees', upload=upload)
        assert job.success_rows == 1
        # DB 断言：中文名称正确存储
        emp = db.scalar(select(Employee).where(Employee.employee_no == 'GBK-001'))
        assert emp is not None
        assert emp.name == '张国标编码', f'Expected 张国标编码, got {emp.name}'

    def test_utf8_bom_csv_parsed(self, db):
        """创建 UTF-8 BOM 编码的 CSV，导入后验证正确解析。"""
        _ensure_department(db)
        csv_text = '员工工号,员工姓名,所属部门,岗位族,岗位级别,在职状态,身份证号\nBOM-001,带BOM头,产品技术中心,技术,P6,active,\n'
        bom_bytes = b'\xef\xbb\xbf' + csv_text.encode('utf-8')
        upload = _make_upload('bom.csv', bom_bytes)
        service = ImportService(db)
        job = service.run_import(import_type='employees', upload=upload)
        assert job.success_rows == 1

    def test_5000_row_limit_enforced(self, db):
        """创建超过 5000 行的 CSV，验证被拒绝并返回正确错误信息。"""
        header = '员工工号,员工姓名,所属部门,岗位族,岗位级别,在职状态,身份证号\n'
        rows = ''.join(f'OVER-{i:05d},测试员工{i},产品技术中心,技术,P6,active,\n' for i in range(5001))
        csv_bytes = (header + rows).encode('utf-8')
        upload = _make_upload('too_many.csv', csv_bytes)
        service = ImportService(db)
        with pytest.raises(ValueError, match='5000'):
            service.run_import(import_type='employees', upload=upload)


class TestXlsxTemplate:
    """IMP-06: xlsx 模板下载。"""

    def test_employee_xlsx_template_generated(self, db):
        """调用 build_template_xlsx('employees')，验证返回 xlsx 字节流。"""
        service = ImportService(db)
        file_name, content, media_type = service.build_template_xlsx('employees')
        assert file_name.endswith('.xlsx')
        assert 'spreadsheetml' in media_type
        # 验证可被 openpyxl 打开且有表头行
        from openpyxl import load_workbook
        wb = load_workbook(io.BytesIO(content))
        ws = wb.active
        assert ws.cell(row=1, column=1).value is not None, 'Header row must exist'

    def test_certification_xlsx_template_generated(self, db):
        """调用 build_template_xlsx('certifications')，验证返回 xlsx 字节流。"""
        service = ImportService(db)
        file_name, content, media_type = service.build_template_xlsx('certifications')
        assert file_name.endswith('.xlsx')
        assert len(content) > 0

    def test_xlsx_template_has_example_rows(self, db):
        """验证 xlsx 模板包含示例数据行（不只有表头）。"""
        service = ImportService(db)
        _, content, _ = service.build_template_xlsx('employees')
        from openpyxl import load_workbook
        wb = load_workbook(io.BytesIO(content))
        ws = wb.active
        assert ws.max_row >= 2, 'Template must have at least header + 1 example row'

    def test_xlsx_error_report_generated(self, db):
        """创建一个有失败行的导入结果，调用 build_export_report_xlsx，验证生成的 xlsx 包含失败行数据。"""
        # 先执行一次有失败行的导入
        csv_bytes = (
            '员工工号,员工姓名,所属部门,岗位族,岗位级别,在职状态,身份证号\n'
            'RPT-001,报告失败,不存在部门,技术,P7,active,\n'
        ).encode('utf-8')
        upload = _make_upload('report.csv', csv_bytes)
        service = ImportService(db)
        job = service.run_import(import_type='employees', upload=upload)
        assert job.failed_rows >= 1
        # 生成错误报告 xlsx
        file_name, content, media_type = service.build_export_report_xlsx(job)
        assert file_name.endswith('.xlsx')
        from openpyxl import load_workbook
        wb = load_workbook(io.BytesIO(content))
        ws = wb.active
        assert ws.max_row >= 2, 'Error report must have header + at least 1 failed row'
```

**文件 2: `backend/tests/test_services/test_import_upsert_audit.py`**

完整行为测试，重点 DB 状态断言：

```python
from __future__ import annotations
import io
import pytest
from unittest.mock import MagicMock
from sqlalchemy import select, func
from backend.app.services.import_service import ImportService
from backend.app.models.employee import Employee
from backend.app.models.audit_log import AuditLog
from backend.app.models.department import Department


def _make_upload(filename, content):
    upload = MagicMock()
    upload.filename = filename
    upload.file = io.BytesIO(content)
    upload.file.read = lambda: content
    return upload


def _ensure_department(db, name='产品技术中心'):
    existing = db.scalar(select(Department).where(Department.name == name))
    if existing is None:
        db.add(Department(name=name, status='active'))
        db.commit()


class TestEmployeeUpsert:
    """IMP-05: employee_no upsert 幂等，重复导入不产生重复数据。"""

    def test_second_import_updates_existing_employee(self, db):
        """导入员工 UPS-001，再次导入同一工号但不同姓名，验证只有 1 条记录且姓名已更新。"""
        _ensure_department(db)
        csv_v1 = '员工工号,员工姓名,所属部门,岗位族,岗位级别,在职状态,身份证号\nUPS-001,原始姓名,产品技术中心,技术,P6,active,\n'.encode('utf-8')
        csv_v2 = '员工工号,员工姓名,所属部门,岗位族,岗位级别,在职状态,身份证号\nUPS-001,更新姓名,产品技术中心,技术,P7,active,\n'.encode('utf-8')

        service = ImportService(db, operator_id='test-admin-id', operator_role='admin')
        service.run_import(import_type='employees', upload=_make_upload('v1.csv', csv_v1))
        service.run_import(import_type='employees', upload=_make_upload('v2.csv', csv_v2))

        # DB 断言：仅 1 条记录
        count = db.scalar(select(func.count()).select_from(Employee).where(Employee.employee_no == 'UPS-001'))
        assert count == 1, f'Expected 1 employee, got {count}'
        # DB 断言：姓名已更新
        emp = db.scalar(select(Employee).where(Employee.employee_no == 'UPS-001'))
        assert emp.name == '更新姓名'

    def test_upsert_does_not_create_duplicate(self, db):
        """导入相同文件两次，验证 Employee 表不产生重复行。"""
        _ensure_department(db)
        csv = '员工工号,员工姓名,所属部门,岗位族,岗位级别,在职状态,身份证号\nDUP-001,重复测试,产品技术中心,技术,P6,active,\n'.encode('utf-8')
        service = ImportService(db)
        service.run_import(import_type='employees', upload=_make_upload('dup1.csv', csv))
        service.run_import(import_type='employees', upload=_make_upload('dup2.csv', csv))
        count = db.scalar(select(func.count()).select_from(Employee).where(Employee.employee_no == 'DUP-001'))
        assert count == 1

    def test_upsert_writes_audit_log(self, db):
        """导入已存在的员工（更新操作），验证 AuditLog 中有 action='employee_import_update' 的记录。"""
        _ensure_department(db)
        csv = '员工工号,员工姓名,所属部门,岗位族,岗位级别,在职状态,身份证号\nAUD-001,审计原始,产品技术中心,技术,P6,active,\n'.encode('utf-8')
        csv_v2 = '员工工号,员工姓名,所属部门,岗位族,岗位级别,在职状态,身份证号\nAUD-001,审计更新,产品技术中心,技术,P7,active,\n'.encode('utf-8')
        service = ImportService(db, operator_id='test-admin-id', operator_role='admin')
        service.run_import(import_type='employees', upload=_make_upload('aud1.csv', csv))
        service.run_import(import_type='employees', upload=_make_upload('aud2.csv', csv_v2))
        # DB 断言：有审计日志
        audit_count = db.scalar(
            select(func.count()).select_from(AuditLog).where(AuditLog.action == 'employee_import_update')
        )
        assert audit_count >= 1, 'Update operation must produce audit log'
        # DB 断言：审计日志包含 old_value 和 new_value
        audit = db.scalar(select(AuditLog).where(AuditLog.action == 'employee_import_update'))
        assert 'old_value' in audit.detail
        assert 'new_value' in audit.detail

    def test_new_employee_no_audit_log(self, db):
        """导入新员工（创建操作），验证不产生 'employee_import_update' 审计日志。"""
        _ensure_department(db)
        csv = '员工工号,员工姓名,所属部门,岗位族,岗位级别,在职状态,身份证号\nNEW-001,新员工,产品技术中心,技术,P6,active,\n'.encode('utf-8')
        service = ImportService(db, operator_id='test-admin-id', operator_role='admin')
        service.run_import(import_type='employees', upload=_make_upload('new.csv', csv))
        audit_count = db.scalar(
            select(func.count()).select_from(AuditLog).where(
                AuditLog.action == 'employee_import_update',
                AuditLog.target_type == 'employee',
            )
        )
        assert audit_count == 0, 'New employee creation must NOT produce update audit log'
```

**文件 3: `backend/tests/test_services/test_import_certification.py`**

认证导入独立测试（解决评审 HIGH 问题：认证导入路径不明确）：

```python
from __future__ import annotations
import io
import pytest
from unittest.mock import MagicMock
from sqlalchemy import select, func
from backend.app.services.import_service import ImportService
from backend.app.models.employee import Employee
from backend.app.models.certification import Certification
from backend.app.models.department import Department


def _make_upload(filename, content):
    upload = MagicMock()
    upload.filename = filename
    upload.file = io.BytesIO(content)
    upload.file.read = lambda: content
    return upload


def _seed_employee(db, employee_no='CERT-EMP-001', name='认证测试员工'):
    """创建测试用部门和员工。"""
    dept = db.scalar(select(Department).where(Department.name == '产品技术中心'))
    if dept is None:
        dept = Department(name='产品技术中心', status='active')
        db.add(dept)
        db.flush()
    emp = db.scalar(select(Employee).where(Employee.employee_no == employee_no))
    if emp is None:
        emp = Employee(employee_no=employee_no, name=name, department='产品技术中心', status='active')
        db.add(emp)
        db.commit()
    return emp


class TestCertificationSavepoint:
    """认证导入 SAVEPOINT 部分成功（解决评审 HIGH：认证导入路径明确化）。"""

    def test_missing_employee_reference_recorded_as_failure(self, db):
        """认证 CSV 中引用不存在的员工工号，该行标记为 failed 而非导致整体中断。"""
        _seed_employee(db, 'CERT-EMP-001')
        csv = (
            '员工工号,认证类型,认证阶段,补贴比例,发证时间,到期时间\n'
            'CERT-EMP-001,AI基础认证,AI意识唤醒,0.02,2026-01-01,2027-01-01\n'
            'NONEXIST-999,AI基础认证,AI意识唤醒,0.02,2026-01-01,2027-01-01\n'
        ).encode('utf-8')
        upload = _make_upload('cert.csv', csv)
        service = ImportService(db)
        job = service.run_import(import_type='certifications', upload=upload)
        assert job.success_rows == 1, 'Valid certification row must succeed'
        assert job.failed_rows == 1, 'Missing employee reference must be recorded as failure'
        failed = [r for r in job.result_summary.get('rows', []) if r['status'] == 'failed']
        assert len(failed) == 1
        assert '未找到' in failed[0]['message'] or 'NONEXIST' in failed[0]['message']

    def test_certification_upsert_overwrites_existing(self, db):
        """同一员工同一认证类型导入两次，第二次覆盖更新而非创建重复。"""
        emp = _seed_employee(db, 'CERT-UPS-001')
        csv_v1 = (
            '员工工号,认证类型,认证阶段,补贴比例,发证时间,到期时间\n'
            'CERT-UPS-001,AI基础认证,AI意识唤醒,0.02,2026-01-01,2027-01-01\n'
        ).encode('utf-8')
        csv_v2 = (
            '员工工号,认证类型,认证阶段,补贴比例,发证时间,到期时间\n'
            'CERT-UPS-001,AI基础认证,AI技能应用,0.05,2026-06-01,2027-06-01\n'
        ).encode('utf-8')
        service = ImportService(db)
        service.run_import(import_type='certifications', upload=_make_upload('cv1.csv', csv_v1))
        service.run_import(import_type='certifications', upload=_make_upload('cv2.csv', csv_v2))
        # DB 断言：仅 1 条 Certification 记录
        count = db.scalar(
            select(func.count()).select_from(Certification).where(
                Certification.employee_id == emp.id,
                Certification.certification_type == 'AI基础认证',
            )
        )
        assert count == 1, f'Expected 1 certification, got {count}'
        # DB 断言：已更新为新阶段和新 bonus_rate
        cert = db.scalar(select(Certification).where(
            Certification.employee_id == emp.id,
            Certification.certification_type == 'AI基础认证',
        ))
        assert cert.certification_stage == 'AI技能应用'
        assert cert.bonus_rate == 0.05

    def test_certification_partial_commit_with_mixed_rows(self, db):
        """认证 CSV 中 3 行：2 行有效（员工存在）+ 1 行无效（员工不存在），验证部分提交。"""
        _seed_employee(db, 'CERT-MIX-001', '混合员工一')
        _seed_employee(db, 'CERT-MIX-002', '混合员工二')
        csv = (
            '员工工号,认证类型,认证阶段,补贴比例,发证时间,到期时间\n'
            'CERT-MIX-001,AI基础认证,AI意识唤醒,0.02,2026-01-01,2027-01-01\n'
            'CERT-MIX-002,AI应用认证,AI技能应用,0.05,2026-03-01,2027-03-01\n'
            'CERT-NOEXIST,AI基础认证,AI意识唤醒,0.02,2026-01-01,2027-01-01\n'
        ).encode('utf-8')
        upload = _make_upload('cert_mix.csv', csv)
        service = ImportService(db)
        job = service.run_import(import_type='certifications', upload=upload)
        assert job.success_rows == 2
        assert job.failed_rows == 1
        # DB 断言：2 条 Certification 被提交
        total_certs = db.scalar(select(func.count()).select_from(Certification))
        assert total_certs >= 2
```

**注意事项：**
- 所有测试文件必须以 `from __future__ import annotations` 开头
- 上述代码为完整行为测试，执行器根据实际 conftest.py fixture 命名适配
- 认证导入测试是评审特别要求的——必须覆盖缺失员工引用和重复处理两个场景
  </action>
  <verify>
    <automated>cd D:/wage_adjust && python -m pytest backend/tests/test_services/test_import_xlsx.py backend/tests/test_services/test_import_upsert_audit.py backend/tests/test_services/test_import_certification.py --collect-only 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" backend/tests/test_services/test_import_xlsx.py` 返回 >= 8
    - `grep -c "def test_" backend/tests/test_services/test_import_upsert_audit.py` 返回 >= 4
    - `grep -c "def test_" backend/tests/test_services/test_import_certification.py` 返回 >= 3
    - `grep -q "assert.*count\|assert.*name ==" backend/tests/test_services/test_import_upsert_audit.py` 成功（DB 状态断言）
    - `grep -q "AuditLog" backend/tests/test_services/test_import_upsert_audit.py` 成功
    - `grep -q "Certification" backend/tests/test_services/test_import_certification.py` 成功
    - `grep -q "NONEXIST\|未找到" backend/tests/test_services/test_import_certification.py` 成功（缺失员工引用测试）
    - `grep -q "certification_stage" backend/tests/test_services/test_import_certification.py` 成功（upsert 覆盖测试）
    - `grep -q "encode.*gbk" backend/tests/test_services/test_import_xlsx.py` 成功（真实 GBK 编码）
    - `python -m pytest --collect-only` 可以收集到所有测试用例
  </acceptance_criteria>
  <done>xlsx 编码行为测试、upsert 幂等 DB 断言测试、认证导入独立测试已创建，覆盖 IMP-04、IMP-05、IMP-06 及评审要求的认证导入路径。</done>
</task>

</tasks>

<verification>
```bash
# 收集所有新测试
cd D:/wage_adjust && python -m pytest \
  backend/tests/test_services/test_import_partial_success.py \
  backend/tests/test_api/test_import_207.py \
  backend/tests/test_services/test_import_xlsx.py \
  backend/tests/test_services/test_import_upsert_audit.py \
  backend/tests/test_services/test_import_certification.py \
  --collect-only

# 确认所有 IMP ID 都有测试覆盖
grep -l "IMP-01" backend/tests/test_services/test_import_partial_success.py
grep -l "IMP-02" backend/tests/test_api/test_import_207.py
grep -l "IMP-03" backend/tests/test_api/test_import_207.py
grep -l "IMP-04" backend/tests/test_services/test_import_xlsx.py
grep -l "IMP-05" backend/tests/test_services/test_import_upsert_audit.py
grep -l "IMP-06" backend/tests/test_services/test_import_xlsx.py
grep -l "认证" backend/tests/test_services/test_import_certification.py
```
</verification>

<success_criteria>
- 5 个测试文件创建完成（新增 test_import_certification.py）
- 所有 6 个 IMP 需求都有对应的行为测试用例（非空桩）
- 认证导入有独立测试覆盖缺失员工引用、重复处理、部分提交
- 所有 SAVEPOINT 测试包含 DB 状态断言（查询 Employee/Certification 表行数）
- upsert 测试断言 Employee 表无重复行且 AuditLog 有记录
- 编码测试使用真实 GBK/BOM 编码（非 UTF-8 伪装）
- API 集成测试包含 100 行门控测试（90 成功 + 10 失败 = 207）
- pytest --collect-only 可收集所有测试
</success_criteria>

<output>
After completion, create `.planning/phases/06-batch-import-reliability/06-02-SUMMARY.md`
</output>
