---
phase: 06-batch-import-reliability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_services/test_import_partial_success.py
  - backend/tests/test_api/test_import_207.py
  - backend/tests/test_services/test_import_xlsx.py
  - backend/tests/test_services/test_import_upsert_audit.py
autonomous: true
requirements: [IMP-01, IMP-02, IMP-03, IMP-04, IMP-05, IMP-06]

must_haves:
  truths:
    - "所有 6 个 IMP 需求都有对应的测试用例"
    - "测试使用 SQLite 内存数据库，无需外部依赖"
    - "测试可通过 pytest 独立运行"
  artifacts:
    - path: "backend/tests/test_services/test_import_partial_success.py"
      provides: "IMP-01 惰性验证 + IMP-02 SAVEPOINT 部分提交测试"
    - path: "backend/tests/test_api/test_import_207.py"
      provides: "IMP-02 HTTP 207 + IMP-03 响应结构测试"
    - path: "backend/tests/test_services/test_import_xlsx.py"
      provides: "IMP-04 xlsx/GBK 编码 + IMP-06 xlsx 模板生成测试"
    - path: "backend/tests/test_services/test_import_upsert_audit.py"
      provides: "IMP-05 upsert 幂等 + 审计日志测试"
  key_links:
    - from: "test files"
      to: "backend/app/services/import_service.py"
      via: "ImportService 直接实例化"
      pattern: "ImportService"
---

<objective>
为批量导入的全部 6 个 IMP 需求创建 RED 测试桩，覆盖 SAVEPOINT 部分成功、HTTP 207、xlsx 读取、编码兼容、upsert 幂等、模板生成等行为。

Purpose: 建立可自动化验证的测试基线，确保 Plan 01 的后端改造和 Plan 03 的前端改造均可被客观验证。
Output: 4 个测试文件，覆盖所有 IMP 需求。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-batch-import-reliability/06-CONTEXT.md
@.planning/phases/06-batch-import-reliability/06-RESEARCH.md

<interfaces>
<!-- 已有测试模式参考 -->

From backend/tests/test_services/test_import_service.py — 现有测试使用的 fixture 模式：
```python
# 典型测试 fixture：使用 SQLAlchemy Session + SQLite 内存数据库
# 参考 backend/tests/conftest.py 中的 db fixture
```

From backend/app/services/import_service.py — 关键方法签名（改造后）：
```python
class ImportService:
    MAX_ROWS = 5000
    def __init__(self, db: Session, *, operator_id: str | None = None, operator_role: str | None = None): ...
    def run_import(self, *, import_type: str, upload: UploadFile) -> ImportJob: ...
    def build_template(self, import_type: str) -> tuple[str, bytes, str]: ...
    def build_template_xlsx(self, import_type: str) -> tuple[str, bytes, str]: ...
    def build_export_report(self, job: ImportJob) -> tuple[str, bytes, str]: ...
    def build_export_report_xlsx(self, job: ImportJob) -> tuple[str, bytes, str]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SAVEPOINT 部分成功 + HTTP 207 响应测试（IMP-01, IMP-02, IMP-03）</name>
  <files>backend/tests/test_services/test_import_partial_success.py, backend/tests/test_api/test_import_207.py</files>
  <read_first>
    - backend/tests/test_services/test_import_service.py（现有测试模式、fixture 用法）
    - backend/tests/test_services/test_import_idempotency.py（现有幂等测试模式）
    - backend/tests/conftest.py（db fixture 定义）
    - backend/app/services/import_service.py（改造后的签名和行为）
    - backend/app/api/v1/imports.py（改造后的 API）
    - backend/app/models/employee.py（Employee 字段）
    - backend/app/models/department.py（Department 字段）
  </read_first>
  <action>
**文件 1: `backend/tests/test_services/test_import_partial_success.py`**

创建以下测试用例（使用项目已有的 db fixture 和 conftest 模式）：

```python
from __future__ import annotations
import io
import pytest
from unittest.mock import MagicMock
from backend.app.services.import_service import ImportService
# ... 必要的 model imports

class TestLazyValidation:
    """IMP-01: 惰性验证——收集所有行级错误后一次性返回，不在第一个错误时中断。"""

    def test_all_row_errors_collected_not_stopped_at_first(self, db):
        """准备包含多行错误的 CSV（如 3 行缺少必填字段），验证所有 3 行都出现在 results 中。"""
        # 构造 CSV: 3 行都缺少 name 字段
        # 调用 service.run_import(...)
        # 断言 job.failed_rows == 3（不是 1）
        # 断言 len([r for r in rows if r['status'] == 'failed']) == 3

    def test_mixed_valid_and_invalid_rows(self, db):
        """准备 5 行 CSV：3 行有效、2 行无效。验证 success_rows=3, failed_rows=2。"""
        pass

class TestSavepointPartialCommit:
    """IMP-02: 每行独立 savepoint，有效行在部分失败时仍然提交。"""

    def test_valid_rows_committed_despite_failures(self, db):
        """导入 10 行：8 有效 + 2 无效。验证数据库中有 8 条 Employee 记录。"""
        # 导入后查询 Employee count == 8
        pass

    def test_single_row_failure_does_not_rollback_others(self, db):
        """导入 3 行：第 2 行部门不存在。验证第 1 行和第 3 行仍被提交。"""
        pass
```

**文件 2: `backend/tests/test_api/test_import_207.py`**

创建 API 集成测试：

```python
from __future__ import annotations
import io
import pytest
from fastapi.testclient import TestClient
from backend.app.main import create_app
# ... 必要的 fixture 和 override

class TestHttp207Response:
    """IMP-02: 部分失败时返回 HTTP 207 Multi-Status。"""

    def test_partial_failure_returns_207(self, client):
        """上传包含有效行和无效行的 CSV，断言 response.status_code == 207。"""
        pass

    def test_all_success_returns_201(self, client):
        """上传全部有效的 CSV，断言 response.status_code == 201。"""
        pass

    def test_all_failure_returns_207(self, client):
        """上传全部无效的 CSV，断言 response.status_code == 207。"""
        pass

class TestResponseSummary:
    """IMP-03: 响应包含 total_rows, success_rows, failed_rows + 逐行错误。"""

    def test_response_contains_summary_fields(self, client):
        """断言响应 JSON 包含 total_rows, success_rows, failed_rows 字段。"""
        pass

    def test_failed_rows_have_error_messages(self, client):
        """断言每条失败行都有 row_index、status、message 字段。"""
        pass

    def test_failed_rows_have_error_column(self, client):
        """断言失败行包含 error_column 字段，标识具体失败的列。"""
        pass
```

**注意事项：**
- 所有测试文件必须以 `from __future__ import annotations` 开头
- 使用项目已有的 conftest.py 中的 db fixture（或创建本地 fixture）
- 对于 API 测试，参考项目中已有的 TestClient 模式（如 `test_evaluation_api_response_contract`）
- 创建测试用的 Department 记录（因为员工导入需要部门存在）
- CSV 内容用 `io.BytesIO` 构造，通过 `MagicMock` 模拟 `UploadFile`
  </action>
  <verify>
    <automated>cd D:/wage_adjust && python -m pytest backend/tests/test_services/test_import_partial_success.py backend/tests/test_api/test_import_207.py --collect-only 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" backend/tests/test_services/test_import_partial_success.py` 返回 >= 4
    - `grep -c "def test_" backend/tests/test_api/test_import_207.py` 返回 >= 5
    - `grep -q "IMP-01" backend/tests/test_services/test_import_partial_success.py` 成功（docstring 中引用需求 ID）
    - `grep -q "IMP-02" backend/tests/test_api/test_import_207.py` 成功
    - `grep -q "IMP-03" backend/tests/test_api/test_import_207.py` 成功
    - `grep -q "207" backend/tests/test_api/test_import_207.py` 成功
    - `grep -q "from __future__ import annotations" backend/tests/test_services/test_import_partial_success.py` 成功
    - `python -m pytest --collect-only` 可以收集到所有测试用例（不需要通过）
  </acceptance_criteria>
  <done>SAVEPOINT 部分成功和 HTTP 207 响应的测试桩已创建，覆盖 IMP-01、IMP-02、IMP-03。</done>
</task>

<task type="auto">
  <name>Task 2: xlsx 编码 + upsert 幂等 + 模板生成测试（IMP-04, IMP-05, IMP-06）</name>
  <files>backend/tests/test_services/test_import_xlsx.py, backend/tests/test_services/test_import_upsert_audit.py</files>
  <read_first>
    - backend/tests/test_services/test_import_service.py（现有测试模式）
    - backend/tests/test_services/test_import_idempotency.py（现有幂等测试模式）
    - backend/tests/conftest.py（fixture 定义）
    - backend/app/services/import_service.py（改造后版本）
  </read_first>
  <action>
**文件 1: `backend/tests/test_services/test_import_xlsx.py`**

```python
from __future__ import annotations
import io
import pytest
from unittest.mock import MagicMock
from backend.app.services.import_service import ImportService

class TestXlsxRead:
    """IMP-04: xlsx 文件可正常读取导入。"""

    def test_xlsx_file_parsed_successfully(self, db):
        """创建一个简单的 xlsx 文件（用 openpyxl），上传导入，验证不报错。"""
        # 用 openpyxl 创建内存中的 xlsx
        # 构造 UploadFile mock
        # 调用 service.run_import(...)
        # 断言 job.status != 'failed' 或 job.total_rows > 0
        pass

    def test_gbk_csv_no_garbled_characters(self, db):
        """创建 GBK 编码的 CSV，导入后验证中文字段正确存储。"""
        # 用 GBK 编码写入包含中文的 CSV
        # 导入后查询 Employee.name == '张三'
        pass

    def test_5000_row_limit_enforced(self, db):
        """创建超过 5000 行的 CSV，验证被拒绝并返回正确错误信息。"""
        # 构造 5001 行 CSV
        # 断言 job.status == 'failed'
        # 断言 '5000' in error message
        pass

class TestXlsxTemplate:
    """IMP-06: xlsx 模板下载。"""

    def test_employee_xlsx_template_generated(self, db):
        """调用 build_template_xlsx('employees')，验证返回 xlsx 字节流。"""
        # 断言 file_name 以 .xlsx 结尾
        # 断言 media_type 包含 'spreadsheetml'
        # 用 openpyxl 打开验证表头行
        pass

    def test_certification_xlsx_template_generated(self, db):
        """调用 build_template_xlsx('certifications')，验证返回 xlsx 字节流。"""
        pass

    def test_xlsx_template_has_example_rows(self, db):
        """验证 xlsx 模板包含示例数据行（不只有表头）。"""
        pass

    def test_xlsx_error_report_generated(self, db):
        """创建一个有失败行的 ImportJob，调用 build_export_report_xlsx，验证生成的 xlsx 包含失败行数据。"""
        pass
```

**文件 2: `backend/tests/test_services/test_import_upsert_audit.py`**

```python
from __future__ import annotations
import io
import pytest
from unittest.mock import MagicMock
from sqlalchemy import select
from backend.app.services.import_service import ImportService
from backend.app.models.employee import Employee
from backend.app.models.audit_log import AuditLog

class TestEmployeeUpsert:
    """IMP-05: employee_no upsert 幂等，重复导入不产生重复数据。"""

    def test_second_import_updates_existing_employee(self, db):
        """导入员工 EMP-001，再次导入同一工号但不同姓名，验证只有 1 条 Employee 记录且姓名已更新。"""
        # 第一次导入
        # 第二次导入（修改 name）
        # 断言 db.query(Employee).count() == 1
        # 断言 employee.name == 新姓名
        pass

    def test_upsert_does_not_create_duplicate(self, db):
        """导入相同文件两次，验证 Employee 表不产生重复行。"""
        pass

    def test_upsert_writes_audit_log(self, db):
        """导入已存在的员工（更新操作），验证 AuditLog 中有 action='employee_import_update' 的记录。"""
        # 断言 db.query(AuditLog).filter_by(action='employee_import_update').count() >= 1
        # 断言 audit.detail 包含 old_value 和 new_value
        pass

    def test_new_employee_no_audit_log(self, db):
        """导入新员工（创建操作），验证不产生 'employee_import_update' 审计日志。"""
        pass
```

**注意事项：**
- 所有测试文件必须以 `from __future__ import annotations` 开头
- 构造 UploadFile mock：`upload = MagicMock(); upload.filename = 'test.csv'; upload.file = io.BytesIO(csv_bytes)`
- 员工导入需要先创建 Department（`db.add(Department(name='产品技术中心', status='active'))`）
- xlsx 文件构造使用 openpyxl 在内存中创建
  </action>
  <verify>
    <automated>cd D:/wage_adjust && python -m pytest backend/tests/test_services/test_import_xlsx.py backend/tests/test_services/test_import_upsert_audit.py --collect-only 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" backend/tests/test_services/test_import_xlsx.py` 返回 >= 6
    - `grep -c "def test_" backend/tests/test_services/test_import_upsert_audit.py` 返回 >= 4
    - `grep -q "IMP-04" backend/tests/test_services/test_import_xlsx.py` 成功
    - `grep -q "IMP-05" backend/tests/test_services/test_import_upsert_audit.py` 成功
    - `grep -q "IMP-06" backend/tests/test_services/test_import_xlsx.py` 成功
    - `grep -q "AuditLog" backend/tests/test_services/test_import_upsert_audit.py` 成功
    - `grep -q "from __future__ import annotations" backend/tests/test_services/test_import_xlsx.py` 成功
    - `python -m pytest --collect-only` 可以收集到所有测试用例
  </acceptance_criteria>
  <done>xlsx 编码、upsert 幂等、审计日志、模板生成的测试桩已创建，覆盖 IMP-04、IMP-05、IMP-06。</done>
</task>

</tasks>

<verification>
```bash
# 收集所有新测试
cd D:/wage_adjust && python -m pytest \
  backend/tests/test_services/test_import_partial_success.py \
  backend/tests/test_api/test_import_207.py \
  backend/tests/test_services/test_import_xlsx.py \
  backend/tests/test_services/test_import_upsert_audit.py \
  --collect-only

# 确认所有 IMP ID 都有测试覆盖
grep -l "IMP-01" backend/tests/test_services/test_import_partial_success.py
grep -l "IMP-02" backend/tests/test_api/test_import_207.py
grep -l "IMP-03" backend/tests/test_api/test_import_207.py
grep -l "IMP-04" backend/tests/test_services/test_import_xlsx.py
grep -l "IMP-05" backend/tests/test_services/test_import_upsert_audit.py
grep -l "IMP-06" backend/tests/test_services/test_import_xlsx.py
```
</verification>

<success_criteria>
- 4 个测试文件创建完成
- 所有 6 个 IMP 需求都有对应的测试用例
- pytest --collect-only 可收集所有测试
- 测试桩可独立运行（collect 不报 import error）
</success_criteria>

<output>
After completion, create `.planning/phases/06-batch-import-reliability/06-02-SUMMARY.md`
</output>
