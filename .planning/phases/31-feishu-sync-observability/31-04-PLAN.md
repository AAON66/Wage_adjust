---
phase: 31-feishu-sync-observability
plan: 04
plan_number: 4
type: execute
wave: 4
depends_on: [03]
files_modified:
  - frontend/src/types/api.ts
  - frontend/src/services/feishuService.ts
  - frontend/src/index.css
  - frontend/src/utils/roleAccess.ts
  - frontend/src/App.tsx
  - frontend/src/pages/SyncLogsPage.tsx
  - frontend/src/components/feishu-sync-logs/SyncLogsTabBar.tsx
  - frontend/src/components/feishu-sync-logs/CountersBadgeCluster.tsx
  - frontend/src/components/feishu-sync-logs/StatusBadge.tsx
  - frontend/src/components/feishu-sync-logs/SyncLogRow.tsx
  - frontend/src/components/feishu-sync-logs/SyncLogDetailDrawer.tsx
  - frontend/src/components/feishu-sync-logs/SyncLogsEmptyState.tsx
autonomous: false
requirements: [IMPORT-03, IMPORT-04]
requirements_addressed: [IMPORT-03, IMPORT-04]

must_haves:
  truths:
    - "SyncLogRead TypeScript 类型含 sync_type (SyncLogSyncType 联合类型) + mapping_failed_count + status 含 'partial'（D-01/D-02/D-09）"
    - "feishuService.getSyncLogs 签名升级为 getSyncLogs(opts?: {syncType?; page?; pageSize?}) 返回 SyncLogRead[]；feishuService.downloadUnmatchedCsv(logId) 新增"
    - "/feishu/sync-logs 路由注册在 <ProtectedRoute allowedRoles={['admin', 'hrbp']}/> 下；employee/manager 直连 URL 被守门重定向（Pitfall I）"
    - "admin + hrbp 的 ROLE_MODULES 在「系统管理」分组新增 { title: '同步日志', href: '/feishu/sync-logs', icon: 'file-text' } 菜单项；employee/manager 无此菜单项"
    - "SyncLogsPage 顶部 Tab 按 D-06 顺序 6 项：全部 / 考勤 / 绩效 / 薪调 / 入职信息 / 社保假勤；Tab 切换驱动 getSyncLogs({syncType}) 刷新"
    - "每行展示 CountersBadgeCluster（五色 badge：绿/蓝/橙/紫/红，D-07），value===0 置灰；点击 badge 打开 SyncLogDetailDrawer"
    - "StatusBadge 展示 4 色（success=绿/partial=橙/failed=红/running=蓝带 spinner，D-09）"
    - "SyncLogRow 的「模式」列按 log.sync_type 条件渲染（D-04）：attendance 显示 full/incremental badge，其他 sync_type 显示 — ；SyncLogsPage <thead> 模式列位于「状态」与「计数」之间"
    - "行 Action 列「下载未匹配工号 CSV」按钮：unmatched_count > 0 启用，=0 禁用+tooltip；点击走 downloadUnmatchedCsv，文件名 sync-log-{id}-unmatched.csv"
    - "SyncLogDetailDrawer 展示 error_message + 完整 unmatched_employee_nos 列表 + leading_zero_fallback_count > 0 黄字提示；role='dialog' aria-modal"
    - "新增 CSS token --color-violet: #722ED1 + --color-violet-bg: #F5EBFA + --color-violet-border: #E1C9F0 到 frontend/src/index.css"
    - "frontend npm run lint（tsc --noEmit）通过"
  artifacts:
    - path: "frontend/src/types/api.ts"
      provides: "SyncLogRead 扩展 + SyncLogSyncType 联合类型"
      contains: "SyncLogSyncType"
    - path: "frontend/src/services/feishuService.ts"
      provides: "getSyncLogs(opts) + downloadUnmatchedCsv(logId)"
      contains: "downloadUnmatchedCsv"
    - path: "frontend/src/index.css"
      provides: "--color-violet token 三个变量"
      contains: "--color-violet"
    - path: "frontend/src/utils/roleAccess.ts"
      provides: "admin + hrbp 「同步日志」菜单项"
      contains: "/feishu/sync-logs"
    - path: "frontend/src/App.tsx"
      provides: "<Route path='/feishu/sync-logs'> 注册 under admin+hrbp ProtectedRoute"
      contains: "SyncLogsPage"
    - path: "frontend/src/pages/SyncLogsPage.tsx"
      provides: "页面壳：页头 + Tab + 表格 + 分页 + 抽屉"
      contains: "SyncLogsPage"
    - path: "frontend/src/components/feishu-sync-logs/SyncLogsTabBar.tsx"
      provides: "6 项 Tab"
      contains: "SyncLogsTabBar"
    - path: "frontend/src/components/feishu-sync-logs/CountersBadgeCluster.tsx"
      provides: "5 色 badge"
      contains: "CountersBadgeCluster"
    - path: "frontend/src/components/feishu-sync-logs/StatusBadge.tsx"
      provides: "4 色 status 徽章"
      contains: "StatusBadge"
    - path: "frontend/src/components/feishu-sync-logs/SyncLogRow.tsx"
      provides: "单行组合（类型/状态/模式/计数/时间/耗时/触发人/操作；模式列按 sync_type === 'attendance' 条件渲染，非 attendance 显示 —，D-04）"
      contains: "SyncLogRow"
    - path: "frontend/src/components/feishu-sync-logs/SyncLogDetailDrawer.tsx"
      provides: "详情抽屉（480px 宽，右侧滑入）"
      contains: "SyncLogDetailDrawer"
    - path: "frontend/src/components/feishu-sync-logs/SyncLogsEmptyState.tsx"
      provides: "空数据态 + CTA「前往飞书配置」"
      contains: "SyncLogsEmptyState"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "SyncLogsPage"
      via: "<Route path='/feishu/sync-logs'> under <ProtectedRoute allowedRoles={['admin', 'hrbp']}/>"
      pattern: "/feishu/sync-logs"
    - from: "frontend/src/pages/SyncLogsPage.tsx"
      to: "feishuService.getSyncLogs"
      via: "useEffect fetch on mount + Tab change"
      pattern: "getSyncLogs\\(\\{\\s*syncType"
    - from: "frontend/src/components/feishu-sync-logs/SyncLogRow.tsx"
      to: "feishuService.downloadUnmatchedCsv"
      via: "onClick handler"
      pattern: "downloadUnmatchedCsv"
    - from: "frontend/src/utils/roleAccess.ts"
      to: "/feishu/sync-logs"
      via: "admin + hrbp ROLE_MODULES system group new item"
      pattern: "同步日志"
---

<objective>
消费 Plan 03 暴露的 API，为 HR (admin + hrbp) 交付独立的「飞书同步日志」前端页面：Tab 切分 6 类 + 五色 badge cluster + 4 色 status + CSV 下载 + 详情抽屉。UI 完全遵循 `31-UI-SPEC.md` 的 6 维度契约（Tab 顺序、5 色 badge token、4 色 status、CSV CTA、组件清单）；无新依赖，无 shadcn，沿用项目既有 Tailwind + CSS token 设计系统。

目的：
- HR 在独立路由 `/feishu/sync-logs`（admin+hrbp 可见）诊断所有 5 类飞书同步（考勤/绩效/薪调/入职信息/社保假勤）的成功/失败
- 每行 1 瞥能看清五色计数（success/updated/unmatched/mapping_failed/failed）；点击 badge 进入抽屉看详情
- 未匹配工号 > 0 时一键下载 CSV 去飞书源表格排查

**本 Plan 含 1 个 checkpoint:human-verify** — UI-SPEC 6 维度 + manual-only 验证（页面角色守门、badge 视觉、CSV 下载、抽屉交互、SC4 双触发观测）均须人工截图/对照 VALIDATION.md 的 Manual-Only 清单完成。

Output：
- TypeScript 类型 + service 层 2 个新函数
- 6 个新 React 组件 + 1 个新页面 + 2 个现有文件修改（App.tsx 路由 + roleAccess.ts 菜单）
- 1 个 CSS token 三变量新增
- Human verification checkpoint（VALIDATION.md 清单对照）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-feishu-sync-observability/31-CONTEXT.md
@.planning/phases/31-feishu-sync-observability/31-RESEARCH.md
@.planning/phases/31-feishu-sync-observability/31-VALIDATION.md
@.planning/phases/31-feishu-sync-observability/31-UI-SPEC.md
@.planning/phases/31-feishu-sync-observability/31-01-SUMMARY.md
@.planning/phases/31-feishu-sync-observability/31-02-SUMMARY.md
@.planning/phases/31-feishu-sync-observability/31-03-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md

@frontend/src/types/api.ts
@frontend/src/services/feishuService.ts
@frontend/src/services/api.ts
@frontend/src/App.tsx
@frontend/src/utils/roleAccess.ts
@frontend/src/index.css
@frontend/src/components/ProtectedRoute.tsx
@frontend/src/components/attendance/SyncStatusCard.tsx
@frontend/src/pages/AttendanceManagement.tsx
@frontend/src/pages/EligibilityManagementPage.tsx

<interfaces>
<!-- Plan 03 暴露的后端契约（TypeScript 消费方）。Executor 直接按这些定义构造前端类型。 -->

**Backend response schema（Plan 01 扩展后的 SyncLogRead）** — 对应 `frontend/src/types/api.ts` 的 `SyncLogRead`：

```typescript
export type SyncLogSyncType =
  | 'attendance'
  | 'performance'
  | 'salary_adjustments'
  | 'hire_info'
  | 'non_statutory_leave';

export type SyncLogStatus = 'running' | 'success' | 'partial' | 'failed';

export interface SyncLogRead {
  id: string;
  sync_type: SyncLogSyncType;               // NEW (D-01)
  mode: string;
  status: SyncLogStatus;                    // CHANGED: add 'partial' (D-09)
  total_fetched: number;
  synced_count: number;
  updated_count: number;
  skipped_count: number;
  unmatched_count: number;
  mapping_failed_count: number;             // NEW (D-02)
  failed_count: number;
  leading_zero_fallback_count: number;
  error_message: string | null;
  unmatched_employee_nos: string[] | null;
  started_at: string;
  finished_at: string | null;
  triggered_by: string | null;
}
```

**Backend endpoints（Plan 03 已实现）：**
- `GET /api/v1/feishu/sync-logs?sync_type=&page=&page_size=` → `SyncLogRead[]`
- `GET /api/v1/feishu/sync-logs/{log_id}/unmatched.csv` → `Blob`（CSV）

**现有 feishuService.ts getSyncLogs（即将替换）：**

```typescript
export async function getSyncLogs(limit?: number): Promise<SyncLogRead[]>;
```

升级为（兼容 AttendanceManagement 既有调用：`getSyncLogs(20)` 仍需工作）：

```typescript
export async function getSyncLogs(
  optsOrLimit?: { syncType?: SyncLogSyncType; page?: number; pageSize?: number } | number,
): Promise<SyncLogRead[]>;

export async function downloadUnmatchedCsv(logId: string): Promise<void>;
```

**既有 index.css token（供 badge 复用，不新增）：**
- `--color-success: #00B42A` / `--color-success-bg: #E8FFEA`
- `--color-info: #1456F0` / `--color-info-bg: #EBF0FE`
- `--color-warning: #FF7D00` / `--color-warning-bg: #FFF3E8`
- `--color-danger: #F53F3F` / `--color-danger-bg: #FFECE8`

**本 Plan 新增 token（放在 `:root` 段末尾，近 --color-info-bg 处）：**

```css
/* Phase 31 / D-07: mapping_failed 专属紫色 */
--color-violet: #722ED1;
--color-violet-bg: #F5EBFA;
--color-violet-border: #E1C9F0;
```

**既有视觉参考组件（像素级对齐）：**
- `frontend/src/components/attendance/SyncStatusCard.tsx` — StatusBadge spinner + badge 配色
- `frontend/src/pages/EligibilityManagementPage.tsx:40-48` — Tab 下划线激活态
- `frontend/src/pages/AttendanceManagement.tsx` — `SyncLogRead` 消费 + CSV 按钮禁用态

**既有 App.tsx ProtectedRoute 嵌套模式（必须严格遵循）：**

```tsx
<Route element={<ProtectedRoute allowedRoles={["admin", "hrbp"]} />}>
  <Route element={<SalarySimulatorPage />} path="/salary-simulator" />
  <Route element={<ImportCenterPage />} path="/import-center" />
  <Route element={<AttendanceManagementPage />} path="/attendance" />
  <Route element={<SyncLogsPage />} path="/feishu/sync-logs" />   {/* Phase 31 */}
</Route>
```

**既有 roleAccess.ts 结构（见 frontend/src/utils/roleAccess.ts:32-66）：**
- admin.ROLE_MODULES 的 `system` 分组现有 5 项（平台账号 / 员工档案 / 导入中心 / 飞书配置 / API Key 管理 / Webhook 管理）— Phase 31 插入「同步日志」到此 group
- hrbp.ROLE_MODULES 的 `system` 分组现有 3 项（平台账号 / 员工档案 / 导入中心）— Phase 31 插入「同步日志」到此 group
- manager / employee 的 ROLE_MODULES 不加

**UI-SPEC 关键契约（必须严格实现）：**
- Tab 顺序：全部 / 考勤 / 绩效 / 薪调 / 入职信息 / 社保假勤
- 五色 badge：绿(success) / 蓝(updated) / 橙(unmatched) / 紫(mapping_failed) / 红(failed)
- 4 色 status：绿(success) / 橙(partial) / 红(failed) / 蓝(running+spinner)
- CSV CTA：文本「下载未匹配工号 CSV」；disabled tooltip「本次同步无未匹配工号，无需下载」
- 抽屉宽度：480px，右侧滑入，role='dialog' aria-modal='true'
- 时间戳格式：2026-04-21 14:32（不含秒，与 SyncStatusCard 一致）
- Page title：飞书同步日志（eyebrow: 飞书集成 / SYNC OBSERVABILITY）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 类型 + service + CSS token + 菜单注册（前端基础设施）</name>
  <files>frontend/src/types/api.ts, frontend/src/services/feishuService.ts, frontend/src/index.css, frontend/src/utils/roleAccess.ts, frontend/src/App.tsx, frontend/src/pages/SyncLogsPage.tsx</files>
  <read_first>
    - frontend/src/types/api.ts — 定位 SyncLogRead 所在行（line 781-797），查看命名风格
    - frontend/src/services/feishuService.ts — 完整文件，getSyncLogs 签名 + api import pattern
    - frontend/src/services/api.ts — Axios instance + blob download 模式（看是否已有 pattern）
    - frontend/src/index.css — :root 段 token 排列，新 token 插入位置
    - frontend/src/utils/roleAccess.ts — ROLE_MODULES 结构（admin 的 system 分组；hrbp 的 system 分组）
    - frontend/src/App.tsx — `<Route element={<ProtectedRoute allowedRoles={["admin", "hrbp"]} />}>` 块的位置（line 459-463）
    - frontend/src/pages/AttendanceManagement.tsx — AttendanceManagement 是否调用 getSyncLogs(limit)，确保升级后不破坏（兼容 overload）
    - .planning/phases/31-feishu-sync-observability/31-UI-SPEC.md §"New Design Tokens" — CSS 变量字面值
  </read_first>
  <action>
**Step 1:** 扩展 `frontend/src/types/api.ts` 的 `SyncLogRead`（替换 line 781-797 内容）：

```typescript
export type SyncLogSyncType =
  | 'attendance'
  | 'performance'
  | 'salary_adjustments'
  | 'hire_info'
  | 'non_statutory_leave';

export type SyncLogStatus = 'running' | 'success' | 'partial' | 'failed';

export interface SyncLogRead {
  id: string;
  sync_type: SyncLogSyncType;
  mode: string;
  status: SyncLogStatus;
  total_fetched: number;
  synced_count: number;
  updated_count: number;
  skipped_count: number;
  unmatched_count: number;
  mapping_failed_count: number;
  failed_count: number;
  leading_zero_fallback_count: number;
  error_message: string | null;
  unmatched_employee_nos: string[] | null;
  started_at: string;
  finished_at: string | null;
  triggered_by: string | null;
}
```

**Step 2:** 升级 `frontend/src/services/feishuService.ts` — 保持 getSyncLogs 签名向后兼容（既有 `AttendanceManagement.tsx` 调用 `getSyncLogs(20)` 必须继续工作），新增 `downloadUnmatchedCsv`：

```typescript
import api from './api';
import type {
  FeishuConfigCreate,
  FeishuConfigRead,
  FeishuConfigUpdate,
  SyncLogRead,
  SyncLogSyncType,
  SyncTriggerResponse,
} from '../types/api';

// ... existing exports unchanged ...

export interface GetSyncLogsOptions {
  syncType?: SyncLogSyncType;
  page?: number;
  pageSize?: number;
}

export async function getSyncLogs(
  optsOrLimit?: GetSyncLogsOptions | number,
): Promise<SyncLogRead[]> {
  const params: Record<string, unknown> = {};
  if (typeof optsOrLimit === 'number') {
    // Legacy call: getSyncLogs(20) → translate limit to page_size with page=1
    params.page = 1;
    params.page_size = optsOrLimit;
  } else if (optsOrLimit) {
    if (optsOrLimit.syncType) params.sync_type = optsOrLimit.syncType;
    if (optsOrLimit.page) params.page = optsOrLimit.page;
    if (optsOrLimit.pageSize) params.page_size = optsOrLimit.pageSize;
  }
  const response = await api.get<SyncLogRead[]>('/feishu/sync-logs', { params });
  return response.data;
}

export async function downloadUnmatchedCsv(logId: string): Promise<void> {
  const response = await api.get<Blob>(`/feishu/sync-logs/${logId}/unmatched.csv`, {
    responseType: 'blob',
  });
  const url = URL.createObjectURL(response.data);
  const link = document.createElement('a');
  link.href = url;
  link.download = `sync-log-${logId}-unmatched.csv`;
  document.body.appendChild(link);
  link.click();
  link.remove();
  URL.revokeObjectURL(url);
}
```

**Step 3:** 在 `frontend/src/index.css` 的 `:root` 段（紧邻 `--color-info-bg` 之后）追加：

```css
/* Phase 31 / D-07: mapping_failed 专属紫色 token */
--color-violet: #722ED1;
--color-violet-bg: #F5EBFA;
--color-violet-border: #E1C9F0;
```

**Step 4:** 修改 `frontend/src/utils/roleAccess.ts` — 在 admin.ROLE_MODULES 的 `system` 分组 items 里、`{ title: '飞书配置', ..., href: '/feishu-config', ... }` **之后** 插入新项；在 hrbp.ROLE_MODULES 的 `system` 分组 items 里、`IMPORT_CENTER_MODULE` 之后插入新项。

admin.system 新 item 位置（在飞书配置后）：

```typescript
{ title: '飞书配置', description: '配置飞书应用凭证与同步。', href: '/feishu-config', icon: 'link' },
{ title: '同步日志', description: '查看飞书同步结果与明细。', href: '/feishu/sync-logs', icon: 'file-text' },
{ title: 'API Key 管理', ... },
```

hrbp.system 新 item 位置（在 IMPORT_CENTER_MODULE 后）：

```typescript
EMPLOYEE_ARCHIVE_MODULE,
IMPORT_CENTER_MODULE,
{ title: '同步日志', description: '查看飞书同步结果与明细。', href: '/feishu/sync-logs', icon: 'file-text' },
```

**不修改 manager / employee 的 ROLE_MODULES**（他们不应看到此菜单）。

**Step 5:** 修改 `frontend/src/App.tsx`：
- 顶部加 import：`import { SyncLogsPage } from "./pages/SyncLogsPage";`（位置与 `AttendanceManagementPage` import 并列）
- 在 line 459-463 的 `<Route element={<ProtectedRoute allowedRoles={["admin", "hrbp"]} />}>` 嵌套块内，`<Route element={<AttendanceManagementPage />} path="/attendance" />` 之后追加：

```tsx
<Route element={<SyncLogsPage />} path="/feishu/sync-logs" />
```
**Step 6:** 创建最小 stub `frontend/src/pages/SyncLogsPage.tsx` 以让 `npm run lint`（即 `tsc --noEmit`）在 Task 1 结束时通过（Task 2 会完全重写该文件）：

```typescript
// Minimal stub — Task 2 will replace with the full page implementation.
export const SyncLogsPage = () => <div>TODO</div>;
```

理由：Task 1 Step 5 在 `App.tsx` 中 `import { SyncLogsPage } from "./pages/SyncLogsPage"`，若文件不存在 tsc 编译失败，Task 1 `<verify>` 的 `npm run lint` 就无法通过。该 stub 是 interface-first 策略：先让契约点（路由注册）编译成立，Task 2 填充实现后覆盖。

**避免与原因：**
- getSyncLogs 必须用 overload/联合参数保持向后兼容 — AttendanceManagement 既有调用不能破坏（Wave 4 不改它）
- downloadUnmatchedCsv 使用 `document.body.appendChild(link)` + `link.remove()` — 部分浏览器（Safari）需 link 挂到 DOM 才能触发 download；`URL.revokeObjectURL` 避免内存泄漏
- CSS 新 token 紧邻既有同类 token — 不在文件末尾乱堆
- 菜单项 icon 用 'file-text'（与「审计日志」同组同图标，HR 心智一致）
- 绝对不加菜单到 manager / employee（Pitfall I + 合规）
- SyncLogsPage 路由挂在与 AttendanceManagementPage 同级的 admin+hrbp ProtectedRoute 块内（非 admin-only 的 feishu-config 块）
  </action>
  <verify>
    <automated>cd frontend && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - grep `export type SyncLogSyncType =` in frontend/src/types/api.ts returns 1 match
    - grep `export type SyncLogStatus = 'running' \| 'success' \| 'partial' \| 'failed'` in frontend/src/types/api.ts returns 1 match
    - grep `mapping_failed_count: number;` in frontend/src/types/api.ts returns 1 match
    - grep `export async function downloadUnmatchedCsv\(logId: string\): Promise<void>` in frontend/src/services/feishuService.ts returns 1 match
    - grep `--color-violet: #722ED1;` in frontend/src/index.css returns 1 match
    - grep `--color-violet-bg: #F5EBFA;` in frontend/src/index.css returns 1 match
    - grep `href: '/feishu/sync-logs'` in frontend/src/utils/roleAccess.ts returns at least 2 matches (admin + hrbp)
    - grep `SyncLogsPage` in frontend/src/App.tsx returns at least 2 matches (import + Route)
    - grep `path="/feishu/sync-logs"` in frontend/src/App.tsx returns 1 match (under admin+hrbp ProtectedRoute)
    - `cd frontend && npm run lint` exits with code 0 (no tsc errors)
    - File `frontend/src/pages/SyncLogsPage.tsx` exists (stub form acceptable — Task 2 overwrites)
    - grep `export const SyncLogsPage` in frontend/src/pages/SyncLogsPage.tsx returns 1 match (stub or full impl)
  </acceptance_criteria>
  <done>
    - 类型 / service / CSS token / 菜单 / 路由 / 页面 stub 全部就位
    - `frontend/src/pages/SyncLogsPage.tsx` 以最小 stub 形式存在（`export const SyncLogsPage = () => <div>TODO</div>`），Task 2 将完全重写
    - tsc 编译通过（App.tsx 的 SyncLogsPage import 已能解析）
  </done>
</task>

<task type="auto">
  <name>Task 2: 实现 SyncLogsPage + 6 个子组件（SyncLogsTabBar / CountersBadgeCluster / StatusBadge / SyncLogRow / SyncLogDetailDrawer / SyncLogsEmptyState）</name>
  <files>frontend/src/pages/SyncLogsPage.tsx, frontend/src/components/feishu-sync-logs/SyncLogsTabBar.tsx, frontend/src/components/feishu-sync-logs/CountersBadgeCluster.tsx, frontend/src/components/feishu-sync-logs/StatusBadge.tsx, frontend/src/components/feishu-sync-logs/SyncLogRow.tsx, frontend/src/components/feishu-sync-logs/SyncLogDetailDrawer.tsx, frontend/src/components/feishu-sync-logs/SyncLogsEmptyState.tsx</files>
  <read_first>
    - frontend/src/pages/AttendanceManagement.tsx — SyncLogRead 消费模式、时间戳格式、CSV 按钮禁用态
    - frontend/src/components/attendance/SyncStatusCard.tsx — StatusBadge 视觉参考（4 色 + spinner）
    - frontend/src/pages/EligibilityManagementPage.tsx — Tab 下划线激活态样式（border-bottom: 2px solid var(--color-primary)）
    - frontend/src/index.css — `.surface` / `.section-head` / `.section-title` / `.chip-button` / `.page-title` / `.eyebrow` / `.page-desc` 语义 class 完整清单
    - frontend/src/services/feishuService.ts — Task 1 落地后的 getSyncLogs / downloadUnmatchedCsv 签名
    - frontend/src/types/api.ts — Task 1 落地后的 SyncLogRead / SyncLogSyncType / SyncLogStatus
    - .planning/phases/31-feishu-sync-observability/31-UI-SPEC.md — 全部 6 维度（Copywriting / Visuals / Color / Typography / Spacing / Registry Safety）
    - .planning/phases/31-feishu-sync-observability/31-CONTEXT.md §D-05 / D-06 / D-07 / D-08 / D-09
  </read_first>
  <action>
严格按 UI-SPEC 的 Component Inventory 和 Copywriting Contract 实现 7 个文件（1 个页面 + 6 个组件）。每个文件以 `import ...` 起，纯 TypeScript + React 18.3.1，不引入新依赖。

**Step 1:** `frontend/src/components/feishu-sync-logs/SyncLogsTabBar.tsx`：

```typescript
import type { SyncLogSyncType } from '../../types/api';

export type TabKey = 'all' | SyncLogSyncType;

interface SyncLogsTabBarProps {
  activeTab: TabKey;
  onChange: (tab: TabKey) => void;
}

const TAB_ITEMS: { key: TabKey; label: string }[] = [
  { key: 'all',                label: '全部' },
  { key: 'attendance',         label: '考勤' },
  { key: 'performance',        label: '绩效' },
  { key: 'salary_adjustments', label: '薪调' },
  { key: 'hire_info',          label: '入职信息' },
  { key: 'non_statutory_leave',label: '社保假勤' },
];

export function SyncLogsTabBar({ activeTab, onChange }: SyncLogsTabBarProps) {
  return (
    <div role="tablist" className="flex items-center gap-2 border-b border-[var(--color-border)]">
      {TAB_ITEMS.map((item) => {
        const active = item.key === activeTab;
        return (
          <button
            key={item.key}
            type="button"
            role="tab"
            aria-selected={active}
            onClick={() => onChange(item.key)}
            className="px-3 py-2 text-sm font-medium transition-colors"
            style={{
              color: active ? 'var(--color-primary)' : 'var(--color-steel)',
              borderBottom: active
                ? '2px solid var(--color-primary)'
                : '2px solid transparent',
            }}
          >
            {item.label}
          </button>
        );
      })}
    </div>
  );
}
```

**Step 2:** `frontend/src/components/feishu-sync-logs/CountersBadgeCluster.tsx`：

```typescript
export type BadgeKey = 'success' | 'updated' | 'unmatched' | 'mapping_failed' | 'failed';

interface CountersBadgeClusterProps {
  success: number;
  updated: number;
  unmatched: number;
  mappingFailed: number;
  failed: number;
  onBadgeClick?: (key: BadgeKey) => void;
}

interface BadgeDef {
  key: BadgeKey;
  label: string;
  color: string;
  bg: string;
}

const BADGES: BadgeDef[] = [
  { key: 'success',        label: '成功',     color: 'var(--color-success)', bg: 'var(--color-success-bg)' },
  { key: 'updated',        label: '更新',     color: 'var(--color-info)',    bg: 'var(--color-info-bg)' },
  { key: 'unmatched',      label: '未匹配',   color: 'var(--color-warning)', bg: 'var(--color-warning-bg)' },
  { key: 'mapping_failed', label: '映射失败', color: 'var(--color-violet)',  bg: 'var(--color-violet-bg)' },
  { key: 'failed',         label: '写库失败', color: 'var(--color-danger)',  bg: 'var(--color-danger-bg)' },
];

export function CountersBadgeCluster({
  success, updated, unmatched, mappingFailed, failed, onBadgeClick,
}: CountersBadgeClusterProps) {
  const values: Record<BadgeKey, number> = {
    success, updated, unmatched,
    mapping_failed: mappingFailed,
    failed,
  };
  return (
    <div className="flex items-center gap-2">
      {BADGES.map((badge) => {
        const value = values[badge.key];
        const muted = value === 0;
        const style = muted
          ? { color: 'var(--color-placeholder)', background: 'var(--color-bg-subtle, #F5F6F8)' }
          : { color: badge.color, background: badge.bg };
        return (
          <button
            key={badge.key}
            type="button"
            role="button"
            tabIndex={0}
            aria-label={`查看${badge.label} ${value} 条明细`}
            title={muted ? `${badge.label}类计数为 0` : undefined}
            onClick={() => !muted && onBadgeClick?.(badge.key)}
            className="flex items-center gap-1 rounded px-2 py-1 text-xs font-semibold transition-opacity"
            style={{ ...style, cursor: muted ? 'default' : 'pointer', opacity: muted ? 0.7 : 1 }}
          >
            <span>{badge.label}</span>
            <span>{value}</span>
          </button>
        );
      })}
    </div>
  );
}
```

**Step 3:** `frontend/src/components/feishu-sync-logs/StatusBadge.tsx`（4 色 + running spinner，样式与 SyncStatusCard.tsx 像素级对齐）：

```typescript
import type { SyncLogStatus } from '../../types/api';

interface StatusBadgeProps {
  status: SyncLogStatus;
}

const STATUS_MAP: Record<SyncLogStatus, { label: string; color: string; bg: string }> = {
  success:  { label: '成功',       color: 'var(--color-success)', bg: 'var(--color-success-bg)' },
  partial:  { label: '部分成功',   color: 'var(--color-warning)', bg: 'var(--color-warning-bg)' },
  failed:   { label: '失败',       color: 'var(--color-danger)',  bg: 'var(--color-danger-bg)' },
  running:  { label: '同步中',     color: 'var(--color-info)',    bg: 'var(--color-info-bg)' },
};

export function StatusBadge({ status }: StatusBadgeProps) {
  const { label, color, bg } = STATUS_MAP[status];
  return (
    <span
      className="inline-flex items-center gap-1 rounded px-2 py-0.5 text-xs font-semibold"
      style={{ color, background: bg }}
    >
      {status === 'running' ? (
        <span
          aria-hidden
          className="inline-block h-3 w-3 animate-spin rounded-full border-2"
          style={{ borderColor: color, borderTopColor: 'transparent' }}
        />
      ) : null}
      {label}
    </span>
  );
}
```

**Step 4:** `frontend/src/components/feishu-sync-logs/SyncLogRow.tsx`：

```typescript
import type { SyncLogRead, SyncLogSyncType } from '../../types/api';
import { CountersBadgeCluster } from './CountersBadgeCluster';
import { StatusBadge } from './StatusBadge';

const SYNC_TYPE_LABEL: Record<SyncLogSyncType, string> = {
  attendance:          '考勤',
  performance:         '绩效',
  salary_adjustments:  '薪调',
  hire_info:           '入职信息',
  non_statutory_leave: '社保假勤',
};

function formatTime(iso: string): string {
  const d = new Date(iso);
  const pad = (n: number) => String(n).padStart(2, '0');
  return `${d.getFullYear()}-${pad(d.getMonth() + 1)}-${pad(d.getDate())} ${pad(d.getHours())}:${pad(d.getMinutes())}`;
}

function formatDuration(startedAt: string, finishedAt: string | null): string {
  if (!finishedAt) return '—';
  const start = new Date(startedAt).getTime();
  const end = new Date(finishedAt).getTime();
  const seconds = Math.max(0, Math.round((end - start) / 1000));
  if (seconds < 60) return `${seconds}s`;
  const minutes = Math.floor(seconds / 60);
  return `${minutes}m${seconds % 60}s`;
}

interface SyncLogRowProps {
  log: SyncLogRead;
  onOpenDetail: (log: SyncLogRead) => void;
  onDownloadCsv: (log: SyncLogRead) => void;
  isDownloadingCsv?: boolean;
}

export function SyncLogRow({ log, onOpenDetail, onDownloadCsv, isDownloadingCsv }: SyncLogRowProps) {
  const canDownload = log.unmatched_count > 0;
  return (
    <tr style={{ borderBottom: '1px solid var(--color-border)' }}>
      <td className="px-3 py-3 text-sm" style={{ color: 'var(--color-ink)' }}>
        {SYNC_TYPE_LABEL[log.sync_type]}
      </td>
      <td className="px-3 py-3 text-sm">
        <StatusBadge status={log.status} />
      </td>
      <td className="px-3 py-3 text-sm" style={{ color: 'var(--color-steel)' }}>
        {log.sync_type === 'attendance' ? (
          <span
            className="inline-flex items-center rounded px-2 py-0.5 text-xs font-semibold"
            style={{ color: 'var(--color-ink)', background: 'var(--color-bg-subtle, #F5F6F8)' }}
          >
            {log.mode === 'incremental' ? '增量' : '全量'}
          </span>
        ) : (
          <span style={{ color: 'var(--color-placeholder)' }}>—</span>
        )}
      </td>
      <td className="px-3 py-3">
        <CountersBadgeCluster
          success={log.synced_count}
          updated={log.updated_count}
          unmatched={log.unmatched_count}
          mappingFailed={log.mapping_failed_count}
          failed={log.failed_count}
          onBadgeClick={() => onOpenDetail(log)}
        />
      </td>
      <td className="px-3 py-3 text-sm" style={{ color: 'var(--color-steel)' }}>
        {formatTime(log.started_at)}
      </td>
      <td className="px-3 py-3 text-sm" style={{ color: 'var(--color-steel)' }}>
        {formatDuration(log.started_at, log.finished_at)}
      </td>
      <td className="px-3 py-3 text-sm" style={{ color: 'var(--color-steel)' }}>
        {log.triggered_by ?? '系统'}
      </td>
      <td className="px-3 py-3 text-sm">
        <button
          type="button"
          onClick={() => canDownload && onDownloadCsv(log)}
          disabled={!canDownload || isDownloadingCsv}
          title={!canDownload ? '本次同步无未匹配工号，无需下载' : undefined}
          className="mr-3 underline"
          style={{
            color: canDownload ? 'var(--color-primary)' : 'var(--color-placeholder)',
            cursor: canDownload ? 'pointer' : 'not-allowed',
          }}
        >
          下载未匹配工号 CSV
        </button>
        <button
          type="button"
          onClick={() => onOpenDetail(log)}
          className="underline"
          style={{ color: 'var(--color-primary)' }}
        >
          查看详情
        </button>
      </td>
    </tr>
  );
}
```

**Step 5:** `frontend/src/components/feishu-sync-logs/SyncLogDetailDrawer.tsx` — 480px 宽，右侧滑入，role='dialog' aria-modal；Esc 关闭：

```typescript
import { useEffect } from 'react';
import type { SyncLogRead } from '../../types/api';

interface SyncLogDetailDrawerProps {
  open: boolean;
  log: SyncLogRead | null;
  onClose: () => void;
}

export function SyncLogDetailDrawer({ open, log, onClose }: SyncLogDetailDrawerProps) {
  useEffect(() => {
    if (!open) return;
    const onKey = (e: KeyboardEvent) => {
      if (e.key === 'Escape') onClose();
    };
    window.addEventListener('keydown', onKey);
    return () => window.removeEventListener('keydown', onKey);
  }, [open, onClose]);

  if (!open || !log) return null;

  const logIdShort = log.id.slice(0, 8);

  return (
    <>
      <div
        onClick={onClose}
        className="fixed inset-0 z-40"
        style={{ background: 'rgba(0,0,0,0.4)' }}
      />
      <aside
        role="dialog"
        aria-modal="true"
        aria-labelledby={`drawer-title-${log.id}`}
        className="fixed right-0 top-0 z-50 h-full overflow-y-auto shadow-xl"
        style={{ width: '480px', background: 'var(--color-bg-surface, #FFFFFF)' }}
      >
        <div
          className="px-5 py-4"
          style={{ borderBottom: '1px solid var(--color-border)', borderLeft: '4px solid var(--color-primary)' }}
        >
          <h2 id={`drawer-title-${log.id}`} className="text-base font-semibold" style={{ color: 'var(--color-ink)' }}>
            同步明细 #{logIdShort}
          </h2>
        </div>
        <div className="px-5 py-4 space-y-4 text-sm" style={{ color: 'var(--color-ink)' }}>
          {log.leading_zero_fallback_count > 0 && (
            <div style={{ color: 'var(--color-warning)' }}>
              {log.leading_zero_fallback_count} 条记录通过前导零容忍匹配成功，建议排查飞书源数据格式
            </div>
          )}
          {log.error_message && (
            <div>
              <div className="text-xs font-semibold" style={{ color: 'var(--color-steel)' }}>错误信息</div>
              <pre className="whitespace-pre-wrap text-sm" style={{ color: 'var(--color-danger)' }}>
                {log.error_message}
              </pre>
            </div>
          )}
          <div>
            <div className="text-xs font-semibold" style={{ color: 'var(--color-steel)' }}>未匹配工号</div>
            {log.unmatched_employee_nos && log.unmatched_employee_nos.length > 0 ? (
              <ul className="mt-1 list-disc pl-5">
                {log.unmatched_employee_nos.map((no) => (
                  <li key={no}>{no}</li>
                ))}
              </ul>
            ) : (
              <div style={{ color: 'var(--color-steel)' }}>本次同步无未匹配工号</div>
            )}
          </div>
          {log.status === 'partial' && (
            <div className="text-xs" style={{ color: 'var(--color-steel)' }}>
              同步完成但存在未匹配 / 映射失败 / 写库失败条目，请点击详情查看根因。
            </div>
          )}
        </div>
      </aside>
    </>
  );
}
```

**Step 6:** `frontend/src/components/feishu-sync-logs/SyncLogsEmptyState.tsx`：

```typescript
import { Link } from 'react-router-dom';

export function SyncLogsEmptyState() {
  return (
    <div className="flex flex-col items-center justify-center py-12 text-center">
      <div className="text-base font-semibold" style={{ color: 'var(--color-ink)' }}>暂无同步日志</div>
      <p className="mt-2 max-w-md text-sm" style={{ color: 'var(--color-steel)' }}>
        HR 触发飞书同步后，每次执行的结果会在此列出。可前往「飞书配置」页面手动触发一次同步。
      </p>
      <Link
        to="/feishu-config"
        className="mt-4 inline-flex items-center rounded border px-3 py-1.5 text-sm"
        style={{ borderColor: 'var(--color-border)', color: 'var(--color-ink)' }}
      >
        前往飞书配置
      </Link>
    </div>
  );
}
```

**Step 7:** `frontend/src/pages/SyncLogsPage.tsx` — 页面壳组装：

```typescript
import { useCallback, useEffect, useState } from 'react';
import { downloadUnmatchedCsv, getSyncLogs } from '../services/feishuService';
import type { SyncLogRead, SyncLogSyncType } from '../types/api';
import { CountersBadgeCluster } from '../components/feishu-sync-logs/CountersBadgeCluster';
import { StatusBadge } from '../components/feishu-sync-logs/StatusBadge';
import { SyncLogRow } from '../components/feishu-sync-logs/SyncLogRow';
import { SyncLogsEmptyState } from '../components/feishu-sync-logs/SyncLogsEmptyState';
import { SyncLogsTabBar, type TabKey } from '../components/feishu-sync-logs/SyncLogsTabBar';
import { SyncLogDetailDrawer } from '../components/feishu-sync-logs/SyncLogDetailDrawer';

const PAGE_SIZE = 20;

export function SyncLogsPage() {
  const [activeTab, setActiveTab] = useState<TabKey>('all');
  const [page, setPage] = useState(1);
  const [logs, setLogs] = useState<SyncLogRead[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);
  const [drawerLog, setDrawerLog] = useState<SyncLogRead | null>(null);
  const [downloadingId, setDownloadingId] = useState<string | null>(null);

  const fetchLogs = useCallback(async () => {
    setLoading(true);
    setError(null);
    try {
      const syncType: SyncLogSyncType | undefined =
        activeTab === 'all' ? undefined : activeTab;
      const data = await getSyncLogs({ syncType, page, pageSize: PAGE_SIZE });
      setLogs(data);
    } catch (err) {
      setError(err instanceof Error ? err.message : '加载失败');
    } finally {
      setLoading(false);
    }
  }, [activeTab, page]);

  useEffect(() => {
    void fetchLogs();
  }, [fetchLogs]);

  const handleTabChange = (next: TabKey) => {
    setActiveTab(next);
    setPage(1);
  };

  const handleDownloadCsv = async (log: SyncLogRead) => {
    setDownloadingId(log.id);
    try {
      await downloadUnmatchedCsv(log.id);
    } catch (err) {
      setError(err instanceof Error ? `未匹配工号导出失败：${err.message}` : '未匹配工号导出失败');
    } finally {
      setDownloadingId(null);
    }
  };

  return (
    <main className="app-main" style={{ padding: '20px 24px 32px' }}>
      <header className="section-head mb-4">
        <div>
          <div className="eyebrow">飞书集成 / SYNC OBSERVABILITY</div>
          <h1 className="page-title">飞书同步日志</h1>
          <p className="page-desc">
            查看每次飞书同步的结果与明细，可按类型筛选、下载未匹配工号 CSV 自助诊断。
          </p>
        </div>
        <button
          type="button"
          onClick={() => void fetchLogs()}
          className="chip-button"
        >
          刷新列表
        </button>
      </header>

      <SyncLogsTabBar activeTab={activeTab} onChange={handleTabChange} />

      <section className="surface mt-4">
        {error && (
          <div role="alert" className="mb-2 text-sm" style={{ color: 'var(--color-danger)' }}>
            同步日志加载失败：{error}。请刷新重试，或联系管理员确认后端服务状态。
          </div>
        )}
        {loading && <div className="p-4 text-sm" style={{ color: 'var(--color-steel)' }}>正在加载同步日志...</div>}
        {!loading && logs.length === 0 && !error && <SyncLogsEmptyState />}
        {!loading && logs.length > 0 && (
          <table className="w-full text-left" aria-live="polite">
            <thead>
              <tr style={{ borderBottom: '1px solid var(--color-border)' }}>
                <th className="px-3 py-2 text-sm font-semibold" style={{ color: 'var(--color-steel)' }}>同步类型</th>
                <th className="px-3 py-2 text-sm font-semibold" style={{ color: 'var(--color-steel)' }}>状态</th>
                <th className="px-3 py-2 text-sm font-semibold" style={{ color: 'var(--color-steel)' }}>模式</th>
                <th className="px-3 py-2 text-sm font-semibold" style={{ color: 'var(--color-steel)' }}>计数</th>
                <th className="px-3 py-2 text-sm font-semibold" style={{ color: 'var(--color-steel)' }}>触发时间</th>
                <th className="px-3 py-2 text-sm font-semibold" style={{ color: 'var(--color-steel)' }}>耗时</th>
                <th className="px-3 py-2 text-sm font-semibold" style={{ color: 'var(--color-steel)' }}>触发人</th>
                <th className="px-3 py-2 text-sm font-semibold" style={{ color: 'var(--color-steel)' }}>操作</th>
              </tr>
            </thead>
            <tbody>
              {logs.map((log) => (
                <SyncLogRow
                  key={log.id}
                  log={log}
                  onOpenDetail={setDrawerLog}
                  onDownloadCsv={handleDownloadCsv}
                  isDownloadingCsv={downloadingId === log.id}
                />
              ))}
            </tbody>
          </table>
        )}
        {logs.length > 0 && (
          <nav className="mt-4 flex items-center justify-center gap-4 text-sm" style={{ color: 'var(--color-steel)' }}>
            <button
              type="button"
              disabled={page === 1}
              onClick={() => setPage((p) => Math.max(1, p - 1))}
              className="underline"
              style={{ cursor: page === 1 ? 'not-allowed' : 'pointer', opacity: page === 1 ? 0.5 : 1 }}
            >
              &lt; 上一页
            </button>
            <span>第 {page} 页</span>
            <button
              type="button"
              disabled={logs.length < PAGE_SIZE}
              onClick={() => setPage((p) => p + 1)}
              className="underline"
              style={{ cursor: logs.length < PAGE_SIZE ? 'not-allowed' : 'pointer', opacity: logs.length < PAGE_SIZE ? 0.5 : 1 }}
            >
              下一页 &gt;
            </button>
          </nav>
        )}
      </section>

      <SyncLogDetailDrawer open={drawerLog !== null} log={drawerLog} onClose={() => setDrawerLog(null)} />
    </main>
  );
}
```

**避免与原因：**
- 不引入 React Query / SWR（项目无此模式；`useEffect + useState` 是既定模式）
- 不在 Tab 切换时做乐观更新 — 每次 fetch 单独 loading 态
- `aria-live="polite"` 在 table 元素上，告知 refetch 状态变化（可访问性）
- `aria-modal="true"` + Esc 关闭抽屉（a11y）
- CSV 下载失败用内联 error 态（toast 组件非本 phase 必须，且项目暂无统一 toast infra）
- 按钮文本 100% 与 UI-SPEC Copywriting Contract 对齐（「下载未匹配工号 CSV」「查看详情」「前往飞书配置」等）
- `chip-button` 已有 class 来自 index.css，不重造
- 不用 tooltip 库 — 用原生 `title` 属性（UI-SPEC 要求说明「为何禁用」）
- 不抽离 useReducer — useState 足够；4 条相关状态（tab/page/logs/loading）可独立管理
- 不做虚拟滚动（page_size=20 数据量小）
- 不给时间戳加秒（UI-SPEC 明确 `2026-04-21 14:32` 格式）
- 「模式」列 D-04 采用行内按 sync_type 条件渲染（而非整列隐藏）— 避免 CSS grid 列数动态重排，且保留整列供 HR 一眼核对 attendance 行的 full/incremental
- ModeBadge 不抽独立组件 — 就地用 `<span>` + Tailwind + CSS token 渲染（与 StatusBadge 形态一致），减少组件数量（UI-SPEC Registry Safety 允许）
- 日期解析不加 timezone 处理（后端返回 ISO8601 UTC，new Date() 自动本地时区显示）
- 分页边界：第一页 `< 上一页` 禁用；当前页数据量 < PAGE_SIZE 时 `下一页 >` 禁用（简单启发式：数据不足一页一定是末页）
  </action>
  <verify>
    <automated>cd frontend && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - 7 个文件全部存在（`ls frontend/src/pages/SyncLogsPage.tsx frontend/src/components/feishu-sync-logs/*.tsx` 返回 7 行）
    - grep `const TAB_ITEMS: \{ key: TabKey; label: string \}\[\] = \[` in SyncLogsTabBar.tsx returns 1 match
    - grep `'全部'` in SyncLogsTabBar.tsx returns 1 match
    - grep `'考勤'` in SyncLogsTabBar.tsx returns 1 match
    - grep `'绩效'` in SyncLogsTabBar.tsx returns 1 match
    - grep `'薪调'` in SyncLogsTabBar.tsx returns 1 match
    - grep `'入职信息'` in SyncLogsTabBar.tsx returns 1 match
    - grep `'社保假勤'` in SyncLogsTabBar.tsx returns 1 match
    - grep `var\(--color-violet\)` in CountersBadgeCluster.tsx returns at least 1 match
    - grep `mapping_failed` in CountersBadgeCluster.tsx returns at least 1 match
    - grep `role="dialog"` in SyncLogDetailDrawer.tsx returns 1 match
    - grep `aria-modal="true"` in SyncLogDetailDrawer.tsx returns 1 match
    - grep `width: '480px'` in SyncLogDetailDrawer.tsx returns 1 match
    - grep `下载未匹配工号 CSV` in SyncLogRow.tsx returns 1 match
    - grep `本次同步无未匹配工号，无需下载` in SyncLogRow.tsx returns 1 match
    - grep `log.sync_type === 'attendance'` in SyncLogRow.tsx returns 1 match (D-04 条件渲染)
    - grep `模式` in SyncLogsPage.tsx returns 1 match (thead 模式 column header)
    - grep `飞书同步日志` in SyncLogsPage.tsx returns 1 match
    - grep `'飞书集成 / SYNC OBSERVABILITY'` in SyncLogsPage.tsx returns 1 match
    - `cd frontend && npm run lint` exits with code 0
    - `cd frontend && npm run build` succeeds
  </acceptance_criteria>
  <done>
    - 7 个文件全部落地，严格遵循 UI-SPEC 的 Copywriting / Color / Typography / Spacing / Visuals / Registry Safety 六维度
    - 「模式」列按 D-04 在 SyncLogRow.tsx 按 sync_type === 'attendance' 条件渲染（attendance 显示 full/incremental badge，其他显示 —）；SyncLogsPage.tsx <thead> 含「模式」列位于「状态」与「计数」之间
    - tsc --noEmit 通过
    - 生产 build 通过
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 [Checkpoint]: 人工 UAT — UI-SPEC 六维度 + VALIDATION.md Manual-Only 清单</name>
  <what-built>
Claude 已完整自动化以下前后端能力：
- Plan 01: FeishuSyncLog 扩展 sync_type + mapping_failed_count + Alembic 迁移
- Plan 02: _with_sync_log helper + _SyncCounters + 五个 sync 方法重构 + per-sync_type 锁
- Plan 03: GET /sync-logs 过滤+分页 + CSV 下载端点 + trigger_sync 409 sync_type 语义 + Celery task alias
- Plan 04 Task 1: TypeScript 类型 / service / CSS token / 菜单 / 路由注册
- Plan 04 Task 2: SyncLogsPage + 6 个子组件

现需 HR 视角人工验证页面表现是否符合 UI-SPEC 与 Success Criteria。
  </what-built>

  <how-to-verify>
**准备环境：**
1. 后端：`uvicorn backend.app.main:app --reload --port 8011`（已先行运行 `alembic upgrade head`）
2. 前端：`cd frontend && npm run dev`（默认 5174 端口）
3. Celery worker：`celery -A backend.app.celery_app worker --loglevel=info`（Redis 已运行）
4. 至少准备 4 个测试账号：admin / hrbp / manager / employee（密码 pw 或项目既定测试账号）
5. 手动或通过 Celery task 生成若干条测试日志：每种 sync_type 各 2-3 条，含一条 `status='partial'`（手工更新 DB 或跑真实同步）

***

**Checklist A — 路由与菜单角色守门（Pitfall I / D-05）：**

- [ ] A1. admin 登录 → AppShell 左侧菜单「系统管理」分组下出现「同步日志」条目
- [ ] A2. hrbp 登录 → 同上
- [ ] A3. manager 登录 → 菜单中**不**出现「同步日志」条目
- [ ] A4. employee 登录 → 菜单中**不**出现「同步日志」条目
- [ ] A5. employee 手动访问 URL `http://localhost:5174/feishu/sync-logs` → 被 `<ProtectedRoute>` 重定向到无权限 / 登录页，不短暂展示页面骨架
- [ ] A6. manager 手动访问 URL → 同上

***

**Checklist B — Tab 顺序与查询参数（D-06）：**

- [ ] B1. admin 打开页面 → Tab 列顺序严格为「全部 · 考勤 · 绩效 · 薪调 · 入职信息 · 社保假勤」6 项
- [ ] B2. 默认激活 Tab 为「全部」
- [ ] B3. 点击「绩效」Tab → Network 面板确认 `GET /api/v1/feishu/sync-logs?sync_type=performance&page=1&page_size=20`
- [ ] B4. 点击「考勤」→ 参数 `sync_type=attendance`
- [ ] B5. 点击「全部」→ 参数不含 `sync_type`
- [ ] B6. Tab 激活态下划线（2px var(--color-primary)）+ 字色 primary，与 EligibilityManagementPage 像素级一致

***

**Checklist C — 五色 Badge Cluster（D-07）：**

- [ ] C1. 同一行含非零 success + updated + unmatched + mapping_failed + failed 的日志，五色分段 badge 按顺序展示：绿（success）→蓝（updated）→橙（unmatched）→紫（mapping_failed）→红（failed）
- [ ] C2. 某类计数 === 0 时 badge 置灰（`--color-placeholder` 文字 + `--color-bg-subtle` 底）
- [ ] C3. 非零 badge 可点击，鼠标 hover 不报错
- [ ] C4. 紫色 (#722ED1) 在 mapping_failed 段正确显示，不与 primary 蓝冲突
- [ ] C5. badge 数值 vs 标签排版（标签在前、数值在后）与 UI-SPEC Visuals 描述一致

***

**Checklist D — 顶层 4 色 Status 徽章（D-09）：**

- [ ] D1. status='success' 日志 → 绿色「成功」徽章
- [ ] D2. status='partial' 日志 → 橙色「部分成功」徽章
- [ ] D3. status='failed' 日志 → 红色「失败」徽章
- [ ] D4. status='running' 日志 → 蓝色「同步中」徽章 + 圆形旋转 spinner（0.8s linear infinite）
- [ ] D5. 页面无 `status='rejected'` 徽章出现（D-16：409 不写 log）

***

**Checklist E — CSV 下载（D-08 / IMPORT-04）：**

- [ ] E1. 某行 unmatched_count > 0 → Action 列「下载未匹配工号 CSV」按钮启用，primary 文字色
- [ ] E2. 某行 unmatched_count === 0 → 按钮禁用，灰色文字，hover 显示 tooltip「本次同步无未匹配工号，无需下载」
- [ ] E3. 点击启用按钮 → 浏览器下载文件，文件名严格匹配 `sync-log-{log_id}-unmatched.csv`
- [ ] E4. 打开 CSV → 第一行 `employee_no`，后续 ≤ 20 行；Excel / 记事本都能正确显示中文（UTF-8）
- [ ] E5. 对某条 unmatched_employee_nos 含 30+ 工号的日志下载 → CSV 只含前 20 行
- [ ] E6. 用 employee 账号在 Network 面板手动请求 `/api/v1/feishu/sync-logs/{id}/unmatched.csv` → 收到 403
- [ ] E7. 用 admin 账号请求不存在的 log_id → 收到 404

***

**Checklist F — 详情抽屉（D-07 抽屉内容）：**

- [ ] F1. 点击任一非零 badge → 右侧 480px 宽抽屉从右滑入，遮罩 `rgba(0,0,0,0.4)`
- [ ] F2. 抽屉标题「同步明细 #{log_id_short}」
- [ ] F3. 抽屉展示完整 unmatched_employee_nos 列表（不限 20 上限）
- [ ] F4. error_message 非空时以 danger 文字色展示
- [ ] F5. leading_zero_fallback_count > 0 时显示黄字「{N} 条记录通过前导零容忍匹配成功，建议排查飞书源数据格式」
- [ ] F6. 按 Esc 键关闭抽屉
- [ ] F7. 点击遮罩关闭抽屉
- [ ] F8. 抽屉含 `role="dialog"` + `aria-modal="true"` + `aria-labelledby`（浏览器 DevTools Accessibility 面板验证）

***

**Checklist G — SC4: 双触发与 per-sync_type 锁（D-15 / D-16）：**

- [ ] G1. admin 点击「触发考勤同步」（通过 AttendanceManagement 页面的现有按钮）→ 第一次触发 → UI 显示「同步中」
- [ ] G2. 锁定期内（第一次尚未完成）再次点击 → 收到 409 错误 + toast 或内联提示「该类型同步正在进行中」
- [ ] G3. 首次同步完成后 → `/feishu/sync-logs` 页面 refresh → 看到第 1 条 log，状态 success/partial
- [ ] G4. 再次触发 → 完成后 → 看到第 2 条独立 log（不是更新旧记录）
- [ ] G5. 数据库查询 `SELECT count(*), sync_type FROM feishu_sync_logs WHERE sync_type='attendance' GROUP BY sync_type` 显示计数增加 2（每次触发各 1 条）
- [ ] G6. 409 发生那次 Network 面板响应 JSON 含 `{error: 'sync_in_progress', sync_type: 'attendance', message: ...}`

***

**Checklist H — 空态 + 加载态 + 错误态（UI-SPEC Copywriting）：**

- [ ] H1. 数据库清空 feishu_sync_logs 表后刷新页面 → 显示「暂无同步日志」空态 + CTA「前往飞书配置」
- [ ] H2. 点击「前往飞书配置」→ 导航到 `/feishu-config`
- [ ] H3. 页面加载中 → 显示「正在加载同步日志...」文字
- [ ] H4. 手动停止后端 → 刷新列表 → 显示错误文案「同步日志加载失败：{error}。请刷新重试，或联系管理员确认后端服务状态。」

***

**Checklist I — Dimension 1-6 UI-SPEC 验收（统一签字）：**

- [ ] I1. Copywriting：所有文案（Tab 标签/列头/按钮/禁用 tooltip/错误态/空态）与 UI-SPEC Copywriting Contract 完全一致
- [ ] I2. Visuals：页面整体与 AttendanceManagement 风格一致（surface 卡片、列表行高、badge 位置）
- [ ] I3. Color：五色 badge + 4 色 status 按 UI-SPEC Color 章节；新增 --color-violet (#722ED1) 生效
- [ ] I4. Typography：页面标题 20px/600、Tab/列头 14-15px/600、body 14px/400、eyebrow 11-12px/600
- [ ] I5. Spacing：表格 px-3 py-3 行内；section-head mb-4；抽屉 px-5 py-4；卡片外层 20/16 padding
- [ ] I6. Registry Safety：不含任何 shadcn 组件（grep `@/components/ui`、`from '@radix-ui'` 在 SyncLogsPage/components/feishu-sync-logs/* 应为 0 条）

***

**验证完成后签字：**

- 若全部通过（含 A1-A6 / B1-B6 / C1-C5 / D1-D5 / E1-E7 / F1-F8 / G1-G6 / H1-H4 / I1-I6），在回复中贴出几张关键截图（至少：admin 主界面、hrbp 主界面、employee 无菜单、详情抽屉、CSV 下载对话框），并注明「approved」
- 若任何一项失败：详细列出失败项 + 观察到的实际行为 + 浏览器/Network 截图，Claude 将进入 Plan 04 revision 流程修复
  </how-to-verify>

  <resume-signal>
Type "approved" if all checklists pass; otherwise describe which items failed with screenshots/console logs. On failure, Claude will enter revision mode for the impacted sub-tasks.
  </resume-signal>

  <files>frontend/src/pages/SyncLogsPage.tsx (no code changes — human verification only)</files>
  <action>Human runs the checklists (A-I) in the <how-to-verify> block above. Claude makes no file changes during this checkpoint.</action>
  <verify>Human signs off on all checklist items; see <resume-signal> above.</verify>
  <done>All UI-SPEC 6 dimensions and VALIDATION.md Manual-Only checklist items (A1-A6, B1-B6, C1-C5, D1-D5, E1-E7, F1-F8, G1-G6, H1-H4, I1-I6) pass human verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/v1/feishu/* | Axios requests carry JWT bearer; untrusted query string params validated server-side (Plan 03) |
| Client-side route guard | `<ProtectedRoute allowedRoles={['admin', 'hrbp']}/>` filters non-HR users; defense in depth alongside backend 403 |
| CSV blob → filesystem | downloadUnmatchedCsv uses URL.createObjectURL + revokeObjectURL; no XSS vector (CSV content parsed only by Excel/text editor) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-21 | Information disclosure | Frontend route `/feishu/sync-logs` accessible by non-HR roles | mitigate | `<ProtectedRoute allowedRoles={['admin', 'hrbp']}/>` nesting in App.tsx (Pitfall I); manual UAT checklist A1-A6 verifies all 4 roles |
| T-31-22 | Information disclosure | SyncLogsPage renders log data before auth check completes | accept | Current ProtectedRoute pattern renders `<Outlet/>` only after auth verification; ratio short (<100ms); no PII in log shell before data fetch |
| T-31-23 | XSS via unmatched_employee_nos in drawer | Tampering / XSS | mitigate | React auto-escapes string interpolation in JSX; employee_no rendered via `{no}` (no dangerouslySetInnerHTML anywhere in new components); CSV filename constructed from log.id (UUID, no XSS chars) |
| T-31-24 | XSS via error_message in drawer | Tampering / XSS | mitigate | React auto-escapes; `<pre>{log.error_message}</pre>` is safe; backend limits error_message to 2000 chars (Plan 02 helper) |
| T-31-25 | CSRF on POST /feishu/sync trigger from drawer | Tampering | mitigate | Drawer is read-only; no state-changing requests made from Phase 31 frontend; existing CSRF protection on /feishu/sync trigger is unchanged |
| T-31-26 | Clickjacking on drawer modal | Elevation of privilege | accept | Project does not set `X-Frame-Options` header for same-origin embedding; drawer is modal within same app, no iframe exposure |
| T-31-27 | localStorage leaks token to sync logs tab | Information disclosure | accept | Existing JWT storage pattern (shared across app); no new storage added by Phase 31 |
| T-31-28 | User pastes arbitrary URL query params | Tampering | mitigate | Query params parsed by `getSyncLogs` via axios param object; backend Pydantic Literal validation returns 422 on invalid sync_type/page/page_size; frontend does not construct raw URL strings |
</threat_model>

<verification>
- `cd frontend && npm run lint` 通过（tsc --noEmit 0 错误）
- `cd frontend && npm run build` 通过
- grep `@/components/ui`、`from '@radix-ui'` 在 Phase 31 新文件中 → 0 结果（shadcn 零依赖契约）
- grep `sync_type` in frontend/src/types/api.ts → 至少 2 处（type 定义 + SyncLogRead 字段）
- 手工 UAT 全绿（Task 3 checkpoint）
- `curl -X GET -H "Authorization: Bearer {admin_token}" "http://localhost:8011/api/v1/feishu/sync-logs"` 返回有效 JSON（后端未被前端改动破坏）
</verification>

<success_criteria>
- IMPORT-03 UI 闭合：HR 在 `/feishu/sync-logs` 看到 5 类同步的日志列表，每行五类计数
- IMPORT-04 UI 闭合：partial 状态 + CSV 下载按钮 + 四类计数分别展示
- SC3（Roadmap SC3）：Tab 切分 + 五色 badge + CSV 下载自助诊断全量落地
- SC4（Roadmap SC4）：双触发产生两条独立 log + 锁定期 409 不写 log 经过人工 UAT 验证
- Pitfall I 闭合：employee/manager 访问 /feishu/sync-logs 被重定向（ProtectedRoute nesting）
- UI-SPEC 6 维度签字（I1-I6）完成
- 无新依赖，无 shadcn；只新增 3 个 CSS token（--color-violet, --color-violet-bg, --color-violet-border）
- Phase 30 既有前端行为不受影响（AttendanceManagement `getSyncLogs(20)` 调用仍然工作）
</success_criteria>

<output>
After completion, create `.planning/phases/31-feishu-sync-observability/31-04-SUMMARY.md` summarizing:
- 7 个新前端组件/页面的清单 + 各自核心职责
- getSyncLogs 兼容性重载（既支持 options，也支持 legacy limit）
- 菜单入口在 admin / hrbp 的位置
- UI-SPEC 6 维度验收结果（附截图链接）
- UAT checklist 执行记录（A-I 全部通过或哪些有 finding）
- Phase 31 收尾：提醒运维在升级前 drain Celery broker 中的 `performance_grades` in-flight tasks（Pitfall H）
- 遗留 TODO：`performance_grades` alias 移除延期到 Phase 32
</output>
