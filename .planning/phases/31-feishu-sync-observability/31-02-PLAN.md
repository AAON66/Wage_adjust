---
phase: 31-feishu-sync-observability
plan: 02
plan_number: 2
type: execute
wave: 2
depends_on: [01]
files_modified:
  - backend/app/services/feishu_service.py
  - backend/tests/test_services/test_feishu_with_sync_log_helper.py
  - backend/tests/test_services/test_feishu_sync_counters_dataclass.py
  - backend/tests/test_services/test_feishu_partial_status_derivation.py
  - backend/tests/test_services/test_feishu_per_sync_type_lock.py
  - backend/tests/test_services/test_feishu_sync_methods_write_log.py
  - backend/tests/test_services/test_feishu_mapping_failed_counter.py
  - backend/tests/test_services/test_feishu_sync_type_whitelist.py
autonomous: true
requirements: [IMPORT-03, IMPORT-04]
requirements_addressed: [IMPORT-03, IMPORT-04]

must_haves:
  truths:
    - "FeishuService._with_sync_log(sync_type, fn, *, triggered_by=None, mode='full', **kwargs) helper 存在：独立 SessionLocal() 创建 running log → 调业务 fn → 独立 SessionLocal() 写终态（D-10 / D-13）"
    - "_SyncCounters 是 @dataclass(frozen=True, slots=True)，含 8 个字段 (success / updated / unmatched / mapping_failed / failed / leading_zero_fallback / total_fetched / unmatched_nos)（D-11）"
    - "_derive_status(counters) 按 D-14 硬切规则：unmatched+mapping_failed+failed>0 → 'partial'，否则 'success'；leading_zero_fallback 不参与派生"
    - "四个业务方法 sync_performance_records / sync_salary_adjustments / sync_hire_info / sync_non_statutory_leave 外部签名仍接收关键字参数 + triggered_by 且返回 FeishuSyncLog（而非 dict），内部委托给 _with_sync_log 调度私有 body fn（D-10）"
    - "sync_attendance 重构到 _with_sync_log('attendance', ...) 模式；外部签名 (mode, triggered_by) → FeishuSyncLog 不变（D-12）；sync_with_retry 不动"
    - "is_sync_running(sync_type: str | None = None) 升级：传值时只查该 type；不传时查所有 running（D-15）"
    - "业务 fn 抛异常 → helper 用独立 session 写 status='failed' + error_message，然后 raise（不吞异常）；独立 session 写 log 本身抛异常 → logger.exception 不 reraise（避免覆盖业务异常）"
    - "mapping_failed_count 正确计数：year 解析失败 / grade 不在枚举 / adjustment_date 解析失败 → mapping_failed += 1；skipped_count 只计业务跳过（如 source 更旧）"
    - "feishu_service 模块内不再出现 'performance_grades' 字符串（用 'performance' — D-01；alias 交给 Plan 03 在 celery task 处理）"
  artifacts:
    - path: "backend/app/services/feishu_service.py"
      provides: "_with_sync_log helper + _SyncCounters dataclass + 5 sync method refactor + is_sync_running(sync_type)"
      contains: "_with_sync_log"
    - path: "backend/tests/test_services/test_feishu_with_sync_log_helper.py"
      provides: "helper 单测：happy / business-exception / rollback-isolation / log-finalize-swallow"
      contains: "_with_sync_log"
    - path: "backend/tests/test_services/test_feishu_sync_counters_dataclass.py"
      provides: "_SyncCounters dataclass frozen + 字段清单测试"
      contains: "_SyncCounters"
    - path: "backend/tests/test_services/test_feishu_partial_status_derivation.py"
      provides: "_derive_status 12+ 用例"
      contains: "_derive_status"
    - path: "backend/tests/test_services/test_feishu_per_sync_type_lock.py"
      provides: "is_sync_running(sync_type) 过滤语义测试"
      contains: "is_sync_running"
    - path: "backend/tests/test_services/test_feishu_sync_methods_write_log.py"
      provides: "四方法写入 FeishuSyncLog 的集成测试（sync_type 正确、counters 五项齐备）"
      contains: "sync_performance_records"
    - path: "backend/tests/test_services/test_feishu_mapping_failed_counter.py"
      provides: "year / grade / date 解析失败 → mapping_failed += 1"
      contains: "mapping_failed"
    - path: "backend/tests/test_services/test_feishu_sync_type_whitelist.py"
      provides: "helper 拒绝非法 sync_type"
      contains: "ValueError"
  key_links:
    - from: "FeishuService._with_sync_log"
      to: "SessionLocal()"
      via: "独立 session 写 log (running + terminal + failed)"
      pattern: "SessionLocal\\(\\)"
    - from: "FeishuService.sync_performance_records"
      to: "_with_sync_log('performance', _sync_performance_records_body, ...)"
      via: "委托 helper 调度"
      pattern: "_with_sync_log"
    - from: "FeishuService.is_sync_running"
      to: "FeishuSyncLog.sync_type"
      via: "select(FeishuSyncLog).where(sync_type == ...)"
      pattern: "is_sync_running.*sync_type"
---

<objective>
把 FeishuService 五个同步方法（sync_attendance / sync_performance_records / sync_salary_adjustments / sync_hire_info / sync_non_statutory_leave）从「各自手写 log / 返回 dict」的冗余模式，重构为「统一通过 `_with_sync_log` helper 调度 + 返回 `_SyncCounters` dataclass」的可维护架构。所有运行期行为语义由 D-10 ~ D-17 锁定：

- 独立 session 写 log（running / terminal / failed）与业务 upsert 事务隔离（D-10 / D-13），业务 rollback 不连带 log rollback
- `_SyncCounters` 强类型 dataclass 替代 dict，避免字段拼写静默错误（D-11）
- `_derive_status` 硬切派生 partial（D-14）：`unmatched+mapping_failed+failed>0` → `partial`，`leading_zero_fallback` 不参与
- `sync_attendance` 外部签名不变（D-12 + Pitfall F），内部委托 helper；`sync_with_retry` 不动
- `is_sync_running(sync_type)` 升级为 per-sync_type 分桶锁（D-15），同 type 互斥、不同 type 可并行
- `mapping_failed_count` 覆盖 year / grade / date 等字段类型转换失败（D-02 新语义）；`skipped_count` 只保留业务跳过
- `expire_stale_running_logs` 保留（D-17），helper 不改该方法

目的：Plan 03 的 API 层能直接 `service.sync_performance_records(triggered_by=...)` → 返回 FeishuSyncLog；Plan 04 的前端按 sync_type 筛选 + 五色 badge 展示的数据源已就绪。**本 Plan 严格不改动路由层 / 前端**，只做 service 与 helper 重构与单测。

Output：
- FeishuService 扩展 `_with_sync_log` + `_SyncCounters` + `_derive_status` + `_apply_counters_to_log`
- 五个 sync 方法重构（外部签名保持兼容；内部走 helper）
- `is_sync_running(sync_type)` 升级
- 7 个新 pytest 测试文件覆盖 helper / dataclass / partial / lock / mapping_failed / whitelist / 四方法写 log
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-feishu-sync-observability/31-CONTEXT.md
@.planning/phases/31-feishu-sync-observability/31-RESEARCH.md
@.planning/phases/31-feishu-sync-observability/31-VALIDATION.md
@.planning/phases/31-feishu-sync-observability/31-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@.planning/research/PITFALLS.md

@backend/app/services/feishu_service.py
@backend/app/models/feishu_sync_log.py
@backend/app/schemas/feishu.py
@backend/app/core/database.py
@backend/tests/test_services/test_feishu_leading_zero.py

<interfaces>
<!-- 既定契约 / 必须遵守的签名。Executor 直接复用，无需探索。 -->

Plan 01 已发布的 Literal 常量（从 backend/app/schemas/feishu.py import）：

```python
from backend.app.schemas.feishu import SyncTypeLiteral, SyncStatusLiteral
# SyncTypeLiteral = Literal['attendance', 'performance', 'salary_adjustments', 'hire_info', 'non_statutory_leave']
# SyncStatusLiteral = Literal['running', 'success', 'partial', 'failed']
```

现有 FeishuService 关键方法签名（backend/app/services/feishu_service.py）：

```python
# line 323
def sync_attendance(self, mode: str, triggered_by: str | None = None) -> FeishuSyncLog: ...

# line 497
def sync_performance_records(self, *, app_token: str, table_id: str, field_mapping: dict[str, str] | None = None) -> dict: ...
# ^ Phase 31: 加 triggered_by 参数，改返回类型为 FeishuSyncLog

# line 606
def sync_salary_adjustments(self, *, app_token: str, table_id: str, field_mapping: dict[str, str] | None = None) -> dict: ...

# line 724
def sync_hire_info(self, *, app_token: str, table_id: str, field_mapping: dict[str, str] | None = None) -> dict: ...

# line 823
def sync_non_statutory_leave(self, *, app_token: str, table_id: str, field_mapping: dict[str, str] | None = None) -> dict: ...

# line 1301
def is_sync_running(self) -> bool: ...
# ^ Phase 31: 升级为 is_sync_running(self, sync_type: str | None = None) -> bool

# line 1317
def expire_stale_running_logs(self, timeout_minutes: int = 30) -> int: ...
# 保留，本 plan 不改

# line ~1360 sync_with_retry 调用 sync_attendance — 外部签名不能变（Pitfall F）
def sync_with_retry(self, mode: str, triggered_by: str | None = None, max_retries: int = 3) -> FeishuSyncLog: ...
```

现有「failed log 独立 session」模式（feishu_service.py:474-491）— _with_sync_log 需按此模式提炼：

```python
except Exception as exc:
    self.db.rollback()
    try:
        fail_db = SessionLocal()
        try:
            fail_log = fail_db.get(FeishuSyncLog, sync_log_id)
            if fail_log:
                fail_log.status = 'failed'
                fail_log.error_message = str(exc)[:2000]
                fail_log.leading_zero_fallback_count = fallback_counter['count']
                fail_log.finished_at = datetime.now(timezone.utc)
                fail_db.commit()
        finally:
            fail_db.close()
    except Exception:
        logger.exception('Failed to save sync failure log')
    raise
```

SessionLocal 导入位置：`from backend.app.core.database import SessionLocal`（已在 feishu_service.py 顶部导入，见 line ~20）

_SyncCounters 与 helper 的完整骨架（Pattern 2 / Pitfall A 示例）：

```python
from dataclasses import dataclass


@dataclass(frozen=True, slots=True)
class _SyncCounters:
    success: int = 0                        # 新增记录数（对应 log.synced_count）
    updated: int = 0                        # 更新现有记录数
    unmatched: int = 0                      # 工号未匹配到 employee
    mapping_failed: int = 0                 # D-02: 字段类型/格式转换失败
    failed: int = 0                         # upsert / commit 异常
    leading_zero_fallback: int = 0          # Phase 30: lstrip('0') 命中
    total_fetched: int = 0                  # 飞书拉取总数
    unmatched_nos: tuple[str, ...] = ()     # 前 100 个未匹配工号（tuple 以满足 frozen）


def _derive_status(c: _SyncCounters) -> str:
    """D-14: partial if any of {unmatched, mapping_failed, failed} > 0. leading_zero_fallback NOT counted."""
    if c.unmatched + c.mapping_failed + c.failed > 0:
        return 'partial'
    return 'success'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 _SyncCounters / _derive_status / _apply_counters_to_log / _with_sync_log helper + per-sync_type lock + 单测</name>
  <files>backend/app/services/feishu_service.py, backend/tests/test_services/test_feishu_sync_counters_dataclass.py, backend/tests/test_services/test_feishu_partial_status_derivation.py, backend/tests/test_services/test_feishu_with_sync_log_helper.py, backend/tests/test_services/test_feishu_per_sync_type_lock.py, backend/tests/test_services/test_feishu_sync_type_whitelist.py</files>
  <read_first>
    - backend/app/services/feishu_service.py — 必须看 sync_attendance 的完整实现（line 323-491），包括 fallback_counter / self.db.flush() / 独立 session failed log 三段（这是 helper 的提炼来源）
    - backend/app/services/feishu_service.py — is_sync_running 和 expire_stale_running_logs（line 1301-1326）
    - backend/app/core/database.py — SessionLocal 使用模式
    - backend/app/models/feishu_sync_log.py — Plan 01 后新字段 sync_type / mapping_failed_count
    - backend/tests/test_services/test_feishu_leading_zero.py — in-memory SQLite + StaticPool fixture 模式（必须复用）
    - .planning/phases/31-feishu-sync-observability/31-RESEARCH.md §Pitfall A — helper 异常语义的完整正确实现
    - .planning/phases/31-feishu-sync-observability/31-CONTEXT.md §D-10/D-11/D-13/D-14/D-15/D-17 — helper 语义的硬契约
  </read_first>
  <behavior>
    _SyncCounters（test_feishu_sync_counters_dataclass.py）：
    - Test 1: _SyncCounters() 无参构造，所有字段默认 0（或空 tuple）
    - Test 2: _SyncCounters(success=5, updated=3) 可构造
    - Test 3: _SyncCounters 是 frozen — 尝试 c.success = 10 抛 FrozenInstanceError
    - Test 4: _SyncCounters 含全部 8 字段（success / updated / unmatched / mapping_failed / failed / leading_zero_fallback / total_fetched / unmatched_nos）— 用 dataclasses.fields 验证

    _derive_status（test_feishu_partial_status_derivation.py）：
    - Test 5: 全零 → 'success'
    - Test 6: success=100 others=0 → 'success'
    - Test 7: success=100, unmatched=1 → 'partial'
    - Test 8: success=100, mapping_failed=1 → 'partial'
    - Test 9: success=100, failed=1 → 'partial'
    - Test 10: unmatched=1, mapping_failed=1, failed=1 → 'partial'
    - Test 11: success=100, leading_zero_fallback=5, unmatched=0, mapping_failed=0, failed=0 → 'success'（关键：fallback 不降级）
    - Test 12: success=100, leading_zero_fallback=100 → 'success'（fallback 即使很高也不降级）
    - Test 13: all non-fallback counters 同时非零 → 'partial'

    _with_sync_log（test_feishu_with_sync_log_helper.py）：
    - Test 14 happy path: fn 返回 _SyncCounters(success=5) → helper 返回 log_id，DB 里有一条 FeishuSyncLog，sync_type='performance', status='success', synced_count=5
    - Test 15 partial path: fn 返回 _SyncCounters(success=5, unmatched=2) → log.status='partial', log.unmatched_count=2
    - Test 16 business exception: fn 抛 RuntimeError('upstream failed') → helper 重抛 RuntimeError，DB 里 log.status='failed', log.error_message='upstream failed'
    - Test 17 rollback isolation (D-13): fn 在抛异常前调用了 self.db.add(PerformanceRecord(...)) 但没 commit；helper 的独立 session 写 log 时 log 仍然可见（业务 rollback 不影响 log）
    - Test 18 log finalize exception swallow (Pitfall A): 如果 finalize 阶段 SessionLocal() 抛异常，helper 不能覆盖 business_exc；business exception 仍被重抛
    - Test 19 sync_type whitelist rejection: helper 接收 sync_type='invalid_type' 抛 ValueError（白名单校验在 helper 入口）
    - Test 20 running log created in independent session: helper 创建 running log 后、fn 尚未执行时，用另一个 session 能查到 status='running' 的 log（证明 log commit 与 business session 无关）

    is_sync_running per-sync_type（test_feishu_per_sync_type_lock.py）：
    - Test 21: 无 running log → is_sync_running() == False, is_sync_running('performance') == False
    - Test 22: 有 sync_type='attendance' status='running' → is_sync_running() == True（无参兼容）, is_sync_running('attendance') == True, is_sync_running('performance') == False（分桶）
    - Test 23: 同时有 attendance running + performance running → is_sync_running('attendance') == True, is_sync_running('performance') == True, is_sync_running() == True
    - Test 24: is_sync_running('invalid_type') — 不抛异常，返回 False（白名单校验留给 helper / 调用方）

    sync_type whitelist（test_feishu_sync_type_whitelist.py）：
    - Test 25: _with_sync_log 受保护集合外的值（如 'bogus'）立即 ValueError，不写任何 log
    - Test 26: 五个合法 sync_type 都不抛
  </behavior>
  <action>
在 `backend/app/services/feishu_service.py` 顶部（`import` 块之后、类定义之前）加入常量与 dataclass；在 FeishuService 类方法区（`sync_attendance` 之上）加入 helper 族方法。具体编辑：

**Step 1:** 新增 import（如已存在保留）：

```python
from dataclasses import dataclass

from backend.app.schemas.feishu import SyncTypeLiteral  # Plan 01 新增的 Literal
```

**Step 2:** 模块级常量与 dataclass（放在现有 `class FeishuService:` 之前）：

```python
_VALID_SYNC_TYPES: frozenset[str] = frozenset({
    'attendance',
    'performance',
    'salary_adjustments',
    'hire_info',
    'non_statutory_leave',
})


@dataclass(frozen=True, slots=True)
class _SyncCounters:
    """Phase 31 / D-11: 业务 sync fn 统一返回该 dataclass，由 _with_sync_log 映射到 FeishuSyncLog 列。"""
    success: int = 0
    updated: int = 0
    unmatched: int = 0
    mapping_failed: int = 0
    failed: int = 0
    leading_zero_fallback: int = 0
    total_fetched: int = 0
    unmatched_nos: tuple[str, ...] = ()


def _derive_status(c: _SyncCounters) -> str:
    """Phase 31 / D-14: partial 派生硬切规则。

    leading_zero_fallback 不参与 — 它代表「救回来的」成功匹配（Pitfall E）。
    """
    if c.unmatched + c.mapping_failed + c.failed > 0:
        return 'partial'
    return 'success'


def _apply_counters_to_log(log: 'FeishuSyncLog', c: _SyncCounters) -> None:
    """把 _SyncCounters 映射到 FeishuSyncLog 列。helper 在终态阶段调用。"""
    import json
    log.total_fetched = c.total_fetched
    log.synced_count = c.success
    log.updated_count = c.updated
    log.unmatched_count = c.unmatched
    log.mapping_failed_count = c.mapping_failed
    log.failed_count = c.failed
    log.leading_zero_fallback_count = c.leading_zero_fallback
    log.status = _derive_status(c)
    log.finished_at = datetime.now(timezone.utc)
    if c.unmatched_nos:
        log.unmatched_employee_nos = json.dumps(list(c.unmatched_nos))
```

**Step 3:** 在 FeishuService 类内，`sync_attendance` 之上加 `_with_sync_log` helper（严格按 Pitfall A 骨架，不吞业务异常）：

```python
def _with_sync_log(
    self,
    sync_type: str,
    fn,
    *,
    triggered_by: str | None = None,
    mode: str = 'full',
    **kwargs,
) -> str:
    """Phase 31 / D-10 / D-13: 统一的 sync log 生命周期调度。

    Returns sync_log_id. 调用方用 self.db.get(FeishuSyncLog, sync_log_id) 获取最终对象。

    Semantics:
      - Stage 1: 独立 SessionLocal() 创建 status='running' log（commit 独立于 self.db）
      - Stage 2: 调 fn(sync_log_id=..., **kwargs) → 期望返回 _SyncCounters
      - Stage 3: 独立 SessionLocal() 写终态（success/partial/failed）
      - 业务 fn 抛异常时：log.status='failed' + error_message，然后重抛业务异常
      - 独立 session 写 log 本身抛异常时：logger.exception 不 reraise（避免覆盖业务异常，Pitfall A）
    """
    if sync_type not in _VALID_SYNC_TYPES:
        raise ValueError(f'Unknown sync_type: {sync_type!r}; valid: {sorted(_VALID_SYNC_TYPES)}')

    # Stage 1: 独立 session 创建 running log
    started_at = datetime.now(timezone.utc)
    log_db = SessionLocal()
    try:
        sync_log = FeishuSyncLog(
            sync_type=sync_type,
            mode=mode,
            status='running',
            total_fetched=0,
            synced_count=0,
            updated_count=0,
            skipped_count=0,
            unmatched_count=0,
            mapping_failed_count=0,
            failed_count=0,
            leading_zero_fallback_count=0,
            started_at=started_at,
            triggered_by=triggered_by,
        )
        log_db.add(sync_log)
        log_db.commit()
        sync_log_id = sync_log.id
    finally:
        log_db.close()

    # Stage 2: 业务 fn（用 self.db）
    counters: _SyncCounters | None = None
    business_exc: Exception | None = None
    try:
        counters = fn(sync_log_id=sync_log_id, **kwargs)
        if not isinstance(counters, _SyncCounters):
            raise TypeError(
                f'Sync fn for sync_type={sync_type!r} returned {type(counters).__name__}, expected _SyncCounters'
            )
    except Exception as exc:
        business_exc = exc
        try:
            self.db.rollback()
        except Exception:
            logger.exception('Business session rollback failed for sync_log_id=%s', sync_log_id)

    # Stage 3: 独立 session 写终态
    finalize_db = SessionLocal()
    try:
        log_row = finalize_db.get(FeishuSyncLog, sync_log_id)
        if log_row is None:
            logger.error('FeishuSyncLog %s disappeared before finalization', sync_log_id)
        elif business_exc is not None:
            log_row.status = 'failed'
            log_row.error_message = str(business_exc)[:2000]
            log_row.finished_at = datetime.now(timezone.utc)
            finalize_db.commit()
        else:
            assert counters is not None
            _apply_counters_to_log(log_row, counters)
            finalize_db.commit()
    except Exception:
        logger.exception('Failed to finalize FeishuSyncLog %s', sync_log_id)
        # Swallow — do NOT overwrite business_exc (Pitfall A)
    finally:
        finalize_db.close()

    if business_exc is not None:
        raise business_exc

    return sync_log_id
```

**Step 4:** 升级 `is_sync_running` 签名（line ~1301）：

```python
def is_sync_running(self, sync_type: str | None = None) -> bool:
    """Phase 31 / D-15: per-sync_type 分桶锁。

    传 sync_type 时仅查该 type 的 status='running' 记录（同 type 互斥）；
    不传时查所有 running（保留向后兼容，现有 /feishu/sync trigger_sync 调用仍有效）。

    非白名单 sync_type 不抛异常，返回 False（让调用方决定是否拒绝）。
    """
    stmt = select(FeishuSyncLog).where(FeishuSyncLog.status == 'running')
    if sync_type is not None:
        stmt = stmt.where(FeishuSyncLog.sync_type == sync_type)
    return self.db.execute(stmt.limit(1)).scalar_one_or_none() is not None
```

**Step 5:** 创建 6 个新测试文件（all 以 `from __future__ import annotations` 起），每个文件顶部本地定义 `db_session` fixture（in-memory SQLite + StaticPool）。fixture 模板参考 `backend/tests/test_services/test_feishu_leading_zero.py`。**六个测试文件名与测试函数名必须严格对应上文 <behavior> 的 Test N 清单**。

示例 `test_feishu_with_sync_log_helper.py` 的 Test 14 / 16 / 17：

```python
from __future__ import annotations

from datetime import datetime, timezone

import pytest
from sqlalchemy import create_engine, select
from sqlalchemy.orm import Session
from sqlalchemy.pool import StaticPool

from backend.app.core.database import Base
from backend.app.models.feishu_sync_log import FeishuSyncLog
from backend.app.services.feishu_service import FeishuService, _SyncCounters


@pytest.fixture()
def engine_fixture(monkeypatch):
    engine = create_engine(
        'sqlite://',
        connect_args={'check_same_thread': False},
        poolclass=StaticPool,
    )
    Base.metadata.create_all(engine)
    # Patch SessionLocal so helper uses this engine
    from backend.app.core import database as db_module
    monkeypatch.setattr(
        db_module,
        'SessionLocal',
        lambda: Session(engine),
    )
    # Also patch the SessionLocal reference inside feishu_service (if imported as symbol)
    from backend.app.services import feishu_service as fs_module
    monkeypatch.setattr(fs_module, 'SessionLocal', lambda: Session(engine))
    return engine


@pytest.fixture()
def db_session(engine_fixture):
    with Session(engine_fixture) as session:
        yield session


def test_helper_happy_path_writes_success_log(db_session: Session) -> None:
    service = FeishuService(db_session)

    def fake_body(*, sync_log_id: str) -> _SyncCounters:
        return _SyncCounters(success=5, updated=3, total_fetched=8)

    log_id = service._with_sync_log('performance', fake_body, triggered_by='tester')
    assert log_id

    log = db_session.get(FeishuSyncLog, log_id)
    db_session.refresh(log)
    assert log.sync_type == 'performance'
    assert log.status == 'success'
    assert log.synced_count == 5
    assert log.updated_count == 3
    assert log.total_fetched == 8
    assert log.triggered_by == 'tester'


def test_helper_business_exception_writes_failed_log_and_reraises(db_session: Session) -> None:
    service = FeishuService(db_session)

    def failing_body(*, sync_log_id: str) -> _SyncCounters:
        raise RuntimeError('upstream exploded')

    with pytest.raises(RuntimeError, match='upstream exploded'):
        service._with_sync_log('hire_info', failing_body)

    log = db_session.scalar(
        select(FeishuSyncLog).where(FeishuSyncLog.sync_type == 'hire_info')
    )
    assert log is not None
    assert log.status == 'failed'
    assert log.error_message == 'upstream exploded'


def test_helper_rejects_invalid_sync_type(db_session: Session) -> None:
    service = FeishuService(db_session)
    with pytest.raises(ValueError, match='Unknown sync_type'):
        service._with_sync_log('bogus', lambda **kw: _SyncCounters())
```

其余文件用相同 fixture 模板 + 对应 Test N 断言。`test_feishu_per_sync_type_lock.py` 可直接插入 FeishuSyncLog 行再调 `service.is_sync_running(...)`，无需 helper。

**避免与原因：**
- 不把 running log 用 `self.db.add(...) + self.db.flush()` 写（Phase 30 之前 sync_attendance 的做法）— 业务 rollback 会连带 running log 回滚，HR 看不到失败记录
- 不让 helper 的 finalize 阶段 re-raise 内部异常 — Pitfall A 明确：finalize 异常不覆盖 business exception
- 不把 `sync_type` 校验留到 DB 层（String(32) 不 enforce 值）— helper 入口显式 ValueError 更早报错
- 不把 `skipped` 映射到 `mapping_failed`（D-02 新语义需要业务 fn 自己分流，留给 Task 2）— 本 task 只建基础设施
- 不改 `expire_stale_running_logs` — D-17 沿用现状；本 plan 不触碰
- 测试用 `monkeypatch.setattr` 把模块级 `SessionLocal` 替换为 in-memory engine 的 factory — 直接打补丁比改 helper 签名接 Session 更简洁（helper 签名保持生产代码路径）
  </action>
  <verify>
    <automated>pytest backend/tests/test_services/test_feishu_sync_counters_dataclass.py backend/tests/test_services/test_feishu_partial_status_derivation.py backend/tests/test_services/test_feishu_with_sync_log_helper.py backend/tests/test_services/test_feishu_per_sync_type_lock.py backend/tests/test_services/test_feishu_sync_type_whitelist.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep `_VALID_SYNC_TYPES: frozenset\[str\] = frozenset\(\{` in backend/app/services/feishu_service.py returns 1 match
    - grep `class _SyncCounters:` in backend/app/services/feishu_service.py returns 1 match
    - grep `@dataclass\(frozen=True, slots=True\)` in backend/app/services/feishu_service.py returns at least 1 match
    - grep `def _derive_status\(c: _SyncCounters\) -> str:` in backend/app/services/feishu_service.py returns 1 match
    - grep `def _with_sync_log\(\s*self,\s*sync_type: str` in backend/app/services/feishu_service.py (multiline) returns 1 match
    - grep `def is_sync_running\(self, sync_type: str \| None = None\)` in backend/app/services/feishu_service.py returns 1 match
    - grep `raise ValueError\(f'Unknown sync_type` in backend/app/services/feishu_service.py returns 1 match
    - 5 new test files exist under backend/tests/test_services/ (dataclass / partial / helper / per_sync_type_lock / whitelist)
    - `pytest backend/tests/test_services/test_feishu_sync_counters_dataclass.py backend/tests/test_services/test_feishu_partial_status_derivation.py backend/tests/test_services/test_feishu_with_sync_log_helper.py backend/tests/test_services/test_feishu_per_sync_type_lock.py backend/tests/test_services/test_feishu_sync_type_whitelist.py -x -v` prints all tests PASSED (min 26 tests total)
  </acceptance_criteria>
  <done>
    - _SyncCounters / _derive_status / _apply_counters_to_log / _with_sync_log / is_sync_running(sync_type) 全部落地
    - 5 个测试文件全绿
    - feishu_service.py 不再出现 'performance_grades' 字符串（该 alias 在 Plan 03 celery task 处理）
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 重构 5 个 sync_* 方法（attendance / performance / salary_adjustments / hire_info / non_statutory_leave）走 _with_sync_log + mapping_failed 新语义 + 集成测试</name>
  <files>backend/app/services/feishu_service.py, backend/tests/test_services/test_feishu_sync_methods_write_log.py, backend/tests/test_services/test_feishu_mapping_failed_counter.py</files>
  <read_first>
    - backend/app/services/feishu_service.py — sync_attendance (line 323-491) / sync_performance_records (497-600) / sync_salary_adjustments (606-718) / sync_hire_info (724-817) / sync_non_statutory_leave (823-927) 全部 5 方法完整实现
    - backend/app/services/feishu_service.py — _build_employee_map / _lookup_employee / _fetch_all_records / _ensure_token / _map_fields（这些 helper 不改，业务 body 仍调用）
    - backend/app/models/performance_record.py / salary_adjustment.py / employee.py / non_statutory_leave.py — 业务实体模型（upsert 目标）
    - Task 1 完成后的 feishu_service.py — _with_sync_log 已就位
    - backend/tests/test_services/test_feishu_leading_zero.py — 集成测试 fixture 模板（含 MagicMock httpx）
    - .planning/phases/31-feishu-sync-observability/31-RESEARCH.md §Pitfall E / Pitfall F — mapping_failed 新语义 + sync_attendance 签名保持
    - .planning/phases/31-feishu-sync-observability/31-CONTEXT.md §D-02 / D-11 / D-12 — skipped vs mapping_failed 分流规则
  </read_first>
  <behavior>
    四个新 sync 方法写 log（test_feishu_sync_methods_write_log.py）：
    - Test 1: mock FeishuService._fetch_all_records 返回 [{'employee_no': 'E001', 'year': 2026, 'grade': 'A'}]；mock _ensure_token / _build_employee_map；调 sync_performance_records(app_token='t', table_id='tbl', triggered_by='user-1') → 返回 FeishuSyncLog 实例，log.sync_type='performance', log.status='success', log.synced_count=1
    - Test 2: 同上但 sync_salary_adjustments → log.sync_type='salary_adjustments'
    - Test 3: 同上但 sync_hire_info → log.sync_type='hire_info'
    - Test 4: 同上但 sync_non_statutory_leave → log.sync_type='non_statutory_leave'
    - Test 5: sync_attendance 外部签名仍接收 (mode, triggered_by) 两位置参数并返回 FeishuSyncLog（签名保持 — Pitfall F）
    - Test 6: sync_attendance 调用后 log.sync_type='attendance', log.mode='full'/'incremental' 取决于入参
    - Test 7: mock fn 内部抛异常 → 返回的 FeishuSyncLog log.status='failed', log.error_message 非空

    mapping_failed vs skipped 分流（test_feishu_mapping_failed_counter.py）：
    - Test 8: sync_performance_records 输入 [{'employee_no': 'E001', 'year': 'not-a-number', 'grade': 'A'}] → log.mapping_failed_count=1, log.skipped_count=0（year 解析失败归 mapping_failed — D-02）
    - Test 9: sync_performance_records 输入 [{'employee_no': 'E001', 'year': 2026, 'grade': 'Z'}] → log.mapping_failed_count=1（grade 非法归 mapping_failed）
    - Test 10: sync_performance_records 输入 [{'employee_no': 'NONEXIST', 'year': 2026, 'grade': 'A'}] → log.unmatched_count=1, log.mapping_failed_count=0（emp_no 找不到归 unmatched，不归 mapping_failed）
    - Test 11: sync_salary_adjustments 输入 [{'employee_no': 'E001', 'adjustment_date': 'garbage'}] → log.mapping_failed_count=1（日期解析失败归 mapping_failed — D-02）
    - Test 12: sync_performance_records 混合输入 2 条合法 + 1 条 year 错 + 1 条 emp 找不到 → log.synced_count=2, log.mapping_failed_count=1, log.unmatched_count=1, log.status='partial'
  </behavior>
  <action>
改造五个 sync 方法，每个都遵循「外部方法保留原签名 + 新增 triggered_by 参数 + 返回 FeishuSyncLog + 内部委托 _with_sync_log + 业务逻辑移入 `_sync_XXX_body` 私有方法（返回 _SyncCounters）」的模式。

**Step 1 (sync_performance_records 改造示例，其他四方法同构):**

在 feishu_service.py 定位现有 `def sync_performance_records` 方法。**完全重写**该方法及其相邻的 `_sync_performance_records_body`：

```python
def sync_performance_records(
    self,
    *,
    app_token: str,
    table_id: str,
    field_mapping: dict[str, str] | None = None,
    triggered_by: str | None = None,
) -> FeishuSyncLog:
    """Phase 31 / D-10: 同步绩效记录。外部签名返回 FeishuSyncLog（替代旧的 dict）。"""
    sync_log_id = self._with_sync_log(
        sync_type='performance',
        fn=self._sync_performance_records_body,
        triggered_by=triggered_by,
        mode='full',
        app_token=app_token,
        table_id=table_id,
        field_mapping=field_mapping,
    )
    log = self.db.get(FeishuSyncLog, sync_log_id)
    if log is None:
        # helper 内部已写 log，此处理论上应能 get 到；防御性 refresh session
        self.db.expire_all()
        log = self.db.get(FeishuSyncLog, sync_log_id)
    return log


def _sync_performance_records_body(
    self,
    *,
    sync_log_id: str,
    app_token: str,
    table_id: str,
    field_mapping: dict[str, str] | None = None,
) -> _SyncCounters:
    """Phase 31 / D-02 / D-11: 绩效同步业务体。返回 _SyncCounters 而非 dict。

    D-02 新语义：
    - year 无法 int 解析 / grade 不在 ABCDE → mapping_failed += 1（不是 skipped）
    - emp_no 找不到 employee → unmatched += 1（不是 skipped）
    - 仅保留「业务跳过」到 skipped_count（目前绩效无业务跳过场景；可能为 0）
    """
    if field_mapping is None:
        field_mapping = {
            '员工工号': 'employee_no',
            '年度': 'year',
            '绩效等级': 'grade',
        }

    config = self.get_config()
    if config is None:
        raise RuntimeError('No active Feishu config found')

    settings = get_settings()
    app_secret = config.get_app_secret(settings.feishu_encryption_key)
    token = self._ensure_token(config.app_id, app_secret)

    records = self._fetch_all_records(token, app_token, table_id, field_mapping)

    emp_map = self._build_employee_map()
    fallback_counter: dict[str, int] = {'count': 0}

    synced = 0
    updated = 0
    unmatched = 0
    mapping_failed = 0
    failed = 0
    total = len(records)
    unmatched_nos: list[str] = []

    for record in records:
        try:
            emp_no = record.get('employee_no')
            employee_id = self._lookup_employee(emp_map, emp_no, fallback_counter=fallback_counter)
            if employee_id is None:
                unmatched += 1
                if emp_no and len(unmatched_nos) < 100:
                    unmatched_nos.append(str(emp_no))
                continue

            # D-02: year 解析失败 → mapping_failed（不是 skipped）
            raw_year = record.get('year')
            if raw_year is None:
                mapping_failed += 1
                continue
            try:
                year = int(float(str(raw_year)))
            except (ValueError, TypeError):
                logger.warning('Performance sync: invalid year=%r for emp_no=%s', raw_year, emp_no)
                mapping_failed += 1
                continue

            # D-02: grade 非法 → mapping_failed
            raw_grade = record.get('grade')
            if raw_grade is None:
                mapping_failed += 1
                continue
            grade = str(raw_grade).strip().upper()
            if grade not in ('A', 'B', 'C', 'D', 'E'):
                logger.warning('Performance sync: invalid grade=%s for emp_no=%s', grade, emp_no)
                mapping_failed += 1
                continue

            existing = self.db.scalar(
                select(PerformanceRecord).where(
                    PerformanceRecord.employee_id == employee_id,
                    PerformanceRecord.year == year,
                )
            )
            if existing is not None:
                existing.grade = grade
                existing.source = 'feishu'
                self.db.add(existing)
                updated += 1
            else:
                new_record = PerformanceRecord(
                    employee_id=employee_id,
                    employee_no=emp_no,
                    year=year,
                    grade=grade,
                    source='feishu',
                )
                self.db.add(new_record)
                synced += 1
            self.db.flush()

        except Exception:
            logger.exception('Failed to process performance record: emp_no=%s', record.get('employee_no'))
            failed += 1

    self.db.commit()

    return _SyncCounters(
        success=synced,
        updated=updated,
        unmatched=unmatched,
        mapping_failed=mapping_failed,
        failed=failed,
        leading_zero_fallback=fallback_counter['count'],
        total_fetched=total,
        unmatched_nos=tuple(unmatched_nos),
    )
```

**Step 2:** 用相同模式重构 `sync_salary_adjustments` / `sync_hire_info` / `sync_non_statutory_leave`：
- 外部方法新增 `triggered_by: str | None = None` 参数
- 外部方法返回 FeishuSyncLog（替代 dict）
- 外部方法委托 `_with_sync_log('salary_adjustments' / 'hire_info' / 'non_statutory_leave', ...)`
- `_sync_salary_adjustments_body`：`adjustment_date` 解析失败归 `mapping_failed`（D-02）；金额字段解析失败归 `mapping_failed`；其余业务跳过场景保留到 `skipped`（目前代码无明显业务跳过，保持 0）
- `_sync_hire_info_body`：`hire_date` 解析失败归 `mapping_failed`；`employee_type` 非法枚举归 `mapping_failed`
- `_sync_non_statutory_leave_body`：`leave_hours` 非数字归 `mapping_failed`；`leave_date` 解析失败归 `mapping_failed`
- 所有 4 body 方法必须 track `unmatched_nos: list[str]`（前 100 个）并通过 `_SyncCounters.unmatched_nos=tuple(...)` 返回

**Step 3:** 重构 `sync_attendance`（Pitfall F — 外部签名不能变）：

```python
def sync_attendance(self, mode: str, triggered_by: str | None = None) -> FeishuSyncLog:
    """Phase 31 / D-12: 重构到 _with_sync_log('attendance', ...) 模式。外部签名不变。

    sync_with_retry 调用本方法，依赖 mode / triggered_by / return FeishuSyncLog 签名 — Pitfall F。
    """
    sync_log_id = self._with_sync_log(
        sync_type='attendance',
        fn=self._sync_attendance_body,
        triggered_by=triggered_by,
        mode=mode,
    )
    log = self.db.get(FeishuSyncLog, sync_log_id)
    if log is None:
        self.db.expire_all()
        log = self.db.get(FeishuSyncLog, sync_log_id)
    return log


def _sync_attendance_body(
    self,
    *,
    sync_log_id: str,
    mode: str,
) -> _SyncCounters:
    """提炼原 sync_attendance 业务代码。返回 _SyncCounters。"""
    # 把现有 line 323-491 的代码主体整段移到这里，删除：
    #   - sync_log = FeishuSyncLog(...)、self.db.add(sync_log)、self.db.flush() 等 running log 创建
    #   - self.db.commit() 前后对 sync_log 字段赋值（改为 return _SyncCounters）
    #   - try/except 块里 fail_db 独立 session 写 failed log（helper 接管）
    #   - 最后的 return sync_log（改为 return _SyncCounters(...)）
    # 保留：fallback_counter / _fetch_all_records / upsert 逻辑 / self.db.commit()
    # 修正：把内部 skipped 分类重新审视 — 目前 sync_attendance 的 skipped 保持业务跳过语义（Phase 31 不改 attendance 的 skipped 归类，见 deferred ideas：sync_attendance 的 skipped_count 语义等实战数据验证）
    ...  # 完整移植现有代码的计数与 upsert 逻辑，末尾 return _SyncCounters(success=..., updated=..., ...)
```

**Step 4:** 全面检查 feishu_service.py 中残留的 `'performance_grades'` 字符串 — grep 结果应为 0（alias 交给 Plan 03 的 celery task 处理）。

**Step 5:** 创建两个集成测试文件。核心 fixture 模式（用 monkeypatch 替换 SessionLocal 指向 in-memory engine + mock httpx）：

```python
# backend/tests/test_services/test_feishu_sync_methods_write_log.py
from __future__ import annotations

from datetime import datetime, timezone
from unittest.mock import MagicMock

import pytest
from sqlalchemy import create_engine, select
from sqlalchemy.orm import Session
from sqlalchemy.pool import StaticPool

from backend.app.core.database import Base
from backend.app.models.employee import Employee
from backend.app.models.feishu_sync_log import FeishuSyncLog
from backend.app.services.feishu_service import FeishuService


@pytest.fixture()
def engine_and_service(monkeypatch):
    engine = create_engine('sqlite://', connect_args={'check_same_thread': False}, poolclass=StaticPool)
    Base.metadata.create_all(engine)

    from backend.app.services import feishu_service as fs_module
    monkeypatch.setattr(fs_module, 'SessionLocal', lambda: Session(engine))

    session = Session(engine)
    # Seed employee
    emp = Employee(id='emp-1', employee_no='E001', name='Alice', department='Eng', ...)  # fill required fields
    session.add(emp)
    session.commit()

    service = FeishuService(session)

    # Mock external collaborators
    monkeypatch.setattr(service, 'get_config', lambda: MagicMock(
        app_id='app', get_app_secret=lambda key: 'secret'
    ))
    monkeypatch.setattr(service, '_ensure_token', lambda app_id, secret: 'token')

    return engine, service, session


def test_sync_performance_records_writes_log_with_sync_type_performance(engine_and_service, monkeypatch):
    engine, service, session = engine_and_service
    monkeypatch.setattr(
        service, '_fetch_all_records',
        lambda *a, **kw: [{'employee_no': 'E001', 'year': 2026, 'grade': 'A'}],
    )
    log = service.sync_performance_records(app_token='t', table_id='tbl', triggered_by='user-1')
    assert log.sync_type == 'performance'
    assert log.status == 'success'
    assert log.synced_count == 1
    assert log.triggered_by == 'user-1'


# ... 其余 test per sync_type + test_sync_attendance_keeps_signature
```

`test_feishu_mapping_failed_counter.py` 同构，覆盖 behaviors Test 8-12。

**避免与原因：**
- 不改 `sync_attendance` 外部签名（Pitfall F）— sync_with_retry 依赖 (mode, triggered_by) 位置参数与 FeishuSyncLog 返回
- 不把 sync_attendance 的 skipped 重新分类到 mapping_failed（Deferred Idea — 风险大等实战数据）
- 不让 body 方法直接写 FeishuSyncLog 字段 — 全部通过 _SyncCounters return，helper 统一映射
- `unmatched_nos` 严格限制前 100 个（Phase 30 既定），避免 Text 列过大
- 每个 body 方法末尾必须 `self.db.commit()` — 业务 upsert 要提交；helper 只管 log commit
- Phase 31 不改 eligibility_import.py 的 ELIGIBILITY_IMPORT_TYPES（那是 Plan 03 / Phase 32 的范围）— feishu_service 只负责自己内部的 'performance' 统一
- 不动 `_fetch_all_records / _build_employee_map / _lookup_employee / _ensure_token / _map_fields` 等工具方法（Phase 30 已建立）
- 测试用 monkeypatch.setattr 而非 DI 改造（让 production code 保持最简）
  </action>
  <verify>
    <automated>pytest backend/tests/test_services/test_feishu_sync_methods_write_log.py backend/tests/test_services/test_feishu_mapping_failed_counter.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep `def sync_performance_records\(` in backend/app/services/feishu_service.py — signature line contains `triggered_by: str \| None = None` and returns `-> FeishuSyncLog`
    - grep `def _sync_performance_records_body\(` in backend/app/services/feishu_service.py returns 1 match
    - grep `def _sync_salary_adjustments_body\(` in backend/app/services/feishu_service.py returns 1 match
    - grep `def _sync_hire_info_body\(` in backend/app/services/feishu_service.py returns 1 match
    - grep `def _sync_non_statutory_leave_body\(` in backend/app/services/feishu_service.py returns 1 match
    - grep `def _sync_attendance_body\(` in backend/app/services/feishu_service.py returns 1 match
    - grep `_with_sync_log\(\s*sync_type='performance'` in backend/app/services/feishu_service.py returns at least 1 match
    - grep `_with_sync_log\(\s*sync_type='attendance'` in backend/app/services/feishu_service.py returns at least 1 match
    - grep `_with_sync_log\(\s*sync_type='salary_adjustments'` in backend/app/services/feishu_service.py returns at least 1 match
    - grep `_with_sync_log\(\s*sync_type='hire_info'` in backend/app/services/feishu_service.py returns at least 1 match
    - grep `_with_sync_log\(\s*sync_type='non_statutory_leave'` in backend/app/services/feishu_service.py returns at least 1 match
    - grep `'performance_grades'` in backend/app/services/feishu_service.py returns 0 matches (key 统一为 'performance'; alias 在 Plan 03 celery task 层)
    - `pytest backend/tests/test_services/test_feishu_sync_methods_write_log.py backend/tests/test_services/test_feishu_mapping_failed_counter.py -x -v` prints all PASSED (min 12 tests)
    - `pytest backend/tests/test_services -k feishu -x` 回归全绿（Phase 30 既有 feishu 测试不破坏）
  </acceptance_criteria>
  <done>
    - 五个 sync_* 方法全部返回 FeishuSyncLog（而非 dict），内部走 _with_sync_log
    - 四个业务 body 方法用 D-02 新语义分流 skipped → mapping_failed
    - 新集成测试 + 回归 feishu 相关测试全绿
    - feishu_service.py 不再出现 'performance_grades' 字符串
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FeishuService.sync_* 方法入口 | triggered_by 来自认证 API 的 current_user.id（可信），但 field_mapping 来自 FeishuConfig（HR 配置，半可信） |
| 独立 SessionLocal() 写 log | 与 self.db 业务 session 隔离，两个事务互不影响（D-13） |
| 业务 body fn 返回值 | 必须是 _SyncCounters 实例；类型不符时 helper 抛 TypeError（防御性校验） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-06 | Tampering | `_SyncCounters` 字段被业务 body 错误填充（如把 unmatched 写到 success） | mitigate | `_SyncCounters` 是 `frozen=True` dataclass，构造错误会在业务 fn 返回点立即暴露（typo 抛 TypeError）；_apply_counters_to_log 的映射由 helper 单点负责，body 无法绕过 |
| T-31-07 | Information disclosure | helper 的 error_message 可能含敏感信息（如 DB 路径） | mitigate | 限制 error_message 长度至 2000 字符（`str(exc)[:2000]`）；logger.exception 写 stacktrace 到服务器日志但不回显给 UI；SyncLogRead 的 error_message 只给 admin+hrbp 看（Plan 03 路由层守门） |
| T-31-08 | Denial of Service | helper 的 Stage 3 finalize 阶段连续抛异常导致无限重试 | mitigate | Stage 3 try/except 吞掉 finalize 异常（不 reraise，Pitfall A）；helper 只 raise business_exc；Celery task 层由 autoretry_for + max_retries=2 兜底 |
| T-31-09 | Repudiation | 业务 rollback 导致 log 丢失（HR 无法追溯失败） | mitigate | D-13 明确：log 用独立 SessionLocal() commit，业务 session rollback 不影响 log；Stage 1 running log 已 commit 到独立 session，即使业务 fn 立刻抛异常 log 依然存在 |
| T-31-10 | Elevation of privilege | triggered_by 可被业务 body 篡改后写到 log | accept | body fn 不接收 triggered_by（由 helper 的 Stage 1 直接写入 running log，body 无法改动）；即使能改，triggered_by 仅用于审计展示，不授予权限 |
| T-31-11 | Tampering | per-sync_type lock 被绕过（同 type 并行写入 running log） | mitigate | `is_sync_running(sync_type)` 在 Plan 03 的 API 层前置守门；本 plan 的 helper 不自带锁（避免与外层锁重复），锁定由调用方（API / Celery task）负责 |
</threat_model>

<verification>
- `pytest backend/tests/test_services -k feishu -x -v` 全绿（Phase 30 既有测试 + Phase 31 新测试）
- grep `'performance_grades'` backend/app/services/feishu_service.py → 0 结果（key 已统一）
- grep `return \{'synced':` backend/app/services/feishu_service.py → 0 结果（dict 返回已全部替换为 `return _SyncCounters(...)`）
- `python -c "from backend.app.services.feishu_service import _SyncCounters, _derive_status, _VALID_SYNC_TYPES, FeishuService; print(_VALID_SYNC_TYPES)"` 打印 5 元素 frozenset
- 现有 `backend/tests/test_services/test_feishu_leading_zero.py` 不破坏
</verification>

<success_criteria>
- IMPORT-03 service 层就绪：四方法 + sync_attendance 全部通过 helper 写入 FeishuSyncLog，含五类计数（success / updated / unmatched / mapping_failed / failed）
- IMPORT-04 service 层就绪：`_derive_status` 硬切 partial（unmatched+mapping_failed+failed>0 → partial）；leading_zero_fallback 不降级（Pitfall E）
- 向后兼容：sync_attendance 外部签名不变，sync_with_retry 不受影响
- 事务隔离：业务 rollback 不清空 log；业务异常始终重抛；finalize 异常吞掉不覆盖业务异常
- per-sync_type 锁基础就绪：`is_sync_running(sync_type)` 按 type 过滤，不同 type 可并行
- 不向下泄漏：Plan 03 API 层可以直接 `service.sync_performance_records(..., triggered_by=user.id)` → 返回 FeishuSyncLog
</success_criteria>

<output>
After completion, create `.planning/phases/31-feishu-sync-observability/31-02-SUMMARY.md` summarizing:
- _with_sync_log / _SyncCounters / _derive_status / _apply_counters_to_log 的 API 契约（供 Plan 03 路由层 / Plan 04 前端间接消费）
- 五个 sync_* 方法的新签名清单（外部：返回 FeishuSyncLog + 接受 triggered_by）
- skipped vs mapping_failed 的 D-02 分流规则落地情况（哪些字段归哪一边）
- is_sync_running 升级的兼容性说明（无参 vs 有参）
- 遗留 TODO（如 sync_attendance 的 skipped 语义重审推迟到 v1.5+）
</output>
