---
phase: 31-feishu-sync-observability
plan: 03
plan_number: 3
type: execute
wave: 3
depends_on: [02]
files_modified:
  - backend/app/api/v1/feishu.py
  - backend/app/services/feishu_service.py
  - backend/app/tasks/feishu_sync_tasks.py
  - backend/tests/test_api/test_feishu_sync_logs_api.py
  - backend/tests/test_api/test_feishu_unmatched_csv.py
  - backend/tests/test_services/test_feishu_expire_stale.py
  - backend/tests/test_tasks/test_feishu_sync_tasks_keys.py
autonomous: true
requirements: [IMPORT-03, IMPORT-04]
requirements_addressed: [IMPORT-03, IMPORT-04]

must_haves:
  truths:
    - "GET /api/v1/feishu/sync-logs 支持 sync_type (SyncTypeLiteral optional) / page (int >=1) / page_size (int >=1 <=100) query params；默认 page=1 page_size=20；按 started_at desc 排序"
    - "GET /api/v1/feishu/sync-logs 对 admin/hrbp 返回 200 + List[SyncLogRead]；对 employee/manager 返回 403"
    - "GET /api/v1/feishu/sync-logs/{log_id}/unmatched.csv 返回 text/csv; charset=utf-8，Content-Disposition: attachment; filename=sync-log-{log_id}-unmatched.csv；内容为单列 header 'employee_no' + 前 20 个 unmatched_employee_nos；对 employee/manager 返回 403"
    - "POST /api/v1/feishu/sync（trigger_sync）升级为按 sync_type='attendance' 检查锁；同 sync_type 并发第二次点击返回 409 + {error:'sync_in_progress', sync_type:'attendance', message:...}；409 不写 FeishuSyncLog（D-16）"
    - "FeishuService.get_sync_logs(sync_type=None, page=1, page_size=20) 存在：按 sync_type 过滤 + offset 分页 + started_at desc；无参调用保留原语义（兼容 AttendanceManagement 轮询）"
    - "feishu_sync_eligibility_task（Celery）的 sync_methods dict key 统一为 'performance'；同时保留 'performance_grades' alias（1-release 过渡期）；调用 service 时规范化 canonical_sync_type = 'performance' if 'performance_grades' else value；方法调用时传 triggered_by=operator_id"
    - "expire_stale_running_logs 对五类 sync_type 统一生效（D-17）；trigger_sync 前置调用该方法"
  artifacts:
    - path: "backend/app/api/v1/feishu.py"
      provides: "GET /sync-logs (filtered+paginated) + GET /sync-logs/{id}/unmatched.csv + POST /sync 409 sync_type 语义"
      contains: "sync_type"
    - path: "backend/app/services/feishu_service.py"
      provides: "get_sync_logs(sync_type, page, page_size) 方法 + is_sync_running(sync_type) 使用端（已 Plan 02 落地）"
      contains: "def get_sync_logs"
    - path: "backend/app/tasks/feishu_sync_tasks.py"
      provides: "sync_methods dict key 统一 + alias + triggered_by 传递"
      contains: "canonical_sync_type"
    - path: "backend/tests/test_api/test_feishu_sync_logs_api.py"
      provides: "list 端点 / 角色守门 / 分页 / 409 不写 log / SC4 sequential logs"
      contains: "sync_type"
    - path: "backend/tests/test_api/test_feishu_unmatched_csv.py"
      provides: "CSV 端点 content / filename / role gate / 404 / 前 20 上限"
      contains: "text/csv"
    - path: "backend/tests/test_services/test_feishu_expire_stale.py"
      provides: "expire_stale_running_logs 对五类 sync_type 都生效"
      contains: "expire_stale"
    - path: "backend/tests/test_tasks/test_feishu_sync_tasks_keys.py"
      provides: "sync_methods alias 'performance_grades' → 'performance' + canonical normalization"
      contains: "performance_grades"
  key_links:
    - from: "backend/app/api/v1/feishu.py get_sync_logs"
      to: "require_roles('admin', 'hrbp')"
      via: "FastAPI Depends"
      pattern: "require_roles\\('admin', 'hrbp'\\)"
    - from: "backend/app/api/v1/feishu.py download_unmatched_csv"
      to: "Response(media_type='text/csv; charset=utf-8')"
      via: "csv.writer + StringIO"
      pattern: "text/csv"
    - from: "backend/app/api/v1/feishu.py trigger_sync"
      to: "is_sync_running(sync_type='attendance')"
      via: "409 HTTPException with sync_type payload"
      pattern: "sync_in_progress"
    - from: "backend/app/tasks/feishu_sync_tasks.py"
      to: "service.sync_performance_records"
      via: "sync_methods['performance']"
      pattern: "'performance':\\s*service\\.sync_performance_records"
---

<objective>
把 Plan 02 完成的 service 层能力暴露给 HTTP 与 Celery task 层，并闭合 per-sync_type 锁语义（D-15 / D-16）与 sync_type key 名统一（D-01 + Pitfall C / H）：

- `GET /api/v1/feishu/sync-logs` 升级支持 `sync_type`、`page`、`page_size` 三个 query param（D-05 / D-06）；response_model 仍是 `List[SyncLogRead]`（Plan 01 已扩展含 sync_type 字段）
- 新 `GET /api/v1/feishu/sync-logs/{log_id}/unmatched.csv` 端点（D-08）返回 CSV，admin+hrbp 守门
- `POST /api/v1/feishu/sync`（现 `trigger_sync`）的 409 语义升级：`is_sync_running(sync_type='attendance')` 分桶检查，错误响应含 `sync_type` 字段；**不写 FeishuSyncLog**（D-16）
- `FeishuService.get_sync_logs` 升级签名接收 `sync_type / page / page_size`；无参调用保留向后兼容
- `feishu_sync_eligibility_task` Celery task 修正 `sync_methods` dict：统一 key 为 `'performance'`，保留 `'performance_grades'` alias（过渡期）；调用 service 时传 `triggered_by=operator_id`
- `expire_stale_running_logs` 行为验证覆盖五类 sync_type（D-17，实现已在 Plan 02 但需补单测）

Output：
- API 层 3 个端点（list 扩展 + CSV 新增 + trigger 升级）
- Service 层 get_sync_logs 扩展
- Celery task 层 key 统一 + triggered_by 传递
- 4 个新测试文件（list API / CSV API / expire stale / task keys）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-feishu-sync-observability/31-CONTEXT.md
@.planning/phases/31-feishu-sync-observability/31-RESEARCH.md
@.planning/phases/31-feishu-sync-observability/31-VALIDATION.md
@.planning/phases/31-feishu-sync-observability/31-01-SUMMARY.md
@.planning/phases/31-feishu-sync-observability/31-02-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/research/PITFALLS.md

@backend/app/api/v1/feishu.py
@backend/app/services/feishu_service.py
@backend/app/tasks/feishu_sync_tasks.py
@backend/app/schemas/feishu.py
@backend/app/dependencies.py

<interfaces>
<!-- 既定契约 / 必须遵守的签名。Executor 直接复用。 -->

Plan 01 导出的 Pydantic Literal（from backend.app.schemas.feishu import SyncTypeLiteral, SyncStatusLiteral）。

Plan 02 导出的 service 方法（from backend.app.services.feishu_service）：
- `FeishuService.sync_performance_records(*, app_token, table_id, field_mapping=None, triggered_by=None) -> FeishuSyncLog`
- `FeishuService.sync_salary_adjustments(*, app_token, table_id, field_mapping=None, triggered_by=None) -> FeishuSyncLog`
- `FeishuService.sync_hire_info(*, app_token, table_id, field_mapping=None, triggered_by=None) -> FeishuSyncLog`
- `FeishuService.sync_non_statutory_leave(*, app_token, table_id, field_mapping=None, triggered_by=None) -> FeishuSyncLog`
- `FeishuService.sync_attendance(mode: str, triggered_by: str | None = None) -> FeishuSyncLog`
- `FeishuService.is_sync_running(sync_type: str | None = None) -> bool`
- `FeishuService.expire_stale_running_logs(timeout_minutes: int = 30) -> int`

现有 `get_sync_logs` 签名（feishu_service.py line ~1290）：

```python
def get_sync_logs(self, limit: int = 20) -> list[FeishuSyncLog]:
    """Return last N sync logs ordered by started_at desc."""
    stmt = select(FeishuSyncLog).order_by(FeishuSyncLog.started_at.desc()).limit(limit)
    return list(self.db.scalars(stmt))
```

Phase 31 升级为（保留 limit 兼容 AttendanceManagement 轮询）：

```python
def get_sync_logs(
    self,
    *,
    sync_type: str | None = None,
    page: int = 1,
    page_size: int = 20,
    limit: int | None = None,   # 向后兼容：旧调用 get_sync_logs(limit=20)
) -> list[FeishuSyncLog]:
    ...
```

现有 `trigger_sync` 路由（backend/app/api/v1/feishu.py:189-225）：

```python
@router.post('/sync', response_model=SyncTriggerResponse)
def trigger_sync(...):
    service = FeishuService(db)
    service.expire_stale_running_logs(timeout_minutes=30)
    if service.is_sync_running():  # Phase 31: 改为 is_sync_running(sync_type='attendance')
        raise HTTPException(
            status_code=status.HTTP_409_CONFLICT,
            detail={'error': 'sync_in_progress', 'message': '同步正在进行中，请稍后再试'},
        )
    # Phase 31: detail 增加 sync_type 字段
    ...
```

现有 `get_sync_logs` 路由（backend/app/api/v1/feishu.py:228-237）：

```python
@router.get('/sync-logs', response_model=list[SyncLogRead])
def get_sync_logs(
    limit: int = 20,
    db: Session = Depends(get_db),
    _current_user: User = Depends(require_roles('admin', 'hrbp')),
) -> list[SyncLogRead]:
    service = FeishuService(db)
    logs = service.get_sync_logs(limit=limit)
    return [_sync_log_to_read(log) for log in logs]
```

Phase 31 升级为：

```python
@router.get('/sync-logs', response_model=list[SyncLogRead])
def get_sync_logs(
    sync_type: SyncTypeLiteral | None = Query(default=None),
    page: int = Query(default=1, ge=1),
    page_size: int = Query(default=20, ge=1, le=100),
    db: Session = Depends(get_db),
    _current_user: User = Depends(require_roles('admin', 'hrbp')),
) -> list[SyncLogRead]:
    ...
```

现有 Celery task sync_methods dict（backend/app/tasks/feishu_sync_tasks.py:37-42）：

```python
sync_methods = {
    'performance_grades': service.sync_performance_records,
    'salary_adjustments': service.sync_salary_adjustments,
    'hire_info': service.sync_hire_info,
    'non_statutory_leave': service.sync_non_statutory_leave,
}
method = sync_methods.get(sync_type)
if method is None:
    return {'status': 'failed', 'error': f'Unsupported sync_type: {sync_type}'}
result = method(app_token=app_token, table_id=table_id, field_mapping=field_mapping)
```

Phase 31 升级为：

```python
canonical_sync_type = 'performance' if sync_type == 'performance_grades' else sync_type

sync_methods = {
    'performance': service.sync_performance_records,
    'performance_grades': service.sync_performance_records,  # alias (TODO phase-32: remove)
    'salary_adjustments': service.sync_salary_adjustments,
    'hire_info': service.sync_hire_info,
    'non_statutory_leave': service.sync_non_statutory_leave,
}
method = sync_methods.get(sync_type)
if method is None:
    return {'status': 'failed', 'error': f'Unsupported sync_type: {sync_type}'}

result = method(
    app_token=app_token,
    table_id=table_id,
    field_mapping=field_mapping,
    triggered_by=operator_id,
)
# result 现在是 FeishuSyncLog，不再是 dict — 需 serialize 出关键计数用于 update_state
```

`_sync_log_to_read` helper（feishu.py 文件内，搜 `_sync_log_to_read`）— 现有签名只需补新字段 sync_type / mapping_failed_count：

```python
def _sync_log_to_read(log: FeishuSyncLog) -> SyncLogRead:
    unmatched = None
    if log.unmatched_employee_nos:
        try:
            import json
            unmatched = json.loads(log.unmatched_employee_nos)
        except Exception:
            unmatched = None
    return SyncLogRead(
        id=log.id,
        sync_type=log.sync_type,                   # NEW (D-01)
        mode=log.mode,
        status=log.status,
        total_fetched=log.total_fetched,
        synced_count=log.synced_count,
        updated_count=log.updated_count,
        skipped_count=log.skipped_count,
        unmatched_count=log.unmatched_count,
        mapping_failed_count=log.mapping_failed_count,   # NEW (D-02)
        failed_count=log.failed_count,
        leading_zero_fallback_count=log.leading_zero_fallback_count,
        error_message=log.error_message,
        unmatched_employee_nos=unmatched,
        started_at=log.started_at,
        finished_at=log.finished_at,
        triggered_by=log.triggered_by,
    )
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 升级 FeishuService.get_sync_logs + API GET /sync-logs 支持 sync_type / page / page_size 过滤与分页 + _sync_log_to_read 映射两个新字段</name>
  <files>backend/app/services/feishu_service.py, backend/app/api/v1/feishu.py, backend/tests/test_api/test_feishu_sync_logs_api.py</files>
  <read_first>
    - backend/app/services/feishu_service.py — 现有 get_sync_logs（line ~1290）与 _sync_log_to_read 在 feishu.py 内的位置（grep `_sync_log_to_read`）
    - backend/app/api/v1/feishu.py — 现有 GET /sync-logs 路由（line ~228）、trigger_sync 路由（line ~189）、import 块（包括 require_roles, Query, Depends）
    - backend/app/schemas/feishu.py — SyncTypeLiteral / SyncLogRead（Plan 01 落地）
    - backend/app/dependencies.py — require_roles 签名
    - .planning/phases/31-feishu-sync-observability/31-CONTEXT.md §D-05 / D-06 — 路由与 Tab 查询参数契约
    - .planning/phases/31-feishu-sync-observability/31-RESEARCH.md §API 路由扩展示例代码
  </read_first>
  <behavior>
    get_sync_logs service 扩展：
    - Test 1: 无参调用 `service.get_sync_logs()` 返回 started_at desc 排序的全部日志（默认 page=1 page_size=20）
    - Test 2: `service.get_sync_logs(sync_type='performance')` 只返回 sync_type='performance' 的日志
    - Test 3: `service.get_sync_logs(limit=5)` 兼容旧签名，返回 5 条
    - Test 4: `service.get_sync_logs(page=2, page_size=3)` 返回第 4-6 条（offset=3）
    - Test 5: `service.get_sync_logs(sync_type='hire_info', page=1, page_size=20)` 过滤 + 分页组合

    GET /api/v1/feishu/sync-logs API：
    - Test 6: admin 调用 → 200，返回 JSON list 含 sync_type / mapping_failed_count 字段
    - Test 7: hrbp 调用 → 200
    - Test 8: employee 调用 → 403
    - Test 9: manager 调用 → 403
    - Test 10: 未登录 → 401
    - Test 11: `?sync_type=performance` 只返回 performance 类型日志
    - Test 12: `?sync_type=invalid_type` → 422（Pydantic Literal 校验）
    - Test 13: `?page=0` → 422（ge=1 校验）
    - Test 14: `?page_size=200` → 422（le=100 校验）
    - Test 15: `?page=2&page_size=5` 返回第 6-10 条记录
    - Test 16: 响应 JSON schema 含 sync_type / mapping_failed_count / status（四值之一）字段
  </behavior>
  <action>
**Step 1:** 升级 `FeishuService.get_sync_logs` 签名（feishu_service.py line ~1290）：

```python
def get_sync_logs(
    self,
    *,
    sync_type: str | None = None,
    page: int = 1,
    page_size: int = 20,
    limit: int | None = None,
) -> list[FeishuSyncLog]:
    """Phase 31 / D-05 / D-06: 列出同步日志（可按 sync_type 过滤 + 分页）。

    - 无参调用：返回 page=1 page_size=20 的最新日志（向后兼容 AttendanceManagement 轮询）
    - limit 参数保留向后兼容（旧调用 get_sync_logs(limit=20)）：有 limit 时忽略 page/page_size，只取前 limit 条
    - started_at desc 排序，offset 分页（Phase 31 不引入游标分页）
    """
    stmt = select(FeishuSyncLog).order_by(FeishuSyncLog.started_at.desc())
    if sync_type is not None:
        stmt = stmt.where(FeishuSyncLog.sync_type == sync_type)
    if limit is not None:
        stmt = stmt.limit(limit)
    else:
        stmt = stmt.offset((page - 1) * page_size).limit(page_size)
    return list(self.db.scalars(stmt))
```

**Step 2:** 升级 `_sync_log_to_read` helper（backend/app/api/v1/feishu.py，搜定位置）— 追加 `sync_type` 和 `mapping_failed_count` 两个 kwarg（Pydantic 模型接受 from ORM attribute）。如果现在 helper 已经完整 kwargs 方式构造，只需加两行；如果用 `SyncLogRead.model_validate(log)`（from_attributes）方式，不需要改 helper，Pydantic 会自动读 ORM attribute。**确认一种方式，严格执行**。

如果 helper 不是 kwargs 方式（例如 `SyncLogRead.model_validate(log)`），确认 from_attributes=True 已在 Plan 01 设置（是的），无需改 helper。

如果 helper 是 kwargs 方式，增补两行：
```python
sync_type=log.sync_type,
mapping_failed_count=log.mapping_failed_count,
```

**Step 3:** 升级 `GET /sync-logs` 路由（backend/app/api/v1/feishu.py）：

```python
from fastapi import Query
from backend.app.schemas.feishu import SyncTypeLiteral  # Plan 01 已导出


@router.get('/sync-logs', response_model=list[SyncLogRead])
def get_sync_logs(
    sync_type: SyncTypeLiteral | None = Query(default=None),
    page: int = Query(default=1, ge=1),
    page_size: int = Query(default=20, ge=1, le=100),
    db: Session = Depends(get_db),
    _current_user: User = Depends(require_roles('admin', 'hrbp')),
) -> list[SyncLogRead]:
    """Phase 31 / D-05 / D-06: 同步日志列表（按 sync_type Tab 筛选 + 分页）。"""
    service = FeishuService(db)
    logs = service.get_sync_logs(sync_type=sync_type, page=page, page_size=page_size)
    return [_sync_log_to_read(log) for log in logs]
```

**保留** 旧 `limit` query 行为的可行方案：因 `sync_type / page / page_size` 无冲突旧客户端默认 `page=1 page_size=20`，而 AttendanceManagement 页面 `getSyncLogs(limit=20)` 调用实际是前端 service 层（Plan 04 会改）。Phase 31 期间若前端 service 尚未更新，旧调用会因缺 `limit` 参数被忽略 → 返回前 20 条，与 `limit=20` 等价。**无需在路由保留 limit query param**（减少 API 冗余）。

**Step 4:** 创建 `backend/tests/test_api/test_feishu_sync_logs_api.py`（全文件，含本地 fixture）。

测试 fixture 沿用项目既定模式（参考 `backend/tests/test_api/` 下已有 feishu/eligibility 测试文件的 fixture 风格）。由于本项目无 conftest.py，本文件需完整构造 TestClient + 测试 DB + 测试用户（admin / hrbp / employee / manager）。核心断言覆盖 Test 1-16。

下列代码片段是**文件骨架**，executor 需按 <behavior> 的所有 Test N 补全每个测试函数：

```python
from __future__ import annotations

from datetime import datetime, timedelta, timezone

import pytest
from fastapi.testclient import TestClient
from sqlalchemy import create_engine
from sqlalchemy.orm import Session
from sqlalchemy.pool import StaticPool

from backend.app.core.database import Base, get_db
from backend.app.main import app
from backend.app.models.feishu_sync_log import FeishuSyncLog
from backend.app.models.user import User
from backend.app.core.security import create_access_token, hash_password


@pytest.fixture()
def engine():
    e = create_engine('sqlite://', connect_args={'check_same_thread': False}, poolclass=StaticPool)
    Base.metadata.create_all(e)
    return e


@pytest.fixture()
def db(engine):
    with Session(engine) as s:
        yield s


@pytest.fixture()
def client(engine, db):
    def _get_db_override():
        yield db
    app.dependency_overrides[get_db] = _get_db_override
    with TestClient(app) as c:
        yield c
    app.dependency_overrides.clear()


def _make_user(db, role: str, uid: str) -> User:
    u = User(
        id=uid,
        username=f'{role}-user',
        email=f'{role}@test.com',
        password_hash=hash_password('pw'),
        role=role,
        is_active=True,
    )
    db.add(u)
    db.commit()
    return u


def _auth(role: str, uid: str) -> dict[str, str]:
    token = create_access_token(data={'sub': uid, 'role': role})
    return {'Authorization': f'Bearer {token}'}


def _seed_log(db, sync_type: str, status: str = 'success', minutes_ago: int = 0) -> FeishuSyncLog:
    log = FeishuSyncLog(
        sync_type=sync_type,
        mode='full',
        status=status,
        total_fetched=10, synced_count=10, updated_count=0, skipped_count=0,
        unmatched_count=0, mapping_failed_count=0, failed_count=0,
        leading_zero_fallback_count=0,
        started_at=datetime.now(timezone.utc) - timedelta(minutes=minutes_ago),
    )
    db.add(log); db.commit(); db.refresh(log)
    return log


def test_list_requires_admin_or_hrbp(client, db):
    _make_user(db, 'employee', 'u-emp')
    resp = client.get('/api/v1/feishu/sync-logs', headers=_auth('employee', 'u-emp'))
    assert resp.status_code == 403


def test_list_admin_sees_all_sync_types(client, db):
    _make_user(db, 'admin', 'u-adm')
    for t in ('attendance', 'performance', 'hire_info'):
        _seed_log(db, t)
    resp = client.get('/api/v1/feishu/sync-logs', headers=_auth('admin', 'u-adm'))
    assert resp.status_code == 200
    data = resp.json()
    assert len(data) == 3
    assert {row['sync_type'] for row in data} == {'attendance', 'performance', 'hire_info'}


def test_list_filter_by_sync_type(client, db):
    _make_user(db, 'hrbp', 'u-hr')
    _seed_log(db, 'performance')
    _seed_log(db, 'attendance')
    resp = client.get('/api/v1/feishu/sync-logs?sync_type=performance', headers=_auth('hrbp', 'u-hr'))
    assert resp.status_code == 200
    data = resp.json()
    assert len(data) == 1
    assert data[0]['sync_type'] == 'performance'


def test_list_rejects_invalid_sync_type(client, db):
    _make_user(db, 'admin', 'u-adm')
    resp = client.get('/api/v1/feishu/sync-logs?sync_type=bogus', headers=_auth('admin', 'u-adm'))
    assert resp.status_code == 422


def test_list_pagination(client, db):
    _make_user(db, 'admin', 'u-adm')
    for i in range(10):
        _seed_log(db, 'performance', minutes_ago=i)  # 0..9 min ago; desc sort gives 0 first
    resp = client.get('/api/v1/feishu/sync-logs?page=2&page_size=5', headers=_auth('admin', 'u-adm'))
    assert resp.status_code == 200
    assert len(resp.json()) == 5


def test_list_rejects_page_zero(client, db):
    _make_user(db, 'admin', 'u-adm')
    resp = client.get('/api/v1/feishu/sync-logs?page=0', headers=_auth('admin', 'u-adm'))
    assert resp.status_code == 422


def test_list_rejects_oversize_page_size(client, db):
    _make_user(db, 'admin', 'u-adm')
    resp = client.get('/api/v1/feishu/sync-logs?page_size=200', headers=_auth('admin', 'u-adm'))
    assert resp.status_code == 422


def test_list_schema_has_new_fields(client, db):
    _make_user(db, 'admin', 'u-adm')
    _seed_log(db, 'performance', status='partial')
    resp = client.get('/api/v1/feishu/sync-logs', headers=_auth('admin', 'u-adm'))
    assert resp.status_code == 200
    row = resp.json()[0]
    assert 'sync_type' in row and row['sync_type'] == 'performance'
    assert 'mapping_failed_count' in row
    assert row['status'] in ('running', 'success', 'partial', 'failed')
```

Executor 补齐剩余 Test 函数（Test 1-5 在 service 层的单测应放在 `backend/tests/test_services/test_feishu_get_sync_logs.py` 或并入本文件；建议放 service 单测到 `backend/tests/test_services/test_feishu_sync_methods_write_log.py`（Plan 02）同类文件，或新增 `test_feishu_get_sync_logs.py` — 本 Plan 选择后者以分层清晰）。

**避免与原因：**
- 不删除 get_sync_logs 的 limit 参数（AttendanceManagement 页面在 Plan 04 之前仍用 `limit=20` 调用；保持兼容避免跨 Plan 破坏）
- 不在路由层验证 sync_type 是否在白名单之外再跑一次白名单 — Pydantic Literal 已在 Query 参数层校验（422 错误 Pydantic 自动生成）
- 不引入游标分页（Deferred：资格批量查询游标分页是 v1.5+ 项目级议题）
- 不改 _sync_log_to_read 的 unmatched_employee_nos JSON 解析逻辑（现有已正确）
- Query 参数顺序：`sync_type / page / page_size / db / current_user` — FastAPI 依赖注入参数在后
  </action>
  <verify>
    <automated>pytest backend/tests/test_api/test_feishu_sync_logs_api.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep `def get_sync_logs\(\s*self,\s*\*,\s*sync_type: str \| None = None,` in backend/app/services/feishu_service.py (multiline) returns 1 match
    - grep `sync_type: SyncTypeLiteral \| None = Query\(default=None\)` in backend/app/api/v1/feishu.py returns 1 match
    - grep `page: int = Query\(default=1, ge=1\)` in backend/app/api/v1/feishu.py returns 1 match
    - grep `page_size: int = Query\(default=20, ge=1, le=100\)` in backend/app/api/v1/feishu.py returns 1 match
    - grep `require_roles\('admin', 'hrbp'\)` appears in all 3 sync-logs related routes (list, csv, trigger)
    - `pytest backend/tests/test_api/test_feishu_sync_logs_api.py -x -v` prints all tests PASSED (min 8 tests)
    - `curl -s -H 'Authorization: Bearer {admin_token}' 'http://127.0.0.1:8011/api/v1/feishu/sync-logs?sync_type=performance'` returns HTTP 200 with JSON list where every row has `"sync_type": "performance"` (manual smoke, optional)
  </acceptance_criteria>
  <done>
    - get_sync_logs service + API 路由支持 sync_type / page / page_size
    - _sync_log_to_read 正确映射 sync_type / mapping_failed_count 到 SyncLogRead
    - 角色守门（admin/hrbp 200，employee/manager 403）经测试覆盖
    - Pydantic Query 边界校验（invalid sync_type / page=0 / page_size=200）正确返回 422
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 新 CSV 下载端点 GET /sync-logs/{log_id}/unmatched.csv + trigger_sync per-sync_type 锁 + Celery task sync_methods key 统一 + alias + triggered_by 传递</name>
  <files>backend/app/api/v1/feishu.py, backend/app/tasks/feishu_sync_tasks.py, backend/tests/test_api/test_feishu_unmatched_csv.py, backend/tests/test_tasks/test_feishu_sync_tasks_keys.py, backend/tests/test_services/test_feishu_expire_stale.py</files>
  <read_first>
    - backend/app/api/v1/feishu.py — trigger_sync (line 189-225) / get_sync_logs / 现有 import 块（确认 Response, csv, io, json 可用）
    - backend/app/tasks/feishu_sync_tasks.py — 完整文件（sync_methods dict + update_state + retry 逻辑）
    - backend/app/services/feishu_service.py Plan 02 后：is_sync_running(sync_type) + sync_performance_records(..., triggered_by=) 等签名
    - .planning/phases/31-feishu-sync-observability/31-CONTEXT.md §D-08 / D-15 / D-16 / D-17 — CSV 契约 + 409 语义 + 过渡期 alias
    - .planning/phases/31-feishu-sync-observability/31-RESEARCH.md §Pitfall C / G / H — 完整 checklist
    - backend/app/tasks/*.py — 参考其他 task 的 result serialization 模式
  </read_first>
  <behavior>
    CSV 端点（test_feishu_unmatched_csv.py）：
    - Test 1: admin 请求 /sync-logs/{id}/unmatched.csv → 200, Content-Type: text/csv; charset=utf-8, Content-Disposition: attachment; filename=sync-log-{id}-unmatched.csv
    - Test 2: CSV body 第一行 `employee_no`，后续为存储的 unmatched_employee_nos（最多 20 行）
    - Test 3: 日志含 30 个 unmatched_employee_nos → CSV 只含前 20 个
    - Test 4: 日志 unmatched_employee_nos=NULL / 空 list → CSV 仅含 header 行，无数据行
    - Test 5: 不存在的 log_id → 404
    - Test 6: employee 请求 → 403
    - Test 7: manager 请求 → 403
    - Test 8: 未登录 → 401
    - Test 9: CSV 对含特殊字符的 employee_no（逗号 / 换行 / 引号）正确转义（csv.writer 自动处理）

    trigger_sync 409 语义（test_feishu_sync_logs_api.py 再加测试 or 合并到本 CSV 文件最下方 session）：
    - Test 10: admin 在无 running log 情况下 POST /sync {mode='full'} → 200 / 202，sync 任务启动
    - Test 11: admin 在已有 sync_type='attendance' status='running' 的情况下 POST /sync → 409 + JSON {error: 'sync_in_progress', sync_type: 'attendance', message: ...}
    - Test 12: 409 发生时数据库里没有新增 FeishuSyncLog（D-16，只有之前的 running log 仍然是唯一一条）
    - Test 13: 409 不影响其他 sync_type — 在 attendance running 期间，若存在 Celery task 触发 sync_performance 则不被阻塞（行为由 service.is_sync_running('performance') == False 验证；API 层 trigger_sync 目前只服务考勤，不直接触发 performance；故该测试仅验证 service.is_sync_running('attendance')=True 且 is_sync_running('performance')=False）
    - Test 14: SC4: 连续两次完整触发（第一次完成后第二次）→ 产生两条独立 FeishuSyncLog（手工设置第一条 status='success' 后触发第二次）

    Celery task sync_methods（test_feishu_sync_tasks_keys.py）：
    - Test 15: 传 sync_type='performance' → 调用 service.sync_performance_records，传递 triggered_by=operator_id
    - Test 16: 传 sync_type='performance_grades' → 仍调用 service.sync_performance_records（alias），并 canonical_sync_type='performance' 用于日志/state
    - Test 17: 传 sync_type='salary_adjustments' / 'hire_info' / 'non_statutory_leave' → 对应 service 方法被调用
    - Test 18: 传 sync_type='bogus' → 返回 {'status': 'failed', 'error': 'Unsupported sync_type: bogus'}，不抛异常
    - Test 19: 传 sync_type='performance' + operator_id='u-1' → service method 接收到 triggered_by='u-1'

    expire_stale_running_logs（test_feishu_expire_stale.py）：
    - Test 20: 为每一种 sync_type 各插入一条 status='running' 且 started_at 早于 31 分钟的日志 → 调用 service.expire_stale_running_logs(timeout_minutes=30) → 五条 log 全部被标记为 status='failed'（或按当前实现标记）
    - Test 21: status='running' 且 started_at 在 30 分钟内 → 不被清理
  </behavior>
  <action>
**Step 1:** 添加 CSV 下载端点到 `backend/app/api/v1/feishu.py`（在现有 get_sync_logs 路由之后）：

```python
import csv
import io
import json

from fastapi import Response


@router.get('/sync-logs/{log_id}/unmatched.csv')
def download_unmatched_csv(
    log_id: str,
    db: Session = Depends(get_db),
    _current_user: User = Depends(require_roles('admin', 'hrbp')),
) -> Response:
    """Phase 31 / D-08: 下载未匹配工号 CSV（admin + hrbp）。

    - 单列 header: employee_no
    - 前 20 行（unmatched_employee_nos[:20]）
    - Content-Type: text/csv; charset=utf-8
    - Content-Disposition: attachment; filename=sync-log-{log_id}-unmatched.csv
    """
    log = db.get(FeishuSyncLog, log_id)
    if log is None:
        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail='sync log not found')

    nos: list[str] = []
    if log.unmatched_employee_nos:
        try:
            parsed = json.loads(log.unmatched_employee_nos)
            if isinstance(parsed, list):
                nos = [str(x) for x in parsed]
        except json.JSONDecodeError:
            logger.warning('Failed to parse unmatched_employee_nos for log_id=%s', log_id)

    buf = io.StringIO()
    writer = csv.writer(buf)
    writer.writerow(['employee_no'])
    for no in nos[:20]:
        writer.writerow([no])

    return Response(
        content=buf.getvalue(),
        media_type='text/csv; charset=utf-8',
        headers={
            'Content-Disposition': f'attachment; filename=sync-log-{log_id}-unmatched.csv',
        },
    )
```

**Step 2:** 升级 `trigger_sync` 路由（同 feishu.py 文件，line ~189）：

```python
@router.post('/sync', response_model=SyncTriggerResponse)
def trigger_sync(
    data: SyncTriggerRequest,
    db: Session = Depends(get_db),
    current_user: User = Depends(require_roles('admin', 'hrbp')),
) -> SyncTriggerResponse:
    """手动触发考勤同步（admin + hrbp），支持 full/incremental 模式。Phase 31 升级为 per-sync_type 锁。"""
    if data.mode not in ('full', 'incremental'):
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail='mode must be "full" or "incremental"',
        )

    service = FeishuService(db)

    # D-17: 过期清理所有 sync_type 的僵死 running log
    service.expire_stale_running_logs(timeout_minutes=30)

    # D-15/D-16: per-sync_type 分桶锁；trigger_sync 专服务考勤，只检查 attendance
    if service.is_sync_running(sync_type='attendance'):
        raise HTTPException(
            status_code=status.HTTP_409_CONFLICT,
            detail={
                'error': 'sync_in_progress',
                'sync_type': 'attendance',
                'message': '考勤同步正在进行中，请稍后再试',
            },
        )

    thread = threading.Thread(
        target=_run_sync_in_background,
        args=(data.mode, current_user.id),
        daemon=True,
    )
    thread.start()

    return SyncTriggerResponse(
        sync_log_id='pending',
        status='running',
        message='同步已启动',
    )
```

**Step 3:** 升级 Celery task（backend/app/tasks/feishu_sync_tasks.py）：

```python
from __future__ import annotations

import logging

from backend.app.celery_app import celery_app
from backend.app.core.database import SessionLocal

logger = logging.getLogger(__name__)


@celery_app.task(
    name='tasks.feishu_sync_eligibility',
    bind=True,
    autoretry_for=(Exception,),
    max_retries=2,
    retry_backoff=True,
    soft_time_limit=600,
    time_limit=660,
)
def feishu_sync_eligibility_task(
    self,
    sync_type: str,
    app_token: str,
    table_id: str,
    field_mapping: dict[str, str],
    operator_id: str | None = None,
) -> dict:
    # Phase 31 / D-01 / Pitfall H: key 名统一为 'performance'，保留 'performance_grades' alias 过渡一个 release
    canonical_sync_type = 'performance' if sync_type == 'performance_grades' else sync_type

    self.update_state(
        state='PROGRESS',
        meta={
            'sync_type': canonical_sync_type,
            'processed': 0, 'total': 0, 'errors': 0, 'user_id': operator_id,
        },
    )
    db = SessionLocal()
    try:
        from backend.app.services.feishu_service import FeishuService

        service = FeishuService(db)

        sync_methods = {
            'performance': service.sync_performance_records,
            'performance_grades': service.sync_performance_records,  # TODO(phase-32): remove alias after drain
            'salary_adjustments': service.sync_salary_adjustments,
            'hire_info': service.sync_hire_info,
            'non_statutory_leave': service.sync_non_statutory_leave,
        }

        method = sync_methods.get(sync_type)
        if method is None:
            return {'status': 'failed', 'error': f'Unsupported sync_type: {sync_type}'}

        # Phase 31 / D-10: method now returns FeishuSyncLog (not dict)
        result_log = method(
            app_token=app_token,
            table_id=table_id,
            field_mapping=field_mapping,
            triggered_by=operator_id,
        )

        # Serialize counters from FeishuSyncLog for Celery state + return payload
        result = {
            'sync_log_id': result_log.id,
            'sync_type': canonical_sync_type,
            'status': result_log.status,
            'synced': result_log.synced_count,
            'updated': result_log.updated_count,
            'unmatched': result_log.unmatched_count,
            'mapping_failed': result_log.mapping_failed_count,
            'failed': result_log.failed_count,
            'total': result_log.total_fetched,
            'leading_zero_fallback_count': result_log.leading_zero_fallback_count,
        }

        self.update_state(
            state='PROGRESS',
            meta={
                'sync_type': canonical_sync_type,
                'processed': result['synced'] + result['updated'] + result['unmatched'] + result['mapping_failed'] + result['failed'],
                'total': result['total'],
                'errors': result['failed'] + result['mapping_failed'],
                'user_id': operator_id,
            },
        )

        return {'status': 'completed', 'result': result}
    except Exception as exc:
        if self.request.retries >= self.max_retries:
            logger.exception('feishu_sync_eligibility_task failed after max retries: %s', exc)
            return {'status': 'failed', 'error': str(exc)}
        raise
    finally:
        db.close()
```

**Step 4:** 创建三个新测试文件：
- `backend/tests/test_api/test_feishu_unmatched_csv.py` — 覆盖 Test 1-9（CSV 端点）+ Test 10-14（409 + SC4）；fixture 复用 Plan 03 Task 1 的 client/db/seed_log 模式
- `backend/tests/test_tasks/test_feishu_sync_tasks_keys.py` — 覆盖 Test 15-19（Celery task key migration）；mock FeishuService 方法
- `backend/tests/test_services/test_feishu_expire_stale.py` — 覆盖 Test 20-21（五类 sync_type 的 expire）

如果 `backend/tests/test_tasks/` 不存在，先创建 `__init__.py`。

**test_feishu_sync_tasks_keys.py 样例：**

```python
from __future__ import annotations

from unittest.mock import MagicMock, patch

import pytest


def test_sync_type_performance_calls_sync_performance_records():
    from backend.app.tasks.feishu_sync_tasks import feishu_sync_eligibility_task

    mock_service = MagicMock()
    mock_log = MagicMock(
        id='log-1', status='success',
        synced_count=5, updated_count=0, unmatched_count=0,
        mapping_failed_count=0, failed_count=0, total_fetched=5,
        leading_zero_fallback_count=0,
    )
    mock_service.sync_performance_records.return_value = mock_log

    with patch('backend.app.services.feishu_service.FeishuService', return_value=mock_service):
        # Call task synchronously (Celery eager mode or direct invocation)
        # Use the .run() method or call the wrapped function directly
        result = feishu_sync_eligibility_task.apply(
            args=['performance', 'app', 'tbl', {'f': 's'}, 'user-1']
        ).result

    assert result['status'] == 'completed'
    assert result['result']['sync_type'] == 'performance'
    mock_service.sync_performance_records.assert_called_once()
    call_kwargs = mock_service.sync_performance_records.call_args.kwargs
    assert call_kwargs['triggered_by'] == 'user-1'


def test_legacy_key_performance_grades_still_routes_to_performance_records():
    from backend.app.tasks.feishu_sync_tasks import feishu_sync_eligibility_task

    mock_service = MagicMock()
    mock_log = MagicMock(
        id='log-2', status='success',
        synced_count=0, updated_count=0, unmatched_count=0,
        mapping_failed_count=0, failed_count=0, total_fetched=0,
        leading_zero_fallback_count=0,
    )
    mock_service.sync_performance_records.return_value = mock_log

    with patch('backend.app.services.feishu_service.FeishuService', return_value=mock_service):
        result = feishu_sync_eligibility_task.apply(
            args=['performance_grades', 'app', 'tbl', {'f': 's'}, 'user-2']
        ).result

    assert result['status'] == 'completed'
    assert result['result']['sync_type'] == 'performance'  # canonical form
    mock_service.sync_performance_records.assert_called_once()


def test_unknown_sync_type_returns_failed():
    from backend.app.tasks.feishu_sync_tasks import feishu_sync_eligibility_task
    result = feishu_sync_eligibility_task.apply(
        args=['bogus', 'app', 'tbl', {}, None]
    ).result
    assert result['status'] == 'failed'
    assert 'bogus' in result['error']
```

**Step 5:** 验证 Celery 测试需要项目已有 eager 配置 — 检查 `backend/app/celery_app.py` 是否支持 `task_always_eager=True` 测试模式；若不支持，用 `feishu_sync_eligibility_task.apply(...).result` 直接调用（Celery 支持 eager fall-back）。如失败改用直接调用私有函数 `feishu_sync_eligibility_task.__wrapped__(*args)`。

**避免与原因：**
- 不用 `StreamingResponse` 做 CSV（20 行小数据，`Response` 更简洁；Research §Alternatives 明确）
- 不在 CSV 响应添加 BOM — UTF-8 charset header 已明确，Excel 中文编码问题由 HR 客户端处理
- 不给 CSV 添加 defense-in-depth `'` prefix on employee_no（Research Security §CSV Injection — defense in depth；本 phase accept，employee_no 来自飞书/DB 非用户直接输入）
- 409 detail 必须含 sync_type 字段（前端 Plan 04 按 sync_type 显示 toast）
- 不在 trigger_sync 内触发非考勤 sync_type（Phase 31 trigger_sync 专服务考勤；其他 sync_type 由 Celery 任务入口 `feishu_sync_eligibility_task` 触发）
- 不移除 `performance_grades` alias — 保留一个 release 过渡期；Phase 32 / Plan 作者在移除时统一清理 ELIGIBILITY_IMPORT_TYPES
- Celery task 内部把 FeishuSyncLog 对象序列化成 dict 返回（而非直接返回 ORM 对象）— Celery result backend 不能直接 pickle SQLAlchemy 实例，且前端消费的是 dict
  </action>
  <verify>
    <automated>pytest backend/tests/test_api/test_feishu_unmatched_csv.py backend/tests/test_api/test_feishu_sync_logs_api.py backend/tests/test_tasks/test_feishu_sync_tasks_keys.py backend/tests/test_services/test_feishu_expire_stale.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep `@router.get\('/sync-logs/\{log_id\}/unmatched.csv'\)` in backend/app/api/v1/feishu.py returns 1 match
    - grep `media_type='text/csv; charset=utf-8'` in backend/app/api/v1/feishu.py returns 1 match
    - grep `filename=sync-log-\{log_id\}-unmatched.csv` in backend/app/api/v1/feishu.py returns 1 match
    - grep `is_sync_running\(sync_type='attendance'\)` in backend/app/api/v1/feishu.py returns 1 match
    - grep `'sync_type': 'attendance'` in the 409 detail dict of feishu.py returns 1 match
    - grep `'performance': service\.sync_performance_records` in backend/app/tasks/feishu_sync_tasks.py returns 1 match
    - grep `'performance_grades': service\.sync_performance_records` in backend/app/tasks/feishu_sync_tasks.py returns 1 match (alias)
    - grep `canonical_sync_type = 'performance' if sync_type == 'performance_grades' else sync_type` in backend/app/tasks/feishu_sync_tasks.py returns 1 match
    - grep `triggered_by=operator_id` in backend/app/tasks/feishu_sync_tasks.py returns 1 match
    - grep `TODO\(phase-32\): remove alias` in backend/app/tasks/feishu_sync_tasks.py returns 1 match
    - `pytest backend/tests/test_api/test_feishu_unmatched_csv.py backend/tests/test_api/test_feishu_sync_logs_api.py backend/tests/test_tasks/test_feishu_sync_tasks_keys.py backend/tests/test_services/test_feishu_expire_stale.py -x -v` prints all PASSED (min 20 tests)
    - `pytest backend/tests -k feishu -x` 全回归绿（Phase 30/31 所有 feishu 相关测试）
  </acceptance_criteria>
  <done>
    - CSV 端点 + trigger_sync 409 + Celery task key 迁移三件事全部落地
    - 所有 feishu 相关测试回归绿（Phase 30 leading_zero + Phase 31 observability）
    - feishu_sync_eligibility_task 支持 alias `performance_grades` + `performance` 双 key 一个 release 过渡
    - Celery task 传递 triggered_by=operator_id 到 service method
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → /api/v1/feishu/* | JWT bearer token authenticated + role-gated via `require_roles('admin', 'hrbp')`; untrusted query params |
| Celery broker → task worker | Task args include sync_type (whitelist check)、field_mapping (dict from HR config)、app_token/table_id (from FeishuConfig) |
| CSV response → Excel client | employee_no values may contain chars requiring CSV escape (csv.writer handles quoting) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-12 | Information disclosure | CSV endpoint leaks employee_no to non-HR roles | mitigate | `require_roles('admin', 'hrbp')` enforced on route; tests Test 6-8 verify employee/manager/unauthenticated return 403/401; Pitfall G explicitly covered |
| T-31-13 | Tampering | SQL injection on log_id path param | mitigate | `db.get(FeishuSyncLog, log_id)` uses SQLAlchemy parameterized query; log_id treated as opaque UUID string by ORM |
| T-31-14 | Information disclosure | Frontend route can be reached by non-HR users seeing SyncLogsPage shell | transfer | Frontend `<ProtectedRoute allowedRoles={['admin', 'hrbp']}/>` (Plan 04) + backend API 403 (this plan) = defense in depth; Pitfall I addressed by Plan 04 |
| T-31-15 | Denial of Service | large page_size exhausts DB / response size | mitigate | `page_size: int = Query(le=100)` caps at 100; `limit` backward-compat also bounded by caller convention |
| T-31-16 | Tampering | CSV injection (employee_no contains `=HYPERLINK(...)`) executes in Excel | accept | employee_no sourced from Feishu/DB (not direct user input); defense-in-depth `'` prefix not added per research §Alternatives; documented in SUMMARY as v1.5+ candidate |
| T-31-17 | Tampering | Concurrent 409-bypass: two workers both pass is_sync_running check | accept | Non-critical race window; if both proceed, two running logs exist briefly; helper's independent session ensures both finalize correctly; no data loss; expire_stale_running_logs cleans up stuck |
| T-31-18 | Information disclosure | 404 vs 403 timing diff reveals existence of log_id | accept | Project-wide policy accepts this (Research Security §Timing attack); not Phase 31 to fix |
| T-31-19 | Repudiation | Celery task alias `performance_grades` vs `performance` creates ambiguity in audit logs | mitigate | `canonical_sync_type` normalization ensures FeishuSyncLog.sync_type always = 'performance' (never 'performance_grades'); alias only accepted at task entry, never written to log |
| T-31-20 | Elevation of privilege | operator_id trusted by Celery task | accept | operator_id set by backend code (API layer takes `current_user.id`), never from client; Celery broker is internal trust boundary |
</threat_model>

<verification>
- `pytest backend/tests -k feishu -x -v` 全绿（30/31 feishu 相关测试）
- `pytest backend/tests -x -v` 全绿（整项目回归）
- `curl -s -H "Authorization: Bearer {admin_token}" "http://127.0.0.1:8011/api/v1/feishu/sync-logs/{id}/unmatched.csv" -o out.csv` — 手动 smoke，文件开头为 `employee_no\n`
- `grep "'performance_grades'" backend/app/services/feishu_service.py` → 0 行（只存在于 celery task alias）
- `python -c "from backend.app.tasks.feishu_sync_tasks import feishu_sync_eligibility_task; print(feishu_sync_eligibility_task.name)"` 打印 `tasks.feishu_sync_eligibility`（task name 不变）
- Celery broker 中 in-flight 的 `performance_grades` 任务通过 alias 能正确路由到 sync_performance_records
</verification>

<success_criteria>
- IMPORT-03 API 层就绪：HR 通过 `/api/v1/feishu/sync-logs?sync_type=...` 能看到所有 5 类同步的日志
- IMPORT-04 API 层就绪：CSV 下载端点能拿到前 20 个未匹配工号；trigger_sync 按 per-sync_type 锁阻止并发考勤同步，409 不写 log
- SC1（Roadmap Phase 31 SC1）：四个新 sync 方法每次执行在 FeishuSyncLog 产生一条记录，含五类计数器（通过 Plan 02 helper + Plan 03 Celery task triggered_by 联通验证）
- SC3（Roadmap SC3）：CSV 下载 / sync_type Tab 筛选 API 就绪，Plan 04 可直接消费
- SC4（Roadmap SC4）：两次独立触发产生两条 log；锁定期内的重复触发返回 409 不写 log（Pitfall D）
- Pitfall C（sync_type key 名统一）+ Pitfall H（in-flight task 兼容）闭合：alias `performance_grades` 保留，canonical_sync_type 规范化；grep feishu_service 不再出现 `performance_grades`
- Pitfall G 闭合：CSV 端点严格 admin+hrbp，已测试覆盖
</success_criteria>

<output>
After completion, create `.planning/phases/31-feishu-sync-observability/31-03-SUMMARY.md` summarizing:
- GET /sync-logs 支持的 query param 组合（sync_type + page + page_size）+ 默认值
- CSV 端点契约（URL / headers / content）
- trigger_sync 409 detail JSON 结构（error / sync_type / message）
- Celery task canonical_sync_type 规范化逻辑 + alias 生命周期（TODO phase-32 移除）
- 前端 Plan 04 需要的接口清单：`GET /sync-logs?sync_type=&page=&page_size=` / `GET /sync-logs/{id}/unmatched.csv`
- 运维上线 checklist（升级前 drain Celery broker 中的 performance_grades 任务；Alembic 已由 Plan 01 完成）
</output>
