---
phase: 36-historical-performance-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/36_01_add_comment_to_performance_records.py
  - backend/app/models/performance_record.py
  - backend/app/schemas/performance.py
  - backend/app/services/import_service.py
  - backend/app/services/performance_service.py
  - backend/tests/test_models/test_schema_models.py
  - backend/tests/test_services/test_import_service.py
  - backend/tests/test_services/test_performance_service.py
autonomous: true
requirements:
  - PERF-07
requirements_addressed:
  - PERF-07

must_haves:
  truths:
    - "PerformanceRecord 表新增 comment 列，nullable Text 类型"
    - "存量行 comment 值保持 NULL，不回填"
    - "POST /api/v1/performance/records 请求体支持可选 comment"
    - "Excel 导入 performance_grades 识别「评语/comment/备注」三种表头别名并落库"
    - "飞书同步 sync_performance_records 对 comment 写 None（本期不做 mapping 扩展）"
  artifacts:
    - path: "alembic/versions/36_01_add_comment_to_performance_records.py"
      provides: "Alembic migration 新增 comment 列 + up/down"
      contains: "add_comment_to_performance_records"
    - path: "backend/app/models/performance_record.py"
      provides: "PerformanceRecord.comment 字段定义"
      contains: "comment: Mapped[str | None]"
    - path: "backend/app/schemas/performance.py"
      provides: "PerformanceRecordRead/CreateRequest 新增 comment + 新增 PerformanceHistoryResponse"
      exports:
        - "PerformanceRecordRead"
        - "PerformanceRecordCreateRequest"
        - "PerformanceHistoryResponse"
    - path: "backend/app/services/import_service.py"
      provides: "_import_performance_grades 写入 comment + COLUMN_ALIASES['performance_grades'] 增加别名"
      contains: "评语"
    - path: "backend/app/services/performance_service.py"
      provides: "PerformanceService.create_record() 支持 comment 参数（D-03）"
      contains: "comment"
  key_links:
    - from: "backend/app/models/performance_record.py"
      to: "alembic/versions/36_01_add_comment_to_performance_records.py"
      via: "列定义与 migration add_column 同 schema"
      pattern: "comment.*String|Text"
    - from: "backend/app/services/import_service.py"
      to: "backend/app/models/performance_record.py"
      via: "_import_performance_grades 写入 record.comment"
      pattern: "comment\\s*=|\\.comment"
    - from: "backend/app/schemas/performance.py"
      to: "backend/app/services/performance_service.py"
      via: "PerformanceRecordRead.comment 由 ORM from_attributes 自动序列化"
      pattern: "from_attributes=True"
---

<objective>
Phase 36 第一步：后端数据层基础设施。交付 D-02 + D-03 要求的全部存储/写入能力，为 Plan 02（新端点）与 Plan 03（前端展示）提供字段就绪的数据源。

Purpose: PerformanceRecord 表长期缺 comment 字段，导致「评语」列无法落库；同时 Excel 导入缺可选列支持。本 plan 统一补齐 model / schema / migration / Import / Service.create_record 5 处，避免字段「落库没端点、端点没字段」的不一致状态。

Schema 命名说明（revision iteration 1 补充）：
本 plan 采纳 CONTEXT § Claude's Discretion 行 113 允许的 `PerformanceHistoryResponse` 命名（与 CONTEXT D-04 行 50 的 `PerformanceRecordsByEmployeeResponse` 等价 — Discretion 允许 planner 自选）。所有 schema/TS 类型/端点 response_model 统一使用 `PerformanceHistoryResponse`，Plan 02/03/04 全部对齐该命名。

Output: 
  - Alembic 迁移 `36_01_add_comment_to_performance_records`（仅 schema，不跑数据回填）
  - `PerformanceRecord.comment: Mapped[str | None]` 字段
  - Pydantic `PerformanceRecordRead.comment` + `PerformanceRecordCreateRequest.comment` + 新增 `PerformanceHistoryResponse`
  - ImportService `_import_performance_grades` 支持可选 `comment` 列；`COLUMN_ALIASES['performance_grades']` 加三别名「评语/comment/备注」
  - PerformanceService.create_record() 支持 comment 参数 + UPSERT 刷新 comment
  - pytest ≥ 7 个新用例（migration smoke + schema + 2 条 Service + 3 条 Import：有/无/别名）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/36-historical-performance-display/36-CONTEXT.md
@.planning/phases/34-performance-management-service-and-api/34-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@CLAUDE.md

# 现有代码参考（阅读后在对应位置扩展）
@backend/app/models/performance_record.py
@backend/app/schemas/performance.py
@backend/app/services/performance_service.py
@backend/app/services/import_service.py
@alembic/versions/p34_01_add_department_snapshot_to_performance_records.py
@alembic/versions/p34_02_add_performance_tier_snapshot_table.py

<interfaces>
<!-- 当前 PerformanceRecord 模型（Phase 34 落地后），本 plan 在其上追加 comment 字段 -->

From backend/app/models/performance_record.py:
```python
class PerformanceRecord(UUIDPrimaryKeyMixin, CreatedAtMixin, UpdatedAtMixin, Base):
    __tablename__ = 'performance_records'
    __table_args__ = (UniqueConstraint('employee_id', 'year', name='uq_performance_employee_year'),)
    employee_id: Mapped[str] = mapped_column(String(36), ForeignKey('employees.id'), nullable=False, index=True)
    employee_no: Mapped[str] = mapped_column(String(64), nullable=False, index=True)
    year: Mapped[int] = mapped_column(Integer, nullable=False, index=True)
    grade: Mapped[str] = mapped_column(String(8), nullable=False, comment='A/B/C/D/E')
    source: Mapped[str] = mapped_column(String(32), nullable=False, default='manual')
    department_snapshot: Mapped[str | None] = mapped_column(String(100), nullable=True)
    # ← 本 plan 追加：comment: Mapped[str | None] = mapped_column(Text, nullable=True)
```

From backend/app/schemas/performance.py（现有）:
```python
class PerformanceRecordRead(BaseModel):
    model_config = ConfigDict(from_attributes=True)
    id: str
    employee_id: str
    employee_no: str
    employee_name: str = ''
    year: int
    grade: str
    source: str
    department_snapshot: str | None = None
    created_at: datetime
    # ← 本 plan 追加：comment: str | None = None

class PerformanceRecordCreateRequest(BaseModel):
    employee_id: str = Field(..., min_length=1, max_length=36)
    year: int = Field(..., ge=2000, le=2100)
    grade: str = Field(..., min_length=1, max_length=2)
    source: str = Field(default='manual', pattern='^(manual|excel|feishu)$')
    # ← 本 plan 追加：comment: str | None = Field(default=None, max_length=2000)
```

From backend/app/services/import_service.py（现有 COLUMN_ALIASES['performance_grades']）:
```python
'performance_grades': {
    '员工工号': 'employee_no',
    '年度': 'year',
    '绩效等级': 'grade',
    # ← 本 plan 追加三项：
    # '评语': 'comment',
    # 'comment': 'comment',
    # '备注': 'comment',
}
```

<!-- Phase 34 migration chain 当前 head -->
down_revision chain: ... → p34_01_dept_snapshot → p34_02_tier_snapshot → [本 plan 36_01_add_comment]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Alembic 迁移 + PerformanceRecord.comment 字段</name>
  <files>
    alembic/versions/36_01_add_comment_to_performance_records.py,
    backend/app/models/performance_record.py,
    backend/tests/test_models/test_schema_models.py
  </files>
  <read_first>
    - alembic/versions/p34_01_add_department_snapshot_to_performance_records.py（模板：同款 add column 风格）
    - alembic/versions/p34_02_add_performance_tier_snapshot_table.py（确认 down_revision head）
    - backend/app/models/performance_record.py（现有 6 字段）
    - .planning/phases/36-historical-performance-display/36-CONTEXT.md § D-02
    - backend/tests/test_models/test_schema_models.py（已有 schema 测试风格）
  </read_first>
  <behavior>
    - 测试 1：create 一条 PerformanceRecord 不传 comment，DB 持久化后 comment 为 None（不报错，默认值 = None）
    - 测试 2：create 一条 PerformanceRecord 传 comment='优秀执行力，Q4 超额完成 120%'，DB 持久化后读回值相同
    - 测试 3：Alembic 升级后 `inspect(engine).get_columns('performance_records')` 包含名为 'comment' 的列，type 为 TEXT 或 String/VARCHAR（取决于 dialect），nullable=True
    - 测试 4：Alembic downgrade 后该列被删除
  </behavior>
  <action>
    1. 创建 `alembic/versions/36_01_add_comment_to_performance_records.py`：
       - `revision = '36_01_add_comment_perf'`
       - **down_revision 兜底话术（revision iteration 1 修正）**：先在终端跑 `alembic heads` 确认当前 head；若是 `p34_02_tier_snapshot` 直接用该值；若不是（例如仓库中已有其他更后的 head），以实际 head 为 `down_revision`，并在 `36-01-SUMMARY.md` 的「遗留项」小节记录偏差（哪个 head + 为什么不是 p34_02）。
       - `upgrade()` 用 `op.batch_alter_table('performance_records')` + `batch_op.add_column(sa.Column('comment', sa.Text(), nullable=True, comment='Phase 36 D-02：绩效评语，存量行 NULL；manual/excel 来源可写入；feishu 来源恒 None'))`
       - `downgrade()` 对应 `batch_op.drop_column('comment')`
       - **严格不跑** UPDATE 回填脚本（遵循 Phase 34 D-07 同款 NULL 语义）
    2. 修改 `backend/app/models/performance_record.py`：
       - import 新增 `from sqlalchemy import Text`（如果未导入）
       - 在 `department_snapshot` 字段下方追加：
         ```python
         comment: Mapped[str | None] = mapped_column(
             Text, nullable=True,
             comment='Phase 36 D-02：绩效评语；manual/excel 来源可写入，feishu 来源恒 None',
         )
         ```
    3. 在 `backend/tests/test_models/test_schema_models.py` 追加 test（或新建 `backend/tests/test_models/test_performance_record_comment.py`）：
       - Test `test_performance_record_comment_defaults_to_none`: create 不传 comment，assert record.comment is None
       - Test `test_performance_record_comment_persists_long_text`: create 传 500 字中文 comment，refresh 后 read 回相同值
       - Test `test_performance_record_comment_column_exists`: 用 `sqlalchemy.inspect(engine).get_columns('performance_records')` 确认 'comment' 列存在且 nullable
    4. 不改 `UniqueConstraint` / `default` / 其他字段。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust && .venv/bin/pytest backend/tests/test_models/ -x -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `ls alembic/versions/36_01_add_comment_to_performance_records.py` 返回 0
    - `grep -q "comment: Mapped\[str | None\]" backend/app/models/performance_record.py` 返回 0
    - `grep -q "Text" backend/app/models/performance_record.py` 返回 0（确认 import Text 用于 mapped_column）
    - **down_revision 放宽断言（revision iteration 1 修正）**：`grep -qE "^down_revision = '[^']+'" alembic/versions/36_01_add_comment_to_performance_records.py` 返回 0（不硬编码 `p34_02_tier_snapshot`，以 `alembic heads` 实际 head 为准；偏差记录在 SUMMARY）
    - `grep -q "drop_column('comment')" alembic/versions/36_01_add_comment_to_performance_records.py` 返回 0
    - `grep -q "add_column" alembic/versions/36_01_add_comment_to_performance_records.py` 返回 0
    - `grep -qE "UPDATE|回填" alembic/versions/36_01_add_comment_to_performance_records.py` 返回 **非 0**（严禁回填脚本）
    - pytest 新用例全部通过
  </acceptance_criteria>
  <done>
    Alembic migration 文件存在、model 字段可读写、schema 测试通过、migration 无回填脚本。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pydantic schema 扩展 + PerformanceService.create_record 支持 comment（D-03）</name>
  <files>
    backend/app/schemas/performance.py,
    backend/app/api/v1/performance.py,
    backend/app/services/performance_service.py,
    backend/tests/test_services/test_performance_service.py
  </files>
  <read_first>
    - backend/app/schemas/performance.py（现有 6 schemas，在文件末尾追加 PerformanceHistoryResponse）
    - backend/app/services/performance_service.py lines 120-175（现有 create_record；在 grade 校验后追加 comment 写入）
    - backend/app/api/v1/performance.py lines 86-114（现有 POST /records handler；传递 payload.comment）
    - .planning/phases/36-historical-performance-display/36-CONTEXT.md § D-03 + § Claude's Discretion (schema 命名)
    - backend/tests/test_services/ 下现有 test_performance_service.py 文件风格（若无则新建）
  </read_first>
  <behavior>
    - 测试 1：POST /records 带 comment='Q4 超额完成' → Service 落库 comment；read 回值相同
    - 测试 2：POST /records 不带 comment → 落库 comment 为 None（向后兼容旧 payload）
    - 测试 3：UPSERT 情况：先 create_record(emp_id, 2025, 'A') comment=None；再 create_record(emp_id, 2025, 'B', comment='改评') → 同 row update，grade='B' 且 comment='改评'（同时 department_snapshot 也被刷新，与 D-08 并存）
    - 测试 4：长度校验 — comment 传 3000 字 → Pydantic 422（Field max_length=2000）
  </behavior>
  <action>
    1. `backend/app/schemas/performance.py`：
       - 在 `PerformanceRecordRead` 的 `department_snapshot: str | None = None` 之后、`created_at` 之前追加：
         ```python
         comment: str | None = None
         ```
       - 在 `PerformanceRecordCreateRequest` 的 `source` 字段后追加：
         ```python
         comment: str | None = Field(default=None, max_length=2000)
         ```
       - 在文件末尾（AvailableYearsResponse 之后）追加新 schema：
         ```python
         class PerformanceHistoryResponse(BaseModel):
             """Phase 36 D-05：GET /performance/records/by-employee/{employee_id} 响应。
             
             不分页、按 year DESC 预排序的 flat items 数组。
             """
             items: list[PerformanceRecordRead]
         ```
    2. `backend/app/services/performance_service.py` `create_record` 方法签名追加 keyword arg `comment: str | None = None`，并在 UPSERT / new 两条路径都显式写入：
       - UPSERT 分支 `existing.comment = comment`
       - NEW 分支 `PerformanceRecord(... comment=comment)`
       - 不对 comment 做 strip/upper 变换（保留原文，NULL 保 NULL）
    3. `backend/app/api/v1/performance.py` `create_performance_record` handler 把 `comment=payload.comment` 传给 `service.create_record(...)`
    4. 在 `backend/tests/test_services/test_performance_service.py` 新增 4 条测试覆盖上面 behavior（若文件不存在则参照 Phase 34 交付的 test_performance_service.py 风格创建 isolated SQLite DB builder）
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust && .venv/bin/pytest backend/tests/test_services/test_performance_service.py -x -v 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "comment: str | None = None" backend/app/schemas/performance.py` 返回 0
    - `grep -q "comment: str | None = Field" backend/app/schemas/performance.py` 返回 0
    - `grep -q "class PerformanceHistoryResponse" backend/app/schemas/performance.py` 返回 0
    - `grep -qE "comment.*:.*str.*None.*=\s*None" backend/app/services/performance_service.py` 返回 0（create_record 签名）
    - `grep -q "existing.comment = comment" backend/app/services/performance_service.py` 返回 0
    - `grep -q "comment=payload.comment" backend/app/api/v1/performance.py` 返回 0
    - pytest test_performance_service.py 4 个新用例全部通过
  </acceptance_criteria>
  <done>
    schema 扩展 + Service 支持 + API 透传 comment 完成，UPSERT 同时刷新 comment 与 department_snapshot。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ImportService performance_grades 支持可选 comment 列 + COLUMN_ALIASES 扩展</name>
  <files>
    backend/app/services/import_service.py,
    backend/tests/test_services/test_import_service.py
  </files>
  <read_first>
    - backend/app/services/import_service.py lines 65-111（现有 COLUMN_ALIASES）
    - backend/app/services/import_service.py lines 840-940（现有 _import_performance_grades）
    - .planning/phases/36-historical-performance-display/36-CONTEXT.md § D-03
    - backend/tests/test_services/test_import_service.py（现有测试风格 + UploadStub / xlsx fixture builder）
  </read_first>
  <behavior>
    - 测试 1：上传 excel 含 '员工工号','年度','绩效等级','评语' 四列，每行 comment 有值 → 落库后 PerformanceRecord.comment 等于表格值
    - 测试 2：上传 excel 不含评语列（仅三必填列）→ 落库 PerformanceRecord.comment 为 None；导入不报错
    - 测试 3：上传 excel 用英文表头「comment」列 → 正确映射（通过 COLUMN_ALIASES 别名）
    - 测试 4：上传 excel 用「备注」中文表头 → 正确映射
    - **测试 5（revision iteration 1 锁定）**：merge 模式 UPSERT 锁定策略为「有列即写、无列不改」：
      - 5a：同一 (emp, year) 先 merge 模式导入无 comment 列 → comment 保持原值 NULL（新建时 None；已有时不变）
      - 5b：同一 (emp, year) 紧接着 merge 模式导入**含 comment 列**（值为 '评语A'）→ UPDATE 把 comment 从 NULL 改为 '评语A'
      - 即：`if comment_value is not None: existing.comment = comment_value`（删除「允许覆盖为 None」分支）
  </behavior>
  <action>
    1. 修改 `backend/app/services/import_service.py` COLUMN_ALIASES['performance_grades']（lines 86-90 附近），在现有 3 项后追加 3 行：
       ```python
       '评语': 'comment',
       'comment': 'comment',
       '备注': 'comment',
       ```
       注意：不要把「评语」加到 REQUIRED_COLUMNS（保持可选）。
    2. 修改 `_import_performance_grades`（lines 844-932 附近），在 grade 校验通过后、`existing` 查询分支前后两处写入 comment：
       - 取 `comment_raw = row.get('comment')`
       - 规范化：`comment_value: str | None = None if pd.isna(comment_raw) or comment_raw is None or str(comment_raw).strip() == '' else str(comment_raw).strip()`
       - **UPSERT 分支（existing is not None）**——**锁定策略（revision iteration 1）**：
         ```python
         if comment_value is not None:
             existing.comment = comment_value
         # 若 comment_value is None（excel 无 comment 列或该行 comment 空）→ 不改动 existing.comment，保留原值
         ```
         **删除「覆盖模式允许覆盖为 None」分支**。理由：D-03 语义「可选列不在时按 None 入库」仅适用于**新建**场景；UPSERT 语义是「有列即更新，无列即保留」，避免 HR 重复导入时无意清空历史评语。
       - **NEW 分支**：`record = PerformanceRecord(..., comment=comment_value)`（新建行可以写 None，与 D-03 语义一致）
    3. 不改动 `REQUIRED_COLUMNS`（grade 仍必填，comment 可选）。
    4. 不改动飞书同步路径（feishu 来源 comment 恒 None；D-03）
    5. 新增 xlsx fixture builder helper（若现有测试未提供）— 使用 openpyxl 构造 BytesIO，或复用 Phase 32 已有的 xlsx fixture builder（参考 test_import_service.py 现有用例）。
    6. 在 `backend/tests/test_services/test_import_service.py` 追加 5 条 behavior 测试（命名 `test_import_performance_grades_with_comment_*`），其中测试 5 拆为 `_merge_without_comment_column_keeps_existing` 和 `_merge_with_comment_column_updates_existing` 两条以精确锚定锁定策略。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust && .venv/bin/pytest backend/tests/test_services/test_import_service.py -x -v -k "performance_grades" 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "'评语': 'comment'" backend/app/services/import_service.py` 返回 0
    - `grep -q "'comment': 'comment'" backend/app/services/import_service.py` 返回 0
    - `grep -q "'备注': 'comment'" backend/app/services/import_service.py` 返回 0
    - `grep -qE "PerformanceRecord\(.*comment=" backend/app/services/import_service.py` 返回 0（NEW 分支写入 comment）
    - **UPSERT 锁定断言（revision iteration 1）**：`grep -qE "if comment_value is not None:\s*$" backend/app/services/import_service.py` 返回 0（锁定守卫存在）
    - `grep -q "existing.comment = comment_value" backend/app/services/import_service.py` 返回 0（UPSERT 分支在守卫内写入）
    - `grep -qE "REQUIRED_COLUMNS\[.performance_grades.\]" backend/app/services/import_service.py` 内容仅 `['employee_no', 'year', 'grade']`（comment 不作必填）
    - pytest test_import_service.py 5 条新用例全部通过（含 `_merge_without_comment_column_keeps_existing` + `_merge_with_comment_column_updates_existing`）
    - 现有 performance_grades 导入测试不被破坏
  </acceptance_criteria>
  <done>
    Excel 导入 performance_grades 接受 3 种表头别名、5 条新测试通过、merge 模式锁定「有列即写、无列不改」策略。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HR 上传的 Excel → ImportService 解析 | 可能被注入异常长文本/控制字符；Pandas 读取后传入 ORM |
| HR 手动 POST /records body → PerformanceService.create_record | 可能传入 3000+ 字文本 / XSS payload |
| Alembic migration 运行 → 生产 DB schema | 不应执行 UPDATE，仅 DDL |

## STRIDE Threat Register (Phase 36-01 本地范围；完整权限威胁在 Plan 02)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01-01 | Tampering | ImportService `_import_performance_grades` comment 处理 | mitigate | `str(comment_raw).strip()`，落库前规范化 None/空串；长度依赖 DB Text 字段（SQLite/PostgreSQL 无 hard limit，应用层靠 Pydantic Field max_length=2000 拦截 manual 路径；excel 路径信任 HR 但 DB Text 列无长度溢出风险） |
| T-36-01-02 | Information Disclosure | Pydantic PerformanceRecordRead.comment 序列化 | accept | comment 是业务内容（评语），非 PII；Phase 36 受众为 admin/hrbp/manager（见 Plan 02 权限），员工无法直读本字段 |
| T-36-01-03 | Tampering | Alembic migration 数据回填 | mitigate | `grep -qE "UPDATE\|回填"` acceptance_criteria 显式禁止；CLAUDE.md「Alembic 唯一 migration 路径」+ D-02「不回填」双重约束 |
| T-36-01-04 | Denial of Service | comment 字段 Text 类型无长度上限 | mitigate | Pydantic `Field(max_length=2000)` 卡 manual 路径；excel 路径 HR 可信但如需硬上限可后续加 VARCHAR(2000) |
| T-36-01-05 | Elevation of Privilege | import_service 绕过 Service 层权限 | accept | ImportService 由 `/eligibility-import/excel/confirm` 端点触发，该端点已 `require_roles('admin','hrbp')`（Phase 32 交付）；comment 字段新增不改变权限面 |
| T-36-01-06 | Tampering | UPSERT 清空既有 comment（revision iteration 1 新增） | mitigate | Task 3 Action 2 锁定 `if comment_value is not None: existing.comment = comment_value`，HR 重复导入无 comment 列时不会无意清空历史评语 |
</threat_model>

<verification>
1. Alembic migration 可 up + down（SQLite 环境 `alembic upgrade head` / `alembic downgrade -1`）
2. PerformanceRecord model 新字段可读写；现有 Phase 34 department_snapshot 逻辑不破坏
3. POST /api/v1/performance/records 接受可选 comment 字段
4. Excel 导入 performance_grades 识别 3 种中英文表头别名
5. 现有 performance_grades 导入单元测试全部通过（向后兼容）
6. `from __future__ import annotations` 在所有修改的 backend 模块顶部保留
7. PEP 604 union 语法一致（`str | None` 而非 `Optional[str]`）
</verification>

<success_criteria>
- [ ] Alembic 文件 `36_01_add_comment_to_performance_records.py` 存在；up/down 可逆；无 UPDATE/回填脚本
- [ ] `PerformanceRecord.comment` 字段可读写，nullable=True，Text 类型
- [ ] `PerformanceRecordRead.comment` 序列化正确（from_attributes=True）
- [ ] `PerformanceRecordCreateRequest.comment` Field(max_length=2000, default=None)
- [ ] `PerformanceHistoryResponse` schema 声明完成（Plan 02 使用）
- [ ] `PerformanceService.create_record(comment=...)` keyword 支持 + UPSERT 同时刷新
- [ ] Excel 导入 performance_grades 支持 3 种别名 + 可选列 + 锁定「有列即写、无列不改」
- [ ] pytest 新增测试 ≥ 9 条全部通过（Task 1: 4 + Task 2: 4 + Task 3: 5，实际可合并去重至 ≥ 9）
- [ ] 现有 backend/tests/ 全部通过（无回归）
</success_criteria>

<output>
完成后创建 `.planning/phases/36-historical-performance-display/36-01-SUMMARY.md`，列出：
- Alembic migration ID 与 down_revision 链路（若实际 head ≠ p34_02_tier_snapshot 需记录偏差）
- model / schema / service / import_service 变更位置（文件:行号）
- comment 字段 NULL 语义与 merge 模式锁定策略（`if comment_value is not None`）的决定依据
- 遗留项（如 Plan 02 依赖 PerformanceHistoryResponse）
</output>
</output>
