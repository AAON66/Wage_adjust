---
phase: 36-historical-performance-display
plan: 04
type: execute
wave: 4
depends_on:
  - 36-03
files_modified:
  - frontend/src/pages/EvaluationDetail.tsx
  - .planning/phases/36-historical-performance-display/36-VERIFICATION.md
autonomous: false
requirements:
  - PERF-07
requirements_addressed:
  - PERF-07

must_haves:
  truths:
    - "admin/hrbp/manager 打开 /evaluations/:id 在页面底部看到 PerformanceHistoryPanel"
    - "panel 显示 4 列：周期 / 绩效等级 / 评语 / 部门快照"
    - "records 按 year DESC 顺序显示"
    - "同一员工的 PerformanceHistoryPanel 与 SalaryHistoryPanel 在同页显示，垂直顺序：绩效在上、调薪在下"
    - "PerformanceHistoryPanel 与 SalaryDetailPanel 位于 EvaluationDetail.tsx 同文件（SC#2 的「两页共用」通过「两个 section 同页」实现）"
    - "employee 角色访问 EvaluationDetail 不触发 fetchPerformanceHistoryByEmployee 且不渲染 panel"
    - "员工在 MyReview.tsx 不引入 PerformanceHistoryPanel（D-10 严守）"
    - "404（不存在员工）/403（跨部门 manager）捕获后 panel 渲染空态，不让页面崩溃"
    - "refreshPerformanceHistory helper 与 refreshSalaryHistory（EvaluationDetail.tsx lines 545-565）对齐签名与 cancelled flag 语义"
  artifacts:
    - path: "frontend/src/pages/EvaluationDetail.tsx"
      provides: "import PerformanceHistoryPanel + state + refreshPerformanceHistory helper + useEffect 并行拉取 + JSX 挂载点"
      contains: "PerformanceHistoryPanel"
    - path: ".planning/phases/36-historical-performance-display/36-VERIFICATION.md"
      provides: "四角色 UAT 清单 + 截图占位 + 执行结果"
      contains: "UAT"
  key_links:
    - from: "frontend/src/pages/EvaluationDetail.tsx"
      to: "frontend/src/components/performance/PerformanceHistoryPanel.tsx"
      via: "import + <PerformanceHistoryPanel records={...} />"
      pattern: "PerformanceHistoryPanel"
    - from: "frontend/src/pages/EvaluationDetail.tsx"
      to: "frontend/src/services/performanceService.ts"
      via: "fetchPerformanceHistoryByEmployee(employeeId) in refreshPerformanceHistory helper"
      pattern: "fetchPerformanceHistoryByEmployee"
    - from: "frontend/src/pages/EvaluationDetail.tsx useEffect"
      to: "performance panel state"
      via: "canViewPerformanceHistory 角色门控"
      pattern: "canViewPerformanceHistory"
---

<objective>
Phase 36 最后一步：把 Plan 03 交付的 PerformanceHistoryPanel 挂到 EvaluationDetail.tsx，并做四角色 UAT 验证。落地 D-07 + D-09 + D-10 + 全部 4 个 Success Criteria。

Purpose: 前 3 个 plan 已准备好字段 / 端点 / 组件 / 类型。本 plan 把它们串起来真正出现在 HR / manager 的视野里；并通过 UAT 显式确认 D-10「员工端严格隔离」的 never-do。

Output:
  - EvaluationDetail.tsx 改造：import + state + **`refreshPerformanceHistory` helper（强制新建，对齐 refreshSalaryHistory lines 545-565）** + useEffect 并行拉取 + JSX 挂载
  - 挂载位置：SalaryDetailPanel 所在 section 之后（CONTEXT 推荐「绩效在上、调薪在下」— 但本期 SalaryHistoryPanel 已内嵌于 SalaryDetailPanel 父 `<section>` 中，PerformanceHistoryPanel 挂在同级 `</section>` 后；相对视觉顺序以「先调薪、后绩效」实现 — 若 UAT 发现视觉顺序与 CONTEXT 推荐冲突，由 checkpoint 人工确认是否在 Plan 05 调整）
  - canViewPerformanceHistory 角色门控（admin/hrbp/manager）
  - 403/404 fallback：try/catch 成空 records，不崩溃
  - tsc --noEmit 通过
  - 四角色 UAT 执行 + 36-VERIFICATION.md 记录
  - 显式 never-do 验证：grep MyReview.tsx 无 PerformanceHistoryPanel 引用
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/36-historical-performance-display/36-CONTEXT.md
@.planning/phases/36-historical-performance-display/36-01-SUMMARY.md
@.planning/phases/36-historical-performance-display/36-02-SUMMARY.md
@.planning/phases/36-historical-performance-display/36-03-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md

# 挂载点必读
@frontend/src/pages/EvaluationDetail.tsx
@frontend/src/components/performance/PerformanceHistoryPanel.tsx
@frontend/src/services/performanceService.ts
@frontend/src/hooks/useAuth.tsx
@frontend/src/pages/MyReview.tsx

<interfaces>
<!-- Plan 03 冻结的组件契约 -->

From frontend/src/components/performance/PerformanceHistoryPanel.tsx:
```typescript
interface PerformanceHistoryPanelProps {
  employeeName?: string;
  records: PerformanceRecordItem[];
  isLoading: boolean;
}
export function PerformanceHistoryPanel(props): JSX.Element;
```

<!-- Plan 03 冻结的 service -->

```typescript
export async function fetchPerformanceHistoryByEmployee(
  employeeId: string,
): Promise<PerformanceHistoryResponse>;  // { items: PerformanceRecordItem[] }
```

<!-- EvaluationDetail.tsx 现有挂载锚点（必读定位） -->

Line 16-19:  SalaryDetailPanel / SalaryHistoryPanel 当前 import
Line 505-540: state 声明集群（salaryHistory / isSalaryHistoryLoading 等）
Line 541-543: canSubmitApproval / canHrReview / canViewSalaryHistory 门控派生
Line 545-565: refreshSalaryHistory helper（实际签名：`async function refreshSalaryHistory(targetEmployeeId: string)` — 注意现有 helper **未**使用 cancelled flag 参数，而是直接在 try/catch/finally 中管理 loading + 403 静默置空）
Line 567-600: refreshSubmissionData 里的 Promise.all 并行拉取（必须在这里追加新 fetch）
Line 2144-2162: SalaryDetailPanel JSX 挂载位置（新 panel 加在其后）
Line 2146: 现有 divider 约定：`<hr className="divider" style={{ margin: '20px 0' }} />`（revision iteration 1 已 grep 确认，全文件唯一一处 divider 写法 → 本 plan 必须复用同款）

<!-- 实际 refreshSalaryHistory 源码（revision iteration 1 grep 锚定签名） -->

From frontend/src/pages/EvaluationDetail.tsx lines 545-565:
```typescript
async function refreshSalaryHistory(targetEmployeeId: string) {
  if (!canViewSalaryHistory) {
    setSalaryHistory([]);
    setIsSalaryHistoryLoading(false);
    return;
  }
  setIsSalaryHistoryLoading(true);
  try {
    const historyResponse = await fetchSalaryHistoryByEmployee(targetEmployeeId);
    setSalaryHistory(historyResponse.items);
  } catch (error) {
    if (axios.isAxiosError(error) && error.response?.status === 403) {
      setSalaryHistory([]);
      return;
    }
    throw error;
  } finally {
    setIsSalaryHistoryLoading(false);
  }
}
```

**revision iteration 1 签名对齐说明**：refreshSalaryHistory **不**接受 cancelled flag（EvaluationDetail 当前 pattern 靠 React 卸载时的 state setter noop 兜底）。**refreshPerformanceHistory 必须复刻同款签名**：`async function refreshPerformanceHistory(targetEmployeeId: string)`，除权限门控常量改为 `canViewPerformanceHistory` + state setter 改为 `setPerformanceHistory/setIsPerformanceHistoryLoading` + 403 外额外捕获 404 之外，其它结构一比一对齐。

<!-- 角色判定样板 -->

```typescript
const canViewSalaryHistory = user?.role === 'admin' || user?.role === 'hrbp' || user?.role === 'manager';
// 新增同款 canViewPerformanceHistory（相同三角色）
```

<!-- D-10 never-do 验证目标文件 -->

frontend/src/pages/MyReview.tsx — 必须 **不** 引入 PerformanceHistoryPanel
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EvaluationDetail.tsx 挂载 PerformanceHistoryPanel（D-07 / D-09）+ 强制新建 refreshPerformanceHistory helper</name>
  <files>
    frontend/src/pages/EvaluationDetail.tsx
  </files>
  <read_first>
    - frontend/src/pages/EvaluationDetail.tsx lines 1-50（现有 import）
    - frontend/src/pages/EvaluationDetail.tsx lines 500-600（state + salary history pattern + **refreshSalaryHistory lines 545-565**）
    - frontend/src/pages/EvaluationDetail.tsx lines 2130-2165（SalaryDetailPanel JSX 挂载点 + line 2146 divider 约定）
    - frontend/src/components/performance/PerformanceHistoryPanel.tsx
    - frontend/src/services/performanceService.ts（Plan 03 Task 2）
    - .planning/phases/36-historical-performance-display/36-CONTEXT.md § D-07 + § D-09
    - .planning/codebase/CONVENTIONS.md § Component Patterns
  </read_first>
  <action>
    分 5 处精确修改：
    
    1. **Import 区（line 16-19 附近）** — 在 SalaryHistoryPanel import 之后追加：
       ```typescript
       import { PerformanceHistoryPanel } from '../components/performance/PerformanceHistoryPanel';
       ```
       在 line 39 附近 `import { fetchSalaryHistoryByEmployee, ... } from '../services/salaryService';` 之后追加：
       ```typescript
       import { fetchPerformanceHistoryByEmployee } from '../services/performanceService';
       ```
       在 type import 区（line 41-50 附近）追加 `PerformanceRecordItem`：
       ```typescript
       import type {
         ...  // 现有
         PerformanceRecordItem,
       } from '../types/api';
       ```
    
    2. **State 声明（line 505-540 附近 salaryHistory 旁边）** — 追加两个 state：
       ```typescript
       const [performanceHistory, setPerformanceHistory] = useState<PerformanceRecordItem[]>([]);
       const [isPerformanceHistoryLoading, setIsPerformanceHistoryLoading] = useState(false);
       ```
    
    3. **角色门控派生（line 541-543 附近）** — 在 `canViewSalaryHistory` 声明后追加：
       ```typescript
       const canViewPerformanceHistory = user?.role === 'admin' || user?.role === 'hrbp' || user?.role === 'manager';
       ```
    
    4. **【强制，revision iteration 1 锁定】新建 `refreshPerformanceHistory` helper**（在 `refreshSalaryHistory`（lines 545-565）之后插入新 helper）：
       ```typescript
       async function refreshPerformanceHistory(targetEmployeeId: string) {
         if (!canViewPerformanceHistory) {
           setPerformanceHistory([]);
           setIsPerformanceHistoryLoading(false);
           return;
         }
         setIsPerformanceHistoryLoading(true);
         try {
           const historyResponse = await fetchPerformanceHistoryByEmployee(targetEmployeeId);
           setPerformanceHistory(historyResponse.items);
         } catch (error) {
           if (
             axios.isAxiosError(error) &&
             (error.response?.status === 403 || error.response?.status === 404)
           ) {
             // 跨部门 manager / 员工不存在 → 静默置空，不阻塞页面
             setPerformanceHistory([]);
             return;
           }
           throw error;
         } finally {
           setIsPerformanceHistoryLoading(false);
         }
       }
       ```
       **签名要求**（revision iteration 1 硬约束，不可偏离）：
       - 函数名：`refreshPerformanceHistory`（小驼峰，与 `refreshSalaryHistory` 命名风格一致）
       - 参数：`targetEmployeeId: string`（与 `refreshSalaryHistory` 对齐；**不**新增 cancelled flag，因为现有 EvaluationDetail pattern 不用该 flag）
       - 返回：`Promise<void>`
       - loading 管理：`setIsPerformanceHistoryLoading(true)` 入口 → `finally` 块 `setIsPerformanceHistoryLoading(false)`
       - 错误处理：403 + 404 → `setPerformanceHistory([])` + `return`；其它错误 `throw error` 给上游
       - **禁止**：在外部 useEffect 用 try/finally 包裹本函数（该 helper 自带完整 try/catch/finally）
    
    5. **Promise.all 并行拉取改造（line 567-600 的 refreshSubmissionData 内）** — 在现有 `Promise.all([...])` 数组里追加第 5 个 entry（与 salaryHistoryResult 并列），注意沿用同款 `{ ok: true, response }` envelope pattern：
       ```typescript
       employeeId && canViewPerformanceHistory
         ? fetchPerformanceHistoryByEmployee(employeeId)
             .then((response) => ({ ok: true as const, response }))
             .catch((error: unknown) => ({ ok: false as const, error }))
         : Promise.resolve({ ok: true as const, response: null }),
       ```
       解构新变量 `performanceHistoryResult`。
       在 `setSalaryHistory(...)` 之后追加对应的 set 分支：
       ```typescript
       if (performanceHistoryResult.ok) {
         setPerformanceHistory(performanceHistoryResult.response?.items ?? []);
       } else if (axios.isAxiosError(performanceHistoryResult.error) &&
                  (performanceHistoryResult.error.response?.status === 403 ||
                   performanceHistoryResult.error.response?.status === 404)) {
         // 跨部门 manager / 员工不存在 → 静默置空，不阻塞页面
         setPerformanceHistory([]);
       } else {
         setPerformanceHistory([]);
       }
       ```
       **loading 管理策略（revision iteration 1 明确）**：`refreshSubmissionData` 本身不管 isPerformanceHistoryLoading（它走 Promise.all 并行，没 per-field loading 状态）；后续如果 UI 需要独立刷新 performance history（非首屏场景，比如 approve 后重拉），调用 `refreshPerformanceHistory(employeeId)` — 该 helper 管自己的 loading 状态。首屏场景下 isPerformanceHistoryLoading 默认 false（state 初值），并行拉取中用 `isLoading={false}` 与 records 为空的组合渲染「空态」直到 Promise.all 完成；这与现有 salaryHistory 首屏表现一致。
    
    6. **JSX 挂载（line 2144-2162 SalaryDetailPanel 块之后）** — 在 `</section>` 前（line 2163 附近）追加挂载点：
       ```tsx
       {canViewPerformanceHistory ? (
         <>
           <hr className="divider" style={{ margin: '20px 0' }} />
           <PerformanceHistoryPanel
             employeeName={employee?.name}
             records={performanceHistory}
             isLoading={isPerformanceHistoryLoading}
           />
         </>
       ) : null}
       ```
       **divider 约定（revision iteration 1 grep 确认）**：`<hr className="divider" style={{ margin: '20px 0' }} />` — 这是 EvaluationDetail.tsx line 2146 现有全文件唯一的 divider 写法，本 plan 严格复用。`.divider` 类在 `frontend/src/index.css` line 578 定义。
       **挂载位置**：挂在 SalaryDetailPanel 外层 `</section>` 之前，与 SalaryDetailPanel 同级；条件渲染确保 employee 角色即使路由到此页面也不看到 panel。
    
    约束：
    - 严守 D-10：只改 EvaluationDetail.tsx，**不**改 MyReview.tsx / Approvals.tsx / 任何其它页面
    - 新增代码紧贴现有 salary history pattern，易审查
    - 错误分支覆盖 403（跨部门 manager）+ 404（员工不存在）+ 其它错误（网络）三类都置空
    - 不引入新依赖（纯 state + 现有 service）
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust/frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "import { PerformanceHistoryPanel }" frontend/src/pages/EvaluationDetail.tsx` 返回 0
    - `grep -q "fetchPerformanceHistoryByEmployee" frontend/src/pages/EvaluationDetail.tsx` 返回 0
    - `grep -q "canViewPerformanceHistory" frontend/src/pages/EvaluationDetail.tsx` 返回 0
    - `grep -q "setPerformanceHistory" frontend/src/pages/EvaluationDetail.tsx` 返回 0
    - `grep -q "isPerformanceHistoryLoading" frontend/src/pages/EvaluationDetail.tsx` 返回 0
    - `grep -q "<PerformanceHistoryPanel" frontend/src/pages/EvaluationDetail.tsx` 返回 0
    - **revision iteration 1 强制 helper 断言**：`grep -qE "function refreshPerformanceHistory\|async function refreshPerformanceHistory" frontend/src/pages/EvaluationDetail.tsx` 返回 0
    - `grep -qE "response\?\.status === 403.*response\?\.status === 404|403.*404|status === 403.*status === 404" frontend/src/pages/EvaluationDetail.tsx` 返回 0（403/404 catch 存在）
    - **divider 约定一致性**：`grep -cE 'className="divider"' frontend/src/pages/EvaluationDetail.tsx` 返回 ≥ 2（原 1 处 + 新增 1 处，均用 `divider` 类）
    - `cd frontend && npx tsc --noEmit` 退出码 0
    - `cd frontend && npm run lint` 等价命令（tsc --noEmit）退出码 0
  </acceptance_criteria>
  <done>
    EvaluationDetail 挂载完成、refreshPerformanceHistory helper 新建并与 refreshSalaryHistory 签名对齐、tsc 通过、403/404 fallback 存在、角色门控生效、divider 约定复用。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 四角色 UAT 验证 + D-10 never-do 确认 + DOM 共存断言 + 36-VERIFICATION.md 记录</name>
  <files>
    .planning/phases/36-historical-performance-display/36-VERIFICATION.md
  </files>
  <read_first>
    - .planning/phases/36-historical-performance-display/36-CONTEXT.md § D-07 + § D-09 + § D-10 + § Specifics
    - .planning/ROADMAP.md lines 207-217（4 SC）
    - frontend/src/pages/MyReview.tsx（D-10 显式 never-do 验证目标）
    - frontend/src/pages/Approvals.tsx（D-07 另一个 never-do 目标）
  </read_first>
  <what-built>
    Plan 01-04 的全部交付物：
    - Alembic migration + PerformanceRecord.comment 字段
    - POST /records 支持 comment + Excel 导入支持 3 种表头别名（merge 锁定「有列即写、无列不改」）
    - GET /records/by-employee/{employee_id} 新端点（admin/hrbp/manager）
    - PerformanceHistoryPanel.tsx 4 列表
    - EvaluationDetail.tsx 挂载 + refreshPerformanceHistory helper + 角色门控 + 403/404 fallback
  </what-built>
  <how-to-verify>
    **启动环境：**
    ```
    后端：.venv/bin/uvicorn backend.app.main:app --reload --port 8011
    前端：cd frontend && npm run dev
    ```
    
    **准备测试数据（若无则在「绩效管理」页手工录入）：**
    - 员工 empA（Dept-A）：3 条绩效（2026/A/评语A、2025/B/评语B、2024/C/无评语）
    - 员工 empB（Dept-B）：1 条绩效（2026/A/评语C）
    - 员工 empC（Dept-A）：0 条绩效
    - 4 个用户：admin / hrbp（无 department） / managerA（Dept-A） / managerB（Dept-B） / empX（绑定 empA）
    
    **UAT 矩阵（8 场景）— 每个场景在 36-VERIFICATION.md 记录 状态码 + 截图 + 实际结果：**
    
    | # | 角色 | 进入路径 | 预期结果 |
    |---|------|---------|---------|
    | 1 | admin | /evaluations/{empA 的 evaluation id} | PerformanceHistoryPanel 在 SalaryDetailPanel 之后显示，3 行 year DESC，列头「周期/绩效等级/评语/部门快照」 |
    | 2 | hrbp | 同上 | 同 1 |
    | 3 | managerA | 同上（empA 同部门） | 同 1 |
    | 4 | managerB | /evaluations/{empA 的 evaluation id}（跨部门） | 要么整页 403（页面层级拦截），要么 panel 显示「暂无历史绩效记录」空态（403 被 fallback 为空）— 不崩溃，不看到他部门 comment |
    | 5 | empX | /evaluations/{empA 的 evaluation id}（empX 绑定 empA） | EvaluationDetail 本身是否允许 employee 进入保留现状；若进入，PerformanceHistoryPanel 不渲染（canViewPerformanceHistory === false） |
    | 6 | admin | /evaluations/{empC 的 evaluation id}（无绩效） | 空态「暂无历史绩效记录」+ 补充「该员工尚未录入任何年度绩效」 |
    | 7 | admin | 任意 evaluation 页面加载时 | 浏览器 Network 面板看到一次 `/performance/records/by-employee/<uuid>` 请求，200 响应 |
    | 8 | empX | /my-review | **不** 看到任何 PerformanceHistoryPanel；只看到 Phase 35 绩效档次徽章（若已交付） |
    
    **revision iteration 1 新增：UAT 场景 1 追加 DOM 共存断言**（映射 SC#2「两页共用」即「两 section 同页」）：
    ```js
    // 在 admin 登录后打开 /evaluations/{empA_eval}，DevTools Console 执行：
    document.querySelectorAll('[data-testid="salary-detail-panel"], [data-testid="performance-history-panel"]').length
    // 预期：=== 2
    ```
    如果组件未加 `data-testid`，改用等价 grep 验证（在 36-VERIFICATION.md 证据块中提供其一）：
    ```bash
    grep -c "<SalaryDetailPanel\|<PerformanceHistoryPanel" frontend/src/pages/EvaluationDetail.tsx
    # 预期：≥ 2（SalaryDetailPanel 1 处 + PerformanceHistoryPanel 1 处，证明两组件同文件共存）
    ```
    **至少选一种 DOM 共存证据**（优先 querySelectorAll；若无 testid 则用 grep 证明同文件 import + 渲染）写入 36-VERIFICATION.md SC#2 条目。
    
    **D-10 never-do 代码级验证（grep 命令）：**
    ```
    cd /Users/mac/PycharmProjects/Wage_adjust
    grep -c "PerformanceHistoryPanel" frontend/src/pages/MyReview.tsx
    # 预期输出：0
    
    grep -c "PerformanceHistoryPanel" frontend/src/pages/Approvals.tsx
    # 预期输出：0
    
    grep -c "/performance/me/history" backend/app/api/v1/performance.py
    # 预期输出：0
    
    grep -c "PerformanceHistoryPanel" frontend/src/pages/EvaluationDetail.tsx
    # 预期输出：≥ 2（import + JSX）
    ```
    
    **4 个 Success Criteria 映射：**
    - SC#1 → UAT 场景 1/2/3（HR/manager 看到 4 列表）
    - **SC#2** → 同页共用（SalaryDetailPanel 与 PerformanceHistoryPanel 在同一 EvaluationDetail 页面）+ **DOM 共存断言**（querySelectorAll length === 2，或等价 grep 证明两组件 JSX 标签同文件出现）— revision iteration 1 强化
    - SC#3 → 场景 6（空态不崩溃）
    - SC#4 → 场景 1 的 3 行数据中，可观察同一员工不同年的 department_snapshot 差异（若无历史部门变动数据，手工在 DB 里把一条 PerformanceRecord.department_snapshot 改成旧部门名验证 UI 显示该旧值而非当前部门）
    
    **创建 `36-VERIFICATION.md` 模板：**
    ```markdown
    # Phase 36 VERIFICATION
    
    **Executed:** YYYY-MM-DD HH:MM
    **Executor:** [name]
    **Env:** backend uvicorn :8011 + frontend vite :5174
    
    ## UAT Matrix
    
    | # | Role | Path | Expected | Actual | Pass |
    |---|------|------|----------|--------|------|
    | 1 | admin | /evaluations/{empA_eval} | 3 行 year DESC + 4 列 + DOM 共存 querySelectorAll length === 2 | ... | ✓/✗ |
    | ... (8 scenarios) |
    
    ## D-10 Never-Do Verification
    
    - [ ] `grep PerformanceHistoryPanel MyReview.tsx` = 0
    - [ ] `grep PerformanceHistoryPanel Approvals.tsx` = 0
    - [ ] `grep /performance/me/history performance.py` = 0
    - [ ] `grep PerformanceHistoryPanel EvaluationDetail.tsx` >= 2
    
    ## SC Coverage
    
    - [ ] SC#1 HR/manager 底部「历史绩效」4 列
    - [ ] SC#2 同页共用（EvaluationDetail 同时有 Salary + Performance panels）+ DOM 共存断言
    - [ ] SC#3 空态不崩溃
    - [ ] SC#4 department_snapshot 反映录入时部门（跨部门数据验证通过）
    
    ## DOM Coexistence Evidence (SC#2)
    
    - Method: [querySelectorAll / grep]
    - Command: [command used]
    - Output: [actual output]
    - Assertion: `=== 2` or `>= 2` passed
    
    ## Screenshots
    
    [embed or link]
    
    ## Findings / Gaps
    
    - None / [list]
    
    ## Sign-off
    
    - Tester: [name]
    - Date: [date]
    ```
  </how-to-verify>
  <action>
    1. 启动后端 + 前端；录入 Task 2 定义的测试数据（empA 3 条、empB 1 条、empC 0 条 + 4 用户）
    2. 依次登录 admin / hrbp / managerA / managerB / empX 五个账号，按 UAT 矩阵 8 场景逐项验证浏览器页面表现
    3. 在 admin 场景 1 执行 **DOM 共存断言**（querySelectorAll 或等价 grep），记录证据到 36-VERIFICATION.md "DOM Coexistence Evidence (SC#2)" 条目
    4. 运行 D-10 never-do grep 命令集（见 how-to-verify 模板），记录输出
    5. 逐一对照 ROADMAP SC#1~SC#4，在 36-VERIFICATION.md 勾选
    6. 为每个 UAT 场景截屏，贴入 36-VERIFICATION.md
    7. 把 36-VERIFICATION.md commit 到 git（connect git log 可追溯 UAT 执行时间）
    8. 若任何场景 Fail，描述 gap 给 orchestrator，触发 `/gsd-plan-phase 36 --gaps` 补 Plan 05
  </action>
  <verify>
    <automated>grep -c "PerformanceHistoryPanel" frontend/src/pages/MyReview.tsx 2>&1; grep -c "PerformanceHistoryPanel" frontend/src/pages/Approvals.tsx 2>&1; grep -c "/performance/me/history" backend/app/api/v1/performance.py 2>&1; grep -c "PerformanceHistoryPanel" frontend/src/pages/EvaluationDetail.tsx 2>&1; grep -cE "<SalaryDetailPanel|<PerformanceHistoryPanel" frontend/src/pages/EvaluationDetail.tsx 2>&1; ls .planning/phases/36-historical-performance-display/36-VERIFICATION.md 2>&1</automated>
  </verify>
  <resume-signal>
用户在完成上述 8 条 UAT + DOM 共存断言 + D-10 grep 验证后回复：
- `approved` —— 全部通过，plan 完成
- `failed: <描述>` —— 具体哪条失败（贴截图或 DOM 片段）

（revision iteration 1：对齐 Phase 34/35 checkpoint 约定 — 经 grep `.planning/phases/35-*/35-04-*.md` 确认，`approved` / `failed: <描述>` 为既有风格。）
  </resume-signal>
  <done>
    36-VERIFICATION.md 文件创建并提交 git，UAT 矩阵 8 行全 Pass，DOM 共存断言通过，D-10 grep 全零/≥2，4 SC 全覆盖。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EvaluationDetail 渲染循环 → PerformanceHistoryPanel props | records 数组由 state 控制，403/404 fallback 为空数组 |
| 员工端 MyReview.tsx → 本期严格不引入 | D-10 never-do 代码级 grep 验证 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-04-01 | Information Disclosure | employee 角色进入 EvaluationDetail 页面可能意外看到他人历史绩效 | mitigate | `canViewPerformanceHistory` 门控 + 条件渲染 `{canViewPerformanceHistory ? ... : null}`；即使 role 绕过，后端 Plan 02 require_roles('admin','hrbp','manager') 兜底 403 |
| T-36-04-02 | Information Disclosure | manager 跨部门 HTTP 403 时前端崩溃暴露错误信息 | mitigate | axios.isAxiosError(error) catch + status 403/404 → setPerformanceHistory([]) 静默置空；panel 渲染「暂无历史绩效记录」空态 |
| T-36-04-03 | Information Disclosure | MyReview 误引入 PerformanceHistoryPanel（D-10 违反） | mitigate | Task 2 grep 验证：`grep PerformanceHistoryPanel MyReview.tsx` = 0；CI-time 可加 lint 规则（本期靠 checkpoint 验证） |
| T-36-04-04 | Denial of Service | useEffect 无限 fetch 循环 | mitigate | 复刻现有 refreshSalaryHistory pattern（refreshPerformanceHistory helper 同款，无 cancelled flag 但依赖 React 卸载 state setter noop 行为，与现有 pattern 一致） |
| T-36-04-05 | Spoofing | 前端展示的 employeeName 与 comment 可能与 URL 中 evaluationId 不匹配 | accept | 后端 AccessScopeService 已做一致性检查（evaluation → submission → employee）；panel 接收 `employee?.name` prop，与 EvaluationDetail 现有 employee 来源一致 |
</threat_model>

<verification>
1. tsc --noEmit 通过
2. npm run dev 启动前端无 console error
3. uvicorn backend 启动无 import error
4. 手动 4 角色 UAT（admin/hrbp/manager-same/manager-cross + employee + 空态）
5. D-10 grep 验证 4 项全部满足
6. SC#1~SC#4 四项逐一验证（含 SC#2 的 DOM 共存断言）
7. 36-VERIFICATION.md 文件 commit 到 git
</verification>

<success_criteria>
- [ ] EvaluationDetail.tsx 含 PerformanceHistoryPanel import + state + refreshPerformanceHistory helper + useEffect fetch + JSX 挂载
- [ ] `refreshPerformanceHistory` helper 存在且签名与 refreshSalaryHistory 对齐（revision iteration 1 强制）
- [ ] canViewPerformanceHistory 角色门控生效
- [ ] 403/404 catch 置空数组不崩溃
- [ ] tsc --noEmit 通过
- [ ] 36-VERIFICATION.md 记录 8 场景 UAT 全部 Pass + DOM 共存断言通过 + D-10 grep 全零 + 4 SC 全覆盖
- [ ] MyReview.tsx 严格不含 PerformanceHistoryPanel（D-10 守住）
- [ ] 手工 UAT 8 场景全部通过
</success_criteria>

<output>
完成后创建 `.planning/phases/36-historical-performance-display/36-04-SUMMARY.md`，列出：
- EvaluationDetail.tsx 5 处修改 diff 摘要（含 refreshPerformanceHistory helper 新建）
- UAT 8 场景执行结果
- DOM 共存断言证据（querySelectorAll length 或 grep 数字）
- D-10 grep 结果
- 4 SC 覆盖证据
- Phase 36 总结：4 plans × 10 decisions × 1 requirement × 4 waves 全部交付
</output>
</output>
