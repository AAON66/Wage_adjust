---
phase: 11-menu-navigation-restructuring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/utils/roleAccess.ts
  - frontend/src/components/layout/AppShell.tsx
  - frontend/src/App.tsx
autonomous: false
requirements:
  - NAV-01
  - NAV-02
  - NAV-03

must_haves:
  truths:
    - "侧边栏菜单项按4组展示：运营管理、数据分析、系统管理、个人（底部）"
    - "每个菜单项前显示对应图标"
    - "分组标题可点击折叠/展开，折叠时显示子项数量"
    - "刷新页面后折叠状态保持不变（localStorage 持久化）"
    - "admin 看到全部分组和菜单项，employee 仅看到个人评估中心和账号设置"
    - "WorkspacePage 模块卡片仍正常渲染（兼容新的分组数据结构）"
  artifacts:
    - path: "frontend/src/utils/roleAccess.ts"
      provides: "MenuGroup 接口、icon 字段、分组 ROLE_MODULES、getRoleModules 返回 MenuGroup[]"
      contains: "interface MenuGroup"
    - path: "frontend/src/components/layout/AppShell.tsx"
      provides: "分组折叠侧边栏渲染、localStorage 持久化"
      contains: "localStorage"
    - path: "frontend/src/App.tsx"
      provides: "WorkspacePage 兼容新 MenuGroup[] 结构"
      contains: "modules"
  key_links:
    - from: "frontend/src/components/layout/AppShell.tsx"
      to: "frontend/src/utils/roleAccess.ts"
      via: "getRoleModules 返回 MenuGroup[]"
      pattern: "getRoleModules"
    - from: "frontend/src/components/layout/AppShell.tsx"
      to: "localStorage"
      via: "nav_collapsed_{groupId} 键存取折叠状态"
      pattern: "nav_collapsed_"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/utils/roleAccess.ts"
      via: "WorkspacePage 使用 getRoleModules 并 flatMap items"
      pattern: "getRoleModules"
---

<objective>
将侧边栏菜单从扁平列表重构为按功能分组的折叠结构，每个菜单项带图标，折叠状态 localStorage 持久化，各角色按权限过滤分组和菜单项。

Purpose: 菜单项从 12+ 项扁平列表变为 3-4 个语义分组，提升导航效率和视觉层次。
Output: 重构后的 roleAccess.ts 数据结构 + AppShell.tsx 分组渲染 + WorkspacePage 兼容更新。
</objective>

<execution_context>
@.planning/phases/11-menu-navigation-restructuring/11-CONTEXT.md
</execution_context>

<context>
@.planning/ROADMAP.md
@frontend/src/utils/roleAccess.ts
@frontend/src/components/layout/AppShell.tsx
@frontend/src/App.tsx

<interfaces>
<!-- 当前接口 — Task 1 将修改这些定义 -->

From frontend/src/utils/roleAccess.ts:
```typescript
export interface WorkspaceModuleLink {
  title: string;
  description: string;
  href: string;
}

export function getRoleModules(role: string | null | undefined): WorkspaceModuleLink[];
```

From frontend/src/components/layout/AppShell.tsx:
```typescript
// ShellSidebar 内部组件，调用 getRoleModules 获取扁平模块列表后 map 渲染 NavLink
function ShellSidebar(): JSX.Element;
```

From frontend/src/App.tsx:
```typescript
// WorkspacePage 调用 getRoleModules 获取扁平列表，用于统计 modules.length 和 map 渲染卡片
function WorkspacePage(): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重构 roleAccess.ts 数据结构为分组菜单</name>
  <files>frontend/src/utils/roleAccess.ts</files>
  <read_first>frontend/src/utils/roleAccess.ts</read_first>
  <action>
**1. 扩展 WorkspaceModuleLink 接口** (per D-07):

在 `WorkspaceModuleLink` 接口中添加 `icon` 字段:
```typescript
export interface WorkspaceModuleLink {
  title: string;
  description: string;
  href: string;
  icon: string;  // emoji 图标
}
```

**2. 新增 MenuGroup 接口** (per D-05):

```typescript
export interface MenuGroup {
  id: string;           // 'operations' | 'analytics' | 'system'
  label: string;        // '运营管理' | '数据分析' | '系统管理'
  collapsible: boolean; // 个人组为 false
  items: WorkspaceModuleLink[];
}
```

**3. 为所有现有模块常量添加 icon 字段** (per D-07):

按 CONTEXT.md D-07 表格分配:
- SETTINGS_MODULE: icon '⚙️'
- EMPLOYEE_ARCHIVE_MODULE: icon '📂'
- IMPORT_CENTER_MODULE: icon '📥'

**4. 重构 ROLE_MODULES 为 `Record<string, MenuGroup[]>`** (per D-01, D-04, D-06):

将现有扁平数组按分组重组。以 admin 为例:

```typescript
const ROLE_MODULES: Record<string, MenuGroup[]> = {
  admin: [
    {
      id: 'operations',
      label: '运营管理',
      collapsible: true,
      items: [
        { title: '员工评估', description: '查看员工与评估流转。', href: '/employees', icon: '📋' },
        { title: '创建周期', description: '新建评估周期与预算。', href: '/cycles/create', icon: '🔄' },
        { title: '调薪模拟', description: '查看预算占用与建议方案。', href: '/salary-simulator', icon: '💰' },
        { title: '审批中心', description: '处理待审批与历史记录。', href: '/approvals', icon: '✅' },
        { title: '考勤管理', description: '查看考勤数据与飞书同步。', href: '/attendance', icon: '📅' },
      ],
    },
    {
      id: 'analytics',
      label: '数据分析',
      collapsible: true,
      items: [
        { title: '组织看板', description: '查看分布、热力和 ROI。', href: '/dashboard', icon: '📊' },
        { title: '审计日志', description: '查看所有操作与变更记录。', href: '/audit-log', icon: '📜' },
      ],
    },
    {
      id: 'system',
      label: '系统管理',
      collapsible: true,
      items: [
        { title: '平台账号', description: '管理账号与权限范围。', href: '/user-admin', icon: '👥' },
        EMPLOYEE_ARCHIVE_MODULE,  // 已含 icon
        IMPORT_CENTER_MODULE,     // 已含 icon
        { title: '飞书配置', description: '配置飞书应用凭证与同步。', href: '/feishu-config', icon: '🔗' },
        { title: 'API Key 管理', description: '创建、轮换和撤销外部 API 访问密钥。', href: '/api-key-management', icon: '🔑' },
        { title: 'Webhook 管理', description: '注册回调 URL，查看通知投递日志。', href: '/webhook-management', icon: '🔔' },
      ],
    },
  ],
  // hrbp、manager、employee 按 D-04 表格和现有 ROLE_MODULES 对应关系定义
  // hrbp: 运营管理(员工评估、调薪模拟、审批中心、考勤管理) + 数据分析(仅组织看板) + 系统管理(平台账号、员工档案、导入中心)
  // manager: 运营管理(员工评估、审批中心) + 数据分析(仅组织看板) + 系统管理(平台账号、员工档案、导入中心)
  // employee: 无可折叠分组，只有个人评估中心和账号设置
};
```

对于 employee 角色 (per D-02 "个人组不折叠"):
```typescript
employee: [
  {
    id: 'personal',
    label: '个人',
    collapsible: false,
    items: [
      { title: '个人评估中心', description: '查看材料与评估进展。', href: '/my-review', icon: '📝' },
    ],
  },
],
```

注意: 「账号设置」不放在任何分组中 (per D-02 "直接放底部")。所有角色的分组列表都不包含 SETTINGS_MODULE。它将在 ShellSidebar 中单独渲染在底部。

**5. 更新 getRoleModules 返回类型**:

```typescript
export function getRoleModules(role: string | null | undefined): MenuGroup[] {
  if (!role) return [];
  return ROLE_MODULES[role] ?? [];
}
```

**6. 新增 helper 函数** (供 WorkspacePage 和其他消费者使用):

```typescript
/** 将 MenuGroup[] 展平为 WorkspaceModuleLink[]，用于向后兼容 */
export function flattenMenuGroups(groups: MenuGroup[]): WorkspaceModuleLink[] {
  return groups.flatMap(g => g.items);
}

/** 获取账号设置模块（所有角色共享，固定在侧边栏底部） */
export function getSettingsModule(): WorkspaceModuleLink {
  return SETTINGS_MODULE;
}
```

**7. 保持 getRoleLabel、getRoleHomePath、isAllowedRole 不变。**

注意: 飞书配置归入系统管理组 (per D-06)。admin 的飞书配置 href 使用 `/feishu-config` (与 App.tsx 路由一致)。
  </action>
  <verify>
cd frontend && npx tsc --noEmit
  </verify>
  <acceptance_criteria>
- MenuGroup 接口已导出
- WorkspaceModuleLink 包含 icon 字段
- ROLE_MODULES 为 Record&lt;string, MenuGroup[]&gt; 结构
- getRoleModules 返回 MenuGroup[]
- flattenMenuGroups helper 已导出
- getSettingsModule helper 已导出
- 4 个角色的分组定义与 D-01/D-04 表格一致
- 飞书配置在 admin 的系统管理组中 (per D-06)
- TypeScript 编译通过 (tsc --noEmit)
  </acceptance_criteria>
  <done>roleAccess.ts 导出 MenuGroup 接口和分组数据，getRoleModules 返回 MenuGroup[]，tsc 编译通过</done>
</task>

<task type="auto">
  <name>Task 2: 实现分组折叠侧边栏 + WorkspacePage 兼容</name>
  <files>frontend/src/components/layout/AppShell.tsx, frontend/src/App.tsx</files>
  <read_first>frontend/src/components/layout/AppShell.tsx, frontend/src/App.tsx</read_first>
  <action>
**Part A: ShellSidebar 分组折叠渲染 (AppShell.tsx)**

**1. 导入新类型和函数:**
```typescript
import { getRoleHomePath, getRoleLabel, getRoleModules, getSettingsModule } from '../../utils/roleAccess';
import type { MenuGroup } from '../../utils/roleAccess';
```

**2. 折叠状态管理 (per D-03):**

在 ShellSidebar 内部:
```typescript
const groups = getRoleModules(user?.role);
const settingsModule = getSettingsModule();

// 从 localStorage 读取初始折叠状态，默认全部展开
const [collapsed, setCollapsed] = useState<Record<string, boolean>>(() => {
  const saved: Record<string, boolean> = {};
  groups.forEach(g => {
    if (g.collapsible) {
      const key = `nav_collapsed_${g.id}`;
      const val = localStorage.getItem(key);
      saved[g.id] = val === 'true';
    }
  });
  return saved;
});

function toggleGroup(groupId: string) {
  setCollapsed(prev => {
    const next = { ...prev, [groupId]: !prev[groupId] };
    localStorage.setItem(`nav_collapsed_${groupId}`, String(next[groupId]));
    return next;
  });
}
```

**3. 替换现有 `<nav>` 内容:**

移除旧的静态 "导航" 标题和扁平 modules.map。替换为:

```tsx
<nav style={{ flex: 1, padding: '6px 0 8px', overflowY: 'auto' }}>
  {/* 角色首页链接 — 始终显示在顶部 */}
  <NavLink className={({ isActive }) => `nav-link${isActive ? ' nav-link-active' : ''}`} to={homePath}>
    🏠 角色首页
  </NavLink>

  {/* 分组菜单 */}
  {groups.map(group => {
    if (!group.collapsible) {
      // 不可折叠的组（如 employee 的个人组）直接渲染 items
      return group.items.map(item => (
        <NavLink
          className={({ isActive }) => `nav-link${isActive ? ' nav-link-active' : ''}`}
          key={item.href}
          title={item.description}
          to={item.href}
        >
          {item.icon} {item.title}
        </NavLink>
      ));
    }

    const isCollapsed = collapsed[group.id] ?? false;

    return (
      <div key={group.id}>
        {/* 分组标题 — 可点击折叠/展开 (per D-02) */}
        <button
          onClick={() => toggleGroup(group.id)}
          type="button"
          style={{
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'space-between',
            width: '100%',
            padding: '8px 16px 4px',
            fontSize: 11,
            fontWeight: 600,
            color: 'var(--color-placeholder)',
            letterSpacing: '0.08em',
            textTransform: 'uppercase',
            background: 'none',
            border: 'none',
            cursor: 'pointer',
          }}
        >
          <span>{isCollapsed ? '▶' : '▼'} {group.label}</span>
          {isCollapsed && (
            <span style={{ fontSize: 10, fontWeight: 500 }}>({group.items.length})</span>
          )}
        </button>
        {/* 子菜单项 — 折叠时隐藏 */}
        {!isCollapsed && group.items.map(item => (
          <NavLink
            className={({ isActive }) => `nav-link${isActive ? ' nav-link-active' : ''}`}
            key={item.href}
            title={item.description}
            to={item.href}
          >
            {item.icon} {item.title}
          </NavLink>
        ))}
      </div>
    );
  })}

  {/* 账号设置 — 始终在导航底部独立显示 (per D-02) */}
  <NavLink
    className={({ isActive }) => `nav-link${isActive ? ' nav-link-active' : ''}`}
    to={settingsModule.href}
    title={settingsModule.description}
  >
    {settingsModule.icon} {settingsModule.title}
  </NavLink>
</nav>
```

**4. 添加 useState 到 import:**
确保从 react 导入 `useState`:
```typescript
import { useState, type ReactNode } from 'react';
```

**Part B: WorkspacePage 兼容更新 (App.tsx)**

WorkspacePage 当前使用 `getRoleModules` 返回扁平数组。需要改为使用 `flattenMenuGroups`:

**1. 更新导入:**
```typescript
import { getRoleHomePath, getRoleLabel, getRoleModules, flattenMenuGroups } from "./utils/roleAccess";
```

**2. 在 WorkspacePage 中:**
将:
```typescript
const modules = getRoleModules(user?.role);
```
改为:
```typescript
const menuGroups = getRoleModules(user?.role);
const modules = flattenMenuGroups(menuGroups);
```

这样 `modules.length` 和 `modules.map(...)` 的用法无需改动，保持完全兼容。

**注意事项:**
- 不修改任何路由定义 (per scope "URL 路由路径不变")
- 不修改 AppShell 组件签名或 main content 区域
- 不修改 HomePage 中的 getRoleModules 调用 — HomePage 也使用 `getRoleModules`，但它在 `WorkspacePage` 外独立使用。检查 HomePage: 它调用的是 `getRoleHomePath` 和 `getRoleLabel`，不调用 `getRoleModules`，因此无需改动。
- 空分组处理: 如果某角色的某个分组 items 为空数组（当前设计中不会出现，但防御性处理），渲染时 `group.items.length === 0` 的分组跳过不显示。在 groups.map 中添加: `if (group.items.length === 0) return null;`
  </action>
  <verify>
cd frontend && npx tsc --noEmit && npm run dev -- --host 127.0.0.1 --port 5174 &amp; sleep 3 && curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:5174/ && kill %1 2>/dev/null; true
  </verify>
  <acceptance_criteria>
- ShellSidebar 按分组渲染菜单项，每项前有图标
- 分组标题可点击折叠/展开
- 折叠时标题旁显示子项数量（如 "▶ 系统管理 (6)"）
- 展开时显示 "▼" 箭头
- 折叠状态通过 localStorage 持久化 (key: nav_collapsed_{groupId})
- 刷新页面后折叠状态恢复
- 账号设置独立显示在导航底部
- WorkspacePage 模块卡片正常渲染，数量统计正确
- TypeScript 编译通过
- 前端开发服务器可启动
  </acceptance_criteria>
  <done>侧边栏按分组折叠展示菜单，图标显示正确，折叠状态 localStorage 持久化，WorkspacePage 兼容正常</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>侧边栏菜单从扁平列表重构为分组折叠结构，包含图标、折叠/展开、localStorage 持久化、角色权限过滤</what-built>
  <how-to-verify>
1. 启动前端: `cd frontend && npm run dev`
2. 用 admin 账号登录，进入工作区
3. 验证侧边栏显示 3 个分组（运营管理、数据分析、系统管理），每个菜单项前有图标
4. 点击「运营管理」标题 — 该组折叠，标题旁显示 "(5)"
5. 刷新页面 — 「运营管理」仍处于折叠状态
6. 点击展开 — 子菜单重新显示
7. 验证「账号设置」在侧边栏底部独立显示，不在任何分组内
8. 用 employee 账号登录 — 验证仅看到「个人评估中心」和「账号设置」
9. 进入 /workspace 页面 — 验证模块卡片正常显示，数量正确
  </how-to-verify>
  <resume-signal>输入 "approved" 或描述需要修改的问题</resume-signal>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` — TypeScript 编译无错误
- 侧边栏按 4 组（运营管理、数据分析、系统管理、个人）展示菜单
- 折叠/展开交互正常，localStorage 持久化有效
- admin 看到全部分组，employee 仅看到个人评估中心 + 账号设置
- WorkspacePage 模块卡片正常渲染
</verification>

<success_criteria>
- NAV-01: 侧边栏菜单按功能类别分组展示，非扁平列表
- NAV-02: 分组可折叠/展开，刷新后保持状态
- NAV-03: 各角色按权限过滤分组和菜单项
- 所有现有路由不变，页面功能不受影响
</success_criteria>

<output>
After completion, create `.planning/phases/11-menu-navigation-restructuring/11-01-SUMMARY.md`
</output>
