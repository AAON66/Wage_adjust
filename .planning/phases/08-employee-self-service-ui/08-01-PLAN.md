---
phase: 08-employee-self-service-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/evaluation/EvaluationStepBar.tsx
  - frontend/src/components/evaluation/DimensionRadarChart.tsx
  - frontend/src/components/evaluation/DimensionCard.tsx
  - frontend/src/components/evaluation/SalaryResultCard.tsx
  - frontend/src/utils/dimensionConstants.ts
  - frontend/src/services/approvalService.ts
autonomous: true
requirements: [EMP-01, EMP-02, EMP-03]
must_haves:
  truths:
    - "EvaluationStepBar 接受 currentStep 0-3 并渲染对应的步骤高亮/完成/未到达状态"
    - "DimensionRadarChart 接受 scores 数组并渲染 ECharts 雷达图"
    - "DimensionCard 显示维度中文名、分数、权重和 LLM 说明"
    - "SalaryResultCard 将 0.12 格式化为 +12.00%"
    - "维度代码到中文名的映射集中在 dimensionConstants.ts"
    - "approvalService 导出 fetchApprovalHistory 函数"
  artifacts:
    - path: "frontend/src/components/evaluation/EvaluationStepBar.tsx"
      provides: "横向四步骤进度条组件"
      exports: ["EvaluationStepBar"]
    - path: "frontend/src/components/evaluation/DimensionRadarChart.tsx"
      provides: "ECharts 雷达图组件"
      exports: ["DimensionRadarChart"]
    - path: "frontend/src/components/evaluation/DimensionCard.tsx"
      provides: "单维度得分卡片"
      exports: ["DimensionCard"]
    - path: "frontend/src/components/evaluation/SalaryResultCard.tsx"
      provides: "调薪百分比展示卡片"
      exports: ["SalaryResultCard"]
    - path: "frontend/src/utils/dimensionConstants.ts"
      provides: "维度代码到中文名/权重的集中映射"
      exports: ["DIMENSION_LABELS", "DIMENSION_WEIGHTS"]
    - path: "frontend/src/services/approvalService.ts"
      provides: "审批历史查询函数"
      exports: ["fetchApprovalHistory"]
  key_links:
    - from: "DimensionRadarChart.tsx"
      to: "dimensionConstants.ts"
      via: "import DIMENSION_LABELS"
      pattern: "DIMENSION_LABELS"
    - from: "DimensionCard.tsx"
      to: "dimensionConstants.ts"
      via: "import DIMENSION_LABELS"
      pattern: "DIMENSION_LABELS"
---

<objective>
创建员工自助 UI 的全部子组件和工具常量。

Purpose: 为 Plan 02 的 MyReview 页面集成提供所有前端构件。独立组件先于页面集成创建，确保类型安全和关注点分离。
Output: 4 个新组件 + 1 个常量文件 + approvalService 新增函数
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-employee-self-service-ui/08-CONTEXT.md
@.planning/phases/08-employee-self-service-ui/08-RESEARCH.md
@.planning/phases/08-employee-self-service-ui/08-UI-SPEC.md

@frontend/src/types/api.ts
@frontend/src/components/dashboard/AILevelChart.tsx
@frontend/src/services/approvalService.ts
@frontend/src/utils/roleAccess.ts

<interfaces>
<!-- 执行者需要的关键类型（来自 api.ts） -->

```typescript
export interface DimensionScoreRecord {
  id: string;
  dimension_code: string;
  weight: number;
  ai_raw_score: number;
  ai_weighted_score: number;
  raw_score: number;
  weighted_score: number;
  ai_rationale: string;
  rationale: string;
  prompt_hash?: string | null;
  created_at: string;
}

export interface ApprovalRecord {
  id: string;
  recommendation_id: string;
  step_name: string;
  step_order: number;
  is_current_step: boolean;
  decision: 'pending' | 'approved' | 'rejected' | 'deferred';
  // ... 其他字段见 api.ts
}

export interface ApprovalListResponse {
  items: ApprovalRecord[];
  total: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>任务 1: 创建维度常量和 4 个 UI 组件</name>
  <files>
    frontend/src/utils/dimensionConstants.ts,
    frontend/src/components/evaluation/EvaluationStepBar.tsx,
    frontend/src/components/evaluation/DimensionRadarChart.tsx,
    frontend/src/components/evaluation/DimensionCard.tsx,
    frontend/src/components/evaluation/SalaryResultCard.tsx
  </files>
  <read_first>
    frontend/src/types/api.ts,
    frontend/src/components/dashboard/AILevelChart.tsx,
    .planning/phases/08-employee-self-service-ui/08-UI-SPEC.md
  </read_first>
  <action>
**1. 创建 `frontend/src/utils/dimensionConstants.ts`**（per D-03, 研究报告反模式建议）

```typescript
export const DIMENSION_LABELS: Record<string, string> = {
  TOOL_MASTERY: 'AI工具掌握度',
  APPLICATION_DEPTH: 'AI应用深度',
  LEARNING_ABILITY: 'AI学习能力',
  SHARING_CONTRIBUTION: 'AI分享贡献',
  OUTCOME_CONVERSION: 'AI成果转化',
};

export const DIMENSION_WEIGHTS: Record<string, number> = {
  TOOL_MASTERY: 0.15,
  APPLICATION_DEPTH: 0.15,
  LEARNING_ABILITY: 0.20,
  SHARING_CONTRIBUTION: 0.20,
  OUTCOME_CONVERSION: 0.30,
};

// 雷达图固定顺序（UI-SPEC 五维度顺序）
export const DIMENSION_ORDER = [
  'TOOL_MASTERY',
  'APPLICATION_DEPTH',
  'LEARNING_ABILITY',
  'SHARING_CONTRIBUTION',
  'OUTCOME_CONVERSION',
] as const;
```

**2. 创建 `EvaluationStepBar.tsx`**（per D-01, D-02）

Props: `{ currentStep: number }` — 0 到 3。

4 步骤横向步骤条：已提交(0) → 主管审核(1) → HR 审核(2) → 已完成(3)。

配色严格按 UI-SPEC 步骤条配色表：
- 已完成步骤: 节点背景 `#E8FFEA`，勾号 `#00B42A`，连线 `#00B42A`
- 当前步骤: 节点背景 `#1456F0`，白色数字，文字 `#1456F0`
- 未到达步骤: 节点背景 `#F2F3F5`，数字 `#8F959E`，连线 `#E0E4EA`

节点 28px 圆形，连线 2px 高。使用 flexbox 布局，纯 CSS 无需 ECharts。
步骤标签：'已提交', '主管审核', 'HR 审核', '已完成'。
纯展示组件，不可点击。

**3. 创建 `DimensionRadarChart.tsx`**（per D-03）

Props: `{ scores: Array<{ dimension_code: string; raw_score: number; weight: number }> }`

使用 `echarts-for-react` 的 `ReactECharts` 组件（参考 AILevelChart.tsx 的 BASE_TEXT_STYLE 和 BASE_TOOLTIP 模式）。

radar 配置：
- indicator: 从 DIMENSION_ORDER 映射中文名，max 100
- 填充区域: `rgba(20, 86, 240, 0.18)`
- 边线: `#1456F0`, width 2
- 数据点: `#1456F0`, symbolSize 4
- 网格线: `#E0E4EA`
- 轴标签: `#646A73`, 13px
- 容器高度 320px

tooltip 显示维度名 + 得分 + 权重。

按 DIMENSION_ORDER 固定顺序排列数据，如果 scores 中找不到某个维度则默认 0。

**4. 创建 `DimensionCard.tsx`**（per D-03）

Props: `{ dimensionCode: string; rawScore: number; weight: number; rationale: string }`

使用 `surface-subtle px-4 py-4` 样式。
顶部一行：左侧显示中文维度名（从 DIMENSION_LABELS 查找），右侧显示 `得分 82 · 权重 30%`。
下方显示 rationale 文字，`text-sm text-steel`。
权重格式：乘 100 后取整显示百分比。

**5. 创建 `SalaryResultCard.tsx`**（per D-05, D-06）

Props: `{ adjustmentRatio: number }` — 0.12 表示 12%。

`surface px-6 py-6` 卡片。
顶部 eyebrow: '调薪建议'。
标题: '最终调薪幅度'（section-title 级别）。
百分比数字: adjustmentRatio * 100 格式化为 `+12.00%`（正数带加号，2 位小数），26px 字号，font-weight 600，颜色 `var(--color-primary)`。
副标题: '以下为审批通过后的最终调整比例。'，`text-sm text-steel`。
  </action>
  <verify>
    <automated>cd D:\wage_adjust\frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - dimensionConstants.ts 导出 DIMENSION_LABELS (5 个中文映射) 和 DIMENSION_WEIGHTS (5 个权重值)
    - EvaluationStepBar.tsx 导出 EvaluationStepBar 组件，接受 currentStep: number
    - DimensionRadarChart.tsx 导出 DimensionRadarChart 组件，使用 ReactECharts 渲染雷达图
    - DimensionCard.tsx 导出 DimensionCard 组件，从 DIMENSION_LABELS 查找中文名
    - SalaryResultCard.tsx 导出 SalaryResultCard 组件，将 ratio 格式化为 +XX.XX%
    - tsc --noEmit 无类型错误
  </acceptance_criteria>
  <done>5 个文件创建完毕，TypeScript 编译通过，所有组件 Props 类型正确</done>
</task>

<task type="auto">
  <name>任务 2: 在 approvalService 中新增 fetchApprovalHistory 函数</name>
  <files>frontend/src/services/approvalService.ts</files>
  <read_first>
    frontend/src/services/approvalService.ts,
    frontend/src/types/api.ts
  </read_first>
  <action>
在 `approvalService.ts` 文件末尾新增导出函数：

```typescript
export async function fetchApprovalHistory(recommendationId: string): Promise<ApprovalListResponse> {
  const response = await api.get<ApprovalListResponse>(`/approvals/recommendations/${recommendationId}/history`);
  return response.data;
}
```

确保在文件顶部的 import type 中已包含 `ApprovalListResponse`（已有）。

此函数对应后端已存在的 `GET /api/v1/approvals/recommendations/{recommendation_id}/history` 端点，使用 `ensure_recommendation_access` 权限检查，employee 角色可以访问自己评估的审批历史。
  </action>
  <verify>
    <automated>cd D:\wage_adjust\frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - approvalService.ts 中存在 fetchApprovalHistory 函数导出
    - 函数调用 /approvals/recommendations/{recommendationId}/history 端点
    - tsc --noEmit 无类型错误
  </acceptance_criteria>
  <done>approvalService 新增 fetchApprovalHistory，TypeScript 编译通过</done>
</task>

</tasks>

<verification>
```bash
cd D:\wage_adjust\frontend && npx tsc --noEmit
```
全部组件类型检查通过，无编译错误。
</verification>

<success_criteria>
- 6 个新增/修改文件全部存在且导出正确
- TypeScript 严格模式编译通过
- 维度常量集中管理，组件不重复定义中文名
- ECharts 雷达图配置遵循 UI-SPEC 配色
- 步骤条配色遵循 UI-SPEC 状态色定义
</success_criteria>

<output>
完成后创建 `.planning/phases/08-employee-self-service-ui/08-01-SUMMARY.md`
</output>
