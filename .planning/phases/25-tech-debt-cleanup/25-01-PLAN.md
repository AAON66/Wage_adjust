---
phase: 25-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/llm_service.py
  - backend/tests/test_eval_pipeline.py
  - frontend/src/components/eligibility-import/FeishuSyncPanel.tsx
autonomous: true
requirements:
  - DEBT-01
  - DEBT-02

must_haves:
  truths:
    - "llm_service.py 不包含本地 InMemoryRateLimiter 类定义"
    - "llm_service.py 从 core/rate_limiter.py 导入 InMemoryRateLimiter"
    - "FeishuSyncPanel 使用 useTaskPolling hook 轮询任务状态"
    - "FeishuSyncPanel 同步过程中显示进度信息（已处理/总数/错误数）"
    - "现有 AI 评估测试通过，无回归"
  artifacts:
    - path: "backend/app/services/llm_service.py"
      provides: "LLM service with shared rate limiter import"
      contains: "from backend.app.core.rate_limiter import InMemoryRateLimiter"
    - path: "frontend/src/components/eligibility-import/FeishuSyncPanel.tsx"
      provides: "Feishu sync panel with shared polling hook"
      contains: "useTaskPolling"
  key_links:
    - from: "backend/app/services/llm_service.py"
      to: "backend/app/core/rate_limiter.py"
      via: "import InMemoryRateLimiter"
      pattern: "from backend\\.app\\.core\\.rate_limiter import InMemoryRateLimiter"
    - from: "frontend/src/components/eligibility-import/FeishuSyncPanel.tsx"
      to: "frontend/src/hooks/useTaskPolling.ts"
      via: "import useTaskPolling"
      pattern: "import.*useTaskPolling.*from.*hooks/useTaskPolling"
---

<objective>
消除 v1.2 遗留的两项技术债：(1) llm_service.py 中重复的 InMemoryRateLimiter 类定义改为从 core/rate_limiter.py 导入；(2) FeishuSyncPanel 手写 setTimeout 轮询改为共享 useTaskPolling hook 并添加进度显示。

Purpose: 为 v1.3 新功能开发提供干净基线，消除重复代码和不一致的轮询模式。
Output: 两个重构后的文件，功能行为不变，代码统一使用共享基础设施。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-tech-debt-cleanup/25-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From backend/app/core/rate_limiter.py:
```python
class InMemoryRateLimiter:
    def __init__(
        self,
        limit: int,
        *,
        window_seconds: int = 60,
        clock: Callable[[], float] | None = None,
        sleeper: Callable[[float], None] | None = None,
    ) -> None: ...

    def acquire(self) -> None: ...
    def wait_and_acquire(self) -> None: ...
```

From frontend/src/hooks/useTaskPolling.ts:
```typescript
interface UseTaskPollingOptions {
  onComplete: (result: unknown) => void;
  onError: (error: string) => void;
  onProgress?: (progress: { processed: number; total: number; errors: number }) => void;
}

export function useTaskPolling(taskId: string | null, options: UseTaskPollingOptions): TaskStatusResponse | null;
```

From frontend/src/types/api.ts:
```typescript
export interface TaskStatusResponse {
  task_id: string;
  status: 'pending' | 'running' | 'completed' | 'failed';
  progress?: { processed: number; total: number; errors: number };
  result?: unknown;
  error?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 删除 llm_service.py 本地 InMemoryRateLimiter，改为从 core 导入 (DEBT-01)</name>
  <files>backend/app/services/llm_service.py, backend/tests/test_eval_pipeline.py</files>
  <read_first>
    - backend/app/services/llm_service.py (查看本地 InMemoryRateLimiter 定义在第73-86行)
    - backend/app/core/rate_limiter.py (共享版本，是超集)
    - backend/tests/test_eval_pipeline.py (第216行从 llm_service 导入 InMemoryRateLimiter)
  </read_first>
  <action>
**llm_service.py 修改：**

1. 在文件顶部导入区域添加：`from backend.app.core.rate_limiter import InMemoryRateLimiter`
2. 删除第73-86行的本地 `class InMemoryRateLimiter` 定义（含 `__init__`、`acquire` 方法）
3. 从顶部导入中移除不再需要的 `deque`（`from collections import deque`），因为 `InMemoryRateLimiter` 是唯一使用者。注意保留 `Callable`（`from collections.abc import Callable`），因为 `LlmService.__init__` 的 `sleeper` 和 `clock` 参数类型仍需要它
4. `_build_rate_limiter` 方法（第305-317行）中的 fallback 路径 `InMemoryRateLimiter(settings.deepseek_requests_per_minute, clock=clock)` 无需修改——core 版本的 `__init__` 签名兼容（`limit, *, window_seconds=60, clock=None, sleeper=None`），`clock=clock` 关键字参数直接匹配

**test_eval_pipeline.py 修改：**

1. 第216行：将 `from backend.app.services.llm_service import DeepSeekService, InMemoryRateLimiter` 改为 `from backend.app.services.llm_service import DeepSeekService` 和 `from backend.app.core.rate_limiter import InMemoryRateLimiter`（两条独立 import 语句）
2. 该测试逻辑不变：仍然验证 `isinstance(service.rate_limiter, InMemoryRateLimiter)` 为 True

Per D-01: 删除本地副本。Per D-02: core 版本是超集，直接替换不影响调用方。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust && python -c "from backend.app.services.llm_service import DeepSeekService; from backend.app.core.rate_limiter import InMemoryRateLimiter; print('import ok')" && python -m pytest backend/tests/test_eval_pipeline.py::test_redis_rate_limiter_fallback -x -q</automated>
  </verify>
  <acceptance_criteria>
    - llm_service.py 不包含 `class InMemoryRateLimiter` 字符串
    - llm_service.py 包含 `from backend.app.core.rate_limiter import InMemoryRateLimiter`
    - llm_service.py 不包含 `from collections import deque`
    - test_eval_pipeline.py 包含 `from backend.app.core.rate_limiter import InMemoryRateLimiter`
    - test_eval_pipeline.py 不包含 `from backend.app.services.llm_service import DeepSeekService, InMemoryRateLimiter`
    - `python -m pytest backend/tests/test_eval_pipeline.py::test_redis_rate_limiter_fallback -x` 退出码为 0
  </acceptance_criteria>
  <done>llm_service.py 中不存在本地 InMemoryRateLimiter 类定义，改为从 core/rate_limiter.py 导入，测试通过</done>
</task>

<task type="auto">
  <name>Task 2: FeishuSyncPanel 改用 useTaskPolling hook 并添加进度显示 (DEBT-02)</name>
  <files>frontend/src/components/eligibility-import/FeishuSyncPanel.tsx</files>
  <read_first>
    - frontend/src/components/eligibility-import/FeishuSyncPanel.tsx (手写轮询在第57-86行)
    - frontend/src/hooks/useTaskPolling.ts (共享 hook 接口)
    - frontend/src/types/api.ts (TaskStatusResponse 接口，搜索 TaskStatusResponse)
    - frontend/src/services/eligibilityImportService.ts (getSyncStatus 定义)
  </read_first>
  <action>
**FeishuSyncPanel.tsx 修改：**

1. **添加导入：**
   - 添加 `import { useTaskPolling } from '../../hooks/useTaskPolling';`
   - 从 `eligibilityImportService` 的导入列表中移除 `getSyncStatus`（仅此组件使用）

2. **添加进度状态：**
   - 在现有 state 声明区域添加：`const [progress, setProgress] = useState<{ processed: number; total: number; errors: number } | null>(null);`

3. **删除手写轮询（第57-86行），替换为 useTaskPolling：**
   删除整个 `useEffect(() => { if (!taskId) return; ... }, [taskId, onResult])` 块。

   在组件中（state 声明之后、handleFetchFields 之前）添加：
   ```tsx
   useTaskPolling(taskId, {
     onComplete: (result) => {
       onResult(result);
       setTaskId(null);
       setIsSyncing(false);
       setProgress(null);
     },
     onError: (error) => {
       setErrorMessage(error);
       setTaskId(null);
       setIsSyncing(false);
       setProgress(null);
     },
     onProgress: (p) => {
       setProgress(p);
     },
   });
   ```

4. **添加进度显示 UI（Per D-05）：**
   将现有的同步中提示（第249-253行）：
   ```tsx
   {isSyncing ? (
     <p role="status" aria-live="polite" style={{ marginTop: 8, fontSize: 13, color: 'var(--color-steel)' }}>
       正在同步飞书数据...
     </p>
   ) : null}
   ```
   替换为带进度信息的版本：
   ```tsx
   {isSyncing ? (
     <div role="status" aria-live="polite" style={{ marginTop: 8 }}>
       {progress ? (
         <>
           <div style={{ marginBottom: 4, height: 6, borderRadius: 3, background: 'var(--color-bg-subtle, #eee)', overflow: 'hidden' }}>
             <div
               style={{
                 height: '100%',
                 borderRadius: 3,
                 background: 'var(--color-primary, #4f46e5)',
                 width: progress.total > 0 ? `${Math.round((progress.processed / progress.total) * 100)}%` : '0%',
                 transition: 'width 0.3s ease',
               }}
             />
           </div>
           <p style={{ fontSize: 13, color: 'var(--color-steel)' }}>
             已处理 {progress.processed}/{progress.total} 条
             {progress.errors > 0 ? `，${progress.errors} 条错误` : ''}
           </p>
         </>
       ) : (
         <p style={{ fontSize: 13, color: 'var(--color-steel)' }}>正在同步飞书数据...</p>
       )}
     </div>
   ) : null}
   ```

5. **清理未使用导入：**
   - 从 React 导入中移除 `useEffect`（如果 `useEffect` 仅被手写轮询和飞书配置检查使用——注意：第46-52行的 `checkFeishuConfigExists` 仍使用 `useEffect`，因此保留 `useEffect`）
   - 确认 `getSyncStatus` 已从 eligibilityImportService 导入中移除

Per D-03: 替换 setTimeout 为 useTaskPolling。Per D-04: useTaskPolling 内部使用 fetchTaskStatus（通用 /tasks/{id} 端点）。Per D-05: 进度条 + "已处理 X/Y 条，Z 条错误"。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - FeishuSyncPanel.tsx 包含 `import { useTaskPolling } from '../../hooks/useTaskPolling'`
    - FeishuSyncPanel.tsx 包含 `useTaskPolling(taskId,`
    - FeishuSyncPanel.tsx 不包含 `getSyncStatus`
    - FeishuSyncPanel.tsx 不包含 `setTimeout`
    - FeishuSyncPanel.tsx 包含 `已处理`（进度文案）
    - FeishuSyncPanel.tsx 包含 `progress.processed`
    - `npx tsc --noEmit` 退出码为 0（无类型错误）
  </acceptance_criteria>
  <done>FeishuSyncPanel 使用 useTaskPolling hook 轮询，同步过程中显示进度条和"已处理 X/Y 条，Z 条错误"信息</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

本阶段为纯内部重构，不引入新的信任边界、新端点、新输入或新依赖。

| Boundary | Description |
|----------|-------------|
| 无新增 | 仅重构现有代码路径，信任边界与 v1.2 一致 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | rate_limiter 导入路径 | accept | 仅更换 import 来源，core/rate_limiter.py 已被 feishu_service.py 验证使用；行为超集兼容 |
| T-25-02 | Denial of Service | useTaskPolling 轮询间隔 | accept | useTaskPolling 使用固定 2s setInterval，与原手写轮询间隔一致，无新 DoS 风险 |
</threat_model>

<verification>
1. 后端：`python -m pytest backend/tests/test_eval_pipeline.py -x -q` 通过
2. 后端：`python -c "from backend.app.services.llm_service import DeepSeekService"` 无报错
3. 前端：`cd frontend && npx tsc --noEmit` 无类型错误
4. grep 验证：llm_service.py 中无 `class InMemoryRateLimiter` 定义
5. grep 验证：FeishuSyncPanel.tsx 中无 `setTimeout` 和 `getSyncStatus`
</verification>

<success_criteria>
- llm_service.py 中不存在本地 InMemoryRateLimiter 类定义，改为从 core/rate_limiter.py 导入
- FeishuSyncPanel 使用 useTaskPolling hook 进行轮询，同步过程中显示进度条和处理数量
- 现有 AI 评估测试通过，TypeScript 编译通过，无回归
</success_criteria>

<output>
After completion, create `.planning/phases/25-tech-debt-cleanup/25-01-SUMMARY.md`
</output>
