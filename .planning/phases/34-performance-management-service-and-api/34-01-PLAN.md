---
phase: 34-performance-management-service-and-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/performance_record.py
  - backend/app/models/performance_tier_snapshot.py
  - backend/app/models/__init__.py
  - alembic/versions/p34_01_add_department_snapshot_to_performance_records.py
  - alembic/versions/p34_02_add_performance_tier_snapshot_table.py
  - backend/tests/test_models/test_performance_tier_snapshot.py
autonomous: true
requirements: [PERF-08]
gap_closure: false

must_haves:
  truths:
    - "PerformanceRecord ORM 模型新增 department_snapshot 字段，nullable=True，String(100)（D-07）"
    - "新建 PerformanceTierSnapshot ORM 模型，含 9 字段（id/year/computed_at/tiers_json/sample_size/insufficient_sample/distribution_warning/actual_distribution_json/skipped_invalid_grades），year 列 UNIQUE（D-01）"
    - "两个 alembic migration 干净 upgrade head + downgrade base 双向运行；存量 performance_records 行 department_snapshot 保持 NULL，不回填（D-07）"
    - "PerformanceTierSnapshot 模型通过 backend.app.models.__init__ 导出（被 metadata 注册）"
  artifacts:
    - path: backend/app/models/performance_record.py
      provides: "department_snapshot Mapped[str | None] 列定义"
      contains: "department_snapshot"
    - path: backend/app/models/performance_tier_snapshot.py
      provides: "PerformanceTierSnapshot ORM 模型 + UNIQUE(year) 约束"
      contains: "class PerformanceTierSnapshot"
    - path: alembic/versions/p34_01_add_department_snapshot_to_performance_records.py
      provides: "ADD COLUMN performance_records.department_snapshot VARCHAR(100) NULL"
      contains: "add_column"
    - path: alembic/versions/p34_02_add_performance_tier_snapshot_table.py
      provides: "CREATE TABLE performance_tier_snapshots"
      contains: "create_table"
    - path: backend/tests/test_models/test_performance_tier_snapshot.py
      provides: "PerformanceTierSnapshot ORM round-trip 测试 + UNIQUE(year) 约束验证"
      min_cases: 4
  key_links:
    - from: "backend/app/models/__init__.py"
      to: "PerformanceTierSnapshot"
      via: "from .performance_tier_snapshot import PerformanceTierSnapshot"
      pattern: "PerformanceTierSnapshot"
    - from: "alembic/versions/p34_02_add_performance_tier_snapshot_table.py"
      to: "performance_tier_snapshots table"
      via: "op.create_table('performance_tier_snapshots', ...)"
      pattern: "performance_tier_snapshots"
---

<objective>
为 Phase 34 提供数据持久化骨架：① 给 PerformanceRecord 加 `department_snapshot` 列，承接 PERF-08 的部门快照需求；② 新建 `PerformanceTierSnapshot` 表，作为档次重算结果的「单年一行」持久化锚点（D-01），让 Wave 2 的 PerformanceService 有读写目标。本 plan 只动数据层（model + migration + 模型层单测），不涉及 Service / API / 前端，确保与同 Wave 的 34-02（Settings + Redis helper）零文件冲突，可真正并行。

Why now: Wave 2 的 Service 实现需要 ORM 模型与表结构就位才能写查询，否则会反复阻塞重构。Migration 落地越早，executor 越容易在 wage_adjust.db 上手动验证 schema。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-performance-management-service-and-api/34-CONTEXT.md
@CLAUDE.md
@backend/app/models/performance_record.py
@backend/app/models/mixins.py
@backend/app/core/database.py

**关键决策（必须严格落地）：**
- D-01：PerformanceTierSnapshot 9 字段，UNIQUE(year)，使用 SQLAlchemy `JSON` 类型存 `tiers_json` / `actual_distribution_json`
- D-07：performance_records 加 `department_snapshot: Mapped[str | None] = mapped_column(String(100), nullable=True)`；migration **不**跑数据回填（按当前部门回填会引入虚假快照，违反语义）
- CLAUDE.md：Alembic 是唯一 migration 路径；禁止 `Base.metadata.create_all`；模型顶部 `from __future__ import annotations`；PEP 604 union（`str | None` 而非 `Optional[str]`）

<interfaces>
<!-- 现有 PerformanceRecord 模型（执行人需修改）：-->
```python
# backend/app/models/performance_record.py 现状
class PerformanceRecord(UUIDPrimaryKeyMixin, CreatedAtMixin, UpdatedAtMixin, Base):
    __tablename__ = 'performance_records'
    __table_args__ = (
        UniqueConstraint('employee_id', 'year', name='uq_performance_employee_year'),
    )
    employee_id: Mapped[str] = mapped_column(String(36), ForeignKey('employees.id'), nullable=False, index=True)
    employee_no: Mapped[str] = mapped_column(String(64), nullable=False, index=True)
    year: Mapped[int] = mapped_column(Integer, nullable=False, index=True)
    grade: Mapped[str] = mapped_column(String(8), nullable=False, comment='A/B/C/D/E')
    source: Mapped[str] = mapped_column(String(32), nullable=False, default='manual', comment='manual/excel/feishu')
```

<!-- 复用 mixins：-->
- `UUIDPrimaryKeyMixin` -> `id: Mapped[str]` UUID PK
- `CreatedAtMixin` -> `created_at: Mapped[datetime]`
- `UpdatedAtMixin` -> `updated_at: Mapped[datetime]` (auto-update)

<!-- alembic 现有 migration 命名约定（参考已有文件）：-->
- 简洁 prefix（如 `b12_`、`013_`、`fa1c02bf9cd1_`）+ snake_case 描述
- 本 plan 使用 `p34_01_` / `p34_02_` 前缀以表明 Phase 34 顺序
- alembic 目录在仓库根 `alembic/versions/`（不是 `backend/alembic/`）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: PerformanceRecord 加 department_snapshot 字段 + 新建 PerformanceTierSnapshot 模型</name>
  <files>
    backend/app/models/performance_record.py
    backend/app/models/performance_tier_snapshot.py
    backend/app/models/__init__.py
  </files>
  <read_first>
    - backend/app/models/performance_record.py（5 字段现状）
    - backend/app/models/mixins.py（UUIDPrimaryKeyMixin / CreatedAtMixin / UpdatedAtMixin 接口）
    - backend/app/core/database.py（Base 来源 + 类型注解模式）
    - backend/app/models/__init__.py（确认现有模型导出顺序，新增按字母序插入）
    - 34-CONTEXT.md §decisions 区域 D-01 / D-07（字段清单、JSON 列、不回填语义）
    - CLAUDE.md「Coding Conventions」段（PEP 604 union、from __future__ import annotations 强制）
  </read_first>
  <action>
1. 修改 `backend/app/models/performance_record.py`：
   - 在现有 5 字段之后追加：
     ```python
     department_snapshot: Mapped[str | None] = mapped_column(
         String(100), nullable=True,
         comment='Phase 34 D-07：录入时员工所属部门快照；存量行 NULL 不回填',
     )
     ```
   - 不动 `__table_args__`、`__tablename__`、其他列；禁止改动 `UniqueConstraint('employee_id', 'year', ...)`
   - 顶部 import 不变（String 已 import）

2. 新建 `backend/app/models/performance_tier_snapshot.py`，完整内容（D-01 9 字段）：
   ```python
   from __future__ import annotations

   from sqlalchemy import JSON, Boolean, Integer, UniqueConstraint
   from sqlalchemy.orm import Mapped, mapped_column

   from backend.app.core.database import Base
   from backend.app.models.mixins import (
       CreatedAtMixin,
       UUIDPrimaryKeyMixin,
       UpdatedAtMixin,
   )


   class PerformanceTierSnapshot(
       UUIDPrimaryKeyMixin, CreatedAtMixin, UpdatedAtMixin, Base,
   ):
       """Phase 34 D-01：单年一行的档次快照表。

       一年一行；HR 重算或导入触发同步重算时 UPSERT；查询走 Redis cache → 表 → 404。
       Service 层用 SELECT ... FOR UPDATE NOWAIT 防并发重算（D-05）。
       """

       __tablename__ = 'performance_tier_snapshots'
       __table_args__ = (
           UniqueConstraint('year', name='uq_performance_tier_snapshot_year'),
       )

       year: Mapped[int] = mapped_column(Integer, nullable=False, index=True)
       tiers_json: Mapped[dict] = mapped_column(
           JSON, nullable=False,
           comment='完整 {employee_id: 1|2|3|null} 映射（D-01）',
       )
       sample_size: Mapped[int] = mapped_column(Integer, nullable=False, default=0)
       insufficient_sample: Mapped[bool] = mapped_column(
           Boolean, nullable=False, default=False,
       )
       distribution_warning: Mapped[bool] = mapped_column(
           Boolean, nullable=False, default=False,
       )
       actual_distribution_json: Mapped[dict] = mapped_column(
           JSON, nullable=False, default=dict,
           comment='{1: 0.22, 2: 0.68, 3: 0.10}（D-01 / Phase 33 D-04）',
       )
       skipped_invalid_grades: Mapped[int] = mapped_column(
           Integer, nullable=False, default=0,
       )
   ```
   - 注意：`computed_at` 复用 `UpdatedAtMixin.updated_at`（每次 UPSERT 触发更新），不另立列；这与 D-01 「`computed_at: datetime` (UTC, `CreatedAtMixin` + `UpdatedAtMixin`)」一致 — 即「最近一次重算时间」= `updated_at`，「首次建行时间」= `created_at`
   - tiers_json 默认值不设（非空必传），actual_distribution_json 默认 `dict`（重算可能产出空 dict 当样本不足）

3. 修改 `backend/app/models/__init__.py`：
   - 加一行 `from backend.app.models.performance_tier_snapshot import PerformanceTierSnapshot  # noqa: F401`
   - 位置按字母序插入到 `performance_record` 导入之后
   - 如该文件用 `__all__` 列表，把 `'PerformanceTierSnapshot'` 加入

**禁止**：
- 修改 `__tablename__='performance_records'` 或现有 5 列任何一列
- 在 `performance_tier_snapshots` 表上加外键到 employees（snapshot 是历史记录，员工删了仍要看历史）
- 用 SQLAlchemy event listener 自动写 department_snapshot（D-08：Service 层显式赋值）
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust && .venv/bin/python -c "from backend.app.models.performance_record import PerformanceRecord; from backend.app.models.performance_tier_snapshot import PerformanceTierSnapshot; assert hasattr(PerformanceRecord, 'department_snapshot'); assert PerformanceTierSnapshot.__tablename__ == 'performance_tier_snapshots'; assert any(uc.name == 'uq_performance_tier_snapshot_year' for uc in PerformanceTierSnapshot.__table_args__); print('OK')"</automated>
  </verify>
  <done>
    - 两个模型 import 成功，无 SQLAlchemy 注册冲突
    - PerformanceRecord 含 6 列（原 5 + department_snapshot）
    - PerformanceTierSnapshot 含 7 业务列 + 3 mixin 列（id/created_at/updated_at）
    - `from backend.app.models import PerformanceTierSnapshot` 成功（__init__.py 已导出）
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 创建两个 Alembic migration（双向 upgrade/downgrade）</name>
  <files>
    alembic/versions/p34_01_add_department_snapshot_to_performance_records.py
    alembic/versions/p34_02_add_performance_tier_snapshot_table.py
  </files>
  <read_first>
    - alembic/env.py（确认 target_metadata 来源）
    - alembic/versions/013_add_eligibility_models.py（参考真实表创建 migration 写法）
    - alembic/versions/9d428de0df97_add_leave_days_to_attendance.py（参考 add_column migration 写法）
    - 34-CONTEXT.md §decisions D-01 / D-07
  </read_first>
  <action>
1. 先确认当前 head：
   ```bash
   .venv/bin/alembic heads
   ```
   记录 head revision id（设为 `<CURRENT_HEAD>`）。如果有多 head，先解决冲突（不应有；Phase 33 未加 migration）。

2. 新建 `alembic/versions/p34_01_add_department_snapshot_to_performance_records.py`：
   ```python
   """Phase 34 D-07: add department_snapshot column to performance_records.

   Revision ID: p34_01_dept_snapshot
   Revises: <CURRENT_HEAD>
   Create Date: 2026-04-22
   """
   from __future__ import annotations

   import sqlalchemy as sa
   from alembic import op

   revision = 'p34_01_dept_snapshot'
   down_revision = '<CURRENT_HEAD>'   # 替换为实际 head id
   branch_labels = None
   depends_on = None


   def upgrade() -> None:
       # D-07：仅添加列，存量行 NULL 不回填（按现部门回填会引入虚假快照，违反语义）
       op.add_column(
           'performance_records',
           sa.Column(
               'department_snapshot',
               sa.String(length=100),
               nullable=True,
               comment='Phase 34 D-07：录入时员工所属部门快照；存量行 NULL',
           ),
       )


   def downgrade() -> None:
       op.drop_column('performance_records', 'department_snapshot')
   ```

3. 新建 `alembic/versions/p34_02_add_performance_tier_snapshot_table.py`：
   ```python
   """Phase 34 D-01: add performance_tier_snapshots table.

   Revision ID: p34_02_tier_snapshot
   Revises: p34_01_dept_snapshot
   Create Date: 2026-04-22
   """
   from __future__ import annotations

   import sqlalchemy as sa
   from alembic import op

   revision = 'p34_02_tier_snapshot'
   down_revision = 'p34_01_dept_snapshot'
   branch_labels = None
   depends_on = None


   def upgrade() -> None:
       op.create_table(
           'performance_tier_snapshots',
           sa.Column('id', sa.String(length=36), primary_key=True),
           sa.Column('created_at', sa.DateTime(timezone=True), nullable=False),
           sa.Column('updated_at', sa.DateTime(timezone=True), nullable=False),
           sa.Column('year', sa.Integer(), nullable=False),
           sa.Column('tiers_json', sa.JSON(), nullable=False),
           sa.Column('sample_size', sa.Integer(), nullable=False, server_default='0'),
           sa.Column('insufficient_sample', sa.Boolean(), nullable=False, server_default=sa.text('0')),
           sa.Column('distribution_warning', sa.Boolean(), nullable=False, server_default=sa.text('0')),
           sa.Column('actual_distribution_json', sa.JSON(), nullable=False),
           sa.Column('skipped_invalid_grades', sa.Integer(), nullable=False, server_default='0'),
           sa.UniqueConstraint('year', name='uq_performance_tier_snapshot_year'),
       )
       op.create_index(
           'ix_performance_tier_snapshots_year',
           'performance_tier_snapshots',
           ['year'],
       )


   def downgrade() -> None:
       op.drop_index('ix_performance_tier_snapshots_year', table_name='performance_tier_snapshots')
       op.drop_table('performance_tier_snapshots')
   ```

4. 替换 `<CURRENT_HEAD>` 为 step 1 拿到的真实 revision id。两个 migration 形成链：head → p34_01 → p34_02。

5. 双向运行验证：
   ```bash
   .venv/bin/alembic upgrade head        # 跑到最新（运行两个新 migration）
   .venv/bin/alembic downgrade -2        # 退回到 Phase 34 之前
   .venv/bin/alembic upgrade head        # 再次升级到最新
   ```
   全程零异常。

**禁止**：
- 在 migration 里跑数据回填脚本（D-07：存量行保持 NULL）
- 跳过 `down_revision = '<CURRENT_HEAD>'` 链接（会出现 multiple heads）
- 把两个 migration 合并成一个文件（保持原子性，便于独立 downgrade）
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust && .venv/bin/alembic upgrade head && .venv/bin/python -c "from sqlalchemy import create_engine, inspect; from backend.app.core.config import get_settings; insp = inspect(create_engine(get_settings().database_url)); cols = {c['name'] for c in insp.get_columns('performance_records')}; assert 'department_snapshot' in cols; tbls = set(insp.get_table_names()); assert 'performance_tier_snapshots' in tbls; tcols = {c['name'] for c in insp.get_columns('performance_tier_snapshots')}; assert {'year','tiers_json','sample_size','insufficient_sample','distribution_warning','actual_distribution_json','skipped_invalid_grades','id','created_at','updated_at'}.issubset(tcols); print('OK')"</automated>
  </verify>
  <done>
    - `alembic upgrade head` 退出码 0
    - `alembic downgrade -2 && alembic upgrade head` 干净往返
    - sqlite schema dump 显示 `performance_records.department_snapshot VARCHAR(100) NULL`
    - sqlite schema dump 显示 `performance_tier_snapshots` 表 10 列 + UNIQUE(year)
    - `alembic heads` 输出唯一 head = `p34_02_tier_snapshot`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PerformanceTierSnapshot 模型层单测（ORM round-trip + UNIQUE 约束）</name>
  <files>
    backend/tests/test_models/test_performance_tier_snapshot.py
  </files>
  <read_first>
    - backend/tests/test_models/（任何现有模型测试，参考 fixture/conftest 模式）
    - backend/tests/conftest.py（DB session fixture：通常 `db_session` / `session` / `db`，确认实际名称）
    - backend/app/models/performance_tier_snapshot.py（task 1 产物）
  </read_first>
  <behavior>
    - Test 1: 插入一行（year=2026, tiers_json={'emp_1': 1, 'emp_2': 2}, sample_size=2, ...）→ commit → 重新查询，所有字段值与 round-trip 后一致（含 JSON 反序列化为 dict）
    - Test 2: 插入两行同 year → second flush 抛 IntegrityError（UNIQUE(year) 约束生效）
    - Test 3: 默认值生效 — 仅设必填（year + tiers_json + actual_distribution_json）创建对象 + commit + 查询，sample_size==0 / insufficient_sample==False / distribution_warning==False / skipped_invalid_grades==0
    - Test 4: updated_at 在 UPDATE 时变化 — 创建一行记录 created_at_initial → 等待 ≥10ms（或显式 sleep 0.05s）→ 修改 sample_size → commit → 再读 updated_at 必须 > created_at_initial
  </behavior>
  <action>
1. 先扫描 `backend/tests/conftest.py` 和现有 `backend/tests/test_models/*.py` 确认 db session fixture 真实名称（推测 `db_session`，但要验证）

2. 新建 `backend/tests/test_models/test_performance_tier_snapshot.py`，骨架：
   ```python
   from __future__ import annotations

   import time

   import pytest
   from sqlalchemy.exc import IntegrityError

   from backend.app.models.performance_tier_snapshot import PerformanceTierSnapshot


   def test_round_trip_persists_all_fields(db_session) -> None:  # noqa: ANN001
       snap = PerformanceTierSnapshot(
           year=2026,
           tiers_json={'emp_1': 1, 'emp_2': 2, 'emp_3': None},
           sample_size=2,
           insufficient_sample=False,
           distribution_warning=False,
           actual_distribution_json={'1': 0.5, '2': 0.5, '3': 0.0},
           skipped_invalid_grades=1,
       )
       db_session.add(snap)
       db_session.commit()
       snap_id = snap.id

       db_session.expire_all()
       loaded = db_session.get(PerformanceTierSnapshot, snap_id)
       assert loaded is not None
       assert loaded.year == 2026
       assert loaded.tiers_json == {'emp_1': 1, 'emp_2': 2, 'emp_3': None}
       assert loaded.sample_size == 2
       assert loaded.insufficient_sample is False
       assert loaded.distribution_warning is False
       assert loaded.actual_distribution_json == {'1': 0.5, '2': 0.5, '3': 0.0}
       assert loaded.skipped_invalid_grades == 1


   def test_unique_year_constraint(db_session) -> None:  # noqa: ANN001
       db_session.add(PerformanceTierSnapshot(
           year=2025, tiers_json={}, actual_distribution_json={},
       ))
       db_session.commit()
       db_session.add(PerformanceTierSnapshot(
           year=2025, tiers_json={'a': 1}, actual_distribution_json={},
       ))
       with pytest.raises(IntegrityError):
           db_session.commit()
       db_session.rollback()


   def test_default_values_applied(db_session) -> None:  # noqa: ANN001
       snap = PerformanceTierSnapshot(
           year=2024,
           tiers_json={},
           actual_distribution_json={},
       )
       db_session.add(snap)
       db_session.commit()
       db_session.expire_all()
       loaded = db_session.get(PerformanceTierSnapshot, snap.id)
       assert loaded.sample_size == 0
       assert loaded.insufficient_sample is False
       assert loaded.distribution_warning is False
       assert loaded.skipped_invalid_grades == 0


   def test_updated_at_changes_on_update(db_session) -> None:  # noqa: ANN001
       snap = PerformanceTierSnapshot(
           year=2023, tiers_json={}, actual_distribution_json={},
       )
       db_session.add(snap)
       db_session.commit()
       initial_updated = snap.updated_at
       time.sleep(0.05)
       snap.sample_size = 99
       db_session.commit()
       db_session.refresh(snap)
       assert snap.updated_at > initial_updated
   ```

3. 如 conftest 的 fixture 名不是 `db_session`（执行人 grep 确认），按真实名替换。

4. 运行：
   ```bash
   .venv/bin/pytest backend/tests/test_models/test_performance_tier_snapshot.py -v
   ```
   全部 4 用例通过；如 round_trip 测试失败因 SQLite `Boolean(server_default=sa.text('0'))` 默认值未通过 ORM 层应用，把 `default=False` 也加在 model（task 1 已有），无需改 migration。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust && .venv/bin/pytest backend/tests/test_models/test_performance_tier_snapshot.py -v --tb=short</automated>
  </verify>
  <done>
    - pytest 退出码 0，4 用例全绿
    - round-trip 含 JSON 字段反序列化正确（dict 进 dict 出）
    - UNIQUE(year) 在 commit 时抛 IntegrityError 而非静默允许
    - updated_at 在 UPDATE 后大于初始值（CreatedAtMixin + UpdatedAtMixin 联动正确）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → live DB | Alembic 脚本写入生产 schema；列默认值与 NOT NULL 约束错配会破坏现有数据 |
| ORM JSON column → app code | `tiers_json` / `actual_distribution_json` 写入时无 schema 校验，依赖 Service/Pydantic 层兜底 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-DATA-01 | Tampering | alembic migration `p34_01` | mitigate | 仅 ADD COLUMN nullable=True，不跑数据回填脚本（D-07）；downgrade 完整支持回滚 |
| T-34-DATA-02 | Tampering | alembic migration `p34_02` | mitigate | server_default + UniqueConstraint 双重约束；downgrade drop_index + drop_table 完整 |
| T-34-DATA-03 | Information Disclosure | tiers_json column | accept | 表本身只 admin/hrbp 通过 Service 层访问，无 employee_id → 个人 PII 映射的暴露风险（员工 id 是 UUID，无个人隐私） |
| T-34-DATA-04 | Denial of Service | tiers_json size | accept | 单年员工 < 5000，dict 序列化后 < 200KB，远低于 PostgreSQL/SQLite JSON 列限制 |
| T-34-DATA-05 | Repudiation | snapshot 重算覆盖 | accept | 本 plan 不引入审计；Phase 34-03 在 Service 层用 audit_log 记录重算操作（actor + timestamp） |
</threat_model>

<verification>
- `alembic upgrade head` 退出码 0
- `alembic downgrade -2 && alembic upgrade head` 双向干净
- `pytest backend/tests/test_models/test_performance_tier_snapshot.py -v` 4 cases pass
- `python -c "from backend.app.models import PerformanceTierSnapshot"` 成功（__init__.py 导出）
- 现有 pytest suite 不退化：`pytest backend/tests/test_engines/ -x` 通过（不动 engine 层，应仍是 64 passed）
</verification>

<success_criteria>
- PerformanceRecord 模型与表均含 `department_snapshot` 列（VARCHAR(100) NULL）
- PerformanceTierSnapshot 模型与表均含 7 业务列 + UNIQUE(year)
- 两个 alembic migration 形成链 + 双向运行
- 模型层 4 个 pytest 用例全绿
- Wave 2（Plan 34-03）可直接 import `PerformanceTierSnapshot` 与 `PerformanceRecord.department_snapshot`，无需 schema 阻塞
</success_criteria>

<output>
After completion, create `.planning/phases/34-performance-management-service-and-api/34-01-SUMMARY.md` with:
- `tech-stack` 段：alembic 新增两条 migration（id 列出）、新增 model 文件路径
- `key-files` 段：created / modified 文件清单
- `key-decisions` 段：computed_at 复用 UpdatedAtMixin.updated_at 而非独立列（与 D-01 等价的化简）
- `Phase 34-03 Service 层接口契约` 段：列出 PerformanceTierSnapshot 的 7 业务字段名称与类型，便于 Wave 2 直接消费
- `requirements-completed: [PERF-08]`（部分；UI 侧由 34-04 完成展示）
</output>
