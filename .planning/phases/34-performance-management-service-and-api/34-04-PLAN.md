---
phase: 34-performance-management-service-and-api
plan: 04
type: execute
wave: 3
depends_on: [34-03]
files_modified:
  - frontend/src/pages/PerformanceManagementPage.tsx
  - frontend/src/components/performance/TierDistributionPanel.tsx
  - frontend/src/components/performance/TierStackedBar.tsx
  - frontend/src/components/performance/TierChip.tsx
  - frontend/src/components/performance/DistributionWarningBanner.tsx
  - frontend/src/components/performance/PerformanceRecordsTable.tsx
  - frontend/src/components/performance/PerformanceRecordsFilters.tsx
  - frontend/src/services/performanceService.ts
  - frontend/src/types/api.ts
  - frontend/src/utils/roleAccess.ts
  - frontend/src/utils/toast.ts
  - frontend/src/App.tsx
  - frontend/src/components/icons/NavIcons.tsx
autonomous: true
requirements: [PERF-01, PERF-02, PERF-05, PERF-08]
gap_closure: false

must_haves:
  truths:
    - "HR 和 admin 登录后在侧边栏看到「绩效管理」菜单入口（href='/performance'）；employee 和 manager 不可见"
    - "导航到 /performance 后看到 3 个垂直 section：档次分布 / 绩效记录导入 / 绩效记录列表（D-13 / UI-SPEC §5）"
    - "档次分布 section 含年份 select + 重算按钮 + 最近重算时间戳 + ECharts 水平堆叠条 + 4 个 chip + 条件渲染 warning 横幅（D-11 / UI-SPEC §6）"
    - "stacked bar 使用 echarts + echarts-for-react，颜色 1档#10b981 / 2档#f59e0b / 3档#ef4444，高度 32px，tooltip 显示 「{档} 档：{count} 人 ({pct}%)」（UI-SPEC §6.3）"
    - "warning 横幅用 role='alert'；文案含实际百分比（UI-SPEC §6.2 + §13）"
    - "重算按钮点击调 POST /performance/recompute-tiers?year=X；loading 期间 aria-busy='true' + disabled + RefreshIcon animate-spin + 文字「重算中…」（UI-SPEC §6.1 + §10.1）"
    - "409 响应触发 toast + 5 秒后启用按钮（UI-SPEC §10.3 + D-06）"
    - "404 响应替换 stacked bar 区域为空状态卡片（ChartIcon + 「{year} 年尚无档次快照」+ 「立即生成档次」按钮触发 POST recompute）（UI-SPEC §9 + D-10 + D-12）"
    - "绩效记录导入 section 直接 ExcelImportPanel importType=performance_grades；confirm 成功后自动重拉 tier-summary + records（UI-SPEC §7）"
    - "**W-1 落地**：handleImportComplete 根据 ConfirmResponse.tier_recompute_status 分支 toast 文案：completed → 绿「导入完成（X 条），档次已刷新」/ in_progress → 蓝「导入完成（X 条），档次正在后台重算…」/ busy_skipped → 黄「导入完成（X 条），系统繁忙后续自动重算」/ failed → 红「导入完成（X 条）但档次重算失败，请手动点击『重算档次』」"
    - "绩效记录列表 section 含 7 列（工号/姓名/年份/绩效等级/部门快照/来源/录入时间）+ year/department filter + 上下页分页（UI-SPEC §8 / D-14）"
    - "工号列用 tabular-nums 保留前导零字符串；部门快照为 null 显示 「—」（UI-SPEC §8.2 + D-07）"
    - "**B-5 落地**：来源 chip 颜色映射只用 frontend/src/index.css :root 段实际定义的 token（如 --color-primary / --color-bg-subtle / --color-violet-bg / --color-violet 等）"
    - "RefreshIcon / WarningIcon / ChartIcon 3 个新图标加入 NavIcons.tsx；禁止引入 lucide-react / Recharts（UI-SPEC §0 反向勘误）"
    - "**B-4 落地**：NavIcons.tsx 的 IconProps 接口扩展 style?: React.CSSProperties；svg() helper 把传入 style 与既有 inline style merge 注入 svg 元素（支持 UI-SPEC §6.2 / §9 的 <WarningIcon style={{...}}/> / <ChartIcon style={{...}}/> 用法）"
    - "**B-3 落地**：performanceService.ts 暴露 6 个函数（含 getAvailableYears）；types/api.ts 含 AvailableYearsResponse interface；PerformanceManagementPage.tsx availableYears state 改用 getAvailableYears() 拉取，fallback 链：返回数组长度 0 → [当前年]"
    - "roleAccess.ts 的 admin + hrbp 菜单组 'operations' 追加 '绩效管理' 菜单项；employee/manager 不追加"
    - "App.tsx 新增 /performance 路由在 admin+hrbp ProtectedRoute 内"
    - "**W-4 落地**：utils/toast.ts 提供单实例 toast helper（cancel 旧 toast 再显示新 toast，避免 alert 多次堆积卡浏览器）；标 // TODO Phase 35+ 替换为正式 toast 库"
    - "performanceService.ts 暴露 6 个函数；404/409 分别抛 NoSnapshotError / TierRecomputeBusyError"
    - "types/api.ts 追加 6 个 Phase 34 类型（与后端 Pydantic schema 字段对齐，含 AvailableYearsResponse）"
    - "npm run lint + npm run build 均退出码 0"
  artifacts:
    - path: frontend/src/pages/PerformanceManagementPage.tsx
      provides: "页面容器 + AppShell + 3 个 section + state + 数据拉取 orchestration"
      contains: "AppShell"
      min_lines: 120
    - path: frontend/src/components/performance/TierDistributionPanel.tsx
      provides: "Section 1 整合：section-head + warning + stacked bar + chip 行 + 空状态"
      contains: "TierDistributionPanel"
    - path: frontend/src/components/performance/TierStackedBar.tsx
      provides: "ECharts horizontal stacked bar（封装 option）"
      contains: "ReactECharts"
    - path: frontend/src/components/performance/TierChip.tsx
      provides: "TierChip + UnTieredChip 导出"
      contains: "TierChip"
    - path: frontend/src/components/performance/DistributionWarningBanner.tsx
      provides: "黄色 warning 横幅"
      contains: "role"
    - path: frontend/src/components/performance/PerformanceRecordsTable.tsx
      provides: "Section 3 表格 + 分页器（含 B-5 来源 chip 颜色映射）"
      contains: "PerformanceRecordsTable"
    - path: frontend/src/components/performance/PerformanceRecordsFilters.tsx
      provides: "年份 + 部门 filter（toolbar-input 样式）"
      contains: "PerformanceRecordsFilters"
    - path: frontend/src/services/performanceService.ts
      provides: "6 个 axios-based 函数（含 getAvailableYears B-3）+ 2 个自定义 Error 类"
      contains: "getTierSummary"
    - path: frontend/src/utils/toast.ts
      provides: "W-4：单实例 toast helper（取消旧 toast + 新 toast 显示）"
      contains: "showToast"
    - path: frontend/src/utils/roleAccess.ts
      provides: "admin + hrbp 菜单追加 '绩效管理'"
      contains: "/performance"
    - path: frontend/src/App.tsx
      provides: "新增 /performance 路由在 admin+hrbp ProtectedRoute"
      contains: "PerformanceManagementPage"
    - path: frontend/src/components/icons/NavIcons.tsx
      provides: "RefreshIcon / WarningIcon / ChartIcon 3 个新图标 + B-4 IconProps 扩 style"
      contains: "RefreshIcon"
  key_links:
    - from: "frontend/src/pages/PerformanceManagementPage.tsx"
      to: "/api/v1/performance/tier-summary"
      via: "getTierSummary(year) in useEffect"
      pattern: "getTierSummary"
    - from: "frontend/src/pages/PerformanceManagementPage.tsx"
      to: "/api/v1/performance/available-years"
      via: "getAvailableYears() in useEffect (B-3 修复 — 替换 200-records hack)"
      pattern: "getAvailableYears"
    - from: "frontend/src/pages/PerformanceManagementPage.tsx"
      to: "ExcelImportPanel importType=performance_grades"
      via: "JSX 复用 Phase 32 组件"
      pattern: "performance_grades"
    - from: "frontend/src/components/performance/TierStackedBar.tsx"
      to: "echarts option 三色"
      via: "ReactECharts option.series 3 段 + itemStyle.color"
      pattern: "10b981"
    - from: "frontend/src/App.tsx"
      to: "/performance route"
      via: "Route element PerformanceManagementPage path /performance"
      pattern: "PerformanceManagementPage"
    - from: "frontend/src/utils/roleAccess.ts"
      to: "admin+hrbp menu"
      via: "operations group 追加 '绩效管理'"
      pattern: "/performance"
---

> **N-3 范围声明（Plan-level Banner）：**
> 本期 Phase 34 **不实现**「行点击跳详情」（Phase 36 范围）、「员工自助 /me/tier」（Phase 35 ESELF-03 范围）、「多年趋势视图」（Phase 36 或 dashboard 范围）。executor 不要在表格行加 onClick 跳转 / 不要写 me/tier 调用代码 / 不要做时间序列 chart。

<objective>
交付 Phase 34 前端：HR + admin 可访问的「绩效管理」独立页面，3 section 垂直排列 —— 档次分布（ECharts 堆叠条 + chip + 重算按钮 + 空状态 + warning）/ 绩效记录导入（复用 Phase 32 ExcelImportPanel）/ 绩效记录列表（7 列 + filter + 分页）。严格遵循 UI-SPEC §0 反向勘误：只用已装的 echarts + echarts-for-react，图标内联 SVG 加入 NavIcons.tsx；零新增第三方 UI 依赖。

Why Wave 3: 依赖 34-03 的 5 个 REST 端点（含 B-3 新增 /available-years）与 Pydantic schema；在 34-03 之前写会没有 API 可调。

Why single plan: 前端 13 个文件变更是一个紧耦合的视觉 + 交互单元；拆成两 plan 会让共享 types/state 管理破碎。任务数控制在 3 个（基础设施 → 分布 panel + 子组件 → 列表 panel + 页面 orchestration + lint/build）。任务 1 改 7 文件，按 sub-step 分批 commit（W-2 修复）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-performance-management-service-and-api/34-CONTEXT.md
@.planning/phases/34-performance-management-service-and-api/34-UI-SPEC.md
@CLAUDE.md
@frontend/src/pages/EligibilityManagementPage.tsx
@frontend/src/components/eligibility-import/ExcelImportPanel.tsx
@frontend/src/components/layout/AppShell.tsx
@frontend/src/components/icons/NavIcons.tsx
@frontend/src/utils/roleAccess.ts
@frontend/src/App.tsx
@frontend/src/services/api.ts
@frontend/src/services/eligibilityService.ts
@frontend/src/types/api.ts
@frontend/src/index.css

**关键决策（必须严格落地）：**
- UI-SPEC §0 反向勘误：不用 Recharts，改 ECharts；不用 lucide-react，改内联 SVG
- UI-SPEC §6.3：ECharts option 完整规范（grid / tooltip / xAxis / yAxis / series 3 段 + color + borderRadius + barWidth 100%）
- UI-SPEC §8.2：7 列 data key + 渲染规则
- UI-SPEC §13：所有 zh-CN 文案锁定（页面 title / section title / copy / toast）
- D-11 堆叠条颜色：1 档 #10b981 / 2 档 #f59e0b / 3 档 #ef4444
- D-13 section 顺序：分布 -> 导入 -> 列表
- D-14：列表分页 50 条/页
- D-15：导入复用 ExcelImportPanel importType=performance_grades，不自己写导入 UI
- D-16：roleAccess 限制 admin+hrbp；employee/manager 菜单不显示；URL 直访由后端 403 兜底

<interfaces>
Phase 32 ExcelImportPanel — 直接复用。Props:
- importType: EligibilityImportType（'performance_grades' 已支持）
- label: string（'绩效等级'）
- onComplete?: (result: ConfirmResponse) => void

后端 34-03 端点（含 B-3 新增）：
- GET /api/v1/performance/records?year=X&department=Y&page=N&page_size=50 -> PerformanceRecordsListResponse
- POST /api/v1/performance/records body PerformanceRecordCreatePayload -> PerformanceRecordItem
- GET /api/v1/performance/tier-summary?year=X -> TierSummaryResponse | 404 {detail: NoSnapshotErrorDetail}
- POST /api/v1/performance/recompute-tiers?year=X -> RecomputeTriggerResponse | 409 {detail: TierRecomputeBusyDetail} | 500
- **B-3 新增**：GET /api/v1/performance/available-years -> AvailableYearsResponse { years: number[] }
- ConfirmResponse（W-1 扩字段）：含可选 tier_recompute_status: 'completed' | 'in_progress' | 'busy_skipped' | 'failed' | 'skipped' | null

NavIcons.tsx svg helper：宽高 stroke=1.75 viewBox=0 0 24 24，stroke=currentColor，按现有 Icon* 风格扩展。
- **B-4 现状**（line 14-31）：IconProps 仅含 size / className；svg() 不接受 style；UI-SPEC §6.2/§9 调用 `<WarningIcon style={{...}}/>` 与 `<ChartIcon style={{...}}/>` 会触发 tsc --noEmit 失败 → 必须扩 IconProps 加 `style?: React.CSSProperties`，并在 svg() 内 merge style

frontend/src/index.css :root 段实际可用 token（B-5 真实清单，line 5-44）：
- Backgrounds: --color-bg-page #F5F6F8 / --color-bg-surface #FFFFFF / --color-bg-subtle #F2F3F5 / --color-bg-hover #EBEDF0
- Primary: --color-primary #1456F0 / --color-primary-hover #0E46CC / --color-primary-light #EBF0FE / --color-primary-border #C5D3FB
- Text: --color-ink #1F2329 / --color-steel #646A73 / --color-placeholder #8F959E
- Borders: --color-border #E0E4EA / --color-border-strong #C9CDD4
- Status: --color-success / --color-success-bg / --color-success-border / --color-warning / --color-warning-bg / --color-warning-border / --color-danger / --color-danger-bg / --color-danger-border / --color-info / --color-info-bg
- Phase 31 D-07 紫色：--color-violet #722ED1 / --color-violet-bg #F5EBFA / --color-violet-border #E1C9F0（已存在，可用）

api.ts 默认 axios instance 已注入 JWT 拦截器；新 service 文件复用 `import api from './api'`。

fetchDepartmentNames 复用：frontend/src/services/eligibilityService.ts line 96。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 基础设施（types + service + icons B-4 + roleAccess + route + page stub + toast helper W-4）— 按 sub-step 分批改 7 文件</name>
  <files>
    frontend/src/types/api.ts
    frontend/src/services/performanceService.ts
    frontend/src/components/icons/NavIcons.tsx
    frontend/src/utils/roleAccess.ts
    frontend/src/utils/toast.ts
    frontend/src/App.tsx
    frontend/src/pages/PerformanceManagementPage.tsx
  </files>
  <read_first>
    - frontend/src/types/api.ts（类型定义文件末尾追加位置）
    - frontend/src/services/api.ts（axios instance 复用模式）
    - frontend/src/services/eligibilityService.ts（service 文件风格）
    - **frontend/src/components/icons/NavIcons.tsx line 14-31**（svg() helper signature — B-4 修复点）
    - frontend/src/utils/roleAccess.ts（ROLE_MODULES.admin/hrbp.operations 结构）
    - frontend/src/App.tsx line 460-465（admin+hrbp ProtectedRoute 块）
    - UI-SPEC §13 zh-CN 文案
  </read_first>
  <action>

**W-2 修复：按文件分 sub-step，每完成一个 sub-step 后建议 git commit（便于回滚）；如执行人觉得有必要，可拆 Task 1 为 1a (sub-step 1.1-1.3) + 1b (sub-step 1.4-1.7) 两个独立任务。executor 自行决定。**

### Sub-step 1.1：types/api.ts 追加 6 个 Phase 34 类型
修改 `frontend/src/types/api.ts`，在文件末尾追加 Phase 34 类型块：
- `TierCounts` — `{ '1': number; '2': number; '3': number; none: number }`
- `TierSummaryResponse` — 9 字段（year/computed_at/sample_size/insufficient_sample/distribution_warning/tiers_count/actual_distribution/skipped_invalid_grades）
- `PerformanceRecordItem` — 8 字段（id/employee_id/employee_no/employee_name/year/grade/source/department_snapshot/created_at）
- `PerformanceRecordsListResponse` — items/total/page/page_size/total_pages
- `PerformanceRecordCreatePayload` — employee_id/year/grade/source
- `RecomputeTriggerResponse` — year/computed_at/sample_size/insufficient_sample/distribution_warning/message
- `NoSnapshotErrorDetail` — error/message/year/hint
- `TierRecomputeBusyDetail` — error/message/year/retry_after_seconds
- **B-3 新增**：`AvailableYearsResponse` — `{ years: number[] }`
- **W-1 扩**：`ConfirmResponse` 增加可选字段 `tier_recompute_status?: 'completed' | 'in_progress' | 'busy_skipped' | 'failed' | 'skipped' | null`（如 ConfirmResponse 已存在，直接增字段；否则在该类型定义处补充）

字段类型严格按后端 Pydantic schema：日期字段用 string（ISO datetime），sample_size/year/integer 用 number，bool 用 boolean。

### Sub-step 1.2：performanceService.ts 含 6 个函数（B-3 新增 getAvailableYears）+ 2 个 Error 类
新建 `frontend/src/services/performanceService.ts`：
- `import axios from 'axios'`（仅用 axios.isAxiosError 类型守卫）
- `import api from './api'`
- 定义 `LONG_RECOMPUTE_TIMEOUT = 30000`（30s 缓冲，后端 5s + 网络）
- 定义 `class NoSnapshotError extends Error`（含 readonly year + hint）
- 定义 `class TierRecomputeBusyError extends Error`（含 readonly year + retryAfterSeconds）
- 实现 `getTierSummary(year)`：catch 404 → throw NoSnapshotError(detail.year, detail.hint, detail.message)
- 实现 `recomputeTiers(year)`：用 timeout=LONG_RECOMPUTE_TIMEOUT；catch 409 → throw TierRecomputeBusyError(detail.year, detail.retry_after_seconds, detail.message)
- 实现 `getPerformanceRecords({year?, department?, page?, page_size?})`
- 实现 `createPerformanceRecord(payload)`
- **B-3 新增**：`getAvailableYears(): Promise<number[]>` — `const { data } = await api.get<AvailableYearsResponse>('/performance/available-years'); return data.years;`
- 不要新建 axios.create 实例（必须复用 api.ts）

### Sub-step 1.3：NavIcons.tsx — B-4 修复 + 3 个新图标
修改 `frontend/src/components/icons/NavIcons.tsx`：

**B-4 强制修复（必须完成）：**
1. 扩展 `IconProps` 接口（line 6-9），加 `style?: React.CSSProperties`：
   ```tsx
   interface IconProps {
     size?: number;
     className?: string;
     style?: React.CSSProperties;  // B-4 修复：支持 UI-SPEC §6.2/§9 的 <WarningIcon style={{...}}/> 调用
   }
   ```
2. 修改 `svg()` helper（line 13-31），把 `style` 注入到 `<svg>` 元素，与既有 `style={{ flexShrink: 0 }}` merge：
   ```tsx
   function svg(props: IconProps, children: React.ReactNode) {
     const s = props.size ?? defaults.size;
     return (
       <svg
         className={props.className}
         width={s}
         height={s}
         viewBox="0 0 24 24"
         fill="none"
         stroke="currentColor"
         strokeWidth={1.75}
         strokeLinecap="round"
         strokeLinejoin="round"
         style={{ flexShrink: 0, ...props.style }}  // B-4 merge
       >
         {children}
       </svg>
     );
   }
   ```

**3 个新图标（追加到文件末尾）：**
- `RefreshIcon`：path d="M21 12a9 9 0 1 1-3.5-7.1" + polyline points="21 4 21 10 15 10"
- `WarningIcon`：path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z" + line x1=12 y1=9 x2=12 y2=13 + circle cx=12 cy=17 r=0.5
- `ChartIcon`：4 line（与既有 IconBarChart 同形）
- 全部用扩展后的 `svg(props, ...)` helper（自动支持 style prop）

### Sub-step 1.4：W-4 toast helper
新建 `frontend/src/utils/toast.ts`：
```tsx
// W-4 修复：单实例 toast helper，避免多次 alert 同时触发卡浏览器
// TODO Phase 35+: 替换为正式 toast 库（如 sonner / react-hot-toast）

let activeTimeoutId: number | null = null;

export type ToastVariant = 'success' | 'info' | 'warning' | 'error';

export function showToast(message: string, variant: ToastVariant = 'info'): void {
  // 取消上一个 toast（若仍在显示）
  if (activeTimeoutId !== null) {
    window.clearTimeout(activeTimeoutId);
    activeTimeoutId = null;
  }
  // MVP：用 console.log 标 variant 便于调试，再 alert 弹出
  // TODO Phase 35+: 替换为正式 toast 系统
  console.log(`[toast/${variant}]`, message);
  // 单帧延迟，确保上一个 alert 已被关闭
  activeTimeoutId = window.setTimeout(() => {
    window.alert(message);
    activeTimeoutId = null;
  }, 0);
}
```

### Sub-step 1.5：roleAccess.ts 加菜单
修改 `frontend/src/utils/roleAccess.ts`：
- 在 ROLE_MODULES.admin.operations.items 数组尾部追加：
  `{ title: '绩效管理', description: 'HR 端：导入绩效记录、查看档次分布、手动触发重算。', href: '/performance', icon: 'bar-chart' }`
- 在 ROLE_MODULES.hrbp.operations.items 数组尾部追加同上条目
- 不加到 manager / employee
- icon 字符串保持 'bar-chart'（与现有「组织看板」一致；icon 渲染是 AppShell 内的 string-to-component 映射）

### Sub-step 1.6：App.tsx 加路由
修改 `frontend/src/App.tsx`：
- 顶部 import：在 EligibilityManagementPage 导入之后加 `import { PerformanceManagementPage } from './pages/PerformanceManagementPage';`
- 路由：在 admin+hrbp ProtectedRoute 块（line 460-465）内追加：
  `<Route element={<PerformanceManagementPage />} path="/performance" />`

### Sub-step 1.7：Page stub
新建 `frontend/src/pages/PerformanceManagementPage.tsx` 占位 stub（task 2/3 填实）：
- export function PerformanceManagementPage() returns 简单 AppShell 包装的占位 div
- 必须 export named 而非 default，匹配 App.tsx 的命名 import

### Sub-step 1.8：验证 lint 通过
```
cd frontend && npm run lint
```
退出码 0（stub 已就位 + 所有 import 有效）。**重点验证 B-4：** tsc --noEmit 不报 IconProps 不接受 style 的错误。

**禁止**：
- 在 performanceService.ts 内 axios.create 建新 instance
- 引入 lucide-react 或 Recharts
- 修改 NavIcons.tsx 现有 icon 的渲染逻辑（仅扩 IconProps + svg() merge style 是允许的；既有 IconHome / IconClipboard / IconRefreshCw / IconDollar 等不改）
- 把 /performance 路由放到 manager/employee 允许的 ProtectedRoute
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust/frontend &amp;&amp; npm run lint 2>&amp;1 | tail -5 &amp;&amp; grep -n "style?: React.CSSProperties" frontend/src/components/icons/NavIcons.tsx &amp;&amp; grep -n "getAvailableYears" frontend/src/services/performanceService.ts &amp;&amp; grep -n "AvailableYearsResponse" frontend/src/types/api.ts &amp;&amp; grep -n "showToast" frontend/src/utils/toast.ts</automated>
  </verify>
  <done>
    - npm run lint 退出码 0
    - **B-4 验证**：`grep "style?: React.CSSProperties" frontend/src/components/icons/NavIcons.tsx` 命中 1 行
    - **B-3 验证**：`grep "getAvailableYears" frontend/src/services/performanceService.ts` 命中；`grep "AvailableYearsResponse" frontend/src/types/api.ts` 命中
    - **W-4 验证**：`frontend/src/utils/toast.ts` 文件存在，含 showToast export
    - grep RefreshIcon / WarningIcon / ChartIcon 在 NavIcons.tsx 命中
    - grep '/performance' 在 roleAccess.ts 命中（admin + hrbp 各一处）
    - grep PerformanceManagementPage 在 App.tsx 命中（import + Route）
    - performanceService.ts 含 6 个 export function（含 getAvailableYears B-3）+ 2 个 Error 类
    - types/api.ts 追加 Phase 34 类型块（grep TierSummaryResponse + AvailableYearsResponse 命中）
    - ConfirmResponse 含可选 tier_recompute_status 字段（W-1 同步）
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Section 1 档次分布（5 个组件 + ECharts + 子状态分支）</name>
  <files>
    frontend/src/components/performance/TierStackedBar.tsx
    frontend/src/components/performance/TierChip.tsx
    frontend/src/components/performance/DistributionWarningBanner.tsx
    frontend/src/components/performance/TierDistributionPanel.tsx
  </files>
  <read_first>
    - frontend/src/index.css line 526-574（.table-shell + .table-lite 样式系统）
    - frontend/src/index.css line 283-308（.toolbar-input 样式）
    - 任何已有 echarts 用例（grep ReactECharts frontend/src）— 参考 option 风格
    - UI-SPEC §6 整段（section-head / warning / stacked bar / chip 全部 JSX 与 CSS-in-JS 已给出）
    - UI-SPEC §9（空状态 CTA）
    - UI-SPEC §10.3（toast 文案）
    - UI-SPEC §13（zh-CN 文案表）
    - frontend/src/services/performanceService.ts（task 1 产物）
    - frontend/src/utils/toast.ts（task 1 sub-step 1.4 产物 W-4）
  </read_first>
  <action>
1. 新建 `frontend/src/components/performance/TierStackedBar.tsx`：
   - props: `{ tiersCount: { '1': number; '2': number; '3': number; none: number }; actualDistribution: { '1': number; '2': number; '3': number } }`
   - 使用 `import ReactECharts from 'echarts-for-react'`
   - 容器 `<div style={{ height: 32, width: '100%', marginBottom: 12 }}>`
   - option 严格按 UI-SPEC §6.3 完整代码：
     - grid: { left: 0, right: 0, top: 0, bottom: 0, containLabel: false }
     - tooltip: trigger 'item' + backgroundColor #FFFFFF + borderColor #E0E4EA + formatter 函数显示 `{seriesName}：{count} 人 ({pct}%)`（1 位小数）
     - xAxis: type 'value' show false max 1
     - yAxis: type 'category' show false data ['']
     - series: 3 段堆叠 stack 'total'
       - 第 1 段 name '1 档' color #10b981 borderRadius [4, 0, 0, 4]
       - 第 2 段 name '2 档' color #f59e0b
       - 第 3 段 name '3 档' color #ef4444 borderRadius [0, 4, 4, 0]
       - 全部 barWidth '100%'，data 形如 [{ value: actualDistribution['1'], count: tiersCount['1'] }]

2. 新建 `frontend/src/components/performance/TierChip.tsx`：
   - 导出 `TierChip({ color, label, count, pct })` 与 `UnTieredChip({ count })`
   - JSX 用 `<span className="status-pill">` 不是 button（不可点击）
   - inline style: padding '4px 10px', fontSize 13, background #FFFFFF, border `1px solid ${color}`, color var(--color-ink), gap 6
   - 内含 8x8 圆点 + label + `<strong>{count}</strong> 人 ({(pct*100).toFixed(0)}%)`
   - UnTieredChip：边框用 var(--color-border)，圆点 var(--color-placeholder)，文字「未分档 N 人」

3. 新建 `frontend/src/components/performance/DistributionWarningBanner.tsx`：
   - 导出 `DistributionWarningBanner({ actualDistribution })`（distribution 偏离变体）和 `RecomputeFailedBanner({ oldComputedAt, onRetry })`（重算失败变体）
   - DistributionWarningBanner JSX 严格按 UI-SPEC §6.2：role='alert'，背景 var(--color-warning-bg)，border var(--color-warning-border)，color var(--color-warning)
   - **B-4 用法**：`<WarningIcon size={16} style={{ marginTop: 2, flexShrink: 0 }} />` — 现 NavIcons.tsx svg() 已支持 style prop（task 1 sub-step 1.3）
   - 内含 WarningIcon + 文本「档次分布偏离 20/70/10 超过 ±5%（实际 22%/68%/10%）。建议复核绩效原始数据或调整评估口径。」(pct() helper 把 0.22 转 '22%')
   - RecomputeFailedBanner 文本「档次基于 {old_computed_at} 的旧快照（重算失败，请重试）」+ 内联「立即重算」chip-button 触发 onRetry

4. 新建 `frontend/src/components/performance/TierDistributionPanel.tsx`（Section 1 整合容器）：
   - props: `{ year: number; availableYears: number[]; onYearChange: (y: number) => void; onRecomputed: () => void }`
   - 内部 state：
     - `summary: TierSummaryResponse | null`（null = loading）
     - `noSnapshot: boolean`
     - `isRecomputing: boolean`
     - `error: string | null`
   - useEffect：当 year 变化 → setSummary(null) + setNoSnapshot(false) + 调 getTierSummary(year)；catch NoSnapshotError → setNoSnapshot(true)；catch 其他 → setError
   - 渲染：
     - section-head：标题「档次分布」+ section-note + 右侧「年份 select + 重算按钮」
     - 重算按钮 onClick → setIsRecomputing(true) → recomputeTiers(year) → 成功重新调 getTierSummary + onRecomputed() + showToast('档次重算完成（共 N 人）', 'success')；catch TierRecomputeBusyError → showToast('系统正在自动重算，请稍后重试', 'warning') + setTimeout 5000ms 后启用按钮；其他错误 → showToast('档次重算失败：' + msg, 'error')
     - 重算按钮 disabled={isRecomputing}，aria-busy={isRecomputing}，文字 isRecomputing ? '重算中…' : '重算档次'
     - computed_at 时间戳：用 Intl.DateTimeFormat('zh-CN', { year, month: 'long', day: 'numeric', hour: '2-digit', minute: '2-digit' })，文本「最近重算：{格式化时间}」
     - 内容分支：
       - noSnapshot=true → 渲染空状态卡片（参照 UI-SPEC §9）：`<ChartIcon size={32} style={{ color: 'var(--color-placeholder)', margin: '0 auto 12px' }} />` + h4「{year} 年尚无档次快照」+ 描述 + 「立即生成档次」按钮（onClick 同重算按钮逻辑）— **B-4 用法**：ChartIcon 也通过 style prop 控制 color，task 1 已支持
       - summary=null && !noSnapshot → 32+12+32 px 高的灰色 skeleton 占位（无文字、无动画）
       - summary 有值 → 渲染 distribution_warning ? `<DistributionWarningBanner>` : null + `<TierStackedBar>` + chip 行（4 个 `<TierChip>`，参数从 summary.tiers_count + summary.actual_distribution 拆出）
   - **W-4 改用**：`import { showToast } from '../../utils/toast'`，所有 toast 调 showToast(message, variant)；不直接 alert（避免多次叠 alert 卡浏览器）
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust/frontend &amp;&amp; npm run lint 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    - 4 个新组件文件存在
    - grep ReactECharts 在 TierStackedBar.tsx 命中
    - grep #10b981 / #f59e0b / #ef4444 在 TierStackedBar.tsx 命中（顺序正确）
    - grep role= / alert 在 DistributionWarningBanner.tsx 命中
    - grep aria-busy 在 TierDistributionPanel.tsx 命中
    - grep getTierSummary / recomputeTiers / NoSnapshotError / TierRecomputeBusyError 在 TierDistributionPanel.tsx 命中
    - grep showToast 在 TierDistributionPanel.tsx 命中（W-4：不再直接 alert）
    - **B-4 用法验证**：grep `style={{` 在 DistributionWarningBanner.tsx + TierDistributionPanel.tsx 命中（WarningIcon / ChartIcon 的 style prop 调用通过 tsc 不报错）
    - npm run lint 退出码 0
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Section 2 复用 + Section 3 列表（B-5 来源 chip 颜色映射）+ 页面 orchestration（B-3 调用 getAvailableYears + W-1 toast 分支）+ lint/build 验收</name>
  <files>
    frontend/src/components/performance/PerformanceRecordsTable.tsx
    frontend/src/components/performance/PerformanceRecordsFilters.tsx
    frontend/src/pages/PerformanceManagementPage.tsx
  </files>
  <read_first>
    - frontend/src/components/eligibility/EligibilityListTab.tsx（参考 fetchDepartmentNames 调用 + filter row + table-lite 表格 + 分页器模式）
    - frontend/src/components/eligibility-import/ExcelImportPanel.tsx（onComplete prop 签名 + ConfirmResponse 类型）
    - frontend/src/services/eligibilityService.ts line 96（fetchDepartmentNames 函数）
    - frontend/src/components/layout/AppShell.tsx（AppShell props: title / description / children）
    - **frontend/src/index.css**（B-5：核实 :root 实际可用 token 清单 line 5-44）
    - UI-SPEC §5（page layout 整体）
    - UI-SPEC §7（Section 2 复用契约 + onComplete 联动）
    - UI-SPEC §8（Section 3 表格 7 列 + filter + 分页）
    - UI-SPEC §10.3（W-1：toast 文案分支表）
    - UI-SPEC §13（zh-CN 文案）
    - frontend/src/services/performanceService.ts（task 1）
    - frontend/src/utils/toast.ts（task 1 sub-step 1.4 W-4）
    - frontend/src/components/performance/TierDistributionPanel.tsx（task 2）
  </read_first>
  <action>
1. 新建 `frontend/src/components/performance/PerformanceRecordsFilters.tsx`：
   - props: `{ year: string; setYear: (v: string) => void; department: string; setDepartment: (v: string) => void; availableYears: number[]; departments: string[] }`
   - JSX：flex 容器 + 两个 `<select className="toolbar-input">` 严格按 UI-SPEC §8.1
     - 第一个：年份 select，包含「全部年份」option + availableYears.map -> `{y} 年`
     - 第二个：部门 select，包含「全部部门」option + departments.map -> `{d}`
   - 暂不实现搜索框（D-14 / UI-SPEC §8.1）

2. 新建 `frontend/src/components/performance/PerformanceRecordsTable.tsx`：
   - props: `{ items: PerformanceRecordItem[]; loading: boolean; total: number; page: number; pageSize: number; totalPages: number; onPageChange: (p: number) => void }`
   - JSX：
     - `<div className="table-shell">` + `<table className="table-lite">`
     - `<caption className="sr-only">绩效记录列表（共 {total} 条）</caption>`
     - `<thead>` 7 列（按 UI-SPEC §8.2）：员工工号 / 姓名 / 年份 / 绩效等级 / 部门快照 / 来源 / 录入时间
     - `<tbody>`：
       - loading=true → 5 行 skeleton（每行 7 td 内 h-3 灰色 div）
       - loading=false items.length===0 → 1 行 colspan=7 「暂无数据」
       - 否则 items.map((r) => `<tr>`) 7 列，按规则渲染：
         - 工号：`<span style={{ fontVariantNumeric: 'tabular-nums' }}>{r.employee_no}</span>`
         - 姓名：r.employee_name
         - 年份：`{r.year} 年`
         - 绩效等级：`<span className="status-pill">` 背景按 grade A/B/C/D/E 不同（A var(--color-success-bg) text var(--color-success), B var(--color-bg-subtle) text var(--color-ink), C/D/E var(--color-warning-bg) text var(--color-warning)）
         - 部门快照：r.department_snapshot ?? `<span style={{ color: 'var(--color-placeholder)' }}>—</span>`
         - **B-5 修复 — 来源 chip 颜色映射表**（必须只用 :root 段实际定义的 token，不可用未定义 token）：
           - `manual` → 「手动」灰底：`<span className="status-pill" style={{ background: 'var(--color-bg-subtle)', color: 'var(--color-steel)' }}>手动</span>`
           - `excel` → 「导入」蓝底：`<span className="status-pill" style={{ background: 'var(--color-primary-light)', color: 'var(--color-primary)' }}>导入</span>` （--color-primary-light 已存在 line 15）
           - `feishu` → 「飞书」紫底：`<span className="status-pill" style={{ background: 'var(--color-violet-bg)', color: 'var(--color-violet)' }}>飞书</span>` （--color-violet-bg / --color-violet 已存在 line 41-43，Phase 31 D-07 引入）
         - 录入时间：Intl.DateTimeFormat('zh-CN', { year:'numeric', month:'2-digit', day:'2-digit', hour:'2-digit', minute:'2-digit' }).format(new Date(r.created_at))
     - 表格底部分页器：UI-SPEC §8.3 完整 JSX（共 N 条 + 第 X/Y 页 + 上下页按钮 disabled 控制）

3. 改写 `frontend/src/pages/PerformanceManagementPage.tsx`（替换 task 1 stub）：
   - import 全部依赖（AppShell / TierDistributionPanel / PerformanceRecordsTable / PerformanceRecordsFilters / ExcelImportPanel / performanceService 含 getAvailableYears / fetchDepartmentNames / showToast / 类型）
   - useState：
     - `selectedYear: number`（默认 new Date().getFullYear()）
     - `availableYears: number[]`
     - `departments: string[]`
     - `filterYear: string` / `filterDepartment: string`
     - `tableState: { items: PerformanceRecordItem[]; loading: boolean; total: number; page: number; pageSize: 50; totalPages: number }`
     - `tierRefreshKey: number`（每次 import/重算后 +1，触发 TierDistributionPanel 重新拉 summary）
   - **B-3 修复（强制）**：useEffect mount 时改用 `getAvailableYears()` 替换原「拉 200 条 records 凑 distinct」hack：
     ```tsx
     useEffect(() => {
       getAvailableYears()
         .then((years) => {
           // B-3 fallback 链：返回数组长度 0 → [当前年]
           setAvailableYears(years.length > 0 ? years : [new Date().getFullYear()]);
         })
         .catch(() => {
           // 网络错误兜底：仍使用当前年
           setAvailableYears([new Date().getFullYear()]);
         });
     }, []);
     ```
   - useEffect 2: mount 时拉 fetchDepartmentNames + initial records
   - useEffect 3: filterYear/filterDepartment/page 变化时重拉 records
   - 函数 `loadRecords()`：setLoading(true) → getPerformanceRecords(filter) → setItems + total + totalPages → setLoading(false)
   - **W-1 修复（强制）**：函数 `handleImportComplete(result: ConfirmResponse)`：根据 `result.tier_recompute_status` 分支 toast：
     ```tsx
     function handleImportComplete(result: ConfirmResponse) {
       const inserted = result.inserted ?? 0;
       const updated = result.updated ?? 0;
       const total = inserted + updated;
       const status = result.tier_recompute_status;
       if (status === 'completed') {
         showToast(`导入完成（${total} 条），档次已刷新`, 'success');
       } else if (status === 'in_progress') {
         showToast(`导入完成（${total} 条），档次正在后台重算…`, 'info');
       } else if (status === 'busy_skipped') {
         showToast(`导入完成（${total} 条），系统繁忙后续自动重算`, 'warning');
       } else if (status === 'failed') {
         showToast(`导入完成（${total} 条）但档次重算失败，请手动点击『重算档次』`, 'error');
       } else {
         // null / 'skipped' / 非 performance_grades 路径
         showToast(`导入成功：${inserted} 条新增 / ${updated} 条更新`, 'success');
       }
       loadRecords();
       setTierRefreshKey((k) => k + 1);
     }
     ```
   - 函数 `handleRecomputed()`：setTierRefreshKey + 1（让 panel 重拉 summary）
   - JSX：
     ```tsx
     <AppShell title="绩效管理" description="HR 端：导入绩效记录、查看档次分布、手动触发档次重算">
       <div className="space-y-4">
         <section className="surface">
           <TierDistributionPanel
             key={tierRefreshKey}
             year={selectedYear}
             availableYears={availableYears}
             onYearChange={setSelectedYear}
             onRecomputed={handleRecomputed}
           />
         </section>
         <section className="surface">
           <div className="section-head"><h3 className="section-title">绩效记录导入</h3></div>
           <ExcelImportPanel
             importType="performance_grades"
             label="绩效等级"
             onComplete={handleImportComplete}
           />
         </section>
         <section className="surface">
           <div className="section-head"><h3 className="section-title">绩效记录</h3></div>
           <PerformanceRecordsFilters
             year={filterYear} setYear={(v) => { setFilterYear(v); setPage(1); }}
             department={filterDepartment} setDepartment={(v) => { setFilterDepartment(v); setPage(1); }}
             availableYears={availableYears} departments={departments}
           />
           <PerformanceRecordsTable
             items={tableState.items}
             loading={tableState.loading}
             total={tableState.total}
             page={tableState.page}
             pageSize={tableState.pageSize}
             totalPages={tableState.totalPages}
             onPageChange={setPage}
           />
         </section>
       </div>
     </AppShell>
     ```
   - export named `PerformanceManagementPage`（与 App.tsx import 匹配）

4. 完整 lint + build 验收：
   ```
   cd frontend && npm run lint && npm run build
   ```
   两条命令退出码均 0。

5. 浏览器人工冒烟（提示 executor 在 SUMMARY 内记录）：
   - admin 登录 → 侧边栏看到「绩效管理」 → 点击进入 /performance
   - 上半部 ECharts 堆叠条渲染（即使后端无数据也应显示空状态卡片）
   - 中部 ExcelImportPanel 渲染（idle 态）
   - 下半部表格表头 7 列正确
   - employee 登录 → 菜单不显示「绩效管理」 + 直访 /performance → 后端 403（前端目前 layout 仍显示 AppShell，提示 executor 后续可加客户端 guard，本期不做）

**禁止**：
- 在 PerformanceManagementPage 内做 ECharts/Recharts/lucide-react import
- 在 ExcelImportPanel 之外另写一套导入 UI
- 在 records 列表里展示 employee_id（UUID）— 仅展示 employee_no + employee_name
- **B-5 反例**：用未在 :root 定义的 token，例如旧 plan 写「manual → 灰底」时若用 `var(--color-grey-100)` 是错的（不存在），必须用 `var(--color-bg-subtle)` 等真实 token
- **B-3 反例**：恢复「拉 200 条 records 凑 distinct」hack（必须用 getAvailableYears）
- **W-1 反例**：单一固定 toast 文案（必须分支 5 种状态）
- **N-3 反例**：在表格 `<tr>` 加 onClick 跳详情 / 在 PerformanceManagementPage 写 me/tier 调用 / 加多年趋势 chart
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust/frontend &amp;&amp; npm run lint &amp;&amp; npm run build 2>&amp;1 | tail -10 &amp;&amp; grep -E "manual|excel|feishu" frontend/src/components/performance/PerformanceRecordsTable.tsx | head -5 &amp;&amp; grep "getAvailableYears" frontend/src/pages/PerformanceManagementPage.tsx &amp;&amp; grep "tier_recompute_status" frontend/src/pages/PerformanceManagementPage.tsx</automated>
  </verify>
  <done>
    - npm run lint 退出码 0
    - npm run build 退出码 0
    - PerformanceManagementPage.tsx 含 3 个 section 与正确顺序（分布 -> 导入 -> 列表）
    - ExcelImportPanel importType="performance_grades" 在 PerformanceManagementPage 中命中
    - PerformanceRecordsTable 7 列表头与 7 列 td 渲染
    - **B-5 验证**：`grep -E "manual|excel|feishu" frontend/src/components/performance/PerformanceRecordsTable.tsx` 命中 ≥ 3 行；所有 chip color/background 都用 :root 已定义的 token（手动 grep 检查无 var(--color-xxxxx) 引用未定义 token）
    - **B-3 验证**：`grep "getAvailableYears" frontend/src/pages/PerformanceManagementPage.tsx` 命中（不是「拉 200 条 records 凑 distinct」hack）
    - **W-1 验证**：`grep "tier_recompute_status" frontend/src/pages/PerformanceManagementPage.tsx` 命中分支判断
    - 分页器在 totalPages > 1 时上一页/下一页按钮可用
    - tierRefreshKey 在 handleImportComplete + handleRecomputed 中 +1，触发 panel 重拉
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器（admin/hrbp 登录态）-> 后端 API | JWT 已由 axios interceptor 自动附加；后端 require_roles 兜底 |
| roleAccess.ts 菜单可见性 | 客户端隐藏，URL 直访由后端 403；不依赖前端做安全决策 |
| ECharts 渲染 user-controlled JSON 数据 | tiers_count / actual_distribution 来自后端聚合，无 PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-FE-01 | Elevation of Privilege | URL 直访 /performance（employee/manager） | mitigate | roleAccess.ts 隐藏菜单 + App.tsx ProtectedRoute allowedRoles=['admin', 'hrbp'] + 后端 require_roles 三重防护 |
| T-34-FE-02 | Information Disclosure | tier-summary 客户端展示 | accept | 仅展示聚合数字（tiers_count/actual_distribution），无 employee_id 明细 |
| T-34-FE-03 | Tampering | 客户端伪造 recompute 调用 | mitigate | 后端 require_roles + 行锁防并发；前端按钮 disabled 仅是 UX 层防误触 |
| T-34-FE-04 | Denial of Service | 重算按钮反复点击 | mitigate | isRecomputing state 控制 disabled；409 后强制 5s 等待 |
| T-34-FE-05 | XSS via department_snapshot | 表格渲染 | mitigate | React 默认 escape；不使用 dangerouslySetInnerHTML |
| T-34-FE-06 | Availability | 多次 toast 触发卡浏览器 | mitigate | W-4：utils/toast.ts 单实例 helper（cancel 旧 toast 再显示新 toast） |
</threat_model>

<verification>
- `cd frontend && npm run lint` 退出码 0
- `cd frontend && npm run build` 退出码 0
- `grep -n "import.*Recharts" frontend/src` 无输出（未引入 Recharts）
- `grep -n "lucide-react" frontend/src` 无输出（未引入 lucide-react）
- `grep -n "RefreshIcon\|WarningIcon\|ChartIcon" frontend/src/components/icons/NavIcons.tsx` 命中 3 处定义
- **B-4**：`grep "style?: React.CSSProperties" frontend/src/components/icons/NavIcons.tsx` 命中 1 行；svg() 内 `style={{ flexShrink: 0, ...props.style }}` 命中
- **B-3**：`grep "getAvailableYears" frontend/src/services/performanceService.ts frontend/src/pages/PerformanceManagementPage.tsx` 各命中至少 1 行；`grep "AvailableYearsResponse" frontend/src/types/api.ts` 命中
- **B-5**：`grep -E "manual|excel|feishu" frontend/src/components/performance/PerformanceRecordsTable.tsx | head -5` 命中 ≥ 3 行；所有 chip 用真实存在的 :root token
- **W-1**：`grep "tier_recompute_status" frontend/src/pages/PerformanceManagementPage.tsx` 命中分支
- **W-4**：`frontend/src/utils/toast.ts` 文件存在，含 showToast；`grep "showToast" frontend/src/components/performance/TierDistributionPanel.tsx frontend/src/pages/PerformanceManagementPage.tsx` 命中（替代 alert）
- `grep -n "performance_grades" frontend/src/pages/PerformanceManagementPage.tsx` 命中 ExcelImportPanel importType
- `grep -n "/performance" frontend/src/utils/roleAccess.ts` 命中 2 处（admin + hrbp）
- `grep -n "PerformanceManagementPage" frontend/src/App.tsx` 命中 import + Route
- 浏览器冒烟（SUMMARY 内记录）：admin/hrbp 看到菜单 + 进入 /performance 看 3 section；employee/manager 菜单不可见
</verification>

<success_criteria>
- ROADMAP SC-1: HR + admin 在导航菜单看到「绩效管理」入口；employee/manager 看不到 ✓（roleAccess + ProtectedRoute）
- ROADMAP SC-2: HR 上传 Excel 后 Preview + diff（ExcelImportPanel 7-state 已支持）+ 落库后 tier 视图刷新（handleImportComplete -> tierRefreshKey++）+ W-1 toast 文案根据 tier_recompute_status 分支 ✓
- ROADMAP SC-3: HR 点击「重算档次」按钮触发重算 + UI 显示重算完成时间戳（recomputeTiers + computed_at zh-CN 格式化） ✓
- ROADMAP SC-4: 列表显示 department_snapshot 列；NULL 显示「—」（PerformanceRecordsTable 渲染规则） ✓
- ROADMAP SC-5: /records 与 /tier-summary 数据来自同一后端 Service，前端不做二次计算 ✓
- B-3：年份 dropdown 数据源是后端 /available-years，不是「拉 200 条 records 凑 distinct」hack
- B-4：NavIcons IconProps + svg() 已支持 style prop，UI-SPEC §6.2/§9 的 `<WarningIcon style={{...}}/>` / `<ChartIcon style={{...}}/>` 用法通过 tsc
- B-5：来源 chip 颜色 100% 用 :root 已定义的 token（无未定义 var 引用）
- W-1：handleImportComplete 5 个分支 toast 文案差异化
- W-2：Task 1 改 7 文件按 sub-step 分批 commit（Task 1.1 → 1.7 + 验证 1.8）
- W-4：toast 单实例 helper 防多 alert 卡浏览器
- N-3：plan top-level banner 明确「行点击跳详情 / me/tier / 多年趋势」均为 Phase 35/36 范围
- 全部 13 个文件交付（含 W-4 新增 utils/toast.ts）；零新增第三方依赖；npm lint+build 双 0
</success_criteria>

<output>
After completion, create `.planning/phases/34-performance-management-service-and-api/34-04-SUMMARY.md` with:
- `tech-stack` 段：复用 echarts 6.0.0 + echarts-for-react 3.0.6；零新增 npm 包；NavIcons 内联 SVG 新增 3 个图标 + IconProps 扩 style（B-4）；新增 utils/toast.ts 单实例 toast helper（W-4）
- `key-files` 段：created 8 + modified 5 = 13 文件清单
- `key-decisions` 段：UI-SPEC §0 反向勘误的具体落地（ECharts 替 Recharts / 内联 SVG 替 lucide-react）；toast 临时用 alert 兜底（标记 follow-up）；B-3/B-4/B-5/W-1/W-4 修复
- `Phase 34 整体收尾` 段：列出后端 5 端点（含 /available-years） + 前端 13 文件全部交付，5 个 ROADMAP SC 全部满足
- `requirements-completed: [PERF-01, PERF-02, PERF-05, PERF-08]`（4 个 PERF 需求 UI 层全部 ready）
- 浏览器冒烟测试结果（admin/hrbp 看到菜单 + 进入页面 + 3 section 渲染 + employee/manager 不可见）
</output>
</output>
