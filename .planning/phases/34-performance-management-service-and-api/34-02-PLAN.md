---
phase: 34-performance-management-service-and-api
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/core/config.py
  - .env.example
  - backend/app/services/tier_cache.py
  - backend/app/services/exceptions.py
  - backend/tests/test_services/test_tier_cache.py
autonomous: true
requirements: [PERF-05]
gap_closure: false

must_haves:
  truths:
    - "Settings 新增 `performance_tier_redis_prefix` 字段（默认 `tier_summary`）+ `performance_tier_redis_ttl_seconds`（默认 86400）+ `performance_tier_recompute_timeout_seconds`（默认 5），全部可被环境变量覆盖（D-02 / D-03）"
    - ".env.example 包含上述 3 个新键的注释与默认值"
    - "新建 `backend/app/services/tier_cache.py`，导出 `TierCache` 类，提供 4 个方法：`get_cached(year)` / `set_cached(year, payload)` / `invalidate(year)` / `cache_key(year)`；构造函数注入 `redis.Redis` 实例 + `Settings`，便于单测 mock"
    - "新建 `backend/app/services/exceptions.py`（如不存在），定义 `TierRecomputeBusyError` + `TierRecomputeFailedError`，继承自内置 `Exception`（D-05/D-06）"
    - "TierCache 在 Redis 不可达时优雅降级（返回 None for get / 静默 noop for set/invalidate + warning log），不抛 ConnectionError 给上层"
  artifacts:
    - path: backend/app/core/config.py
      provides: "3 个新 Settings 字段"
      contains: "performance_tier_redis_prefix"
    - path: backend/app/services/tier_cache.py
      provides: "TierCache 类（Redis 读写 + 优雅降级 + cache key 生成）"
      contains: "class TierCache"
    - path: backend/app/services/exceptions.py
      provides: "TierRecomputeBusyError / TierRecomputeFailedError 自定义异常"
      contains: "class TierRecomputeBusyError"
    - path: backend/tests/test_services/test_tier_cache.py
      provides: "TierCache 单测（>=8 cases，含 Redis 不可达降级路径）"
      min_cases: 8
  key_links:
    - from: "backend/app/services/tier_cache.py"
      to: "backend/app/core/config.Settings"
      via: "constructor injection"
      pattern: "settings.performance_tier_redis_prefix"
    - from: "backend/app/services/tier_cache.py"
      to: "redis.Redis"
      via: "constructor injection（不直接 import get_redis，便于 mock）"
      pattern: "redis_client.get|redis_client.set|redis_client.delete"
---

<objective>
为 Phase 34-03 的 Service 层提供两个独立基础设施：① 配置化的 Redis cache 参数（prefix / TTL / recompute timeout，全部 env 可覆盖）；② 一个细粒度封装的 `TierCache` 类，把 Redis read/write/invalidate 抽离为 4 个方法 + 优雅降级路径，让 PerformanceService 不需要再处理 Redis 异常；③ 两个自定义业务异常类（TierRecomputeBusyError / TierRecomputeFailedError），让 Service 层抛业务语义异常、API 层 catch 并转 HTTP 状态码（D-05/D-06）。

Why this is independent of 34-01: 本 plan 修改 `core/config.py` + `.env.example` + 新建 services 子模块；34-01 只动 models + migrations + tests/test_models。零文件交集 → 同 Wave 真并行。

Why now: 34-03 的 Service / API 用到这些设施时已就位；如果合并到 34-03，会让 34-03 任务数膨胀超过 3 个，违反「2-3 task/plan」预算。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-performance-management-service-and-api/34-CONTEXT.md
@CLAUDE.md
@backend/app/core/config.py
@backend/app/core/redis.py

**关键决策（必须严格落地）：**
- D-02：Redis cache key `tier_summary:{year}`，TTL 86400 秒（24h），写穿透
- D-03：同步重算超时 5 秒
- D-05/D-06：业务异常（busy / failed）必须走自定义异常类，不能用 raw `Exception` 或 `HTTPException`（service 层不该 import HTTPException）
- 33-01-SUMMARY.md：Settings 注入用 `@lru_cache get_settings()` + `Depends(get_app_settings)`

<interfaces>
<!-- 现有 Settings（执行人需追加字段，不动其他）：-->
```python
# backend/app/core/config.py 当前 line 79-86 段落
# Eligibility thresholds (D-04)
eligibility_min_tenure_months: int = 6
eligibility_min_adjustment_interval_months: int = 6
eligibility_performance_fail_grades: str = 'C,D,E'
eligibility_max_non_statutory_leave_days: float = 30.0

# Performance tier engine threshold (D-10 — Phase 33)
performance_tier_min_sample_size: int = 50
```

<!-- 现有 Redis helper（直接复用，不修改）：-->
```python
# backend/app/core/redis.py
def get_redis() -> redis.Redis:
    """Lazily-initialized Redis singleton; raises ConnectionError if unreachable."""
```

<!-- 注意：Settings 已注入用 @lru_cache + get_settings()；新字段 env 大小写不敏感 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Settings 新增 3 字段 + .env.example 文档化 + exceptions 模块</name>
  <files>
    backend/app/core/config.py
    .env.example
    backend/app/services/exceptions.py
  </files>
  <read_first>
    - backend/app/core/config.py（确认 Phase 33 已加 `performance_tier_min_sample_size: int = 50`，本 plan 在它之后追加 3 个新字段）
    - .env.example（找到「Performance tier engine threshold」段，在它之后加新段「Performance tier cache & recompute」）
    - 34-CONTEXT.md §decisions D-02 / D-03（TTL 86400 / 同步重算 5s）
    - backend/app/services/（确认是否已有 exceptions.py；若存在，追加；不存在则新建）
  </read_first>
  <action>
1. 修改 `backend/app/core/config.py`：
   - 在 `performance_tier_min_sample_size: int = 50` 之后插入：
     ```python
     # Performance tier cache & recompute (Phase 34 D-02 / D-03)
     performance_tier_redis_prefix: str = 'tier_summary'
     performance_tier_redis_ttl_seconds: int = 86_400
     performance_tier_recompute_timeout_seconds: int = 5
     ```
   - 不动其他字段、Validators、`@lru_cache` `get_settings()`

2. 修改 `.env.example`：
   - 在 Phase 33 引入的「ELIGIBILITY / PERFORMANCE THRESHOLDS」段尾追加：
     ```
     # Performance tier cache & recompute (Phase 34 D-02 / D-03)
     # Redis key prefix for tier summary cache (final key = "{prefix}:{year}")
     PERFORMANCE_TIER_REDIS_PREFIX=tier_summary
     # Cache TTL in seconds; 86400 = 24 hours (D-02)
     PERFORMANCE_TIER_REDIS_TTL_SECONDS=86400
     # Synchronous recompute hard timeout for import-confirm path (D-03);
     # if exceeded, confirm returns 202 with tier_recompute='in_progress'.
     PERFORMANCE_TIER_RECOMPUTE_TIMEOUT_SECONDS=5
     ```

3. 新建 `backend/app/services/exceptions.py`（如已存在则只追加两个类）：
   ```python
   from __future__ import annotations


   class TierRecomputeBusyError(Exception):
       """Phase 34 D-05/D-06：另一个事务持有该 year 的 snapshot 行锁（FOR UPDATE NOWAIT 失败）。

       API 层 catch → 409 Conflict + retry_after_seconds=5。
       """

       def __init__(self, year: int, message: str | None = None) -> None:
           self.year = year
           super().__init__(message or f'Tier recompute busy for year {year}')


   class TierRecomputeFailedError(Exception):
       """Phase 34 D-04：重算执行失败（Engine 异常 / 数据库写入失败）。

       Service 层不阻塞调用方（import 落库已成功，旧快照保留）；上层根据需要决定是
       422（手动调 recompute）还是 silently log（import 自动重算路径）。
       """

       def __init__(self, year: int, cause: str) -> None:
           self.year = year
           self.cause = cause
           super().__init__(f'Tier recompute failed for year {year}: {cause}')
   ```

**禁止**：
- 在 exceptions.py 顶部 import HTTPException 或 fastapi 任何东西（service 层与 web 层解耦）
- 修改 `backend/app/core/redis.py`（保持原 helper 不变；TierCache 通过 ctor 注入 client，便于单测）
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust && .venv/bin/python -c "from backend.app.core.config import Settings; s = Settings(); assert s.performance_tier_redis_prefix == 'tier_summary'; assert s.performance_tier_redis_ttl_seconds == 86400; assert s.performance_tier_recompute_timeout_seconds == 5; from backend.app.services.exceptions import TierRecomputeBusyError, TierRecomputeFailedError; e = TierRecomputeBusyError(2026); assert e.year == 2026; print('OK')"</automated>
  </verify>
  <done>
    - `Settings()` 含 3 个新字段，默认值正确
    - .env.example 含 3 个新键的注释与默认值
    - exceptions.py 导出两个自定义异常类，可独立 import
    - TierRecomputeBusyError 实例 `.year` 属性可用
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TierCache 类实现 + 单测</name>
  <files>
    backend/app/services/tier_cache.py
    backend/tests/test_services/test_tier_cache.py
  </files>
  <read_first>
    - backend/app/core/redis.py（redis client 接口：get/set/delete + redis.exceptions.ConnectionError）
    - backend/app/core/config.py（task 1 后的新字段）
    - 34-CONTEXT.md §decisions D-02（cache key + TTL + 写穿透；D-04 `redis.set(key, json, ex=86400)` 模式）
    - backend/tests/conftest.py（确认是否有 redis mock fixture，没有也无所谓 — 用 unittest.mock）
  </read_first>
  <behavior>
    - Test 1: `cache_key(2026)` 返回 `'tier_summary:2026'`（默认 prefix）
    - Test 2: `cache_key(2026)` 当 prefix 设为 `'foo'` 时返回 `'foo:2026'`
    - Test 3: `set_cached(2026, payload_dict)` 调用 mock redis_client.set(key='tier_summary:2026', value=json.dumps(payload), ex=86400)
    - Test 4: `get_cached(2026)` redis 返回 JSON 字符串 → 解析为 dict 返回
    - Test 5: `get_cached(2026)` redis 返回 None → 返回 None
    - Test 6: `invalidate(2026)` 调用 mock redis_client.delete(key='tier_summary:2026')
    - Test 7: Redis ConnectionError on get → TierCache 静默返回 None（不抛异常），并 warning log
    - Test 8: Redis ConnectionError on set → TierCache 静默 noop，不抛异常
    - Test 9: Redis ConnectionError on invalidate → TierCache 静默 noop
    - Test 10: TTL 自定义 — 用 settings(performance_tier_redis_ttl_seconds=10) 构造 TierCache，set_cached 调用 redis.set 时 ex=10
  </behavior>
  <action>
1. 新建 `backend/app/services/tier_cache.py`：
   ```python
   from __future__ import annotations

   import json
   import logging
   from typing import Any

   import redis as redis_lib

   from backend.app.core.config import Settings

   logger = logging.getLogger(__name__)


   class TierCache:
       """Phase 34 D-02：tier-summary Redis 缓存层（写穿透 + 24h TTL）。

       构造函数注入 redis.Redis 与 Settings，便于单测 mock；
       Redis 不可达时优雅降级（get 返回 None / set/invalidate 静默 noop + warn log），
       不向上层抛 ConnectionError —— 让上层永远走表层 fallback。
       """

       def __init__(
           self,
           redis_client: redis_lib.Redis | None,
           settings: Settings,
       ) -> None:
           self._redis = redis_client
           self._settings = settings

       def cache_key(self, year: int) -> str:
           return f'{self._settings.performance_tier_redis_prefix}:{year}'

       def get_cached(self, year: int) -> dict[str, Any] | None:
           if self._redis is None:
               return None
           try:
               raw = self._redis.get(self.cache_key(year))
           except redis_lib.RedisError as exc:
               logger.warning('Tier cache get failed for year %s: %s', year, exc)
               return None
           if raw is None:
               return None
           try:
               return json.loads(raw)
           except (TypeError, ValueError) as exc:
               logger.warning('Tier cache decode failed for year %s: %s', year, exc)
               return None

       def set_cached(self, year: int, payload: dict[str, Any]) -> None:
           if self._redis is None:
               return
           try:
               self._redis.set(
                   self.cache_key(year),
                   json.dumps(payload, default=str),
                   ex=self._settings.performance_tier_redis_ttl_seconds,
               )
           except redis_lib.RedisError as exc:
               logger.warning('Tier cache set failed for year %s: %s', year, exc)

       def invalidate(self, year: int) -> None:
           if self._redis is None:
               return
           try:
               self._redis.delete(self.cache_key(year))
           except redis_lib.RedisError as exc:
               logger.warning('Tier cache invalidate failed for year %s: %s', year, exc)
   ```

2. 新建 `backend/tests/test_services/test_tier_cache.py`：
   ```python
   from __future__ import annotations

   import json
   from unittest.mock import MagicMock

   import pytest
   from redis.exceptions import ConnectionError as RedisConnectionError

   from backend.app.core.config import Settings
   from backend.app.services.tier_cache import TierCache


   @pytest.fixture
   def settings() -> Settings:
       return Settings()


   def _mock_redis() -> MagicMock:
       m = MagicMock()
       m.get.return_value = None
       m.set.return_value = True
       m.delete.return_value = 1
       return m


   def test_cache_key_default_prefix(settings: Settings) -> None:
       cache = TierCache(redis_client=_mock_redis(), settings=settings)
       assert cache.cache_key(2026) == 'tier_summary:2026'


   def test_cache_key_custom_prefix() -> None:
       custom = Settings(performance_tier_redis_prefix='foo')
       cache = TierCache(redis_client=_mock_redis(), settings=custom)
       assert cache.cache_key(2026) == 'foo:2026'


   def test_set_cached_uses_default_ttl(settings: Settings) -> None:
       redis_mock = _mock_redis()
       cache = TierCache(redis_client=redis_mock, settings=settings)
       payload = {'year': 2026, 'sample_size': 100}
       cache.set_cached(2026, payload)
       redis_mock.set.assert_called_once_with(
           'tier_summary:2026',
           json.dumps(payload, default=str),
           ex=86_400,
       )


   def test_set_cached_uses_custom_ttl() -> None:
       custom = Settings(performance_tier_redis_ttl_seconds=10)
       redis_mock = _mock_redis()
       cache = TierCache(redis_client=redis_mock, settings=custom)
       cache.set_cached(2026, {'a': 1})
       _, kwargs = redis_mock.set.call_args
       assert kwargs['ex'] == 10


   def test_get_cached_returns_decoded_dict(settings: Settings) -> None:
       redis_mock = _mock_redis()
       redis_mock.get.return_value = '{"year": 2026, "n": 99}'
       cache = TierCache(redis_client=redis_mock, settings=settings)
       got = cache.get_cached(2026)
       assert got == {'year': 2026, 'n': 99}


   def test_get_cached_returns_none_on_miss(settings: Settings) -> None:
       redis_mock = _mock_redis()
       redis_mock.get.return_value = None
       cache = TierCache(redis_client=redis_mock, settings=settings)
       assert cache.get_cached(2026) is None


   def test_invalidate_calls_redis_delete(settings: Settings) -> None:
       redis_mock = _mock_redis()
       cache = TierCache(redis_client=redis_mock, settings=settings)
       cache.invalidate(2026)
       redis_mock.delete.assert_called_once_with('tier_summary:2026')


   def test_get_silent_on_redis_unavailable(settings: Settings) -> None:
       redis_mock = MagicMock()
       redis_mock.get.side_effect = RedisConnectionError('boom')
       cache = TierCache(redis_client=redis_mock, settings=settings)
       assert cache.get_cached(2026) is None  # 不抛异常


   def test_set_silent_on_redis_unavailable(settings: Settings) -> None:
       redis_mock = MagicMock()
       redis_mock.set.side_effect = RedisConnectionError('boom')
       cache = TierCache(redis_client=redis_mock, settings=settings)
       cache.set_cached(2026, {'a': 1})  # 不抛异常


   def test_invalidate_silent_on_redis_unavailable(settings: Settings) -> None:
       redis_mock = MagicMock()
       redis_mock.delete.side_effect = RedisConnectionError('boom')
       cache = TierCache(redis_client=redis_mock, settings=settings)
       cache.invalidate(2026)  # 不抛异常


   def test_redis_client_none_returns_none_no_op(settings: Settings) -> None:
       cache = TierCache(redis_client=None, settings=settings)
       assert cache.get_cached(2026) is None
       cache.set_cached(2026, {'a': 1})  # 不抛
       cache.invalidate(2026)  # 不抛
   ```

3. 运行：
   ```bash
   .venv/bin/pytest backend/tests/test_services/test_tier_cache.py -v
   ```
   全部 11 用例通过（10 + 1 None client 兜底）。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust && .venv/bin/pytest backend/tests/test_services/test_tier_cache.py -v --tb=short</automated>
  </verify>
  <done>
    - tier_cache.py 类含 4 个公开方法（cache_key / get_cached / set_cached / invalidate）
    - pytest 11 cases 全绿，含 Redis 不可达 3 条降级路径
    - 静态检查：`grep -n "from fastapi" backend/app/services/tier_cache.py` 无输出（service 层不耦合 web 层）
    - `grep -n "raise " backend/app/services/tier_cache.py` 无输出（优雅降级；零 raise）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings env → Config 对象 | env 文件可被运维改写，但项目已经在用 pydantic-settings 强类型解析（int / str），错误值会 startup-fail |
| Service 层 → Redis | Redis 不可达不应让 HR 操作失败；TierCache 优雅降级到表层 |
| Cache → 缓存数据真实性 | 缓存可能与 DB 不同步（数据陈旧）；D-02 明确写穿透：导入 confirm 后立即 invalidate + 重算后 set |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-CACHE-01 | Denial of Service | Redis 不可达 | mitigate | TierCache 全方法 catch RedisError → 降级（get→None / set/invalidate→noop）+ warn log；上层走表 fallback |
| T-34-CACHE-02 | Tampering | Redis 缓存被旁路写入 | accept | Redis 在 VPC 内，无外部直连；恶意改写需要主机层入侵（超出本 plan 范围） |
| T-34-CACHE-03 | Information Disclosure | tier 缓存内容 | accept | 仅 admin/hrbp 通过 Service 层读取；employee_id UUID 无 PII |
| T-34-CACHE-04 | Repudiation | cache invalidate 静默失败 | accept | 失败仅 warn log；下次 get 仍走表 fallback，最坏 24h TTL 内拿到旧数据；HR 手动重算可强制刷新 |
</threat_model>

<verification>
- `.venv/bin/python -c "from backend.app.core.config import Settings; print(Settings().performance_tier_redis_prefix)"` 输出 `tier_summary`
- `.venv/bin/pytest backend/tests/test_services/test_tier_cache.py -v` 11 cases pass
- `grep -n "from fastapi" backend/app/services/tier_cache.py` 无输出
- `grep -n "raise " backend/app/services/tier_cache.py` 无输出
- `grep -n "TierRecomputeBusyError\|TierRecomputeFailedError" backend/app/services/exceptions.py` 命中 2 处类定义
- 现有 pytest 不退化：`.venv/bin/pytest backend/tests/test_engines/ -x` 通过
</verification>

<success_criteria>
- Settings 含 `performance_tier_redis_prefix` / `performance_tier_redis_ttl_seconds` / `performance_tier_recompute_timeout_seconds` 三字段
- TierCache 类完整封装 Redis 读写 + 优雅降级
- TierRecomputeBusyError / TierRecomputeFailedError 自定义异常可被 Wave 2 Service 直接 import
- 不依赖 34-01；与 34-01 零文件交集；可与 34-01 真并行执行
</success_criteria>

<output>
After completion, create `.planning/phases/34-performance-management-service-and-api/34-02-SUMMARY.md` with:
- `tech-stack` 段：新增 services/tier_cache.py + services/exceptions.py + 3 个 settings 字段
- `key-files` 段：created / modified 文件清单
- `key-decisions` 段：service 层与 fastapi 完全解耦的边界（不 import HTTPException）；Redis 优雅降级而非抛错
- `Phase 34-03 Service 层接口契约` 段：列出 TierCache 4 方法签名 + 两个自定义异常签名，便于 Wave 2 直接消费
- `requirements-completed: [PERF-05]`（部分；Service 层 invalidate + recompute 接线由 34-03 完成）
</output>
