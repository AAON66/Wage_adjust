---
phase: 26-oauth2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/user.py
  - backend/app/core/config.py
  - backend/app/schemas/user.py
  - alembic/versions/add_feishu_open_id_to_users.py
  - .env.example
autonomous: true
requirements:
  - FAUTH-04
must_haves:
  truths:
    - "User 模型包含 feishu_open_id 字段，唯一约束，可为 null"
    - "Settings 包含 feishu_app_id、feishu_app_secret、feishu_redirect_uri 配置项"
    - "Alembic 迁移可成功执行，在 users 表添加 feishu_open_id 列"
    - "UserRead schema 包含 feishu_open_id 字段用于 API 响应"
  artifacts:
    - path: "backend/app/models/user.py"
      provides: "feishu_open_id 字段定义"
      contains: "feishu_open_id"
    - path: "backend/app/core/config.py"
      provides: "飞书 OAuth 配置项"
      contains: "feishu_app_id"
    - path: "alembic/versions/add_feishu_open_id_to_users.py"
      provides: "数据库迁移脚本"
      contains: "feishu_open_id"
  key_links:
    - from: "backend/app/models/user.py"
      to: "alembic migration"
      via: "Alembic autogenerate detects new column"
      pattern: "feishu_open_id"
---

<objective>
为飞书 OAuth2 登录建立数据层和配置基础：User 模型新增 feishu_open_id 字段、Settings 新增飞书 OAuth 配置项、创建 Alembic 迁移脚本、扩展 UserRead schema。

Purpose: 为 Plan 02 的 FeishuOAuthService 和 API 端点提供模型和配置基础设施。
Output: 可执行的 Alembic 迁移 + 扩展的 User 模型 + 飞书 OAuth 配置项 + FeishuCallbackRequest schema
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-oauth2/26-CONTEXT.md
@.planning/phases/26-oauth2/26-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From backend/app/models/user.py (current):
```python
class User(UUIDPrimaryKeyMixin, CreatedAtMixin, Base):
    __tablename__ = "users"
    email: Mapped[str] = mapped_column(String(255), nullable=False, unique=True, index=True)
    hashed_password: Mapped[str] = mapped_column(String(255), nullable=False)
    role: Mapped[str] = mapped_column(String(50), nullable=False, index=True)
    id_card_no: Mapped[Optional[str]] = mapped_column(EncryptedString(256), nullable=True, unique=True, index=True)
    must_change_password: Mapped[bool] = mapped_column(Boolean, nullable=False, default=False, server_default='0')
    token_version: Mapped[int] = mapped_column(Integer, nullable=False, default=0, server_default='0')
    employee_id: Mapped[Optional[str]] = mapped_column(ForeignKey('employees.id'), nullable=True, unique=True, index=True)
```

From backend/app/core/config.py (current feishu config):
```python
feishu_encryption_key: str = ''
```

From backend/app/schemas/user.py (current UserRead):
```python
class UserRead(BaseModel):
    model_config = ConfigDict(from_attributes=True)
    id: str
    email: EmailStr
    role: str
    id_card_no: Optional[str] = None
    must_change_password: bool = False
    employee_id: Optional[str] = None
    employee_name: Optional[str] = None
    employee_no: Optional[str] = None
    departments: List[DepartmentRead] = []
    created_at: datetime
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: User 模型 + Settings 配置 + Schema 扩展</name>
  <files>
    backend/app/models/user.py,
    backend/app/core/config.py,
    backend/app/schemas/user.py,
    .env.example
  </files>
  <read_first>
    backend/app/models/user.py,
    backend/app/core/config.py,
    backend/app/schemas/user.py,
    .env.example
  </read_first>
  <action>
1. 在 `backend/app/models/user.py` 的 User 类中，在 `employee_id` 字段之后添加：
   ```python
   feishu_open_id: Mapped[str | None] = mapped_column(String(255), nullable=True, unique=True, index=True)
   ```
   确保 `from typing import Optional` 已导入（现有代码已有）。

2. 在 `backend/app/core/config.py` 的 Settings 类中，在 `feishu_encryption_key` 行之后添加三个新配置项（per D-10）：
   ```python
   feishu_app_id: str = ''
   feishu_app_secret: str = ''
   feishu_redirect_uri: str = ''
   ```

3. 在 `backend/app/schemas/user.py` 的 UserRead 类中，在 `employee_no` 字段之后添加：
   ```python
   feishu_open_id: Optional[str] = None
   ```

4. 新增 FeishuCallbackRequest schema（在 user.py 文件末尾或在 schemas 目录中新建 feishu.py — 推荐直接加在 user.py 中与认证相关 schema 放一起）：
   ```python
   class FeishuCallbackRequest(BaseModel):
       code: str = Field(min_length=1, max_length=512)
       state: str = Field(min_length=1, max_length=128)
   ```

5. 在 `.env.example` 中，在 `FEISHU_ENCRYPTION_KEY=` 行之后添加：
   ```
   FEISHU_APP_ID=
   FEISHU_APP_SECRET=
   FEISHU_REDIRECT_URI=http://localhost:5174/auth/feishu/callback
   ```
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust && python -c "from backend.app.models.user import User; assert hasattr(User, 'feishu_open_id'); print('Model OK')" && python -c "from backend.app.core.config import Settings; s = Settings(); assert hasattr(s, 'feishu_app_id'); assert hasattr(s, 'feishu_app_secret'); assert hasattr(s, 'feishu_redirect_uri'); print('Config OK')" && python -c "from backend.app.schemas.user import UserRead, FeishuCallbackRequest; print('Schema OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "feishu_open_id" backend/app/models/user.py 返回 0
    - grep -q "unique=True" backend/app/models/user.py 匹配 feishu_open_id 行
    - grep -q "feishu_app_id" backend/app/core/config.py 返回 0
    - grep -q "feishu_app_secret" backend/app/core/config.py 返回 0
    - grep -q "feishu_redirect_uri" backend/app/core/config.py 返回 0
    - grep -q "FeishuCallbackRequest" backend/app/schemas/user.py 返回 0
    - grep -q "feishu_open_id" backend/app/schemas/user.py 返回 0 (在 UserRead 中)
    - grep -q "FEISHU_APP_ID" .env.example 返回 0
  </acceptance_criteria>
  <done>User 模型包含 feishu_open_id 唯一约束字段，Settings 包含 3 个飞书 OAuth 配置项，UserRead 和 FeishuCallbackRequest schema 就绪，.env.example 更新</done>
</task>

<task type="auto">
  <name>Task 2: Alembic 迁移脚本</name>
  <files>
    alembic/versions/add_feishu_open_id_to_users.py
  </files>
  <read_first>
    backend/app/models/user.py,
    alembic.ini,
    alembic/env.py
  </read_first>
  <action>
生成 Alembic 迁移脚本添加 feishu_open_id 列到 users 表（per D-07）。

运行命令：
```bash
cd /Users/mac/PycharmProjects/Wage_adjust
source .venv/bin/activate
alembic revision --autogenerate -m "add_feishu_open_id_to_users"
```

如果 autogenerate 不可用或生成不正确，手动创建迁移脚本：

```python
"""add_feishu_open_id_to_users

Revision ID: [auto]
Revises: [auto]
Create Date: [auto]
"""
from __future__ import annotations

from alembic import op
import sqlalchemy as sa

# revision identifiers
revision = '[auto]'
down_revision = '[auto]'
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.add_column('users', sa.Column('feishu_open_id', sa.String(255), nullable=True))
    op.create_unique_constraint('uq_users_feishu_open_id', 'users', ['feishu_open_id'])
    op.create_index('ix_users_feishu_open_id', 'users', ['feishu_open_id'])


def downgrade() -> None:
    op.drop_index('ix_users_feishu_open_id', table_name='users')
    op.drop_constraint('uq_users_feishu_open_id', 'users', type_='unique')
    op.drop_column('users', 'feishu_open_id')
```

然后执行迁移：
```bash
alembic upgrade head
```

验证迁移成功后，确认 users 表中存在 feishu_open_id 列。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust && source .venv/bin/activate && alembic upgrade head && python -c "
from backend.app.core.database import engine
from sqlalchemy import inspect
inspector = inspect(engine)
columns = [c['name'] for c in inspector.get_columns('users')]
assert 'feishu_open_id' in columns, f'feishu_open_id not in {columns}'
print('Migration OK: feishu_open_id column exists')
"</automated>
  </verify>
  <acceptance_criteria>
    - alembic upgrade head 成功执行无报错
    - users 表包含 feishu_open_id 列
    - feishu_open_id 列为 nullable
    - feishu_open_id 列有 unique constraint
    - 迁移文件存在于 alembic/versions/ 目录下
  </acceptance_criteria>
  <done>Alembic 迁移脚本已创建并成功执行，users 表包含 feishu_open_id 列（nullable, unique）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 配置文件 -> 运行时 | FEISHU_APP_SECRET 从 .env 加载，需确保 .env 不被提交到版本控制 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Information Disclosure | .env.example | mitigate | .env.example 只包含空值模板，真实 secret 在 .env 中（已在 .gitignore） |
| T-26-02 | Information Disclosure | config.py | accept | feishu_app_secret 以 Settings 字段存在内存中，与现有 jwt_secret_key 模式一致，风险等级相同 |
</threat_model>

<verification>
- User 模型导入成功且包含 feishu_open_id 字段
- Settings 成功加载三个新飞书配置项
- Alembic 迁移成功执行
- 现有测试不受影响：`python -m pytest backend/tests/ -x --timeout=30`
</verification>

<success_criteria>
- feishu_open_id 字段在 User 模型中定义，nullable=True, unique=True
- Settings 包含 feishu_app_id, feishu_app_secret, feishu_redirect_uri
- UserRead schema 包含 feishu_open_id
- FeishuCallbackRequest schema 包含 code 和 state 字段
- Alembic 迁移可成功执行
- 后端服务可正常启动
</success_criteria>

<output>
After completion, create `.planning/phases/26-oauth2/26-01-SUMMARY.md`
</output>
