---
phase: 27.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/core/config.py
  - .env.example
  - backend/app/services/feishu_oauth_service.py
  - backend/tests/test_services/test_feishu_oauth_service.py
autonomous: true
requirements:
  - FAUTH-06
  - FAUTH-07
  - FAUTH-08
  - FAUTH-09

must_haves:
  truths:
    - "FeishuOAuthService 可以按 purpose 参数生成两套 authorize_url 且 state 键前缀互斥"
    - "FeishuOAuthService.handle_bind_callback 校验 employee_no 一致并在 open_id 冲突时抛 HTTP 409"
    - "FeishuOAuthService.handle_unbind 把 feishu_open_id 清空并提交"
    - "Settings.feishu_bind_redirect_uri 可从 .env 加载，空值时 bind 路径返回 503"
    - "pytest 覆盖 bind 成功 / open_id 冲突 / employee_no 不一致 / 未绑定员工 / unbind 成功 五个场景"
  artifacts:
    - path: "backend/app/core/config.py"
      provides: "feishu_bind_redirect_uri Settings 字段"
      contains: "feishu_bind_redirect_uri"
    - path: ".env.example"
      provides: "FEISHU_BIND_REDIRECT_URI 环境变量示例"
      contains: "FEISHU_BIND_REDIRECT_URI"
    - path: "backend/app/services/feishu_oauth_service.py"
      provides: "handle_bind_callback / handle_unbind / 扩展 generate_authorize_url(purpose=...)"
      exports: ["FeishuOAuthService", "handle_bind_callback", "handle_unbind"]
    - path: "backend/tests/test_services/test_feishu_oauth_service.py"
      provides: "新增 5 个 bind/unbind 场景测试"
      contains: "test_bind"
  key_links:
    - from: "backend/app/services/feishu_oauth_service.py"
      to: "backend/app/models/user.py"
      via: "select User WHERE feishu_open_id OR employee_id"
      pattern: "feishu_open_id|employee_id"
    - from: "backend/app/services/feishu_oauth_service.py"
      to: "backend/app/core/config.py"
      via: "self._settings.feishu_bind_redirect_uri"
      pattern: "feishu_bind_redirect_uri"
    - from: "handle_bind_callback"
      to: "_exchange_code_for_token / _get_user_info / _validate_state / _check_code_replay"
      via: "服务内部复用（purpose='bind' 使用 bind state key 前缀 + bind redirect_uri）"
      pattern: "feishu_oauth_state:bind:"
---

<objective>
扩展 `FeishuOAuthService` 以支持登录后的「飞书账号绑定 / 解绑」能力，完整覆盖 Phase 27.1 CONTEXT 的 D-01/D-02/D-05/D-06/D-07。
本 plan 只改后端 service 与配置层，不触碰 API 路由（Plan 02 负责）与前端（Plan 03 负责）。

Purpose: 为设置页绑定/解绑流程提供纯业务服务方法与隔离的 OAuth state 路径，保证登录 state 与绑定 state 互不串扰（D-02），并校验绑定安全边界（D-06 employee_no 一致 + D-05 open_id 冲突 409 + D-07 未绑定员工账号拒绝）。
Output: 一个可独立 pytest 通过的服务层改动 + Settings 配置字段 + .env.example 注释；AuditLog 扩展放在 Plan 02（与 API handler 同提交），此 Plan 不写 AuditLog（保持 service 纯业务，遵循 CONTEXT code_context.Established Patterns「审计调用在端点 handler 层」）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27.1-feishu-binding/27.1-CONTEXT.md
@.planning/phases/26-oauth2/26-CONTEXT.md
@.planning/phases/26-oauth2/26-02-SUMMARY.md

@backend/app/services/feishu_oauth_service.py
@backend/app/models/user.py
@backend/app/core/config.py
@backend/tests/test_services/test_feishu_oauth_service.py

<interfaces>
<!-- 现有可复用契约，Plan 02/03 也会引用；无需再查源码 -->

已存在于 `backend/app/services/feishu_oauth_service.py`：
```python
class FeishuOAuthService:
    FEISHU_AUTHORIZE_URL = 'https://accounts.feishu.cn/open-apis/authen/v1/authorize'
    FEISHU_TOKEN_URL = 'https://open.feishu.cn/open-apis/authen/v2/oauth/token'
    FEISHU_USER_INFO_URL = 'https://open.feishu.cn/open-apis/authen/v1/user_info'
    FEISHU_AUTHORIZE_SCOPE = 'contact:user.employee_id:readonly'

    def __init__(self, db: Session, settings: Settings | None = None) -> None: ...
    def generate_authorize_url(self, redis_client) -> dict[str, str]: ...  # 当前只支持 login
    def handle_callback(self, code: str, state: str, redis_client) -> User: ...  # login-only
    def require_redis(self) -> redis_lib.Redis: ...  # 503 when unreachable
    def _validate_state(self, state: str, redis_client) -> None: ...  # 当前只读 'feishu_oauth_state:{state}'
    def _check_code_replay(self, code: str, redis_client) -> None: ...
    def _exchange_code_for_token(self, code: str) -> dict: ...  # 使用 self._settings.feishu_redirect_uri
    def _get_user_info(self, access_token: str) -> dict: ...  # 返回 {open_id, employee_no, ...}
    def _find_or_bind_user(self, open_id: str, employee_no: str) -> User: ...  # login path
```

已存在于 `backend/app/services/feishu_service.py`：
```python
class FeishuService:
    @staticmethod
    def _lookup_employee(emp_map: dict[str, str], emp_no: str | None) -> str | None:
        """精确匹配 → leading-zero 容差匹配。复用此方法。"""
```

已存在于 `backend/app/models/user.py`：
```python
class User(UUIDPrimaryKeyMixin, CreatedAtMixin, Base):
    id: Mapped[str]
    employee_id: Mapped[Optional[str]]  # FK to employees.id, unique, nullable
    feishu_open_id: Mapped[str | None]  # String(255), unique, nullable
    employee = relationship('Employee', back_populates='bound_user', foreign_keys=[employee_id])
    # property employee_no 读 self.employee.employee_no
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 新增 feishu_bind_redirect_uri Settings 字段 + .env.example 注释</name>
  <files>backend/app/core/config.py, .env.example</files>
  <read_first>
    - backend/app/core/config.py（已存字段 feishu_app_id / feishu_app_secret / feishu_redirect_uri 在第 70-73 行）
    - .env.example 第 56-61 行 FEISHU 段落
    - .planning/phases/27.1-feishu-binding/27.1-CONTEXT.md（D-02 中 bind redirect_uri 的用途；code_context 段落的「Settings 类加 feishu_bind_redirect_uri: str = ''（空值则绑定流程不可用并返回 503）」约束）
  </read_first>
  <action>
在 `backend/app/core/config.py` 的 `class Settings(BaseSettings):` 里，紧接已有行 `feishu_redirect_uri: str = ''`（第 73 行）之后插入新字段：

```python
    feishu_bind_redirect_uri: str = ''
```

字段排序紧挨 `feishu_redirect_uri`，保持该段配置视觉对齐。不要改 env_prefix / case_sensitive，pydantic-settings 自动把 `FEISHU_BIND_REDIRECT_URI` env 映射到此字段（CONTEXT code_context 段已确认）。

在 `.env.example` 第 61 行 `FEISHU_REDIRECT_URI=http://localhost:5174/auth/feishu/callback` 之后插入一行：

```
FEISHU_BIND_REDIRECT_URI=http://localhost:5174/settings/feishu-bind-callback
```

此字段实现 D-02 的 bind 路径 redirect_uri。开发环境填 `http://localhost:5174/settings/feishu-bind-callback`（前端 Vite dev 端口）。生产部署时由运维按 D-04 在飞书开放平台登记对应域名后再填入 .env。空值时 Plan 02 API 路由会返回 503（由 Plan 02 任务处理）。

保持其他字段不变。不新增任何 env 映射装饰器、不改 `model_config`。
  </action>
  <verify>
    <automated>grep -n "feishu_bind_redirect_uri: str = ''" backend/app/core/config.py && grep -n "FEISHU_BIND_REDIRECT_URI=" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "feishu_bind_redirect_uri" backend/app/core/config.py` 返回 1（仅在 Settings 中声明一次）
    - `grep -c "FEISHU_BIND_REDIRECT_URI" .env.example` 返回 1
    - `.env.example` 中 `FEISHU_BIND_REDIRECT_URI` 示例值包含字符串 `settings/feishu-bind-callback`
    - `cd /Users/mac/PycharmProjects/Wage_adjust && python -c "from backend.app.core.config import Settings; s = Settings(); assert hasattr(s, 'feishu_bind_redirect_uri'); assert s.feishu_bind_redirect_uri == ''"` 退出码 0
    - `backend/app/core/config.py` 中 `feishu_app_id` / `feishu_app_secret` / `feishu_redirect_uri` 原有 4 个字段依旧存在未被改动
  </acceptance_criteria>
  <done>
    Settings 新字段存在，默认空字符串；.env.example 有对应注释行；`python -c "from backend.app.core.config import Settings; Settings()"` 成功；现有测试不回归。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 扩展 FeishuOAuthService 支持 bind / unbind</name>
  <files>backend/app/services/feishu_oauth_service.py, backend/tests/test_services/test_feishu_oauth_service.py</files>
  <read_first>
    - backend/app/services/feishu_oauth_service.py 全文（242 行，focus: generate_authorize_url / handle_callback / _validate_state / _find_or_bind_user）
    - backend/app/services/feishu_service.py 第 275-282 行（`_lookup_employee` leading-zero 容差）
    - backend/tests/test_services/test_feishu_oauth_service.py（复用 db / mock_redis / settings fixture 模板）
    - .planning/phases/27.1-feishu-binding/27.1-CONTEXT.md 的 D-01 / D-02 / D-05 / D-06 / D-07 / D-15
    - backend/app/models/user.py（employee_id 外键 / feishu_open_id 唯一约束）
  </read_first>
  <behavior>
    - Behavior 1（D-02 authorize purpose=bind）：`generate_authorize_url(redis_client, purpose='login')` 默认 purpose='login'，state key 前缀 `feishu_oauth_state:{state}`，redirect_uri 取 `feishu_redirect_uri`；purpose='bind' 时 state key 前缀 `feishu_oauth_state:bind:{state}`，redirect_uri 取 `feishu_bind_redirect_uri`；purpose='bind' 且 `feishu_bind_redirect_uri` 为空串时 raise HTTPException(503, '绑定服务暂不可用，请联系管理员')。
    - Behavior 2（D-01 + D-05 + D-06 + D-07 bind happy path）：`handle_bind_callback(code, state, user, redis_client)` — (a) 调 `_validate_state_bind(state, redis_client)` 消费 bind 前缀 state；(b) 调 `_check_code_replay(code, redis_client)`；(c) 调 `_exchange_code_for_token(code)`（复用，但必须使用 `feishu_bind_redirect_uri` — 见 implementation 段）；(d) 调 `_get_user_info`；(e) 若 user.employee_id is None → raise 400 '当前账号未关联员工信息，请联系管理员'（D-07）；(f) 加载 `user.employee.employee_no` 用 `FeishuService._lookup_employee` 风格对比来自飞书的 `employee_no`（leading-zero 容差）；不一致 → raise 400 '飞书工号与当前账号绑定的工号不一致，无法绑定'（D-06）；(g) `db.scalar(select(User).where(User.feishu_open_id == open_id, User.id != user.id))` 若存在 → raise 409 '该飞书账号已被其他系统账号绑定，请先在原账号解绑'（D-05）；(h) 写入 `user.feishu_open_id = open_id`，`db.flush()` 返回 user。
    - Behavior 3（D-05 冲突 409）：当前 user 的 feishu_open_id 已有值时允许重新绑定同 open_id（幂等），但别人占用必须 409。
    - Behavior 4（handle_unbind happy path）：`handle_unbind(user)` 把 `user.feishu_open_id = None`，`db.flush()`，返回 user；若本就为 None 幂等返回 user（不抛）。
    - Behavior 5（state 键前缀隔离）：purpose='login' 生成的 state 不能被 bind 路径消费，反之亦然；测试用 `_validate_state` vs `_validate_state_bind` 分别命中不同 Redis 键。
  </behavior>
  <action>
**目标文件：`backend/app/services/feishu_oauth_service.py`**

引入 `from backend.app.services.feishu_service import FeishuService` 用于复用 `_lookup_employee`（已在底部既有 `_find_or_bind_user` 中用过该类静态方法，但顶部 import 已存在 — 核对第 19 行 `from backend.app.services.feishu_service import FeishuService`）。

**改动 1 — 扩展 `generate_authorize_url` 签名为 `purpose` 可选参数：**

```python
def generate_authorize_url(
    self,
    redis_client: redis_lib.Redis,
    *,
    purpose: str = 'login',
) -> dict[str, str]:
    """Generate a Feishu OAuth authorize URL with CSRF state token.

    purpose='login' → state key 'feishu_oauth_state:{state}', redirect_uri=feishu_redirect_uri
    purpose='bind'  → state key 'feishu_oauth_state:bind:{state}', redirect_uri=feishu_bind_redirect_uri
    """
    if purpose not in ('login', 'bind'):
        raise HTTPException(status_code=400, detail='无效的 purpose 参数')

    if purpose == 'bind':
        redirect_uri = self._settings.feishu_bind_redirect_uri
        if not redirect_uri:
            raise HTTPException(status_code=503, detail='绑定服务暂不可用，请联系管理员')
        state_key_prefix = 'feishu_oauth_state:bind:'
    else:
        redirect_uri = self._settings.feishu_redirect_uri
        state_key_prefix = 'feishu_oauth_state:'

    state = secrets.token_urlsafe(32)
    redis_client.setex(f'{state_key_prefix}{state}', 300, '1')

    params = urllib.parse.urlencode({
        'client_id': self._settings.feishu_app_id,
        'response_type': 'code',
        'redirect_uri': redirect_uri,
        'state': state,
        'scope': self.FEISHU_AUTHORIZE_SCOPE,
    })
    authorize_url = f'{self.FEISHU_AUTHORIZE_URL}?{params}'
    return {'authorize_url': authorize_url, 'state': state}
```

既有调用方 `/api/v1/auth/feishu/authorize` 路由（backend/app/api/v1/auth.py 第 269-277 行）调用 `service.generate_authorize_url(redis_client)` 不传 purpose，默认 'login' 保持向后兼容 — Plan 02 才会扩展路由接受 `?purpose=login|bind` 并转发参数。

**改动 2 — 新增 `_validate_state_bind` 私有方法（紧邻既有 `_validate_state`）：**

```python
def _validate_state_bind(self, state: str, redis_client: redis_lib.Redis) -> None:
    """Validate CSRF state for bind flow; consume on use."""
    key = f'feishu_oauth_state:bind:{state}'
    val = redis_client.get(key)
    if val is None:
        raise HTTPException(status_code=400, detail='无效的 state 参数，请重新发起授权')
    redis_client.delete(key)
```

**改动 3 — 新增 `handle_bind_callback` 公共方法：**

```python
def handle_bind_callback(
    self,
    code: str,
    state: str,
    user: User,
    redis_client: redis_lib.Redis,
) -> User:
    """Handle the bind callback: validate state(bind prefix), exchange code, check employee_no consistency, bind open_id to user.

    Raises:
        HTTPException 400 — 当前 user 未绑定 Employee / employee_no 不一致 / state 无效 / 授权码已使用
        HTTPException 409 — open_id 已被其他账号占用
        HTTPException 502/503 — 飞书或 Redis 不可达
    """
    self._validate_state_bind(state, redis_client)
    self._check_code_replay(code, redis_client)
    redis_client.setex(f'feishu_oauth_code:{code}', 600, '1')

    token_data = self._exchange_code_for_token_for_bind(code)
    access_token = (
        token_data.get('access_token')
        or token_data.get('data', {}).get('access_token')
        or ''
    )
    if not access_token:
        logger.error('Feishu bind token exchange returned no access_token: %s', token_data)
        raise HTTPException(status_code=502, detail='飞书认证服务异常，请稍后重试')

    user_info = self._get_user_info(access_token)
    open_id = user_info.get('open_id', '')
    feishu_employee_no = user_info.get('employee_no', '')

    # D-07: user must have Employee
    if user.employee_id is None or user.employee is None:
        raise HTTPException(status_code=400, detail='当前账号未关联员工信息，请联系管理员')

    # D-06: employee_no must match (leading-zero tolerant)
    current_emp_no = user.employee.employee_no or ''
    emp_map = {current_emp_no: user.employee_id}
    stripped = current_emp_no.lstrip('0') or '0'
    if stripped not in emp_map:
        emp_map[stripped] = user.employee_id
    if FeishuService._lookup_employee(emp_map, feishu_employee_no) is None:
        raise HTTPException(
            status_code=400,
            detail='飞书工号与当前账号绑定的工号不一致，无法绑定',
        )

    # D-05: open_id must not be occupied by another user
    if not open_id:
        raise HTTPException(status_code=502, detail='获取飞书用户信息失败')
    other = self.db.scalar(
        select(User).where(
            User.feishu_open_id == open_id,
            User.id != user.id,
        )
    )
    if other is not None:
        raise HTTPException(
            status_code=409,
            detail='该飞书账号已被其他系统账号绑定，请先在原账号解绑',
        )

    user.feishu_open_id = open_id
    self.db.flush()
    return user
```

**改动 4 — 新增 `_exchange_code_for_token_for_bind` 私有方法** — 因为 `_exchange_code_for_token` 硬编码了 `redirect_uri=self._settings.feishu_redirect_uri`（login 场景）。bind 场景需要用 `feishu_bind_redirect_uri`。实现方式：复制既有方法体，仅替换 `redirect_uri` 来源（不重构既有登录路径，保持 Plan 26 契约稳定）：

```python
def _exchange_code_for_token_for_bind(self, code: str) -> dict:
    """Bind-flow token exchange — uses feishu_bind_redirect_uri instead of feishu_redirect_uri."""
    try:
        resp = _call_feishu_with_retry(
            lambda: httpx.post(
                self.FEISHU_TOKEN_URL,
                json={
                    'grant_type': 'authorization_code',
                    'client_id': self._settings.feishu_app_id,
                    'client_secret': self._settings.feishu_app_secret,
                    'code': code,
                    'redirect_uri': self._settings.feishu_bind_redirect_uri,
                },
                timeout=10,
            ),
            endpoint_label='token_bind',
        )
    except _TRANSIENT_HTTPX_ERRORS as exc:
        logger.warning('Feishu token endpoint unreachable after retries (bind): %s', exc)
        raise HTTPException(status_code=502, detail='无法连接飞书认证服务，请稍后重试') from exc

    data = resp.json()
    error_code = data.get('code', 0)
    if error_code != 0:
        status_code, message = _FEISHU_ERROR_MAP.get(error_code, (502, '飞书认证服务异常'))
        logger.warning('Feishu bind token exchange failed: code=%s msg=%s', error_code, data.get('msg'))
        raise HTTPException(status_code=status_code, detail=message)
    return data
```

**改动 5 — 新增 `handle_unbind` 公共方法：**

```python
def handle_unbind(self, user: User) -> User:
    """Clear user.feishu_open_id; idempotent (no-op when already None)."""
    if user.feishu_open_id is not None:
        user.feishu_open_id = None
        self.db.flush()
    return user
```

（解绑不动 token_version — CONTEXT D-10 明确「保留当前 session」。）

**改动 6 — 服务层不写 AuditLog**：按 CONTEXT code_context 段「审计调用在端点 handler 层」，AuditLog 写入在 Plan 02 的 API handler 中完成，保持 service 纯业务可测。

---

**目标文件：`backend/tests/test_services/test_feishu_oauth_service.py`**

追加 5 个测试到文件尾部（沿用既有 fixtures：`db` / `settings` / `mock_redis`）。`settings` fixture 需扩展 `feishu_bind_redirect_uri='https://example.com/settings/feishu-bind-callback'`（在现有 fixture 上加这一项）。

```python
# --- Phase 27.1 bind / unbind tests ---

def _make_user_with_employee(db, *, employee_no='E0001', open_id=None):
    emp = Employee(employee_no=employee_no, name='Alice', id_card_no=None, department='R&D', sub_department=None, job_family='Eng', job_level='P5', manager_id=None, status='active')
    db.add(emp); db.flush()
    user = User(email=f'{uuid.uuid4().hex[:8]}@ex.com', hashed_password='x', role='employee', employee_id=emp.id, feishu_open_id=open_id)
    db.add(user); db.flush()
    return user

def test_generate_authorize_url_login_vs_bind_isolation(db, settings, mock_redis):
    settings.feishu_bind_redirect_uri = 'https://ex.com/settings/feishu-bind-callback'
    svc = FeishuOAuthService(db, settings=settings)
    login_res = svc.generate_authorize_url(mock_redis, purpose='login')
    bind_res = svc.generate_authorize_url(mock_redis, purpose='bind')
    assert f'feishu_oauth_state:{login_res["state"]}' in mock_redis._store
    assert f'feishu_oauth_state:bind:{bind_res["state"]}' in mock_redis._store
    # bind url must use bind redirect
    assert 'settings%2Ffeishu-bind-callback' in bind_res['authorize_url']

def test_generate_authorize_url_bind_503_when_unset(db, settings, mock_redis):
    settings.feishu_bind_redirect_uri = ''
    svc = FeishuOAuthService(db, settings=settings)
    with pytest.raises(HTTPException) as exc:
        svc.generate_authorize_url(mock_redis, purpose='bind')
    assert exc.value.status_code == 503

def test_handle_bind_callback_happy_path(db, settings, mock_redis):
    settings.feishu_bind_redirect_uri = 'https://ex.com/settings/feishu-bind-callback'
    user = _make_user_with_employee(db, employee_no='E0001')
    # pre-seed bind state + user_info mock
    mock_redis._store['feishu_oauth_state:bind:S1'] = '1'
    svc = FeishuOAuthService(db, settings=settings)
    with patch.object(svc, '_exchange_code_for_token_for_bind', return_value={'access_token': 'T'}), \
         patch.object(svc, '_get_user_info', return_value={'open_id': 'ou_abc', 'employee_no': 'E0001'}):
        result = svc.handle_bind_callback('C1', 'S1', user, mock_redis)
    assert result.feishu_open_id == 'ou_abc'

def test_handle_bind_callback_conflict_409(db, settings, mock_redis):
    settings.feishu_bind_redirect_uri = 'https://ex.com/settings/feishu-bind-callback'
    other = _make_user_with_employee(db, employee_no='E0002', open_id='ou_taken')
    me = _make_user_with_employee(db, employee_no='E0001')
    mock_redis._store['feishu_oauth_state:bind:S2'] = '1'
    svc = FeishuOAuthService(db, settings=settings)
    with patch.object(svc, '_exchange_code_for_token_for_bind', return_value={'access_token': 'T'}), \
         patch.object(svc, '_get_user_info', return_value={'open_id': 'ou_taken', 'employee_no': 'E0001'}):
        with pytest.raises(HTTPException) as exc:
            svc.handle_bind_callback('C2', 'S2', me, mock_redis)
    assert exc.value.status_code == 409
    assert '该飞书账号已被其他系统账号绑定' in exc.value.detail

def test_handle_bind_callback_employee_no_mismatch_400(db, settings, mock_redis):
    settings.feishu_bind_redirect_uri = 'https://ex.com/settings/feishu-bind-callback'
    user = _make_user_with_employee(db, employee_no='E0001')
    mock_redis._store['feishu_oauth_state:bind:S3'] = '1'
    svc = FeishuOAuthService(db, settings=settings)
    with patch.object(svc, '_exchange_code_for_token_for_bind', return_value={'access_token': 'T'}), \
         patch.object(svc, '_get_user_info', return_value={'open_id': 'ou_x', 'employee_no': 'E9999'}):
        with pytest.raises(HTTPException) as exc:
            svc.handle_bind_callback('C3', 'S3', user, mock_redis)
    assert exc.value.status_code == 400
    assert '飞书工号与当前账号绑定的工号不一致' in exc.value.detail

def test_handle_bind_callback_user_without_employee_400(db, settings, mock_redis):
    settings.feishu_bind_redirect_uri = 'https://ex.com/settings/feishu-bind-callback'
    user = User(email='no-emp@ex.com', hashed_password='x', role='admin', employee_id=None)
    db.add(user); db.flush()
    mock_redis._store['feishu_oauth_state:bind:S4'] = '1'
    svc = FeishuOAuthService(db, settings=settings)
    with patch.object(svc, '_exchange_code_for_token_for_bind', return_value={'access_token': 'T'}), \
         patch.object(svc, '_get_user_info', return_value={'open_id': 'ou_z', 'employee_no': 'E0001'}):
        with pytest.raises(HTTPException) as exc:
            svc.handle_bind_callback('C4', 'S4', user, mock_redis)
    assert exc.value.status_code == 400
    assert '当前账号未关联员工信息' in exc.value.detail

def test_handle_unbind_clears_open_id(db, settings, mock_redis):
    user = _make_user_with_employee(db, open_id='ou_present')
    svc = FeishuOAuthService(db, settings=settings)
    result = svc.handle_unbind(user)
    assert result.feishu_open_id is None
    # idempotent
    result2 = svc.handle_unbind(result)
    assert result2.feishu_open_id is None
```

Employee 模型字段请与 `backend/app/models/employee.py` 对齐；若字段不匹配（比如 `sub_department` 是 NOT NULL），按实际 schema 填补默认值（执行时 grep 现有 test_services 目录的 Employee 构造用例取样）。

全部测试需通过 `cd /Users/mac/PycharmProjects/Wage_adjust && python -m pytest backend/tests/test_services/test_feishu_oauth_service.py -x`。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust && python -m pytest backend/tests/test_services/test_feishu_oauth_service.py -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def generate_authorize_url" backend/app/services/feishu_oauth_service.py` 显示签名含 `purpose: str = 'login'`
    - `grep -n "def handle_bind_callback" backend/app/services/feishu_oauth_service.py` 返回 1 处
    - `grep -n "def handle_unbind" backend/app/services/feishu_oauth_service.py` 返回 1 处
    - `grep -n "feishu_oauth_state:bind:" backend/app/services/feishu_oauth_service.py` 至少出现 2 处（generate + _validate_state_bind）
    - `grep -n "该飞书账号已被其他系统账号绑定" backend/app/services/feishu_oauth_service.py` 返回 1 处
    - `grep -n "飞书工号与当前账号绑定的工号不一致" backend/app/services/feishu_oauth_service.py` 返回 1 处
    - `grep -n "当前账号未关联员工信息" backend/app/services/feishu_oauth_service.py` 返回 1 处
    - 新增测试数量：`grep -cE "def test_(handle_bind|handle_unbind|generate_authorize_url_login_vs_bind|generate_authorize_url_bind_503)" backend/tests/test_services/test_feishu_oauth_service.py` 返回 >= 7
    - `pytest backend/tests/test_services/test_feishu_oauth_service.py -x` 退出码 0，所有测试绿（既有 + 新增）
    - 既有 `handle_callback` 方法（login path）未被改动（`grep -n "def handle_callback" backend/app/services/feishu_oauth_service.py` 仍显示原方法名；签名保持原样）
    - `grep -n "token_version" backend/app/services/feishu_oauth_service.py` 依然为 0 处（不改 token_version）
  </acceptance_criteria>
  <done>
    FeishuOAuthService 提供 handle_bind_callback / handle_unbind / generate_authorize_url(purpose=...) 三个新能力；5+ 个新测试全部通过；既有 login 路径测试无回归；login vs bind state 键前缀物理隔离。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 前端已登录用户 → /auth/feishu/authorize?purpose=bind | JWT 携带的用户 id 是可信主体；purpose 参数需白名单校验 |
| 前端已登录用户 → /auth/feishu/bind | JWT 校验完成后，handle_bind_callback 操作信任 `user` 对象；code/state 来自浏览器 URL 不可信 |
| 飞书开放平台 → 后端 _exchange_code_for_token_for_bind | httpx 出站请求跨信任边界；依赖 TLS + App Secret |
| Redis state 存储 → 服务层 _validate_state_bind | Redis 键前缀「feishu_oauth_state:bind:」与登录路径「feishu_oauth_state:」在同一库同一 TTL 生命周期，逻辑隔离由键前缀保证 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27.1-01 | Elevation of Privilege (跨用户绑定劫持) | handle_bind_callback | mitigate | D-06 强制 `FeishuService._lookup_employee` 校验飞书 employee_no 与 `user.employee.employee_no` 一致（leading-zero 容差），不一致 400 拒绝；即便攻击者控制浏览器发起授权，也无法把他人飞书账号绑到自己的 User |
| T-27.1-02 | Elevation of Privilege (open_id 盗用) | handle_bind_callback | mitigate | D-05 查询 `User.feishu_open_id == open_id AND User.id != current.id` 存在则 409 拒绝；不允许覆盖，保证 feishu_open_id 唯一约束绝对成立 |
| T-27.1-04 | Tampering (state 混用绕过) | generate_authorize_url / _validate_state_bind | mitigate | D-02 state key 前缀隔离：`feishu_oauth_state:bind:{state}` 不能被 login 路径 `_validate_state`（查询 `feishu_oauth_state:{state}`）消费；登录 state 同理不能被绑定路径消费 |
| T-27.1-05 | Information Disclosure (open_id 泄漏) | (service 层本身不做审计写入) | transfer | 本 Plan service 不写 AuditLog；open_id 截断策略在 Plan 02 handler 层实施（D-15 前 8 位/后 8 位） |
| T-27.1-07 | Denial of Service (空 bind redirect 配置) | generate_authorize_url(purpose='bind') | mitigate | feishu_bind_redirect_uri 空值时 503 拒绝，避免向飞书发起带空 redirect_uri 的请求（会被飞书拒绝导致半开状态） |
| T-27.1-08 | Denial of Service (code 重放) | handle_bind_callback | mitigate | 复用 `_check_code_replay`（Redis `feishu_oauth_code:{code}` TTL 600s）；bind / login 共享 code 防重放空间，同一 code 不能在两条路径分别消费 |
</threat_model>

<verification>
## Plan-level Verification

- [ ] `cd /Users/mac/PycharmProjects/Wage_adjust && python -c "from backend.app.core.config import Settings; s = Settings(); assert hasattr(s, 'feishu_bind_redirect_uri')"` 退出码 0
- [ ] `grep FEISHU_BIND_REDIRECT_URI .env.example` 命中 1 处
- [ ] `pytest backend/tests/test_services/test_feishu_oauth_service.py -x` 全绿（既有 + 新增 7+ 个测试）
- [ ] `pytest backend/tests/test_api/test_feishu_oauth_integration.py -x` 全绿（登录路径未回归）
- [ ] `grep -c "def generate_authorize_url" backend/app/services/feishu_oauth_service.py` 返回 1（未重复定义）
</verification>

<success_criteria>
Plan 01 成功当且仅当：

1. `Settings.feishu_bind_redirect_uri` 字段可从 .env 加载，空串时绑定 authorize 路径抛 503。
2. `generate_authorize_url(redis_client, purpose='bind')` 返回的 authorize_url 含 `feishu_bind_redirect_uri` 且 Redis state 键前缀为 `feishu_oauth_state:bind:`。
3. `handle_bind_callback` 在 5 个关键场景（happy / 409 conflict / 400 employee_no mismatch / 400 no employee / state 无效）均按 CONTEXT D-01/05/06/07 行为表现。
4. `handle_unbind` 幂等清空 feishu_open_id 不动 token_version。
5. pytest 全绿；既有 login 路径测试无回归。
6. 服务层不写 AuditLog — 审计实现由 Plan 02 的 API handler 承担（遵循项目 Established Pattern）。
</success_criteria>

<output>
完成后生成 `.planning/phases/27.1-feishu-binding/27.1-01-SUMMARY.md`，记录：
- 新增/修改文件 diff 摘要
- 7+ 测试用例覆盖清单
- T-27.1-01/02/04/07/08 威胁缓解落地点
- Plan 02 依赖接口（handle_bind_callback / handle_unbind / generate_authorize_url(purpose=...) 签名 verbatim）
</output>
