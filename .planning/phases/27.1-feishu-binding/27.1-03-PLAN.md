---
phase: 27.1
plan: 03
type: execute
wave: 3
depends_on:
  - "27.1-01"
  - "27.1-02"
files_modified:
  - frontend/src/types/api.ts
  - frontend/src/services/auth.ts
  - frontend/src/utils/feishuErrors.ts
  - frontend/src/pages/FeishuBindCallbackPage.tsx
  - frontend/src/App.tsx
  - frontend/src/pages/Settings.tsx
autonomous: false
requirements:
  - FAUTH-06
  - FAUTH-07
  - FAUTH-08
  - FAUTH-09

must_haves:
  truths:
    - "设置页显示「飞书账号」section，含状态 badge（已绑定/未绑定）+ 按钮"
    - "未绑定用户点击「使用飞书绑定」整页跳转飞书授权，回流到 /settings/feishu-bind-callback 完成绑定并回到 /settings"
    - "已绑定用户点击「解除飞书绑定」弹 confirm 后调用 unbind，feishu_open_id 清空且 session 保留"
    - "绑定失败时显示分类中文错误：409 显示「该飞书账号已被其他系统账号绑定…」；400+工号不一致显示「飞书工号与当前账号绑定的工号不一致…」；其余复用 Phase 27 既有分类"
    - "绑定成功后 SettingsPage 自动从「未绑定」切换为「已绑定」（refreshProfile 生效）"
    - "feishuErrors.ts 新增两类 code：feishu_already_bound（409）+ feishu_employee_mismatch（400 with 工号不一致 detail）"
  artifacts:
    - path: "frontend/src/types/api.ts"
      provides: "可选扩展：保持 UserProfile.feishu_open_id 已有；无需新增类型（可复用 UserProfile 作为 bind/unbind 响应）"
      contains: "feishu_open_id"
    - path: "frontend/src/services/auth.ts"
      provides: "authorizeFeishu(purpose?) / bindFeishu(code, state) / unbindFeishu()"
      exports: ["authorizeFeishu", "bindFeishu", "unbindFeishu"]
    - path: "frontend/src/utils/feishuErrors.ts"
      provides: "FeishuErrorCode 追加 feishu_already_bound / feishu_employee_mismatch"
      contains: "feishu_already_bound|feishu_employee_mismatch"
    - path: "frontend/src/pages/FeishuBindCallbackPage.tsx"
      provides: "三态 UI，成功后 navigate('/settings', { state: { bindSuccess: true } })"
      exports: ["FeishuBindCallbackPage"]
    - path: "frontend/src/App.tsx"
      provides: "ProtectedRoute 下新增 /settings/feishu-bind-callback"
      contains: "/settings/feishu-bind-callback"
    - path: "frontend/src/pages/Settings.tsx"
      provides: "FeishuBindingSection（eyebrow=登录方式 + h3=飞书账号 + badge + 按钮 + inline banner）"
      contains: "飞书账号|使用飞书绑定|解除飞书绑定"
  key_links:
    - from: "Settings.tsx (FeishuBindingSection)"
      to: "services/auth.ts::authorizeFeishu('bind') / unbindFeishu()"
      via: "按钮 click handler"
      pattern: "authorizeFeishu|unbindFeishu"
    - from: "FeishuBindCallbackPage"
      to: "services/auth.ts::bindFeishu + useAuth.refreshProfile"
      via: "useEffect (hasRunRef guard)"
      pattern: "bindFeishu|refreshProfile"
    - from: "App.tsx"
      to: "FeishuBindCallbackPage"
      via: "ProtectedRoute (allowedRoles 含全部 4 个角色) 下注册 /settings/feishu-bind-callback"
      pattern: "/settings/feishu-bind-callback"
---

<objective>
把 Plan 02 交付的 REST 契约接入前端设置页：用户可在设置页主动发起飞书账号绑定 / 解绑，分类中文错误按 feishuErrors.ts 映射，绑定成功自动刷新 profile。覆盖 CONTEXT D-03 / D-09 / D-10 / D-11 / D-12 / D-13 / D-14。

Purpose: 闭环 FAUTH-06/07/08/09 的用户可见行为 — 设置页入口 + 绑定回流 + 解绑二次确认 + 分类错误；确保 session 在解绑后保留（D-10）。
Output: 6 个前端文件改动（4 个扩展 + 2 个新增）+ manual UAT checkpoint 覆盖 8 步人工验证。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27.1-feishu-binding/27.1-CONTEXT.md
@.planning/phases/27.1-feishu-binding/27.1-01-PLAN.md
@.planning/phases/27.1-feishu-binding/27.1-02-PLAN.md
@.planning/phases/27-oauth2/27-02-SUMMARY.md
@.planning/phases/27-oauth2/27-03-SUMMARY.md

@frontend/src/pages/Settings.tsx
@frontend/src/pages/FeishuCallbackPage.tsx
@frontend/src/services/auth.ts
@frontend/src/utils/feishuErrors.ts
@frontend/src/hooks/useAuth.tsx
@frontend/src/App.tsx
@frontend/src/types/api.ts

<interfaces>
<!-- Plan 02 交付的 REST 契约 -->

```
GET  /api/v1/auth/feishu/authorize?purpose=login|bind
  200 → { authorize_url: string, state: string }
  503 → 绑定 redirect_uri 未配置
  422 → purpose 不在白名单

POST /api/v1/auth/feishu/bind       (Bearer JWT 必需)
  Body: { code: string, state: string }
  200 → UserRead (含 feishu_open_id)
  400 → {error, message: '飞书工号与当前账号绑定的工号不一致，无法绑定'}
  400 → {error, message: '当前账号未关联员工信息，请联系管理员'}
  409 → {error, message: '该飞书账号已被其他系统账号绑定，请先在原账号解绑'}
  502 → {error, message: '飞书认证服务异常，请稍后重试' / '无法连接飞书认证服务…'}
  503 → Redis 不可达
  401 → 无/无效 JWT

POST /api/v1/auth/feishu/unbind     (Bearer JWT 必需)
  Body: (empty)
  200 → UserRead (feishu_open_id=null)
  401 → 无/无效 JWT
```

<!-- 前端现有契约（不动） -->

```ts
// frontend/src/services/auth.ts (既有)
export async function authorizeFeishu(): Promise<FeishuAuthorizeResponse>;   // 不带 purpose
export async function feishuCallback(code, state): Promise<AuthResponse>;   // 登录路径

// frontend/src/hooks/useAuth.tsx (既有)
interface AuthContextValue {
  user: UserProfile | null;
  loginWithFeishu(code, state): Promise<UserProfile>;
  refreshProfile(): Promise<UserProfile | null>;
  // ...
}

// frontend/src/utils/feishuErrors.ts (既有 9 类 code + classifyBackend)
type FeishuErrorCode = 'authorization_cancelled' | 'employee_not_matched' | 'state_invalid_or_expired'
  | 'code_expired_or_replayed' | 'redis_unavailable' | 'feishu_unreachable' | 'network_error'
  | 'sdk_load_failed' | 'unknown_error';
```

<!-- Phase 27 模板 — FeishuCallbackPage 三态 UI 模板直接复制即可 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 扩展 auth service + feishuErrors 新增两类 code</name>
  <files>frontend/src/services/auth.ts, frontend/src/utils/feishuErrors.ts</files>
  <read_first>
    - frontend/src/services/auth.ts 全文（104-113 行 authorizeFeishu / feishuCallback 是参考模板；Axios baseURL 已配 /api/v1）
    - frontend/src/utils/feishuErrors.ts 全文（74 行 — FeishuErrorCode 联合、COPY 映射、classifyBackend 分支）
    - frontend/src/types/api.ts（FeishuAuthorizeResponse / UserProfile 已导出）
    - .planning/phases/27.1-feishu-binding/27.1-CONTEXT.md D-09 / D-13
  </read_first>
  <action>
**目标文件 1：`frontend/src/services/auth.ts`**

**改动 1 — 把现有 `authorizeFeishu()` 签名改为接受 purpose 可选参数（向后兼容）**：

用 Edit 替换当前的 `export async function authorizeFeishu(): Promise<FeishuAuthorizeResponse>` 实现为：

```ts
export async function authorizeFeishu(purpose?: 'login' | 'bind'): Promise<FeishuAuthorizeResponse> {
  const response = await api.get<FeishuAuthorizeResponse>('/auth/feishu/authorize', {
    params: purpose ? { purpose } : undefined,
  });
  return response.data;
}
```

Phase 27 `FeishuLoginPanel.tsx` 调用 `authorizeFeishu()` 不传参数，继续走 login 默认路径（后端已在 Plan 02 默认 `purpose='login'`）。

**改动 2 — 文件末尾追加 bindFeishu / unbindFeishu**：

```ts
export async function bindFeishu(code: string, state: string): Promise<UserProfile> {
  const response = await api.post<UserProfile>('/auth/feishu/bind', { code, state });
  return response.data;
}

export async function unbindFeishu(): Promise<UserProfile> {
  const response = await api.post<UserProfile>('/auth/feishu/unbind');
  return response.data;
}
```

（UserProfile 已从 `../types/api` 导入 — 在已有 import 列表里；若未导入先补。）

**目标文件 2：`frontend/src/utils/feishuErrors.ts`**

**改动 1 — 扩展 `FeishuErrorCode` 联合类型**（在 `'unknown_error'` 之前插入两项）：

```ts
export type FeishuErrorCode =
  | 'authorization_cancelled'
  | 'employee_not_matched'
  | 'state_invalid_or_expired'
  | 'code_expired_or_replayed'
  | 'redis_unavailable'
  | 'feishu_unreachable'
  | 'network_error'
  | 'sdk_load_failed'
  | 'feishu_already_bound'
  | 'feishu_employee_mismatch'
  | 'unknown_error';
```

**改动 2 — 扩展 `COPY` 映射**（插入两条，顺序与 union 对齐）：

```ts
  feishu_already_bound: '该飞书账号已被其他系统账号绑定，请先在原账号解绑',
  feishu_employee_mismatch: '飞书工号与当前账号绑定的工号不一致，无法绑定',
```

文案必须与后端 Plan 01 / Plan 02 错误 detail 的中文字面量**完全一致**（保证回归时 classifyBackend 关键字匹配能工作）。

**改动 3 — 扩展 `classifyBackend`**：在 `if (status === 409) return 'feishu_already_bound';` 新增一行（在 `status === 403` 与 `status === 400` 之间）。对 `status === 400` 的分支内部再新增：

```ts
  if (status === 409) return 'feishu_already_bound';
  if (status === 400) {
    if (detail.includes('工号与当前账号')) return 'feishu_employee_mismatch';
    if (detail.includes('当前账号未关联员工信息')) return 'feishu_employee_mismatch';  // D-07 亦归并
    if (detail.includes('state')) return 'state_invalid_or_expired';
    if (detail.includes('工号')) return 'employee_not_matched';
    if (detail.includes('授权码')) return 'code_expired_or_replayed';
    return 'unknown_error';
  }
```

匹配顺序：先特化的 `'工号与当前账号'`（绑定不一致）/ `'当前账号未关联员工信息'`（D-07 未关联员工），再到通用的 `'工号'`（登录工号未匹配）。这样登录路径 `'工号未匹配，请联系管理员开通'` 仍归入 `employee_not_matched`，绑定路径 `'飞书工号与当前账号绑定的工号不一致'` 归入 `feishu_employee_mismatch`，未关联员工 D-07 归并到 `feishu_employee_mismatch` 同一用户感知「绑定被拒」。

**关键保持**：既有 9 类 code 的分支行为不变，Phase 27 登录路径无回归。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust/frontend && npm run lint 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export async function bindFeishu" frontend/src/services/auth.ts` 返回 1
    - `grep -n "export async function unbindFeishu" frontend/src/services/auth.ts` 返回 1
    - `grep -n "purpose?: 'login' | 'bind'" frontend/src/services/auth.ts` 返回 1
    - `grep -n "feishu_already_bound" frontend/src/utils/feishuErrors.ts` 返回 >= 2（union + COPY + 分类）
    - `grep -n "feishu_employee_mismatch" frontend/src/utils/feishuErrors.ts` 返回 >= 2
    - `grep -n "'该飞书账号已被其他系统账号绑定'" frontend/src/utils/feishuErrors.ts` 命中
    - `grep -n "'飞书工号与当前账号绑定的工号不一致'" frontend/src/utils/feishuErrors.ts` 命中
    - `grep -n "if (status === 409)" frontend/src/utils/feishuErrors.ts` 返回 1
    - `cd frontend && npm run lint` 退出码 0（tsc --noEmit 通过）
    - `cd frontend && npm run build` 退出码 0
    - Phase 27 既有 `employee_not_matched` / `state_invalid_or_expired` / `code_expired_or_replayed` 分支 grep 依然命中
  </acceptance_criteria>
  <done>
    auth service 含 3 个方法（含 purpose 参数化的 authorizeFeishu）；feishuErrors.ts 含 11 类 code；lint + build 双绿；Phase 27 登录路径无回归。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 新建 FeishuBindCallbackPage + App.tsx 注册受保护路由</name>
  <files>frontend/src/pages/FeishuBindCallbackPage.tsx, frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/pages/FeishuCallbackPage.tsx 全文（145 行 — 三态 UI 模板，直接复制）
    - frontend/src/App.tsx 全文（focus: 第 416-470 行路由表；第 444-448 行 ProtectedRoute 含全部 4 角色的块是集成点）
    - frontend/src/hooks/useAuth.tsx 第 141-150 行 refreshProfile 方法
    - frontend/src/services/auth.ts（Task 1 产物，bindFeishu 可用）
    - frontend/src/utils/feishuErrors.ts（Task 1 产物，2 个新 code 可用）
    - .planning/phases/27.1-feishu-binding/27.1-CONTEXT.md D-03 / D-12
  </read_first>
  <action>
**目标文件 1：新建 `frontend/src/pages/FeishuBindCallbackPage.tsx`**

完整复制 `FeishuCallbackPage.tsx` 作为模板，替换以下差异点：

```tsx
import { useEffect, useRef, useState } from 'react';
import { useNavigate, useSearchParams } from 'react-router-dom';

import { useAuth } from '../hooks/useAuth';
import { bindFeishu } from '../services/auth';
import { resolveFeishuError } from '../utils/feishuErrors';

type CallbackState = 'processing' | 'success' | 'failed';

export function FeishuBindCallbackPage() {
  const [searchParams] = useSearchParams();
  const navigate = useNavigate();
  const { refreshProfile } = useAuth();
  const [state, setState] = useState<CallbackState>('processing');
  const [errorMessage, setErrorMessage] = useState<string>('');
  const hasRunRef = useRef(false);

  useEffect(() => {
    if (hasRunRef.current) return;
    hasRunRef.current = true;

    const code = searchParams.get('code');
    const stateParam = searchParams.get('state');

    if (!code || !stateParam) {
      setState('failed');
      setErrorMessage(resolveFeishuError('backend', new Error('缺少授权参数')).message);
      return;
    }

    void (async () => {
      try {
        await bindFeishu(code, stateParam);
        await refreshProfile();
        setState('success');
        navigate('/settings', { replace: true, state: { bindSuccess: true } });
      } catch (err) {
        setState('failed');
        setErrorMessage(resolveFeishuError('backend', err).message);
      }
    })();
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, []);

  return (
    <main
      style={{
        minHeight: '100vh',
        background: 'var(--color-bg-page)',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        padding: '24px 20px',
      }}
    >
      <section
        className="surface animate-fade-up"
        style={{ padding: '32px 32px', maxWidth: 420, width: '100%', textAlign: 'center' }}
      >
        {state === 'processing' ? (
          <>
            <p className="eyebrow">飞书绑定</p>
            <h1 style={{ marginTop: 8, fontSize: 22, fontWeight: 600, letterSpacing: '-0.02em', color: 'var(--color-ink)' }}>
              正在完成飞书绑定…
            </h1>
            <p style={{ marginTop: 12, fontSize: 13, lineHeight: 1.7, color: 'var(--color-steel)' }}>
              正在校验授权，请稍候。
            </p>
          </>
        ) : null}

        {state === 'success' ? (
          <>
            <p className="eyebrow">飞书绑定</p>
            <h1 style={{ marginTop: 8, fontSize: 22, fontWeight: 600, letterSpacing: '-0.02em', color: 'var(--color-ink)' }}>
              绑定成功，正在返回设置页…
            </h1>
          </>
        ) : null}

        {state === 'failed' ? (
          <>
            <p className="eyebrow">飞书绑定失败</p>
            <h1 style={{ marginTop: 8, fontSize: 22, fontWeight: 600, letterSpacing: '-0.02em', color: 'var(--color-ink)' }}>
              无法完成绑定
            </h1>
            <p style={{ marginTop: 12, fontSize: 13, lineHeight: 1.7, color: 'var(--color-danger)' }}>
              {errorMessage}
            </p>
            <div style={{ marginTop: 20 }}>
              <button
                className="action-primary"
                onClick={() => navigate('/settings', { replace: true })}
                type="button"
              >
                返回设置
              </button>
            </div>
          </>
        ) : null}
      </section>
    </main>
  );
}
```

关键差异 vs `FeishuCallbackPage`：
- 使用 `bindFeishu(code, state)` 而非 `loginWithFeishu`；调用 `refreshProfile()` 同步 UserProfile.feishu_open_id 到 context/localStorage（不 storeAuthSession，因为本就是已登录态）。
- 成功跳 `/settings` 并带 `state: { bindSuccess: true }`（Settings.tsx 会监听）。
- 失败按钮文案「返回设置」跳 `/settings`（而非 Phase 27 的「返回登录」跳 `/login`）。
- 三态 UI 的 eyebrow/h1 文案替换为绑定语义（「飞书绑定」/「正在完成飞书绑定…」/「无法完成绑定」）。
- 复用 Phase 27 的 `hasRunRef` + `replace: true` 防 StrictMode 与 back stack 污染（T-27-03 延续）。

**目标文件 2：`frontend/src/App.tsx`**

**改动 1 — import 段字母序插入**（在 `import { FeishuCallbackPage }` 之后）：

```tsx
import { FeishuBindCallbackPage } from "./pages/FeishuBindCallbackPage";
```

**改动 2 — 在 `ProtectedRoute allowedRoles={["admin", "hrbp", "manager", "employee"]}` 块（第 444-448 行）内部新增一行 Route**：

```tsx
<Route element={<ProtectedRoute allowedRoles={["admin", "hrbp", "manager", "employee"]} />}>
  <Route element={<EmployeeScopedEvaluationPage />} path="/employees/:employeeId" />
  <Route element={<SettingsPage />} path="/settings" />
  <Route element={<FeishuBindCallbackPage />} path="/settings/feishu-bind-callback" />
  <Route element={<SharingRequestsPage />} path="/sharing-requests" />
</Route>
```

关键：飞书回调必须在 `ProtectedRoute`（需登录）下注册（与 Phase 27 的 `/auth/feishu/callback` 公开路由相反 — 因为绑定是登录后动作 D-14）；`allowedRoles` 包含全部 4 个角色以满足「所有登录角色可见」。不改其他路由。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust/frontend && npm run lint 2>&1 | tail -5 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 新文件存在：`test -f frontend/src/pages/FeishuBindCallbackPage.tsx` 退出 0
    - `grep -n "export function FeishuBindCallbackPage" frontend/src/pages/FeishuBindCallbackPage.tsx` 返回 1
    - `grep -n "bindFeishu(code, stateParam)" frontend/src/pages/FeishuBindCallbackPage.tsx` 返回 1
    - `grep -n "refreshProfile()" frontend/src/pages/FeishuBindCallbackPage.tsx` 返回 1
    - `grep -n "state: { bindSuccess: true }" frontend/src/pages/FeishuBindCallbackPage.tsx` 返回 1
    - `grep -n "hasRunRef" frontend/src/pages/FeishuBindCallbackPage.tsx` 返回 >= 2
    - `grep -n "replace: true" frontend/src/pages/FeishuBindCallbackPage.tsx` 返回 >= 2
    - `grep -n "dangerouslySetInnerHTML\|innerHTML" frontend/src/pages/FeishuBindCallbackPage.tsx` 返回 0（T-27.1-06 XSS）
    - `grep -n "console\\.\\(log\\|error\\|warn\\|debug\\)" frontend/src/pages/FeishuBindCallbackPage.tsx` 返回 0
    - `grep -n "FeishuBindCallbackPage" frontend/src/App.tsx` 返回 >= 2（import + Route element）
    - `grep -n '/settings/feishu-bind-callback' frontend/src/App.tsx` 返回 1
    - App.tsx 中该 Route **位于 ProtectedRoute 块内部**：`grep -nA6 'allowedRoles={\\["admin", "hrbp", "manager", "employee"\\]}' frontend/src/App.tsx` 能看到 FeishuBindCallbackPage
    - `cd frontend && npm run lint` 退出码 0
    - `cd frontend && npm run build` 退出码 0
  </acceptance_criteria>
  <done>
    新回调页三态 UI 模板化实现；`/settings/feishu-bind-callback` 注册在 ProtectedRoute 下；lint/build 双通过；Phase 27 登录回调路由未改动。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: SettingsPage 添加 FeishuBindingSection</name>
  <files>frontend/src/pages/Settings.tsx</files>
  <read_first>
    - frontend/src/pages/Settings.tsx 全文（245 行；focus: 第 123-167 行「员工绑定」section + 第 170-242 行密码管理 section 的视觉语言；第 21-38 行 state hooks 布局）
    - frontend/src/hooks/useAuth.tsx（refreshProfile / user 来源）
    - frontend/src/services/auth.ts（Task 1 产物：authorizeFeishu / unbindFeishu）
    - frontend/src/utils/feishuErrors.ts（Task 1 产物：resolveFeishuError 分类）
    - .planning/phases/27.1-feishu-binding/27.1-CONTEXT.md D-10 / D-11 / D-12 / D-13 / D-14
  </read_first>
  <action>
**目标文件：`frontend/src/pages/Settings.tsx`**

**改动 1 — import 扩展**（第 7 行 `from '../services/auth'` 改为）：

```ts
import { authorizeFeishu, changePassword, selfBindConfirm, selfBindPreview, unbindFeishu } from '../services/auth';
import { resolveFeishuError } from '../utils/feishuErrors';
```

**改动 2 — 组件内 state 新增**（紧接 `const [isBinding, setIsBinding] = useState(false);` 之后）：

```ts
const [feishuError, setFeishuError] = useState<string | null>(null);
const [feishuSuccess, setFeishuSuccess] = useState<string | null>(null);
const [isFeishuProcessing, setIsFeishuProcessing] = useState(false);

// 监听 FeishuBindCallbackPage 跳回时的 state: { bindSuccess: true }
useEffect(() => {
  const navState = location.state as { bindSuccess?: boolean } | null;
  if (navState?.bindSuccess) {
    setFeishuSuccess('飞书账号已绑定');
    // 清除 history state 避免刷新重复显示
    window.history.replaceState({}, '');
  }
}, [location.state]);
```

**需要 import `useEffect`**：第 3 行 `import { useMemo, useState } from 'react';` 改为 `import { useEffect, useMemo, useState } from 'react';`。

**改动 3 — 组件内新增两个 handler**（紧接 `async function handleBindConfirm`之后）：

```ts
async function handleFeishuBind() {
  setFeishuError(null);
  setFeishuSuccess(null);
  setIsFeishuProcessing(true);
  try {
    const { authorize_url } = await authorizeFeishu('bind');
    window.location.href = authorize_url;
    // 整页跳转，后续不再继续（setIsFeishuProcessing 无需 reset）
  } catch (err) {
    setFeishuError(resolveFeishuError('backend', err).message);
    setIsFeishuProcessing(false);
  }
}

async function handleFeishuUnbind() {
  // D-10: 二次确认
  const confirmed = window.confirm('解绑后你将无法再使用飞书账号登录，需重新绑定。确认解绑？');
  if (!confirmed) return;

  setFeishuError(null);
  setFeishuSuccess(null);
  setIsFeishuProcessing(true);
  try {
    await unbindFeishu();
    await refreshProfile();
    setFeishuSuccess('已解除飞书绑定');
  } catch (err) {
    setFeishuError(resolveFeishuError('backend', err).message);
  } finally {
    setIsFeishuProcessing(false);
  }
}
```

**改动 4 — 在密码管理 section（第 194-242 行）**之后、整个 AppShell 的 `<>` 闭合之前，插入新 section：

```tsx
<section className="surface px-6 py-6 lg:px-7">
  <div className="section-head">
    <div>
      <p className="eyebrow">登录方式</p>
      <h2 className="section-title">飞书账号</h2>
      <p className="mt-2 text-sm leading-6 text-steel">
        {user?.feishu_open_id
          ? '你的飞书账号已绑定到系统账号，可在登录页使用飞书扫码登录。'
          : '绑定飞书账号后，可用飞书扫码快速登录。'}
      </p>
    </div>
  </div>

  {feishuError ? (
    <p style={{ marginTop: 16, background: 'var(--color-danger-bg)', border: '1px solid #FFCDD0', borderRadius: 6, padding: '10px 14px', fontSize: 13, color: 'var(--color-danger)' }}>
      {feishuError}
    </p>
  ) : null}
  {feishuSuccess ? (
    <p style={{ marginTop: 16, background: 'var(--color-success-bg)', border: '1px solid #B7F5C2', borderRadius: 6, padding: '10px 14px', fontSize: 13, color: 'var(--color-success)' }}>
      {feishuSuccess}
    </p>
  ) : null}

  <div className="mt-5 flex items-center gap-3">
    {user?.feishu_open_id ? (
      <span
        style={{
          display: 'inline-flex',
          alignItems: 'center',
          borderRadius: 999,
          background: 'var(--color-success-bg)',
          color: 'var(--color-success)',
          padding: '4px 12px',
          fontSize: 13,
          fontWeight: 500,
        }}
      >
        已绑定
      </span>
    ) : (
      <span
        style={{
          display: 'inline-flex',
          alignItems: 'center',
          borderRadius: 999,
          background: 'var(--color-bg-subtle)',
          color: 'var(--color-steel)',
          padding: '4px 12px',
          fontSize: 13,
          fontWeight: 500,
        }}
      >
        未绑定
      </span>
    )}
  </div>

  <div className="mt-5 flex flex-wrap gap-3">
    {user?.feishu_open_id ? (
      <button
        className="action-secondary"
        disabled={isFeishuProcessing}
        onClick={() => void handleFeishuUnbind()}
        type="button"
      >
        {isFeishuProcessing ? '处理中…' : '解除飞书绑定'}
      </button>
    ) : (
      <button
        className="action-primary"
        disabled={isFeishuProcessing}
        onClick={() => void handleFeishuBind()}
        type="button"
      >
        {isFeishuProcessing ? '处理中…' : '使用飞书绑定'}
      </button>
    )}
  </div>
</section>
```

该 section 作为第三个 `<section>` 放在 AppShell children 的最后位置。保持既有 section 结构不动（员工绑定 + 网格内的账号信息 / 密码管理）。

视觉要点（对应 CONTEXT D-11 / D-12）：
- 外框 `className="surface px-6 py-6 lg:px-7"` 与密码管理 section 对等。
- `eyebrow: '登录方式'` + `section-title: '飞书账号'`。
- 徽标使用 `borderRadius: 999` 圆角 pill；已绑定绿色 `var(--color-success)` + `var(--color-success-bg)`；未绑定灰色 `var(--color-steel)` + `var(--color-bg-subtle)`。
- 按钮：已绑定显示 `action-secondary` 的「解除飞书绑定」，未绑定显示 `action-primary` 的「使用飞书绑定」。
- inline banner（error / success）复用密码管理 section 相同样式（`var(--color-danger-bg)` / `var(--color-success-bg)`）。
- **严禁** `innerHTML` / `dangerouslySetInnerHTML`；所有错误文案通过 `resolveFeishuError('backend', err).message` 经 React 文本节点渲染（T-27.1-06）。

**改动 5 — 确认不改既有功能**：员工绑定 section、密码管理 section、账号信息 section 均保持原样。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust/frontend && npm run lint 2>&1 | tail -5 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "authorizeFeishu, changePassword" frontend/src/pages/Settings.tsx` 命中（Task 1 的 authorizeFeishu + unbindFeishu 已 import）
    - `grep -n "unbindFeishu" frontend/src/pages/Settings.tsx` 返回 >= 2（import + 调用）
    - `grep -n "飞书账号" frontend/src/pages/Settings.tsx` 返回 >= 1
    - `grep -n "使用飞书绑定" frontend/src/pages/Settings.tsx` 返回 >= 1
    - `grep -n "解除飞书绑定" frontend/src/pages/Settings.tsx` 返回 >= 1
    - `grep -n "登录方式" frontend/src/pages/Settings.tsx` 返回 >= 1
    - `grep -n "已绑定" frontend/src/pages/Settings.tsx` 返回 >= 2
    - `grep -n "未绑定" frontend/src/pages/Settings.tsx` 返回 >= 2
    - `grep -n "window.confirm" frontend/src/pages/Settings.tsx` 返回 1
    - `grep -n "解绑后你将无法再使用飞书账号登录" frontend/src/pages/Settings.tsx` 返回 1
    - `grep -n "bindSuccess" frontend/src/pages/Settings.tsx` 返回 1（location.state 监听）
    - `grep -n "resolveFeishuError" frontend/src/pages/Settings.tsx` 返回 >= 2（两个 handler）
    - `grep -n "dangerouslySetInnerHTML\|innerHTML" frontend/src/pages/Settings.tsx` 返回 0
    - `cd frontend && npm run lint` 退出码 0
    - `cd frontend && npm run build` 退出码 0
  </acceptance_criteria>
  <done>
    SettingsPage 新增第三个 section（飞书账号），状态 badge 切换 + 按钮切换 + 错误/成功 banner + window.confirm 二次确认；lint/build 双通过；既有三个 section（员工绑定/账号信息/密码管理）未回归。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: 飞书绑定/解绑人工 UAT（8 步）</name>
  <what-built>
    Phase 27.1 完整闭环：设置页飞书账号 section + bind/unbind 回流 + 分类错误 + AuditLog 审计。
    - Plan 01：FeishuOAuthService.handle_bind_callback / handle_unbind + Settings.feishu_bind_redirect_uri + 7+ unit tests
    - Plan 02：POST /auth/feishu/bind / unbind + GET /auth/feishu/authorize?purpose=bind + AuditLog(feishu_bound/unbound) + 6 integration tests
    - Plan 03：authorizeFeishu(purpose)/bindFeishu/unbindFeishu + feishuErrors 11 类 code + FeishuBindCallbackPage + /settings/feishu-bind-callback 受保护路由 + SettingsPage 「飞书账号」section
  </what-built>
  <files>N/A (manual UAT + automated test suites across all three plans)</files>
  <action>
**前置：**
1. `.env` 中 `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 已配置（Phase 26 遗产）。
2. **`.env` 中新增 `FEISHU_BIND_REDIRECT_URI=http://localhost:5174/settings/feishu-bind-callback`**。
3. **飞书开放平台后台为应用登记第二个 Redirect URL：`http://localhost:5174/settings/feishu-bind-callback`**（D-04）；生产域名同理。
4. 启动后端：`cd /Users/mac/PycharmProjects/Wage_adjust && .venv/bin/python -m uvicorn backend.app.main:app --reload`（8011）。
5. 启动前端：`cd frontend && npm run dev`（5174）。
6. 确认 Redis 可用：`redis-cli ping` 返回 PONG。
7. 准备两个测试账号：**Alice** （Employee.employee_no='E0001'，对应某飞书账号 A）；**Bob**（Employee.employee_no='E0002'，对应某飞书账号 B）。暂都未绑定。

**Step 1 — 未绑定状态入口可见（FAUTH-06 前半）**
- 浏览器用 Alice 密码登录 `/login`。
- 导航到 `/settings`。
- 验证：
  - 出现第三个 section「飞书账号」，badge 显示灰色「未绑定」，按钮显示「使用飞书绑定」。
  - 不显示「解除飞书绑定」按钮。
  - 其他 section（员工绑定 / 账号信息 / 密码管理）正常显示。

**Step 2 — 绑定 happy path（FAUTH-06）**
- 点击「使用飞书绑定」按钮。
- 整页跳转到 `accounts.feishu.cn/open-apis/authen/v1/authorize?...`（飞书授权页）。
- URL query 中 `redirect_uri=http...settings/feishu-bind-callback`（验证 Plan 02 的 purpose=bind 走通）。
- 在飞书 App 中用「与 Alice 对应的飞书账号 A」点同意授权。
- 浏览器跳转到 `http://localhost:5174/settings/feishu-bind-callback?code=...&state=...`。
- 回调页显示「正在完成飞书绑定…」骨架 ~1-2s，然后自动跳回 `/settings`。
- `/settings` 页面显示：badge 变绿色「已绑定」，按钮变「解除飞书绑定」，顶部出现绿色 banner「飞书账号已绑定」。
- 验证 DB（sqlite or psql）：`SELECT feishu_open_id FROM users WHERE email = 'alice@...';` 返回非空 open_id。
- 验证 AuditLog：`SELECT action, detail FROM audit_logs WHERE operator_id = '<alice_id>' AND action = 'feishu_bound';` 命中 1 条，detail 含 `open_id_prefix`（8 字符）+ `open_id_suffix`（8 字符），**无完整 open_id**（FAUTH-09）。

**Step 3 — 冲突 409（FAUTH-08）**
- 退出 Alice，用密码登录 Bob。
- 导航到 `/settings`，点「使用飞书绑定」。
- 在飞书授权页仍用「飞书账号 A」（已被 Alice 占用）同意授权。
- 回调页显示失败卡片，文案「该飞书账号已被其他系统账号绑定，请先在原账号解绑」。
- 点「返回设置」跳回 `/settings`，Bob 的「飞书账号」section 依然显示「未绑定」。
- 验证：`SELECT feishu_open_id FROM users WHERE email = 'bob@...';` 仍为 NULL。
- 验证 AuditLog：**不存在** Bob 的 `feishu_bound` 记录（失败不写成功审计）。

**Step 4 — employee_no 不一致 400（FAUTH-06 安全边界）**
- 在 Bob 的 Employee 表把 employee_no 暂改为 'E9999'（模拟数据不一致）：`UPDATE employees SET employee_no='E9999' WHERE id = '<bob_emp_id>';`
- Bob 重新进 `/settings`，点「使用飞书绑定」。
- 在飞书授权页用「飞书账号 B」（真实 employee_no='E0002'，与 DB 中被改后的 'E9999' 不匹配）同意授权。
- 回调页显示失败文案「飞书工号与当前账号绑定的工号不一致，无法绑定」。
- **记得把 Bob 的 employee_no 改回 E0002**：`UPDATE employees SET employee_no='E0002' WHERE id='<bob_emp_id>';`

**Step 5 — 解绑 happy path 保留 session（FAUTH-07）**
- 用 Alice 登录 `/settings`（此时 Alice 已绑定）。
- 记录当前 localStorage 的 `wage_adjust.access_token`。
- 点「解除飞书绑定」按钮。
- 浏览器弹出原生 confirm 对话框，文案「解绑后你将无法再使用飞书账号登录，需重新绑定。确认解绑？」。
- 点「确定」。
- 按钮文字变「处理中…」，1s 内 badge 切回「未绑定」，出现绿色 banner「已解除飞书绑定」。
- 验证：`SELECT feishu_open_id, token_version FROM users WHERE email = 'alice@...';` → `feishu_open_id=NULL`；token_version **未变化**。
- 验证 localStorage.access_token 未改变。
- 导航到任意其他页面（如 `/employees`）验证当前 session 依然有效（不被踢回 `/login`）。
- 验证 AuditLog：`SELECT action, detail FROM audit_logs WHERE operator_id = '<alice_id>' ORDER BY created_at DESC LIMIT 1;` 返回 `action='feishu_unbound'`，detail 含 prefix/suffix 各 8 字符。

**Step 6 — 解绑二次确认可取消**
- Alice 重新绑定（同 Step 2）后再点「解除飞书绑定」→ confirm 弹窗 → 点「取消」。
- 验证：badge 依然「已绑定」；无 banner；无 AuditLog 写入（不存在新增 `feishu_unbound`）。

**Step 7 — 未登录 /settings/feishu-bind-callback 被 ProtectedRoute 拦截**
- 完全清空 localStorage（DevTools → Application → Clear storage）。
- 直接访问 `http://localhost:5174/settings/feishu-bind-callback?code=x&state=y`。
- 验证：被重定向到 `/login`（`ProtectedRoute` 行为 — D-14）。

**Step 8 — state 键前缀隔离验证（T-27.1-04）**
- 已登录 Alice 的浏览器中。
- 手动 curl 拿登录 state：`curl -s 'http://127.0.0.1:8011/api/v1/auth/feishu/authorize' | jq`（purpose 默认 login）。记下 state 值 `S_LOGIN`。
- Redis 验证：`redis-cli GET feishu_oauth_state:S_LOGIN` 返回 "1"，`redis-cli GET feishu_oauth_state:bind:S_LOGIN` 返回 (nil)。
- 登录拿 bind state：`curl -s -H "Authorization: Bearer <alice_jwt>" 'http://127.0.0.1:8011/api/v1/auth/feishu/authorize?purpose=bind' | jq`。记下 state 值 `S_BIND`。
- `redis-cli GET feishu_oauth_state:bind:S_BIND` 返回 "1"，`redis-cli GET feishu_oauth_state:S_BIND` 返回 (nil)。
- 用 `S_LOGIN` 作为 state POST `/auth/feishu/bind`（bind 路径不应认 login 前缀 state）：应返回 400「无效的 state 参数，请重新发起授权」。反之亦然。

**验证完成后的期望结果：**
- 所有 8 步全部通过 → FAUTH-06/07/08/09 完整闭环，Phase 27.1 可标记 done。
- 任意步骤失败 → 描述失败现象，回到对应 Plan 进行修复（Step 1-6 → Plan 03；Step 7 → App.tsx 路由配置；Step 8 → Plan 01 service 层 state 前缀逻辑）。

**自动化部分（Claude 必须先跑通后再呈现 checkpoint）：**
- `cd /Users/mac/PycharmProjects/Wage_adjust && python -m pytest backend/tests/test_services/test_feishu_oauth_service.py -x` → 0
- `cd /Users/mac/PycharmProjects/Wage_adjust && python -m pytest backend/tests/test_api/test_feishu_oauth_integration.py -x` → 0
- `cd /Users/mac/PycharmProjects/Wage_adjust/frontend && npm run lint` → 0
- `cd /Users/mac/PycharmProjects/Wage_adjust/frontend && npm run build` → 0
  </action>
  <verify>
    <automated>python -m pytest backend/tests/test_services/test_feishu_oauth_service.py -x && python -m pytest backend/tests/test_api/test_feishu_oauth_integration.py -x && cd frontend && npm run lint && npm run build</automated>
  </verify>
  <done>
    8 步人工 UAT 全通过 + 4 条自动化校验全绿 → FAUTH-06/07/08/09 闭环，Phase 27.1 可标记 done。任何步骤失败须回到对应 Plan 修复。
  </done>
  <resume-signal>
    输入「已通过」或描述失败的 Step 编号与现象。
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器 URL 参数 → FeishuBindCallbackPage | code/state 来自飞书跳回不可信；仅透传给后端 Plan 02 端点处理 |
| 浏览器 localStorage → api.ts Axios 拦截器 | JWT 持久化在 localStorage；XSS 可读取但已在 Phase 1 安全加固评估为接受风险 |
| Settings.tsx 用户点击 → /auth/feishu/authorize?purpose=bind | 前端发起的普通 GET 请求携带 JWT（虽然后端 authorize 端点不要求 JWT，但 Axios 默认注入 Bearer header 无副作用） |
| 后端错误 detail → Settings.tsx / FeishuBindCallbackPage UI | 必须经 `resolveFeishuError` 映射为前端静态 COPY，禁止直接渲染后端 message |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27.1-01 | Elevation of Privilege (跨用户绑定) | SettingsPage 绑定按钮 | mitigate | 后端 Plan 02 强制 employee_no 一致校验；前端无 bypass 路径 |
| T-27.1-02 | Elevation of Privilege (open_id 盗用) | FeishuBindCallbackPage | mitigate | 后端 Plan 02 返回 409 → 前端映射为 feishu_already_bound 显示「该飞书账号已被其他系统账号绑定…」，不提供「强制绑定」UI |
| T-27.1-03 | CSRF on unbind | SettingsPage window.confirm | accept | D-10：POST + JWT Bearer 本身已是非简单请求，外站不能跨站发起；二次确认 window.confirm 作为用户意图确认（非安全层）。剩余风险沿用 Phase 12 的 SameSite 策略 |
| T-27.1-04 | Tampering (state 混用) | FeishuBindCallbackPage | mitigate | 后端 Plan 01 state 前缀隔离；前端只透传 code/state，不做 state 路径判断 |
| T-27.1-05 | Information Disclosure (open_id 在前端泄漏) | SettingsPage | mitigate | UI 仅显示状态 badge「已绑定」/「未绑定」，**不显示 `user.feishu_open_id` 明文**（D-12）；refreshProfile 后 open_id 仅存于 UserProfile state，不投影到 DOM |
| T-27.1-06 | XSS (错误文案注入) | Settings.tsx / FeishuBindCallbackPage | mitigate | 所有错误文案都走 `resolveFeishuError('backend', err).message` → 前端静态 COPY 表；React 文本节点自动 HTML escape；`grep dangerouslySetInnerHTML` 必须返回 0 |
| T-27.1-10 | 路由权限缺失 | App.tsx /settings/feishu-bind-callback | mitigate | 注册在 `ProtectedRoute allowedRoles=["admin","hrbp","manager","employee"]` 下；未登录访问被重定向到 /login（Step 7 人工验证） |
| T-27.1-11 | history stack 污染 (code/state 回退可重放) | FeishuBindCallbackPage | mitigate | `navigate(..., { replace: true })` 确保 code/state URL 不进入浏览器历史栈，用户点后退不能重放授权码 |
</threat_model>

<verification>
## Plan-level Verification

- [ ] `cd frontend && npm run lint` 退出码 0
- [ ] `cd frontend && npm run build` 退出码 0
- [ ] `pytest backend/tests/test_services/test_feishu_oauth_service.py -x` 退出码 0（Plan 01 无回归）
- [ ] `pytest backend/tests/test_api/test_feishu_oauth_integration.py -x` 退出码 0（Plan 02 无回归）
- [ ] 8 步 manual UAT 全通过（Task 4 checkpoint）
- [ ] Phase 27 登录路径手动回归通过（/login → 飞书授权 → /auth/feishu/callback → 角色首页；LOGIN-04 不受影响）
</verification>

<success_criteria>
Plan 03 成功当且仅当：

1. 设置页在任一登录角色下均显示「飞书账号」section，badge + 按钮 + banner 三者状态正确切换（未绑定/已绑定）。
2. 未绑定用户点「使用飞书绑定」完成飞书授权后，回流到设置页，badge 自动切为「已绑定」，无需手动刷新页面。
3. 已绑定用户点「解除飞书绑定」弹原生 confirm，确认后 feishu_open_id 清空、session 保留、localStorage JWT 不变。
4. 冲突 409（别账号占用）/employee_no 不一致 400 / 未关联员工 400 均显示分类中文错误，无后端英文原文。
5. `/settings/feishu-bind-callback` 路由未登录访问被 ProtectedRoute 拦截重定向 /login。
6. 8 步人工 UAT 全通过。
7. lint + build 双绿；Phase 27 登录链路无回归。
</success_criteria>

<output>
完成后生成 `.planning/phases/27.1-feishu-binding/27.1-03-SUMMARY.md`，记录：
- 6 个前端文件的 diff 摘要
- 8 步人工 UAT 逐项结果
- T-27.1-01..11 前端侧威胁缓解落地点
- FAUTH-06/07/08/09 完整验收签字
</output>
