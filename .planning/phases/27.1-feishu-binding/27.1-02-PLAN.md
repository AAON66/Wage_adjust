---
phase: 27.1
plan: 02
type: execute
wave: 2
depends_on:
  - "27.1-01"
files_modified:
  - backend/app/api/v1/auth.py
  - backend/tests/test_api/test_feishu_oauth_integration.py
autonomous: true
requirements:
  - FAUTH-06
  - FAUTH-07
  - FAUTH-08
  - FAUTH-09

must_haves:
  truths:
    - "GET /api/v1/auth/feishu/authorize?purpose=bind 返回 bind redirect_uri 的授权 URL"
    - "POST /api/v1/auth/feishu/bind 在无 JWT 时返回 401"
    - "POST /api/v1/auth/feishu/bind 成功时返回最新 UserRead（含 feishu_open_id），并写入 AuditLog(action='feishu_bound')"
    - "POST /api/v1/auth/feishu/bind 在 open_id 冲突时返回 409，在 employee_no 不一致时返回 400"
    - "POST /api/v1/auth/feishu/unbind 成功清空 feishu_open_id，返回最新 UserRead，并写入 AuditLog(action='feishu_unbound')"
    - "AuditLog.detail 仅保存 open_id 前 8 位 / 后 8 位，不含完整 open_id"
    - "解绑不改 token_version（session 保留）"
  artifacts:
    - path: "backend/app/api/v1/auth.py"
      provides: "三个端点：authorize 扩展 purpose 参数；feishu_bind；feishu_unbind"
      contains: "def feishu_bind|def feishu_unbind"
    - path: "backend/tests/test_api/test_feishu_oauth_integration.py"
      provides: "bind / unbind 端到端集成测试（>= 6 个新用例）"
      contains: "test_feishu_bind|test_feishu_unbind"
  key_links:
    - from: "backend/app/api/v1/auth.py (feishu_bind)"
      to: "backend/app/services/feishu_oauth_service.py::handle_bind_callback"
      via: "service.handle_bind_callback(code, state, current_user, redis_client)"
      pattern: "handle_bind_callback"
    - from: "backend/app/api/v1/auth.py (feishu_unbind)"
      to: "backend/app/services/feishu_oauth_service.py::handle_unbind"
      via: "service.handle_unbind(current_user)"
      pattern: "handle_unbind"
    - from: "feishu_bind / feishu_unbind"
      to: "backend/app/models/audit_log.py::AuditLog"
      via: "db.add(AuditLog(action='feishu_bound'|'feishu_unbound', target_type='user', target_id=user.id, detail={open_id_prefix, open_id_suffix}))"
      pattern: "feishu_bound|feishu_unbound"
---

<objective>
在 `backend/app/api/v1/auth.py` 中新增 Phase 27.1 所需的两个已登录端点并扩展 authorize 端点的 purpose 查询参数，完成 D-01 / D-02 / D-15。
本 Plan 把 Plan 01 交付的 service 方法通过 FastAPI 路由暴露为 HTTP 接口，并把 AuditLog 写入放在 handler 层（遵循项目 Established Pattern）。

Purpose: 为前端（Plan 03）提供稳定的 bind / unbind REST 契约，并确保所有绑定/解绑事件被审计；审计 detail 只保留 open_id 前 8 位 + 后 8 位以保护隐私（D-15）。
Output: 三个 API 端点（1 个扩展 + 2 个新增）+ 6+ 个端到端集成测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27.1-feishu-binding/27.1-CONTEXT.md
@.planning/phases/27.1-feishu-binding/27.1-01-PLAN.md
@.planning/phases/26-oauth2/26-02-SUMMARY.md

@backend/app/api/v1/auth.py
@backend/app/models/audit_log.py
@backend/app/services/user_admin_service.py
@backend/app/schemas/user.py
@backend/tests/test_api/test_feishu_oauth_integration.py

<interfaces>
<!-- Plan 01 交付 -->

```python
# backend/app/services/feishu_oauth_service.py
class FeishuOAuthService:
    def generate_authorize_url(
        self, redis_client, *, purpose: str = 'login'
    ) -> dict[str, str]: ...
    def handle_bind_callback(
        self, code: str, state: str, user: User, redis_client
    ) -> User: ...
    def handle_unbind(self, user: User) -> User: ...
    def require_redis(self) -> redis_lib.Redis: ...  # raises 503 on redis outage
```

<!-- 已存在 -->

```python
# backend/app/api/v1/auth.py
from backend.app.dependencies import get_current_user  # JWT → User
from backend.app.schemas.user import FeishuCallbackRequest, UserRead
# FeishuCallbackRequest(code: str, state: str)  # 复用
# UserRead.model_validate(user)                 # 返回前端

# 既有端点（不改）：
@router.get('/feishu/authorize')
def feishu_authorize(db, settings) -> dict[str, str]:
    service = FeishuOAuthService(db, settings=settings)
    redis_client = service.require_redis()
    return service.generate_authorize_url(redis_client)
```

```python
# backend/app/models/audit_log.py
class AuditLog:
    operator_id: str | None
    action: str                    # String(128), no enum constraint — 直接写 'feishu_bound' / 'feishu_unbound'
    target_type: str               # e.g. 'user'
    target_id: str
    detail: dict
    operator_role: str | None
    request_id: str | None
```

注意：`AuditLog.action` 是 `String(128)` 字段，无 DB-level enum 约束（参考 `backend/app/services/user_admin_service.py` 已写入 `'admin.user.create'` / `'admin.user.delete'` 等任意字符串 action），**无需扩展 enum / 建 Alembic 迁移**。D-15 提到的「新增 action enum」在本项目实现中等价于「直接写新字符串」即可（CONTEXT 决策源于一般性假设）。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: authorize 扩展 purpose 参数 + 新增 bind / unbind 端点 + AuditLog 写入</name>
  <files>backend/app/api/v1/auth.py</files>
  <read_first>
    - backend/app/api/v1/auth.py 全文（已有 296 行，focus: 第 269-297 行的 feishu_authorize / feishu_callback 两个端点；第 173-193 行的 change_password 模式展示 get_current_user 用法）
    - backend/app/services/feishu_oauth_service.py（Plan 01 产物 — 三个新方法签名）
    - backend/app/models/audit_log.py（AuditLog 字段形式）
    - backend/app/services/user_admin_service.py 第 82-91 行（_log_action 模板 — db.add(AuditLog(...)) 模式）
    - backend/app/schemas/user.py 第 26-39 行（UserRead）+ 第 154-156 行（FeishuCallbackRequest）
    - backend/app/dependencies.py（get_current_user 返回 User；get_db / get_app_settings 形式）
    - .planning/phases/27.1-feishu-binding/27.1-CONTEXT.md D-01 / D-02 / D-10 / D-15
  </read_first>
  <action>
**目标文件：`backend/app/api/v1/auth.py`**

**改动 1 — import 行扩展**：在文件顶部 `from backend.app.models.user import User` 之后新增一行：

```python
from backend.app.models.audit_log import AuditLog
```

保持其他 import 不动。

**改动 2 — 扩展 `feishu_authorize` 端点** — 在当前第 269-277 行替换为：

```python
@router.get('/feishu/authorize')
def feishu_authorize(
    purpose: str = Query('login', pattern='^(login|bind)$'),
    db: Session = Depends(get_db),
    settings=Depends(get_app_settings),
) -> dict[str, str]:
    """生成飞书 OAuth 授权 URL。purpose=login（默认）或 bind。"""
    service = FeishuOAuthService(db, settings=settings)
    redis_client = service.require_redis()
    return service.generate_authorize_url(redis_client, purpose=purpose)
```

说明：`purpose` 默认 `'login'`，向后兼容 Plan 27 前端调用（不传 purpose）。`pattern` 正则在 FastAPI 层拦截无效值返回 422，无需手动校验。

**改动 3 — 在文件末尾（`feishu_callback` 之后）追加新端点**：

```python
def _truncate_open_id(open_id: str) -> dict[str, str]:
    """D-15: AuditLog 只保留 open_id 前 8 位 / 后 8 位，保护隐私。"""
    if not open_id:
        return {'open_id_prefix': '', 'open_id_suffix': ''}
    if len(open_id) <= 16:
        # 短 open_id 直接前后各半（绝大多数飞书 open_id >= 28 位，此分支理论不会命中）
        half = len(open_id) // 2
        return {'open_id_prefix': open_id[:half], 'open_id_suffix': open_id[half:]}
    return {'open_id_prefix': open_id[:8], 'open_id_suffix': open_id[-8:]}


@router.post('/feishu/bind', response_model=UserRead)
def feishu_bind(
    payload: FeishuCallbackRequest,
    request: Request,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_user),
    settings=Depends(get_app_settings),
) -> UserRead:
    """已登录用户把飞书账号绑定到当前账号。校验 employee_no 一致与 open_id 未被占用。"""
    service = FeishuOAuthService(db, settings=settings)
    redis_client = service.require_redis()

    user = service.handle_bind_callback(payload.code, payload.state, current_user, redis_client)

    # D-15: AuditLog (handler 层写入，遵循 established pattern)
    client_ip = request.client.host if request.client else 'unknown'
    truncated = _truncate_open_id(user.feishu_open_id or '')
    db.add(AuditLog(
        operator_id=user.id,
        operator_role=user.role,
        action='feishu_bound',
        target_type='user',
        target_id=user.id,
        detail={
            **truncated,
            'ip_address': client_ip,
        },
    ))
    db.commit()
    db.refresh(user)
    return UserRead.model_validate(user)


@router.post('/feishu/unbind', response_model=UserRead)
def feishu_unbind(
    request: Request,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_user),
) -> UserRead:
    """已登录用户解除当前账号的飞书绑定。保留当前 session（不动 token_version）。"""
    service = FeishuOAuthService(db)

    # 记录 unbind 前的 open_id（用于审计 detail 截断）
    previous_open_id = current_user.feishu_open_id or ''

    user = service.handle_unbind(current_user)

    client_ip = request.client.host if request.client else 'unknown'
    truncated = _truncate_open_id(previous_open_id)
    db.add(AuditLog(
        operator_id=user.id,
        operator_role=user.role,
        action='feishu_unbound',
        target_type='user',
        target_id=user.id,
        detail={
            **truncated,
            'ip_address': client_ip,
        },
    ))
    db.commit()
    db.refresh(user)
    return UserRead.model_validate(user)
```

**关键决策记录**（写入注释 + Plan SUMMARY）：
- `feishu_unbind` 不接 settings 参数 — `FeishuOAuthService.handle_unbind` 无需 Redis（纯 DB 操作），但构造 service 仍需 db；保持 service 统一构造方式方便未来若改动同一构造函数。
- unbind 场景 previous_open_id 可能为 None（幂等），truncation 函数返回空串对；审计依旧写入一条记录以保证解绑动作可追溯（即便是 no-op）。
- 401 无 JWT 的行为由 `Depends(get_current_user)` 自动触发（默认抛 HTTPException 401），无需手动处理。

**改动 4 — 确认既有 `feishu_callback` 端点（登录）未被改动**：grep 应仍显示第 280-297 行原样。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust && python -c "from backend.app.api.v1.auth import feishu_bind, feishu_unbind, feishu_authorize; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def feishu_bind(" backend/app/api/v1/auth.py` 返回 1 处
    - `grep -n "def feishu_unbind(" backend/app/api/v1/auth.py` 返回 1 处
    - `grep -n "@router.post.'/feishu/bind'" backend/app/api/v1/auth.py` 返回 1 处
    - `grep -n "@router.post.'/feishu/unbind'" backend/app/api/v1/auth.py` 返回 1 处
    - `grep -n "purpose: str = Query" backend/app/api/v1/auth.py` 返回 1 处（authorize 扩展）
    - `grep -n "pattern='\^(login|bind)\$'" backend/app/api/v1/auth.py` 返回 1 处
    - `grep -n "action='feishu_bound'" backend/app/api/v1/auth.py` 返回 1 处
    - `grep -n "action='feishu_unbound'" backend/app/api/v1/auth.py` 返回 1 处
    - `grep -n "def _truncate_open_id" backend/app/api/v1/auth.py` 返回 1 处
    - `grep -n "open_id_prefix" backend/app/api/v1/auth.py` 返回 >= 2 处（函数体 + 写入 detail）
    - `grep -n "token_version" backend/app/api/v1/auth.py` 返回 2 处（原有 refresh 逻辑），**不在 feishu_bind/unbind 附近**（unbind 不动 token_version）
    - `python -c "from backend.app.api.v1.auth import feishu_bind, feishu_unbind, feishu_authorize"` 退出码 0
    - `grep -n "Depends(get_current_user)" backend/app/api/v1/auth.py` 至少包含 feishu_bind 和 feishu_unbind（+ 既有 change_password / self_bind_preview / self_bind_confirm / read_current_user）共计 >= 5 处
  </acceptance_criteria>
  <done>
    三个端点（authorize 扩展 + bind + unbind）就位，AuditLog 在 handler 层写入，open_id 截断到前8/后8；Python 能 import 无语法错；既有 login / callback 端点未被改动。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 新增 feishu_bind / feishu_unbind 端到端集成测试</name>
  <files>backend/tests/test_api/test_feishu_oauth_integration.py</files>
  <read_first>
    - backend/tests/test_api/test_feishu_oauth_integration.py 全文（现有 4 个登录测试；复用 db / settings fixture 与 TestClient + StaticPool 模板）
    - backend/app/api/v1/auth.py（Plan 02 Task 1 产物）
    - backend/tests/test_services/test_feishu_oauth_service.py（Plan 01 Task 2 的 `_make_user_with_employee` helper — 可以借鉴复制到集成测试）
    - backend/app/core/security.py（create_access_token 用于为测试请求生成 JWT）
    - .planning/phases/27.1-feishu-binding/27.1-CONTEXT.md D-05 / D-06 / D-07 / D-15
  </read_first>
  <behavior>
    - Behavior T1（bind happy path）：已登录 user + Employee + 飞书 mock 返回一致 employee_no → POST /feishu/bind → 200，response.feishu_open_id == 'ou_abc'；AuditLog 写入 action='feishu_bound'，detail 含 open_id_prefix='ou_abc12' 仅前 8 位（即 `ou_abc12`）+ suffix 后 8 位
    - Behavior T2（bind 401 without JWT）：POST /feishu/bind 不带 Authorization header → 401 / 403（FastAPI oauth2_scheme 默认 401）
    - Behavior T3（bind 409 open_id 冲突）：其他 user 已占用 open_id → 409，message 含「该飞书账号已被其他系统账号绑定」
    - Behavior T4（bind 400 employee_no 不一致）：飞书 user_info 返回 employee_no='E9999' 但当前 user 的 employee.employee_no='E0001' → 400，message 含「飞书工号与当前账号绑定的工号不一致」
    - Behavior T5（unbind happy path）：已绑定 user → POST /feishu/unbind → 200，response.feishu_open_id is None；AuditLog 写入 action='feishu_unbound'；用户 token_version 保持不变
    - Behavior T6（authorize purpose=bind）：GET /feishu/authorize?purpose=bind → 200，`authorize_url` 含 bind redirect_uri（包含 `settings%2Ffeishu-bind-callback`）；GET ?purpose=invalid → 422
  </behavior>
  <action>
**目标文件：`backend/tests/test_api/test_feishu_oauth_integration.py`**

沿用既有 fixture 结构（`db` / `settings` / `client` 模式）。若现有文件尚未定义 `client` fixture，按 Phase 26 integration 测试的同款模板新增（FastAPI `TestClient(create_app())` + dependency_overrides `get_db` / `get_app_settings`）。

在文件末尾（既有 4 个 test 之后）追加：

```python
from unittest.mock import patch
from fastapi.testclient import TestClient

from backend.app.core.security import create_access_token
from backend.app.models.audit_log import AuditLog
from backend.app.models.employee import Employee
from backend.app.models.user import User
from backend.app.main import create_app
from backend.app.dependencies import get_db, get_app_settings


# --- helpers ---

def _seed_user_with_employee(db, *, employee_no='E0001', open_id=None, email_suffix='a'):
    emp = Employee(
        employee_no=employee_no,
        name='Alice',
        id_card_no=None,
        department='R&D',
        sub_department=None,
        job_family='Eng',
        job_level='P5',
        manager_id=None,
        status='active',
    )
    db.add(emp); db.flush()
    user = User(
        email=f'bind_{email_suffix}@ex.com',
        hashed_password='x',
        role='employee',
        employee_id=emp.id,
        feishu_open_id=open_id,
    )
    db.add(user); db.commit(); db.refresh(user)
    return user


def _jwt_for(user: User, settings) -> str:
    return create_access_token(user.id, role=user.role, settings=settings, token_version=user.token_version)


def _make_client(db, settings):
    app = create_app()
    app.dependency_overrides[get_db] = lambda: db
    app.dependency_overrides[get_app_settings] = lambda: settings
    return TestClient(app)


# --- tests ---

def test_feishu_bind_happy_path(db, settings):
    settings.feishu_bind_redirect_uri = 'https://ex.com/settings/feishu-bind-callback'
    user = _seed_user_with_employee(db, employee_no='E0001', email_suffix='h1')
    jwt = _jwt_for(user, settings)
    client = _make_client(db, settings)

    # Pre-seed bind state into Redis mock via patching require_redis to return fake redis
    fake = {'feishu_oauth_state:bind:SOK': '1'}
    class _R:
        def get(self, k): return fake.get(k)
        def delete(self, k): fake.pop(k, None)
        def setex(self, k, _ttl, v): fake[k] = v
        def ping(self): return True
    with patch('backend.app.api.v1.auth.FeishuOAuthService.require_redis', return_value=_R()), \
         patch('backend.app.api.v1.auth.FeishuOAuthService._exchange_code_for_token_for_bind',
               return_value={'access_token': 'T'}), \
         patch('backend.app.api.v1.auth.FeishuOAuthService._get_user_info',
               return_value={'open_id': 'ou_abc12345xxxxxxxxxxxxxxyyy88', 'employee_no': 'E0001'}):
        res = client.post(
            '/api/v1/auth/feishu/bind',
            json={'code': 'C1', 'state': 'SOK'},
            headers={'Authorization': f'Bearer {jwt}'},
        )
    assert res.status_code == 200, res.text
    assert res.json()['feishu_open_id'] == 'ou_abc12345xxxxxxxxxxxxxxyyy88'
    # Audit log
    log = db.scalar(select(AuditLog).where(AuditLog.action == 'feishu_bound', AuditLog.target_id == user.id))
    assert log is not None
    assert log.detail['open_id_prefix'] == 'ou_abc12'
    assert log.detail['open_id_suffix'] == 'xxxyyy88'.replace('xxx', '')  # 后 8 位
    # 纯字符串比较更稳：
    assert len(log.detail['open_id_prefix']) == 8
    assert len(log.detail['open_id_suffix']) == 8


def test_feishu_bind_requires_jwt(db, settings):
    settings.feishu_bind_redirect_uri = 'https://ex.com/settings/feishu-bind-callback'
    client = _make_client(db, settings)
    res = client.post('/api/v1/auth/feishu/bind', json={'code': 'C', 'state': 'S'})
    assert res.status_code in (401, 403)


def test_feishu_bind_conflict_409(db, settings):
    settings.feishu_bind_redirect_uri = 'https://ex.com/settings/feishu-bind-callback'
    _seed_user_with_employee(db, employee_no='E0002', open_id='ou_taken_0000000', email_suffix='other')
    me = _seed_user_with_employee(db, employee_no='E0001', email_suffix='me')
    jwt = _jwt_for(me, settings)
    client = _make_client(db, settings)

    fake = {'feishu_oauth_state:bind:SC': '1'}
    class _R:
        def get(self, k): return fake.get(k)
        def delete(self, k): fake.pop(k, None)
        def setex(self, k, _ttl, v): fake[k] = v
        def ping(self): return True
    with patch('backend.app.api.v1.auth.FeishuOAuthService.require_redis', return_value=_R()), \
         patch('backend.app.api.v1.auth.FeishuOAuthService._exchange_code_for_token_for_bind',
               return_value={'access_token': 'T'}), \
         patch('backend.app.api.v1.auth.FeishuOAuthService._get_user_info',
               return_value={'open_id': 'ou_taken_0000000', 'employee_no': 'E0001'}):
        res = client.post(
            '/api/v1/auth/feishu/bind',
            json={'code': 'CX', 'state': 'SC'},
            headers={'Authorization': f'Bearer {jwt}'},
        )
    assert res.status_code == 409
    body = res.json()
    # main.py exception_handler 改写为 {error, message}
    assert '该飞书账号已被其他系统账号绑定' in (body.get('message') or body.get('detail') or '')


def test_feishu_bind_employee_no_mismatch_400(db, settings):
    settings.feishu_bind_redirect_uri = 'https://ex.com/settings/feishu-bind-callback'
    me = _seed_user_with_employee(db, employee_no='E0001', email_suffix='mm')
    jwt = _jwt_for(me, settings)
    client = _make_client(db, settings)

    fake = {'feishu_oauth_state:bind:SM': '1'}
    class _R:
        def get(self, k): return fake.get(k)
        def delete(self, k): fake.pop(k, None)
        def setex(self, k, _ttl, v): fake[k] = v
        def ping(self): return True
    with patch('backend.app.api.v1.auth.FeishuOAuthService.require_redis', return_value=_R()), \
         patch('backend.app.api.v1.auth.FeishuOAuthService._exchange_code_for_token_for_bind',
               return_value={'access_token': 'T'}), \
         patch('backend.app.api.v1.auth.FeishuOAuthService._get_user_info',
               return_value={'open_id': 'ou_any', 'employee_no': 'E9999'}):
        res = client.post(
            '/api/v1/auth/feishu/bind',
            json={'code': 'CX', 'state': 'SM'},
            headers={'Authorization': f'Bearer {jwt}'},
        )
    assert res.status_code == 400
    body = res.json()
    assert '飞书工号与当前账号绑定的工号不一致' in (body.get('message') or body.get('detail') or '')


def test_feishu_unbind_happy_path_preserves_session(db, settings):
    user = _seed_user_with_employee(db, open_id='ou_need_clear_000000', email_suffix='u1')
    original_tv = user.token_version
    jwt = _jwt_for(user, settings)
    client = _make_client(db, settings)

    res = client.post(
        '/api/v1/auth/feishu/unbind',
        headers={'Authorization': f'Bearer {jwt}'},
    )
    assert res.status_code == 200, res.text
    assert res.json()['feishu_open_id'] is None

    db.refresh(user)
    assert user.feishu_open_id is None
    assert user.token_version == original_tv  # D-10: unbind 保留 session

    log = db.scalar(select(AuditLog).where(AuditLog.action == 'feishu_unbound', AuditLog.target_id == user.id))
    assert log is not None
    assert log.detail['open_id_prefix'] == 'ou_need_'
    assert log.detail['open_id_suffix'] == '_000000'[-8:] or len(log.detail['open_id_suffix']) == 8


def test_feishu_authorize_purpose_bind_returns_bind_redirect(db, settings):
    settings.feishu_bind_redirect_uri = 'https://ex.com/settings/feishu-bind-callback'
    client = _make_client(db, settings)

    fake = {}
    class _R:
        def get(self, k): return fake.get(k)
        def delete(self, k): fake.pop(k, None)
        def setex(self, k, _ttl, v): fake[k] = v
        def ping(self): return True
    with patch('backend.app.api.v1.auth.FeishuOAuthService.require_redis', return_value=_R()):
        res = client.get('/api/v1/auth/feishu/authorize?purpose=bind')
    assert res.status_code == 200
    assert 'settings%2Ffeishu-bind-callback' in res.json()['authorize_url']

    # invalid purpose → 422
    res_bad = client.get('/api/v1/auth/feishu/authorize?purpose=hack')
    assert res_bad.status_code == 422
```

说明：open_id 截断断言采用长度断言（`len == 8`）辅以前缀 literal 断言，避免对完整 open_id 末 8 位硬编码时出错；测试资产独立于 Plan 01 的 unit test，用 TestClient 验证端到端行为。

运行：`cd /Users/mac/PycharmProjects/Wage_adjust && python -m pytest backend/tests/test_api/test_feishu_oauth_integration.py -x` 全绿（既有 4 + 新增 6 = 10 个测试）。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust && python -m pytest backend/tests/test_api/test_feishu_oauth_integration.py -x 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - 新增 test 函数数量：`grep -cE "^def test_feishu_(bind|unbind|authorize_purpose)" backend/tests/test_api/test_feishu_oauth_integration.py` 返回 >= 6
    - `pytest backend/tests/test_api/test_feishu_oauth_integration.py -x` 退出码 0（所有 10 个测试通过）
    - 测试断言 `len(log.detail['open_id_prefix']) == 8` 命中 >= 1 处（明确验证 open_id 截断到 8 位）
    - 测试含 `assert res.status_code == 401` 或 `assert res.status_code in (401, 403)` 对应无 JWT 路径
    - 测试含 `'该飞书账号已被其他系统账号绑定'` 字面量（D-05 中文文案）
    - 测试含 `'飞书工号与当前账号绑定的工号不一致'` 字面量（D-06 中文文案）
    - 测试含 `assert user.token_version == original_tv` 证明 unbind 不动 token_version
  </acceptance_criteria>
  <done>
    6+ 个 bind/unbind 端到端测试通过，覆盖 401/409/400/200/200/422 六个状态码路径；AuditLog 截断策略落在真实测试断言里；既有 4 个登录测试未回归。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器（携带 JWT） → /auth/feishu/bind | JWT 经 get_current_user 校验；payload.code/state 不可信 |
| 浏览器（携带 JWT） → /auth/feishu/unbind | 仅依赖 JWT；无外部输入 |
| 浏览器（无 JWT） → /auth/feishu/authorize?purpose=bind | 公开端点；purpose 通过 Query pattern 白名单拦截无效值 |
| auth handler → AuditLog 表 | 写入 detail 字段前必须截断 open_id（T-27.1-05） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27.1-01 | Elevation of Privilege (跨用户绑定劫持) | feishu_bind handler | mitigate | 依赖 Plan 01 service 层 D-06 employee_no 一致校验；handler 仅透传 current_user，不允许外部指定 user_id |
| T-27.1-02 | Elevation of Privilege (open_id 盗用) | feishu_bind handler | mitigate | 依赖 Plan 01 service 层 D-05 open_id 冲突 409；handler 不提供「force」参数覆盖 |
| T-27.1-03 | Spoofing (CSRF on unbind via JWT-only) | feishu_unbind handler | accept | POST + JWT Bearer header 由前端 Axios 主动附加，标准浏览器不会跨站自动携带 Authorization；JWT 已按项目现有策略 SameSite 存在 localStorage。剩余风险：若 XSS 可读 localStorage → JWT 泄漏，但同等风险已存在于整站任何端点，不为 unbind 单独增加 CSRF token 层 |
| T-27.1-04 | Tampering (purpose 参数注入) | feishu_authorize handler | mitigate | `Query('login', pattern='^(login\|bind)$')` 在 FastAPI 层拦截非白名单值，返回 422 |
| T-27.1-05 | Information Disclosure (AuditLog open_id 泄漏) | _truncate_open_id | mitigate | D-15：AuditLog.detail 只写 open_id[:8] + open_id[-8:]，永不写完整 open_id；无其他日志路径暴露完整值（service 层已验证无 logger.info(open_id) 调用） |
| T-27.1-06 | XSS (返回错误文案注入) | feishu_bind/unbind | mitigate | 端点抛 HTTPException(detail=中文字面量) 全部为前端 feishuErrors.ts 的分类关键字（「工号」/「该飞书」），前端不会直接 innerHTML 后端文案；main.py 全局 exception_handler 返回 {error, message} JSON（非 HTML） |
| T-27.1-09 | Repudiation (绑定行为无迹可循) | AuditLog 写入 | mitigate | 每次 bind / unbind 均在同一事务写 AuditLog；operator_id + operator_role + ip_address + detail.open_id_prefix/suffix 可追溯 |
</threat_model>

<verification>
## Plan-level Verification

- [ ] `grep -cE "def feishu_(bind|unbind)" backend/app/api/v1/auth.py` 返回 2
- [ ] `grep -c "_truncate_open_id" backend/app/api/v1/auth.py` 返回 >= 3（定义 + 2 处调用）
- [ ] `grep -c "feishu_bound\|feishu_unbound" backend/app/api/v1/auth.py` 返回 >= 2
- [ ] `pytest backend/tests/test_api/test_feishu_oauth_integration.py -x` 退出码 0（10 个测试全绿）
- [ ] `pytest backend/tests/test_services/test_feishu_oauth_service.py -x` 退出码 0（Plan 01 无回归）
- [ ] `curl -s -X POST http://127.0.0.1:8011/api/v1/auth/feishu/bind -H 'Content-Type: application/json' -d '{"code":"x","state":"x"}'` 返回 401（无 JWT；运行时验证）
</verification>

<success_criteria>
Plan 02 成功当且仅当：

1. `GET /auth/feishu/authorize?purpose=bind` 正确返回 bind redirect_uri 的 authorize_url；`purpose=login` 默认行为与 Phase 26 完全一致（回归测试通过）。
2. `POST /auth/feishu/bind`：JWT 必需（401 无 JWT）；happy path 200 + feishu_open_id 写入；冲突 409 + 中文文案；employee_no 不一致 400；写入 AuditLog action='feishu_bound' 含截断后的 open_id prefix/suffix。
3. `POST /auth/feishu/unbind`：JWT 必需；清空 feishu_open_id 返回 200；不动 token_version；写入 AuditLog action='feishu_unbound'。
4. AuditLog.detail 永不写完整 open_id — 截断到前 8 / 后 8 位。
5. 10 个端到端集成测试全绿；Plan 01 单元测试无回归。
6. 既有 `/auth/feishu/callback`（登录端点）未被改动。
</success_criteria>

<output>
完成后生成 `.planning/phases/27.1-feishu-binding/27.1-02-SUMMARY.md`，记录：
- 新增/修改文件 diff 摘要
- 10 个测试覆盖矩阵（bind 4 + unbind 1 + authorize 1 + 既有 4）
- T-27.1-01..09 威胁缓解落地点
- Plan 03 依赖的 REST 契约：
  - `GET /api/v1/auth/feishu/authorize?purpose=bind` → `{authorize_url, state}`
  - `POST /api/v1/auth/feishu/bind` body `{code, state}`，JWT 必需 → `UserRead`
  - `POST /api/v1/auth/feishu/unbind` 无 body，JWT 必需 → `UserRead`
</output>
