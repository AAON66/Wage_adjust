# Phase 9: 飞书考勤集成 - 讨论日志

> **仅作审计记录。** 决策已记录在 CONTEXT.md 中。

**日期:** 2026-03-29
**阶段:** 09-feishu-attendance-integration
**讨论的领域:** API 认证、字段映射、定时同步、考勤展示、失败处理、Mock、考勤页面功能、页面权限、配置布局、同步粒度

---

## 飞书 API 认证与安全
- **选中:** 数据库加密存储（复用已有加密方案）

## 字段映射与数据模型
- **选中:** UI 可配置映射

## 定时同步方案
- **选中:** APScheduler（内嵌 FastAPI 进程）

## 考勤展示位置与样式
- **选中:** 两个位置——人工调薪窗口内嵌 + 独立考勤管理页面卡片面板

## 同步失败处理
- **选中:** 自动重试 3 次（间隔递增），全部失败后记录错误

## 开发环境 Mock
- **选中:** 不需要 Mock

## 考勤管理页面功能
- **选中:** 员工考勤卡片面板 + 同步状态卡片 + 手动同步按钮 + 飞书配置入口

## 考勤页面权限
- **选中:** admin + hrbp 可见，飞书配置仅 admin

## 飞书配置页面布局
- **选中:** 单页表单（连接配置 + 字段映射）

## 同步粒度
- **选中:** 默认增量拉取，手动提供全量同步和增量同步两个按钮

## 延后事项
- 考勤数据影响调薪计算
- 考勤数据导出
- 飞书审批流对接
- Mock 数据
