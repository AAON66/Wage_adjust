---
phase: 09-feishu-attendance-integration
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - backend/app/services/feishu_service.py
  - backend/app/services/attendance_service.py
  - backend/app/scheduler/feishu_scheduler.py
  - backend/app/scheduler/__init__.py
  - backend/app/api/v1/feishu.py
  - backend/app/api/v1/attendance.py
  - backend/app/api/v1/router.py
  - backend/app/main.py
autonomous: true
requirements:
  - ATT-01
  - ATT-02
  - ATT-03
  - ATT-05
  - ATT-06
  - ATT-07
user_setup:
  - service: feishu
    why: "飞书多维表格 API 认证"
    env_vars:
      - name: FEISHU_ENCRYPTION_KEY
        source: "自行生成 32 字节 base64 密钥: python -c \"import os, base64; print(base64.b64encode(os.urandom(32)).decode())\""
    dashboard_config:
      - task: "创建飞书自建应用"
        location: "飞书开放平台 -> 创建应用 -> 企业自建应用"
      - task: "开通 bitable:app 和 bitable:record:read 权限"
        location: "飞书开放平台 -> 应用权限 -> 多维表格"

must_haves:
  truths:
    - "FeishuService 能通过飞书 API 获取 tenant_access_token 并自动刷新"
    - "FeishuService 能分页拉取多维表格记录并按字段映射转换"
    - "sync_attendance 支持 full 和 incremental 两种模式"
    - "增量同步使用 source_modified_at 水印（而非 synced_at），带 5 分钟重叠窗口，通过 feishu_record_id 确定性去重"
    - "同步失败自动重试 3 次（递增间隔 5/15/45 秒）"
    - "手动同步 API 端点可触发全量或增量同步"
    - "POST /api/v1/feishu/test-connection 端点验证飞书凭证有效性"
    - "APScheduler 在 FastAPI lifespan 中启动定时同步任务（单 worker 部署假设）"
    - "GET /api/v1/attendance/{employee_id} 返回单员工考勤概览含 synced_at"
    - "GET /api/v1/attendance/ HRBP 仅能查看自己部门的考勤数据"
    - "并发同步防护基于 FeishuSyncLog status='running' 数据库查询，而非 threading.Lock"
  artifacts:
    - path: "backend/app/services/feishu_service.py"
      provides: "飞书 API 调用 + Token 管理 + 数据同步"
      contains: "class FeishuService"
    - path: "backend/app/services/attendance_service.py"
      provides: "考勤查询服务（含 AccessScopeService 部门过滤）"
      contains: "class AttendanceService"
    - path: "backend/app/scheduler/feishu_scheduler.py"
      provides: "APScheduler 定时任务"
      contains: "start_scheduler"
    - path: "backend/app/api/v1/feishu.py"
      provides: "飞书配置 + 同步 + 测试连接 API 端点"
      contains: "router"
    - path: "backend/app/api/v1/attendance.py"
      provides: "考勤查询 API 端点（含部门作用域过滤）"
      contains: "router"
  key_links:
    - from: "backend/app/services/feishu_service.py"
      to: "https://open.feishu.cn/open-apis/bitable/v1/apps"
      via: "httpx POST 调用飞书 Search Records API"
      pattern: "httpx\\.post.*bitable"
    - from: "backend/app/services/feishu_service.py"
      to: "backend/app/models/feishu_sync_log.py"
      via: "并发防护: 查询 status='running' 拒绝重复同步"
      pattern: "FeishuSyncLog.*running"
    - from: "backend/app/scheduler/feishu_scheduler.py"
      to: "backend/app/services/feishu_service.py"
      via: "定时任务调用 sync_attendance"
      pattern: "sync_attendance|run_incremental_sync"
    - from: "backend/app/api/v1/feishu.py"
      to: "backend/app/services/feishu_service.py"
      via: "API 端点调用服务层"
      pattern: "FeishuService"
    - from: "backend/app/api/v1/attendance.py"
      to: "backend/app/services/access_scope_service.py"
      via: "HRBP 部门作用域过滤"
      pattern: "AccessScopeService|ensure_employee_access"
    - from: "backend/app/main.py"
      to: "backend/app/scheduler/feishu_scheduler.py"
      via: "lifespan 启动/停止调度器"
      pattern: "start_scheduler|stop_scheduler"
---

<objective>
服务层 + API + 调度器 — 实现飞书 API 对接、考勤同步逻辑、手动/定时触发机制和数据查询端点

Purpose: 完成飞书考勤集成的全部后端逻辑，包括 Token 管理、分页拉取、增量/全量同步、重试机制、并发防护、定时调度和 REST API。
Output: FeishuService + AttendanceService + APScheduler 调度器 + 5 个新 API 端点（含 test-connection）

**Review concerns addressed:**
- [HIGH] Scheduler concurrency: 并发防护改用 FeishuSyncLog status='running' 数据库查询（替代 threading.Lock），支持多进程安全；文档化单 worker 部署假设
- [HIGH] Incremental sync semantics: 使用 source_modified_at 水印字段，5 分钟重叠窗口，feishu_record_id 确定性去重
- [MEDIUM] API contract gap: POST /feishu/test-connection 端点显式列出，返回 `{ success: bool, message: str }`
- [MEDIUM] HRBP department scoping: attendance 列表端点使用 AccessScopeService 进行部门级过滤
- [MEDIUM] Secret masking: 配置读取 API 使用服务层方法生成 app_secret_masked，绝不返回明文
</objective>

<execution_context>
@C:/Users/AAON/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/AAON/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-feishu-attendance-integration/09-CONTEXT.md
@.planning/phases/09-feishu-attendance-integration/09-RESEARCH.md
@.planning/phases/09-feishu-attendance-integration/09-01-SUMMARY.md

<interfaces>
<!-- Plan 01 创建的类型契约 -->

From backend/app/models/feishu_config.py:
```python
class FeishuConfig(UUIDPrimaryKeyMixin, CreatedAtMixin, UpdatedAtMixin, Base):
    __tablename__ = 'feishu_configs'
    app_id: Mapped[str]
    encrypted_app_secret: Mapped[str]
    bitable_app_token: Mapped[str]
    bitable_table_id: Mapped[str]
    field_mapping: Mapped[str]  # JSON
    sync_hour: Mapped[int]
    sync_minute: Mapped[int]
    is_active: Mapped[bool]
    # 方法: encrypt_secret(plaintext, key) -> str
    # 方法: decrypt_secret(ciphertext, key) -> str
```

From backend/app/models/attendance_record.py:
```python
class AttendanceRecord(UUIDPrimaryKeyMixin, CreatedAtMixin, UpdatedAtMixin, Base):
    __tablename__ = 'attendance_records'
    # UniqueConstraint('employee_id', 'period')
    employee_id, employee_no, period  # period = 'YYYY-MM' 月份字符串
    attendance_rate, absence_days, overtime_hours, late_count, early_leave_count
    feishu_record_id: str | None     # 飞书记录唯一 ID，用于去重
    source_modified_at: datetime | None  # 飞书记录修改时间，增量同步水印
    synced_at: datetime              # 本系统同步写入时间
```

From backend/app/models/feishu_sync_log.py:
```python
class FeishuSyncLog(UUIDPrimaryKeyMixin, CreatedAtMixin, Base):
    __tablename__ = 'feishu_sync_logs'
    mode, status, total_records, matched_records, unmatched_records
    error_message, started_at, finished_at
    # status 值: 'success' | 'failed' | 'running'
```

From backend/app/schemas/feishu.py:
```python
class FeishuConfigCreate(BaseModel): ...
class FeishuConfigRead(BaseModel):
    app_secret_masked: str  # '****cdef'，绝不返回明文
class FeishuConfigUpdate(BaseModel): ...
class SyncTriggerRequest(BaseModel): mode: str = 'incremental'
class SyncResultResponse(BaseModel): status, total_records, matched_records, ...
class SyncStatusRead(BaseModel): ...
class FieldMapping(BaseModel): employee_no, attendance_rate, absence_days, ...
```

From backend/app/schemas/attendance.py:
```python
class AttendanceRecordRead(BaseModel): ...
class AttendanceSummary(BaseModel):
    employee_id, employee_no, period, attendance_rate, absence_days, overtime_hours
    late_count, early_leave_count, synced_at, is_stale
```

From backend/app/core/database.py:
```python
SessionLocal: sessionmaker[Session]
```

From backend/app/dependencies.py:
```python
def require_roles(*roles) -> Callable  # 权限工厂
def get_db() -> Generator[Session]     # DB Session 依赖
def get_current_user(...)              # 当前用户
```

From backend/app/services/access_scope_service.py:
```python
class AccessScopeService:
    def __init__(self, db: Session)
    def can_access_employee(self, current_user: User, employee: Employee | None) -> bool
    def ensure_employee_access(self, current_user: User, employee_id: str) -> Employee | None
    # admin: 全部可见
    # hrbp/manager: 仅自己部门
    # employee: 仅自己
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现 FeishuService + AttendanceService + 飞书配置 CRUD</name>
  <files>
    backend/app/services/feishu_service.py,
    backend/app/services/attendance_service.py
  </files>
  <read_first>
    backend/app/services/evaluation_service.py,
    backend/app/services/llm_service.py,
    backend/app/services/access_scope_service.py,
    backend/app/core/encryption.py,
    backend/app/core/config.py,
    backend/app/models/feishu_config.py,
    backend/app/models/attendance_record.py,
    backend/app/models/feishu_sync_log.py,
    backend/app/models/employee.py,
    backend/app/schemas/feishu.py,
    backend/app/schemas/attendance.py
  </read_first>
  <action>
1. 创建 `backend/app/services/feishu_service.py`（per D-01, D-02, D-07, D-08, D-09, ATT-01, ATT-07）:
   - 类 FeishuService，构造函数接受 `db: Session`，可选 `http_client: httpx.Client = None`（DI 测试友好）
   - **Token 管理**（per D-02）:
     - `_token: str | None = None`, `_token_expires_at: float = 0`
     - `_ensure_token(app_id, app_secret) -> str`: 检查过期（提前 300 秒刷新），过期则 POST `https://open.feishu.cn/open-apis/auth/v3/tenant_access_token/internal`，发送 JSON body `{"app_id": app_id, "app_secret": app_secret}`，检查 `response.json()['code'] == 0`
   - **配置 CRUD**（per D-01, D-04）:
     - `get_config() -> FeishuConfig | None`: 查询 is_active=True 的配置（系统只维护一条活跃配置）
     - `create_config(data: FeishuConfigCreate) -> FeishuConfig`: 加密 app_secret 后存储
     - `update_config(config_id: str, data: FeishuConfigUpdate) -> FeishuConfig`: 部分更新，app_secret 为 None 时不更新密钥
     - `get_config_read(config: FeishuConfig) -> FeishuConfigRead`: 生成脱敏响应，`app_secret_masked` = `'****' + decrypt_secret(config.encrypted_app_secret)[-4:]`。如果解密失败，`app_secret_masked` = `'****'`
     - `test_connection() -> dict[str, Any]`: 获取活跃配置，解密 Secret，尝试获取 token，返回 `{"success": True, "message": "连接成功..."}`  或  `{"success": False, "message": "连接失败: {错误信息}"}`
   - **数据同步**（per D-07, D-08, ATT-01）:
     - `_fetch_all_records(token, app_token, table_id, field_mapping, since_timestamp=None) -> list[dict]`:
       - POST `https://open.feishu.cn/open-apis/bitable/v1/apps/{app_token}/tables/{table_id}/records/search`
       - page_size=500, automatic_fields=True
       - 增量模式: 使用 `filter` 参数添加 `last_modified_time` isGreater 条件（since_timestamp 参数，格式为 unix 毫秒）
       - 循环 page_token 直到 has_more=false
       - 每条记录保留 `record_id` 和 `last_modified_time`（来自 automatic_fields）
     - `_map_fields(record: dict, mapping: FieldMapping) -> dict | None`:
       - 按 mapping 将飞书字段名转换为系统字段名
       - 提取 `record_id` -> `feishu_record_id`
       - 提取 `last_modified_time` -> `source_modified_at`（unix 毫秒转 datetime）
       - 类型强制转换（str->float/int），单条失败返回 None 并 log warning
     - `_upsert_records(mapped_records: list[dict], current_period: str) -> tuple[int, int, list[str]]`:
       - 按 employee_no 查找 Employee 获取 employee_id
       - upsert 策略: 按 `(employee_id, period)` 查找已有记录。如果找到 **且** `feishu_record_id` 相同，则更新字段值和 source_modified_at。如果找到但 feishu_record_id 不同，也更新（覆盖）。如果未找到，创建新记录。
       - 设置 `synced_at = datetime.now(UTC)`
       - 设置 `period = current_period`（当前月份 'YYYY-MM'）
       - 返回 (matched, unmatched, unmatched_nos)

     **CRITICAL: 增量同步语义（addresses review HIGH concern #2）**
     - `sync_attendance(mode: str = 'incremental') -> SyncResultResponse`:
       - 获取活跃配置，解密 Secret，获取 token
       - **增量模式水印策略**:
         1. 查询 AttendanceRecord 表中 `source_modified_at` 的最大值作为水印
         2. 如果有水印，减去 5 分钟作为重叠窗口（overlap window），确保不遗漏在水印附近修改的记录
         3. 将水印时间传给 `_fetch_all_records` 的 `since_timestamp` 参数
         4. 拉取到的记录通过 `feishu_record_id` + `(employee_id, period)` 联合唯一约束确定性去重
       - **全量模式**: `since_timestamp=None`，拉取全部记录 + upsert
       - `current_period`: 使用 `datetime.now().strftime('%Y-%m')` 作为当前月份
       - 写 FeishuSyncLog（status='running'）在开始时
       - 调用 _fetch_all_records + _upsert_records
       - 更新 FeishuSyncLog（status='success' 或 'failed'，填写 finished_at）
       - 返回 SyncResultResponse

   **CRITICAL: 并发防护（addresses review HIGH concern #1）**
   - **不使用 `threading.Lock`**。改用数据库级防护:
     - `_is_sync_running(db: Session) -> bool`: 查询 FeishuSyncLog 最近 1 小时内是否有 status='running' 的记录。如果有，返回 True（拒绝新同步）。
     - `sync_with_retry` 入口先调用 `_is_sync_running`，如果返回 True，返回 SyncResultResponse(status='failed', error_message='同步任务正在进行中，请稍后再试')
     - 如果同步运行超过 1 小时仍为 running 状态，视为僵尸记录，允许新同步覆盖。
   - **部署假设文档**: 在 feishu_service.py 模块文档注释中明确写:
     ```
     NOTE: APScheduler 运行在 FastAPI 进程内。生产部署应使用单 worker（uvicorn --workers 1）
     或外部 Gunicorn preload 模式以确保调度器只在一个进程中运行。
     并发防护基于 FeishuSyncLog 数据库状态查询，多 worker 下也能正确防止重复同步。
     ```

   - **重试机制**（per D-09, ATT-07）:
     - `sync_with_retry(mode: str) -> SyncResultResponse`:
       - 重试 3 次，间隔 [5, 15, 45] 秒
       - 每次失败 log warning
       - 全部失败 log error + 更新 FeishuSyncLog(status='failed', error_message=最终错误)

2. 创建 `backend/app/services/attendance_service.py`（per ATT-05, ATT-06）:
   - 类 AttendanceService，构造函数接受 `db: Session`

   **CRITICAL: HRBP 部门作用域（addresses review MEDIUM concern #6）**
   - `get_employee_attendance(employee_id: str, current_user: User | None = None) -> AttendanceSummary | None`:
     - 如果 current_user 提供，先调用 `AccessScopeService(db).ensure_employee_access(current_user, employee_id)`，无权限则 raise PermissionError
     - 查询该员工最新 period（按 period DESC）的 AttendanceRecord
     - 计算 is_stale: synced_at 距离 now(UTC) 超过 24 小时
     - 返回 AttendanceSummary
   - `list_attendance(department_id: str | None = None, search: str | None = None, offset: int = 0, limit: int = 20, current_user: User | None = None) -> tuple[list[AttendanceRecordRead], int]`:
     - 如果 current_user 角色是 hrbp 或 manager: 先获取 current_user 的部门名称集合，过滤 Employee.department in 该集合
     - 如果 current_user 角色是 admin: 不过滤部门
     - 支持按 department_id 额外筛选、按姓名/工号搜索
     - 返回分页结果 + 总数
  </action>
  <verify>
    <automated>cd D:/wage_adjust && .venv/Scripts/python -c "from backend.app.services.feishu_service import FeishuService; from backend.app.services.attendance_service import AttendanceService; print('Services OK')" && .venv/Scripts/python -c "import inspect; from backend.app.services.feishu_service import FeishuService; src = inspect.getsource(FeishuService); assert '_is_sync_running' in src; assert 'source_modified_at' in src; assert 'feishu_record_id' in src; assert 'threading.Lock' not in src; print('Review concerns addressed')"</automated>
  </verify>
  <acceptance_criteria>
    - FeishuService 包含 _ensure_token、sync_attendance、sync_with_retry、get_config、create_config、update_config、get_config_read、test_connection 方法
    - FeishuService._is_sync_running 查询 FeishuSyncLog status='running'（不使用 threading.Lock）
    - FeishuService.sync_attendance 增量模式使用 source_modified_at 最大值减 5 分钟作为水印
    - FeishuService._upsert_records 使用 feishu_record_id 进行确定性去重
    - FeishuService 模块文档注释包含单 worker 部署假设说明
    - FeishuService.get_config_read 返回 app_secret_masked（'****' + 最后 4 位）
    - AttendanceService.get_employee_attendance 支持 current_user 参数并调用 AccessScopeService
    - AttendanceService.list_attendance 对 hrbp/manager 角色进行部门过滤
    - _fetch_all_records 循环 page_token 处理分页
    - _map_fields 提取 record_id 和 last_modified_time 并类型转换
    - 重试间隔为 [5, 15, 45] 秒
  </acceptance_criteria>
  <done>FeishuService 和 AttendanceService 可导入，包含水印增量同步、数据库级并发防护、部门作用域过滤逻辑</done>
</task>

<task type="auto">
  <name>Task 2: API 端点 + APScheduler 调度器 + 路由注册 + lifespan 集成</name>
  <files>
    backend/app/api/v1/feishu.py,
    backend/app/api/v1/attendance.py,
    backend/app/api/v1/router.py,
    backend/app/scheduler/__init__.py,
    backend/app/scheduler/feishu_scheduler.py,
    backend/app/main.py
  </files>
  <read_first>
    backend/app/api/v1/salary.py,
    backend/app/api/v1/imports.py,
    backend/app/api/v1/router.py,
    backend/app/main.py,
    backend/app/dependencies.py,
    backend/app/services/feishu_service.py,
    backend/app/services/attendance_service.py,
    backend/app/services/access_scope_service.py
  </read_first>
  <action>
1. 创建 `backend/app/scheduler/__init__.py`（空文件，Python 包）

2. 创建 `backend/app/scheduler/feishu_scheduler.py`（per D-05, D-06, ATT-03）:

   **CRITICAL: 单 worker 部署假设（addresses review HIGH concern #1）**
   在模块顶部文档注释中明确:
   ```
   """
   飞书考勤定时同步调度器。

   部署要求:
   - 生产环境应使用单 worker（uvicorn --workers 1）或 Gunicorn preload 模式
   - 这确保 APScheduler 只在一个进程中运行，避免重复调度
   - 即使意外多 worker 运行，FeishuService._is_sync_running 的数据库级防护
     也能防止同一时刻多个同步任务并行执行
   """
   ```

   ```python
   from apscheduler.schedulers.asyncio import AsyncIOScheduler
   from apscheduler.triggers.cron import CronTrigger
   import logging
   from backend.app.core.database import SessionLocal

   logger = logging.getLogger(__name__)
   scheduler = AsyncIOScheduler()

   def _run_incremental_sync():
       """定时同步 job — 在独立 DB Session 中执行"""
       db = SessionLocal()
       try:
           from backend.app.services.feishu_service import FeishuService
           service = FeishuService(db)
           result = service.sync_with_retry(mode='incremental')
           db.commit()
           logger.info('定时考勤同步完成: %s', result)
       except Exception:
           db.rollback()
           logger.exception('定时考勤同步失败')
       finally:
           db.close()

   def start_scheduler(sync_hour: int = 6, sync_minute: int = 0):
       # 从数据库读取配置的同步时间
       db = SessionLocal()
       try:
           from backend.app.services.feishu_service import FeishuService
           config = FeishuService(db).get_config()
           if config:
               sync_hour = config.sync_hour
               sync_minute = config.sync_minute
       finally:
           db.close()

       scheduler.add_job(
           _run_incremental_sync,
           trigger=CronTrigger(hour=sync_hour, minute=sync_minute),
           id='feishu_attendance_sync',
           replace_existing=True,
       )
       scheduler.start()
       logger.info('考勤同步调度器已启动: 每天 %02d:%02d', sync_hour, sync_minute)

   def stop_scheduler():
       if scheduler.running:
           scheduler.shutdown(wait=False)

   def reschedule(sync_hour: int, sync_minute: int):
       """配置更新后重新调度"""
       scheduler.reschedule_job(
           'feishu_attendance_sync',
           trigger=CronTrigger(hour=sync_hour, minute=sync_minute),
       )
       logger.info('考勤同步已重新调度: 每天 %02d:%02d', sync_hour, sync_minute)
   ```

3. 创建 `backend/app/api/v1/feishu.py`（per D-13, D-15, ATT-02, ATT-04）:
   - router = APIRouter(prefix='/feishu', tags=['feishu'])
   - **GET /feishu/config** — 获取当前飞书配置（admin + hrbp 可读，per D-13）
     - 调用 FeishuService.get_config() 获取模型
     - 调用 FeishuService.get_config_read(config) 生成脱敏响应
     - 返回 FeishuConfigRead（app_secret_masked，绝不返回明文）
   - **POST /feishu/config** — 创建/更新飞书配置（仅 admin，per D-15）
     - 接受 FeishuConfigCreate
     - 调用 FeishuService.create_config 或 update_config
     - 配置保存后调用 reschedule 更新定时任务时间
   - **PUT /feishu/config/{config_id}** — 更新飞书配置（仅 admin）
     - 接受 FeishuConfigUpdate

   **CRITICAL: 测试连接端点（addresses review MEDIUM concern #4）**
   - **POST /feishu/test-connection** — 测试飞书连接（仅 admin）
     - 调用 FeishuService.test_connection()
     - 返回 `{"success": bool, "message": str}`
     - 成功消息: "连接成功，已验证飞书应用凭证有效。"
     - 失败消息: "连接失败：{具体错误}。请检查 App ID 和 App Secret 是否正确。"

   - **POST /feishu/sync** — 手动触发同步（admin + hrbp，per D-08, D-13）
     - 接受 SyncTriggerRequest（mode: full | incremental）
     - 调用 FeishuService.sync_with_retry
     - 返回 SyncResultResponse
   - **GET /feishu/sync-status** — 获取最新同步状态（admin + hrbp）
     - 查询最新 FeishuSyncLog（按 created_at DESC LIMIT 1）
     - 返回 SyncStatusRead

4. 创建 `backend/app/api/v1/attendance.py`（per ATT-05, ATT-06）:
   - router = APIRouter(prefix='/attendance', tags=['attendance'])

   **CRITICAL: 部门作用域（addresses review MEDIUM concern #6）**
   - **GET /attendance/{employee_id}** — 获取单员工考勤概览（admin + hrbp + manager）
     - 将 current_user 传入 AttendanceService.get_employee_attendance(employee_id, current_user=current_user)
     - 服务层内部调用 AccessScopeService.ensure_employee_access 检查权限
     - 无权限返回 403，无数据返回 404
     - 返回 AttendanceSummary（含 synced_at + is_stale + period）
   - **GET /attendance/** — 获取考勤列表（admin + hrbp，分页 + 搜索 + 部门筛选）
     - 将 current_user 传入 AttendanceService.list_attendance(current_user=current_user, ...)
     - 服务层内部对 hrbp 进行部门过滤
     - 返回 `{ items: AttendanceRecordRead[], total: int }`

5. 更新 `backend/app/api/v1/router.py`:
   - 导入并注册 feishu_router 和 attendance_router

6. 更新 `backend/app/main.py` lifespan（per D-05）:
   - 在 yield 之前: `from backend.app.scheduler.feishu_scheduler import start_scheduler, stop_scheduler; start_scheduler()`
   - 在 yield 之后: `stop_scheduler()`
   - 用 try/except 包裹 scheduler 启动，失败只 log warning 不影响应用启动

7. 运行 Plan 01 的 RED 测试桩，确认部分测试从 RED 转为 GREEN:
   - `pytest backend/tests/test_services/test_feishu_service.py -x --no-header -q`
  </action>
  <verify>
    <automated>cd D:/wage_adjust && .venv/Scripts/python -c "from backend.app.api.v1.feishu import router as fr; from backend.app.api.v1.attendance import router as ar; from backend.app.scheduler.feishu_scheduler import start_scheduler, stop_scheduler; print('API + Scheduler OK')" && .venv/Scripts/python -c "import inspect; src = open('backend/app/api/v1/feishu.py').read(); assert 'test-connection' in src or 'test_connection' in src; print('test-connection endpoint exists')" && .venv/Scripts/python -c "src = open('backend/app/api/v1/attendance.py').read(); assert 'current_user' in src; print('attendance uses current_user')" && .venv/Scripts/python -m pytest backend/tests/test_services/test_feishu_config.py -x --no-header -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - POST /api/v1/feishu/test-connection 端点存在，返回 `{"success": bool, "message": str}`
    - POST /api/v1/feishu/sync 端点存在，接受 mode 参数
    - GET /api/v1/feishu/config 端点存在，返回 FeishuConfigRead（app_secret_masked，绝不含明文）
    - GET /api/v1/attendance/{employee_id} 端点存在，将 current_user 传入服务层做权限检查
    - GET /api/v1/attendance/ 端点存在，HRBP 角色仅返回自己部门的数据
    - POST /api/v1/feishu/config 仅 admin 可访问（require_roles('admin')）
    - GET /api/v1/feishu/sync-status 返回最新 FeishuSyncLog
    - APScheduler 在 lifespan 中启动，使用配置的 sync_hour/sync_minute
    - feishu_scheduler.py 模块文档包含单 worker 部署假设说明
    - router.py 注册了 feishu 和 attendance 路由
    - main.py lifespan 包含 start_scheduler/stop_scheduler，用 try/except 包裹
  </acceptance_criteria>
  <done>全部后端 API 端点可访问（含 test-connection），APScheduler 定时任务在应用启动时注册，权限作用域正确，部分 RED 测试转绿</done>
</task>

</tasks>

<verification>
```bash
# 启动后端验证端点注册
uvicorn backend.app.main:app --host 127.0.0.1 --port 8011 &
sleep 3
curl -s http://127.0.0.1:8011/api/v1/feishu/config | python -m json.tool
curl -s http://127.0.0.1:8011/api/v1/feishu/sync-status | python -m json.tool

# 验证 test-connection 端点存在
curl -s -X POST http://127.0.0.1:8011/api/v1/feishu/test-connection | python -m json.tool

# 运行测试
pytest backend/tests/test_services/test_feishu_service.py backend/tests/test_services/test_feishu_config.py backend/tests/test_services/test_attendance_service.py -x --no-header -q

# 验证并发防护不使用 threading.Lock
grep -c 'threading.Lock' backend/app/services/feishu_service.py  # 应该输出 0

# 验证增量同步使用 source_modified_at
grep -c 'source_modified_at' backend/app/services/feishu_service.py  # 应该 >= 2
```
</verification>

<success_criteria>
- FeishuService 完整实现飞书 API Token 管理 + 分页拉取 + 增量/全量同步 + 重试 + 数据库级并发防护
- 增量同步使用 source_modified_at 水印 + 5 分钟重叠窗口 + feishu_record_id 去重
- POST /feishu/test-connection 端点明确存在并返回结构化结果
- AttendanceService 实现单员工查询（含 is_stale 标记和 AccessScopeService 权限检查）和列表查询（含 HRBP 部门过滤）
- 5 个新 API 端点注册并可访问
- APScheduler 在应用启动时注册 CronTrigger 定时任务，模块文档明确单 worker 假设
- 配置更新后调度器自动重新调度
</success_criteria>

<output>
完成后创建 `.planning/phases/09-feishu-attendance-integration/09-02-SUMMARY.md`
</output>
