---
phase: 32.1-employee-eligibility-visibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/schemas/eligibility.py
  - backend/app/services/eligibility_service.py
  - backend/app/api/v1/eligibility.py
  - backend/tests/test_api/test_eligibility_me.py
autonomous: true
requirements:
  - ESELF-01
  - ESELF-02
  - ESELF-04
  - ESELF-05

must_haves:
  truths:
    - "已登录的 employee（user.employee_id 已绑定）调用 GET /api/v1/eligibility/me 返回 200 + EligibilityResultSchema，含 overall_status / rules[] / data_updated_at"
    - "未绑定员工的 user 调用 GET /api/v1/eligibility/me 返回 422 + 中文 detail '您尚未绑定员工信息'"
    - "user.employee_id 指向的 employee 已被删除时返回 404 + 中文 detail '员工档案缺失'"
    - "FastAPI 路由匹配把字符串 'me' 优先解析为 /me 端点（不再被 /{employee_id} 当作 employee_id 捕获）"
    - "rules[].detail 文本不含 YYYY-MM-DD 模式（pytest 用正则断言），保持脱敏"
    - "data_updated_at 字段反映 max(employee.updated_at, 该员工最近 PerformanceRecord/SalaryAdjustmentRecord/NonStatutoryLeave 的 updated_at)"
  artifacts:
    - path: "backend/app/schemas/eligibility.py"
      provides: "EligibilityResultSchema 增加 data_updated_at: datetime | None 字段"
      contains: "data_updated_at"
    - path: "backend/app/services/eligibility_service.py"
      provides: "EligibilityService.compute_data_updated_at(employee_id) -> datetime | None"
      contains: "compute_data_updated_at"
    - path: "backend/app/api/v1/eligibility.py"
      provides: "GET /me 端点，注册在 GET /{employee_id} 之前"
      contains: "@router.get('/me'"
    - path: "backend/tests/test_api/test_eligibility_me.py"
      provides: "API 测试覆盖：bound 200 / unbound 422 / employee_missing 404 / detail 脱敏 / data_updated_at 存在"
      min_lines: 120
  key_links:
    - from: "backend/app/api/v1/eligibility.py:@router.get('/me')"
      to: "EligibilityService.check_employee(current_user.employee_id)"
      via: "依赖注入 get_current_user 取 employee_id"
      pattern: "current_user\\.employee_id"
    - from: "backend/app/api/v1/eligibility.py:@router.get('/me')"
      to: "EligibilityService.compute_data_updated_at(current_user.employee_id)"
      via: "拼接 data_updated_at 进 EligibilityResultSchema"
      pattern: "compute_data_updated_at"
    - from: "backend/tests/test_api/test_eligibility_me.py"
      to: "TestClient.get('/api/v1/eligibility/me', headers={Bearer ...})"
      via: "JWT auth_header + 校验 200/422/404 三态"
      pattern: "/api/v1/eligibility/me"
---

<objective>
为 Phase 32.1 建立后端契约：在 `GET /api/v1/eligibility/{employee_id}` 之前注册无参数 `GET /api/v1/eligibility/me` 端点，复用 `EligibilityService.check_employee()`，并在 `EligibilityResultSchema` 中扩展 `data_updated_at` 字段（取 4 个数据源 updated_at 的最大值）。

Purpose: 实现 ESELF-04（无参数路由 + 跨员工 403 自然不可达）+ ESELF-01/02 后端基础（员工自访问入口）+ ESELF-05 时间戳数据源。本期不修改 EligibilityEngine / RuleResult / 现有 /{employee_id} 端点。

Output: 1 个新端点 + 1 个新 schema 字段 + 1 个新 service helper + 1 个 API 测试文件（5 个用例）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/32.1-employee-eligibility-visibility/32.1-CONTEXT.md
@backend/app/engines/eligibility_engine.py
@backend/app/services/eligibility_service.py
@backend/app/schemas/eligibility.py
@backend/app/api/v1/eligibility.py
@backend/app/dependencies.py
@backend/app/models/employee.py
@backend/app/models/performance_record.py
@backend/app/models/salary_adjustment_record.py
@backend/app/models/non_statutory_leave.py
@backend/app/models/user.py
@backend/tests/test_api/test_eligibility_visibility.py
@backend/tests/conftest.py

<interfaces>
<!-- 真理来源：执行者必须直接使用以下契约，不要再去全代码库搜索 -->

From backend/app/services/eligibility_service.py（已存在，本期复用且不修改）:
```python
class EligibilityService:
    def __init__(self, db: Session, *, settings: Settings | None = None) -> None: ...
    def check_employee(
        self,
        employee_id: str,
        *,
        reference_date: date | None = None,
        year: int | None = None,
    ) -> EligibilityResult:
        """已存在；本期 GET /me 直接调用此方法。失败时抛 HTTPException(404)。"""
```

From backend/app/engines/eligibility_engine.py（已存在，不修改）:
```python
@dataclass
class EligibilityResult:
    overall_status: str          # 'eligible' | 'ineligible' | 'pending'
    rules: list[RuleResult]      # 长度恒为 4：TENURE/ADJUSTMENT_INTERVAL/PERFORMANCE/LEAVE

@dataclass(frozen=True)
class RuleResult:
    rule_code: str               # TENURE / ADJUSTMENT_INTERVAL / PERFORMANCE / LEAVE
    rule_label: str              # 中文标签
    status: str                  # 'eligible' | 'ineligible' | 'data_missing' | 'overridden'
    detail: str                  # 已脱敏（月数 / 等级字符 / 天数；无 YYYY-MM-DD / 无薪资数字）
```

From backend/app/dependencies.py（已存在）:
```python
def get_current_user(...) -> User:
    """JWT 鉴权；返回 User ORM 对象，含 employee_id（可为 None）"""
```

From backend/app/models/user.py:
```python
class User(...):
    employee_id: Mapped[Optional[str]] = mapped_column(
        ForeignKey('employees.id'), nullable=True, unique=True, index=True,
    )
```

From backend/app/models/employee.py / performance_record.py / salary_adjustment_record.py / non_statutory_leave.py:
四个 model 均含 `UpdatedAtMixin`（Mapped[datetime] updated_at）；employee_id 列均为 String(36) FK。

From backend/app/api/v1/eligibility.py:351（路由顺序点）:
```python
@router.get('/{employee_id}', response_model=EligibilityResultSchema)
def check_eligibility(employee_id: str, ...) -> EligibilityResultSchema: ...
```
本期 `/me` 端点必须在文件中位于本行**之前**（FastAPI 按声明顺序匹配；否则 'me' 会被 employee_id 捕获）。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 EligibilityResultSchema 加 data_updated_at + Service 层 compute_data_updated_at helper</name>
  <files>backend/app/schemas/eligibility.py, backend/app/services/eligibility_service.py</files>

  <read_first>
    - backend/app/schemas/eligibility.py（当前 EligibilityResultSchema 定义；要在其上加字段）
    - backend/app/services/eligibility_service.py（在 check_employee 上方插入 compute_data_updated_at 方法）
    - backend/app/models/employee.py（确认 updated_at 字段，来自 UpdatedAtMixin）
    - backend/app/models/performance_record.py（确认 updated_at；employee_id FK）
    - backend/app/models/salary_adjustment_record.py（确认 updated_at；employee_id FK）
    - backend/app/models/non_statutory_leave.py（确认 updated_at；employee_id FK）
  </read_first>

  <behavior>
    - 测试 1: EligibilityResultSchema 实例化时不传 data_updated_at 默认 None，传入 datetime 对象正确序列化为 ISO 8601 字符串
    - 测试 2: compute_data_updated_at(employee_id) 返回 4 个数据源中最大 updated_at；任一缺失自动跳过；全部缺失返回 None
    - 测试 3: 不存在的 employee_id 调用 compute_data_updated_at 返回 None（不抛异常；端点层另行 404）
  </behavior>

  <action>
    **改动 1：`backend/app/schemas/eligibility.py`**

    在 `EligibilityResultSchema` 类内追加字段（保持向后兼容，默认 None）：

    ```python
    class EligibilityResultSchema(BaseModel):
        """Overall eligibility evaluation result.

        Terminology mapping (ELIG-08/D-10):
        - all eligible -> 'eligible'
        - any ineligible -> 'ineligible'
        - no ineligible but some data_missing -> 'pending'

        Phase 32.1 D-15: data_updated_at = max(updated_at) across 4 data sources
        (employee/performance/salary_adjustment/non_statutory_leave). None if all sources missing.
        """

        overall_status: Literal['eligible', 'ineligible', 'pending']
        rules: List[RuleResultSchema]
        data_updated_at: Optional[datetime] = None  # Phase 32.1 D-15
    ```

    注意：
    - 不动 `RuleResultSchema`、`EligibilityBatchItemSchema`、`EligibilityBatchResponse` 等其他 schema
    - `datetime` 已在文件顶部导入（第 3 行），无需新增 import
    - `Optional` 已在第 5 行导入

    **改动 2：`backend/app/services/eligibility_service.py`**

    在 `check_employee` 方法**之前**（即 `_build_thresholds` 之后、`# Single-employee check` 注释段下方）插入新方法：

    ```python
    # ------------------------------------------------------------------
    # Data freshness timestamp (Phase 32.1 D-16, ESELF-05)
    # ------------------------------------------------------------------

    def compute_data_updated_at(self, employee_id: str) -> datetime | None:
        """计算员工资格相关数据的最新 updated_at（D-16）。

        取 4 个数据源 updated_at 的最大值：
          - Employee.updated_at（员工档案）
          - max(PerformanceRecord.updated_at) where employee_id（绩效）
          - max(SalaryAdjustmentRecord.updated_at) where employee_id（调薪记录）
          - max(NonStatutoryLeave.updated_at) where employee_id（非法定假期）

        任一数据源缺失自动跳过；全部缺失返回 None（前端展示「数据从未更新」）。
        不存在的 employee_id 也返回 None（不抛异常；端点层另做 404）。
        """
        candidates: list[datetime] = []

        emp_updated = self.db.scalar(
            select(Employee.updated_at).where(Employee.id == employee_id)
        )
        if emp_updated is not None:
            candidates.append(emp_updated)

        perf_updated = self.db.scalar(
            select(func.max(PerformanceRecord.updated_at))
            .where(PerformanceRecord.employee_id == employee_id)
        )
        if perf_updated is not None:
            candidates.append(perf_updated)

        adj_updated = self.db.scalar(
            select(func.max(SalaryAdjustmentRecord.updated_at))
            .where(SalaryAdjustmentRecord.employee_id == employee_id)
        )
        if adj_updated is not None:
            candidates.append(adj_updated)

        leave_updated = self.db.scalar(
            select(func.max(NonStatutoryLeave.updated_at))
            .where(NonStatutoryLeave.employee_id == employee_id)
        )
        if leave_updated is not None:
            candidates.append(leave_updated)

        return max(candidates) if candidates else None
    ```

    注意：
    - `datetime` 已在文件顶部导入（line 4）
    - `Employee` / `PerformanceRecord` / `SalaryAdjustmentRecord` / `NonStatutoryLeave` 均已导入
    - `func` / `select` 已导入
    - 返回类型用 PEP 604 union（`datetime | None`），与文件其他 method 一致
    - 不在 check_employee 内调用此方法，保持单一职责（端点层另行调用）

    **不要做：**
    - 不要修改 EligibilityEngine 或 RuleResult
    - 不要在 RuleResultSchema 加新字段
    - 不要引入 sanitize helper（D-08）
    - 不要给 compute_data_updated_at 加 employee_id 存在性校验（端点层用 check_employee 的 404 处理）
  </action>

  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust && .venv/bin/python -c "from backend.app.schemas.eligibility import EligibilityResultSchema; s = EligibilityResultSchema(overall_status='eligible', rules=[]); assert s.data_updated_at is None; from datetime import datetime, timezone; s2 = EligibilityResultSchema(overall_status='eligible', rules=[], data_updated_at=datetime.now(timezone.utc)); assert s2.data_updated_at is not None; print('OK')"</automated>
    <automated>grep -n "data_updated_at" /Users/mac/PycharmProjects/Wage_adjust/backend/app/schemas/eligibility.py | wc -l | awk '{exit !($1>=2)}'</automated>
    <automated>grep -n "def compute_data_updated_at" /Users/mac/PycharmProjects/Wage_adjust/backend/app/services/eligibility_service.py</automated>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust && .venv/bin/python -c "from backend.app.services.eligibility_service import EligibilityService; assert hasattr(EligibilityService, 'compute_data_updated_at'); print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'data_updated_at: Optional\[datetime\]' backend/app/schemas/eligibility.py` >= 1
    - `grep -c 'def compute_data_updated_at' backend/app/services/eligibility_service.py` == 1
    - `grep -c 'Phase 32.1 D-15\|Phase 32.1 D-16' backend/app/{schemas/eligibility.py,services/eligibility_service.py}` >= 2（决策追溯注释）
    - Python 导入无 ImportError；EligibilityResultSchema 实例化默认 data_updated_at=None
    - 不修改 RuleResultSchema 字段
    - 不修改 EligibilityService.check_employee 签名（grep 'def check_employee' 行数仍 == 1）
  </acceptance_criteria>

  <done>
    EligibilityResultSchema 含 data_updated_at: datetime | None = None；EligibilityService 含 compute_data_updated_at(employee_id) -> datetime | None 方法，正确取 4 个数据源最大 updated_at。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 注册 GET /api/v1/eligibility/me 端点 + API 测试覆盖 5 用例</name>
  <files>backend/app/api/v1/eligibility.py, backend/tests/test_api/test_eligibility_me.py</files>

  <read_first>
    - backend/app/api/v1/eligibility.py（行 311-345 是 sub-resource /{employee_id}/* 端点；行 351 是 /{employee_id}；/me 必须在所有 /{employee_id} 系列**之前**注册——即在 line 311 之前）
    - backend/app/dependencies.py:get_current_user（注入 User 对象，含 .employee_id 可能为 None）
    - backend/tests/test_api/test_eligibility_visibility.py（参考 _TestContext + UserCreds + auth_header 模式）
    - backend/tests/conftest.py（参考 db_session / employee_factory / user_factory fixture）
    - backend/app/services/eligibility_service.py:check_employee（已存在；body 含 raise HTTPException(404)）
  </read_first>

  <behavior>
    - 测试 1 (bound_user_returns_200): 已绑定 employee 的 user 调用 GET /me 返回 200，body 含 overall_status / rules（长度 4）/ data_updated_at（ISO 8601 字符串或 null）
    - 测试 2 (unbound_user_returns_422): user.employee_id 为 None 时返回 422，detail 含中文 '您尚未绑定员工信息'
    - 测试 3 (missing_employee_returns_404): user.employee_id 指向已删除 employee 时返回 404，detail 含中文（'员工未找到'，由 check_employee 抛出）
    - 测试 4 (detail_remains_desensitized): 200 响应中 rules[].detail 不匹配正则 r'\d{4}-\d{2}-\d{2}'（无 YYYY-MM-DD 模式）
    - 测试 5 (route_priority_me_before_path_param): 字符串 'me' 不会被 /{employee_id} 端点捕获（员工 id='me' 的极端构造也不会绕过；GET /me 在未鉴权时返回 401，不是 422-employee-not-found 之类的）
  </behavior>

  <action>
    **改动 1：`backend/app/api/v1/eligibility.py`**

    在文件 line 308（`# Sub-resource endpoints` 注释段）**之前**（即 `decide_override` 函数之后、`# Sub-resource endpoints` 注释之前）插入新端点段：

    ```python
    # ------------------------------------------------------------------
    # Phase 32.1 ESELF-01/02/04/05: Employee self-service eligibility endpoint
    # 必须注册在 /{employee_id} 系列之前，FastAPI 按声明顺序匹配，
    # 否则 'me' 会被 /{employee_id} 当作 employee_id 参数捕获（T-32.1-04）。
    #
    # Employee-facing endpoint — RuleResult.detail must remain desensitized (D-09).
    # 当前 4 条规则的 detail 已为月数/等级字符/天数（无 YYYY-MM-DD / 无薪资数字）；
    # 任何后续修改 RuleResult.detail 的 phase 必须保留这一脱敏属性。
    # ------------------------------------------------------------------

    @router.get('/me', response_model=EligibilityResultSchema)
    def get_my_eligibility(
        reference_date: date | None = None,
        year: int | None = None,
        db: Session = Depends(get_db),
        current_user: User = Depends(get_current_user),
    ) -> EligibilityResultSchema:
        """员工自助查询本人调薪资格（ESELF-01/02/04/05）。

        - 无参数路由 → ESELF-04 横向越权天然不可达
        - 复用 EligibilityService.check_employee（D-06，不新增引擎逻辑）
        - 返回 EligibilityResultSchema，含 data_updated_at（D-15/D-16）
        """
        if current_user.employee_id is None:
            raise HTTPException(
                status_code=422,
                detail='您尚未绑定员工信息，请前往「账号设置」完成绑定',
            )

        service = EligibilityService(db)
        # check_employee 在 employee 不存在时抛 HTTPException(404, '员工未找到')
        result = service.check_employee(
            current_user.employee_id,
            reference_date=reference_date,
            year=year,
        )
        data_updated_at = service.compute_data_updated_at(current_user.employee_id)

        return EligibilityResultSchema(
            overall_status=result.overall_status,
            rules=[
                {
                    'rule_code': r.rule_code,
                    'rule_label': r.rule_label,
                    'status': r.status,
                    'detail': r.detail,
                }
                for r in result.rules
            ],
            data_updated_at=data_updated_at,
        )
    ```

    在 import 段（约 line 9）确认已有 `get_current_user`：当前 import 行是
    `from backend.app.dependencies import get_current_user, get_db, require_roles`——已包含 `get_current_user`，**无需改动 import**。

    **改动 2：新建 `backend/tests/test_api/test_eligibility_me.py`**

    采用与 `test_eligibility_visibility.py` 一致的 `_TestContext` + `UserCreds` 模式（per-test 隔离 SQLite，避免依赖 conftest.py 里的全局 fixture 顺序）：

    ```python
    """API tests for Phase 32.1 GET /api/v1/eligibility/me endpoint.

    Covers ESELF-01/02/04/05:
    - bound user → 200 with EligibilityResultSchema + data_updated_at
    - unbound user → 422 中文 detail
    - missing employee → 404
    - detail desensitization (no YYYY-MM-DD pattern)
    - route priority: /me 不被 /{employee_id} 捕获
    """
    from __future__ import annotations

    import re
    from dataclasses import dataclass
    from datetime import date, datetime, timezone
    from pathlib import Path
    from uuid import uuid4

    import pytest
    from fastapi.testclient import TestClient
    from sqlalchemy import create_engine
    from sqlalchemy.orm import Session, sessionmaker

    from backend.app.core.config import Settings
    from backend.app.core.database import Base
    from backend.app.core.security import create_access_token, get_password_hash
    from backend.app.dependencies import get_db
    from backend.app.main import create_app
    from backend.app.models import load_model_modules
    from backend.app.models.employee import Employee
    from backend.app.models.user import User


    @dataclass
    class UserCreds:
        id: str
        role: str
        token_version: int


    class _Ctx:
        def __init__(self) -> None:
            temp_root = Path('.tmp').resolve()
            temp_root.mkdir(parents=True, exist_ok=True)
            db_path = (temp_root / f'elig-me-{uuid4().hex}.db').as_posix()
            self.settings = Settings(database_url=f'sqlite+pysqlite:///{db_path}')
            self.engine = create_engine(
                self.settings.database_url,
                connect_args={'check_same_thread': False},
                echo=False,
            )
            load_model_modules()
            Base.metadata.create_all(bind=self.engine)
            self.session_factory = sessionmaker(bind=self.engine, autocommit=False, autoflush=False)
            self.app = create_app(self.settings)
            self.app.dependency_overrides[get_db] = self._override_get_db

        def _override_get_db(self):
            db = self.session_factory()
            try:
                yield db
            finally:
                db.close()

        def db(self) -> Session:
            return self.session_factory()

        def auth_header(self, creds: UserCreds) -> dict[str, str]:
            token = create_access_token(
                subject=creds.id,
                role=creds.role,
                settings=self.settings,
                token_version=creds.token_version,
            )
            return {'Authorization': f'Bearer {token}'}

        def client(self) -> TestClient:
            return TestClient(self.app)


    def _make_employee(db: Session, *, employee_no: str = 'E001') -> Employee:
        emp = Employee(
            employee_no=employee_no,
            name='测试员工',
            department='R&D',
            job_family='engineer',
            job_level='P5',
            status='active',
            hire_date=date(2020, 1, 1),
        )
        db.add(emp)
        db.commit()
        db.refresh(emp)
        return emp


    def _make_user(
        db: Session,
        *,
        email: str,
        role: str = 'employee',
        employee_id: str | None = None,
    ) -> User:
        user = User(
            email=email,
            hashed_password=get_password_hash('test_pwd_123'),
            role=role,
            employee_id=employee_id,
            token_version=0,
        )
        db.add(user)
        db.commit()
        db.refresh(user)
        return user


    @pytest.fixture()
    def ctx() -> _Ctx:
        return _Ctx()


    # ------------------------------------------------------------------
    # T1 — bound user → 200
    # ------------------------------------------------------------------
    def test_bound_user_returns_200_with_eligibility_payload(ctx: _Ctx) -> None:
        with ctx.db() as db:
            emp = _make_employee(db)
            user = _make_user(db, email='emp@test.com', employee_id=emp.id)
            creds = UserCreds(id=user.id, role=user.role, token_version=user.token_version)

        resp = ctx.client().get('/api/v1/eligibility/me', headers=ctx.auth_header(creds))
        assert resp.status_code == 200, resp.text
        body = resp.json()
        assert body['overall_status'] in ('eligible', 'ineligible', 'pending')
        assert isinstance(body['rules'], list)
        assert len(body['rules']) == 4
        # data_updated_at 字段存在；可能是 ISO 字符串或 null（小样本场景）
        assert 'data_updated_at' in body
        # employee.updated_at 由 UpdatedAtMixin 自动写入，所以应非空
        assert body['data_updated_at'] is not None


    # ------------------------------------------------------------------
    # T2 — unbound user → 422 中文 detail
    # ------------------------------------------------------------------
    def test_unbound_user_returns_422_chinese_detail(ctx: _Ctx) -> None:
        with ctx.db() as db:
            user = _make_user(db, email='unbound@test.com', employee_id=None)
            creds = UserCreds(id=user.id, role=user.role, token_version=user.token_version)

        resp = ctx.client().get('/api/v1/eligibility/me', headers=ctx.auth_header(creds))
        assert resp.status_code == 422, resp.text
        body = resp.json()
        # main.py http_exception_handler 把 detail 装进 message 字段
        text = body.get('message') or body.get('detail') or ''
        assert '尚未绑定员工' in text


    # ------------------------------------------------------------------
    # T3 — missing employee → 404
    # ------------------------------------------------------------------
    def test_user_with_dangling_employee_id_returns_404(ctx: _Ctx) -> None:
        with ctx.db() as db:
            # 关联一个不存在的 employee_id (UUID 字符串)
            ghost_id = str(uuid4())
            user = _make_user(db, email='ghost@test.com', employee_id=ghost_id)
            creds = UserCreds(id=user.id, role=user.role, token_version=user.token_version)

        resp = ctx.client().get('/api/v1/eligibility/me', headers=ctx.auth_header(creds))
        assert resp.status_code == 404, resp.text


    # ------------------------------------------------------------------
    # T4 — rules[].detail 保持脱敏（无 YYYY-MM-DD）
    # ------------------------------------------------------------------
    def test_rule_details_remain_desensitized(ctx: _Ctx) -> None:
        with ctx.db() as db:
            emp = _make_employee(db)
            user = _make_user(db, email='emp2@test.com', employee_id=emp.id)
            creds = UserCreds(id=user.id, role=user.role, token_version=user.token_version)

        resp = ctx.client().get('/api/v1/eligibility/me', headers=ctx.auth_header(creds))
        assert resp.status_code == 200
        date_pattern = re.compile(r'\d{4}-\d{2}-\d{2}')
        for rule in resp.json()['rules']:
            assert not date_pattern.search(rule['detail']), (
                f"detail 中出现 YYYY-MM-DD 模式（违反 ESELF-02 脱敏）: {rule['detail']}"
            )


    # ------------------------------------------------------------------
    # T5 — 路由顺序：/me 优先于 /{employee_id}
    # ------------------------------------------------------------------
    def test_me_route_registered_before_employee_id_path(ctx: _Ctx) -> None:
        # 未鉴权访问 /me 应该返回 401（来自 oauth2_scheme），
        # 而不是 404（如果被 /{employee_id} 捕获，access_scope 会先要求鉴权也是 401，
        # 但 422-validation 或 path mismatch 都会暴露顺序错乱）
        resp = ctx.client().get('/api/v1/eligibility/me')
        # 401 Unauthorized = 鉴权拦截，证明 endpoint 已经匹配到（route exists）
        assert resp.status_code == 401, (
            f"路由顺序错误：/me 未被识别为独立端点（status={resp.status_code}, body={resp.text})"
        )
    ```

    **不要做：**
    - 不要修改现有 GET /{employee_id} 端点（D-05）
    - 不要在 /me 端点上加 require_roles（D-04 只用 get_current_user，因为所有已登录用户都能查自己）
    - 不要在端点内重复实现 EligibilityEngine 逻辑（D-06）
    - 不要在 detail 字段上做二次 sanitize（D-08）
    - 不要预设缓存策略（CONTEXT scope_constraints 第 6 条）
  </action>

  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust && .venv/bin/python -m pytest backend/tests/test_api/test_eligibility_me.py -x -v 2>&1 | tail -40</automated>
    <automated>grep -n "@router.get('/me'" /Users/mac/PycharmProjects/Wage_adjust/backend/app/api/v1/eligibility.py</automated>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust && .venv/bin/python -c "import re; src=open('backend/app/api/v1/eligibility.py').read(); me_pos=src.find(\"@router.get('/me'\"); path_pos=src.find(\"@router.get('/{employee_id}'\"); assert me_pos>0 and me_pos<path_pos, f'route order broken: me={me_pos} path={path_pos}'; print('order OK')"</automated>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust && .venv/bin/python -c "from backend.app.main import create_app; from backend.app.core.config import Settings; app = create_app(Settings()); routes = [r.path for r in app.routes if hasattr(r, 'path')]; assert '/api/v1/eligibility/me' in routes, f'missing /me route; got: {[r for r in routes if \"eligibility\" in r]}'; print('route registered OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `pytest backend/tests/test_api/test_eligibility_me.py -x` 退出码 0，5 个用例全 PASS
    - `grep -c "@router.get('/me'" backend/app/api/v1/eligibility.py` == 1
    - `/me` 端点声明位置在 `/{employee_id}` 之前（Python 字符串位置断言；见上面 verify 第 3 条）
    - `/api/v1/eligibility/me` 出现在 FastAPI app.routes 列表中
    - 端点函数签名包含 `current_user: User = Depends(get_current_user)`，**不**包含 `require_roles(...)`
    - 端点 body 包含「Employee-facing endpoint — RuleResult.detail must remain desensitized」注释（D-09）
    - 端点 body 包含 `compute_data_updated_at` 调用（接 D-16）
  </acceptance_criteria>

  <done>
    GET /api/v1/eligibility/me 端点已注册在 /{employee_id} 之前，返回 200/422/404 三态符合 D-04 + D-15；data_updated_at 字段正确填充；测试文件 test_eligibility_me.py 5 用例全 PASS；无 RuleResult.detail 脱敏退化（pytest 正则断言）。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器 → API (employee role) | employee 角色用户携带 JWT 调用 /api/v1/eligibility/me，无 path 参数；身份完全由 JWT subject 决定 |
| 浏览器 → API (其他角色误用 /me) | hrbp/manager/admin 也持有 JWT，理论上也会调用 /me，会拿到自己绑定的 employee 资格（如果有），需确认不会泄漏其他员工 |
| Service → DB | EligibilityService 用 user.employee_id 查询，不接受外部传入 employee_id；天然防越权 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32.1-01 | Information Disclosure | GET /api/v1/eligibility/me 端点 employee_id 取值 | mitigate | 端点 body 仅使用 `current_user.employee_id`（来自 get_current_user JWT 校验），**不接受任何 path/query/body 参数指定 employee_id**；测试 T1+T5 验证。员工 A 持有的 JWT subject 是自己 user.id，db lookup 决定 employee_id，无任何拼接路径让其拿到员工 B 的资格 |
| T-32.1-02 | Information Disclosure / Tampering | 未绑定员工的 user 调用 /me 应被拒绝，避免误返回空结果或 500 暴露 stacktrace | mitigate | 端点显式 `if current_user.employee_id is None: raise HTTPException(422, '您尚未绑定员工信息...')`；测试 T2 断言 422 + 中文 detail；不返回 200 + empty payload，避免前端误判合格 |
| T-32.1-03 | Information Disclosure | RuleResult.detail 复用 employee-facing endpoint，未来 phase 修改 detail 时可能加入敏感数据（YYYY-MM-DD / 薪资数字 / 同事姓名） | mitigate | (a) 端点 body 含审计注释 `# Employee-facing endpoint — RuleResult.detail must remain desensitized`（D-09）；(b) 测试 T4 用正则 `r'\d{4}-\d{2}-\d{2}'` 断言 detail 不含日期模式，未来 PR 加入日期会立即 RED |
| T-32.1-04 | Spoofing / Routing Bypass | FastAPI 路由匹配按声明顺序，如果 /me 在 /{employee_id} 之后注册，字符串 'me' 会被 path_param 当作 employee_id 捕获 → check_eligibility(employee_id='me') → AccessScopeService 取 employee with id='me' → 大概率 404，但若数据库里偶然存在 id='me' 的员工则严重越权 | mitigate | (a) /me 注册位置硬性放在文件 line 308 之前（即所有 /{employee_id} 之前）；(b) 单元测试 T5 用未鉴权访问验证 401 而非 404，间接证明端点已被独立路由识别；(c) acceptance_criteria 内置 Python 字符串位置断言（me_pos < path_pos），任何后续 PR 调整顺序立即 fail |
| T-32.1-05 | Denial of Service | compute_data_updated_at 在每次 /me 调用都跑 4 个 max() 查询，N+1 隐患；employee 角色频繁刷新页面可能放大 | accept | 本期不引入缓存（CONTEXT scope_constraints #6）；4 个查询都是带索引的简单 max（employee_id 是 indexed FK），SQLite 单查询 < 5ms。生产 PostgreSQL 性能更好。监控阈值留 Phase 35 评估，必要时加 Redis 缓存 60s |
</threat_model>

<verification>
执行后整体校验：

1. `cd /Users/mac/PycharmProjects/Wage_adjust && .venv/bin/python -m pytest backend/tests/test_api/test_eligibility_me.py backend/tests/test_api/test_eligibility_visibility.py -x -v` —— 新测试全 PASS，旧测试无回归（确认未破坏 /{employee_id} 端点）
2. `cd /Users/mac/PycharmProjects/Wage_adjust && .venv/bin/python -m pytest backend/tests/test_services/test_eligibility_batch.py backend/tests/test_services/test_eligibility_override.py -x` —— Service 层 batch / override 测试无回归（确认未破坏 EligibilityService 内部状态）
3. `cd /Users/mac/PycharmProjects/Wage_adjust && .venv/bin/uvicorn backend.app.main:app --port 8011` 启动后 `curl -s http://localhost:8011/openapi.json | python -c "import sys,json; d=json.load(sys.stdin); paths=list(d['paths'].keys()); assert '/api/v1/eligibility/me' in paths; print('openapi OK')"` —— OpenAPI 文档暴露 /me 端点
</verification>

<success_criteria>
- ✅ EligibilityResultSchema 含 `data_updated_at: Optional[datetime] = None` 字段（D-15）
- ✅ EligibilityService 含 `compute_data_updated_at(employee_id) -> datetime | None` 方法，正确取 4 数据源 max(updated_at)（D-16）
- ✅ GET /api/v1/eligibility/me 端点已注册，位于 /{employee_id} 之前（D-04, T-32.1-04 mitigation）
- ✅ 端点对未绑定 user 返回 422 + 中文（D-04, T-32.1-02 mitigation）
- ✅ 端点对悬挂 employee_id 返回 404（D-04 边界）
- ✅ RuleResult.detail 保持脱敏（pytest 正则断言；T-32.1-03 mitigation）
- ✅ test_eligibility_me.py 5 用例全 PASS
- ✅ 旧 /{employee_id} 端点无回归（test_eligibility_visibility.py 全 PASS）
- ✅ Service 层 batch/override 无回归
</success_criteria>

<output>
完成后创建 `.planning/phases/32.1-employee-eligibility-visibility/32.1-01-SUMMARY.md`，覆盖：
- 端点签名 + 错误码 + 测试用例总数
- compute_data_updated_at 性能数据（SQLite 实测耗时）
- 脱敏注释位置（line number）
- 路由顺序证据（grep 输出）
- 4 数据源最大 updated_at 验证场景
</output>
