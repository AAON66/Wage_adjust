---
phase: 32.1-employee-eligibility-visibility
plan: 02
type: execute
wave: 2
depends_on:
  - 32.1-01
files_modified:
  - frontend/src/types/api.ts
  - frontend/src/services/eligibilityService.ts
  - frontend/src/components/eligibility/MyEligibilityPanel.tsx
  - frontend/src/pages/MyReview.tsx
autonomous: false
requirements:
  - ESELF-01
  - ESELF-02
  - ESELF-04
  - ESELF-05

must_haves:
  truths:
    - "员工登录后访问 /my-review，页面顶部出现「本人调薪资格」section（在「当前角色」标头之后、「材料与提交概览」section 之前）"
    - "顶部状态徽章 4 种正确呈现：eligible 绿底 / ineligible 红底 + 未通过项数 / pending 黄底 / 未绑定灰底带跳「账号设置」链接"
    - "4 条规则全部摆开（不折叠），按 status 染色：eligible 绿左条 / ineligible 红左条 / data_missing 灰左条 / overridden 蓝左条；每行显示 rule_label + detail + status badge"
    - "右上角显示「数据更新于 YYYY-MM-DD HH:MM」（zh-CN locale，无秒）；data_updated_at 为 null 时显示「数据从未更新」"
    - "未绑定员工（API 422）显示灰底卡片 + 「请先在『账号设置』绑定员工」+ 跳转链接"
    - "网络错误（500/timeout）显示「资格信息暂时不可用，请稍后重试」+ 「重试」按钮"
    - "TS 编译通过（npm run lint）；浏览器无 console error"
  artifacts:
    - path: "frontend/src/types/api.ts"
      provides: "EligibilityResultWithTimestamp 类型扩展（D-19）"
      contains: "EligibilityResultWithTimestamp"
    - path: "frontend/src/services/eligibilityService.ts"
      provides: "fetchMyEligibility(): Promise<EligibilityResultWithTimestamp>（D-18）"
      contains: "fetchMyEligibility"
    - path: "frontend/src/components/eligibility/MyEligibilityPanel.tsx"
      provides: "新组件，封装 4 态徽章 + 4 色规则行 + 时间戳 + 错误态"
      min_lines: 150
    - path: "frontend/src/pages/MyReview.tsx"
      provides: "在 AppShell 内插入 <MyEligibilityPanel />，位置见 D-10"
      contains: "MyEligibilityPanel"
  key_links:
    - from: "frontend/src/pages/MyReview.tsx"
      to: "frontend/src/components/eligibility/MyEligibilityPanel.tsx"
      via: "import + JSX 插入位置：errorMessage 之后、employee section 之前"
      pattern: "<MyEligibilityPanel"
    - from: "frontend/src/components/eligibility/MyEligibilityPanel.tsx"
      to: "frontend/src/services/eligibilityService.ts:fetchMyEligibility"
      via: "useEffect 内调用；axios.isAxiosError 区分 422/404/500"
      pattern: "fetchMyEligibility"
    - from: "frontend/src/services/eligibilityService.ts:fetchMyEligibility"
      to: "GET /api/v1/eligibility/me"
      via: "api.get('/eligibility/me') —— 与 fetchEmployeeEligibility 同 axios instance"
      pattern: "/eligibility/me"
---

<objective>
为 Phase 32.1 完成前端 + UAT：在 MyReview 页面插入「本人调薪资格」section，封装为独立 `MyEligibilityPanel` 组件，覆盖 4 态徽章 + 4 条规则全展开 + 4 色行语义 + 时间戳 + 完整错误态（422 未绑定 / 404 档案缺失 / 500 重试）。

Purpose: 完成 ESELF-01（资格状态可见）+ ESELF-02（不合格规则可读）+ ESELF-04（前端通过无参 fetchMyEligibility 调用）+ ESELF-05（时间戳渲染）的端到端用户体验。

Output: 1 个新组件 + 1 个 service 函数 + 1 个 type 扩展 + MyReview 集成 + 浏览器 UAT。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/32.1-employee-eligibility-visibility/32.1-CONTEXT.md
@.planning/phases/32.1-employee-eligibility-visibility/32.1-01-SUMMARY.md
@frontend/src/pages/MyReview.tsx
@frontend/src/services/eligibilityService.ts
@frontend/src/types/api.ts
@frontend/src/services/api.ts

<interfaces>
<!-- 真理来源：Plan 01 已完成的后端契约 + 前端既有 types/api.ts 契约 -->

From frontend/src/types/api.ts:915-925（已存在）:
```typescript
export interface EligibilityRuleResult {
  rule_code: string;
  rule_label: string;
  status: 'eligible' | 'ineligible' | 'data_missing' | 'overridden';
  detail: string;
}

export interface EligibilityResult {
  overall_status: 'eligible' | 'ineligible' | 'pending';
  rules: EligibilityRuleResult[];
}
```

From Plan 01 (Phase 32.1) 后端契约（已交付）:
```
GET /api/v1/eligibility/me
Response 200: EligibilityResult & { data_updated_at: string | null }
Response 422: { error: 'http_error', message: '您尚未绑定员工信息...' }
Response 404: { error: 'http_error', message: '员工未找到' }
Response 401: { error: 'http_error', message: 'Could not validate credentials.' }（未携带 JWT）
```

From frontend/src/services/api.ts（已存在）:
```typescript
const api: AxiosInstance = axios.create({ baseURL: VITE_API_BASE_URL });
// JWT 拦截器自动注入 Authorization header
// 401 拦截器自动尝试 refresh
```

From frontend/src/pages/MyReview.tsx 插入点（D-10）:
- 文件 line 527-537: `<AppShell title="个人评估中心" actions={...}>`
- 文件 line 538: `{errorMessage ? <p className="surface px-5 py-4...">...</p> : null}`
- 文件 line 539: `{isLoading ? <p>...</p> : null}`
- 文件 line 575-580: `{!isLoading && !employee ? <section className="surface px-6 py-8">...暂未匹配到你的员工档案...</section> : null}`

新 section **必须插入**位置：在 line 539（isLoading 行）**之后**，line 541（fileQueue currentDuplicate IIFE）**之前** —— 即「当前角色 actions」之后、「材料与提交概览」section 之前，符合 D-10 原文「在『当前角色』标头之后，『周期选择 / 上传区』之前」。

注意：MyEligibilityPanel **独立于** employee 是否匹配 —— 它直接调 /me，由 user.employee_id 决定能否拿到资格；MyReview 的 `findEmployeeForUser` 失败也不影响 panel 渲染。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 类型扩展 + Service 函数 fetchMyEligibility + MyEligibilityPanel 组件</name>
  <files>frontend/src/types/api.ts, frontend/src/services/eligibilityService.ts, frontend/src/components/eligibility/MyEligibilityPanel.tsx</files>

  <read_first>
    - frontend/src/types/api.ts（line 915-925：已存在 EligibilityResult / EligibilityRuleResult；在其后追加 EligibilityResultWithTimestamp）
    - frontend/src/services/eligibilityService.ts（line 76-83：参考 fetchEmployeeEligibility 的 axios 调用样式）
    - frontend/src/services/api.ts（确认 axios instance 已带 Bearer 拦截器）
    - frontend/src/pages/MyReview.tsx:69-76（resolveError 函数样式 —— 决定如何用 axios.isAxiosError 拆分错误码）
  </read_first>

  <behavior>
    - 测试 1（类型）: `EligibilityResultWithTimestamp` 必须 = `EligibilityResult & { data_updated_at: string | null }`，TS 编译通过
    - 测试 2（service）: `fetchMyEligibility()` 调用 `api.get<EligibilityResultWithTimestamp>('/eligibility/me')`，无参数版本
    - 测试 3（组件 loading）: 初次渲染显示 skeleton 占位
    - 测试 4（组件 success eligible）: 接到 `{overall_status: 'eligible', rules: [...4...], data_updated_at: '2026-04-22T09:38:00Z'}` 后，渲染绿底徽章 + 4 行规则 + 「数据更新于 2026-04-22 09:38」
    - 测试 5（组件 success ineligible）: `overall_status: 'ineligible'` + 2 条 ineligible rules → 红底徽章「✗ 调薪资格不合格（2 项未通过）」
    - 测试 6（组件 422）: axios 422 → 灰底卡片 + 链接「账号设置」
    - 测试 7（组件 500）: axios 500 → 「资格信息暂时不可用，请稍后重试」+ 「重试」按钮（点击重新调 fetchMyEligibility）
    - 测试 8（组件 data_updated_at = null）: 显示「数据从未更新」
  </behavior>

  <action>
    **改动 1：`frontend/src/types/api.ts`**

    在 line 925（`EligibilityResult` interface 定义之后）追加：

    ```typescript
    /**
     * Phase 32.1 D-19: GET /api/v1/eligibility/me 返回扩展类型
     * data_updated_at: ISO 8601 字符串，源自 max(employee/performance/salary_adjustment/leave .updated_at)
     * null 时前端展示「数据从未更新」
     */
    export type EligibilityResultWithTimestamp = EligibilityResult & {
      data_updated_at: string | null;
    };
    ```

    **改动 2：`frontend/src/services/eligibilityService.ts`**

    在 import 区追加 `EligibilityResultWithTimestamp`，在文件末尾（line 88 附近，与 fetchEmployeeEligibility 同级）追加：

    ```typescript
    /**
     * Phase 32.1 D-18: 员工自助查询本人调薪资格（无参数路由）
     * 后端端点：GET /api/v1/eligibility/me
     * 错误码：401 未鉴权 / 422 未绑定员工 / 404 档案缺失 / 500 内部错误
     */
    export async function fetchMyEligibility(): Promise<EligibilityResultWithTimestamp> {
      const response = await api.get<EligibilityResultWithTimestamp>('/eligibility/me');
      return response.data;
    }
    ```

    确保 import 语句变成：
    ```typescript
    import type {
      EligibilityBatchResponse,
      EligibilityOverrideRecord,
      EligibilityOverrideListResponse,
      EligibilityOverrideCreatePayload,
      EligibilityOverrideDecisionPayload,
      DepartmentListResponse,
      EligibilityResult,
      EligibilityResultWithTimestamp,
    } from '../types/api';
    ```

    **改动 3：新建 `frontend/src/components/eligibility/MyEligibilityPanel.tsx`**

    创建 directory（若不存在）+ 文件：

    ```typescript
    import axios from 'axios';
    import { Link } from 'react-router-dom';
    import { useCallback, useEffect, useState } from 'react';

    import { fetchMyEligibility } from '../../services/eligibilityService';
    import type { EligibilityResultWithTimestamp, EligibilityRuleResult } from '../../types/api';

    type LoadState =
      | { kind: 'loading' }
      | { kind: 'success'; data: EligibilityResultWithTimestamp }
      | { kind: 'unbound' }
      | { kind: 'employee_missing' }
      | { kind: 'error'; message: string };

    /**
     * Phase 32.1 ESELF-01/02/04/05: 员工自助调薪资格 panel
     * - 顶部 4 态徽章（eligible/ineligible/pending/未绑定）
     * - 4 条规则全展开（不折叠），按 status 4 色染色
     * - 右上时间戳「数据更新于 YYYY-MM-DD HH:MM」（zh-CN locale，无秒）
     * - 错误态处理：422 未绑定 / 404 档案缺失 / 500 重试
     */
    export function MyEligibilityPanel() {
      const [state, setState] = useState<LoadState>({ kind: 'loading' });

      const load = useCallback(async () => {
        setState({ kind: 'loading' });
        try {
          const data = await fetchMyEligibility();
          setState({ kind: 'success', data });
        } catch (err) {
          if (axios.isAxiosError(err)) {
            const status = err.response?.status;
            if (status === 422) { setState({ kind: 'unbound' }); return; }
            if (status === 404) { setState({ kind: 'employee_missing' }); return; }
          }
          setState({ kind: 'error', message: '资格信息暂时不可用，请稍后重试' });
        }
      }, []);

      useEffect(() => { void load(); }, [load]);

      return (
        <section className="surface px-6 py-8">
          <div style={{ display: 'flex', alignItems: 'flex-start', justifyContent: 'space-between', gap: 16 }}>
            <div>
              <p className="eyebrow">本期调薪</p>
              <h2 className="mt-2 text-[24px] font-semibold tracking-[-0.03em] text-ink">本人调薪资格</h2>
            </div>
            {state.kind === 'success' ? (
              <span className="text-xs text-steel" data-testid="data-updated-at">
                {formatTimestamp(state.data.data_updated_at)}
              </span>
            ) : null}
          </div>
          {state.kind === 'loading' ? <SkeletonRows /> : null}
          {state.kind === 'success' ? <EligibilityContent data={state.data} /> : null}
          {state.kind === 'unbound' ? <UnboundCard /> : null}
          {state.kind === 'employee_missing' ? <EmployeeMissingCard /> : null}
          {state.kind === 'error' ? <ErrorCard message={state.message} onRetry={load} /> : null}
        </section>
      );
    }

    function SkeletonRows() {
      return (
        <div className="mt-4 space-y-3" aria-busy="true">
          {[0, 1, 2, 3].map((i) => (
            <div key={i} className="surface-subtle px-5 py-4 animate-pulse" style={{ height: 56, opacity: 0.6 }} />
          ))}
        </div>
      );
    }

    function EligibilityContent({ data }: { data: EligibilityResultWithTimestamp }) {
      const ineligibleCount = data.rules.filter((r) => r.status === 'ineligible').length;
      const dataMissingCount = data.rules.filter((r) => r.status === 'data_missing').length;
      return (
        <>
          <div className="mt-4">
            <OverallBadge status={data.overall_status} ineligibleCount={ineligibleCount} dataMissingCount={dataMissingCount} />
          </div>
          <div className="mt-5 space-y-3" data-testid="rule-rows">
            {data.rules.map((rule) => <RuleRow key={rule.rule_code} rule={rule} />)}
          </div>
        </>
      );
    }

    function OverallBadge({ status, ineligibleCount, dataMissingCount }: {
      status: 'eligible' | 'ineligible' | 'pending';
      ineligibleCount: number;
      dataMissingCount: number;
    }) {
      const config = {
        eligible: { bg: '#d1fae5', color: '#065f46', label: '✓ 调薪资格合格' },
        ineligible: { bg: '#fee2e2', color: '#991b1b', label: `✗ 调薪资格不合格（${ineligibleCount} 项未通过）` },
        pending: { bg: '#fef3c7', color: '#92400e', label: `⏳ 资格待定（${dataMissingCount} 项数据缺失）` },
      }[status];
      return (
        <span className="status-pill" data-testid={`overall-badge-${status}`}
          style={{ background: config.bg, color: config.color, fontSize: 14, padding: '6px 12px' }}>
          {config.label}
        </span>
      );
    }

    const RULE_STATUS_CONFIG: Record<EligibilityRuleResult['status'],
      { borderColor: string; badgeBg: string; badgeColor: string; badgeLabel: string }> = {
      eligible: { borderColor: '#10b981', badgeBg: '#d1fae5', badgeColor: '#065f46', badgeLabel: '通过' },
      ineligible: { borderColor: '#ef4444', badgeBg: '#fee2e2', badgeColor: '#991b1b', badgeLabel: '未通过' },
      data_missing: { borderColor: '#9ca3af', badgeBg: '#e5e7eb', badgeColor: '#374151', badgeLabel: '数据缺失' },
      overridden: { borderColor: '#3b82f6', badgeBg: '#dbeafe', badgeColor: '#1e40af', badgeLabel: '已覆盖' },
    };

    function RuleRow({ rule }: { rule: EligibilityRuleResult }) {
      const cfg = RULE_STATUS_CONFIG[rule.status];
      return (
        <div className="surface-subtle px-5 py-4" data-testid={`rule-row-${rule.rule_code}`}
          style={{ borderLeft: `4px solid ${cfg.borderColor}`, display: 'flex', alignItems: 'center', justifyContent: 'space-between', gap: 16 }}>
          <div style={{ flex: 1 }}>
            <p className="text-base font-semibold text-ink">{rule.rule_label}</p>
            <p className="mt-1 text-sm" style={{ color: 'var(--color-steel)' }}>{rule.detail}</p>
          </div>
          <span className="status-pill" style={{ background: cfg.badgeBg, color: cfg.badgeColor, fontSize: 12, padding: '4px 10px' }}>
            {cfg.badgeLabel}
          </span>
        </div>
      );
    }

    function UnboundCard() {
      return (
        <div className="mt-4 surface-subtle px-5 py-5" data-testid="unbound-card" style={{ background: '#f3f4f6' }}>
          <p className="text-sm text-ink">您的账号尚未绑定员工档案。</p>
          <p className="mt-2 text-sm text-steel">
            请前往<Link to="/settings" className="ml-1" style={{ color: 'var(--color-primary)' }}>账号设置</Link>完成绑定，然后再查看本人调薪资格。
          </p>
        </div>
      );
    }

    function EmployeeMissingCard() {
      return (
        <div className="mt-4 surface-subtle px-5 py-5" data-testid="employee-missing-card" style={{ background: '#fef3c7' }}>
          <p className="text-sm text-ink">未找到您的员工档案。</p>
          <p className="mt-2 text-sm text-steel">您的账号绑定了一个不存在的员工记录，请联系 HR 核对。</p>
        </div>
      );
    }

    function ErrorCard({ message, onRetry }: { message: string; onRetry: () => void }) {
      return (
        <div className="mt-4 surface-subtle px-5 py-5" data-testid="error-card" style={{ background: '#fef2f2' }}>
          <p className="text-sm" style={{ color: 'var(--color-danger)' }}>{message}</p>
          <button type="button" className="chip-button mt-3" onClick={onRetry} data-testid="retry-button">
            重试
          </button>
        </div>
      );
    }

    function formatTimestamp(iso: string | null): string {
      if (!iso) return '数据从未更新';
      try {
        const d = new Date(iso);
        const formatter = new Intl.DateTimeFormat('zh-CN', { dateStyle: 'medium', timeStyle: 'short' });
        return `数据更新于 ${formatter.format(d)}`;
      } catch {
        return '数据时间无效';
      }
    }
    ```

    **不要做：**
    - 不要在组件内做任何业务规则计算（rules 直接来自 API）
    - 不要预设缓存策略（CONTEXT scope_constraints #6）
    - 不要把 panel 嵌进 employee 匹配判断里（panel 独立调 /me）
    - 不要折叠规则（D-12 全部摆开）
    - 不要给 panel 加 cycle/year 参数（D-10 「不依赖 cycle 选择」）
  </action>

  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust/frontend && npm run lint 2>&1 | tail -20</automated>
    <automated>grep -n "EligibilityResultWithTimestamp" /Users/mac/PycharmProjects/Wage_adjust/frontend/src/types/api.ts</automated>
    <automated>grep -n "fetchMyEligibility" /Users/mac/PycharmProjects/Wage_adjust/frontend/src/services/eligibilityService.ts</automated>
    <automated>test -f /Users/mac/PycharmProjects/Wage_adjust/frontend/src/components/eligibility/MyEligibilityPanel.tsx && wc -l /Users/mac/PycharmProjects/Wage_adjust/frontend/src/components/eligibility/MyEligibilityPanel.tsx | awk '{exit !($1>=150)}'</automated>
    <automated>grep -c "data-testid=" /Users/mac/PycharmProjects/Wage_adjust/frontend/src/components/eligibility/MyEligibilityPanel.tsx | awk '{exit !($1>=8)}'</automated>
  </verify>

  <acceptance_criteria>
    - `frontend/src/types/api.ts` 包含 `export type EligibilityResultWithTimestamp`
    - `frontend/src/services/eligibilityService.ts` 包含 `export async function fetchMyEligibility`
    - `frontend/src/components/eligibility/MyEligibilityPanel.tsx` 存在且 ≥ 150 行
    - 文件含 ≥ 8 个 `data-testid` 属性（便于 UAT 定位）
    - `npm run lint`（即 `tsc --noEmit`）退出码 0，无 TS 错误
    - 4 种状态徽章配色：eligible 绿(#d1fae5/#065f46) / ineligible 红(#fee2e2/#991b1b) / pending 黄(#fef3c7/#92400e)
    - 4 色行 borderLeft 见 RULE_STATUS_CONFIG 常量定义
    - 时间戳 formatter 用 `Intl.DateTimeFormat('zh-CN', { dateStyle: 'medium', timeStyle: 'short' })`（D-17）
    - 错误态处理 422→UnboundCard / 404→EmployeeMissingCard / 其他→ErrorCard + retry button（D-20）
  </acceptance_criteria>

  <done>
    类型 + service + 组件三件套就绪；TS 编译 PASS；data-testid 锚点齐全（≥8 个）便于浏览器 UAT 抓取。
  </done>
</task>

<task type="auto">
  <name>Task 2: MyReview 集成 MyEligibilityPanel + 联通性快速校验</name>
  <files>frontend/src/pages/MyReview.tsx</files>

  <read_first>
    - frontend/src/pages/MyReview.tsx:1-50（import 区，决定 import 顺序）
    - frontend/src/pages/MyReview.tsx:527-580（AppShell 内容树；插入点位于 line 539 isLoading 之后、line 541 fileQueue IIFE 之前）
    - frontend/src/components/eligibility/MyEligibilityPanel.tsx（Plan 02 Task 1 产出）
  </read_first>

  <action>
    **改动 1：`frontend/src/pages/MyReview.tsx` import 区**

    在 line 14（`import { AppShell } from '../components/layout/AppShell';`）之后追加：

    ```typescript
    import { MyEligibilityPanel } from '../components/eligibility/MyEligibilityPanel';
    ```

    保持 alphabetic / 域分组规则：MyEligibilityPanel 与其它 components 同级，按文件路径排序插入。

    **改动 2：`frontend/src/pages/MyReview.tsx` JSX body**

    在 line 539（`{isLoading ? <p className="px-2 text-sm text-steel">正在加载个人评估中心...</p> : null}`）**之后**、line 541（`{(() => { const currentDuplicate = fileQueue.find(...) ...})()}` IIFE）**之前**插入：

    ```jsx
          {/* Phase 32.1 ESELF-01/02/04/05: 员工自助调薪资格 panel
              位置：在「当前角色 actions」标头之后、「材料与提交概览」section 之前（D-10）
              独立于 employee 匹配 —— panel 直接调 /eligibility/me 由 user.employee_id 决定 */}
          <MyEligibilityPanel />
    ```

    **不要做：**
    - 不要把 `<MyEligibilityPanel />` 嵌进 `{employee ? ... : null}` 三元式（panel 独立于 employee 匹配）
    - 不要传任何 props（组件无参）
    - 不要修改 MyReview 现有 useEffect / state / handler
    - 不要修改 employee 匹配逻辑、fileQueue 处理、cycle 选择 UI

    **联通性自检（Claude 在标记 task 完成前自动执行）：**
    - 启动后端：后台运行 `cd /Users/mac/PycharmProjects/Wage_adjust && .venv/bin/uvicorn backend.app.main:app --port 8011`
    - 检查端点暴露：`curl -s http://localhost:8011/openapi.json | python3 -c "import sys,json; d=json.load(sys.stdin); assert '/api/v1/eligibility/me' in d['paths']; print('endpoint live')"`
    - 关闭后端进程
    - 前端 lint：`cd /Users/mac/PycharmProjects/Wage_adjust/frontend && npm run lint`
    - 前端 build：`cd /Users/mac/PycharmProjects/Wage_adjust/frontend && npm run build`
  </action>

  <verify>
    <automated>grep -n "import { MyEligibilityPanel }" /Users/mac/PycharmProjects/Wage_adjust/frontend/src/pages/MyReview.tsx</automated>
    <automated>grep -n "<MyEligibilityPanel" /Users/mac/PycharmProjects/Wage_adjust/frontend/src/pages/MyReview.tsx</automated>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust/frontend && npm run lint 2>&1 | tail -10</automated>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust/frontend && npm run build 2>&1 | tail -10</automated>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust && .venv/bin/python -c "from backend.app.main import create_app; from backend.app.core.config import Settings; app = create_app(Settings()); routes = [r.path for r in app.routes if hasattr(r, 'path')]; assert '/api/v1/eligibility/me' in routes; print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "import { MyEligibilityPanel }" frontend/src/pages/MyReview.tsx` == 1
    - `grep -c "<MyEligibilityPanel" frontend/src/pages/MyReview.tsx` == 1
    - JSX 中 `<MyEligibilityPanel />` 出现位置在 `isLoading ? ... : null}` 之后、`fileQueue.find` IIFE 之前（用 grep -n 行号判断）
    - `<MyEligibilityPanel />` **不**嵌套在 `{employee ? ... }` 条件块内（独立顶层渲染）
    - `npm run lint` 退出码 0
    - `npm run build` 退出码 0
    - 后端 OpenAPI 暴露 `/api/v1/eligibility/me`（来自 Plan 01）
  </acceptance_criteria>

  <done>
    MyReview.tsx 已成功 import + 渲染 MyEligibilityPanel；TS lint + build 全 PASS；后端端点联通。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 浏览器 UAT 4 项验证</name>
  <files>frontend/src/pages/MyReview.tsx, frontend/src/components/eligibility/MyEligibilityPanel.tsx</files>

  <action>
    本 task 是人工 UAT checkpoint。Claude 在此前已完成 Task 1 + Task 2 的所有自动化集成；此处不修改任何代码，只引导用户在浏览器中执行 4 项验证。Claude 收到 `approved` 后才能提交。
  </action>

  <verify>
    <automated>echo "manual UAT checkpoint — see how-to-verify"</automated>
  </verify>

  <done>
    用户回复 `approved` 表示 4 项 UAT 全 PASS；或回复 `Item N failed: <描述>` 触发修复循环。
  </done>

  <what-built>
    Task 1 + Task 2 已完成：
    - MyEligibilityPanel 组件（4 态 + 4 色规则行 + 时间戳 + 3 种错误卡片）
    - fetchMyEligibility service 函数 + EligibilityResultWithTimestamp 类型
    - MyReview.tsx 在「当前角色」之后渲染 panel（独立于 employee 匹配）
    - TS lint + build PASS；后端 /api/v1/eligibility/me 端点联通
  </what-built>

  <how-to-verify>
    **前置准备**（HR/admin 操作）：
    - 后端启动：`cd /Users/mac/PycharmProjects/Wage_adjust && .venv/bin/uvicorn backend.app.main:app --reload --port 8011`
    - 前端启动：`cd /Users/mac/PycharmProjects/Wage_adjust/frontend && npm run dev`（http://127.0.0.1:5174）
    - 准备 3 个测试账号（用 admin 登录后通过「员工管理」+「账号管理」创建）：
      a. `bound_employee@test.com` — role=employee，employee_id 已绑定到一个 active 员工
      b. `unbound@test.com` — role=employee，employee_id=None
      c. `bound_ineligible@test.com` — role=employee，employee_id 已绑定，且该员工 hire_date 设置为 2 个月前（触发 TENURE ineligible）

    **UAT 检查清单（4 项，全部 PASS 才能 approved）：**

    **Item 1（合格/规则全展示场景 + 时间戳，验证 ESELF-01 + ESELF-05）：**
    - 用账号 a 登录 → 跳转 /my-review
    - 期望：页面顶部出现「本人调薪资格」section（在「当前角色」chip 之后、「材料与提交概览」之前）
    - 期望：顶部状态徽章为绿底「✓ 调薪资格合格」**或** 黄底「⏳ 资格待定（N 项数据缺失）」（取决于该员工的实际数据完整度——重点验证徽章存在且颜色正确）
    - 期望：右上角显示「数据更新于 YYYY-MM-DD HH:MM」格式（如「数据更新于 2026年4月22日 09:38」），无秒数
    - 期望：4 条规则全部摆开（rule_code = TENURE / ADJUSTMENT_INTERVAL / PERFORMANCE / LEAVE），每行有 rule_label（中文）+ detail（次级文字）+ 右侧 status badge

    **Item 2（不合格场景 + 4 色规则行 + 脱敏，验证 ESELF-02）：**
    - 用账号 c 登录
    - 期望：顶部状态徽章为红底「✗ 调薪资格不合格（N 项未通过）」（N ≥ 1）
    - 期望：TENURE 行左条为红色（borderLeft: 4px solid #ef4444），detail 显示中文如「入职仅 2 个月，需满 6 个月」
    - 期望：detail 文本**不**含 YYYY-MM-DD 格式日期（如 `2026-04-22`），**不**含薪资数字（如 `12000`）
    - 期望：data_missing 状态行（如 PERFORMANCE 在测试环境无数据时）左条为灰色 + status badge「数据缺失」
    - 期望：detail 文本均为中文，无英文 stack trace 或 raw error message

    **Item 3（未绑定 422，验证 ESELF-04 + D-20）：**
    - 用账号 b 登录（未绑定员工）
    - 期望：「本人调薪资格」section 内显示灰底卡片，文本「您的账号尚未绑定员工档案。请前往**账号设置**完成绑定...」
    - 期望：「账号设置」是可点击 Link，点击跳转 /settings
    - 期望：浏览器 DevTools Network tab 显示 GET /api/v1/eligibility/me 返回 422
    - 期望：浏览器 DevTools Console 无 unhandled promise rejection 红错

    **Item 4（重试按钮 + 跨员工越权天然不可达，验证 ESELF-04 安全 + D-20）：**
    - 用账号 a 登录后，DevTools → Network → 选择 Offline → 刷新页面 → 看到「资格信息暂时不可用，请稍后重试」+ 「重试」按钮
    - 取消 Offline → 点击「重试」→ 期望恢复显示资格内容（成功重新调用 /me）
    - 用账号 a 登录，DevTools → Network 中找 /eligibility/me 请求 → 选 Edit and Resend：尝试改 URL 为 `/api/v1/eligibility/{某个其他 employee_id}` → 期望返回 401/403（被现有 require_roles 拦截，因 a 账号是 employee 角色，不在 admin/hrbp/manager 内）
    - 关键确认：a 账号永远拿不到员工 b、c、d 的资格数据 —— 这是 D-04 + T-32.1-01 的核心保证

    **回归校验（每项 UAT 通过后顺带验证）：**
    - 切回账号 a → /my-review 页面其它 section（材料与提交、证据摘要、文件列表）功能不变，无 console error
    - 用 admin 账号访问 /eligibility（HR 资格 batch 页）→ 旧 batch 列表功能不变（验证未影响 /{employee_id} 端点）
  </how-to-verify>

  <resume-signal>
    在每一项 UAT 通过后回复：
    - `approved` — 所有 4 项 PASS，标记 phase 完成
    - `Item N failed: <描述>` — 某项失败，附截图与错误信息，Claude 进入修复循环
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器 → 前端 (employee) | employee 用户在 /my-review 页面渲染 MyEligibilityPanel；组件直接调 /eligibility/me |
| 浏览器 DevTools 篡改 → API | 用户在 DevTools 手动改 URL 调 /eligibility/{otherEmployeeId} |
| 前端组件 → axios → API | 共享 api instance；JWT 拦截器自动注入 Authorization header |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32.1-01 | Information Disclosure | 用户在 DevTools 复制 /me 请求改成 /{employee_id} 拿别人数据 | mitigate | 端到端：(a) 前端 fetchMyEligibility 永远调 /me 无参；(b) 后端 /{employee_id} 端点用 `require_roles('admin', 'hrbp', 'manager')`，employee 角色访问该路径返回 403 — 已存在的旧契约（CONTEXT D-05 不修改）。本期 UAT Item 4 显式验证 |
| T-32.1-02 | Information Disclosure | 422 未绑定场景前端误显示空 panel 让员工以为「自己合格」 | mitigate | UnboundCard 明确显示「尚未绑定员工档案」+ 跳转「账号设置」，不显示任何资格判断。UAT Item 3 验证；data-testid='unbound-card' 锚点便于自动化复测 |
| T-32.1-03 | Information Disclosure | rules[].detail 字段在前端被错误地截断/截图分享时仍可能含敏感数据 | mitigate | (a) 前端不做二次脱敏（D-08：detail 来源已脱敏）；(b) UAT Item 2 显式人眼检查 detail 文本无 YYYY-MM-DD / 无薪资数字；(c) 已有后端 pytest 正则断言（Plan 01 Task 2 测试 T4） |
| T-32.1-04 | Routing Bypass | 前端组件依赖后端路由顺序正确；如果后端把 /me 后注册，'me' 会被 /{employee_id} 当作 employee_id 捕获 → check_eligibility(employee_id='me') | mitigate | 由 Plan 01 Task 2 acceptance_criteria 内 Python 字符串位置断言保证；前端 Item 1 的 200 响应 + 正确数据间接证明 |
| T-32.1-06 | Denial of Service | 用户狂点「重试」按钮可能放大对后端的查询压力 | accept | 单 employee 调用，频率受限于人手速度；axios 默认无 throttle 但 backend SQLite 单查询 < 5ms。生产环境可加前端 debounce，本期不引入（CONTEXT scope_constraints #6） |
</threat_model>

<verification>
1. `cd frontend && npm run lint` — TS 编译通过（`tsc --noEmit`）
2. `cd frontend && npm run build` — 生产构建通过
3. 浏览器 UAT 4 项全 PASS（见 Task 3 how-to-verify）
4. 浏览器 DevTools Console 全程无 red error / unhandled rejection
5. 回归：admin/hrbp 用户登录 /eligibility 页面（资格批量列表）功能不变
</verification>

<success_criteria>
- ✅ EligibilityResultWithTimestamp 类型导出（D-19）
- ✅ fetchMyEligibility() service 函数无参，调 GET /eligibility/me（D-18）
- ✅ MyEligibilityPanel 组件含 4 态徽章 + 4 色规则行 + 时间戳 + 3 种错误态卡片（D-10/11/12/13/14/20）
- ✅ MyReview.tsx 在 AppShell 内固定位置渲染 panel（在「当前角色」之后、其他 section 之前）
- ✅ 浏览器 UAT 4 项全 PASS（合格 / 不合格 / 未绑定 / 重试 + 越权防护）
- ✅ TS 编译 + 构建均通过
- ✅ 旧资格 batch 页面无回归
</success_criteria>

<output>
完成后创建 `.planning/phases/32.1-employee-eligibility-visibility/32.1-02-SUMMARY.md`，覆盖：
- 组件文件位置 + 行数
- 4 种 LoadState 处理代码段
- UAT 4 项截图引用
- 时间戳渲染示例（实际看到的字符串）
- 越权防护验证步骤截图
- 任何 UAT 期间发现的小问题与修复
</output>
