---
phase: 32-eligibility-import-completion
plan: 03
type: execute
wave: 2
depends_on:
  - 32-01
files_modified:
  - backend/app/services/import_service.py
  - backend/app/schemas/import_preview.py
  - backend/tests/test_services/test_import_lock.py
  - backend/tests/test_services/test_import_expire_stale.py
  - backend/tests/test_services/test_import_staged_path_safety.py
autonomous: true
requirements:
  - IMPORT-06
  - IMPORT-07

must_haves:
  truths:
    - "ImportService.is_import_running(import_type) 仅查 'previewing' 与 'processing' 状态的 ImportJob，per-import_type 锁可同时支持 4 类不同类型并行"
    - "ImportService.expire_stale_import_jobs() 双时限清理：processing 30min → failed；previewing 60min → cancelled + 删暂存文件"
    - "暂存文件路径 _staged_file_path(job_id) 防路径遍历（job_id 含 ../ → ValueError）"
    - "PreviewResponse Pydantic schema 含 counters / rows / rows_truncated 三字段，rows 最多 200 行（D-07 / D-08）"
    - "_detect_in_file_conflicts 在同一批文件内按 D-14 业务键分组检测重复，返回 {pandas_idx: 冲突原因}"
  artifacts:
    - path: backend/app/services/import_service.py
      provides: "并发锁 / expire / 暂存文件 / preview / confirm 五大新方法"
      contains: "def is_import_running"
    - path: backend/app/schemas/import_preview.py
      provides: "PreviewResponse / ConfirmResponse / ActiveJobResponse / PreviewRow Pydantic schemas"
      contains: "class PreviewResponse"
    - path: backend/tests/test_services/test_import_lock.py
      provides: "is_import_running 单测：filters by type、previewing+processing 都持锁"
      contains: "def test_is_import_running_filters_by_type"
    - path: backend/tests/test_services/test_import_expire_stale.py
      provides: "expire_stale_import_jobs 单测：双时限 + 删文件 + 不影响其他状态"
      contains: "def test_expire_stale_processing_to_failed"
    - path: backend/tests/test_services/test_import_staged_path_safety.py
      provides: "_staged_file_path 防路径遍历单测"
      contains: "def test_staged_path_rejects_traversal"
  key_links:
    - from: backend/app/services/import_service.py
      to: backend/app/services/feishu_service.py
      via: "is_import_running 模式直接抄 feishu_service:1685-1696；expire_stale_import_jobs 抄 1698-1717"
      pattern: "is_import_running"
    - from: backend/app/services/import_service.py
      to: backend/app/core/storage.py
      via: "_staged_file_path 复用 LocalStorageService 路径遍历防护模式（is_relative_to）"
      pattern: "is_relative_to"
    - from: backend/app/schemas/import_preview.py
      to: backend/app/services/import_service.py
      via: "build_preview 返回 PreviewResponse 实例（service → schema 单向依赖）"
      pattern: "PreviewResponse"
---

<objective>
建立 Phase 32 两阶段提交（preview + confirm）+ 并发锁 + 暂存文件 + 僵尸清理的核心服务层基础设施（IMPORT-06 + IMPORT-07）。Plan 04（API 端点）和 Plan 06（前端）都依赖本 plan 完成。

具体实现：
1. **PreviewResponse schema** — D-07 PreviewResponse 结构定义（Pydantic v2 BaseModel）
2. **`is_import_running(import_type)`** — D-16 per-import_type 锁，包含 'previewing' + 'processing' 双状态
3. **`expire_stale_import_jobs(processing_timeout=30, previewing_timeout=60)`** — D-17 双时限清理，删暂存文件
4. **`_staged_file_path(job_id) / _save_staged_file / _read_staged_file / _delete_staged_file`** — D-06 暂存文件管理 + 路径遍历防护（Pitfall 3）
5. **`build_preview(import_type, file_name, raw_bytes)`** — preview 阶段：解析 + 冲突检测 + diff 计算 + 文件存盘 + 写 ImportJob status='previewing'
6. **`_detect_in_file_conflicts(import_type, dataframe)`** — D-09 同文件业务键重复检测
7. **`confirm_import(job_id, overwrite_mode, current_user)`** — confirm 阶段：读暂存 + 落库 + 写 AuditLog + 删暂存 + status='processing'/'completed'/'partial'/'failed'
8. **`cancel_import(job_id)`** — HR 显式取消 → status='cancelled' + 删暂存
9. **文件 hash 校验**（解决 Open Question 5 + F3）— preview 时存 sha256，confirm 时校验

Purpose: 把 Phase 32 的两阶段交付能力放在 service 层，API 层（Plan 04）只是薄路由。

Output:
- ImportService 5 大新方法 + 1 个 helper
- 1 个新 Pydantic schema 文件
- 3 个新测试文件覆盖锁 / expire / 路径安全
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-eligibility-import-completion/32-01-SUMMARY.md
@.planning/phases/32-eligibility-import-completion/32-CONTEXT.md
@.planning/phases/32-eligibility-import-completion/32-RESEARCH.md
@.planning/phases/31-feishu-sync-observability/31-CONTEXT.md
@CLAUDE.md
@backend/app/services/import_service.py
@backend/app/services/feishu_service.py
@backend/app/core/storage.py
@backend/app/core/config.py
@backend/app/models/import_job.py
@backend/app/models/audit_log.py

<interfaces>
<!-- Phase 31 既定锁模式（直接抄）-->
backend/app/services/feishu_service.py:1685-1696（D-16 参考）：
```python
def is_sync_running(self, sync_type: str | None = None) -> bool:
    stmt = select(FeishuSyncLog).where(FeishuSyncLog.status == 'running')
    if sync_type is not None:
        stmt = stmt.where(FeishuSyncLog.sync_type == sync_type)
    return self.db.execute(stmt.limit(1)).scalar_one_or_none() is not None
```

<!-- Phase 31 既定 expire 模式 -->
backend/app/services/feishu_service.py:1698-1717（D-17 参考）：
```python
def expire_stale_running_logs(self, *, timeout_minutes: int = 30) -> int:
    cutoff = datetime.now(timezone.utc) - timedelta(minutes=timeout_minutes)
    stale = list(self.db.execute(
        select(FeishuSyncLog).where(
            FeishuSyncLog.status == 'running',
            FeishuSyncLog.created_at < cutoff,
        )
    ).scalars().all())
    for log in stale:
        log.status = 'failed'
        # ...
    self.db.commit()
    return len(stale)
```

<!-- 路径遍历防护模式（既定）-->
backend/app/core/storage.py:23-30：
```python
target = (base_dir / safe_filename).resolve()
if not target.is_relative_to(base_dir.resolve()):
    raise ValueError(f'Path traversal attempt: {safe_filename!r}')
```

<!-- AuditLog 真实字段（D-13 文档术语错误，必须用真实字段）-->
backend/app/models/audit_log.py:
- operator_id (NOT actor_id)
- target_type (NOT resource_type)
- target_id (NOT resource_id)
- detail: dict
- operator_role
- request_id

<!-- ImportJob 终态判断常量 -->
建议在 ImportService 类加：
_TERMINAL_STATUSES = frozenset({'completed', 'failed', 'partial', 'cancelled'})
_LOCKING_STATUSES = frozenset({'previewing', 'processing'})

<!-- 业务键映射（D-14） -->
business_keys = {
    'performance_grades': ['employee_no', 'year'],
    'salary_adjustments': ['employee_no', 'adjustment_date', 'adjustment_type'],  # 与 _import_salary_adjustments 一致
    'hire_info': ['employee_no'],
    'non_statutory_leave': ['employee_no', 'year'],
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PreviewResponse schema + is_import_running + expire_stale_import_jobs + _staged_file_path（含路径遍历防护）</name>
  <files>
    backend/app/schemas/import_preview.py,
    backend/app/services/import_service.py,
    backend/tests/test_services/test_import_lock.py,
    backend/tests/test_services/test_import_expire_stale.py,
    backend/tests/test_services/test_import_staged_path_safety.py
  </files>
  <read_first>
    - backend/app/services/feishu_service.py:1685-1717（is_sync_running + expire_stale_running_logs 完整实现 — Task 1 直接照抄结构）
    - backend/app/core/storage.py 全文（LocalStorageService 路径遍历防护模式）
    - backend/app/core/config.py（确认 storage_base_dir 字段名与默认值 'uploads'）
    - backend/app/schemas/feishu.py（参考 Pydantic v2 schema 风格 — model_config = ConfigDict(from_attributes=True)）
    - backend/app/services/import_service.py（确认 ImportService.__init__ 签名，新方法挂在 self.db）
    - backend/app/models/import_job.py（确认 32-01 已加 overwrite_mode/actor_id/updated_at）
  </read_first>
  <behavior>
    - Test 1: PreviewResponse 实例化 {counters: {insert:0,...}, rows: [], rows_truncated: False, job_id: 'xxx', total_rows: 0, import_type: 'hire_info'} 通过
    - Test 2: is_import_running('hire_info') 返回 False 当无任何 ImportJob
    - Test 3: 创建 status='processing' 的 ImportJob → is_import_running('hire_info') = True
    - Test 4: 创建 status='previewing' 的 ImportJob → is_import_running('hire_info') = True
    - Test 5: 创建 status='completed' 的 ImportJob → is_import_running('hire_info') = False（终态不持锁）
    - Test 6: 不同 import_type 互不影响：is_import_running('hire_info') 不被 'performance_grades' 的活跃 job 触发
    - Test 7: expire_stale_import_jobs() 把 created_at > 30min 的 'processing' job 标 'failed' + result_summary 含 'error': 'timeout'
    - Test 8: expire_stale_import_jobs() 把 created_at > 60min 的 'previewing' job 标 'cancelled' + 调用 _delete_staged_file（如文件存在则删除，不存在不报错）
    - Test 9: expire_stale_import_jobs() 不影响 'completed' / 'failed' job（终态保持）
    - Test 10: _staged_file_path('valid-uuid') 返回 base/imports/valid-uuid.xlsx
    - Test 11: _staged_file_path('../etc/passwd') 抛 ValueError
    - Test 12: _staged_file_path('valid/../../escape') 抛 ValueError
  </behavior>
  <action>
    1) 创建 `backend/app/schemas/import_preview.py`：

    ```python
    """Phase 32 两阶段提交 schemas。

    PreviewResponse: D-07 preview 阶段返回结构
    ConfirmResponse: confirm 阶段返回结构
    ActiveJobResponse: D-18 GET /excel/active 返回结构
    """
    from __future__ import annotations
    from typing import Any, Literal, Optional
    from datetime import datetime
    from pydantic import BaseModel, ConfigDict, Field


    class FieldDiff(BaseModel):
        """字段级 diff（D-08）：old_value → new_value 并排"""
        model_config = ConfigDict(extra='forbid')
        old: Any | None = None
        new: Any | None = None


    class PreviewRow(BaseModel):
        """preview 单行结果（D-07）"""
        model_config = ConfigDict(extra='forbid')
        row_number: int = Field(..., description='Excel 行号（含表头偏移；与 HR 看到的行号一致）')
        action: Literal['insert', 'update', 'no_change', 'conflict']
        employee_no: str
        fields: dict[str, FieldDiff] = Field(default_factory=dict)
        conflict_reason: Optional[str] = None


    class PreviewCounters(BaseModel):
        model_config = ConfigDict(extra='forbid')
        insert: int = 0
        update: int = 0
        no_change: int = 0
        conflict: int = 0


    class PreviewResponse(BaseModel):
        """D-07: preview 端点返回结构"""
        model_config = ConfigDict(extra='forbid')
        job_id: str
        import_type: str
        file_name: str
        total_rows: int
        counters: PreviewCounters
        rows: list[PreviewRow] = Field(default_factory=list, max_length=200)
        rows_truncated: bool = False
        truncated_count: int = 0
        preview_expires_at: datetime
        file_sha256: str = Field(..., description='文件 hash，confirm 阶段校验防止外部篡改')


    class ConfirmRequest(BaseModel):
        """confirm 端点请求体（job_id 在 URL path）"""
        model_config = ConfigDict(extra='forbid')
        overwrite_mode: Literal['merge', 'replace'] = 'merge'
        confirm_replace: bool = Field(
            default=False,
            description='replace 模式下必须同时为 True（与前端二次确认 modal 的 checkbox 对应）',
        )


    class ConfirmResponse(BaseModel):
        model_config = ConfigDict(extra='forbid')
        job_id: str
        status: Literal['completed', 'partial', 'failed']
        total_rows: int
        inserted_count: int
        updated_count: int
        no_change_count: int
        failed_count: int
        execution_duration_ms: int


    class ActiveJobResponse(BaseModel):
        """D-18: GET /excel/active?import_type=X 返回"""
        model_config = ConfigDict(extra='forbid')
        active: bool
        job_id: Optional[str] = None
        status: Optional[Literal['previewing', 'processing']] = None
        created_at: Optional[datetime] = None
        file_name: Optional[str] = None
    ```

    2) 修改 `backend/app/services/import_service.py` — 在类顶部加常量与 import：

    ```python
    # 在文件顶部 import 区域加：
    import hashlib
    from datetime import datetime, timedelta, timezone
    from pathlib import Path

    from backend.app.core.config import get_settings

    # 在 ImportService 类内常量区域加：
    _TERMINAL_STATUSES = frozenset({'completed', 'failed', 'partial', 'cancelled'})
    _LOCKING_STATUSES = frozenset({'previewing', 'processing'})

    # D-09 业务键映射（与 _import_* 业务键一致；hire_info=employee_no 对应 employee_id PK）
    _BUSINESS_KEYS = {
        'performance_grades': ['employee_no', 'year'],
        'salary_adjustments': ['employee_no', 'adjustment_date', 'adjustment_type'],
        'hire_info': ['employee_no'],
        'non_statutory_leave': ['employee_no', 'year'],
    }
    ```

    3) 在 `ImportService` 加新方法（推荐放在 `expire_stale_import_jobs` 之前，逻辑相邻）：

    ```python
    # === D-16 并发锁 ===

    def is_import_running(self, import_type: str | None = None) -> bool:
        """D-16: 检测同 import_type 是否有活跃 job（previewing 或 processing）。"""
        from sqlalchemy import select
        stmt = select(ImportJob).where(ImportJob.status.in_(list(self._LOCKING_STATUSES)))
        if import_type is not None:
            stmt = stmt.where(ImportJob.import_type == import_type)
        return self.db.execute(stmt.limit(1)).scalar_one_or_none() is not None

    def get_active_job(self, import_type: str) -> ImportJob | None:
        """D-18: 取该 import_type 的活跃 job（最多一条；多条时返回最新创建的）。"""
        from sqlalchemy import select
        stmt = (select(ImportJob)
                .where(ImportJob.status.in_(list(self._LOCKING_STATUSES)),
                       ImportJob.import_type == import_type)
                .order_by(ImportJob.created_at.desc())
                .limit(1))
        return self.db.execute(stmt).scalar_one_or_none()

    # === D-17 僵尸清理 ===

    def expire_stale_import_jobs(
        self,
        *,
        processing_timeout_minutes: int = 30,
        previewing_timeout_minutes: int = 60,
    ) -> dict[str, int]:
        """D-17: 双时限清理 — processing 30min → failed；previewing 60min → cancelled + 删暂存。"""
        from sqlalchemy import select
        now = datetime.now(timezone.utc)
        expired = {'processing': 0, 'previewing': 0}

        cutoff_p = now - timedelta(minutes=processing_timeout_minutes)
        stale_p = list(self.db.execute(
            select(ImportJob).where(
                ImportJob.status == 'processing',
                ImportJob.created_at < cutoff_p,
            )
        ).scalars().all())
        for job in stale_p:
            job.status = 'failed'
            existing = job.result_summary or {}
            job.result_summary = {**existing, 'error': 'timeout', 'expired_at': now.isoformat()}
        expired['processing'] = len(stale_p)

        cutoff_v = now - timedelta(minutes=previewing_timeout_minutes)
        stale_v = list(self.db.execute(
            select(ImportJob).where(
                ImportJob.status == 'previewing',
                ImportJob.created_at < cutoff_v,
            )
        ).scalars().all())
        for job in stale_v:
            job.status = 'cancelled'
            existing = job.result_summary or {}
            job.result_summary = {**existing, 'cancellation_reason': 'preview_timeout',
                                  'cancelled_at': now.isoformat()}
            try:
                self._delete_staged_file(job.id)
            except Exception:
                logger.exception('Failed to delete staged file for expired job %s', job.id)
        expired['previewing'] = len(stale_v)

        if stale_p or stale_v:
            self.db.commit()
        return expired

    # === D-06 暂存文件管理（含路径遍历防护 - Pitfall 3）===

    @staticmethod
    def _staged_file_path(job_id: str) -> Path:
        """安全的暂存文件路径，job_id 必须是 UUID（无路径分隔符）。"""
        # 双重防护：先字符校验 + 再 resolve 后 is_relative_to
        if not job_id or '/' in job_id or '\\' in job_id or '..' in job_id:
            raise ValueError(f'Invalid job_id (path traversal blocked): {job_id!r}')
        base = (Path(get_settings().storage_base_dir).resolve() / 'imports').resolve()
        base.mkdir(parents=True, exist_ok=True)
        target = (base / f'{job_id}.xlsx').resolve()
        if not target.is_relative_to(base):
            raise ValueError(f'Staged file path escapes base_dir: {job_id!r}')
        return target

    def _save_staged_file(self, job_id: str, content: bytes) -> str:
        """存盘 + 返回 sha256（preview 阶段写入 result_summary.preview.file_sha256）。"""
        path = self._staged_file_path(job_id)
        path.write_bytes(content)
        return hashlib.sha256(content).hexdigest()

    def _read_staged_file(self, job_id: str, *, expected_sha256: str | None = None) -> bytes:
        """读取暂存文件，可选校验 hash（confirm 阶段防外部篡改）。"""
        path = self._staged_file_path(job_id)
        if not path.exists():
            raise ValueError(f'Staged file for job {job_id} not found (expired or deleted)')
        content = path.read_bytes()
        if expected_sha256 is not None:
            actual = hashlib.sha256(content).hexdigest()
            if actual != expected_sha256:
                raise ValueError(
                    f'Staged file hash mismatch for job {job_id}: '
                    f'expected {expected_sha256[:8]}..., got {actual[:8]}... '
                    f'(file modified externally; please re-upload)'
                )
        return content

    def _delete_staged_file(self, job_id: str) -> None:
        path = self._staged_file_path(job_id)
        if path.exists():
            path.unlink()
    ```

    **关键安全点：** `_staged_file_path` 字符级 + resolve 双重校验（防 `../etc/passwd` 与 `valid/../../escape`）。

    4) 创建测试 `backend/tests/test_services/test_import_lock.py`：

    ```python
    from __future__ import annotations
    import pytest


    def test_is_import_running_no_jobs(db_session):
        from backend.app.services.import_service import ImportService
        svc = ImportService(db_session)
        assert svc.is_import_running('hire_info') is False
        assert svc.is_import_running() is False

    def test_is_import_running_filters_by_type(db_session, import_job_factory):
        from backend.app.services.import_service import ImportService
        import_job_factory(import_type='hire_info', status='processing')
        svc = ImportService(db_session)
        assert svc.is_import_running('hire_info') is True
        assert svc.is_import_running('performance_grades') is False
        assert svc.is_import_running() is True  # 不传 type 时全局检查

    def test_is_import_running_locks_previewing(db_session, import_job_factory):
        from backend.app.services.import_service import ImportService
        import_job_factory(import_type='non_statutory_leave', status='previewing')
        svc = ImportService(db_session)
        assert svc.is_import_running('non_statutory_leave') is True

    def test_is_import_running_terminal_does_not_lock(db_session, import_job_factory):
        from backend.app.services.import_service import ImportService
        for status in ['completed', 'failed', 'partial', 'cancelled']:
            import_job_factory(import_type='hire_info', status=status)
        svc = ImportService(db_session)
        assert svc.is_import_running('hire_info') is False

    def test_get_active_job_returns_latest(db_session, import_job_factory):
        from backend.app.services.import_service import ImportService
        old = import_job_factory(import_type='hire_info', status='previewing')
        new = import_job_factory(import_type='hire_info', status='processing')
        svc = ImportService(db_session)
        active = svc.get_active_job('hire_info')
        assert active is not None
        assert active.id == new.id  # 最新创建优先
    ```

    5) 创建测试 `backend/tests/test_services/test_import_expire_stale.py`：

    ```python
    from __future__ import annotations
    from datetime import datetime, timedelta, timezone
    import pytest


    def test_expire_stale_processing_to_failed(db_session, import_job_factory, monkeypatch):
        from backend.app.services.import_service import ImportService
        from backend.app.models.import_job import ImportJob
        # 制造一个超时 job（手动改 created_at）
        job = import_job_factory(import_type='hire_info', status='processing')
        old_time = datetime.now(timezone.utc) - timedelta(minutes=45)
        job.created_at = old_time
        db_session.commit()
        svc = ImportService(db_session)
        result = svc.expire_stale_import_jobs(processing_timeout_minutes=30)
        assert result['processing'] == 1
        db_session.refresh(job)
        assert job.status == 'failed'
        assert job.result_summary.get('error') == 'timeout'

    def test_expire_stale_previewing_to_cancelled_and_deletes_file(
        db_session, import_job_factory, tmp_uploads_dir,
    ):
        from backend.app.services.import_service import ImportService
        from datetime import datetime, timedelta, timezone
        job = import_job_factory(import_type='hire_info', status='previewing')
        # 创建假暂存文件
        svc = ImportService(db_session)
        svc._save_staged_file(job.id, b'fake xlsx bytes')
        assert svc._staged_file_path(job.id).exists()
        # 倒车 created_at
        job.created_at = datetime.now(timezone.utc) - timedelta(minutes=70)
        db_session.commit()
        result = svc.expire_stale_import_jobs(previewing_timeout_minutes=60)
        assert result['previewing'] == 1
        db_session.refresh(job)
        assert job.status == 'cancelled'
        assert not svc._staged_file_path(job.id).exists()  # 文件已删

    def test_expire_stale_does_not_touch_terminal(db_session, import_job_factory):
        from backend.app.services.import_service import ImportService
        from datetime import datetime, timedelta, timezone
        job = import_job_factory(import_type='hire_info', status='completed')
        job.created_at = datetime.now(timezone.utc) - timedelta(hours=10)
        db_session.commit()
        svc = ImportService(db_session)
        result = svc.expire_stale_import_jobs()
        assert result == {'processing': 0, 'previewing': 0}
        db_session.refresh(job)
        assert job.status == 'completed'

    def test_expire_stale_no_jobs(db_session):
        from backend.app.services.import_service import ImportService
        svc = ImportService(db_session)
        assert svc.expire_stale_import_jobs() == {'processing': 0, 'previewing': 0}
    ```

    6) 创建测试 `backend/tests/test_services/test_import_staged_path_safety.py`：

    ```python
    from __future__ import annotations
    import pytest


    def test_staged_path_valid_uuid(db_session, tmp_uploads_dir):
        from backend.app.services.import_service import ImportService
        svc = ImportService(db_session)
        path = svc._staged_file_path('123e4567-e89b-12d3-a456-426614174000')
        assert str(tmp_uploads_dir) in str(path)
        assert path.name == '123e4567-e89b-12d3-a456-426614174000.xlsx'

    def test_staged_path_rejects_traversal_dotdot(db_session, tmp_uploads_dir):
        from backend.app.services.import_service import ImportService
        svc = ImportService(db_session)
        with pytest.raises(ValueError, match='path traversal'):
            svc._staged_file_path('../etc/passwd')

    def test_staged_path_rejects_traversal_slash(db_session, tmp_uploads_dir):
        from backend.app.services.import_service import ImportService
        svc = ImportService(db_session)
        with pytest.raises(ValueError, match='path traversal'):
            svc._staged_file_path('valid/../../escape')

    def test_staged_path_rejects_backslash(db_session, tmp_uploads_dir):
        from backend.app.services.import_service import ImportService
        svc = ImportService(db_session)
        with pytest.raises(ValueError, match='path traversal'):
            svc._staged_file_path('windows\\escape')

    def test_staged_path_rejects_empty(db_session, tmp_uploads_dir):
        from backend.app.services.import_service import ImportService
        svc = ImportService(db_session)
        with pytest.raises(ValueError, match='path traversal'):
            svc._staged_file_path('')

    def test_save_and_read_staged_with_hash(db_session, tmp_uploads_dir):
        from backend.app.services.import_service import ImportService
        import hashlib
        svc = ImportService(db_session)
        job_id = '123e4567-e89b-12d3-a456-426614174000'
        content = b'fake xlsx bytes for test'
        sha = svc._save_staged_file(job_id, content)
        assert sha == hashlib.sha256(content).hexdigest()
        # 读回 + hash 校验通过
        read_back = svc._read_staged_file(job_id, expected_sha256=sha)
        assert read_back == content

    def test_read_staged_hash_mismatch_raises(db_session, tmp_uploads_dir):
        from backend.app.services.import_service import ImportService
        svc = ImportService(db_session)
        job_id = '123e4567-e89b-12d3-a456-426614174001'
        svc._save_staged_file(job_id, b'original')
        with pytest.raises(ValueError, match='hash mismatch'):
            svc._read_staged_file(job_id, expected_sha256='wrong_hash_value' * 4)
    ```
  </action>
  <verify>
    <automated>
      .venv/bin/pytest backend/tests/test_services/test_import_lock.py backend/tests/test_services/test_import_expire_stale.py backend/tests/test_services/test_import_staged_path_safety.py -x --tb=short
    </automated>
  </verify>
  <acceptance_criteria>
    - 文件 backend/app/schemas/import_preview.py 存在且 grep "class PreviewResponse" 命中
    - grep "class ConfirmRequest" backend/app/schemas/import_preview.py 命中
    - grep "class ActiveJobResponse" backend/app/schemas/import_preview.py 命中
    - grep "def is_import_running" backend/app/services/import_service.py 命中
    - grep "def expire_stale_import_jobs" backend/app/services/import_service.py 命中
    - grep "def _staged_file_path" backend/app/services/import_service.py 命中
    - grep "is_relative_to" backend/app/services/import_service.py 命中（路径遍历防护）
    - grep "_LOCKING_STATUSES = frozenset" backend/app/services/import_service.py 命中
    - 命令 `.venv/bin/pytest backend/tests/test_services/test_import_lock.py -x` 5 个测试全 PASS
    - 命令 `.venv/bin/pytest backend/tests/test_services/test_import_expire_stale.py -x` 4 个测试全 PASS
    - 命令 `.venv/bin/pytest backend/tests/test_services/test_import_staged_path_safety.py -x` 7 个测试全 PASS
  </acceptance_criteria>
  <done>
    并发锁 / 双时限 expire / 暂存文件管理（含路径遍历防护）+ PreviewResponse schema 全部就绪；16+ 测试 PASS。Plan 04 API 端点可直接调用 svc.is_import_running / svc.expire_stale_import_jobs / svc._save_staged_file。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2a: build_preview + _detect_in_file_conflicts + _build_row_diff（preview 阶段；不含 confirm/cancel）</name>
  <files>
    backend/app/services/import_service.py,
    backend/tests/test_services/test_import_preview.py
  </files>
  <read_first>
    - backend/app/services/import_service.py（Task 1 完成的 is_import_running / expire / _staged_file_path / _save_staged_file 方法签名）
    - backend/app/schemas/import_preview.py（PreviewResponse / PreviewRow / PreviewCounters / FieldDiff Task 1 已建）
    - backend/app/services/import_service.py:188-252（run_import 现有实现 — preview 仅借用 _load_table / _normalize_columns，不调 _dispatch_import）
    - backend/app/models/non_statutory_leave.py / performance_record.py / salary_adjustment_record.py / employee.py（_build_row_diff 各分支查询的目标表）
  </read_first>
  <behavior>
    - Test 1: build_preview('hire_info', file_name, valid_xlsx_bytes) → 返回 PreviewResponse，job_id 已写库（status='previewing'），暂存文件已存盘
    - Test 2: build_preview 检测同业务键冲突（D-09）→ counters.conflict > 0 + 对应 row.action == 'conflict'
    - Test 3: build_preview rows > 200 → rows_truncated=True，rows[:200] 只含 conflict + insert + update（no_change 优先截断）
    - Test 4: build_preview 写入文件 sha256 到 result_summary.preview.file_sha256
    - Test 5: _detect_in_file_conflicts(non_statutory_leave, df with dup) → 返回 dict 含两个 idx
    - Test 6: _build_row_diff(hire_info, row 已有 emp 且新值不同) → ('update', {'hire_date': {old, new}})
    - Test 7: _build_row_diff(performance_grades, row 新员工) → ('insert', {'grade': {old: None, new: ...}})
  </behavior>
  <action>
    1) 在 `ImportService` 加 `_detect_in_file_conflicts`：

    ```python
    def _detect_in_file_conflicts(
        self, import_type: str, dataframe: pd.DataFrame,
    ) -> dict[int, str]:
        """D-09: 同一批文件内业务键重复 → {pandas_idx: conflict_reason}。"""
        keys = self._BUSINESS_KEYS.get(import_type)
        if not keys or not all(k in dataframe.columns for k in keys):
            return {}
        # 按业务键分组找 count > 1 的 group
        # 注意：pandas dropna=False 保证 NaN 也参与分组（避免空值被忽略）
        grouped = dataframe.groupby(keys, dropna=False)
        conflicts: dict[int, str] = {}
        for key_tuple, group in grouped:
            if len(group) > 1:
                if not isinstance(key_tuple, tuple):
                    key_tuple = (key_tuple,)
                key_str = ', '.join(f'{k}={v}' for k, v in zip(keys, key_tuple))
                reason = f'同文件内 ({key_str}) 出现 {len(group)} 次，请仅保留一行后重新上传'
                for idx in group.index:
                    conflicts[int(idx)] = reason
        return conflicts
    ```

    2) 在 `ImportService` 加 `_build_row_diff`（per-type 内联查询；不真正落库）：

    ```python
    def _build_row_diff(
        self, import_type: str, row: 'pd.Series',
    ) -> tuple[str, dict[str, dict]]:
        """逐行算 diff：返回 (action, fields_dict)。

        简化策略：仅查 DB 现状对比新值，不真正落库。各 import_type 的查询逻辑：
          - hire_info: 找 Employee by employee_no，对比 hire_date / last_salary_adjustment_date
          - non_statutory_leave: 找 NonStatutoryLeave by (employee_id, year)，对比 total_days / leave_type
          - performance_grades: 找 PerformanceRecord by (employee_id, year)，对比 grade
          - salary_adjustments: 找 SalaryAdjustmentRecord by (employee_id, adjustment_date, adjustment_type)，对比 amount
        """
        from sqlalchemy import select
        from backend.app.models.employee import Employee
        from backend.app.models.non_statutory_leave import NonStatutoryLeave
        from backend.app.models.performance_record import PerformanceRecord
        from backend.app.models.salary_adjustment_record import SalaryAdjustmentRecord

        emp_no = str(row.get('employee_no', '')).strip()
        if not emp_no:
            return 'no_change', {}

        emp = self.db.execute(
            select(Employee).where(Employee.employee_no == emp_no)
        ).scalar_one_or_none()
        if emp is None:
            return 'conflict', {}

        if import_type == 'hire_info':
            new_hire = self._parse_excel_date(row.get('hire_date'))
            new_last_adj = self._parse_excel_date(row.get('last_salary_adjustment_date'))
            old_hire = emp.hire_date
            old_last_adj = emp.last_salary_adjustment_date
            fields = {}
            changed = False
            if new_hire is not None and new_hire != old_hire:
                fields['hire_date'] = {'old': old_hire.isoformat() if old_hire else None,
                                       'new': new_hire.isoformat()}
                changed = True
            if new_last_adj is not None and new_last_adj != old_last_adj:
                fields['last_salary_adjustment_date'] = {
                    'old': old_last_adj.isoformat() if old_last_adj else None,
                    'new': new_last_adj.isoformat(),
                }
                changed = True
            if not changed:
                return 'no_change', {}
            return 'update', fields

        if import_type == 'non_statutory_leave':
            try:
                year = int(float(str(row.get('year', '')).strip()))
            except (ValueError, TypeError):
                return 'conflict', {}
            existing = self.db.execute(
                select(NonStatutoryLeave).where(
                    NonStatutoryLeave.employee_id == emp.id,
                    NonStatutoryLeave.year == year,
                )
            ).scalar_one_or_none()
            new_days_str = str(row.get('total_days', '')).strip()
            new_type = str(row.get('leave_type', '')).strip() or None
            if existing is None:
                return 'insert', {
                    'year': {'old': None, 'new': year},
                    'total_days': {'old': None, 'new': new_days_str},
                    'leave_type': {'old': None, 'new': new_type},
                }
            from decimal import Decimal as _D
            try:
                new_days = _D(new_days_str) if new_days_str else None
            except Exception:
                return 'conflict', {}
            fields = {}
            if new_days is not None and new_days != existing.total_days:
                fields['total_days'] = {'old': str(existing.total_days), 'new': str(new_days)}
            if new_type != existing.leave_type:
                fields['leave_type'] = {'old': existing.leave_type, 'new': new_type}
            return ('update' if fields else 'no_change'), fields

        if import_type == 'performance_grades':
            try:
                year = int(float(str(row.get('year', '')).strip()))
            except (ValueError, TypeError):
                return 'conflict', {}
            existing = self.db.execute(
                select(PerformanceRecord).where(
                    PerformanceRecord.employee_id == emp.id,
                    PerformanceRecord.year == year,
                )
            ).scalar_one_or_none()
            new_grade = str(row.get('grade', '')).strip()
            if existing is None:
                return 'insert', {'grade': {'old': None, 'new': new_grade}}
            if existing.grade == new_grade:
                return 'no_change', {}
            return 'update', {'grade': {'old': existing.grade, 'new': new_grade}}

        if import_type == 'salary_adjustments':
            try:
                adj_date = self._parse_excel_date(row.get('adjustment_date'))
            except ValueError:
                return 'conflict', {}
            adj_type = str(row.get('adjustment_type', '')).strip() or None
            if not adj_date or not adj_type:
                return 'conflict', {}
            existing = self.db.execute(
                select(SalaryAdjustmentRecord).where(
                    SalaryAdjustmentRecord.employee_id == emp.id,
                    SalaryAdjustmentRecord.adjustment_date == adj_date,
                    SalaryAdjustmentRecord.adjustment_type == adj_type,
                )
            ).scalar_one_or_none()
            new_amount_str = str(row.get('amount', '')).strip()
            if existing is None:
                return 'insert', {'amount': {'old': None, 'new': new_amount_str or None}}
            old_amount_str = str(existing.amount) if existing.amount is not None else None
            if old_amount_str == (new_amount_str or None):
                return 'no_change', {}
            return 'update', {'amount': {'old': old_amount_str, 'new': new_amount_str or None}}

        return 'no_change', {}
    ```

    3) 在 `ImportService` 加 `build_preview`：

    ```python
    def build_preview(
        self,
        *,
        import_type: str,
        file_name: str,
        raw_bytes: bytes,
        actor_id: str | None = None,
    ) -> 'PreviewResponse':
        """D-06 + D-07 + D-08 + D-09: preview 阶段。

        1) 解析 xlsx → dataframe
        2) 检测同文件内业务键冲突
        3) 一次性 prefetch DB 现有记录（按业务键）
        4) 逐行算 diff（insert/update/no_change/conflict）
        5) 生成 PreviewResponse（最多 200 行）
        6) 写 ImportJob status='previewing' + 暂存文件 + 记录 file_sha256
        """
        from backend.app.schemas.import_preview import (
            PreviewResponse, PreviewRow, PreviewCounters, FieldDiff,
        )
        normalized_type = import_type.strip().lower()
        if normalized_type not in self.SUPPORTED_TYPES:
            raise ValueError(self._localize_error_message('Unsupported import type.'))
        if not raw_bytes:
            raise ValueError(self._localize_error_message('Uploaded file is empty.'))

        # 解析 xlsx
        dataframe = self._load_table(file_name, raw_bytes)
        if len(dataframe) > self.MAX_ROWS:
            raise ValueError(f'单次导入不能超过 {self.MAX_ROWS} 行，请分批导入。')
        dataframe = self._normalize_columns(normalized_type, dataframe)

        # 创建 ImportJob status='previewing'
        job = ImportJob(
            file_name=file_name,
            import_type=normalized_type,
            status='previewing',
            total_rows=len(dataframe),
            success_rows=0,
            failed_rows=0,
            result_summary={},
            overwrite_mode='merge',  # 默认；confirm 时按用户选择更新
            actor_id=actor_id,
        )
        self.db.add(job)
        self.db.commit()
        self.db.refresh(job)

        # 暂存文件 + 算 hash
        file_sha256 = self._save_staged_file(job.id, raw_bytes)

        # 检测冲突
        conflicts = self._detect_in_file_conflicts(normalized_type, dataframe)

        # Diff 计算（仅查 DB 现状对比，不模拟落库）
        rows_all: list[PreviewRow] = []
        counters = {'insert': 0, 'update': 0, 'no_change': 0, 'conflict': 0}

        for pandas_idx, row in dataframe.iterrows():
            row_no = int(pandas_idx) + 2  # +2: pandas idx 0 = 数据第一行 = Excel 第二行（含表头）
            emp_no = str(row.get('employee_no', '')).strip()

            if pandas_idx in conflicts:
                rows_all.append(PreviewRow(
                    row_number=row_no, action='conflict', employee_no=emp_no,
                    fields={}, conflict_reason=conflicts[pandas_idx],
                ))
                counters['conflict'] += 1
                continue

            try:
                action, fields = self._build_row_diff(normalized_type, row)
            except Exception as exc:
                rows_all.append(PreviewRow(
                    row_number=row_no, action='conflict', employee_no=emp_no,
                    fields={}, conflict_reason=f'行解析失败: {exc}',
                ))
                counters['conflict'] += 1
                continue

            counters[action] += 1
            rows_all.append(PreviewRow(
                row_number=row_no, action=action, employee_no=emp_no,
                fields={k: FieldDiff(**v) for k, v in fields.items()},
            ))

        # 截断策略（D-08）：conflict + insert + update 优先；no_change 按剩余配额
        priority = {'conflict': 0, 'insert': 1, 'update': 2, 'no_change': 3}
        rows_sorted = sorted(rows_all, key=lambda r: priority[r.action])
        rows_kept = rows_sorted[:200]
        rows_truncated = len(rows_all) > 200

        expires_at = datetime.now(timezone.utc) + timedelta(minutes=60)

        # 把 preview 结果存入 ImportJob.result_summary（confirm 时只更新 execution 部分）
        job.result_summary = {
            'preview': {
                'counters': counters,
                'rows': [r.model_dump() for r in rows_kept],
                'rows_truncated': rows_truncated,
                'truncated_count': max(0, len(rows_all) - 200),
                'file_sha256': file_sha256,
                'preview_expires_at': expires_at.isoformat(),
            },
            'supported_types': sorted(self.SUPPORTED_TYPES),
        }
        self.db.commit()
        self.db.refresh(job)

        return PreviewResponse(
            job_id=job.id,
            import_type=normalized_type,
            file_name=file_name,
            total_rows=len(dataframe),
            counters=PreviewCounters(**counters),
            rows=rows_kept,
            rows_truncated=rows_truncated,
            truncated_count=max(0, len(rows_all) - 200),
            preview_expires_at=expires_at,
            file_sha256=file_sha256,
        )
    ```

    4) 创建测试 `backend/tests/test_services/test_import_preview.py`：

    ```python
    from __future__ import annotations
    import pytest


    def test_build_preview_basic_structure(db_session, employee_factory, xlsx_factory, tmp_uploads_dir):
        from backend.app.services.import_service import ImportService
        employee_factory(employee_no='E00001')
        employee_factory(employee_no='E00002')
        svc = ImportService(db_session)
        data = xlsx_factory['hire_info']()  # 2 行默认数据
        resp = svc.build_preview(import_type='hire_info', file_name='test.xlsx', raw_bytes=data)
        assert resp.job_id
        assert resp.import_type == 'hire_info'
        assert resp.total_rows == 2
        assert resp.file_sha256
        assert isinstance(resp.counters.insert + resp.counters.update + resp.counters.no_change + resp.counters.conflict, int)

    def test_build_preview_writes_job_status_previewing(db_session, employee_factory, xlsx_factory, tmp_uploads_dir):
        from backend.app.services.import_service import ImportService
        from backend.app.models.import_job import ImportJob
        employee_factory(employee_no='E00001')
        svc = ImportService(db_session)
        resp = svc.build_preview(import_type='hire_info', file_name='t.xlsx',
                                  raw_bytes=xlsx_factory['hire_info'](rows=[['E00001', '2026-01-01', None]]))
        from sqlalchemy import select
        job = db_session.execute(select(ImportJob).where(ImportJob.id == resp.job_id)).scalar_one()
        assert job.status == 'previewing'
        # 暂存文件存在
        assert svc._staged_file_path(resp.job_id).exists()

    def test_build_preview_detects_in_file_conflicts(db_session, employee_factory, xlsx_factory, tmp_uploads_dir):
        from backend.app.services.import_service import ImportService
        employee_factory(employee_no='E00001')
        svc = ImportService(db_session)
        data = xlsx_factory['non_statutory_leave'](with_conflict=True)
        resp = svc.build_preview(import_type='non_statutory_leave', file_name='t.xlsx', raw_bytes=data)
        assert resp.counters.conflict >= 2
        conflict_rows = [r for r in resp.rows if r.action == 'conflict']
        assert any('出现' in r.conflict_reason for r in conflict_rows)

    def test_detect_in_file_conflicts_helper(db_session):
        from backend.app.services.import_service import ImportService
        import pandas as pd
        svc = ImportService(db_session)
        df = pd.DataFrame([
            {'employee_no': 'E001', 'year': 2026, 'total_days': 5},
            {'employee_no': 'E001', 'year': 2026, 'total_days': 8},  # dup
            {'employee_no': 'E002', 'year': 2026, 'total_days': 3},
        ])
        conflicts = svc._detect_in_file_conflicts('non_statutory_leave', df)
        assert 0 in conflicts and 1 in conflicts
        assert 2 not in conflicts

    def test_build_row_diff_hire_info_update(db_session, employee_factory):
        """_build_row_diff 单元测试：hire_info 已有员工，新值不同 → update。"""
        from backend.app.services.import_service import ImportService
        import pandas as pd
        from datetime import date
        emp = employee_factory(employee_no='E00001', hire_date=date(2020, 1, 1))
        svc = ImportService(db_session)
        row = pd.Series({'employee_no': 'E00001', 'hire_date': '2024-03-15', 'last_salary_adjustment_date': None})
        action, fields = svc._build_row_diff('hire_info', row)
        assert action == 'update'
        assert 'hire_date' in fields

    def test_build_row_diff_perf_grades_insert_for_new(db_session, employee_factory):
        from backend.app.services.import_service import ImportService
        import pandas as pd
        emp = employee_factory(employee_no='E00001')
        svc = ImportService(db_session)
        row = pd.Series({'employee_no': 'E00001', 'year': 2026, 'grade': 'A'})
        action, fields = svc._build_row_diff('performance_grades', row)
        assert action == 'insert'
        assert fields['grade']['new'] == 'A'
    ```
  </action>
  <verify>
    <automated>
      .venv/bin/pytest backend/tests/test_services/test_import_preview.py -x --tb=short
    </automated>
  </verify>
  <acceptance_criteria>
    - grep "def build_preview" backend/app/services/import_service.py 命中
    - grep "def _detect_in_file_conflicts" backend/app/services/import_service.py 命中
    - grep "def _build_row_diff" backend/app/services/import_service.py 命中
    - 命令 `.venv/bin/pytest backend/tests/test_services/test_import_preview.py -x` 全部 PASS（6 用例）
  </acceptance_criteria>
  <done>
    Preview 阶段 service 方法（build_preview / _detect_in_file_conflicts / _build_row_diff）就绪；6 个 preview 单测 PASS；Plan 04 API 端点 preview_excel_import 可直接调用 svc.build_preview；Task 2b 实现 confirm/cancel。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2b: confirm_import + cancel_import + AuditLog 写入（用真实字段 target_type/target_id/operator_id）</name>
  <files>
    backend/app/services/import_service.py,
    backend/tests/test_services/test_import_confirm.py
  </files>
  <read_first>
    - backend/app/services/import_service.py（Task 2a 已建 build_preview / _build_row_diff，本 task 加 confirm_import / cancel_import）
    - backend/app/services/import_service.py:188-252（run_import 现有实现 — confirm_import 复用其 _dispatch_import 调用 + status 派生逻辑）
    - backend/app/models/audit_log.py（**真实字段名 operator_id/target_type/target_id**，不是 D-13 文档假设的 actor_id/resource_*）
    - backend/app/schemas/import_preview.py（ConfirmRequest / ConfirmResponse Task 1 已建）
  </read_first>
  <behavior>
    - Test 1: confirm_import(job_id, 'merge') 读暂存 → 落库 → status='completed'/'partial'/'failed' → 删暂存 → 写 AuditLog
    - Test 2: AuditLog 字段：operator_id=current_user.id, target_type='import_job', target_id=job_id, action='import_confirmed', detail 含 overwrite_mode
    - Test 3: confirm_import 文件 hash 不匹配（暂存被外部修改）→ raises ValueError 'hash mismatch'
    - Test 4: confirm_import 同 job_id 第二次调用（status 已是 'completed'）→ raises ValueError 'already confirmed'
    - Test 5: cancel_import(job_id) → status='cancelled' + 删暂存
    - Test 6: confirm_import replace 模式但 confirm_replace=False → raises ValueError（前端二次确认未完成）
  </behavior>
  <action>
    在 `ImportService` 加 `confirm_import` 与 `cancel_import`：

    ```python
    def confirm_import(
        self,
        *,
        job_id: str,
        overwrite_mode: str,
        confirm_replace: bool = False,
        actor_id: str | None = None,
        actor_role: str | None = None,
    ) -> 'ConfirmResponse':
        """D-06 + D-13: confirm 阶段。

        1) 校验 job 状态必须是 previewing
        2) replace 模式必须 confirm_replace=True（前端二次确认 modal 已通过）
        3) 读暂存文件 + sha256 校验
        4) 调用 _dispatch_import 落库
        5) 派生 status (completed/partial/failed)
        6) 删暂存文件
        7) 写 AuditLog action='import_confirmed'（用真实字段 operator_id/target_type/target_id）
        """
        from backend.app.schemas.import_preview import ConfirmResponse
        from backend.app.models.audit_log import AuditLog
        from sqlalchemy import select
        if overwrite_mode not in ('merge', 'replace'):
            raise ValueError(f'Invalid overwrite_mode: {overwrite_mode}')
        if overwrite_mode == 'replace' and not confirm_replace:
            raise ValueError('替换模式必须显式确认（前端二次确认未完成）')

        job = self.db.execute(
            select(ImportJob).where(ImportJob.id == job_id)
        ).scalar_one_or_none()
        if job is None:
            raise ValueError(f'ImportJob {job_id} 不存在')
        if job.status != 'previewing':
            raise ValueError(f'ImportJob {job_id} 状态为 {job.status}，无法确认导入（已确认或已取消）')

        expected_sha = (job.result_summary or {}).get('preview', {}).get('file_sha256')
        raw_bytes = self._read_staged_file(job_id, expected_sha256=expected_sha)

        # 标记 processing
        job.status = 'processing'
        job.overwrite_mode = overwrite_mode
        if actor_id is not None:
            job.actor_id = actor_id
        self.db.commit()

        start_ms = datetime.now(timezone.utc)
        try:
            dataframe = self._load_table(job.file_name, raw_bytes)
            dataframe = self._normalize_columns(job.import_type, dataframe)
            row_results = self._dispatch_import(
                job.import_type, dataframe, overwrite_mode=overwrite_mode,
            )
            inserted = sum(1 for r in row_results if r.get('action') == 'insert' and r.get('status') == 'success')
            updated = sum(1 for r in row_results if r.get('action') == 'update' and r.get('status') == 'success')
            no_change = sum(1 for r in row_results if r.get('action') == 'no_change' and r.get('status') == 'success')
            failed = sum(1 for r in row_results if r.get('status') == 'failed')

            job.total_rows = len(row_results)
            job.success_rows = inserted + updated + no_change
            job.failed_rows = failed
            if failed == 0:
                job.status = 'completed'
            elif job.success_rows == 0:
                job.status = 'failed'
            else:
                job.status = 'partial'
            duration_ms = int((datetime.now(timezone.utc) - start_ms).total_seconds() * 1000)
            existing = job.result_summary or {}
            job.result_summary = {
                **existing,
                'execution': {
                    'rows': row_results[:200],  # 截断保护，与 preview 同口径
                    'inserted_count': inserted,
                    'updated_count': updated,
                    'no_change_count': no_change,
                    'failed_count': failed,
                    'execution_duration_ms': duration_ms,
                    'executed_at': datetime.now(timezone.utc).isoformat(),
                },
            }
        except Exception as exc:
            job.status = 'failed'
            existing = job.result_summary or {}
            job.result_summary = {**existing, 'execution': {'error': str(exc)}}
            self.db.commit()
            raise
        finally:
            try:
                self._delete_staged_file(job_id)
            except Exception:
                logger.exception('Failed to delete staged file for confirmed job %s', job_id)

        # D-13 写 AuditLog（**真实字段名 operator_id / target_type / target_id**）
        audit = AuditLog(
            operator_id=actor_id,
            operator_role=actor_role,
            action='import_confirmed',
            target_type='import_job',
            target_id=job.id,
            detail={
                'import_type': job.import_type,
                'overwrite_mode': overwrite_mode,
                'file_name': job.file_name,
                'total_rows': job.total_rows,
                'inserted_count': inserted,
                'updated_count': updated,
                'no_change_count': no_change,
                'failed_count': failed,
            },
        )
        self.db.add(audit)
        self.db.commit()
        self.db.refresh(job)

        return ConfirmResponse(
            job_id=job.id,
            status=job.status,
            total_rows=job.total_rows,
            inserted_count=inserted,
            updated_count=updated,
            no_change_count=no_change,
            failed_count=failed,
            execution_duration_ms=duration_ms,
        )

    def cancel_import(self, job_id: str) -> None:
        """HR 显式取消 preview → status='cancelled' + 删暂存。"""
        from sqlalchemy import select
        job = self.db.execute(
            select(ImportJob).where(ImportJob.id == job_id)
        ).scalar_one_or_none()
        if job is None:
            raise ValueError(f'ImportJob {job_id} 不存在')
        if job.status != 'previewing':
            # 只能取消 previewing 状态；其他状态忽略（幂等）
            return
        job.status = 'cancelled'
        existing = job.result_summary or {}
        job.result_summary = {**existing, 'cancellation_reason': 'user_cancelled'}
        try:
            self._delete_staged_file(job_id)
        except Exception:
            logger.exception('Failed to delete staged file for cancelled job %s', job_id)
        self.db.commit()
    ```

    创建测试 `backend/tests/test_services/test_import_confirm.py`：

    ```python
    from __future__ import annotations
    import pytest


    def test_confirm_import_writes_audit_log_with_real_fields(
        db_session, employee_factory, xlsx_factory, tmp_uploads_dir, user_factory,
    ):
        """关键：AuditLog 用真实字段 operator_id / target_type / target_id（不是文档假设的 actor_id / resource_*）。"""
        from backend.app.services.import_service import ImportService
        from backend.app.models.audit_log import AuditLog
        from sqlalchemy import select
        emp = employee_factory(employee_no='E00001')
        actor = user_factory(role='hrbp')
        svc = ImportService(db_session)
        # preview
        data = xlsx_factory['hire_info'](rows=[['E00001', '2026-01-01', None]])
        prev = svc.build_preview(import_type='hire_info', file_name='t.xlsx',
                                  raw_bytes=data, actor_id=str(actor.id))
        # confirm
        resp = svc.confirm_import(job_id=prev.job_id, overwrite_mode='merge',
                                   actor_id=str(actor.id), actor_role='hrbp')
        assert resp.status in ('completed', 'partial')
        # AuditLog 用真实字段名
        log = db_session.execute(select(AuditLog).where(
            AuditLog.target_id == prev.job_id, AuditLog.action == 'import_confirmed',
        )).scalar_one()
        assert log.operator_id == str(actor.id)  # 真实字段：operator_id（不是 actor_id）
        assert log.target_type == 'import_job'   # 真实字段：target_type（不是 resource_type）
        assert log.detail['overwrite_mode'] == 'merge'
        assert log.detail['import_type'] == 'hire_info'

    def test_confirm_replace_requires_confirm_replace_flag(
        db_session, employee_factory, xlsx_factory, tmp_uploads_dir, user_factory,
    ):
        from backend.app.services.import_service import ImportService
        employee_factory(employee_no='E00001')
        actor = user_factory(role='hrbp')
        svc = ImportService(db_session)
        prev = svc.build_preview(import_type='hire_info', file_name='t.xlsx',
                                  raw_bytes=xlsx_factory['hire_info'](rows=[['E00001', '2026-01-01', None]]),
                                  actor_id=str(actor.id))
        with pytest.raises(ValueError, match='替换模式'):
            svc.confirm_import(job_id=prev.job_id, overwrite_mode='replace', confirm_replace=False)

    def test_confirm_double_confirm_rejected(
        db_session, employee_factory, xlsx_factory, tmp_uploads_dir, user_factory,
    ):
        from backend.app.services.import_service import ImportService
        employee_factory(employee_no='E00001')
        actor = user_factory(role='hrbp')
        svc = ImportService(db_session)
        prev = svc.build_preview(import_type='hire_info', file_name='t.xlsx',
                                  raw_bytes=xlsx_factory['hire_info'](rows=[['E00001', '2026-01-01', None]]),
                                  actor_id=str(actor.id))
        svc.confirm_import(job_id=prev.job_id, overwrite_mode='merge', actor_id=str(actor.id))
        with pytest.raises(ValueError, match='已确认或已取消'):
            svc.confirm_import(job_id=prev.job_id, overwrite_mode='merge', actor_id=str(actor.id))

    def test_confirm_hash_mismatch_raises(
        db_session, employee_factory, xlsx_factory, tmp_uploads_dir,
    ):
        from backend.app.services.import_service import ImportService
        employee_factory(employee_no='E00001')
        svc = ImportService(db_session)
        prev = svc.build_preview(import_type='hire_info', file_name='t.xlsx',
                                  raw_bytes=xlsx_factory['hire_info'](rows=[['E00001', '2026-01-01', None]]))
        # 篡改暂存文件
        path = svc._staged_file_path(prev.job_id)
        path.write_bytes(b'tampered content')
        with pytest.raises(ValueError, match='hash mismatch'):
            svc.confirm_import(job_id=prev.job_id, overwrite_mode='merge')

    def test_cancel_import_deletes_staged_file(
        db_session, employee_factory, xlsx_factory, tmp_uploads_dir,
    ):
        from backend.app.services.import_service import ImportService
        from sqlalchemy import select
        from backend.app.models.import_job import ImportJob
        employee_factory(employee_no='E00001')
        svc = ImportService(db_session)
        prev = svc.build_preview(import_type='hire_info', file_name='t.xlsx',
                                  raw_bytes=xlsx_factory['hire_info'](rows=[['E00001', '2026-01-01', None]]))
        assert svc._staged_file_path(prev.job_id).exists()
        svc.cancel_import(prev.job_id)
        job = db_session.execute(select(ImportJob).where(ImportJob.id == prev.job_id)).scalar_one()
        assert job.status == 'cancelled'
        assert not svc._staged_file_path(prev.job_id).exists()

    def test_cancel_idempotent_for_completed(
        db_session, import_job_factory, tmp_uploads_dir,
    ):
        from backend.app.services.import_service import ImportService
        job = import_job_factory(import_type='hire_info', status='completed')
        svc = ImportService(db_session)
        svc.cancel_import(job.id)  # 不抛异常
        db_session.refresh(job)
        assert job.status == 'completed'  # 终态保持
    ```
  </action>
  <verify>
    <automated>
      .venv/bin/pytest backend/tests/test_services/test_import_confirm.py -x --tb=short
    </automated>
  </verify>
  <acceptance_criteria>
    - grep "def confirm_import" backend/app/services/import_service.py 命中
    - grep "def cancel_import" backend/app/services/import_service.py 命中
    - grep "target_type='import_job'" backend/app/services/import_service.py 命中（**确认用真实字段名 target_type，不是 resource_type**）
    - grep "operator_id=actor_id" backend/app/services/import_service.py 命中（**确认用真实字段名 operator_id，不是 D-13 文档的 actor_id 列**）
    - grep "target_id=job.id" backend/app/services/import_service.py 命中（**确认用 target_id，不是 resource_id**）
    - grep "action='import_confirmed'" backend/app/services/import_service.py 命中
    - 命令 `.venv/bin/pytest backend/tests/test_services/test_import_confirm.py -x` 全部 PASS（6 用例：含 hash mismatch / 双 confirm / replace 二次确认 / AuditLog 真实字段断言 / cancel 幂等）
    - 命令 `.venv/bin/pytest backend/tests/ -x --tb=short -q` 现有测试无 regression
  </acceptance_criteria>
  <done>
    confirm_import / cancel_import + AuditLog（真实字段 operator_id/target_type/target_id）就绪；6 个 confirm/cancel 测试 PASS；Plan 04 API 端点 confirm_excel_import / cancel_excel_import 可直接调用 svc.confirm_import / svc.cancel_import。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HR 上传 xlsx 字节 → ImportService.build_preview | 文件内容不可信，需类型 / 大小校验 |
| job_id (UUID) → _staged_file_path | 路径遍历风险（Pitfall 3） |
| 暂存文件 → _read_staged_file | 文件被外部进程修改风险（F3） |
| current_user.id → AuditLog.operator_id | 操作者身份不可被伪造（API 层 JWT 校验） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | T (Tampering) | _staged_file_path 路径遍历（job_id 含 ../） | mitigate | Task 1 双重校验：字符级（拒 / \\ ..）+ resolve 后 is_relative_to(base_dir)；单测 test_staged_path_rejects_traversal_dotdot/slash/backslash 验证 |
| T-32-03 | D (DoS) | 上传文件超大耗尽内存 | mitigate | Task 2 build_preview 调用 _load_table 时已有 MAX_ROWS=5000 检查；API 层（Plan 04）加文件大小硬上限 |
| T-32-05 | T (Tampering) | 并发锁绕过（DB 层 vs 仅前端禁用） | mitigate | Task 1 is_import_running 在 service 层；Plan 04 API 端点上传/确认两处都强制调用 → 即使前端绕过，DB 层 409 拦截 |
| T-32-06 | R (Repudiation) | AuditLog 缺 actor_id / overwrite_mode → 无法回溯 | mitigate | Task 2 confirm_import 强制写 operator_id（来自 current_user.id）+ detail.overwrite_mode；测试 test_confirm_import_writes_audit_log_with_real_fields 验证字段完整 |
| T-32-14 | T (Tampering) | confirm 阶段暂存文件被外部修改 → 落库与 preview 不一致 | mitigate | Task 1 _save_staged_file 算 sha256；Task 2 confirm_import 调用 _read_staged_file(expected_sha256) 校验；测试 test_confirm_hash_mismatch_raises 验证 |
| T-32-15 | T (Tampering) | replace 模式被绕过二次确认 → 数据丢失 | mitigate | Task 2 confirm_import 强制 confirm_replace=True 才接受 replace；测试 test_confirm_replace_requires_confirm_replace_flag 验证 |
</threat_model>

<verification>
- 全套测试无 regression
- 单测覆盖：锁 5 + expire 4 + 路径安全 7（Task 1）+ preview 6（Task 2a）+ confirm/cancel 6（Task 2b）= 28+ 用例 PASS
- service 方法签名稳定（Plan 04 API 直接调用：is_import_running / get_active_job / build_preview / confirm_import / cancel_import / expire_stale_import_jobs / _detect_in_file_conflicts / _build_row_diff）
</verification>

<success_criteria>
- IMPORT-06 service 层完成（API 入口在 Plan 04 加 409 处理）
- IMPORT-07 service 层完成（Preview + diff + confirm 两阶段链路；UI 在 Plan 05/06 完成）
- 解决 Open Question 5：文件 hash 校验做（5 行代码价值高）
- 解决 Open Question 3：confirm 走同步（小于 5000 行实测 < 5 秒；本 plan service 同步实现，不引 Celery）
</success_criteria>

<output>
完成后创建 `.planning/phases/32-eligibility-import-completion/32-03-SUMMARY.md`，重点记录：
- 5 个新 service 方法签名（Plan 04 API 端点直接调用）
- PreviewResponse / ConfirmRequest / ConfirmResponse / ActiveJobResponse Pydantic schemas 入口
- AuditLog 写入正确字段名（operator_id/target_type/target_id；解决 RESEARCH 关键差异 #1）
- _BUSINESS_KEYS 业务键映射（Plan 04 / Plan 06 集成测试可复用）
</output>
