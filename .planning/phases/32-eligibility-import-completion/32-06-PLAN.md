---
phase: 32-eligibility-import-completion
plan: 06
type: execute
wave: 5
depends_on:
  - 32-03
  - 32-04
  - 32-05
files_modified:
  - frontend/src/components/eligibility-import/ExcelImportPanel.tsx
  - frontend/src/components/eligibility-import/ImportTabContent.tsx
  - backend/app/scheduler/import_scheduler.py
  - backend/app/main.py
  - backend/tests/test_integration/__init__.py
  - backend/tests/test_integration/test_import_e2e.py
autonomous: false

requirements:
  - IMPORT-01
  - IMPORT-02
  - IMPORT-05
  - IMPORT-06
  - IMPORT-07

must_haves:
  truths:
    - "ExcelImportPanel 状态机改造：idle → uploading → previewing → confirming → done/cancelled/error；6 个新组件完整组合在 panel 内"
    - "下载模板按钮调用 downloadTemplate（blob 模式），不再用 getTemplateUrl + <a href>（D-05 + Pitfall 5 解决）"
    - "进入 Tab 时调用 getActiveImportJob 检测活跃 job，「选择文件」按钮在有活跃 job 时禁用 + tooltip + ImportActiveJobBanner 顶部提示"
    - "上传 → preview → 显示 ImportPreviewPanel；确认 → confirm → success toast → 重置；取消 → cancel → 重置"
    - "APScheduler 定时任务 expire_stale_import_jobs 每 15 分钟执行（决议 Open Question 2：选 APScheduler 而非 Celery Beat）"
    - "E2E 集成测试覆盖 4 类 import_type × merge/replace × 重复导入幂等 4 个核心场景"
    - "Manual UAT checkpoint：浏览器实际触发 4 个交互（模板下载 / Preview 抽屉 / Modal focus trap / 409 toast）"
  artifacts:
    - path: frontend/src/components/eligibility-import/ExcelImportPanel.tsx
      provides: "状态机改造后的主 panel：idle/uploading/previewing/confirming/done/cancelled/error 7 态机"
      contains: "type ImportFlowState"
    - path: backend/app/scheduler/import_scheduler.py
      provides: "APScheduler cron job：每 15 分钟跑 expire_stale_import_jobs（D-17）"
      contains: "def start_import_scheduler"
    - path: backend/app/main.py
      provides: "lifespan 启动 import_scheduler（与 feishu_scheduler 同模式）"
      contains: "start_import_scheduler"
    - path: backend/tests/test_integration/test_import_e2e.py
      provides: "E2E 集成测试：4 类 × merge/replace × 重复幂等 + 双 Tab 并发 + 模板下载真实字节"
      contains: "def test_e2e_hire_info_merge"
  key_links:
    - from: frontend/src/components/eligibility-import/ExcelImportPanel.tsx
      to: frontend/src/components/eligibility-import/ImportPreviewPanel.tsx
      via: "状态 'previewing' 时渲染 ImportPreviewPanel；状态切换由 onConfirm/onCancel callback 触发"
      pattern: "ImportPreviewPanel"
    - from: backend/app/scheduler/import_scheduler.py
      to: backend/app/services/import_service.py
      via: "调用 ImportService.expire_stale_import_jobs（独立 SessionLocal session 模式，参考 feishu_scheduler.run_incremental_sync）"
      pattern: "expire_stale_import_jobs"
    - from: backend/app/main.py
      to: backend/app/scheduler/import_scheduler.py
      via: "lifespan 启动时 import + 调用 start_import_scheduler；shutdown 时 stop_import_scheduler"
      pattern: "start_import_scheduler"
---

<objective>
Phase 32 收尾：把 Plan 03-05 的所有基础设施串成端到端可用功能。具体三件事：

1. **ExcelImportPanel 状态机改造**（核心）—— 把现有「上传即落库」单步改造为 7 态机：idle / uploading / previewing / confirming / done / cancelled / error；接入 6 个新组件；接入 5 个新 service 函数；下载模板改 blob 模式；进入 Tab 时调用 getActiveImportJob 显示 banner + 禁用按钮。

2. **APScheduler 定时清理任务**（D-17 决议 Open Question 2）—— 新增 `backend/app/scheduler/import_scheduler.py`，每 15 分钟跑 `ImportService.expire_stale_import_jobs()`；在 `main.py` lifespan 启动 / 停止。**选 APScheduler 而非 Celery Beat**：理由（与 RESEARCH 一致）— 复用既有 `feishu_scheduler.py` 模式、不增加新部署进程、单元测试友好。

3. **E2E 集成测试 + Manual UAT 检查点**（Plan 04 已覆盖 API 单测；本 plan 加跨层端到端）—— 4 类 import_type × merge/replace × 重复幂等核心场景；前端浏览器交互依赖人工验证（模板下载兼容性 / Modal focus trap / 409 toast 显示）。

Purpose: Phase 32 的全部 SUCCESS CRITERIA 1-5（ROADMAP）通过本 plan 完成端到端验收。**本 plan 含 1 个 checkpoint:human-verify**（autonomous: false），交付完成后 HR 视角浏览器验证。

Output:
- ExcelImportPanel 状态机重写
- ImportTabContent 微调（如需）
- import_scheduler.py + main.py 集成
- E2E 集成测试文件
- VERIFICATION.md 占位（由 /gsd-verify-work 阶段填充）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-eligibility-import-completion/32-03-SUMMARY.md
@.planning/phases/32-eligibility-import-completion/32-04-SUMMARY.md
@.planning/phases/32-eligibility-import-completion/32-05-SUMMARY.md
@.planning/phases/32-eligibility-import-completion/32-CONTEXT.md
@.planning/phases/32-eligibility-import-completion/32-UI-SPEC.md
@.planning/phases/32-eligibility-import-completion/32-RESEARCH.md
@.planning/phases/32-eligibility-import-completion/32-VALIDATION.md
@CLAUDE.md
@frontend/src/components/eligibility-import/ExcelImportPanel.tsx
@frontend/src/components/eligibility-import/ImportTabContent.tsx
@backend/app/scheduler/feishu_scheduler.py
@backend/app/main.py
@backend/app/services/import_service.py

<interfaces>
<!-- Plan 05 已建立的组件 / service 入口 -->
- frontend/src/components/eligibility-import/ImportPreviewPanel.tsx (整合 5 个子组件)
- frontend/src/components/eligibility-import/ImportActiveJobBanner.tsx
- frontend/src/services/eligibilityImportService.ts:
    downloadTemplate(importType): Promise<void>
    uploadAndPreview(importType, file, onUploadProgress?): Promise<PreviewResponse>
    confirmImport(jobId, overwriteMode, confirmReplace?): Promise<ConfirmResponse>
    cancelImport(jobId): Promise<void>
    getActiveImportJob(importType): Promise<ActiveJobResponse>

<!-- ExcelImportPanel 现状 -->
frontend/src/components/eligibility-import/ExcelImportPanel.tsx (200 行)：
- useState: file / dragOver / taskId / uploading / error / progress
- useTaskPolling
- handleUpload → uploadEligibilityExcel (旧 API) → setTaskId → onResult callback
- 「下载模板」用 <a href={getTemplateUrl(...)} target="_blank">

<!-- 改造后的状态机（UI-SPEC § Component Inventory）-->
type ImportFlowState =
  | { kind: 'idle' }
  | { kind: 'uploading'; file: File; progress?: number }
  | { kind: 'previewing'; preview: PreviewResponse }
  | { kind: 'confirming'; preview: PreviewResponse; overwriteMode: OverwriteMode }
  | { kind: 'done'; result: ConfirmResponse }
  | { kind: 'cancelled' }
  | { kind: 'error'; message: string };

<!-- APScheduler 既有模式（D-17 决议直接抄）-->
backend/app/scheduler/feishu_scheduler.py:
- scheduler = AsyncIOScheduler()
- def start_scheduler(...) → scheduler.add_job(... CronTrigger ...)
- def stop_scheduler() → scheduler.shutdown()
- def run_incremental_sync() → 独立 SessionLocal() session
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ExcelImportPanel 7 态状态机改造 + 整合 6 个新组件 + blob 模板下载 + 活跃 job banner</name>
  <files>
    frontend/src/components/eligibility-import/ExcelImportPanel.tsx,
    frontend/src/components/eligibility-import/ImportTabContent.tsx
  </files>
  <read_first>
    - frontend/src/components/eligibility-import/ExcelImportPanel.tsx 全文（现有逻辑：拖拽 / 上传 / 进度 / 完成回调；保留 Drop zone + 文件类型校验 + 进度提示，**仅替换 service 调用与 onResult 流程**）
    - frontend/src/components/eligibility-import/ImportTabContent.tsx（确认与 ExcelImportPanel 的 onResult 接口约定 — 本 plan 可能需要把 onResult 改为更结构化的 onComplete）
    - frontend/src/components/eligibility-import/ImportPreviewPanel.tsx（Plan 05 已建，确认 props 签名）
    - frontend/src/components/eligibility-import/ImportActiveJobBanner.tsx（Plan 05 已建，确认 props 签名）
    - frontend/src/services/eligibilityImportService.ts（Plan 05 5 个新函数签名）
    - frontend/src/types/api.ts（Plan 05 已建 PreviewResponse / ConfirmResponse / ActiveJobResponse / OverwriteMode）
    - .planning/phases/32-eligibility-import-completion/32-UI-SPEC.md § Copywriting / Interaction States（文案与禁用 tooltip）
    - frontend/src/utils/ 与 frontend/src/components/ 目录（搜索 toast / Notification / sonner — 确认是否有 toast 系统可复用；若无，本 plan 用 alert + console.error 临时方案）
  </read_first>
  <action>
    1) 重写 `frontend/src/components/eligibility-import/ExcelImportPanel.tsx`（保留 Drop zone 视觉与拖拽逻辑，状态机用 discriminated union）：

    ```typescript
    import { useCallback, useEffect, useRef, useState } from 'react';
    import {
      uploadAndPreview,
      confirmImport,
      cancelImport,
      getActiveImportJob,
      downloadTemplate,
    } from '../../services/eligibilityImportService';
    import type {
      EligibilityImportType,
      PreviewResponse,
      ConfirmResponse,
      ActiveJobResponse,
      OverwriteMode,
    } from '../../types/api';
    import { ImportPreviewPanel } from './ImportPreviewPanel';
    import { ImportActiveJobBanner } from './ImportActiveJobBanner';

    interface ExcelImportPanelProps {
      importType: EligibilityImportType;
      label: string;
      onComplete?: (result: ConfirmResponse) => void;
    }

    type ImportFlowState =
      | { kind: 'idle' }
      | { kind: 'uploading'; file: File; progress: number }
      | { kind: 'previewing'; preview: PreviewResponse }
      | { kind: 'confirming'; preview: PreviewResponse; overwriteMode: OverwriteMode }
      | { kind: 'done'; result: ConfirmResponse }
      | { kind: 'cancelled' }
      | { kind: 'error'; message: string };

    function formatFileSize(bytes: number): string {
      if (bytes < 1024) return `${bytes} B`;
      if (bytes < 1024 * 1024) return `${(bytes / 1024).toFixed(1)} KB`;
      return `${(bytes / (1024 * 1024)).toFixed(1)} MB`;
    }

    export function ExcelImportPanel({ importType, label, onComplete }: ExcelImportPanelProps) {
      const fileInputRef = useRef<HTMLInputElement>(null);
      const [flow, setFlow] = useState<ImportFlowState>({ kind: 'idle' });
      const [activeJob, setActiveJob] = useState<ActiveJobResponse>({ active: false });
      const [activeJobLoading, setActiveJobLoading] = useState(true);
      const [downloadingTemplate, setDownloadingTemplate] = useState(false);
      const [dragOver, setDragOver] = useState(false);
      const [pickedFile, setPickedFile] = useState<File | null>(null);

      const refreshActiveJob = useCallback(async () => {
        setActiveJobLoading(true);
        try {
          const resp = await getActiveImportJob(importType);
          setActiveJob(resp);
        } catch (err) {
          console.error('Failed to fetch active import job', err);
        } finally {
          setActiveJobLoading(false);
        }
      }, [importType]);

      useEffect(() => { void refreshActiveJob(); }, [refreshActiveJob]);

      const isLocked = activeJob.active;
      const lockTooltip = isLocked
        ? `该类型导入正在进行中（${activeJob.status === 'previewing' ? '预览待确认' : '落库中'}）。请等待完成，或在「同步日志」查看进度。`
        : undefined;

      const handleFileSelect = useCallback((selected: File | null) => {
        if (!selected) return;
        const ext = selected.name.split('.').pop()?.toLowerCase();
        if (ext !== 'xlsx' && ext !== 'xls') {
          setFlow({ kind: 'error', message: '仅支持 .xlsx 或 .xls 格式文件' });
          return;
        }
        setPickedFile(selected);
        setFlow({ kind: 'idle' });
      }, []);

      const handleDownloadTemplate = useCallback(async () => {
        setDownloadingTemplate(true);
        try {
          await downloadTemplate(importType);
        } catch (err) {
          const msg = err instanceof Error ? err.message : String(err);
          setFlow({ kind: 'error', message: `模板下载失败：${msg}。请刷新页面重试，或联系管理员确认后端服务状态。` });
        } finally {
          setDownloadingTemplate(false);
        }
      }, [importType]);

      const handleUpload = useCallback(async () => {
        if (!pickedFile) return;
        setFlow({ kind: 'uploading', file: pickedFile, progress: 0 });
        try {
          const preview = await uploadAndPreview(
            importType, pickedFile,
            (percent) => setFlow({ kind: 'uploading', file: pickedFile, progress: percent }),
          );
          setFlow({ kind: 'previewing', preview });
          await refreshActiveJob();
        } catch (err: unknown) {
          let msg = '文件上传失败';
          if (typeof err === 'object' && err !== null && 'response' in err) {
            const status = (err as { response?: { status?: number; data?: { detail?: unknown } } }).response?.status;
            const detailMsg = (err as { response?: { data?: { detail?: { message?: string } | string } } }).response?.data?.detail;
            if (status === 409) {
              msg = typeof detailMsg === 'object' && detailMsg && 'message' in detailMsg
                ? detailMsg.message ?? '该类型导入正在进行中，请等待当前任务完成后再试。'
                : '该类型导入正在进行中，请等待当前任务完成后再试。';
              await refreshActiveJob();
            } else if (typeof detailMsg === 'string') {
              msg = detailMsg;
            }
          }
          setFlow({ kind: 'error', message: msg });
        }
      }, [pickedFile, importType, refreshActiveJob]);

      const handleConfirm = useCallback(async (overwriteMode: OverwriteMode) => {
        if (flow.kind !== 'previewing') return;
        const preview = flow.preview;
        setFlow({ kind: 'confirming', preview, overwriteMode });
        try {
          const result = await confirmImport(preview.job_id, overwriteMode, overwriteMode === 'replace');
          setFlow({ kind: 'done', result });
          onComplete?.(result);
          await refreshActiveJob();
        } catch (err: unknown) {
          const msg = err instanceof Error ? err.message : String(err);
          setFlow({ kind: 'error', message: `导入失败：${msg}` });
        }
      }, [flow, onComplete, refreshActiveJob]);

      const handleCancel = useCallback(async () => {
        if (flow.kind !== 'previewing') return;
        try {
          await cancelImport(flow.preview.job_id);
        } catch (err) { console.error('Cancel failed', err); }
        setFlow({ kind: 'cancelled' });
        setPickedFile(null);
        await refreshActiveJob();
      }, [flow, refreshActiveJob]);

      const handleReset = useCallback(() => {
        setFlow({ kind: 'idle' });
        setPickedFile(null);
        if (fileInputRef.current) fileInputRef.current.value = '';
      }, []);

      // Preview / confirming 阶段 — 渲染 ImportPreviewPanel
      if (flow.kind === 'previewing' || flow.kind === 'confirming') {
        return (
          <div>
            <ImportPreviewPanel
              label={label}
              preview={flow.preview}
              onConfirm={handleConfirm}
              onCancel={handleCancel}
              isConfirming={flow.kind === 'confirming'}
            />
          </div>
        );
      }

      // Done 阶段
      if (flow.kind === 'done') {
        return (
          <div className="surface" style={{ padding: 24 }}>
            <h3 className="section-title" style={{ color: 'var(--color-success)' }}>导入成功</h3>
            <p style={{ fontSize: 14, color: 'var(--color-ink)' }}>
              共处理 {flow.result.total_rows} 行：新增 {flow.result.inserted_count}、更新 {flow.result.updated_count}、无变化 {flow.result.no_change_count}，失败 {flow.result.failed_count}。
            </p>
            <div style={{ marginTop: 12 }}>
              <button type="button" className="action-secondary" onClick={handleReset}>
                继续导入新文件
              </button>
            </div>
          </div>
        );
      }

      // idle / uploading / cancelled / error 阶段
      const isUploading = flow.kind === 'uploading';
      return (
        <div>
          {activeJob.active && <ImportActiveJobBanner activeJob={activeJob} />}

          <p className="eyebrow">Excel 导入</p>
          <h3 className="section-title" style={{ marginBottom: 12 }}>
            上传{label}数据文件
          </h3>

          <div
            role="button" tabIndex={0} aria-disabled={isLocked}
            onDrop={isLocked ? undefined : (e) => {
              e.preventDefault(); setDragOver(false);
              const dropped = e.dataTransfer.files[0];
              if (dropped) handleFileSelect(dropped);
            }}
            onDragOver={isLocked ? undefined : (e) => { e.preventDefault(); setDragOver(true); }}
            onDragLeave={() => setDragOver(false)}
            onClick={isLocked ? undefined : () => fileInputRef.current?.click()}
            onKeyDown={(e) => {
              if (isLocked) return;
              if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); fileInputRef.current?.click(); }
            }}
            title={lockTooltip}
            style={{
              border: `2px dashed ${dragOver ? 'var(--color-primary)' : 'var(--color-border)'}`,
              borderRadius: 8, padding: '32px 24px', textAlign: 'center',
              cursor: isLocked ? 'not-allowed' : 'pointer',
              background: dragOver ? 'var(--color-primary-light, rgba(59,130,246,0.05))' : 'transparent',
              opacity: isLocked ? 0.5 : 1,
            }}
          >
            <input ref={fileInputRef} type="file" accept=".xlsx,.xls" hidden disabled={isLocked}
                   onChange={(e) => handleFileSelect(e.target.files?.[0] ?? null)} />
            {pickedFile ? (
              <span style={{ fontSize: 14, color: 'var(--color-ink)' }}>
                {pickedFile.name} ({formatFileSize(pickedFile.size)})
              </span>
            ) : (
              <p style={{ fontSize: 14, color: 'var(--color-steel)', margin: 0 }}>
                将 .xlsx 或 .xls 文件拖拽到此处，或点击选择文件
              </p>
            )}
          </div>

          {flow.kind === 'error' && (
            <p style={{ marginTop: 8, fontSize: 13, color: 'var(--color-danger)' }} role="alert">
              {flow.message}
            </p>
          )}

          {isUploading && (
            <p style={{ marginTop: 8, fontSize: 13, color: 'var(--color-primary)' }} aria-live="polite">
              正在上传文件... {flow.progress > 0 ? `${flow.progress}%` : ''}
            </p>
          )}

          <div style={{ display: 'flex', gap: 8, marginTop: 12 }}>
            <button type="button" className="action-secondary"
                    onClick={handleDownloadTemplate} disabled={downloadingTemplate}>
              {downloadingTemplate ? '正在生成模板...' : '下载模板'}
            </button>
            <button type="button" className="action-primary"
                    disabled={!pickedFile || isUploading || isLocked || activeJobLoading}
                    onClick={handleUpload} title={isLocked ? lockTooltip : undefined}>
              上传并生成预览
            </button>
          </div>
        </div>
      );
    }
    ```

    2) 修改 `frontend/src/components/eligibility-import/ImportTabContent.tsx` — 把 `<ExcelImportPanel onResult={...} />` 改为 `<ExcelImportPanel onComplete={(r) => console.info('Import done', r)} />`；如原本在外渲染 `ImportResultPanel`，移除（ExcelImportPanel 内部 'done' 分支已显示 success summary）；旧 `ImportResultPanel.tsx` 文件保留但不再渲染。
  </action>
  <verify>
    <automated>
      cd frontend &amp;&amp; npm run lint &amp;&amp; npm run build
    </automated>
  </verify>
  <acceptance_criteria>
    - grep "type ImportFlowState" frontend/src/components/eligibility-import/ExcelImportPanel.tsx 命中
    - grep "downloadTemplate" frontend/src/components/eligibility-import/ExcelImportPanel.tsx 命中（不再用 getTemplateUrl）
    - grep "uploadAndPreview" frontend/src/components/eligibility-import/ExcelImportPanel.tsx 命中
    - grep "confirmImport" frontend/src/components/eligibility-import/ExcelImportPanel.tsx 命中
    - grep "cancelImport" frontend/src/components/eligibility-import/ExcelImportPanel.tsx 命中
    - grep "getActiveImportJob" frontend/src/components/eligibility-import/ExcelImportPanel.tsx 命中
    - grep "ImportPreviewPanel" frontend/src/components/eligibility-import/ExcelImportPanel.tsx 命中
    - grep "ImportActiveJobBanner" frontend/src/components/eligibility-import/ExcelImportPanel.tsx 命中
    - grep "上传并生成预览" frontend/src/components/eligibility-import/ExcelImportPanel.tsx 命中
    - grep "正在生成模板" frontend/src/components/eligibility-import/ExcelImportPanel.tsx 命中
    - 命令 `cd frontend && npm run lint` 退出码 0
    - 命令 `cd frontend && npm run build` 退出码 0
  </acceptance_criteria>
  <done>
    ExcelImportPanel 改造完成；7 态机正确切换；6 个新组件接入；blob 模板下载替代旧实现；活跃 job banner + 按钮禁用 + 409 处理全部就绪；npm run build 通过。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: APScheduler import_scheduler 注册 + main.py 启动 / 停止 + E2E 集成测试</name>
  <files>
    backend/app/scheduler/import_scheduler.py,
    backend/app/main.py,
    backend/tests/test_integration/__init__.py,
    backend/tests/test_integration/test_import_e2e.py
  </files>
  <read_first>
    - backend/app/scheduler/feishu_scheduler.py 全文（**直接照抄结构** — AsyncIOScheduler 实例 + start/stop/reload 函数 + run_xxx 内部用独立 SessionLocal）
    - backend/app/main.py（确认 lifespan 函数位置 + feishu_scheduler 启动 / 停止的现有调用模式 — 本 plan 加 import_scheduler.start_import_scheduler / stop_import_scheduler 紧邻）
    - backend/app/services/import_service.py（确认 expire_stale_import_jobs 签名 — Plan 03 已建）
    - backend/tests/test_api/test_eligibility_import_preview_api.py（Plan 04 已建，本 plan 集成测试可复用 client_hrbp / xlsx_factory / employee_factory fixtures）
    - backend/app/core/config.py（确认 storage_base_dir 字段 — E2E 测试需 tmp_uploads_dir 隔离）
  </read_first>
  <behavior>
    - Test 1: import_scheduler.start_import_scheduler() 注册 cron job id='import_expire_stale'，trigger 是 IntervalTrigger(minutes=15)
    - Test 2: import_scheduler.stop_import_scheduler() 停止后 scheduler.running == False
    - Test 3: run_expire_stale_jobs() 用独立 SessionLocal session 调用 ImportService.expire_stale_import_jobs，异常被 logger.exception 捕获不抛
    - Test 4: E2E hire_info：preview → confirm（merge）→ DB 更新 → AuditLog 写入 → 暂存文件删除
    - Test 5: E2E hire_info：preview → confirm（replace + confirm_replace=True）→ AuditLog detail.overwrite_mode == 'replace'
    - Test 6: E2E non_statutory_leave：上传冲突文件（同业务键 2 行）→ preview counters.conflict >= 2
    - Test 7: E2E performance_grades：重复导入相同 (employee_no, year)，第二次 preview action='no_change'，DB 中只有 1 条 PerformanceRecord
    - Test 8: E2E salary_adjustments：重复导入相同 (employee_no, date, type)，第二次不产生重复行（解决 Pitfall 4）
    - Test 9: E2E 模板下载真实字节：4 类 import_type 都返回非空 xlsx 且 openpyxl 读回 sheet 名为 'Sheet'
  </behavior>
  <action>
    1) 创建 `backend/app/scheduler/import_scheduler.py`（**结构完全照抄 feishu_scheduler.py** 但用 IntervalTrigger）：

    ```python
    """Phase 32 D-17: APScheduler 定时清理 import 僵尸 job。

    选 APScheduler 而非 Celery Beat 的理由：
      1. 复用既有 feishu_scheduler.py 模式（v1.4 已确立）
      2. 不增加新部署进程
      3. 单元测试友好（直接调用 run_expire_stale_jobs 不依赖 broker）
    """
    from __future__ import annotations
    import logging

    from apscheduler.schedulers.asyncio import AsyncIOScheduler
    from apscheduler.triggers.interval import IntervalTrigger

    logger = logging.getLogger(__name__)

    scheduler = AsyncIOScheduler()


    def start_import_scheduler(*, interval_minutes: int = 15) -> None:
        """启动 import 僵尸清理调度器，每 interval_minutes 分钟执行一次。"""
        scheduler.add_job(
            run_expire_stale_jobs,
            trigger=IntervalTrigger(minutes=interval_minutes),
            id='import_expire_stale',
            replace_existing=True,
        )
        scheduler.start()
        logger.info('Import scheduler started: every %d minutes', interval_minutes)


    def stop_import_scheduler() -> None:
        if scheduler.running:
            scheduler.shutdown(wait=False)
            logger.info('Import scheduler stopped')


    def run_expire_stale_jobs() -> None:
        """定时清理 — 用独立 SessionLocal 避免与请求 session 冲突。"""
        from backend.app.core.database import SessionLocal
        db = SessionLocal()
        try:
            from backend.app.services.import_service import ImportService
            service = ImportService(db)
            result = service.expire_stale_import_jobs()
            if result['processing'] or result['previewing']:
                logger.info('Expired stale import jobs: %s', result)
        except Exception:
            logger.exception('Failed to expire stale import jobs')
        finally:
            db.close()
    ```

    2) 修改 `backend/app/main.py` — 在 lifespan 函数中加 import_scheduler 启动 / 停止：

    ```python
    # main.py 顶部 import 区域加：
    from backend.app.scheduler import import_scheduler

    # lifespan 函数内（具体位置根据现有代码）：
    @asynccontextmanager
    async def lifespan(app: FastAPI):
        # ... 现有 init_database / feishu_scheduler.start_scheduler 等 ...
        import_scheduler.start_import_scheduler(interval_minutes=15)  # Phase 32 D-17
        try:
            yield
        finally:
            # ... 现有 feishu_scheduler.stop_scheduler 等 ...
            import_scheduler.stop_import_scheduler()
    ```

    3) 创建 `backend/tests/test_integration/__init__.py`（空文件）。

    4) 创建 `backend/tests/test_integration/test_import_e2e.py`：

    ```python
    """Phase 32 E2E 集成测试 — 跨层验收 SC1-SC5。"""
    from __future__ import annotations
    import io
    import pytest
    from openpyxl import load_workbook


    def _post_preview(client, xlsx_bytes, import_type, filename='t.xlsx'):
        return client.post(
            f'/api/v1/eligibility-import/excel/preview?import_type={import_type}',
            files={'file': (filename, io.BytesIO(xlsx_bytes),
                            'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet')},
        )


    def test_e2e_hire_info_merge(client_hrbp, db_session, employee_factory, xlsx_factory, tmp_uploads_dir):
        from datetime import date
        from backend.app.models.audit_log import AuditLog
        from sqlalchemy import select
        emp = employee_factory(employee_no='E00001', hire_date=None)
        data = xlsx_factory['hire_info'](rows=[['E00001', '2024-03-15', '2025-08-01']])
        prev = _post_preview(client_hrbp, data, 'hire_info')
        assert prev.status_code == 200
        job_id = prev.json()['job_id']
        confirm = client_hrbp.post(
            f'/api/v1/eligibility-import/excel/{job_id}/confirm',
            json={'overwrite_mode': 'merge', 'confirm_replace': False},
        )
        assert confirm.status_code == 200
        assert confirm.json()['status'] == 'completed'
        db_session.refresh(emp)
        assert emp.hire_date == date(2024, 3, 15)
        log = db_session.execute(select(AuditLog).where(
            AuditLog.target_id == job_id, AuditLog.action == 'import_confirmed',
        )).scalar_one()
        assert log.detail['overwrite_mode'] == 'merge'

    def test_e2e_hire_info_replace_with_confirm_flag(client_hrbp, db_session, employee_factory, xlsx_factory, tmp_uploads_dir):
        emp = employee_factory(employee_no='E00001')
        data = xlsx_factory['hire_info']()
        prev = _post_preview(client_hrbp, data, 'hire_info')
        job_id = prev.json()['job_id']
        confirm = client_hrbp.post(
            f'/api/v1/eligibility-import/excel/{job_id}/confirm',
            json={'overwrite_mode': 'replace', 'confirm_replace': True},
        )
        assert confirm.status_code == 200
        from backend.app.models.audit_log import AuditLog
        from sqlalchemy import select
        log = db_session.execute(select(AuditLog).where(AuditLog.target_id == job_id)).scalar_one()
        assert log.detail['overwrite_mode'] == 'replace'

    def test_e2e_perf_grades_idempotent(client_hrbp, db_session, employee_factory, xlsx_factory, tmp_uploads_dir):
        from backend.app.models.performance_record import PerformanceRecord
        from sqlalchemy import select
        emp = employee_factory(employee_no='E00001')
        data = xlsx_factory['performance_grades'](rows=[['E00001', 2026, 'A']])
        p1 = _post_preview(client_hrbp, data, 'performance_grades')
        client_hrbp.post(f'/api/v1/eligibility-import/excel/{p1.json()["job_id"]}/confirm',
                         json={'overwrite_mode': 'merge'})
        p2 = _post_preview(client_hrbp, data, 'performance_grades')
        client_hrbp.post(f'/api/v1/eligibility-import/excel/{p2.json()["job_id"]}/confirm',
                         json={'overwrite_mode': 'merge'})
        recs = db_session.execute(
            select(PerformanceRecord).where(PerformanceRecord.employee_id == emp.id, PerformanceRecord.year == 2026)
        ).scalars().all()
        assert len(recs) == 1

    def test_e2e_salary_adjustments_idempotent(client_hrbp, db_session, employee_factory, xlsx_factory, tmp_uploads_dir):
        """解决 Pitfall 4：upsert by (employee_no, date, type)。"""
        from backend.app.models.salary_adjustment_record import SalaryAdjustmentRecord
        from sqlalchemy import select
        emp = employee_factory(employee_no='E00001')
        data = xlsx_factory['salary_adjustments'](rows=[['E00001', '2026-01-01', 'annual', 1000.0]])
        p1 = _post_preview(client_hrbp, data, 'salary_adjustments')
        client_hrbp.post(f'/api/v1/eligibility-import/excel/{p1.json()["job_id"]}/confirm',
                         json={'overwrite_mode': 'merge'})
        p2 = _post_preview(client_hrbp, data, 'salary_adjustments')
        client_hrbp.post(f'/api/v1/eligibility-import/excel/{p2.json()["job_id"]}/confirm',
                         json={'overwrite_mode': 'merge'})
        recs = db_session.execute(
            select(SalaryAdjustmentRecord).where(SalaryAdjustmentRecord.employee_id == emp.id)
        ).scalars().all()
        assert len(recs) == 1

    def test_e2e_non_statutory_leave_conflict_detection(client_hrbp, db_session, employee_factory, xlsx_factory, tmp_uploads_dir):
        """同文件内业务键重复 → preview counters.conflict 反映。"""
        emp = employee_factory(employee_no='E00001')
        data = xlsx_factory['non_statutory_leave'](with_conflict=True)
        prev = _post_preview(client_hrbp, data, 'non_statutory_leave')
        assert prev.status_code == 200
        body = prev.json()
        assert body['counters']['conflict'] >= 2

    @pytest.mark.parametrize('import_type', [
        'performance_grades', 'salary_adjustments', 'hire_info', 'non_statutory_leave',
    ])
    def test_e2e_template_xlsx_downloadable(client_hrbp, import_type):
        """SC1: 4 类 import_type 模板下载真实字节，openpyxl 可读回。"""
        resp = client_hrbp.get(f'/api/v1/eligibility-import/templates/{import_type}?format=xlsx')
        assert resp.status_code == 200
        assert len(resp.content) > 100
        wb = load_workbook(io.BytesIO(resp.content))
        assert wb.sheetnames

    def test_e2e_scheduler_startup_smoke():
        from backend.app.scheduler import import_scheduler
        assert hasattr(import_scheduler, 'start_import_scheduler')
        assert hasattr(import_scheduler, 'stop_import_scheduler')
        assert hasattr(import_scheduler, 'run_expire_stale_jobs')
        assert hasattr(import_scheduler, 'scheduler')

    def test_run_expire_stale_jobs_uses_isolated_session(tmp_uploads_dir):
        from backend.app.scheduler import import_scheduler
        import_scheduler.run_expire_stale_jobs()  # 应不抛异常
    ```
  </action>
  <verify>
    <automated>
      .venv/bin/pytest backend/tests/test_integration/test_import_e2e.py -x --tb=short
    </automated>
  </verify>
  <acceptance_criteria>
    - 文件 backend/app/scheduler/import_scheduler.py 存在且 grep "def start_import_scheduler" 命中
    - grep "IntervalTrigger(minutes=interval_minutes)" backend/app/scheduler/import_scheduler.py 命中
    - grep "id='import_expire_stale'" backend/app/scheduler/import_scheduler.py 命中
    - grep "def run_expire_stale_jobs" backend/app/scheduler/import_scheduler.py 命中
    - grep "expire_stale_import_jobs" backend/app/scheduler/import_scheduler.py 命中
    - grep "from backend.app.scheduler import import_scheduler" backend/app/main.py 命中
    - grep "start_import_scheduler" backend/app/main.py 命中
    - grep "stop_import_scheduler" backend/app/main.py 命中
    - 命令 `.venv/bin/pytest backend/tests/test_integration/test_import_e2e.py -x` 全部 PASS（9 用例）
    - 命令 `.venv/bin/pytest backend/tests/ -x --tb=short -q` 全套测试无 regression
    - 命令 `.venv/bin/python -c "from backend.app.main import app; print('OK')"` 启动加载无错误
  </acceptance_criteria>
  <done>
    APScheduler import_scheduler 注册并集成到 lifespan；E2E 9 用例 PASS；所有后端端到端流程可用。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 浏览器端到端 UAT — 4 个关键交互验证</name>
  <files>
    （无文件变更 — 人工验证 checkpoint）
  </files>
  <what-built>
    Phase 32 全部交付：
    - 后端：6 类 import_type 全支持、preview/confirm/cancel/active 4 端点、APScheduler 定时清理
    - 前端：ExcelImportPanel 7 态状态机、6 个新组件、blob 模板下载、活跃 job banner、ReplaceModeConfirmModal
    - 数据：ImportJob 加 overwrite_mode/actor_id/updated_at；SalaryAdjustmentRecord 加 UniqueConstraint；AuditLog import_confirmed action 写入
  </what-built>
  <action>
    **人工 UAT 步骤（无代码变更；请按顺序完成 4 项验证，拍屏截图留档）**

    **前置：** 启动后端（`uvicorn backend.app.main:app --reload`）+ 前端（`cd frontend && npm run dev`），用 hrbp 角色账号登录，进入「调薪资格管理」页面。

    **验证项 1（IMPORT-02 + Pitfall 5 + D-05）：模板下载跨浏览器**
    - 步骤：在 4 个 Tab（performance_grades / salary_adjustments / hire_info / non_statutory_leave）每个都点「下载模板」按钮
    - 期望：浏览器立即触发下载（不在新 Tab 渲染乱码）；文件名为 `{import_type}_template.xlsx`
    - 验证字节正确：在终端运行 `python -c "from openpyxl import load_workbook; wb = load_workbook('hire_info_template.xlsx'); print(wb.active['A1'].value, wb.active['A1'].number_format)"` 应输出 `员工工号 @`
    - 关键浏览器：Chrome（必测）+ Safari（如有 Mac，验证 D-05 Safari 兼容）+ Edge

    **验证项 2（IMPORT-07 + D-08 / D-10）：Preview 视图与字段级 diff**
    - 步骤：上传一个 hire_info 文件（含 1 行 insert + 1 行 update + 1 行 no_change + 1 行同业务键重复触发 conflict）
    - 期望：
      - 顶部 4 色计数卡片：新增/更新/无变化/冲突分别是绿/蓝/灰/红
      - Diff 表格 conflict 行整行浅红 + 左侧红色指示条 + 行内显示「同文件内 (employee_no=...) 出现 N 次」
      - 「显示未变化 N 行」chip 默认折叠 no_change 行；点击展开
      - 「确认导入」按钮在有 conflict 时禁用且 tooltip 显示「存在 N 条冲突，请先修正 Excel 后重新上传」

    **验证项 3（IMPORT-05 + D-11）：Replace 模式二次确认 Modal 焦点循环**
    - 步骤：上传任意 hire_info 文件 → preview → 选择「替换模式」Radio → 看到红色 inline 警告 → 点「确认导入（替换模式）」
    - 期望：
      - Modal 弹出，焦点自动移入 checkbox（按 Tab 键应在 Modal 内循环 — checkbox → 返回 → 继续 → 回到 checkbox）
      - checkbox 未勾选时「继续（替换模式）」按钮禁用且 tooltip「请先勾选上方...」
      - 按 ESC 键 Modal 关闭，焦点回到「确认导入（替换模式）」按钮
      - 勾选 checkbox 后「继续（替换模式）」可点击

    **验证项 4（IMPORT-06 + D-18）：双 Tab 并发 → 409 toast + 按钮禁用**
    - 步骤：
      1. Tab A：上传 hire_info 文件 → preview 出现，不点确认
      2. Tab B：打开同一页面，进入 hire_info Tab
    - 期望：
      - Tab B 顶部出现黄色 ImportActiveJobBanner「该类型导入正在进行中（预览待确认...）」
      - Tab B「选择文件」按钮禁用（hover 显示 tooltip）
      - 如 Tab B 仍尝试上传（强行点击）→ 应收到 409 错误 toast/提示「该类型导入正在进行中，请等待当前任务完成后再试。」
  </action>
  <how-to-verify>
    按照 <action> 步骤逐项完成 4 个验证点。每项记录：浏览器版本 + 时间戳 + 截图 + 期望 vs 实际对照表。
  </how-to-verify>
  <verify>
    （人工 checkpoint — 无 automated 验证；由 HR 操作者对照 4 个验证项清单判断 PASS/FAIL）
  </verify>
  <acceptance_criteria>
    - 验证项 1：4 类模板全部下载成功 + openpyxl 可读回 + 工号列 cell.number_format == '@'
    - 验证项 2：4 色计数卡片颜色对齐 UI-SPEC + conflict 行视觉 + no_change 折叠 + 按钮 disabled 语义
    - 验证项 3：Modal focus trap + ESC 关闭 + checkbox 必勾选 + 继续按钮 disabled 语义
    - 验证项 4：ImportActiveJobBanner 显示 + 选择文件按钮 disabled + 409 toast/提示文案对齐 UI-SPEC
    - 截图存档位置：`.planning/phases/32-eligibility-import-completion/uat-screenshots/` 或在 32-06-SUMMARY.md 中 inline
  </acceptance_criteria>
  <done>
    4 项验证全部 PASS；截图与浏览器版本记录已归档；准备 `/gsd-verify-work` 生成 VERIFICATION.md 与 milestone 推进记录。
  </done>
  <resume-signal>
    所有 4 项验证通过后回复 "approved"。如发现问题，列出具体 issue（截图 + 浏览器版本 + 复现步骤），转 /gsd-plan-phase --gaps 修复。
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器拖拽上传 → 前端组件 → backend API | 完整链路 |
| APScheduler → backend SessionLocal → DB | 独立 session 写终态（与 feishu_scheduler 同模式） |
| HR 浏览器 Modal 交互 → confirm_replace flag | 防止 replace 模式被误触发 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-15 | T (Tampering) | replace 二次确认被绕过 | mitigate | Task 1 ReplaceModeConfirmModal 强制 checkbox + Task 1 confirmImport 调用始终透传 confirm_replace；后端 Plan 04 服务端校验兜底 |
| T-32-20 | D (DoS) | APScheduler 进程崩溃 → expire 任务停摆 | accept | Task 2 run_expire_stale_jobs 内部 try/except 防单次失败影响调度；如 ASGI 进程整体崩溃，进程重启时 lifespan 自动重启 scheduler |
| T-32-21 | I (Information disclosure) | 浏览器 Diff 表格 XSS | accept | React JSX 默认 escape；Plan 05 PreviewDiffTable 用 `{String(diff.old)}` 模式 |
</threat_model>

<verification>
- 后端集成测试 9 用例 PASS（4 类 × 模板 + merge/replace + 幂等 + 冲突检测 + 调度器 smoke）
- 前端 npm run lint + build 全部通过
- 全套 `.venv/bin/pytest backend/tests/ -x` 无 regression
- Manual UAT 4 个验证点通过（checkpoint）
- ROADMAP Phase 32 SC1-SC5 全部满足：
  - SC1：4 类资格 import_type 模板下载非空 xlsx（test_e2e_template_xlsx_downloadable parametrize）
  - SC2：Preview + diff（test_e2e_non_statutory_leave_conflict_detection）+ HR 显式确认才落库
  - SC3：merge/replace Radio + AuditLog 记录 overwrite_mode（test_e2e_hire_info_replace_with_confirm_flag）
  - SC4：per-import_type 锁 + 409（Plan 04 test_preview_concurrent_same_type_returns_409 + 本 plan UAT 验证项 4）
  - SC5：重复导入按业务键 upsert（test_e2e_perf_grades_idempotent + test_e2e_salary_adjustments_idempotent）
</verification>

<success_criteria>
Phase 32 全部 5 个 ROADMAP SUCCESS CRITERIA 满足：
1. ✅ 4 类资格 import_type 模板下载非空 xlsx（IMPORT-02）
2. ✅ Preview + diff + HR 显式确认（IMPORT-07）
3. ✅ merge/replace Radio + AuditLog 记录 overwrite_mode（IMPORT-05）
4. ✅ per-import_type 锁 409 拒绝（IMPORT-06）
5. ✅ 重复导入按业务键 upsert 不产生重复行（IMPORT-01 + IMPORT-05）

技术决议归档：
- D-17 选 APScheduler（决议 RESEARCH Open Question 2）
- _import_salary_adjustments 业务键 = (employee_no, adjustment_date, adjustment_type)（决议 Open Question 1）
- confirm 走同步而非 Celery（决议 Open Question 3）
- 旧 POST /excel 标 deprecated 保留兼容（决议 Open Question 4）
- 文件 hash 校验做（决议 Open Question 5）
</success_criteria>

<output>
完成后创建 `.planning/phases/32-eligibility-import-completion/32-06-SUMMARY.md`，重点记录：
- ExcelImportPanel 7 态状态机说明（旧→新对比）
- import_scheduler 入口（每 15 分钟 cron）
- 5 个 RESEARCH Open Question 的最终决议
- Manual UAT 通过记录（截图 / 浏览器版本 / 时间戳）
- Phase 32 全部 5 个 SC 的验收映射

随后由 /gsd-verify-work 阶段创建 32-VERIFICATION.md（不在本 plan 范围）。
</output>
