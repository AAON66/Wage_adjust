---
phase: 32-eligibility-import-completion
plan: 05
type: execute
wave: 4
depends_on:
  - 32-04
files_modified:
  - frontend/src/types/api.ts
  - frontend/src/services/eligibilityImportService.ts
  - frontend/src/components/eligibility-import/PreviewCountersStrip.tsx
  - frontend/src/components/eligibility-import/PreviewDiffTable.tsx
  - frontend/src/components/eligibility-import/OverwriteModeRadio.tsx
  - frontend/src/components/eligibility-import/ReplaceModeConfirmModal.tsx
  - frontend/src/components/eligibility-import/ImportActiveJobBanner.tsx
  - frontend/src/components/eligibility-import/ImportPreviewPanel.tsx
autonomous: true
requirements:
  - IMPORT-02
  - IMPORT-05
  - IMPORT-06
  - IMPORT-07

must_haves:
  truths:
    - "frontend/src/types/api.ts 含 PreviewResponse / PreviewRow / ConfirmRequest / ConfirmResponse / ActiveJobResponse / ImportJobStatus / EligibilityImportType 完整类型定义；ImportJobStatus union 含 'previewing' / 'cancelled' 两个新状态"
    - "eligibilityImportService.downloadTemplate(importType): Promise<void> 用 axios responseType:'blob' + URL.createObjectURL + <a download> 触发下载，替代旧 getTemplateUrl"
    - "eligibilityImportService 新增 5 个函数：uploadAndPreview / confirmImport / cancelImport / getActiveImportJob / downloadTemplate"
    - "5 个新前端组件创建：PreviewCountersStrip / PreviewDiffTable / OverwriteModeRadio / ReplaceModeConfirmModal / ImportActiveJobBanner"
    - "ImportPreviewPanel 整合 5 个子组件，受控状态 overwriteMode/page/noChangeCollapsed/replaceModalOpen，对外暴露 onConfirm/onCancel/onOverwriteModeChange"
    - "tsc --noEmit 通过（项目 lint 命令），无类型错误"
  artifacts:
    - path: frontend/src/types/api.ts
      provides: "Phase 32 完整 TypeScript 类型契约（与 backend Pydantic schemas 一一对应）"
      contains: "interface PreviewResponse"
    - path: frontend/src/services/eligibilityImportService.ts
      provides: "5 个新 service 函数 + downloadTemplate 替代旧 getTemplateUrl"
      contains: "export async function uploadAndPreview"
    - path: frontend/src/components/eligibility-import/PreviewCountersStrip.tsx
      provides: "4 色计数卡片组件（D-08 / UI-SPEC § 4 色计数卡片）"
      contains: "export function PreviewCountersStrip"
    - path: frontend/src/components/eligibility-import/PreviewDiffTable.tsx
      provides: "Diff 表格 + 分页 + no_change 折叠 + conflict 红底高亮（D-08 / D-10）"
      contains: "export function PreviewDiffTable"
    - path: frontend/src/components/eligibility-import/OverwriteModeRadio.tsx
      provides: "merge/replace Radio + replace 选中 inline 警告（D-11 / UI-SPEC § Copywriting 覆盖模式选择区）"
      contains: "export function OverwriteModeRadio"
    - path: frontend/src/components/eligibility-import/ReplaceModeConfirmModal.tsx
      provides: "二次确认 Modal + 强制 checkbox + focus trap + ESC 关闭（D-11 / UI-SPEC § Accessibility）"
      contains: "export function ReplaceModeConfirmModal"
    - path: frontend/src/components/eligibility-import/ImportActiveJobBanner.tsx
      provides: "Tab 顶部 inline 提示条（D-18 + UI-SPEC § 并发锁与 409 提示）"
      contains: "export function ImportActiveJobBanner"
    - path: frontend/src/components/eligibility-import/ImportPreviewPanel.tsx
      provides: "整合 5 个子组件的 Preview 面板壳"
      contains: "export function ImportPreviewPanel"
  key_links:
    - from: frontend/src/services/eligibilityImportService.ts
      to: backend/app/api/v1/eligibility_import.py
      via: "5 个 service 函数 endpoint URL 与后端 4 个新端点一一对应"
      pattern: "/eligibility-import/excel/preview"
    - from: frontend/src/components/eligibility-import/ImportPreviewPanel.tsx
      to: frontend/src/types/api.ts
      via: "props 类型完全用 PreviewResponse / ConfirmRequest"
      pattern: "PreviewResponse"
---

<objective>
建立 Phase 32 前端类型契约与可复用组件库。本 plan 不接通 ExcelImportPanel 状态机（留 Plan 06），只建立：

1. **TypeScript 类型契约** — `types/api.ts` 加 PreviewResponse / PreviewRow / FieldDiff / PreviewCounters / ConfirmRequest / ConfirmResponse / ActiveJobResponse / ImportJobStatus union（含 'previewing' / 'cancelled' 两新状态，解决 Pitfall 2 stricter typing）
2. **service 层 5 个新函数** — uploadAndPreview / confirmImport / cancelImport / getActiveImportJob / downloadTemplate（替代旧 getTemplateUrl，blob 下载 D-05）
3. **5 个独立 UI 子组件** — 按 UI-SPEC § Component Inventory 实现，最小 props 原则，可在 Plan 06 由 ExcelImportPanel 直接组合，也可在未来 Phase 34 绩效管理页面复用

Purpose: 把契约 + 可复用组件做成稳定基线，Plan 06 做 ExcelImportPanel 状态机改造时只是「拼装」5 个子组件。

Output:
- types/api.ts 扩展
- eligibilityImportService.ts 扩展
- 6 个新 .tsx 组件文件（5 个子 + 1 个 ImportPreviewPanel 整合壳）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-eligibility-import-completion/32-04-SUMMARY.md
@.planning/phases/32-eligibility-import-completion/32-CONTEXT.md
@.planning/phases/32-eligibility-import-completion/32-UI-SPEC.md
@.planning/phases/32-eligibility-import-completion/32-RESEARCH.md
@CLAUDE.md
@frontend/src/types/api.ts
@frontend/src/services/eligibilityImportService.ts
@frontend/src/services/feishuService.ts
@frontend/src/components/eligibility-import/ExcelImportPanel.tsx
@frontend/src/components/feishu-sync-logs/CountersBadgeCluster.tsx
@frontend/src/components/feishu-sync-logs/SyncLogDetailDrawer.tsx
@frontend/src/index.css

<interfaces>
<!-- Backend 契约（Plan 04 已落地）-->
| Method | URL | Body | Response |
|--------|-----|------|----------|
| POST | /api/v1/eligibility-import/excel/preview?import_type=X | multipart file | PreviewResponse |
| POST | /api/v1/eligibility-import/excel/{job_id}/confirm | ConfirmRequest JSON | ConfirmResponse |
| POST | /api/v1/eligibility-import/excel/{job_id}/cancel | (none) | 204 |
| GET | /api/v1/eligibility-import/excel/active?import_type=X | — | ActiveJobResponse |
| GET | /api/v1/eligibility-import/templates/{import_type}?format=xlsx | — | binary xlsx |

<!-- Pydantic schemas (backend/app/schemas/import_preview.py)，TypeScript 端必须一一映射 -->
PreviewResponse: { job_id, import_type, file_name, total_rows, counters: PreviewCounters,
                   rows: PreviewRow[], rows_truncated, truncated_count,
                   preview_expires_at: ISO datetime string, file_sha256 }
PreviewRow: { row_number, action: 'insert'|'update'|'no_change'|'conflict',
              employee_no, fields: Record<string, FieldDiff>, conflict_reason?: string }
FieldDiff: { old: any, new: any }
PreviewCounters: { insert, update, no_change, conflict }
ConfirmRequest: { overwrite_mode: 'merge'|'replace', confirm_replace?: boolean }
ConfirmResponse: { job_id, status: 'completed'|'partial'|'failed', total_rows,
                   inserted_count, updated_count, no_change_count, failed_count,
                   execution_duration_ms }
ActiveJobResponse: { active, job_id?, status?: 'previewing'|'processing',
                     created_at?: ISO, file_name? }

<!-- ImportJobStatus union（解决 Pitfall 2）-->
type ImportJobStatus = 'pending' | 'previewing' | 'processing'
                     | 'completed' | 'failed' | 'partial' | 'cancelled';

<!-- 既有 Phase 31 blob 下载模式（D-05 直接抄）-->
frontend/src/services/feishuService.ts:69-83:
```typescript
export async function downloadUnmatchedCsv(logId: string): Promise<void> {
  const response = await api.get<Blob>(`/feishu/sync-logs/${logId}/unmatched.csv`,
    { responseType: 'blob' });
  const url = URL.createObjectURL(response.data);
  const link = document.createElement('a');
  link.href = url; link.download = `sync-log-${logId}-unmatched.csv`;
  document.body.appendChild(link); link.click(); link.remove();
  URL.revokeObjectURL(url);
}
```

<!-- index.css 既定 class（UI-SPEC §「不允许重造」）-->
- .surface — 卡片外框
- .action-primary / .action-secondary / .action-danger — CTA 按钮
- .table-lite / .table-shell — 表格容器
- .chip-button / .chip-button-active — 分页 / 折叠按钮
- .empty-state — 空状态
- .animate-fade-up / .animate-fade-soft — 进入动画
- .eyebrow / .section-title — 标题
- .metric-tile / .metric-value — 计数卡片基底

<!-- CSS 变量 token（不引入新色）-->
- --color-success / --color-success-bg / --color-success-border
- --color-info / --color-info-bg / --color-primary-border
- --color-steel / --color-bg-subtle / --color-border
- --color-danger / --color-danger-bg / --color-danger-border
- --color-primary / --color-primary-light
- --color-warning / --color-warning-bg
- --color-ink / --color-placeholder

<!-- 已有 Phase 31 视觉参考 -->
frontend/src/components/feishu-sync-logs/CountersBadgeCluster.tsx — 0 值弱化模式
frontend/src/components/feishu-sync-logs/SyncLogDetailDrawer.tsx — focus trap / ESC 关闭参考
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 扩展 types/api.ts + 改造 eligibilityImportService.ts（含 blob 下载）</name>
  <files>
    frontend/src/types/api.ts,
    frontend/src/services/eligibilityImportService.ts
  </files>
  <read_first>
    - frontend/src/types/api.ts（确认现有类型组织模式，本 plan 在文件末尾追加 Phase 32 类型；同时检查既有 ImportJob / ImportJobStatus 是否已定义，避免重名）
    - frontend/src/services/eligibilityImportService.ts 全文（确认现有函数签名，本 plan 追加 5 个新函数 + 替换 getTemplateUrl 为 downloadTemplate）
    - frontend/src/services/feishuService.ts:69-83（D-05 blob 下载模式参考，**直接照抄结构**）
    - frontend/src/services/api.ts（确认 axios instance 名为 `api`，JWT 拦截器已就绪）
    - frontend/src/services/importService.ts（如有；可能含旧 ImportJob 类型 — 不要重复定义，跨服务复用）
  </read_first>
  <action>
    1) 在 `frontend/src/types/api.ts` 末尾追加 Phase 32 类型块（**追加，不替换现有**；如已存在 ImportJob/ImportJobStatus 则改为扩展或合并 union）：

    ```typescript
    // ===================== Phase 32 调薪资格导入 (IMPORT-01/02/05/06/07) =====================

    export type EligibilityImportType =
      | 'performance_grades'
      | 'salary_adjustments'
      | 'hire_info'
      | 'non_statutory_leave';

    /** ImportJob 状态机 union — Phase 32 加 'previewing' / 'cancelled'（解决 Pitfall 2 stricter typing） */
    export type ImportJobStatus =
      | 'pending'
      | 'previewing'
      | 'processing'
      | 'completed'
      | 'failed'
      | 'partial'
      | 'cancelled';

    export type OverwriteMode = 'merge' | 'replace';

    export type PreviewRowAction = 'insert' | 'update' | 'no_change' | 'conflict';

    export interface FieldDiff {
      old: unknown | null;
      new: unknown | null;
    }

    export interface PreviewRow {
      row_number: number;          // Excel 行号（含表头偏移）
      action: PreviewRowAction;
      employee_no: string;
      fields: Record<string, FieldDiff>;
      conflict_reason?: string | null;
    }

    export interface PreviewCounters {
      insert: number;
      update: number;
      no_change: number;
      conflict: number;
    }

    export interface PreviewResponse {
      job_id: string;
      import_type: EligibilityImportType;
      file_name: string;
      total_rows: number;
      counters: PreviewCounters;
      rows: PreviewRow[];
      rows_truncated: boolean;
      truncated_count: number;
      preview_expires_at: string;     // ISO datetime
      file_sha256: string;
    }

    export interface ConfirmRequest {
      overwrite_mode: OverwriteMode;
      confirm_replace?: boolean;
    }

    export interface ConfirmResponse {
      job_id: string;
      status: Extract<ImportJobStatus, 'completed' | 'partial' | 'failed'>;
      total_rows: number;
      inserted_count: number;
      updated_count: number;
      no_change_count: number;
      failed_count: number;
      execution_duration_ms: number;
    }

    export interface ActiveJobResponse {
      active: boolean;
      job_id?: string | null;
      status?: Extract<ImportJobStatus, 'previewing' | 'processing'> | null;
      created_at?: string | null;
      file_name?: string | null;
    }

    /** 409 错误响应 detail 结构（D-16 + Plan 04） */
    export interface ImportConflictDetail {
      error: 'import_in_progress';
      import_type: EligibilityImportType;
      message: string;
    }
    ```

    2) 修改 `frontend/src/services/eligibilityImportService.ts` — 替换 getTemplateUrl + 追加 5 个新函数：

    ```typescript
    import api from './api';
    import type {
      EligibilityImportType,
      PreviewResponse,
      ConfirmRequest,
      ConfirmResponse,
      ActiveJobResponse,
      OverwriteMode,
    } from '../types/api';

    // 现有类型保留：FeishuFieldInfo + 现有函数 uploadEligibilityExcel (deprecated)
    // / parseBitableUrl / fetchBitableFields / triggerFeishuSync / getSyncStatus

    export type { EligibilityImportType };

    export interface FeishuFieldInfo { /* ... 现有 ... */ }

    /**
     * @deprecated Phase 32 起请改用 uploadAndPreview + confirmImport 两阶段提交
     */
    export async function uploadEligibilityExcel(importType: EligibilityImportType, file: File) {
      const formData = new FormData();
      formData.append('file', file);
      return api.post(`/eligibility-import/excel?import_type=${importType}`, formData, {
        headers: { 'Content-Type': 'multipart/form-data' },
        timeout: 120000,
      });
    }

    // ... 现有 parseBitableUrl / fetchBitableFields / triggerFeishuSync / getSyncStatus 保留 ...

    // ===================== Phase 32 新增 =====================

    /** D-05 + IMPORT-02: blob 下载模板（替代旧 getTemplateUrl） */
    export async function downloadTemplate(importType: EligibilityImportType): Promise<void> {
      const response = await api.get<Blob>(
        `/eligibility-import/templates/${importType}?format=xlsx`,
        { responseType: 'blob' },
      );
      const url = URL.createObjectURL(response.data);
      const link = document.createElement('a');
      link.href = url;
      link.download = `${importType}_template.xlsx`;
      document.body.appendChild(link);
      link.click();
      link.remove();
      URL.revokeObjectURL(url);
    }

    /** D-06 + IMPORT-07: 上传 + preview，返回 PreviewResponse */
    export async function uploadAndPreview(
      importType: EligibilityImportType,
      file: File,
      onUploadProgress?: (percent: number) => void,
    ): Promise<PreviewResponse> {
      const formData = new FormData();
      formData.append('file', file);
      const response = await api.post<PreviewResponse>(
        `/eligibility-import/excel/preview?import_type=${importType}`,
        formData,
        {
          headers: { 'Content-Type': 'multipart/form-data' },
          timeout: 120000,
          onUploadProgress: (e) => {
            if (onUploadProgress && e.total) {
              onUploadProgress(Math.round((e.loaded / e.total) * 100));
            }
          },
        },
      );
      return response.data;
    }

    /** D-06 + IMPORT-05: 确认导入；replace 模式必须 confirmReplace=true */
    export async function confirmImport(
      jobId: string,
      overwriteMode: OverwriteMode,
      confirmReplace: boolean = false,
    ): Promise<ConfirmResponse> {
      const body: ConfirmRequest = { overwrite_mode: overwriteMode, confirm_replace: confirmReplace };
      const response = await api.post<ConfirmResponse>(
        `/eligibility-import/excel/${jobId}/confirm`,
        body,
        { timeout: 120000 },
      );
      return response.data;
    }

    /** D-06: 取消 previewing 状态 job + 删暂存文件 */
    export async function cancelImport(jobId: string): Promise<void> {
      await api.post(`/eligibility-import/excel/${jobId}/cancel`);
    }

    /** D-18: 进入 Tab 时查询活跃 job，前端据此禁用「选择文件」按钮 */
    export async function getActiveImportJob(
      importType: EligibilityImportType,
    ): Promise<ActiveJobResponse> {
      const response = await api.get<ActiveJobResponse>(
        `/eligibility-import/excel/active?import_type=${importType}`,
      );
      return response.data;
    }

    /**
     * @deprecated Phase 32 起请改用 downloadTemplate(importType): Promise<void>
     * 旧实现返回 URL 字符串，被 <a href target=_blank> 消费会导致 Safari 内嵌渲染乱码（Pitfall 5）
     */
    export function getTemplateUrl(importType: EligibilityImportType): string {
      const baseUrl = import.meta.env.VITE_API_BASE_URL ?? 'http://127.0.0.1:8011/api/v1';
      return `${baseUrl}/eligibility-import/templates/${importType}?format=xlsx`;
    }
    ```

    **关键：保留 getTemplateUrl** 标 deprecated（避免 Plan 06 改造前的过渡期 build 错误）；Plan 06 完成后可在未来 phase 移除。
  </action>
  <verify>
    <automated>
      cd frontend &amp;&amp; npm run lint
    </automated>
  </verify>
  <acceptance_criteria>
    - grep "interface PreviewResponse" frontend/src/types/api.ts 命中
    - grep "interface ConfirmRequest" frontend/src/types/api.ts 命中
    - grep "interface ActiveJobResponse" frontend/src/types/api.ts 命中
    - grep "type ImportJobStatus" frontend/src/types/api.ts 命中且包含 'previewing' 与 'cancelled'
    - grep "export async function uploadAndPreview" frontend/src/services/eligibilityImportService.ts 命中
    - grep "export async function confirmImport" frontend/src/services/eligibilityImportService.ts 命中
    - grep "export async function cancelImport" frontend/src/services/eligibilityImportService.ts 命中
    - grep "export async function getActiveImportJob" frontend/src/services/eligibilityImportService.ts 命中
    - grep "export async function downloadTemplate" frontend/src/services/eligibilityImportService.ts 命中
    - grep "responseType: 'blob'" frontend/src/services/eligibilityImportService.ts 命中（D-05 blob 下载）
    - grep "@deprecated" frontend/src/services/eligibilityImportService.ts 至少 2 处命中（uploadEligibilityExcel + getTemplateUrl）
    - 命令 `cd frontend && npm run lint` 退出码 0（tsc --noEmit 通过）
  </acceptance_criteria>
  <done>
    types/api.ts + eligibilityImportService.ts 扩展完毕；TypeScript strict 模式编译通过；旧函数保留并标 deprecated 防止 Plan 06 改造前 build 中断。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 创建 5 个独立 UI 子组件 + ImportPreviewPanel 整合壳</name>
  <files>
    frontend/src/components/eligibility-import/PreviewCountersStrip.tsx,
    frontend/src/components/eligibility-import/PreviewDiffTable.tsx,
    frontend/src/components/eligibility-import/OverwriteModeRadio.tsx,
    frontend/src/components/eligibility-import/ReplaceModeConfirmModal.tsx,
    frontend/src/components/eligibility-import/ImportActiveJobBanner.tsx,
    frontend/src/components/eligibility-import/ImportPreviewPanel.tsx
  </files>
  <read_first>
    - .planning/phases/32-eligibility-import-completion/32-UI-SPEC.md 全文（视觉契约 — 颜色 / 字号 / 文案 / a11y / 状态机）
    - frontend/src/components/feishu-sync-logs/CountersBadgeCluster.tsx（0 值弱化模式 / aria-label 生成 — PreviewCountersStrip 借鉴）
    - frontend/src/components/feishu-sync-logs/SyncLogDetailDrawer.tsx（focus trap / ESC 关闭 / role=dialog / aria-labelledby — ReplaceModeConfirmModal 借鉴）
    - frontend/src/components/feishu-sync-logs/StatusBadge.tsx（状态徽章配色 — ImportActiveJobBanner 可复用色族）
    - frontend/src/index.css 全文（确认 .surface / .table-lite / .action-* / .chip-button / .metric-* 等 class 已定义）
    - frontend/src/types/api.ts（Task 1 加好的 PreviewResponse 等类型）
  </read_first>
  <action>
    1) 创建 `frontend/src/components/eligibility-import/PreviewCountersStrip.tsx`（D-08 4 色计数卡片，UI-SPEC §「Color > Preview 4 色计数卡片专属色」）：

    ```typescript
    import type { PreviewCounters } from '../../types/api';

    interface PreviewCountersStripProps {
      counters: PreviewCounters;
    }

    interface CounterCellConfig {
      key: keyof PreviewCounters;
      label: string;
      mainTextVar: string;
      bgVar: string;
      borderVar: string;
      subtitleWhenPositive?: string;
    }

    const CELLS: CounterCellConfig[] = [
      { key: 'insert', label: '新增', mainTextVar: '--color-success',
        bgVar: '--color-success-bg', borderVar: '--color-success-border' },
      { key: 'update', label: '更新', mainTextVar: '--color-info',
        bgVar: '--color-info-bg', borderVar: '--color-primary-border' },
      { key: 'no_change', label: '无变化', mainTextVar: '--color-steel',
        bgVar: '--color-bg-subtle', borderVar: '--color-border' },
      { key: 'conflict', label: '冲突', mainTextVar: '--color-danger',
        bgVar: '--color-danger-bg', borderVar: '--color-danger-border',
        subtitleWhenPositive: '需先修正' },
    ];

    export function PreviewCountersStrip({ counters }: PreviewCountersStripProps) {
      return (
        <div
          role="group"
          aria-label="导入变更概览"
          style={{
            display: 'grid',
            gap: 16,
            gridTemplateColumns: 'repeat(4, 1fr)',
          }}
        >
          {CELLS.map((cell) => {
            const value = counters[cell.key];
            const zeroState = value === 0;
            const cellBg = zeroState ? 'var(--color-bg-subtle)' : `var(${cell.bgVar})`;
            const cellBorder = zeroState ? 'var(--color-border)' : `var(${cell.borderVar})`;
            const valueColor = zeroState ? 'var(--color-placeholder)' : `var(${cell.mainTextVar})`;
            return (
              <div
                key={cell.key}
                role="status"
                aria-label={zeroState ? `${cell.label} 类变更为 0 条` : `${cell.label} ${value} 条`}
                style={{
                  background: cellBg,
                  border: `1px solid ${cellBorder}`,
                  borderRadius: 8,
                  padding: 16,
                  display: 'flex',
                  flexDirection: 'column',
                  gap: 4,
                }}
              >
                <span className="eyebrow" style={{ color: 'var(--color-steel)' }}>
                  {cell.label}
                </span>
                <span
                  className="metric-value"
                  style={{ color: valueColor, fontSize: 26, fontWeight: 600, lineHeight: 1.1 }}
                >
                  {value}
                </span>
                {!zeroState && cell.subtitleWhenPositive && (
                  <span style={{ fontSize: 12, fontWeight: 600, color: `var(${cell.mainTextVar})` }}>
                    {cell.subtitleWhenPositive}
                  </span>
                )}
              </div>
            );
          })}
        </div>
      );
    }
    ```

    2) 创建 `frontend/src/components/eligibility-import/PreviewDiffTable.tsx`（D-08 + D-10 行级 + 字段级 diff，分页 50/页，no_change 折叠，conflict 红底高亮）：

    ```typescript
    import { useMemo, useState } from 'react';
    import type { PreviewRow, PreviewRowAction } from '../../types/api';

    interface PreviewDiffTableProps {
      rows: PreviewRow[];
      rowsTruncated?: boolean;
      truncatedCount?: number;
    }

    const ACTION_LABELS: Record<PreviewRowAction, string> = {
      insert: '新增',
      update: '更新',
      no_change: '无变化',
      conflict: '冲突',
    };

    const ACTION_INDICATOR_VAR: Record<PreviewRowAction, string> = {
      insert: '--color-success',
      update: '--color-info',
      no_change: '--color-steel',
      conflict: '--color-danger',
    };

    const PAGE_SIZE = 50;

    export function PreviewDiffTable({ rows, rowsTruncated, truncatedCount }: PreviewDiffTableProps) {
      const [page, setPage] = useState(1);
      const [noChangeCollapsed, setNoChangeCollapsed] = useState(true);

      const visibleRows = useMemo(() => {
        return noChangeCollapsed ? rows.filter((r) => r.action !== 'no_change') : rows;
      }, [rows, noChangeCollapsed]);

      const noChangeCount = useMemo(
        () => rows.filter((r) => r.action === 'no_change').length,
        [rows],
      );

      const totalPages = Math.max(1, Math.ceil(visibleRows.length / PAGE_SIZE));
      const safePage = Math.min(page, totalPages);
      const pagedRows = visibleRows.slice((safePage - 1) * PAGE_SIZE, safePage * PAGE_SIZE);

      return (
        <div style={{ display: 'flex', flexDirection: 'column', gap: 12 }}>
          {noChangeCount > 0 && (
            <button
              type="button"
              className="chip-button"
              onClick={() => { setNoChangeCollapsed((v) => !v); setPage(1); }}
              aria-expanded={!noChangeCollapsed}
            >
              {noChangeCollapsed ? `显示未变化 ${noChangeCount} 行` : `收起未变化 ${noChangeCount} 行`}
            </button>
          )}

          <div className="table-shell">
            <table className="table-lite" style={{ width: '100%' }}>
              <thead>
                <tr>
                  <th scope="col" style={{ width: 64 }}>行号</th>
                  <th scope="col" style={{ width: 120 }}>员工工号</th>
                  <th scope="col" style={{ width: 80 }}>动作</th>
                  <th scope="col">字段变更</th>
                </tr>
              </thead>
              <tbody>
                {pagedRows.map((row) => {
                  const isConflict = row.action === 'conflict';
                  const isNoChange = row.action === 'no_change';
                  const rowBg = isConflict
                    ? 'var(--color-danger-bg)'
                    : isNoChange ? 'var(--color-bg-subtle)' : 'transparent';
                  const indicator = ACTION_INDICATOR_VAR[row.action];
                  return (
                    <tr key={`${row.row_number}-${row.employee_no}`}
                        style={{ background: rowBg, borderLeft: `3px solid var(${indicator})` }}>
                      <td>{row.row_number}</td>
                      <td>{row.employee_no}</td>
                      <td>{ACTION_LABELS[row.action]}</td>
                      <td>
                        {isConflict ? (
                          <span style={{ color: 'var(--color-danger)' }}>
                            {row.conflict_reason}
                          </span>
                        ) : isNoChange ? (
                          <span style={{ color: 'var(--color-steel)' }}>—</span>
                        ) : (
                          <FieldDiffsCell row={row} />
                        )}
                      </td>
                    </tr>
                  );
                })}
              </tbody>
            </table>
          </div>

          {totalPages > 1 && (
            <div style={{ display: 'flex', alignItems: 'center', gap: 8 }}>
              <button type="button" className="chip-button"
                      onClick={() => setPage((p) => Math.max(1, p - 1))}
                      disabled={safePage === 1}>
                上一页
              </button>
              <span style={{ fontSize: 13, color: 'var(--color-steel)' }}>
                第 {safePage} 页 共 {totalPages} 页 · 每页 {PAGE_SIZE} 行
              </span>
              <button type="button" className="chip-button"
                      onClick={() => setPage((p) => Math.min(totalPages, p + 1))}
                      disabled={safePage === totalPages}>
                下一页
              </button>
            </div>
          )}

          {rowsTruncated && truncatedCount && truncatedCount > 0 && (
            <p style={{ fontSize: 13, color: 'var(--color-steel)', margin: 0 }}>
              已省略 {truncatedCount} 行 no-change 展示
            </p>
          )}
        </div>
      );
    }

    function FieldDiffsCell({ row }: { row: PreviewRow }) {
      const entries = Object.entries(row.fields);
      if (entries.length === 0) {
        return <span style={{ color: 'var(--color-steel)' }}>—</span>;
      }
      return (
        <div style={{ display: 'flex', flexDirection: 'column', gap: 4 }}>
          {entries.map(([fieldName, diff]) => {
            const oldDisplay = diff.old === null || diff.old === undefined || diff.old === ''
              ? '(空)' : String(diff.old);
            const newDisplay = diff.new === null || diff.new === undefined || diff.new === ''
              ? '(空)' : String(diff.new);
            return (
              <div key={fieldName} style={{ fontSize: 14 }}>
                <span style={{ color: 'var(--color-steel)', fontWeight: 600 }}>{fieldName}:</span>{' '}
                <span style={{ color: 'var(--color-steel)', textDecoration: 'line-through' }}>
                  {oldDisplay}
                </span>
                <span style={{ color: 'var(--color-steel)', margin: '0 6px' }}>→</span>
                <span style={{ color: 'var(--color-ink)', fontWeight: 600 }}>{newDisplay}</span>
              </div>
            );
          })}
        </div>
      );
    }
    ```

    3) 创建 `frontend/src/components/eligibility-import/OverwriteModeRadio.tsx`（D-11 Radio + replace inline 警告，UI-SPEC §「Copywriting > 覆盖模式选择区」）：

    ```typescript
    import type { OverwriteMode } from '../../types/api';

    interface OverwriteModeRadioProps {
      value: OverwriteMode;
      onChange: (value: OverwriteMode) => void;
      disabled?: boolean;
    }

    export function OverwriteModeRadio({ value, onChange, disabled }: OverwriteModeRadioProps) {
      return (
        <fieldset
          style={{
            border: 'none', padding: 0, margin: 0,
            display: 'flex', flexDirection: 'column', gap: 8,
          }}
        >
          <legend className="section-title" style={{ marginBottom: 4 }}>
            覆盖模式
          </legend>

          <label style={{ display: 'flex', alignItems: 'flex-start', gap: 8, cursor: disabled ? 'not-allowed' : 'pointer' }}>
            <input
              type="radio"
              name="overwrite_mode"
              value="merge"
              checked={value === 'merge'}
              onChange={() => onChange('merge')}
              disabled={disabled}
              style={{ marginTop: 4 }}
            />
            <div>
              <div style={{ fontSize: 14, color: 'var(--color-ink)', fontWeight: 500 }}>
                合并模式（空值保留旧值，推荐）
              </div>
              <div style={{ fontSize: 12, color: 'var(--color-steel)' }}>
                仅更新 Excel 中有值的字段；Excel 里为空的单元格不会改动已有数据。适合常规维护。
              </div>
            </div>
          </label>

          <label style={{ display: 'flex', alignItems: 'flex-start', gap: 8, cursor: disabled ? 'not-allowed' : 'pointer' }}>
            <input
              type="radio"
              name="overwrite_mode"
              value="replace"
              checked={value === 'replace'}
              onChange={() => onChange('replace')}
              disabled={disabled}
              style={{ marginTop: 4 }}
            />
            <div>
              <div style={{ fontSize: 14, color: 'var(--color-ink)', fontWeight: 500 }}>
                替换模式（空值清空字段）
              </div>
              <div style={{ fontSize: 12, color: 'var(--color-steel)' }}>
                Excel 中为空的可选字段会被清空为 NULL；必填字段为空则该行失败。适合全量替换场景，<strong>操作不可自动回滚</strong>。
              </div>
            </div>
          </label>

          {value === 'replace' && (
            <div
              role="alert"
              className="animate-fade-soft"
              style={{
                background: 'var(--color-danger-bg)',
                borderLeft: '3px solid var(--color-danger)',
                color: 'var(--color-danger)',
                padding: '8px 12px',
                fontSize: 14,
                fontWeight: 500,
                borderRadius: 4,
              }}
            >
              ⚠ 替换模式会清空你未填的可选字段，这是破坏性操作。点击「确认导入」时需要再次确认。
            </div>
          )}
        </fieldset>
      );
    }
    ```

    4) 创建 `frontend/src/components/eligibility-import/ReplaceModeConfirmModal.tsx`（D-11 二次确认 + focus trap + ESC + 强制 checkbox）：

    ```typescript
    import { useCallback, useEffect, useRef, useState } from 'react';

    interface ReplaceModeConfirmModalProps {
      open: boolean;
      totalRows: number;
      onClose: () => void;
      onConfirm: () => void;
    }

    export function ReplaceModeConfirmModal({
      open, totalRows, onClose, onConfirm,
    }: ReplaceModeConfirmModalProps) {
      const [acknowledged, setAcknowledged] = useState(false);
      const checkboxRef = useRef<HTMLInputElement>(null);
      const modalRef = useRef<HTMLDivElement>(null);

      // 每次 open 重置 checkbox
      useEffect(() => {
        if (open) {
          setAcknowledged(false);
          // focus checkbox（modal 首个可交互元素）
          setTimeout(() => checkboxRef.current?.focus(), 0);
        }
      }, [open]);

      // ESC 关闭
      useEffect(() => {
        if (!open) return;
        const handler = (e: KeyboardEvent) => {
          if (e.key === 'Escape') {
            e.preventDefault();
            onClose();
          }
        };
        document.addEventListener('keydown', handler);
        return () => document.removeEventListener('keydown', handler);
      }, [open, onClose]);

      // focus trap
      const handleKeyDown = useCallback((e: React.KeyboardEvent<HTMLDivElement>) => {
        if (e.key !== 'Tab') return;
        const focusables = modalRef.current?.querySelectorAll<HTMLElement>(
          'button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])',
        );
        if (!focusables || focusables.length === 0) return;
        const first = focusables[0];
        const last = focusables[focusables.length - 1];
        if (e.shiftKey && document.activeElement === first) {
          e.preventDefault();
          last.focus();
        } else if (!e.shiftKey && document.activeElement === last) {
          e.preventDefault();
          first.focus();
        }
      }, []);

      if (!open) return null;

      return (
        <div
          style={{
            position: 'fixed', inset: 0,
            background: 'rgba(0,0,0,0.4)',
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            zIndex: 1000,
          }}
          onClick={onClose}
        >
          <div
            ref={modalRef}
            role="dialog"
            aria-modal="true"
            aria-labelledby="replace-modal-title"
            aria-describedby="replace-modal-body"
            className="surface animate-fade-up"
            style={{
              width: 'min(480px, 90vw)',
              maxHeight: '70vh',
              overflowY: 'auto',
              padding: 24,
              display: 'flex', flexDirection: 'column', gap: 16,
            }}
            onClick={(e) => e.stopPropagation()}
            onKeyDown={handleKeyDown}
          >
            <h3 id="replace-modal-title" className="section-title">
              确认以替换模式导入 {totalRows} 行？
            </h3>
            <div id="replace-modal-body" style={{ fontSize: 14, lineHeight: 1.6, color: 'var(--color-ink)' }}>
              <p style={{ margin: '0 0 12px 0' }}>
                <strong style={{ color: 'var(--color-danger)' }}>替换模式</strong> 会将 Excel 中为空的<strong>可选字段清空</strong>（设为 NULL），必填字段为空的行将失败。
              </p>
              <p style={{ margin: 0 }}>
                本操作会直接写入数据库，<strong style={{ color: 'var(--color-danger)' }}>已入库数据无法自动恢复</strong>；请确认 Excel 中的空值均为你的预期。
              </p>
            </div>

            <label style={{ display: 'flex', alignItems: 'center', gap: 8, fontSize: 14 }}>
              <input
                ref={checkboxRef}
                type="checkbox"
                checked={acknowledged}
                onChange={(e) => setAcknowledged(e.target.checked)}
              />
              我已理解并确认以替换模式导入，愿意承担空值清空的后果
            </label>

            <div style={{ display: 'flex', gap: 8, justifyContent: 'flex-end', marginTop: 8 }}>
              <button type="button" className="action-secondary" onClick={onClose}>
                返回
              </button>
              <button
                type="button"
                className="action-danger"
                disabled={!acknowledged}
                onClick={onConfirm}
                title={!acknowledged ? '请先勾选上方「我已理解并确认」复选框' : undefined}
              >
                继续（替换模式）
              </button>
            </div>
          </div>
        </div>
      );
    }
    ```

    5) 创建 `frontend/src/components/eligibility-import/ImportActiveJobBanner.tsx`：

    ```typescript
    import type { ActiveJobResponse } from '../../types/api';

    interface ImportActiveJobBannerProps {
      activeJob: ActiveJobResponse;
    }

    const STATUS_LABELS: Record<NonNullable<ActiveJobResponse['status']>, string> = {
      previewing: '预览待确认',
      processing: '落库中',
    };

    export function ImportActiveJobBanner({ activeJob }: ImportActiveJobBannerProps) {
      if (!activeJob.active || !activeJob.status) return null;
      const statusLabel = STATUS_LABELS[activeJob.status];
      const startedAt = activeJob.created_at ? new Date(activeJob.created_at).toLocaleString('zh-CN') : '';
      return (
        <div
          role="status"
          style={{
            background: 'var(--color-warning-bg)',
            color: 'var(--color-warning)',
            border: '1px solid var(--color-warning)',
            borderRadius: 8,
            padding: '8px 12px',
            fontSize: 14,
            marginBottom: 16,
          }}
        >
          <strong>该类型导入正在进行中</strong>（{statusLabel}{startedAt && `，开始于 ${startedAt}`}）。请等待完成，或在「同步日志」查看进度。
        </div>
      );
    }
    ```

    6) 创建 `frontend/src/components/eligibility-import/ImportPreviewPanel.tsx`（整合壳）：

    ```typescript
    import { useState } from 'react';
    import type { PreviewResponse, OverwriteMode } from '../../types/api';
    import { PreviewCountersStrip } from './PreviewCountersStrip';
    import { PreviewDiffTable } from './PreviewDiffTable';
    import { OverwriteModeRadio } from './OverwriteModeRadio';
    import { ReplaceModeConfirmModal } from './ReplaceModeConfirmModal';

    interface ImportPreviewPanelProps {
      label: string;
      preview: PreviewResponse;
      onConfirm: (overwriteMode: OverwriteMode) => Promise<void> | void;
      onCancel: () => void;
      isConfirming?: boolean;
    }

    export function ImportPreviewPanel({
      label, preview, onConfirm, onCancel, isConfirming,
    }: ImportPreviewPanelProps) {
      const [overwriteMode, setOverwriteMode] = useState<OverwriteMode>('merge');
      const [modalOpen, setModalOpen] = useState(false);

      const hasConflicts = preview.counters.conflict > 0;
      const conflictTooltip = hasConflicts
        ? `存在 ${preview.counters.conflict} 条冲突，请先修正 Excel 后重新上传`
        : undefined;

      const handlePrimaryClick = () => {
        if (hasConflicts) return;
        if (overwriteMode === 'replace') {
          setModalOpen(true);
          return;
        }
        void onConfirm('merge');
      };

      const handleModalConfirm = () => {
        setModalOpen(false);
        void onConfirm('replace');
      };

      const ctaLabel = overwriteMode === 'replace' ? '确认导入（替换模式）' : '确认导入';
      const expiresAt = new Date(preview.preview_expires_at).toLocaleString('zh-CN');

      return (
        <section
          aria-labelledby="preview-panel-title"
          className="surface animate-fade-up"
          style={{
            padding: '16px 20px 24px',
            display: 'flex',
            flexDirection: 'column',
            gap: 24,
          }}
        >
          <div>
            <p className="eyebrow">导入预览 · 确认前可检查</p>
            <h3 id="preview-panel-title" className="section-title">
              预览{label}导入结果（共 {preview.total_rows} 行）
            </h3>
            <p style={{ fontSize: 13, color: 'var(--color-steel)', margin: '4px 0 0' }}>
              已上传：{preview.file_name} · 本次预览将在 {expiresAt} 前有效
            </p>
            <p style={{ fontSize: 14, color: 'var(--color-ink)', margin: '8px 0 0' }}>
              下方展示本次导入会产生的变更。确认无误后点击「确认导入」才会写入数据库；若有冲突，请修正 Excel 后重新上传。
            </p>
          </div>

          <PreviewCountersStrip counters={preview.counters} />

          <PreviewDiffTable
            rows={preview.rows}
            rowsTruncated={preview.rows_truncated}
            truncatedCount={preview.truncated_count}
          />

          <OverwriteModeRadio
            value={overwriteMode}
            onChange={setOverwriteMode}
            disabled={isConfirming}
          />

          <div style={{ display: 'flex', gap: 8, justifyContent: 'flex-end' }}>
            <button
              type="button"
              className="action-secondary"
              onClick={onCancel}
              disabled={isConfirming}
            >
              取消本次预览
            </button>
            <button
              type="button"
              className="action-primary"
              disabled={hasConflicts || isConfirming}
              onClick={handlePrimaryClick}
              title={conflictTooltip}
              aria-describedby={hasConflicts ? 'conflict-disabled-reason' : undefined}
            >
              {ctaLabel}
            </button>
            {hasConflicts && (
              <span id="conflict-disabled-reason" style={{ position: 'absolute', left: -9999 }}>
                {conflictTooltip}
              </span>
            )}
          </div>

          <ReplaceModeConfirmModal
            open={modalOpen}
            totalRows={preview.total_rows}
            onClose={() => setModalOpen(false)}
            onConfirm={handleModalConfirm}
          />
        </section>
      );
    }
    ```
  </action>
  <verify>
    <automated>
      cd frontend &amp;&amp; npm run lint &amp;&amp; npm run build
    </automated>
  </verify>
  <acceptance_criteria>
    - 文件 frontend/src/components/eligibility-import/PreviewCountersStrip.tsx 存在且 grep "export function PreviewCountersStrip" 命中
    - 文件 frontend/src/components/eligibility-import/PreviewDiffTable.tsx 存在且 grep "PAGE_SIZE = 50" 命中
    - 文件 frontend/src/components/eligibility-import/OverwriteModeRadio.tsx 存在且 grep "替换模式会清空你未填的可选字段" 命中（UI-SPEC 文案严格对齐）
    - 文件 frontend/src/components/eligibility-import/ReplaceModeConfirmModal.tsx 存在且 grep "role=\"dialog\"" 与 "aria-modal=\"true\"" 与 "Escape" 都命中（focus trap + ESC + a11y）
    - 文件 frontend/src/components/eligibility-import/ImportActiveJobBanner.tsx 存在且 grep "STATUS_LABELS" 命中
    - 文件 frontend/src/components/eligibility-import/ImportPreviewPanel.tsx 存在且 grep "ReplaceModeConfirmModal" 命中（5 个子组件都被整合）
    - grep "我已理解并确认" frontend/src/components/eligibility-import/ReplaceModeConfirmModal.tsx 命中（强制 checkbox 文案 D-11）
    - grep "新增" frontend/src/components/eligibility-import/PreviewCountersStrip.tsx 命中
    - 命令 `cd frontend && npm run lint` 退出码 0
    - 命令 `cd frontend && npm run build` 退出码 0（生产构建通过 → 6 个新组件无 TypeScript 错误）
  </acceptance_criteria>
  <done>
    5 个独立子组件 + 1 个 ImportPreviewPanel 整合壳全部就绪；npm run lint + build 通过；UI-SPEC 文案严格对齐；a11y（focus trap + role=dialog + ESC）落地；Plan 06 ExcelImportPanel 状态机改造可直接 import 这 6 个组件。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 前端 service → backend API | JWT 已由 axios 拦截器（services/api.ts）自动加 |
| 用户输入 → 前端组件 | UI 控件输入（Radio 选择 / Modal checkbox）— 受控组件，无 XSS 风险 |
| Backend response → 前端渲染 | PreviewRow.fields 含用户上传的字段值 — 渲染时 React 自动 escape |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-15 | T (Tampering) | replace 模式被绕过二次确认 | mitigate | Task 2 ReplaceModeConfirmModal 强制 checkbox 勾选 + acknowledged state；Task 1 confirmImport 函数把 confirm_replace 透传到后端，**后端 Plan 04 强制校验**（即使前端绕过仍 422 拦截） |
| T-32-17 | I (Information disclosure) | Diff 表格泄露其他员工数据给当前 HR | accept | UI-SPEC 明确 HR 端导入 Preview 看完整 diff 是允许的（员工端 Phase 35 才有合规约束）；当前 HR 已通过 require_roles 鉴权 |
| T-32-18 | T (Tampering) | XSS via 字段值（FieldDiff.old/new 含恶意 HTML） | accept | React 默认 JSX text node escape；Task 2 PreviewDiffTable 用 `{String(diff.old)}` 模式不用 dangerouslySetInnerHTML |
| T-32-19 | E (Elevation of privilege) | 旧 getTemplateUrl 被前端代码继续使用（无 JWT 头） | mitigate | Task 1 标 @deprecated；Plan 06 改造 ExcelImportPanel 改用 downloadTemplate；未来 phase 可移除 getTemplateUrl |
</threat_model>

<verification>
- 类型契约稳定：types/api.ts 与 backend Pydantic schemas 字段名/类型一致
- 6 个新组件文件全部存在，npm run build 通过
- UI-SPEC 文案 100% 对齐（grep 抽样验证）
- focus trap / ESC / role=dialog 按 UI-SPEC § Accessibility 落地
</verification>

<success_criteria>
- IMPORT-02 前端：downloadTemplate 函数就绪（Plan 06 接通到 ExcelImportPanel）
- IMPORT-05 前端：OverwriteModeRadio + ReplaceModeConfirmModal 就绪
- IMPORT-06 前端：getActiveImportJob + ImportActiveJobBanner 就绪
- IMPORT-07 前端：uploadAndPreview + confirmImport + cancelImport + PreviewCountersStrip + PreviewDiffTable + ImportPreviewPanel 就绪
- 类型 union ImportJobStatus 含 'previewing' / 'cancelled'（解决 Pitfall 2 stricter typing）
</success_criteria>

<output>
完成后创建 `.planning/phases/32-eligibility-import-completion/32-05-SUMMARY.md`，重点记录：
- 5 个新 service 函数签名 + import 入口
- 6 个新组件 props 签名 + 入口
- ImportPreviewPanel 受控状态机说明（Plan 06 直接 import 拿即用）
- UI-SPEC 文案对齐 checklist
</output>
