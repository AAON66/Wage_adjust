---
phase: 32-eligibility-import-completion
plan: 04
type: execute
wave: 3
depends_on:
  - 32-02
  - 32-03
files_modified:
  - backend/app/api/v1/eligibility_import.py
  - backend/tests/test_api/__init__.py
  - backend/tests/test_api/test_eligibility_import_preview_api.py
  - backend/tests/test_api/test_eligibility_import_confirm_api.py
  - backend/tests/test_api/test_eligibility_import_concurrency.py
  - backend/tests/test_api/test_eligibility_import_template_api.py
autonomous: true
requirements:
  - IMPORT-01
  - IMPORT-02
  - IMPORT-05
  - IMPORT-06
  - IMPORT-07

must_haves:
  truths:
    - "POST /api/v1/eligibility-import/excel/preview 接收 multipart/form-data + import_type 参数，返回 PreviewResponse；并发同 import_type 第二次返回 409"
    - "POST /api/v1/eligibility-import/excel/{job_id}/confirm 接收 ConfirmRequest body，校验 replace 模式 confirm_replace=True，返回 ConfirmResponse；并发同 import_type 第二次返回 409；同 job_id 重复 confirm 返回 409"
    - "POST /api/v1/eligibility-import/excel/{job_id}/cancel 取消 previewing 状态的 job，返回 204"
    - "GET /api/v1/eligibility-import/excel/active?import_type=X 返回 ActiveJobResponse（D-18）"
    - "GET /api/v1/eligibility-import/templates/{import_type} 对 4 类资格类型返回非空 .xlsx，openpyxl 可读回（IMPORT-02）"
    - "上传文件类型校验：拒绝 .exe/.html/.svg；上传文件大小 <= 10MB（防 DoS）"
    - "所有写端点强制 require_roles('admin', 'hrbp')；replace 模式仅 admin/hrbp 可用"
  artifacts:
    - path: backend/app/api/v1/eligibility_import.py
      provides: "新增 4 个端点：preview / confirm / cancel / active；模板下载端点 4 类全覆盖；旧 POST /excel 标 deprecated 但保留"
      contains: "@router.post('/excel/preview')"
    - path: backend/tests/test_api/test_eligibility_import_preview_api.py
      provides: "preview 端点端到端：成功 / 409 锁 / 文件类型拒绝 / 文件过大拒绝 / 鉴权"
      contains: "def test_preview_endpoint_returns_preview_response"
    - path: backend/tests/test_api/test_eligibility_import_confirm_api.py
      provides: "confirm 端点：成功 / 双 confirm 409 / replace 二次确认校验 / hash 篡改 / AuditLog 写入"
      contains: "def test_confirm_endpoint_writes_audit_log"
    - path: backend/tests/test_api/test_eligibility_import_concurrency.py
      provides: "4 个并发场景：preview 409 / confirm 409 / 不同 import_type 可并行 / preview→confirm 序列锁"
      contains: "def test_preview_409_when_running"
    - path: backend/tests/test_api/test_eligibility_import_template_api.py
      provides: "4 类资格 import_type 模板下载 + Content-Disposition 校验 + openpyxl 可读回"
      contains: "def test_template_returns_non_empty_xlsx"
  key_links:
    - from: backend/app/api/v1/eligibility_import.py
      to: backend/app/services/import_service.py
      via: "router 端点是 service 方法的薄包装：build_preview / confirm_import / cancel_import / get_active_job / is_import_running / expire_stale_import_jobs"
      pattern: "service.build_preview"
    - from: backend/app/api/v1/eligibility_import.py
      to: backend/app/schemas/import_preview.py
      via: "返回类型 PreviewResponse / ConfirmResponse / ActiveJobResponse 由 schemas 提供"
      pattern: "PreviewResponse"
---

<objective>
建立 Phase 32 的 HTTP API 表面（IMPORT-01 / 02 / 05 / 06 / 07 端点完整暴露）。所有 service 层逻辑已在 Plan 02 / 03 完成，本 plan 只做 4 件事：

1. **新增 4 个端点** — `POST /excel/preview` / `POST /excel/{job_id}/confirm` / `POST /excel/{job_id}/cancel` / `GET /excel/active?import_type=X`
2. **加文件上传安全校验** — 类型白名单（仅 .xlsx / .xls）+ 大小上限（10MB） + Content-Type 校验
3. **保留旧 `POST /excel`** 标 deprecated（兼容性，决议 Open Question 4）
4. **验证模板下载端点 4 类资格 import_type 全覆盖**（IMPORT-02）— 现有端点已存在，只需补 hire_info / non_statutory_leave 的 service 端 build_template_xlsx 已在 Plan 02 加好，本 plan 加 e2e 测试

Purpose: 后端 API 完整闭环。Plan 05（前端 service 层）和 Plan 06（前端组件改造）依赖本 plan 的 API 契约。

Output:
- eligibility_import.py 加 4 个新端点 + 安全校验
- 4 个测试文件覆盖端到端 + 并发 + 鉴权 + 文件安全
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-eligibility-import-completion/32-02-SUMMARY.md
@.planning/phases/32-eligibility-import-completion/32-03-SUMMARY.md
@.planning/phases/32-eligibility-import-completion/32-CONTEXT.md
@.planning/phases/32-eligibility-import-completion/32-RESEARCH.md
@CLAUDE.md
@backend/app/api/v1/eligibility_import.py
@backend/app/api/v1/feishu.py
@backend/app/services/import_service.py
@backend/app/schemas/import_preview.py
@backend/app/schemas/eligibility_import.py
@backend/app/dependencies.py

<interfaces>
<!-- 现有 eligibility_import 路由 -->
backend/app/api/v1/eligibility_import.py:
- 已有：POST /excel（旧一步导入；本 plan 标 deprecated）
- 已有：POST /feishu/parse-url, /feishu/fields, /feishu/sync
- 已有：GET /templates/{import_type}（本 plan 验证 4 类全覆盖）

<!-- 新增端点 -->
| Method | Path | Service 调用 |
|--------|------|--------------|
| POST | /eligibility-import/excel/preview?import_type=X | service.expire_stale_import_jobs() + is_import_running(X) → 409 / build_preview |
| POST | /eligibility-import/excel/{job_id}/confirm | service.confirm_import(job_id, overwrite_mode, confirm_replace, actor_id, actor_role) |
| POST | /eligibility-import/excel/{job_id}/cancel | service.cancel_import(job_id) → 204 |
| GET | /eligibility-import/excel/active?import_type=X | service.get_active_job(X) → ActiveJobResponse |

<!-- ELIGIBILITY_IMPORT_TYPES 来源 -->
backend/app/schemas/eligibility_import.py:
ELIGIBILITY_IMPORT_TYPES = {'performance_grades', 'salary_adjustments', 'hire_info', 'non_statutory_leave'}

<!-- 文件大小上限 -->
推荐 10 MB（5000 行 × ~2KB/row 估算 + buffer）；写在 ImportService 类常量更易测试，但 API 层必须先校验 raw_bytes 长度（避免 _save_staged_file 浪费 IO）

<!-- 鉴权 -->
backend/app/dependencies.py:
- get_current_user — 从 JWT 提取
- require_roles('admin', 'hrbp') — 写端点
- get_db — DB session

<!-- Phase 31 已落地 409 模式参考 -->
backend/app/api/v1/feishu.py:208-238 — trigger_sync 的 is_sync_running → 409 detail 结构
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新增 preview / confirm / cancel / active 4 个端点 + 文件安全校验 + 旧端点 deprecated</name>
  <files>
    backend/app/api/v1/eligibility_import.py,
    backend/tests/test_api/__init__.py,
    backend/tests/test_api/test_eligibility_import_preview_api.py,
    backend/tests/test_api/test_eligibility_import_confirm_api.py
  </files>
  <read_first>
    - backend/app/api/v1/eligibility_import.py 全文（确认现有 router 与 endpoints，本 plan 追加不替换）
    - backend/app/api/v1/feishu.py:189-318（Phase 31 既定 409 / require_roles / get_current_user 模式 — 直接抄 trigger_sync 的 detail 结构）
    - backend/app/dependencies.py（确认 require_roles / get_current_user / get_db 的 import 路径与签名）
    - backend/app/services/import_service.py（确认 Plan 02 / 03 完成的方法签名：build_preview / confirm_import / cancel_import / get_active_job / is_import_running / expire_stale_import_jobs）
    - backend/app/schemas/import_preview.py（PreviewResponse / ConfirmRequest / ConfirmResponse / ActiveJobResponse）
    - backend/app/schemas/eligibility_import.py（ELIGIBILITY_IMPORT_TYPES 常量）
    - backend/tests/conftest.py（确认是否有 client / authenticated_client / hrbp_client fixture；如无，按 backend/tests/test_api/test_feishu.py 等已有 API 测试模板创建）
  </read_first>
  <behavior>
    - Test 1: POST /excel/preview?import_type=hire_info + valid xlsx → 200 + PreviewResponse JSON 结构
    - Test 2: POST /excel/preview 同 import_type 第二次 → 409 + detail.error='import_in_progress'
    - Test 3: POST /excel/preview 不同 import_type 不冲突（hire_info 活跃，performance_grades 仍可上传）
    - Test 4: POST /excel/preview 文件 .exe → 422 拒绝
    - Test 5: POST /excel/preview 文件 > 10MB → 413 / 422 拒绝
    - Test 6: POST /excel/preview 不带 JWT → 401；employee 角色 JWT → 403
    - Test 7: POST /excel/{job_id}/confirm with merge → 200 + AuditLog 写入
    - Test 8: POST /excel/{job_id}/confirm with replace + confirm_replace=False → 422
    - Test 9: POST /excel/{job_id}/confirm with replace + confirm_replace=True → 200
    - Test 10: POST /excel/{job_id}/confirm 同 job_id 第二次 → 409
    - Test 11: POST /excel/{job_id}/cancel previewing 状态 → 204 + 暂存文件删除
    - Test 12: GET /excel/active?import_type=hire_info 无活跃 → {active: false}
    - Test 13: GET /excel/active?import_type=hire_info 有 previewing → {active: true, status: 'previewing', job_id: ...}
  </behavior>
  <action>
    1) 修改 `backend/app/api/v1/eligibility_import.py` — 在文件末尾追加 4 个端点 + 安全校验 helper：

    ```python
    # ===== Phase 32 新增 =====
    from backend.app.schemas.import_preview import (
        PreviewResponse, ConfirmRequest, ConfirmResponse, ActiveJobResponse,
    )

    # IMPORT-02 + 安全校验
    _ALLOWED_XLSX_EXTENSIONS = {'.xlsx', '.xls'}
    _ALLOWED_XLSX_CONTENT_TYPES = {
        'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
        'application/vnd.ms-excel',
        'application/octet-stream',  # 部分浏览器在拖拽上传时不带正确 mime
    }
    _MAX_UPLOAD_SIZE_BYTES = 10 * 1024 * 1024  # 10 MB


    def _validate_upload_file(file: UploadFile) -> bytes:
        """文件类型 + 大小 + 内容校验（T-32-01/02/03 防护）。"""
        filename = (file.filename or '').lower()
        ext = '.' + filename.rsplit('.', 1)[-1] if '.' in filename else ''
        if ext not in _ALLOWED_XLSX_EXTENSIONS:
            raise HTTPException(
                status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
                detail=f'仅接受 .xlsx 或 .xls 文件（实际：{ext or "未知"}）',
            )
        # Content-Type 软校验（部分浏览器不准）
        ct = (file.content_type or '').lower()
        if ct and ct not in _ALLOWED_XLSX_CONTENT_TYPES:
            logger.warning('Unexpected content_type %s for filename %s', ct, filename)
        raw_bytes = file.file.read()
        if not raw_bytes:
            raise HTTPException(
                status_code=status.HTTP_400_BAD_REQUEST,
                detail='Uploaded file is empty.',
            )
        if len(raw_bytes) > _MAX_UPLOAD_SIZE_BYTES:
            raise HTTPException(
                status_code=status.HTTP_413_REQUEST_ENTITY_TOO_LARGE,
                detail=f'文件超过大小限制（{_MAX_UPLOAD_SIZE_BYTES // 1024 // 1024}MB）',
            )
        return raw_bytes


    @router.post('/excel/preview', response_model=PreviewResponse)
    def preview_excel_import(
        import_type: str,
        file: UploadFile = File(...),
        current_user=Depends(require_roles('admin', 'hrbp')),
        db: Session = Depends(get_db),
    ) -> PreviewResponse:
        """D-06 + D-07 + IMPORT-07: 上传 + 解析 + 暂存 + 返回 PreviewResponse。"""
        normalized_type = import_type.strip().lower()
        if normalized_type not in ELIGIBILITY_IMPORT_TYPES:
            raise HTTPException(
                status_code=status.HTTP_400_BAD_REQUEST,
                detail=f'Unsupported import type. Must be one of: {sorted(ELIGIBILITY_IMPORT_TYPES)}',
            )

        raw_bytes = _validate_upload_file(file)
        file_name = file.filename or f'{normalized_type}.xlsx'

        from backend.app.services.import_service import ImportService
        service = ImportService(db)

        # D-17: 先清理僵尸 job
        service.expire_stale_import_jobs()

        # D-16: per-import_type 锁
        if service.is_import_running(import_type=normalized_type):
            raise HTTPException(
                status_code=status.HTTP_409_CONFLICT,
                detail={
                    'error': 'import_in_progress',
                    'import_type': normalized_type,
                    'message': '该类型导入正在进行中，请等待当前任务完成后再试',
                },
            )

        try:
            return service.build_preview(
                import_type=normalized_type,
                file_name=file_name,
                raw_bytes=raw_bytes,
                actor_id=str(current_user.id),
            )
        except ValueError as exc:
            raise HTTPException(
                status_code=status.HTTP_400_BAD_REQUEST,
                detail=str(exc),
            ) from exc


    @router.post('/excel/{job_id}/confirm', response_model=ConfirmResponse)
    def confirm_excel_import(
        job_id: str,
        body: ConfirmRequest,
        current_user=Depends(require_roles('admin', 'hrbp')),
        db: Session = Depends(get_db),
    ) -> ConfirmResponse:
        """D-06 + D-13 + IMPORT-05/06/07: 确认导入。"""
        from backend.app.services.import_service import ImportService
        service = ImportService(db)

        # D-16: per-import_type 锁（confirm 阶段也校验：防止同 import_type 并发 confirm）
        # 先取 job 拿 import_type
        from backend.app.models.import_job import ImportJob
        from sqlalchemy import select
        job = db.execute(select(ImportJob).where(ImportJob.id == job_id)).scalar_one_or_none()
        if job is None:
            raise HTTPException(
                status_code=status.HTTP_404_NOT_FOUND,
                detail=f'ImportJob {job_id} 不存在',
            )

        # 同 import_type 是否有 processing job（previewing 是当前 job 自己，不算冲突）
        from sqlalchemy import select as _sel
        other_running = db.execute(_sel(ImportJob).where(
            ImportJob.import_type == job.import_type,
            ImportJob.status == 'processing',
            ImportJob.id != job_id,
        ).limit(1)).scalar_one_or_none()
        if other_running is not None:
            raise HTTPException(
                status_code=status.HTTP_409_CONFLICT,
                detail={
                    'error': 'import_in_progress',
                    'import_type': job.import_type,
                    'message': '该类型导入正在进行中，请等待当前任务完成后再试',
                },
            )

        try:
            return service.confirm_import(
                job_id=job_id,
                overwrite_mode=body.overwrite_mode,
                confirm_replace=body.confirm_replace,
                actor_id=str(current_user.id),
                actor_role=current_user.role,
            )
        except ValueError as exc:
            msg = str(exc)
            if '已确认' in msg or '已取消' in msg:
                raise HTTPException(status_code=status.HTTP_409_CONFLICT, detail=msg) from exc
            if '替换模式' in msg:
                raise HTTPException(status_code=status.HTTP_422_UNPROCESSABLE_ENTITY, detail=msg) from exc
            raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail=msg) from exc


    @router.post('/excel/{job_id}/cancel', status_code=status.HTTP_204_NO_CONTENT)
    def cancel_excel_import(
        job_id: str,
        current_user=Depends(require_roles('admin', 'hrbp')),
        db: Session = Depends(get_db),
    ):
        """HR 取消 previewing 状态的 job（幂等：其他状态忽略）。"""
        from backend.app.services.import_service import ImportService
        service = ImportService(db)
        try:
            service.cancel_import(job_id)
        except ValueError as exc:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail=str(exc)) from exc
        return None


    @router.get('/excel/active', response_model=ActiveJobResponse)
    def get_active_import(
        import_type: str,
        current_user=Depends(require_roles('admin', 'hrbp')),
        db: Session = Depends(get_db),
    ) -> ActiveJobResponse:
        """D-18: 进入 Tab 时查询活跃 job，前端据此禁用「选择文件」按钮。"""
        normalized_type = import_type.strip().lower()
        if normalized_type not in ELIGIBILITY_IMPORT_TYPES:
            raise HTTPException(
                status_code=status.HTTP_400_BAD_REQUEST,
                detail=f'Unsupported import type. Must be one of: {sorted(ELIGIBILITY_IMPORT_TYPES)}',
            )
        from backend.app.services.import_service import ImportService
        service = ImportService(db)
        # 顺手清理僵尸（HR 进入 Tab 即触发清理，无需等定时任务）
        service.expire_stale_import_jobs()
        active = service.get_active_job(normalized_type)
        if active is None:
            return ActiveJobResponse(active=False)
        return ActiveJobResponse(
            active=True,
            job_id=active.id,
            status=active.status,
            created_at=active.created_at,
            file_name=active.file_name,
        )
    ```

    2) 修改旧 `POST /excel`（line 33-60）— 加 deprecated 注释 + Warning header（保留兼容性，决议 Open Question 4）：

    ```python
    @router.post('/excel', status_code=status.HTTP_202_ACCEPTED, deprecated=True)
    def import_excel(
        import_type: str,
        file: UploadFile = File(...),
        current_user=Depends(require_roles('admin', 'hrbp')),
    ) -> TaskTriggerResponse:
        """**Deprecated (Phase 32):** 一步上传立即落库。

        请改用两阶段端点：
          1. POST /excel/preview?import_type=X → 拿到 PreviewResponse
          2. POST /excel/{job_id}/confirm → 确认落库
        """
        # 现有实现保持不变 ...
    ```

    3) 创建 `backend/tests/test_api/__init__.py`（空文件）。

    4) 创建测试 `backend/tests/test_api/test_eligibility_import_preview_api.py`：

    ```python
    from __future__ import annotations
    import io
    import pytest
    from fastapi.testclient import TestClient


    def _upload_file(xlsx_bytes: bytes, filename: str = 'test.xlsx') -> dict:
        return {'file': (filename, io.BytesIO(xlsx_bytes),
                         'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet')}


    def test_preview_endpoint_returns_preview_response(
        client_hrbp, db_session, employee_factory, xlsx_factory, tmp_uploads_dir,
    ):
        employee_factory(employee_no='E00001')
        data = xlsx_factory['hire_info'](rows=[['E00001', '2026-01-01', None]])
        resp = client_hrbp.post(
            '/api/v1/eligibility-import/excel/preview?import_type=hire_info',
            files=_upload_file(data),
        )
        assert resp.status_code == 200, resp.text
        body = resp.json()
        assert body['import_type'] == 'hire_info'
        assert 'job_id' in body
        assert 'counters' in body
        assert 'file_sha256' in body

    def test_preview_409_when_running(
        client_hrbp, db_session, import_job_factory, xlsx_factory, tmp_uploads_dir,
    ):
        # 制造活跃 job
        import_job_factory(import_type='hire_info', status='previewing')
        data = xlsx_factory['hire_info']()
        resp = client_hrbp.post(
            '/api/v1/eligibility-import/excel/preview?import_type=hire_info',
            files=_upload_file(data),
        )
        assert resp.status_code == 409
        body = resp.json()
        assert body['detail']['error'] == 'import_in_progress'
        assert body['detail']['import_type'] == 'hire_info'

    def test_preview_different_type_not_blocked(
        client_hrbp, db_session, import_job_factory, employee_factory, xlsx_factory, tmp_uploads_dir,
    ):
        import_job_factory(import_type='hire_info', status='previewing')
        employee_factory(employee_no='E00001')
        data = xlsx_factory['performance_grades'](rows=[['E00001', 2026, 'A']])
        resp = client_hrbp.post(
            '/api/v1/eligibility-import/excel/preview?import_type=performance_grades',
            files=_upload_file(data),
        )
        assert resp.status_code == 200

    def test_preview_rejects_exe_file(client_hrbp, tmp_uploads_dir):
        resp = client_hrbp.post(
            '/api/v1/eligibility-import/excel/preview?import_type=hire_info',
            files={'file': ('malware.exe', io.BytesIO(b'MZ\\x00fake'),
                            'application/x-msdownload')},
        )
        assert resp.status_code == 422

    def test_preview_rejects_oversized_file(client_hrbp, tmp_uploads_dir):
        big = b'X' * (11 * 1024 * 1024)  # 11 MB
        resp = client_hrbp.post(
            '/api/v1/eligibility-import/excel/preview?import_type=hire_info',
            files={'file': ('big.xlsx', io.BytesIO(big),
                            'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet')},
        )
        assert resp.status_code == 413

    def test_preview_requires_jwt(client_anon, xlsx_factory, tmp_uploads_dir):
        data = xlsx_factory['hire_info']()
        resp = client_anon.post(
            '/api/v1/eligibility-import/excel/preview?import_type=hire_info',
            files=_upload_file(data),
        )
        assert resp.status_code == 401

    def test_preview_employee_role_forbidden(client_employee, xlsx_factory, tmp_uploads_dir):
        data = xlsx_factory['hire_info']()
        resp = client_employee.post(
            '/api/v1/eligibility-import/excel/preview?import_type=hire_info',
            files=_upload_file(data),
        )
        assert resp.status_code == 403

    def test_preview_unknown_import_type(client_hrbp, xlsx_factory, tmp_uploads_dir):
        data = xlsx_factory['hire_info']()
        resp = client_hrbp.post(
            '/api/v1/eligibility-import/excel/preview?import_type=unknown_type',
            files=_upload_file(data),
        )
        assert resp.status_code == 400

    def test_active_endpoint_no_active(client_hrbp, db_session):
        resp = client_hrbp.get('/api/v1/eligibility-import/excel/active?import_type=hire_info')
        assert resp.status_code == 200
        assert resp.json() == {'active': False, 'job_id': None, 'status': None,
                                'created_at': None, 'file_name': None}

    def test_active_endpoint_with_previewing(client_hrbp, import_job_factory):
        job = import_job_factory(import_type='hire_info', status='previewing')
        resp = client_hrbp.get('/api/v1/eligibility-import/excel/active?import_type=hire_info')
        assert resp.status_code == 200
        body = resp.json()
        assert body['active'] is True
        assert body['job_id'] == job.id
        assert body['status'] == 'previewing'
    ```

    5) 创建测试 `backend/tests/test_api/test_eligibility_import_confirm_api.py`：

    ```python
    from __future__ import annotations
    import io
    import pytest


    def _do_preview(client_hrbp, employee_factory, xlsx_factory, import_type='hire_info'):
        emp = employee_factory(employee_no='E00001')
        data = xlsx_factory[import_type](rows=[['E00001', '2026-01-01', None]] if import_type == 'hire_info' else None)
        resp = client_hrbp.post(
            f'/api/v1/eligibility-import/excel/preview?import_type={import_type}',
            files={'file': ('t.xlsx', io.BytesIO(data),
                            'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet')},
        )
        assert resp.status_code == 200
        return resp.json()['job_id']


    def test_confirm_endpoint_writes_audit_log(
        client_hrbp, db_session, employee_factory, xlsx_factory, tmp_uploads_dir,
    ):
        from backend.app.models.audit_log import AuditLog
        from sqlalchemy import select
        job_id = _do_preview(client_hrbp, employee_factory, xlsx_factory)
        resp = client_hrbp.post(
            f'/api/v1/eligibility-import/excel/{job_id}/confirm',
            json={'overwrite_mode': 'merge', 'confirm_replace': False},
        )
        assert resp.status_code == 200, resp.text
        body = resp.json()
        assert body['job_id'] == job_id
        assert body['status'] in ('completed', 'partial', 'failed')
        # AuditLog 已写
        log = db_session.execute(select(AuditLog).where(
            AuditLog.action == 'import_confirmed', AuditLog.target_id == job_id,
        )).scalar_one()
        assert log.detail['overwrite_mode'] == 'merge'
        assert log.target_type == 'import_job'

    def test_confirm_replace_without_flag_returns_422(
        client_hrbp, db_session, employee_factory, xlsx_factory, tmp_uploads_dir,
    ):
        job_id = _do_preview(client_hrbp, employee_factory, xlsx_factory)
        resp = client_hrbp.post(
            f'/api/v1/eligibility-import/excel/{job_id}/confirm',
            json={'overwrite_mode': 'replace', 'confirm_replace': False},
        )
        assert resp.status_code == 422

    def test_confirm_replace_with_flag_succeeds(
        client_hrbp, db_session, employee_factory, xlsx_factory, tmp_uploads_dir,
    ):
        job_id = _do_preview(client_hrbp, employee_factory, xlsx_factory)
        resp = client_hrbp.post(
            f'/api/v1/eligibility-import/excel/{job_id}/confirm',
            json={'overwrite_mode': 'replace', 'confirm_replace': True},
        )
        assert resp.status_code == 200

    def test_confirm_double_returns_409(
        client_hrbp, db_session, employee_factory, xlsx_factory, tmp_uploads_dir,
    ):
        job_id = _do_preview(client_hrbp, employee_factory, xlsx_factory)
        r1 = client_hrbp.post(
            f'/api/v1/eligibility-import/excel/{job_id}/confirm',
            json={'overwrite_mode': 'merge', 'confirm_replace': False},
        )
        assert r1.status_code == 200
        r2 = client_hrbp.post(
            f'/api/v1/eligibility-import/excel/{job_id}/confirm',
            json={'overwrite_mode': 'merge', 'confirm_replace': False},
        )
        assert r2.status_code == 409

    def test_confirm_unknown_job_returns_404(client_hrbp, tmp_uploads_dir):
        resp = client_hrbp.post(
            '/api/v1/eligibility-import/excel/00000000-0000-0000-0000-000000000000/confirm',
            json={'overwrite_mode': 'merge'},
        )
        assert resp.status_code == 404

    def test_cancel_endpoint_returns_204(
        client_hrbp, employee_factory, xlsx_factory, tmp_uploads_dir,
    ):
        job_id = _do_preview(client_hrbp, employee_factory, xlsx_factory)
        resp = client_hrbp.post(f'/api/v1/eligibility-import/excel/{job_id}/cancel')
        assert resp.status_code == 204

    def test_cancel_employee_role_forbidden(client_employee, import_job_factory):
        job = import_job_factory(import_type='hire_info', status='previewing')
        resp = client_employee.post(f'/api/v1/eligibility-import/excel/{job.id}/cancel')
        assert resp.status_code == 403
    ```

    **fixture 假设：** `client_hrbp` / `client_employee` / `client_anon` / `db_session` / `employee_factory` / `import_job_factory` / `xlsx_factory` / `tmp_uploads_dir` 由 conftest.py 提供。如不存在 client_hrbp / client_employee / client_anon，本 plan 在 conftest.py 中加入：

    ```python
    @pytest.fixture
    def client_anon(test_app):
        from fastapi.testclient import TestClient
        return TestClient(test_app)

    @pytest.fixture
    def client_hrbp(test_app, hrbp_user_token):
        from fastapi.testclient import TestClient
        client = TestClient(test_app)
        client.headers.update({'Authorization': f'Bearer {hrbp_user_token}'})
        return client

    @pytest.fixture
    def client_employee(test_app, employee_user_token):
        from fastapi.testclient import TestClient
        client = TestClient(test_app)
        client.headers.update({'Authorization': f'Bearer {employee_user_token}'})
        return client
    ```

    `test_app` / `hrbp_user_token` / `employee_user_token` 应已在项目其他 API 测试中存在；如不存在，参考 `backend/tests/test_api/test_feishu.py` 等已有 API 测试的 setup 模式。
  </action>
  <verify>
    <automated>
      .venv/bin/pytest backend/tests/test_api/test_eligibility_import_preview_api.py backend/tests/test_api/test_eligibility_import_confirm_api.py -x --tb=short
    </automated>
  </verify>
  <acceptance_criteria>
    - grep "@router.post('/excel/preview'" backend/app/api/v1/eligibility_import.py 命中
    - grep "@router.post('/excel/{job_id}/confirm'" backend/app/api/v1/eligibility_import.py 命中
    - grep "@router.post('/excel/{job_id}/cancel'" backend/app/api/v1/eligibility_import.py 命中
    - grep "@router.get('/excel/active'" backend/app/api/v1/eligibility_import.py 命中
    - grep "_MAX_UPLOAD_SIZE_BYTES = 10 \* 1024 \* 1024" backend/app/api/v1/eligibility_import.py 命中
    - grep "_ALLOWED_XLSX_EXTENSIONS" backend/app/api/v1/eligibility_import.py 命中
    - grep "deprecated=True" backend/app/api/v1/eligibility_import.py 命中（旧 /excel 端点已标 deprecated）
    - grep "require_roles('admin', 'hrbp')" backend/app/api/v1/eligibility_import.py 至少 4 处命中（preview/confirm/cancel/active 全部强制鉴权）
    - 命令 `.venv/bin/pytest backend/tests/test_api/test_eligibility_import_preview_api.py -x` 全部 PASS
    - 命令 `.venv/bin/pytest backend/tests/test_api/test_eligibility_import_confirm_api.py -x` 全部 PASS
  </acceptance_criteria>
  <done>
    4 个新端点上线 + 文件安全校验 + 旧端点 deprecated；preview/confirm 共 15+ API 测试 PASS；replace 二次确认 + 鉴权 + 文件类型 + 大小防护全部覆盖。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 并发场景集成测试 + 4 类资格 import_type 模板下载端到端测试</name>
  <files>
    backend/tests/test_api/test_eligibility_import_concurrency.py,
    backend/tests/test_api/test_eligibility_import_template_api.py
  </files>
  <read_first>
    - backend/tests/test_api/test_eligibility_import_preview_api.py（Task 1 已建，确认 client/_upload_file helper 模式）
    - backend/app/api/v1/eligibility_import.py:127-156（现有 GET /templates/{import_type} 端点 — 本 plan 验证 4 类全覆盖且返回非空 xlsx）
    - backend/app/services/import_service.py（确认 build_template_xlsx 在 Plan 02 已支持 6 类）
  </read_first>
  <behavior>
    - Test 1: HR A preview hire_info → status='previewing'；HR B preview hire_info → 409（per-import_type 锁，previewing 也持锁）
    - Test 2: HR A preview hire_info；HR B preview performance_grades → 200（不同 type 可并行）
    - Test 3: HR A preview → confirm in flight；HR B 同 type 第二次 preview → 409（processing 也持锁）
    - Test 4: HR A confirm 中（设 status='processing'）；HR B 对**同一个 job_id** confirm → 409
    - Test 5: 4 类资格 import_type 模板下载 → 200 + Content-Type 是 xlsx + Content-Disposition 含 attachment
    - Test 6: 模板字节用 openpyxl 读回 → 第 1 行表头匹配 D-02/D-03 字段
    - Test 7: 模板下载 employee 角色 → 403
  </behavior>
  <action>
    1) 创建 `backend/tests/test_api/test_eligibility_import_concurrency.py`：

    ```python
    from __future__ import annotations
    import io


    def _upload(client, xlsx_bytes, import_type='hire_info'):
        return client.post(
            f'/api/v1/eligibility-import/excel/preview?import_type={import_type}',
            files={'file': ('t.xlsx', io.BytesIO(xlsx_bytes),
                            'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet')},
        )


    def test_preview_concurrent_same_type_returns_409(
        client_hrbp, employee_factory, xlsx_factory, tmp_uploads_dir,
    ):
        employee_factory(employee_no='E00001')
        data = xlsx_factory['hire_info'](rows=[['E00001', '2026-01-01', None]])
        r1 = _upload(client_hrbp, data)
        assert r1.status_code == 200
        r2 = _upload(client_hrbp, data)
        assert r2.status_code == 409
        assert r2.json()['detail']['error'] == 'import_in_progress'

    def test_preview_concurrent_different_type_not_blocked(
        client_hrbp, employee_factory, xlsx_factory, tmp_uploads_dir,
    ):
        employee_factory(employee_no='E00001')
        d1 = xlsx_factory['hire_info'](rows=[['E00001', '2026-01-01', None]])
        d2 = xlsx_factory['performance_grades'](rows=[['E00001', 2026, 'A']])
        r1 = _upload(client_hrbp, d1, 'hire_info')
        r2 = _upload(client_hrbp, d2, 'performance_grades')
        assert r1.status_code == 200
        assert r2.status_code == 200

    def test_preview_after_processing_same_type_returns_409(
        client_hrbp, db_session, import_job_factory, employee_factory, xlsx_factory, tmp_uploads_dir,
    ):
        # 制造一个 processing job
        import_job_factory(import_type='hire_info', status='processing')
        employee_factory(employee_no='E00001')
        data = xlsx_factory['hire_info'](rows=[['E00001', '2026-01-01', None]])
        resp = _upload(client_hrbp, data)
        assert resp.status_code == 409

    def test_double_confirm_returns_409(
        client_hrbp, db_session, employee_factory, xlsx_factory, tmp_uploads_dir,
    ):
        employee_factory(employee_no='E00001')
        data = xlsx_factory['hire_info'](rows=[['E00001', '2026-01-01', None]])
        r1 = _upload(client_hrbp, data)
        job_id = r1.json()['job_id']
        c1 = client_hrbp.post(
            f'/api/v1/eligibility-import/excel/{job_id}/confirm',
            json={'overwrite_mode': 'merge'},
        )
        assert c1.status_code == 200
        c2 = client_hrbp.post(
            f'/api/v1/eligibility-import/excel/{job_id}/confirm',
            json={'overwrite_mode': 'merge'},
        )
        assert c2.status_code == 409

    def test_active_endpoint_after_409_still_works(
        client_hrbp, db_session, import_job_factory,
    ):
        # 即使锁住 preview，active 端点仍可用（HR 用来诊断）
        job = import_job_factory(import_type='hire_info', status='previewing')
        resp = client_hrbp.get('/api/v1/eligibility-import/excel/active?import_type=hire_info')
        assert resp.status_code == 200
        assert resp.json()['job_id'] == job.id
    ```

    2) 创建 `backend/tests/test_api/test_eligibility_import_template_api.py`：

    ```python
    from __future__ import annotations
    import io
    import pytest
    from openpyxl import load_workbook


    @pytest.mark.parametrize('import_type,expected_headers', [
        ('performance_grades', ['员工工号', '年度', '绩效等级']),
        ('salary_adjustments', ['员工工号', '调薪日期', '调薪类型', '调薪金额']),
        ('hire_info', ['员工工号', '入职日期', '末次调薪日期']),
        ('non_statutory_leave', ['员工工号', '年度', '假期天数', '假期类型']),
    ])
    def test_template_returns_non_empty_xlsx(client_hrbp, import_type, expected_headers):
        resp = client_hrbp.get(f'/api/v1/eligibility-import/templates/{import_type}?format=xlsx')
        assert resp.status_code == 200, resp.text
        # Content-Disposition + Content-Type
        assert 'attachment' in resp.headers.get('content-disposition', '').lower()
        assert 'spreadsheetml' in resp.headers.get('content-type', '') or \
               'octet-stream' in resp.headers.get('content-type', '')
        # 内容非空
        assert len(resp.content) > 100
        # openpyxl 可读回
        wb = load_workbook(io.BytesIO(resp.content))
        ws = wb.active
        actual_headers = [c.value for c in ws[1]]
        assert actual_headers == expected_headers
        # 工号列文本格式
        assert ws.cell(row=2, column=1).number_format == '@'

    def test_template_unknown_type_returns_400(client_hrbp):
        resp = client_hrbp.get('/api/v1/eligibility-import/templates/unknown_type?format=xlsx')
        assert resp.status_code == 400

    def test_template_employee_role_forbidden(client_employee):
        resp = client_employee.get('/api/v1/eligibility-import/templates/hire_info?format=xlsx')
        assert resp.status_code == 403
    ```
  </action>
  <verify>
    <automated>
      .venv/bin/pytest backend/tests/test_api/test_eligibility_import_concurrency.py backend/tests/test_api/test_eligibility_import_template_api.py -x --tb=short
    </automated>
  </verify>
  <acceptance_criteria>
    - 命令 `.venv/bin/pytest backend/tests/test_api/test_eligibility_import_concurrency.py -x` 全部 PASS（5 个并发场景）
    - 命令 `.venv/bin/pytest backend/tests/test_api/test_eligibility_import_template_api.py -x` 全部 PASS（4 类 + unknown + 鉴权 共 6 用例）
    - 命令 `.venv/bin/pytest backend/tests/test_api/ -x --tb=short -q` 整个 test_api/ 目录无 regression
    - 命令 `.venv/bin/pytest backend/tests/ -x --tb=short -q` 全套测试无 regression
  </acceptance_criteria>
  <done>
    4 个并发场景测试 + 4 类模板下载 e2e 测试 PASS；后端 API 完整闭环；前端（Plan 05/06）可基于稳定的 API 契约开发。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HR JWT → API endpoints | 鉴权门禁（require_roles('admin', 'hrbp')） |
| HR client → multipart/form-data file | 文件类型 / 大小 / Content-Type 不可信 |
| HR client → import_type query param | enum 校验（必须在 ELIGIBILITY_IMPORT_TYPES） |
| HR client → job_id path param | UUID 格式（service 层 _staged_file_path 已防路径遍历） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-02 | T (Tampering) / S (Spoofing) | 文件类型校验 — 拒绝 .exe/.html/.svg | mitigate | Task 1 _validate_upload_file 白名单（仅 .xlsx/.xls）；测试 test_preview_rejects_exe_file 验证 |
| T-32-03 | D (DoS) | 文件大小防 OOM | mitigate | Task 1 _MAX_UPLOAD_SIZE_BYTES = 10MB；测试 test_preview_rejects_oversized_file 验证 |
| T-32-04 | E (Elevation of privilege) | confirm 端点鉴权 + actor_id 来源 | mitigate | Task 1 require_roles('admin', 'hrbp') + actor_id 来自 current_user.id（不接受请求体传入）；测试 test_confirm_employee_role_forbidden 验证 |
| T-32-05 | T (Tampering) | 并发锁绕过 — 必须在 DB 层 | mitigate | Task 1 preview/confirm/cancel/active 4 个端点都强制 service.is_import_running 检查；测试 test_preview_concurrent_same_type_returns_409 / test_double_confirm_returns_409 验证 |
| T-32-07 | E (Elevation of privilege) | replace 模式越权 — 仅 admin/hrbp 可用 | mitigate | Task 1 require_roles('admin', 'hrbp') 约束所有写端点；replace 与 merge 共用同一鉴权门 — 不再额外限制（CONTEXT 未要求按 admin 单独限制 replace）；可在未来 phase 加 admin-only-for-replace |
| T-32-16 | I (Information disclosure) | 错误响应不暴露内部异常 stack | mitigate | Task 1 try/except ValueError → HTTPException with 业务文案；项目既有 main.py 的 generic Exception handler 兜底 |
</threat_model>

<verification>
- 完整 API 契约：4 个新端点 + 旧端点 deprecated + 模板端点 4 类全覆盖
- 并发覆盖：preview / confirm / cancel / active 全部经过锁检查
- 鉴权覆盖：所有写端点 require_roles，403 测试存在
- 文件安全：类型 + 大小 + Content-Type 三重校验，恶意文件测试存在
- 全套 `.venv/bin/pytest backend/tests/ -x` 无 regression
</verification>

<success_criteria>
- IMPORT-01：API 已支持上传 hire_info / non_statutory_leave（Plan 02 service 层 + Plan 04 API 联通）
- IMPORT-02：4 类资格 import_type 模板下载端到端通过；test_template_returns_non_empty_xlsx 4 个 parametrize 全部 PASS
- IMPORT-05：confirm 端点强制 confirm_replace 校验，AuditLog 写入 overwrite_mode
- IMPORT-06：per-import_type 锁在 preview / confirm 双端生效；不同 type 可并行
- IMPORT-07：preview + confirm 两阶段端点完整，PreviewResponse 结构稳定
- 旧 POST /excel 标 deprecated（决议 Open Question 4）
</success_criteria>

<output>
完成后创建 `.planning/phases/32-eligibility-import-completion/32-04-SUMMARY.md`，重点记录：
- 4 个新端点的 URL / Method / 请求/响应 schema
- 旧 POST /excel 的 deprecated 状态
- 文件安全校验规则（类型白名单 + 10MB 上限）
- 并发锁场景矩阵（preview vs preview / preview vs confirm / 不同 type）
- Plan 05（前端 service）所需的 API 契约总览
</output>
