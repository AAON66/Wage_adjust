---
phase: 30-employee-no-leading-zero
plan: 03
plan_number: 3
type: execute
wave: 2
depends_on: [30-01]
files_modified:
  - backend/app/services/feishu_service.py
  - backend/tests/test_services/test_feishu_leading_zero.py
autonomous: true
requirements: [EMPNO-02, EMPNO-03, EMPNO-04]
requirements_addressed: [EMPNO-02, EMPNO-03, EMPNO-04]

must_haves:
  truths:
    - "FeishuService._map_fields 对 employee_no 字段永远按 text 处理：删除 `str(int(value))` 分支；raw value 非 str 时 logger.warning 记录「飞书 employee_no 非文本类型，已强制转字符串（可能已丢失前导零）」"
    - "FeishuService._build_employee_map 不再预填充 stripped 版本 — emp_map 仅含 DB 原始 emp_no；_lookup_employee 的 fallback 分支通过 lstrip 比对每个 emp_map key 实现容忍匹配，使计数器反映真正的「飞书源丢前导零」信号"
    - "FeishuService 新增方法 validate_field_mapping 与 _validate_field_mapping_with_credentials，create_config / update_config 在保存前调用，非 text 类型 employee_no 字段抛 FeishuConfigValidationError 且配置未持久化"
    - "5 个 sync_* 方法在开头创建 fallback_counter 并在 sync_log 写入时落 leading_zero_fallback_count"
    - "既有同步逻辑不因本 plan 崩溃；计数器在未命中容忍匹配时保持 0"
  artifacts:
    - path: "backend/app/services/feishu_service.py"
      provides: "_map_fields 修复 + _build_employee_map 重构 + _lookup_employee 计数器 + validate_field_mapping 新方法 + sync_* 方法落 leading_zero_fallback_count"
      min_lines: 80
    - path: "backend/tests/test_services/test_feishu_leading_zero.py"
      provides: "EMPNO-02/03/04 回归测试（含本地 db_session fixture，4 场景覆盖 fallback 计数器真信号）"
      min_lines: 150
  key_links:
    - from: "FeishuService._map_fields"
      to: "employee_no text-only handling"
      via: "删除 str(int(value)) 分支 + logger.warning"
      pattern: "飞书 employee_no 非文本类型"
    - from: "FeishuService._build_employee_map"
      to: "DB 真实视图（不预填充 stripped）"
      via: "取消 lstrip 预填充"
      pattern: "_build_employee_map"
    - from: "FeishuService._lookup_employee fallback"
      to: "lstrip 比对 emp_map keys"
      via: "any(k.lstrip('0') == stripped_key for k in emp_map)"
      pattern: "fallback_counter"
    - from: "FeishuService.validate_field_mapping"
      to: "FeishuService.list_bitable_fields"
      via: "HTTP GET bitable fields"
      pattern: "validate_field_mapping"
    - from: "FeishuService.create_config / update_config"
      to: "_validate_field_mapping_with_credentials"
      via: "保存前调用，失败时阻断"
      pattern: "_validate_field_mapping_with_credentials\\("
    - from: "_lookup_employee fallback hit"
      to: "FeishuSyncLog.leading_zero_fallback_count"
      via: "fallback_counter dict + sync_log 落库"
      pattern: "leading_zero_fallback_count"
---

<objective>
FeishuService 关键改造，闭合工号前导零的飞书链路。包含一个**语义修复**（B-10 计数器信号失真根因）：

1. **`_map_fields` 去 int 误用（EMPNO-02 / D-08）** — 删除 `str(int(value))` 分支，employee_no 永远按 text 处理；raw value 非 str 时 logger.warning 告警
2. **`_build_employee_map` 语义修复 + `_lookup_employee` 容忍匹配计数器（EMPNO-04 / D-03 / D-04 + B-10 修复）** — 现状 `_build_employee_map` 方法内预填充 stripped 版本（即方法体内 `stripped = emp_no.lstrip('0')` + `emp_map[stripped] = emp_id` 这段逻辑），导致「DB 存 '02615'、飞书返回 '2615'」直接 exact 命中，fallback 分支**永远不进**、计数器**永远为 0**，与 HR 想观测的「飞书源丢前导零」语义**相反**。本 plan 修复：`_build_employee_map` 只存 DB 原始 emp_no；`_lookup_employee` exact miss 后对 map 每个 key 做 lstrip 比对，此时命中的才是真正的「飞书丢前导零容忍匹配」——计数器在此 +1
3. **`validate_field_mapping` 新方法（EMPNO-03 / D-01 / D-02）** — 保存配置前校验飞书端 employee_no 映射字段类型必须是 text；非 text 抛结构化错误阻断配置保存

目的：
- 飞书源表工号字段若 HR 误设为「数字」类型，保存配置时即阻断（不等到运行时出错）
- 飞书 float 返回的工号不再走 `str(int(value))` 丢前导零路径
- HR 在同步日志页看到的 `leading_zero_fallback_count` 真正反映「飞书源与 DB 前导零不一致、靠 lstrip 救回」的记录数
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/30-employee-no-leading-zero/30-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/codebase/CONVENTIONS.md

@backend/app/services/feishu_service.py
@backend/app/models/feishu_sync_log.py
@.planning/phases/30-employee-no-leading-zero/30-01-PLAN.md

<interfaces>
<!-- 关键既定契约。Executor 直接使用，无需再去 codebase 翻找。 -->

**当前 `_map_fields` 方法 employee_no 分支（需要修改，基于实际代码确认）：**

```python
if system_name == 'employee_no':
    has_employee_no = True
    if isinstance(value, float) and value == int(value):
        value = str(int(value))      # ← BUG：丢前导零
    else:
        value = str(value).strip()
```

**改为：**

```python
if system_name == 'employee_no':
    has_employee_no = True
    if not isinstance(raw_value, str):
        logger.warning(
            '飞书 employee_no 非文本类型，已强制转字符串（可能已丢失前导零）。raw_value=%r',
            raw_value,
        )
    value = str(value).strip()  # 不做 int(value) 转换
```

注意：`raw_value` 是 _map_fields 循环内 `raw_value = raw_fields.get(feishu_name)` 取的原始飞书字段值；`value` 是经 `_extract_cell_value` 处理后的值。判断 raw_value 类型才是语义正确的。

**`_extract_cell_value` 实现确认**（已读源码）：
```python
if isinstance(value, (int, float)):
    return value       # 原样返回 int/float
if isinstance(value, str):
    return value
...
```
→ 当 raw_value 是 float `2615.0` 时，`value = _extract_cell_value(2615.0) = 2615.0`（float 原样），后续 `str(2615.0).strip() = '2615.0'`。
→ 当 raw_value 是 int `2615` 时，`value = 2615`（int 原样），后续 `str(2615).strip() = '2615'`。

**当前 `_build_employee_map` 方法的根因代码：**

```python
def _build_employee_map(self) -> dict[str, str]:
    emp_rows = self.db.execute(select(Employee.employee_no, Employee.id)).all()
    emp_map: dict[str, str] = {}
    for emp_no, emp_id in emp_rows:
        emp_map[emp_no] = emp_id
        # Also index by stripped version so '02615' matches '2615' and vice versa
        stripped = emp_no.lstrip('0') or '0'
        if stripped not in emp_map:
            emp_map[stripped] = emp_id   # ← 这三行使计数器永远为 0（B-10 根因）
    return emp_map
```

**当前 `_lookup_employee`（staticmethod）：**

```python
@staticmethod
def _lookup_employee(emp_map: dict[str, str], emp_no: str | None) -> str | None:
    if not emp_no:
        return None
    emp_id = emp_map.get(emp_no)
    if emp_id is not None:
        return emp_id
    return emp_map.get(emp_no.lstrip('0') or '0')  # fallback: 对 key 做 lstrip
```

问题：因 `_build_employee_map` 已预插 stripped 版本 → exact match 直接命中 → fallback 分支**永远不进**。

**新版 `_build_employee_map`（本 plan 修改）：**
```python
def _build_employee_map(self) -> dict[str, str]:
    """Build employee_no → id map from DB (真实视图，无预填充)。

    容忍匹配在 _lookup_employee 的 fallback 分支实现，该分支命中时计数器 +1，
    用于观测「飞书源工号前导零丢失」的信号（EMPNO-04 / D-03 / D-04）。
    """
    emp_rows = self.db.execute(select(Employee.employee_no, Employee.id)).all()
    return {emp_no: emp_id for emp_no, emp_id in emp_rows}
```

**新版 `_lookup_employee`（实例方法，改自 staticmethod）：**
```python
def _lookup_employee(
    self,
    emp_map: dict[str, str],
    emp_no: str | None,
    *,
    fallback_counter: dict[str, int] | None = None,
) -> str | None:
    """EMPNO-04 / D-03: exact match 优先；miss 后对 emp_map 每个 key 做 lstrip 比对。

    fallback 分支命中（即：exact 失败但 lstrip 后找到）时，若传入 fallback_counter，
    counter['count'] += 1。该计数反映「飞书源与 DB 前导零不一致靠 lstrip 救回」的记录数。
    """
    if not emp_no:
        return None
    # exact match
    emp_id = emp_map.get(emp_no)
    if emp_id is not None:
        return emp_id
    # fallback: 对 emp_map 每个 key 做 lstrip 比对
    stripped_key = emp_no.lstrip('0') or '0'
    for map_key, map_id in emp_map.items():
        if (map_key.lstrip('0') or '0') == stripped_key:
            if fallback_counter is not None:
                fallback_counter['count'] = fallback_counter.get('count', 0) + 1
            return map_id
    return None
```

**现有 `list_bitable_fields` 返回结构：**

```python
[
    {'field_id': str, 'field_name': str, 'type': int, 'ui_type': str},
    ...
]
```

飞书 field type 枚举：
- `1` = 多行文本（text / Text）
- `2` = 数字（Number）
- `3` = 单选（SingleSelect）
- `4` = 多选（MultiSelect）
- `5` = 日期（DateTime）
- `7` = 复选框（Checkbox）
- `11` = 人员（User）
- `13` = 电话号码（PhoneNumber）
- `15` = 超链接（Url）
- `17` = 附件（Attachment）
- `18` = 单向关联（SingleLink）
- `19` = 查找引用（Lookup）
- `20` = 公式（Formula）
- `21` = 双向关联（DuplexLink）

**EMPNO-03 规则：** employee_no 映射的飞书字段 `type` 必须 == 1（多行文本）。非 1 时抛结构化错误。

**现有 `FeishuService.create_config` 和 `FeishuService.update_config` 方法**在文件 `backend/app/services/feishu_service.py` 内（非 `backend/app/api/v1/feishu.py`），可以在这两个 service 方法内的保存前插入 `_validate_field_mapping_with_credentials` 调用。**API 层的 create_config / update_config（backend/app/api/v1/feishu.py）改动由 Plan 04 负责，本 plan 只动 service 层。**

**既定异常模式：** 项目用 `RuntimeError` / `ValueError` / `HTTPException`。新增 `FeishuConfigValidationError(ValueError)`，payload 存为 `.detail` 属性字典（供 API 层转 422）。

**FeishuSyncLog 字段（Plan 01 已添加）**：
```python
leading_zero_fallback_count: Mapped[int] = mapped_column(Integer, nullable=False, default=0, server_default='0')
```

**重要环境事实：本项目 `backend/tests/` 目录下 *不存在* conftest.py**。测试 fixture 在文件顶部本地定义 —— **禁止新建 conftest.py**。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: _map_fields 去 int 误用 + _build_employee_map 语义修复 + _lookup_employee 容忍匹配计数器 + sync_* 方法落库</name>
  <files>backend/app/services/feishu_service.py</files>
  <read_first>
    - backend/app/services/feishu_service.py（整文件；重点看 `_extract_cell_value`、`_map_fields`、`_build_employee_map`、`_lookup_employee`、5 个 `sync_*` 方法的完整实现）
    - .planning/phases/30-employee-no-leading-zero/30-CONTEXT.md § D-08（原文）
    - .planning/research/PITFALLS.md § Pitfall 5（Excel/飞书前导零根因）
  </read_first>
  <action>
    在 `backend/app/services/feishu_service.py` 做以下四处修改。**所有代码位置以 类/方法名 + 方法内结构位置 描述 —— 禁止引用绝对行号。**

    **1. 修改 `FeishuService._map_fields` 方法内 employee_no 分支：**

    定位：`_map_fields` 方法内部，`for feishu_name, system_name in field_mapping.items():` 循环体内，`if system_name == 'employee_no':` 这段代码块。

    找到并**完整替换** employee_no 分支为：

    ```python
    if system_name == 'employee_no':
        has_employee_no = True
        # D-08: employee_no 永远按 text 处理。原 int(value) 路径会丢前导零（'02615' -> 2615.0 -> '2615'）。
        # 飞书源字段类型若为「数字」，raw_value 就是 float，此时工号已在飞书存储层丢失前导零 —
        # 仅记录 warning 让 HR 感知；配置保存时 validate_field_mapping 会阻断非 text 字段。
        if not isinstance(raw_value, str):
            logger.warning(
                '飞书 employee_no 非文本类型，已强制转字符串（可能已丢失前导零）。raw_value=%r',
                raw_value,
            )
        value = str(value).strip()
    ```

    保留 `has_employee_no = True` 行。`raw_value` 在循环内 `raw_value = raw_fields.get(feishu_name)` 已定义（方法内先有该行，直接复用）。

    **2. 重构 `_build_employee_map` 方法 —— 取消 stripped 预填充（B-10 修复）：**

    定位：`FeishuService._build_employee_map` 方法（当前有一个预填充循环 `for emp_no, emp_id in emp_rows: emp_map[emp_no] = emp_id; stripped = emp_no.lstrip('0') ... emp_map[stripped] = emp_id`）。

    **完整替换**为：

    ```python
    def _build_employee_map(self) -> dict[str, str]:
        """Build employee_no → id map from DB (DB 真实视图，不预填充 stripped 版本)。

        容忍匹配（lstrip('0')）在 _lookup_employee 的 fallback 分支实现；
        fallback 命中时 fallback_counter['count'] += 1（EMPNO-04 / D-03 / D-04）。
        该计数反映「飞书源与 DB 前导零不一致、靠 lstrip 救回」的真实记录数。
        """
        emp_rows = self.db.execute(select(Employee.employee_no, Employee.id)).all()
        return {emp_no: emp_id for emp_no, emp_id in emp_rows}
    ```

    **关键：不再有任何 `stripped = emp_no.lstrip('0')` 或 `emp_map[stripped] = ...` 代码。**

    **3. 重构 `_lookup_employee` 方法 —— staticmethod 改实例方法 + fallback 做 lstrip 比对 + 计数器：**

    定位：`FeishuService._lookup_employee` 方法（当前是 `@staticmethod`）。

    **完整替换**（移除 `@staticmethod` 装饰器）：

    ```python
    def _lookup_employee(
        self,
        emp_map: dict[str, str],
        emp_no: str | None,
        *,
        fallback_counter: dict[str, int] | None = None,
    ) -> str | None:
        """Find employee_id by trying exact match first, then lstrip fallback.

        EMPNO-04 / D-03: exact match 失败后，对 emp_map 每个 key 做 lstrip('0') 比对；
        fallback 命中时若传入 fallback_counter，counter['count'] += 1。
        该计数反映「飞书源工号与 DB 前导零不一致」的信号，不降级同步 status（D-04）。

        fallback_counter 是一个共享的 dict（调用方传入），同步结束后随 FeishuSyncLog 落库。
        """
        if not emp_no:
            return None
        # exact match
        emp_id = emp_map.get(emp_no)
        if emp_id is not None:
            return emp_id
        # fallback: 对 emp_map 每个 key 做 lstrip 比对（B-10 修复：真正的「飞书丢前导零」路径）
        stripped_key = emp_no.lstrip('0') or '0'
        for map_key, map_id in emp_map.items():
            if (map_key.lstrip('0') or '0') == stripped_key:
                if fallback_counter is not None:
                    fallback_counter['count'] = fallback_counter.get('count', 0) + 1
                return map_id
        return None
    ```

    **关键点：**
    - 从 `@staticmethod` 改为实例方法（第一个参数改为 `self`）
    - `fallback_counter` 是可选的 dict（`{'count': N}` 形态）；不传时行为与原函数一致（向后兼容）
    - fallback 分支 **遍历 emp_map 的每个 key 做 lstrip 比对**（而不是仅 `emp_map.get(lstrip_key)`），因为 `_build_employee_map` 已去掉 stripped 预填充
    - 空字符串处理：`lstrip('0') or '0'` 避免全 '0' 被 strip 空

    **4. 在所有 5 个 `sync_*` 方法中创建 fallback_counter 并落库。**

    定位：`FeishuService.sync_attendance`、`FeishuService.sync_performance_records`、`FeishuService.sync_salary_adjustments`、`FeishuService.sync_hire_info`、`FeishuService.sync_non_statutory_leave` 共 5 个方法。

    对每个方法做以下三步改动：

    **a.** 在方法开头 `sync_log = FeishuSyncLog(...)` 实例化之后紧跟一行创建计数器：
    ```python
    fallback_counter: dict[str, int] = {'count': 0}
    ```

    **b.** 搜索方法内**所有** `self._lookup_employee(emp_map, ...)` 或 `FeishuService._lookup_employee(emp_map, ...)` 调用（原为 staticmethod），改为：
    ```python
    emp_id = self._lookup_employee(emp_map, emp_no, fallback_counter=fallback_counter)
    ```
    （保留各方法已有的 emp_no 变量名与调用链；只是为调用加 `fallback_counter=fallback_counter` 关键字参数。由于本 plan 已把 `_lookup_employee` 从 staticmethod 改为实例方法，原 `FeishuService._lookup_employee(emp_map, emp_no)` 写法必须统一改成 `self._lookup_employee(emp_map, emp_no, fallback_counter=...)`。）

    **c.** 在每个 sync 方法的**所有** sync_log 写入点（每个方法都有 `sync_log.status = ...` + `sync_log.finished_at = ...` 的块，包括成功路径和异常路径）新增：
    ```python
    sync_log.leading_zero_fallback_count = fallback_counter['count']
    ```

    该赋值必须与 `sync_log.unmatched_count = unmatched_count`（或等价字段）并列，即在同一个写入块内。

    **关键：** 若某方法内异常路径（except 块）在 sync_log 写入前就返回了，需要在该路径下也显式写 `sync_log.leading_zero_fallback_count = fallback_counter['count']`。由于 `server_default='0'` 已保证 DB NOT NULL 满足，漏写不会 crash，但为保证 HR 能看到准确计数，**所有 sync_log 写入点统一加**。

    **禁止：**
    - 不要修改 `_extract_cell_value` 方法（与本 plan 无关）
    - 不要把 `_lookup_employee` 留成 staticmethod（必须改成实例方法，否则计数器参数传不进去）
    - 不要改动 SyncLogRead schema（Plan 04 处理）
  </action>
  <acceptance_criteria>
    - `grep -c "str(int(value))" backend/app/services/feishu_service.py` 输出 `0`（D-08 要求完全删除该路径）
    - `grep -c "飞书 employee_no 非文本类型，已强制转字符串（可能已丢失前导零）" backend/app/services/feishu_service.py` 输出 `1`
    - `_map_fields` 中保留 `has_employee_no = True` 行（grep 命中 ≥ 1）
    - **`_build_employee_map` 不再含 stripped 预填充**：`grep -A 15 "def _build_employee_map" backend/app/services/feishu_service.py | grep -c "emp_map\[stripped\]"` 输出 `0` 且 `grep -A 15 "def _build_employee_map" backend/app/services/feishu_service.py | grep -c "stripped = emp_no.lstrip"` 输出 `0`
    - `_lookup_employee` 方法签名含 `fallback_counter` 参数（grep `fallback_counter: dict\[str, int\] \| None = None` 命中）
    - `_lookup_employee` 不再是 staticmethod：`grep -B 1 "def _lookup_employee" backend/app/services/feishu_service.py | grep -c "@staticmethod"` 输出 `0`
    - `_lookup_employee` fallback 分支含对 emp_map keys 的遍历比对：`grep "for map_key, map_id in emp_map.items" backend/app/services/feishu_service.py` 命中
    - `fallback_counter: dict[str, int] = {'count': 0}` 在 5 个 sync_* 方法中各出现 1 次（grep 命中 ≥ 5）
    - `sync_log.leading_zero_fallback_count = fallback_counter['count']` 或等价 grep pattern 命中 ≥ 5 次（每个 sync_* 方法至少一处）
    - `python3 -c "from backend.app.services.feishu_service import FeishuService; print(FeishuService._lookup_employee.__doc__)"` 不崩溃，docstring 含「EMPNO-04」
    - 运行 `python -m pytest backend/tests/test_services/test_feishu_service.py backend/tests/test_services/test_feishu_config.py -v` 既有测试无回归（若旧测试 mock _lookup_employee 的 staticmethod 签名，可以改为 instance method 调用；本 acceptance 允许测试被更新以适配新行为）
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 新增 validate_field_mapping + _validate_field_mapping_with_credentials + create_config / update_config 集成</name>
  <files>backend/app/services/feishu_service.py</files>
  <read_first>
    - backend/app/services/feishu_service.py（整文件；重点看 `list_bitable_fields`、`create_config`、`update_config`、`_ensure_token`、`get_config`、`parse_bitable_url` 的完整实现）
    - .planning/phases/30-employee-no-leading-zero/30-CONTEXT.md § D-01 / D-02（原文）
    - backend/app/schemas/feishu.py（整文件；FeishuConfigCreate / FeishuConfigUpdate 的 field_mapping 结构）
  </read_first>
  <action>
    在 `backend/app/services/feishu_service.py` 做以下修改。**所有代码位置以 类/方法名 边界描述 —— 禁止引用绝对行号。**

    **1. 在文件顶部（import 区之后、`FeishuService` 类定义之前）新增自定义异常类：**

    ```python
    class FeishuConfigValidationError(ValueError):
        """EMPNO-03 / D-01: 飞书配置校验失败（字段类型不符合要求等）。

        detail dict 结构：
            {'error': 'invalid_field_type', 'field': 'employee_no',
             'expected': 'text', 'actual': '<type-name>'}
        """

        def __init__(self, detail: dict) -> None:
            super().__init__(str(detail))
            self.detail = detail
    ```

    位置：紧跟既有 `logger = logging.getLogger(__name__)` 之后、`class FeishuService:` 之前。

    **2. 在 `FeishuService` 类内新增常量和两个方法。**

    位置：放在 `FeishuService.list_bitable_fields` 方法之后、`FeishuService.parse_bitable_url` 方法之前。

    ```python
    # EMPNO-03 / D-01 / D-02: 飞书字段类型枚举 — type=1 表示多行文本
    _FEISHU_TEXT_FIELD_TYPE = 1

    # D-01: 仅对关键业务键列做类型校验；其他字段 HR 可自由映射不同类型（例如日期列是 5）
    _FIELD_MAPPING_REQUIRED_TYPES: dict[str, int] = {
        'employee_no': _FEISHU_TEXT_FIELD_TYPE,
    }

    # D-01: 飞书 field type 整数 → 人类可读名称（错误 payload 用）
    _FEISHU_FIELD_TYPE_NAMES: dict[int, str] = {
        1: 'text',
        2: 'number',
        3: 'single_select',
        4: 'multi_select',
        5: 'datetime',
        7: 'checkbox',
        11: 'user',
        13: 'phone',
        15: 'url',
        17: 'attachment',
    }

    def validate_field_mapping(
        self,
        *,
        app_token: str,
        table_id: str,
        field_mapping: dict[str, str],
    ) -> None:
        """D-01 公共 API：基于已持久化的 config 做字段类型校验。"""
        config = self.get_config()
        if config is None:
            raise FeishuConfigValidationError({
                'error': 'no_active_config',
                'message': '未找到激活的飞书配置',
            })
        settings = get_settings()
        app_secret = config.get_app_secret(settings.feishu_encryption_key)
        self._validate_field_mapping_with_credentials(
            app_id=config.app_id,
            app_secret=app_secret,
            app_token=app_token,
            table_id=table_id,
            field_mapping=field_mapping,
        )

    def _validate_field_mapping_with_credentials(
        self,
        *,
        app_id: str,
        app_secret: str,
        app_token: str,
        table_id: str,
        field_mapping: dict[str, str],
    ) -> None:
        """D-01 辅助：直接用传入的 credentials 校验字段类型，不依赖已持久化 config。

        创建/更新配置时可在 commit 前调用此方法；若校验失败抛 FeishuConfigValidationError。
        """
        token = self._ensure_token(app_id, app_secret)
        url = f'{self.FEISHU_BASE_URL}/bitable/v1/apps/{app_token}/tables/{table_id}/fields'
        headers = {'Authorization': f'Bearer {token}'}

        fields_meta: list[dict] = []
        page_token: str | None = None
        while True:
            params: dict = {'page_size': 100}
            if page_token:
                params['page_token'] = page_token
            self._rate_limiter.wait_and_acquire()
            try:
                resp = httpx.get(url, headers=headers, params=params, timeout=30)
                data = resp.json()
            except Exception as exc:
                raise FeishuConfigValidationError({
                    'error': 'bitable_fields_fetch_failed',
                    'message': f'无法拉取飞书字段元信息进行校验：{exc}',
                }) from exc
            if data.get('code') != 0:
                raise FeishuConfigValidationError({
                    'error': 'bitable_fields_fetch_failed',
                    'message': f'飞书字段接口返回错误：code={data.get("code")} msg={data.get("msg")}',
                })
            for item in data.get('data', {}).get('items', []):
                fields_meta.append({
                    'field_name': item.get('field_name', ''),
                    'type': item.get('type', 0),
                })
            has_more = data.get('data', {}).get('has_more', False)
            page_token = data.get('data', {}).get('page_token')
            if not has_more or not page_token:
                break

        feishu_name_to_type = {
            item['field_name']: int(item.get('type', 0)) for item in fields_meta
        }
        # field_mapping 格式：{feishu_field_name: system_field_name}
        reverse_mapping = {v: k for k, v in field_mapping.items()}

        for system_field, required_type in self._FIELD_MAPPING_REQUIRED_TYPES.items():
            feishu_field_name = reverse_mapping.get(system_field)
            if feishu_field_name is None:
                # HR 未映射该 system_field，validate 跳过
                continue
            actual_type_int = feishu_name_to_type.get(feishu_field_name)
            if actual_type_int is None:
                raise FeishuConfigValidationError({
                    'error': 'field_not_found_in_bitable',
                    'field': system_field,
                    'feishu_field_name': feishu_field_name,
                })
            if actual_type_int != required_type:
                actual_name = self._FEISHU_FIELD_TYPE_NAMES.get(
                    actual_type_int, f'unknown({actual_type_int})',
                )
                expected_name = self._FEISHU_FIELD_TYPE_NAMES.get(
                    required_type, str(required_type),
                )
                raise FeishuConfigValidationError({
                    'error': 'invalid_field_type',
                    'field': system_field,
                    'expected': expected_name,
                    'actual': actual_name,
                })
    ```

    **3. 修改 `FeishuService.create_config` 方法 —— 在实例化 FeishuConfig 之前插入校验。**

    定位：`FeishuService.create_config` 方法内部（**不是** `backend/app/api/v1/feishu.py` 的路由！—— 本 plan 只动 service 层）。在方法体内找到 `config = FeishuConfig(...)` 实例化语句之前，插入 `self._validate_field_mapping_with_credentials(...)` 调用。

    修改后的完整方法应为：

    ```python
    def create_config(self, data: FeishuConfigCreate) -> FeishuConfig:
        """创建飞书配置，加密 app_secret，序列化 field_mapping。

        EMPNO-03 / D-01: 保存前校验 employee_no 映射字段类型必须为 text。
        """
        settings = get_settings()
        field_mapping_dict = {item.feishu_field: item.system_field for item in data.field_mapping}

        # D-01: 阻断非 text 类型的 employee_no 字段映射保存 — 必须在 FeishuConfig 实例化之前
        self._validate_field_mapping_with_credentials(
            app_id=data.app_id,
            app_secret=data.app_secret,
            app_token=data.bitable_app_token,
            table_id=data.bitable_table_id,
            field_mapping=field_mapping_dict,
        )

        config = FeishuConfig(
            app_id=data.app_id,
            encrypted_app_secret=encrypt_value(data.app_secret, settings.feishu_encryption_key),
            bitable_app_token=data.bitable_app_token,
            bitable_table_id=data.bitable_table_id,
            field_mapping=json.dumps(field_mapping_dict, ensure_ascii=False),
            sync_hour=data.sync_hour,
            sync_minute=data.sync_minute,
            sync_timezone=data.sync_timezone,
        )
        self.db.add(config)
        self.db.commit()
        self.db.refresh(config)
        return config
    ```

    **4. 修改 `FeishuService.update_config` 方法 —— 条件性调用 validator。**

    定位：`FeishuService.update_config` 方法内部。在既有 `config = self.db.get(FeishuConfig, config_id)` 与 `self.db.commit()` 之间，增加条件性 validator 调用。

    修改后的完整方法应为：

    ```python
    def update_config(self, config_id: str, data: FeishuConfigUpdate) -> FeishuConfig:
        """更新飞书配置。app_secret 为 None 或空字符串时保留原值。

        EMPNO-03 / D-01: 若 HR 修改了 field_mapping / bitable_app_token / bitable_table_id，
        保存前重新校验字段类型；非 text 类型抛 FeishuConfigValidationError。
        """
        config = self.db.get(FeishuConfig, config_id)
        if config is None:
            raise RuntimeError(f'Config {config_id} not found')

        settings = get_settings()

        # D-01: 如果 field_mapping / bitable 坐标任一被修改，需要重新校验
        needs_validation = (
            data.field_mapping is not None
            or data.bitable_app_token is not None
            or data.bitable_table_id is not None
        )
        if needs_validation:
            effective_app_id = data.app_id if data.app_id is not None else config.app_id
            if data.app_secret and data.app_secret.strip():
                effective_app_secret = data.app_secret
            else:
                effective_app_secret = config.get_app_secret(settings.feishu_encryption_key)
            effective_app_token = (
                data.bitable_app_token if data.bitable_app_token is not None
                else config.bitable_app_token
            )
            effective_table_id = (
                data.bitable_table_id if data.bitable_table_id is not None
                else config.bitable_table_id
            )
            if data.field_mapping is not None:
                effective_mapping = {
                    item.feishu_field: item.system_field for item in data.field_mapping
                }
            else:
                raw = config.field_mapping
                effective_mapping = json.loads(raw) if isinstance(raw, str) else (raw or {})

            self._validate_field_mapping_with_credentials(
                app_id=effective_app_id,
                app_secret=effective_app_secret,
                app_token=effective_app_token,
                table_id=effective_table_id,
                field_mapping=effective_mapping,
            )

        # 以下保留原有写入逻辑
        if data.app_id is not None:
            config.app_id = data.app_id
        if data.app_secret and data.app_secret.strip():
            config.encrypted_app_secret = encrypt_value(data.app_secret, settings.feishu_encryption_key)
        if data.bitable_app_token is not None:
            config.bitable_app_token = data.bitable_app_token
        if data.bitable_table_id is not None:
            config.bitable_table_id = data.bitable_table_id
        if data.field_mapping is not None:
            field_mapping_dict = {item.feishu_field: item.system_field for item in data.field_mapping}
            config.field_mapping = json.dumps(field_mapping_dict, ensure_ascii=False)
        if data.sync_hour is not None:
            config.sync_hour = data.sync_hour
        if data.sync_minute is not None:
            config.sync_minute = data.sync_minute
        if data.sync_timezone is not None:
            config.sync_timezone = data.sync_timezone

        self.db.commit()
        self.db.refresh(config)
        return config
    ```

    **禁止：**
    - 不要往 `list_bitable_fields` 原签名塞新参数
    - 不要在运行时 sync 流程中调用 validate_field_mapping（D-02 明确运行时不做二次校验）
    - 不要阻断 `create_config` 在 validate 失败时 db.commit — 异常抛出时 db.add 还未执行，不需要手动 rollback
    - 不要修改 `backend/app/api/v1/feishu.py` —— API 层改动由 Plan 04 负责
  </action>
  <acceptance_criteria>
    - `grep -c "class FeishuConfigValidationError" backend/app/services/feishu_service.py` 输出 `1`
    - `grep -c "def validate_field_mapping" backend/app/services/feishu_service.py` 输出 `1`
    - `grep -c "def _validate_field_mapping_with_credentials" backend/app/services/feishu_service.py` 输出 `1`
    - `grep -c "_FEISHU_TEXT_FIELD_TYPE = 1" backend/app/services/feishu_service.py` 输出 `1`
    - `grep -c "_FIELD_MAPPING_REQUIRED_TYPES" backend/app/services/feishu_service.py` ≥ 2（定义 + 使用）
    - `grep "'error': 'invalid_field_type'" backend/app/services/feishu_service.py` 命中
    - `FeishuService.create_config` 方法内在 `config = FeishuConfig(` 实例化之前含 `_validate_field_mapping_with_credentials` 调用（通过行号：`grep -n "_validate_field_mapping_with_credentials\|config = FeishuConfig(" backend/app/services/feishu_service.py` 验证 validate 调用行号 < FeishuConfig 实例化行号，对应 create_config 块内）
    - `FeishuService.update_config` 方法内含 `needs_validation`（grep 命中）
    - **不修改 backend/app/api/v1/feishu.py**（本 plan 范围）：`git diff --name-only HEAD backend/app/api/v1/feishu.py` 执行时不显示变更（或者人工确认该文件未 touch）
    - Python import 正常：`python3 -c "from backend.app.services.feishu_service import FeishuService, FeishuConfigValidationError; e = FeishuConfigValidationError({'error': 'invalid_field_type', 'field': 'employee_no', 'expected': 'text', 'actual': 'number'}); print(e.detail)"` 输出 `{'error': 'invalid_field_type', 'field': 'employee_no', 'expected': 'text', 'actual': 'number'}`
    - `python -m pytest backend/tests/test_services/test_feishu_config.py -v` 无回归（若既有测试 mock `create_config` 不走 validate，需要 mock list_bitable_fields 返回 text 类型使 validate 通过 — 这属于测试调整，本 acceptance 允许测试被更新以适配新行为）
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 新增 pytest 测试 test_feishu_leading_zero.py（含本地 fixture + 4 场景 fallback 计数器真信号）</name>
  <files>backend/tests/test_services/test_feishu_leading_zero.py</files>
  <read_first>
    - backend/app/services/feishu_service.py（Task 1/2 完成后的最新版本；**确认 `_extract_cell_value` 对 float 输入原样返回**，以确定下方 Test 1 断言值）
    - backend/tests/test_api/test_feishu_oauth_integration.py（整文件；复用文件顶部本地 `db` fixture 模式）
    - backend/tests/test_services/test_import_partial_success.py（整文件；复用 `_make_test_db()` 模式）
  </read_first>
  <behavior>
    测试覆盖 7 类场景（含 B-10 修复的 4 个 fallback 计数器场景）：
    - Test 1 `test_map_fields_float_employee_no_produces_string_with_decimal`: **W-1 修复** — _map_fields 接受 raw_fields['工号']=2615.0（float），经过 `_extract_cell_value` 返回 2615.0 → `str(2615.0).strip() = '2615.0'`；断言 `mapped['employee_no'] == '2615.0'`。同时断言 warning 被触发
    - Test 2 `test_map_fields_warns_when_employee_no_not_string`: raw value 是 int 12345 时 logger.warning 被触发（caplog 捕获）；断言 `mapped['employee_no'] == '12345'`
    - Test 3 `test_map_fields_preserves_string_employee_no_with_leading_zero`: raw value 已是 str '02615' 时 mapped['employee_no'] == '02615'（前导零保留），不触发 warning
    - Test 4 `test_lookup_employee_fallback_scenario_A_db_has_leading_zero_feishu_missing`: **B-10 场景 A** — DB 存 '02615'、飞书返回 '2615' → exact miss → fallback 通过 lstrip 比对命中 → fallback_counter['count'] == 1
    - Test 5 `test_lookup_employee_exact_match_scenario_B_no_fallback`: **B-10 场景 B** — DB 存 '02615'、飞书返回 '02615' → exact 命中 → fallback_counter['count'] == 0
    - Test 6 `test_lookup_employee_exact_match_scenario_C_no_leading_zero`: **B-10 场景 C** — DB 存 '12345'、飞书返回 '12345' → exact 命中 → fallback_counter['count'] == 0
    - Test 7 `test_lookup_employee_fallback_scenario_D_feishu_extra_zero`: **B-10 场景 D** — DB 存 '02615'、飞书返回 '002615'（罕见：飞书多 0）→ exact miss → fallback lstrip('0')='2615' 与 DB '02615'.lstrip('0')='2615' 匹配 → fallback_counter['count'] == 1
    - Test 8 `test_validate_field_mapping_rejects_number_type_for_employee_no`: mock httpx 返回 employee_no 映射字段 type=2（number），_validate_field_mapping_with_credentials 抛 FeishuConfigValidationError({'error': 'invalid_field_type', 'field': 'employee_no', 'expected': 'text', 'actual': 'number'})
    - Test 9 `test_validate_field_mapping_accepts_text_type_for_employee_no`: mock 返回 type=1（text），不抛异常
    - Test 10 `test_validate_field_mapping_raises_when_field_name_not_in_bitable`: mock 返回的 field_name 集合不含 HR 配置里的 employee_no 映射名，抛 FeishuConfigValidationError，detail['error'] == 'field_not_found_in_bitable'
  </behavior>
  <action>
    创建 `backend/tests/test_services/test_feishu_leading_zero.py`。**本项目 `backend/tests/` 无 conftest.py；fixture 必须在本文件顶部本地定义**。

    文件以 `from __future__ import annotations` 开头。

    **完整文件内容（字面照抄，含本地 fixture）：**

    ```python
    from __future__ import annotations

    import logging
    from unittest.mock import MagicMock, patch

    import pytest
    from sqlalchemy import create_engine
    from sqlalchemy.orm import Session, sessionmaker
    from sqlalchemy.pool import StaticPool

    from backend.app.core.database import Base
    from backend.app.models import load_model_modules
    from backend.app.services.feishu_service import (
        FeishuConfigValidationError,
        FeishuService,
    )

    load_model_modules()


    # ---------------------------------------------------------------------------
    # Local fixtures (no conftest.py exists for this test tree — inline by design)
    # ---------------------------------------------------------------------------

    @pytest.fixture()
    def db_session():
        """In-memory SQLite session shared across threads via StaticPool."""
        engine = create_engine(
            'sqlite://',
            connect_args={'check_same_thread': False},
            poolclass=StaticPool,
        )
        Base.metadata.create_all(engine)
        session_factory = sessionmaker(bind=engine)
        session = session_factory()
        try:
            yield session
        finally:
            session.close()
            Base.metadata.drop_all(engine)
            engine.dispose()


    def _make_service(db_session: Session) -> FeishuService:
        return FeishuService(db_session)


    # ---------------------------------------------------------------------------
    # _map_fields tests (EMPNO-02 / D-08)
    # ---------------------------------------------------------------------------

    def test_map_fields_float_employee_no_produces_string_with_decimal(
        caplog: pytest.LogCaptureFixture,
    ) -> None:
        """W-1 修复：_extract_cell_value 对 float 原样返回 → str(2615.0) = '2615.0'。

        关键断言：value == '2615.0'（不再走 str(int(value)) 丢零路径，得到的是 str(float)）。
        同时 raw_value 为 float 会触发 warning。
        """
        raw_fields = {'工号': 2615.0, '姓名': '张三'}
        field_mapping = {'工号': 'employee_no', '姓名': 'name'}
        with caplog.at_level(logging.WARNING):
            mapped = FeishuService._map_fields(raw_fields, field_mapping)
        assert mapped is not None
        assert mapped['employee_no'] == '2615.0'
        assert '飞书 employee_no 非文本类型' in caplog.text


    def test_map_fields_warns_when_employee_no_not_string(
        caplog: pytest.LogCaptureFixture,
    ) -> None:
        """int 12345 经 _extract_cell_value 原样返回 → str(12345) = '12345'；warning 触发。"""
        raw_fields = {'工号': 12345, '姓名': '李四'}
        field_mapping = {'工号': 'employee_no', '姓名': 'name'}
        with caplog.at_level(logging.WARNING):
            mapped = FeishuService._map_fields(raw_fields, field_mapping)
        assert mapped is not None
        assert mapped['employee_no'] == '12345'
        assert '飞书 employee_no 非文本类型，已强制转字符串（可能已丢失前导零）' in caplog.text


    def test_map_fields_preserves_string_employee_no_with_leading_zero(
        caplog: pytest.LogCaptureFixture,
    ) -> None:
        """raw value 是 str '02615' → 保留前导零；不触发 warning。"""
        raw_fields = {'工号': '02615', '姓名': '王五'}
        field_mapping = {'工号': 'employee_no', '姓名': 'name'}
        with caplog.at_level(logging.WARNING):
            mapped = FeishuService._map_fields(raw_fields, field_mapping)
        assert mapped is not None
        assert mapped['employee_no'] == '02615'
        assert '飞书 employee_no 非文本类型' not in caplog.text


    # ---------------------------------------------------------------------------
    # _lookup_employee fallback counter tests (EMPNO-04 / D-03 / D-04 / B-10 修复)
    # ---------------------------------------------------------------------------

    def test_lookup_employee_fallback_scenario_A_db_has_leading_zero_feishu_missing(
        db_session: Session,
    ) -> None:
        """场景 A：DB 存 '02615'、飞书返回 '2615' → fallback 命中 → 计数 +1。

        验证 B-10 修复：_build_employee_map 不再预填充 stripped 版本，
        此场景下 exact '2615' miss、走 lstrip 比对分支。
        """
        svc = _make_service(db_session)
        emp_map = {'02615': 'emp-uuid-1'}  # 模拟 _build_employee_map 的真实视图（无预填充）
        counter: dict[str, int] = {'count': 0}
        emp_id = svc._lookup_employee(emp_map, '2615', fallback_counter=counter)
        assert emp_id == 'emp-uuid-1'
        assert counter['count'] == 1


    def test_lookup_employee_exact_match_scenario_B_no_fallback(
        db_session: Session,
    ) -> None:
        """场景 B：DB 存 '02615'、飞书返回 '02615' → exact 命中 → 计数 0。"""
        svc = _make_service(db_session)
        emp_map = {'02615': 'emp-uuid-1'}
        counter: dict[str, int] = {'count': 0}
        emp_id = svc._lookup_employee(emp_map, '02615', fallback_counter=counter)
        assert emp_id == 'emp-uuid-1'
        assert counter['count'] == 0


    def test_lookup_employee_exact_match_scenario_C_no_leading_zero(
        db_session: Session,
    ) -> None:
        """场景 C：DB 存 '12345'、飞书返回 '12345' → exact 命中 → 计数 0。"""
        svc = _make_service(db_session)
        emp_map = {'12345': 'emp-uuid-1'}
        counter: dict[str, int] = {'count': 0}
        emp_id = svc._lookup_employee(emp_map, '12345', fallback_counter=counter)
        assert emp_id == 'emp-uuid-1'
        assert counter['count'] == 0


    def test_lookup_employee_fallback_scenario_D_feishu_extra_zero(
        db_session: Session,
    ) -> None:
        """场景 D：DB 存 '02615'、飞书返回 '002615'（飞书多 0）→ fallback 命中 → 计数 +1。

        两端 lstrip('0') 都得到 '2615'，匹配成功。
        """
        svc = _make_service(db_session)
        emp_map = {'02615': 'emp-uuid-1'}
        counter: dict[str, int] = {'count': 0}
        emp_id = svc._lookup_employee(emp_map, '002615', fallback_counter=counter)
        assert emp_id == 'emp-uuid-1'
        assert counter['count'] == 1


    def test_lookup_employee_miss_returns_none_no_counter_change(
        db_session: Session,
    ) -> None:
        """完全找不到匹配 → None，计数 0。"""
        svc = _make_service(db_session)
        emp_map = {'02615': 'emp-uuid-1'}
        counter: dict[str, int] = {'count': 0}
        emp_id = svc._lookup_employee(emp_map, '99999', fallback_counter=counter)
        assert emp_id is None
        assert counter['count'] == 0


    def test_build_employee_map_no_stripped_prefill(db_session: Session) -> None:
        """B-10 修复验证：_build_employee_map 仅含 DB 原始 emp_no，无预填充 stripped 版本。"""
        from backend.app.models.employee import Employee
        emp = Employee(
            id='emp-test-1', employee_no='02615', name='Test',
            department='Eng', job_family='SW', job_level='P5',
        )
        db_session.add(emp)
        db_session.commit()

        svc = _make_service(db_session)
        emp_map = svc._build_employee_map()
        assert emp_map == {'02615': 'emp-test-1'}
        assert '2615' not in emp_map  # 关键：stripped 版本不被预填充


    # ---------------------------------------------------------------------------
    # validate_field_mapping tests (EMPNO-03 / D-01 / D-02)
    # ---------------------------------------------------------------------------

    def test_validate_field_mapping_accepts_text_type_for_employee_no(
        db_session: Session,
    ) -> None:
        """mock httpx.get 返回 type=1（text） — validate 通过不抛。"""
        svc = _make_service(db_session)

        mock_resp = MagicMock()
        mock_resp.json.return_value = {
            'code': 0,
            'data': {
                'items': [
                    {'field_id': 'fld_1', 'field_name': '工号', 'type': 1, 'ui_type': 'Text'},
                ],
                'has_more': False,
                'page_token': None,
            },
        }
        with patch.object(FeishuService, '_ensure_token', return_value='mock-token'), \
             patch('backend.app.services.feishu_service.httpx.get', return_value=mock_resp):
            svc._validate_field_mapping_with_credentials(
                app_id='test-id',
                app_secret='test-secret',
                app_token='test-token',
                table_id='test-table',
                field_mapping={'工号': 'employee_no'},
            )  # 不应抛异常


    def test_validate_field_mapping_rejects_number_type_for_employee_no(
        db_session: Session,
    ) -> None:
        """mock httpx 返回 type=2（number），走真实 validator 逻辑，断言 raise 且 detail 结构严格。"""
        svc = _make_service(db_session)

        mock_resp = MagicMock()
        mock_resp.json.return_value = {
            'code': 0,
            'data': {
                'items': [
                    {'field_id': 'fld_1', 'field_name': '工号', 'type': 2, 'ui_type': 'Number'},
                ],
                'has_more': False,
                'page_token': None,
            },
        }
        with patch.object(FeishuService, '_ensure_token', return_value='mock-token'), \
             patch('backend.app.services.feishu_service.httpx.get', return_value=mock_resp):
            with pytest.raises(FeishuConfigValidationError) as exc_info:
                svc._validate_field_mapping_with_credentials(
                    app_id='test-id',
                    app_secret='test-secret',
                    app_token='test-token',
                    table_id='test-table',
                    field_mapping={'工号': 'employee_no'},
                )
            assert exc_info.value.detail == {
                'error': 'invalid_field_type',
                'field': 'employee_no',
                'expected': 'text',
                'actual': 'number',
            }


    def test_validate_field_mapping_raises_when_field_name_not_in_bitable(
        db_session: Session,
    ) -> None:
        """mock 返回的 field_name 不含配置里的映射名 → field_not_found_in_bitable。"""
        svc = _make_service(db_session)

        mock_resp = MagicMock()
        mock_resp.json.return_value = {
            'code': 0,
            'data': {
                'items': [
                    {'field_id': 'fld_1', 'field_name': '员工姓名', 'type': 1},
                ],
                'has_more': False,
                'page_token': None,
            },
        }
        with patch.object(FeishuService, '_ensure_token', return_value='mock-token'), \
             patch('backend.app.services.feishu_service.httpx.get', return_value=mock_resp):
            with pytest.raises(FeishuConfigValidationError) as exc_info:
                svc._validate_field_mapping_with_credentials(
                    app_id='test-id',
                    app_secret='test-secret',
                    app_token='test-token',
                    table_id='test-table',
                    field_mapping={'工号': 'employee_no'},  # '工号' 不在 bitable 中
                )
            assert exc_info.value.detail['error'] == 'field_not_found_in_bitable'
            assert exc_info.value.detail['field'] == 'employee_no'
    ```

    **关键点：**
    - Test 1 断言 `mapped['employee_no'] == '2615.0'` —— 基于已验证的 `_extract_cell_value` 对 float 原样返回的行为。**若 executor 在实际执行时发现 `_extract_cell_value` 有改动导致返回值不同，应先 Read 该方法确认然后调整断言**
    - B-10 场景测试（A/B/C/D 共 4 个）全部直接构造 emp_map，不走 `_build_employee_map`，以精确测试 `_lookup_employee` 的 fallback 分支 + 计数器
    - 新增 `test_build_employee_map_no_stripped_prefill` 专门回归 B-10 的根因修复
    - `db_session` 使用 in-memory SQLite + StaticPool（无 conftest.py）
    - validator 测试使用完整路径 mock（httpx.get 返回值），验证 detail 结构严格匹配
    - **禁止**新建 `backend/tests/conftest.py`
  </action>
  <acceptance_criteria>
    - 文件存在：`ls backend/tests/test_services/test_feishu_leading_zero.py` 返回 0
    - 文件首行含 `from __future__ import annotations`
    - **文件顶部含本地 `db_session` fixture 定义**：`grep -c "@pytest.fixture" backend/tests/test_services/test_feishu_leading_zero.py` ≥ 1 且 `grep -c "def db_session" backend/tests/test_services/test_feishu_leading_zero.py` ≥ 1
    - **不新增 `backend/tests/conftest.py`**：`ls backend/tests/conftest.py` 返回非 0
    - 以下 11 个 test 函数定义全部存在（grep 逐一字面验证）：
      - `def test_map_fields_float_employee_no_produces_string_with_decimal`
      - `def test_map_fields_warns_when_employee_no_not_string`
      - `def test_map_fields_preserves_string_employee_no_with_leading_zero`
      - `def test_lookup_employee_fallback_scenario_A_db_has_leading_zero_feishu_missing`
      - `def test_lookup_employee_exact_match_scenario_B_no_fallback`
      - `def test_lookup_employee_exact_match_scenario_C_no_leading_zero`
      - `def test_lookup_employee_fallback_scenario_D_feishu_extra_zero`
      - `def test_lookup_employee_miss_returns_none_no_counter_change`
      - `def test_build_employee_map_no_stripped_prefill`
      - `def test_validate_field_mapping_accepts_text_type_for_employee_no`
      - `def test_validate_field_mapping_rejects_number_type_for_employee_no`
      - `def test_validate_field_mapping_raises_when_field_name_not_in_bitable`
    - 运行 `python -m pytest backend/tests/test_services/test_feishu_leading_zero.py -v` 退出码 0，**无** `fixture '...' not found` 错误，全部 PASS
    - 运行 `python -m pytest backend/tests/test_services/test_feishu_service.py backend/tests/test_services/test_feishu_config.py backend/tests/test_services/test_feishu_scheduler.py -v` 无新增失败（既有测试若因签名变化失败，可就地修正适配 instance method 版 _lookup_employee 与新 validate_field_mapping 集成）
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 飞书 API → 系统 | list_bitable_fields 返回的 meta JSON 为不可信输入（来自外部 SaaS） |
| HR 用户 → 飞书配置保存接口 | HR 可提供任意 app_token / table_id / field_mapping；validate 在保存前执行 |
| _lookup_employee fallback counter | 进程内共享 dict，不跨 session；无并发写入风险（单 sync 任务内单线程） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-06 | Tampering / Spoofing | 飞书 list_bitable_fields 返回的 field type 字段 | mitigate | `int(item.get('type', 0))` 强制类型转换 + 白名单匹配（== 1）；未知 type 落入 `'unknown(N)'` 文案；不信任 ui_type |
| T-30-07 | Information Disclosure | FeishuConfigValidationError.detail | mitigate | detail 仅含系统字段名 + 飞书字段名 + 类型名，不含 token / app_secret |
| T-30-08 | DoS | validate_field_mapping 对 bitable 分页拉取 | accept | `page_size=100` + `_rate_limiter.wait_and_acquire()` 已限流；配置保存是低频操作 |
| T-30-09 | Elevation of Privilege | FeishuConfigValidationError 未被 API 层捕获导致 500 | mitigate | Plan 04 负责 API 层 exception handler 将其转 422；本 plan 通过自定义异常类与明确 detail 结构为 Plan 04 提供契约 |
| T-30-13 | Logic Error | _build_employee_map 取消预填充后若某 sync 方法忘记传 fallback_counter 则计数丢失 | mitigate | acceptance_criteria 逐 sync 方法 grep `fallback_counter: dict[str, int] = {'count': 0}` ≥ 5；测试覆盖场景 A/D 确保 fallback 路径可见 |
</threat_model>

<verification>
- [ ] `_map_fields` 源码不再含 `str(int(value))`
- [ ] `_map_fields` raw_value 非 str 时触发 logger.warning（字符串字面含「飞书 employee_no 非文本类型」）
- [ ] `_build_employee_map` 不再含 `emp_map[stripped] = emp_id` 或 `stripped = emp_no.lstrip('0')` 代码（B-10 修复）
- [ ] `_lookup_employee` 方法不再是 `@staticmethod`，签名含 `fallback_counter: dict[str, int] | None = None`
- [ ] `_lookup_employee` fallback 分支遍历 emp_map 每个 key 做 lstrip 比对
- [ ] 5 个 sync_* 方法都在开头创建 `fallback_counter: dict[str, int] = {'count': 0}`，都把计数写入 `sync_log.leading_zero_fallback_count`
- [ ] `FeishuConfigValidationError` 类存在，`.detail` 属性可访问
- [ ] `validate_field_mapping` 与 `_validate_field_mapping_with_credentials` 两个方法都存在
- [ ] `FeishuService.create_config` 与 `FeishuService.update_config` 在保存前调用 validator；update 场景下仅在 `field_mapping`/`bitable_app_token`/`bitable_table_id` 被改时校验
- [ ] 不修改 `backend/app/api/v1/feishu.py`（API 层改动由 Plan 04 负责）
- [ ] 新增 12 个测试全部 PASS
- [ ] `pytest backend/tests/test_services/test_feishu_*.py` 总集无回归
</verification>

<success_criteria>
- `FeishuService._map_fields` 对 employee_no 不再做 int 转换，raw value 非 str 时 logger.warning 告警（EMPNO-02）
- `FeishuService._build_employee_map` 不再预填充 stripped 版本，`_lookup_employee` fallback 分支真正做容忍匹配，计数器反映「飞书源丢前导零」的真实信号（B-10 根因修复 / EMPNO-04）
- `FeishuService.validate_field_mapping` 新方法 + `create_config`/`update_config` 集成：保存配置时强制 employee_no 映射字段为飞书 text 类型（EMPNO-03）
- 5 个 sync_* 方法追踪容忍匹配命中次数，同步结束写入 `FeishuSyncLog.leading_zero_fallback_count`（EMPNO-04）
- 测试覆盖：_map_fields float/int/str 三分支 + warning 捕获 + _lookup_employee 场景 A/B/C/D 计数器真信号 + _build_employee_map 无预填充回归 + validate_field_mapping 三类场景 共 12 个用例
</success_criteria>

<output>
After completion, create `.planning/phases/30-employee-no-leading-zero/30-03-SUMMARY.md` including:
- _map_fields 修改 diff
- _build_employee_map 重构 diff（B-10 根因修复说明）
- _lookup_employee 签名变更说明 + 对既有调用点（5 个 sync_* 方法）的升级摘要
- FeishuConfigValidationError + validate_field_mapping + _validate_field_mapping_with_credentials 完整 API
- 对既有 test_feishu_config.py / test_feishu_service.py 的测试 fixture 调整说明（若有）
- pytest 输出（新 12 个 + 既有 feishu 测试回归）
</output>
</content>
</invoke>