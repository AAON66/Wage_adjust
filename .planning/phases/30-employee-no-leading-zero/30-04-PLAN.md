---
phase: 30-employee-no-leading-zero
plan: 04
plan_number: 4
type: execute
wave: 3
depends_on: [30-03]
files_modified:
  - backend/app/api/v1/feishu.py
  - backend/app/schemas/feishu.py
  - frontend/src/pages/FeishuConfig.tsx
  - frontend/src/services/feishuService.ts
  - frontend/src/types/api.ts
  - frontend/src/components/attendance/SyncStatusCard.tsx
  - backend/tests/test_api/test_feishu_config_validation.py
autonomous: true
requirements: [EMPNO-03, EMPNO-04]
requirements_addressed: [EMPNO-03, EMPNO-04]

must_haves:
  truths:
    - "POST /api/v1/feishu/config 与 PUT /api/v1/feishu/config/{id} 在 FeishuConfigValidationError 抛出时返回 422 + 结构化 body {error: 'invalid_field_type', field: 'employee_no', expected: 'text', actual: '<actual>'}；配置未持久化"
    - "SyncLogRead schema 与 TypeScript SyncLogRead 接口新增 leading_zero_fallback_count 字段，由 _sync_log_to_read 透传"
    - "FeishuConfig.tsx 在收到 invalid_field_type 错误时显示中文错误「工号字段类型必须为文本（当前为 <actual>），请在飞书多维表格中将该字段改为「文本」类型后重试」"
    - "SyncStatusCard.tsx 在 syncStatus.leading_zero_fallback_count > 0 时显示黄色文字「N 条记录通过前导零容忍匹配成功，建议排查飞书源数据格式」（非阻断、非 error）"
    - "所有新增代码通过 tsc --noEmit 和 npm run build（前端）与 pytest（后端）"
    - "后端 API 测试文件顶部本地定义 admin_client / db_session fixture（无 conftest.py）"
  artifacts:
    - path: "backend/app/api/v1/feishu.py"
      provides: "FeishuConfigValidationError → 422 映射"
    - path: "backend/app/schemas/feishu.py"
      provides: "SyncLogRead 新增 leading_zero_fallback_count 字段"
    - path: "frontend/src/types/api.ts"
      provides: "SyncLogRead interface 新增 leading_zero_fallback_count 字段"
    - path: "frontend/src/pages/FeishuConfig.tsx"
      provides: "invalid_field_type 错误展示"
    - path: "frontend/src/components/attendance/SyncStatusCard.tsx"
      provides: "leading_zero_fallback_count > 0 时黄色提示"
    - path: "backend/tests/test_api/test_feishu_config_validation.py"
      provides: "POST /feishu/config invalid_field_type 返回 422 的端到端测试（含本地 fixture）"
  key_links:
    - from: "backend/app/api/v1/feishu.py create_config / update_config"
      to: "FeishuConfigValidationError"
      via: "try/except 映射到 HTTPException(status_code=422, detail=error.detail)"
      pattern: "FeishuConfigValidationError"
    - from: "frontend/src/pages/FeishuConfig.tsx handleSave catch"
      to: "error.response.data.error === 'invalid_field_type'"
      via: "axios 错误响应解析"
      pattern: "invalid_field_type"
    - from: "frontend/src/components/attendance/SyncStatusCard.tsx"
      to: "syncStatus.leading_zero_fallback_count"
      via: "conditional yellow warning render"
      pattern: "leading_zero_fallback_count"
---

<objective>
将 Plan 03 打下的后端 validator 与计数器基础通过 API 与前端 UI 面向用户呈现：

1. **API 层（EMPNO-03）** — `backend/app/api/v1/feishu.py` 的 `create_config` / `update_config` 路由在 `FeishuConfigValidationError` 抛出时返回 422 + 结构化 JSON body
2. **Schema 透传（EMPNO-04）** — `SyncLogRead` schema（后端 Pydantic + 前端 TS interface）新增 `leading_zero_fallback_count: int` 字段，由 `_sync_log_to_read` 映射
3. **前端错误展示（EMPNO-03）** — `FeishuConfig.tsx` 识别 `invalid_field_type` 错误码，显示具体文案「工号字段类型必须为文本（当前为 <actual>）」
4. **前端观测提示（EMPNO-04 / D-04）** — `SyncStatusCard.tsx` 在 `leading_zero_fallback_count > 0` 时以黄色文字显示容忍匹配提示

**UI 位置决策（W-3）：** 经核查 `frontend/src/pages/` 与 `frontend/src/components/attendance/` 目录，**当前无独立的同步日志列表组件**（仅有 `SyncStatusCard.tsx` 显示单条最新状态）。因此 Plan 04 将黄色提示挂载在 `SyncStatusCard.tsx`（HR 观察最新同步健康度的唯一入口）。Phase 31 若引入五类计数器列表页或同步日志历史页，再把该提示迁移/扩展到列表中，本 plan 保持当前最小 UI 改动。

Output：HR 保存一个 `employee_no` 映射到飞书「数字」类型字段的配置时，前端直接报错中断；HR 看同步状态卡片时，能看到容忍匹配的数量作为数据质量信号。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/30-employee-no-leading-zero/30-CONTEXT.md
@.planning/phases/30-employee-no-leading-zero/30-03-PLAN.md
@.planning/codebase/CONVENTIONS.md

@backend/app/api/v1/feishu.py
@backend/app/schemas/feishu.py
@backend/app/main.py
@frontend/src/pages/FeishuConfig.tsx
@frontend/src/services/feishuService.ts
@frontend/src/types/api.ts
@frontend/src/components/attendance/SyncStatusCard.tsx

<interfaces>
<!-- 关键既定契约。Executor 直接使用，无需再去 codebase 翻找。 -->

**Plan 03 在 FeishuService 新增的错误类（应已存在）：**

```python
class FeishuConfigValidationError(ValueError):
    """detail: dict，含 error/field/expected/actual 等字段"""
    def __init__(self, detail: dict) -> None:
        super().__init__(str(detail))
        self.detail = detail
```

**当前 `backend/app/api/v1/feishu.py` 的相关路由**（本 plan 改动目标，位置以函数名边界描述，不引用绝对行号）：

- `create_config` 函数：装饰器 `@router.post('/config', response_model=FeishuConfigRead, status_code=status.HTTP_201_CREATED)`，函数体内调用 `service.create_config(data)`
- `update_config` 函数：装饰器 `@router.put('/config/{config_id}', response_model=FeishuConfigRead)`，函数体内调用 `service.update_config(config_id, data)`
- `_sync_log_to_read` 辅助函数：把 `FeishuSyncLog` ORM 实例映射为 `SyncLogRead` schema

**当前 `SyncLogRead` schema（`backend/app/schemas/feishu.py`）：**

```python
class SyncLogRead(BaseModel):
    model_config = ConfigDict(from_attributes=True)
    id: str
    mode: str
    status: str
    total_fetched: int
    synced_count: int
    updated_count: int
    skipped_count: int
    unmatched_count: int
    failed_count: int
    error_message: Optional[str]
    unmatched_employee_nos: Optional[List[str]]
    started_at: datetime
    finished_at: Optional[datetime]
    triggered_by: Optional[str]
```

**前端 TypeScript `SyncLogRead` interface（`frontend/src/types/api.ts`）：**

```typescript
export interface SyncLogRead {
  id: string;
  mode: string;
  status: 'running' | 'success' | 'failed';
  total_fetched: number;
  synced_count: number;
  updated_count: number;
  skipped_count: number;
  unmatched_count: number;
  failed_count: number;
  error_message: string | null;
  unmatched_employee_nos: string[] | null;
  started_at: string;
  finished_at: string | null;
  triggered_by: string | null;
}
```

**`FeishuConfig.tsx handleSave` 错误处理既有模板：**

```typescript
} catch (err: unknown) {
  if (err instanceof Error) {
    setErrorMessage(err.message);
  } else {
    setErrorMessage('保存失败');
  }
}
```

需要扩展：优先识别 axios 错误的 `err.response.data.detail.error === 'invalid_field_type'` 并显示具体文案。

**`SyncStatusCard.tsx` 现有 unmatched_count > 0 黄色提示模板：**

```tsx
{syncStatus && syncStatus.unmatched_count > 0 ? (
  <p className="mt-2 text-sm" style={{ color: 'var(--color-warning, #FF7D00)' }} title={syncStatus.unmatched_employee_nos?.slice(0, 5).join(', ') ?? ''}>
    {syncStatus.unmatched_count} 条记录因工号不匹配被跳过
  </p>
) : null}
```

新增 `leading_zero_fallback_count > 0` 的黄色文字紧跟在下面，同样 color + `mt-2 text-sm`。

**UI 挂载位置核查结果（W-3）：**

```
frontend/src/components/attendance/ 仅有：
  AttendanceKpiCard.tsx
  FieldMappingTable.tsx
  SyncStatusCard.tsx
```

→ **无独立同步日志列表组件**。Plan 04 将黄色提示挂载 `SyncStatusCard`（当前展示最新同步状态的唯一入口）。Phase 31 引入历史日志列表时可再扩展。

**重要环境事实：本项目 `backend/tests/` 目录下 *不存在* conftest.py**。API 测试 fixture（`admin_client`、`db_session`）必须在测试文件顶部本地定义 —— **禁止新建 conftest.py**。参考现有 `backend/tests/test_api/test_feishu_oauth_integration.py` 的 `db` / `client` fixture 本地定义模式。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 后端 API 层映射 FeishuConfigValidationError 到 422 + 扩展 SyncLogRead schema 与映射</name>
  <files>backend/app/api/v1/feishu.py, backend/app/schemas/feishu.py</files>
  <read_first>
    - backend/app/api/v1/feishu.py（整文件；重点看 `_sync_log_to_read`、`create_config`、`update_config` 三个函数的完整实现）
    - backend/app/schemas/feishu.py（整文件；SyncLogRead 完整定义）
    - backend/app/services/feishu_service.py（Plan 03 完成后；确认 `FeishuConfigValidationError` 可从此导入）
    - backend/app/main.py（整文件；看既定 exception handler + build_error_response 模式，决定如何向前端透传 dict detail）
  </read_first>
  <action>
    **A. 修改 `backend/app/schemas/feishu.py` —— `SyncLogRead` 新增字段：**

    定位：`SyncLogRead` 类定义内部。在既有 `failed_count: int` 字段之后、`error_message: Optional[str]` 之前插入：

    ```python
    leading_zero_fallback_count: int = 0
    ```

    赋默认 0 是为了向后兼容（序列化旧的 FeishuSyncLog 实例时若 ORM 尚未 refresh 能 fallback）。

    **B. 修改 `backend/app/api/v1/feishu.py`：**

    **B1. 在文件顶部 import 区加入：**

    定位：文件顶部既有 `from backend.app.services.feishu_service import FeishuService` 这一行（或类似 import），扩展为：

    ```python
    from backend.app.services.feishu_service import FeishuConfigValidationError, FeishuService
    ```

    **B2. 修改 `_sync_log_to_read` 函数：**

    定位：`_sync_log_to_read` 函数体内部的 `return SyncLogRead(...)` 语句。在 `failed_count=log.failed_count,` 之后加一行：

    ```python
    leading_zero_fallback_count=log.leading_zero_fallback_count,
    ```

    完整 return 块修改后应为：
    ```python
    return SyncLogRead(
        id=log.id,
        mode=log.mode,
        status=log.status,
        total_fetched=log.total_fetched,
        synced_count=log.synced_count,
        updated_count=log.updated_count,
        skipped_count=log.skipped_count,
        unmatched_count=log.unmatched_count,
        failed_count=log.failed_count,
        leading_zero_fallback_count=log.leading_zero_fallback_count,
        error_message=log.error_message,
        unmatched_employee_nos=unmatched,
        started_at=log.started_at,
        finished_at=log.finished_at,
        triggered_by=log.triggered_by,
    )
    ```

    **B3. 修改 `create_config` 路由函数：**

    定位：`create_config` 函数体内部（装饰器 `@router.post('/config', ...)` 下方）。把既有 `config = service.create_config(data)` 调用包进 `try/except` 块。

    修改后的函数完整实现：

    ```python
    @router.post('/config', response_model=FeishuConfigRead, status_code=status.HTTP_201_CREATED)
    def create_config(
        data: FeishuConfigCreate,
        db: Session = Depends(get_db),
        _current_user: User = Depends(require_roles('admin')),
    ) -> FeishuConfigRead:
        """创建飞书配置（仅 admin）。创建后自动重载调度器。

        EMPNO-03 / D-01: 配置保存前会校验飞书端 employee_no 字段类型必须为 text；
        非 text 类型返回 422 + 结构化错误 body。
        """
        service = FeishuService(db)
        try:
            config = service.create_config(data)
        except FeishuConfigValidationError as exc:
            raise HTTPException(
                status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
                detail=exc.detail,
            ) from exc
        try:
            reload_scheduler(config.sync_hour, config.sync_minute, config.sync_timezone)
        except Exception:
            logger.warning('Failed to reload scheduler after config creation', exc_info=True)
        return _config_to_read(config)
    ```

    **B4. 修改 `update_config` 路由函数：**

    定位：`update_config` 函数体内部（装饰器 `@router.put('/config/{config_id}', ...)` 下方）。同样方式 try/except。

    修改后的函数完整实现：

    ```python
    @router.put('/config/{config_id}', response_model=FeishuConfigRead)
    def update_config(
        config_id: str,
        data: FeishuConfigUpdate,
        db: Session = Depends(get_db),
        _current_user: User = Depends(require_roles('admin')),
    ) -> FeishuConfigRead:
        """更新飞书配置（仅 admin）。更新后自动重载调度器。

        EMPNO-03 / D-01: 若 HR 修改了 field_mapping / bitable_app_token / bitable_table_id，
        保存前会重新校验字段类型；非 text 类型返回 422 + 结构化错误 body。
        """
        service = FeishuService(db)
        try:
            config = service.update_config(config_id, data)
        except FeishuConfigValidationError as exc:
            raise HTTPException(
                status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
                detail=exc.detail,
            ) from exc
        try:
            reload_scheduler(config.sync_hour, config.sync_minute, config.sync_timezone)
        except Exception:
            logger.warning('Failed to reload scheduler after config update', exc_info=True)
        return _config_to_read(config)
    ```

    **B5. Exception handler 路径选择（关键）：**

    executor **必须先** Read `backend/app/main.py` 找到 `http_exception_handler`（或等价的 FastAPI `@app.exception_handler(HTTPException)` 注册）确认既定行为：

    - **路径 X**：若既有 handler 对 `HTTPException.detail` 为 dict 时能原样透传（例如 `content=exc.detail` 或 `{'detail': exc.detail}`），保持 B3/B4 的 `raise HTTPException(detail=exc.detail)` 写法
    - **路径 Y**：若既有 handler 一律 `str(exc.detail)`（例如 `build_error_response(status_code, error='http_error', message=str(exc.detail))`），则修改 B3/B4 为：

      ```python
      from fastapi.responses import JSONResponse
      # ...
      except FeishuConfigValidationError as exc:
          return JSONResponse(status_code=422, content=exc.detail)
      ```

    executor 在 PR diff 中必须以 `# Exception handler 透传路径: X / Y` 形式在 catch 块前注释选择理由。

    **禁止：**
    - 不要修改既有 main.py 的 http_exception_handler
    - 不要为 FeishuConfigValidationError 单独注册一个 exception_handler（维持 API 层 try/catch 模式）
  </action>
  <acceptance_criteria>
    - `grep -c "leading_zero_fallback_count: int = 0" backend/app/schemas/feishu.py` 输出 `1`
    - `grep -c "leading_zero_fallback_count=log.leading_zero_fallback_count" backend/app/api/v1/feishu.py` 输出 `1`
    - `grep -c "FeishuConfigValidationError" backend/app/api/v1/feishu.py` ≥ 3（import + 2 个 except 块）
    - `create_config` 与 `update_config` 函数内分别含 `except FeishuConfigValidationError`：`grep -c "except FeishuConfigValidationError" backend/app/api/v1/feishu.py` ≥ 2
    - 执行 `python -m pytest backend/tests/test_api/test_feishu_config_validation.py -v`（见 Task 3）退出码 0
    - 既有 `pytest backend/tests/test_api/ -v` 无非本 plan 引入的失败
    - `python3 -c "from backend.app.schemas.feishu import SyncLogRead; print('leading_zero_fallback_count' in SyncLogRead.model_fields)"` 输出 `True`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 前端 TypeScript 类型扩展 + FeishuConfig.tsx 错误展示 + SyncStatusCard 黄色提示</name>
  <files>frontend/src/types/api.ts, frontend/src/pages/FeishuConfig.tsx, frontend/src/components/attendance/SyncStatusCard.tsx</files>
  <read_first>
    - frontend/src/types/api.ts（整文件；重点看 `SyncLogRead` interface 完整定义）
    - frontend/src/pages/FeishuConfig.tsx（整文件；重点看 `handleSave` 函数的 catch 块）
    - frontend/src/components/attendance/SyncStatusCard.tsx（整文件；看现有 unmatched_count 黄色提示模板的完整结构）
    - frontend/src/services/feishuService.ts（整文件；axios 错误响应结构）
    - frontend/src/services/api.ts（整文件；axios 配置和拦截器处理错误的方式）
  </read_first>
  <action>
    **A. `frontend/src/types/api.ts` 的 `SyncLogRead` interface 新增字段。**

    定位：`SyncLogRead` interface 内部。在既有 `failed_count: number;` 之后、`error_message: string | null;` 之前插入：

    ```typescript
      leading_zero_fallback_count: number;
    ```

    **B. `frontend/src/pages/FeishuConfig.tsx` handleSave 错误处理扩展。**

    定位：`handleSave` 函数体内的 `} catch (err: unknown) {` 块。

    首先确认文件顶部 import 区已引入 axios（如果还未引入，加入）：
    ```typescript
    import axios from 'axios';
    ```

    将现有 catch 块：
    ```typescript
    } catch (err: unknown) {
      if (err instanceof Error) {
        setErrorMessage(err.message);
      } else {
        setErrorMessage('保存失败');
      }
    }
    ```

    替换为：
    ```typescript
    } catch (err: unknown) {
      // EMPNO-03: 优先识别字段类型校验失败的 422 错误，给出具体文案
      if (axios.isAxiosError(err) && err.response?.status === 422) {
        const detail = err.response.data?.detail ?? err.response.data;
        if (detail && typeof detail === 'object' && detail.error === 'invalid_field_type') {
          const actual = detail.actual ?? '未知';
          const field = detail.field ?? 'employee_no';
          if (field === 'employee_no') {
            setErrorMessage(
              `工号字段类型必须为文本（当前为 ${actual}），请在飞书多维表格中将该字段改为「文本」类型后重试`,
            );
            setErrors((prev) => ({
              ...prev,
              field_mapping: '工号字段类型必须为文本',
            }));
          } else {
            setErrorMessage(`字段类型校验失败：${field} 应为文本，当前为 ${actual}`);
          }
          return;
        }
        if (detail && typeof detail === 'object' && detail.error === 'field_not_found_in_bitable') {
          setErrorMessage(
            `飞书多维表格中未找到字段「${detail.feishu_field_name ?? ''}」，请检查字段名是否拼写一致`,
          );
          return;
        }
        if (detail && typeof detail === 'object' && detail.error === 'bitable_fields_fetch_failed') {
          setErrorMessage(
            `无法校验飞书字段类型：${detail.message ?? '请稍后重试'}`,
          );
          return;
        }
      }
      if (err instanceof Error) {
        setErrorMessage(err.message);
      } else {
        setErrorMessage('保存失败');
      }
    }
    ```

    **关键点：**
    - `err.response.data?.detail ?? err.response.data` 兼容两种 FastAPI 响应形态（Task 1 B5 路径 X 或 Y）
    - 用 `axios.isAxiosError` 而不是 `err instanceof AxiosError`
    - 在 setErrorMessage 之后也 setErrors 让 field_mapping 行出现红色校验提示（项目既定 FormErrors 模式）

    **C. `frontend/src/components/attendance/SyncStatusCard.tsx` 新增黄色提示。**

    定位：组件内现有 `unmatched_count > 0` 的 `<p>` 标签（JSX 表达式 `{syncStatus && syncStatus.unmatched_count > 0 ? (...) : null}`）。

    在该 `<p>` 标签后面（即 `) : null}` 之后）紧跟一个新的黄色 `<p>`：

    ```tsx
    {syncStatus && syncStatus.leading_zero_fallback_count > 0 ? (
      <p className="mt-2 text-sm" style={{ color: 'var(--color-warning, #FF7D00)' }}>
        {syncStatus.leading_zero_fallback_count} 条记录通过前导零容忍匹配成功，建议排查飞书源数据格式
      </p>
    ) : null}
    ```

    **关键点：**
    - 字面复用既定「黄色 warning 文本」样式（`color: 'var(--color-warning, #FF7D00)'` + `className="mt-2 text-sm"`）
    - 不加 title / 不加下载按钮 — 计数器是诊断信号，保持简洁
    - 文案严格遵循 CONTEXT.md § specifics 原文：「N 条记录通过前导零容忍匹配成功，建议排查飞书源数据格式」
    - **SyncStatusCard 是当前最新同步状态的唯一展示点**。Phase 31 引入五类计数器列表页或同步日志历史页时，本提示可迁移至列表行。本 plan 保持当前最小 UI 改动（W-3）。

    **D. `frontend/src/services/feishuService.ts` 无需修改** — 既有 `createFeishuConfig` / `updateFeishuConfig` 保留 axios 默认错误传播。

    **禁止：**
    - 不要引入新 npm 依赖
    - 不要创建新组件（可复用既定 `<p>` + `className`）
    - 不要改 SyncLogRead 其他字段顺序 / 类型
    - 不要修改 getSyncLogs / getLatestSyncStatus service 方法
  </action>
  <acceptance_criteria>
    - `grep -c "leading_zero_fallback_count: number;" frontend/src/types/api.ts` 输出 `1`
    - `grep -c "leading_zero_fallback_count" frontend/src/components/attendance/SyncStatusCard.tsx` 输出 `1`
    - 文件含字面字符串 `条记录通过前导零容忍匹配成功，建议排查飞书源数据格式`：`grep "条记录通过前导零容忍匹配成功，建议排查飞书源数据格式" frontend/src/components/attendance/SyncStatusCard.tsx` 命中
    - `grep "invalid_field_type" frontend/src/pages/FeishuConfig.tsx` 命中 ≥ 1
    - `grep "工号字段类型必须为文本" frontend/src/pages/FeishuConfig.tsx` 命中 ≥ 1
    - `cd frontend && npm run lint` 退出码 0（即 `tsc --noEmit` 通过，无 type error）
    - `cd frontend && npm run build` 退出码 0
    - `grep "axios.isAxiosError" frontend/src/pages/FeishuConfig.tsx` 命中
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 后端 API 集成测试 test_feishu_config_validation.py（含本地 admin_client / db_session fixture）</name>
  <files>backend/tests/test_api/test_feishu_config_validation.py</files>
  <read_first>
    - backend/tests/test_api/test_feishu_oauth_integration.py（整文件；**重点复用文件顶部 `db` fixture 与 `client` fixture 的本地定义模式（均以 `@pytest.fixture()` 装饰）** —— 基于 `create_engine('sqlite://', poolclass=StaticPool)` + `create_app()` + `app.dependency_overrides[get_db]`）
    - backend/app/api/v1/feishu.py（Task 1 完成后的最新版本）
    - backend/app/services/feishu_service.py（`FeishuConfigValidationError` 签名）
    - backend/app/models/feishu_sync_log.py（含 leading_zero_fallback_count 字段）
    - backend/app/core/security.py（`create_access_token` 签名，用于生成 admin JWT）
    - backend/app/dependencies.py（`require_roles` / `get_current_user` 依赖链，用于 override admin 身份）
  </read_first>
  <behavior>
    - Test 1 `test_create_config_rejects_invalid_field_type_with_422`: admin 身份 POST /api/v1/feishu/config，mock `FeishuService.create_config` 抛 FeishuConfigValidationError，断言 HTTP 422 + body 含 error=invalid_field_type / field=employee_no / actual=number
    - Test 2 `test_update_config_rejects_invalid_field_type_with_422`: 同 Test 1 但路由是 PUT /api/v1/feishu/config/{id}
    - Test 3 `test_create_config_succeeds_when_validator_passes`: mock validator 跳过，断言 HTTP 非 422
    - Test 4 `test_sync_logs_response_includes_leading_zero_fallback_count`: 创建 FeishuSyncLog(leading_zero_fallback_count=7)，GET /api/v1/feishu/sync-logs 返回 JSON 含该字段值 7
  </behavior>
  <action>
    创建文件 `backend/tests/test_api/test_feishu_config_validation.py`。**本项目 `backend/tests/` 无 conftest.py；fixture 必须在本文件顶部本地定义**（参考 `backend/tests/test_api/test_feishu_oauth_integration.py` 的 `db` / `client` 模式）。

    文件首行 `from __future__ import annotations`。

    **完整文件内容（字面照抄，含本地 fixture）：**

    ```python
    """EMPNO-03 / EMPNO-04 API integration tests.

    Covers POST/PUT /api/v1/feishu/config 422 + GET /api/v1/feishu/sync-logs
    field passthrough for leading_zero_fallback_count.
    """
    from __future__ import annotations

    import uuid
    from datetime import datetime, timezone
    from unittest.mock import patch

    import pytest
    from fastapi.testclient import TestClient
    from sqlalchemy import create_engine
    from sqlalchemy.orm import Session, sessionmaker
    from sqlalchemy.pool import StaticPool

    from backend.app.core.config import Settings
    from backend.app.core.database import Base
    from backend.app.core.security import create_access_token
    from backend.app.main import create_app
    from backend.app.models import load_model_modules
    from backend.app.models.feishu_sync_log import FeishuSyncLog
    from backend.app.models.user import User
    from backend.app.services.feishu_service import FeishuConfigValidationError

    load_model_modules()


    # ---------------------------------------------------------------------------
    # Local fixtures (no conftest.py exists for this test tree — inline by design)
    # ---------------------------------------------------------------------------

    @pytest.fixture()
    def db_session():
        """In-memory SQLite session shared via StaticPool."""
        engine = create_engine(
            'sqlite://',
            connect_args={'check_same_thread': False},
            poolclass=StaticPool,
        )
        Base.metadata.create_all(engine)
        session_factory = sessionmaker(bind=engine)
        session = session_factory()
        try:
            yield session
        finally:
            session.close()
            Base.metadata.drop_all(engine)
            engine.dispose()


    @pytest.fixture()
    def settings():
        return Settings(
            feishu_app_id='cli_test',
            feishu_app_secret='test',
            redis_url='redis://localhost:6379/0',
            jwt_secret_key='test-secret-key-12345',
            database_url='sqlite://',
            feishu_encryption_key='test-enc-key-1234567890123456',
        )


    @pytest.fixture()
    def admin_user(db_session: Session):
        """Seed an admin user for dependency override / JWT generation."""
        user = User(
            id=str(uuid.uuid4()),
            email='admin@test.com',
            hashed_password='x',
            role='admin',
        )
        db_session.add(user)
        db_session.commit()
        db_session.refresh(user)
        return user


    @pytest.fixture()
    def admin_client(db_session: Session, settings, admin_user):
        """FastAPI TestClient with admin JWT + get_db override."""
        app = create_app()
        from backend.app.dependencies import get_app_settings, get_db

        def override_get_db():
            try:
                yield db_session
            finally:
                pass

        def override_get_settings():
            return settings

        app.dependency_overrides[get_db] = override_get_db
        app.dependency_overrides[get_app_settings] = override_get_settings

        jwt = create_access_token(
            admin_user.id,
            role=admin_user.role,
            settings=settings,
            token_version=admin_user.token_version,
        )
        client = TestClient(app)
        client.headers.update({'Authorization': f'Bearer {jwt}'})
        return client


    def _config_create_payload() -> dict:
        return {
            'app_id': 'cli_test',
            'app_secret': 'secret_test',
            'bitable_app_token': 'token_test',
            'bitable_table_id': 'tbl_test',
            'field_mapping': [
                {'feishu_field': '工号', 'system_field': 'employee_no'},
            ],
            'sync_hour': 6,
            'sync_minute': 0,
            'sync_timezone': 'Asia/Shanghai',
        }


    # ---------------------------------------------------------------------------
    # Tests
    # ---------------------------------------------------------------------------

    def test_create_config_rejects_invalid_field_type_with_422(
        admin_client: TestClient,
    ) -> None:
        with patch(
            'backend.app.api.v1.feishu.FeishuService.create_config',
            side_effect=FeishuConfigValidationError({
                'error': 'invalid_field_type',
                'field': 'employee_no',
                'expected': 'text',
                'actual': 'number',
            }),
        ):
            resp = admin_client.post('/api/v1/feishu/config', json=_config_create_payload())
        assert resp.status_code == 422
        body = resp.json()
        payload = body.get('detail') if isinstance(body.get('detail'), dict) else body
        assert payload.get('error') == 'invalid_field_type'
        assert payload.get('field') == 'employee_no'
        assert payload.get('expected') == 'text'
        assert payload.get('actual') == 'number'


    def test_update_config_rejects_invalid_field_type_with_422(
        admin_client: TestClient,
    ) -> None:
        with patch(
            'backend.app.api.v1.feishu.FeishuService.update_config',
            side_effect=FeishuConfigValidationError({
                'error': 'invalid_field_type',
                'field': 'employee_no',
                'expected': 'text',
                'actual': 'number',
            }),
        ):
            resp = admin_client.put(
                '/api/v1/feishu/config/test-config-id',
                json={
                    'field_mapping': [{'feishu_field': '工号', 'system_field': 'employee_no'}],
                },
            )
        assert resp.status_code == 422
        body = resp.json()
        payload = body.get('detail') if isinstance(body.get('detail'), dict) else body
        assert payload.get('error') == 'invalid_field_type'
        assert payload.get('field') == 'employee_no'


    def test_create_config_succeeds_when_validator_passes(
        admin_client: TestClient,
    ) -> None:
        """Validator 通过时路由正常返回非 422。"""
        with patch(
            'backend.app.services.feishu_service.FeishuService._validate_field_mapping_with_credentials',
            return_value=None,
        ), patch(
            'backend.app.api.v1.feishu.reload_scheduler',
            return_value=None,
        ), patch(
            'backend.app.services.feishu_service.FeishuService._ensure_token',
            return_value='mock-token',
        ):
            resp = admin_client.post('/api/v1/feishu/config', json=_config_create_payload())
        # Validator 不是错误源；期望非 422
        assert resp.status_code != 422


    def test_sync_logs_response_includes_leading_zero_fallback_count(
        admin_client: TestClient, db_session: Session,
    ) -> None:
        log = FeishuSyncLog(
            mode='full',
            status='success',
            total_fetched=100,
            synced_count=100,
            updated_count=0,
            skipped_count=0,
            unmatched_count=0,
            failed_count=0,
            leading_zero_fallback_count=7,
            started_at=datetime.now(timezone.utc),
            finished_at=datetime.now(timezone.utc),
        )
        db_session.add(log)
        db_session.commit()

        resp = admin_client.get('/api/v1/feishu/sync-logs?limit=10')
        assert resp.status_code == 200
        logs = resp.json()
        matched = [entry for entry in logs if entry.get('id') == log.id]
        assert len(matched) == 1
        assert matched[0]['leading_zero_fallback_count'] == 7
    ```

    **关键点：**
    - 所有 fixture（`db_session`、`settings`、`admin_user`、`admin_client`）均在本文件顶部本地定义 —— **无 conftest.py 依赖**
    - `admin_client` 通过 `app.dependency_overrides[get_db]` 复用 `db_session`，保证 TestClient 和测试断言看到同一个数据库
    - Test 1/2 `patch` 目标直指 API 路由文件里 import 的 `FeishuService`（`backend.app.api.v1.feishu.FeishuService` 命名空间）
    - Test 3 不强制断言 201（DB 层可能有其他约束）；只要非 422 就证明 validator 不是错误源
    - Test 4 是 schema 透传的端到端验证（EMPNO-04 observability）
    - **禁止**新建 `backend/tests/conftest.py`
  </action>
  <acceptance_criteria>
    - 文件存在：`ls backend/tests/test_api/test_feishu_config_validation.py` 返回 0
    - 首行字面含 `from __future__ import annotations`
    - **文件顶部含本地 `db_session` 和 `admin_client` fixture 定义**：
      - `grep -c "@pytest.fixture" backend/tests/test_api/test_feishu_config_validation.py` ≥ 4（db_session / settings / admin_user / admin_client）
      - `grep -c "def db_session" backend/tests/test_api/test_feishu_config_validation.py` ≥ 1
      - `grep -c "def admin_client" backend/tests/test_api/test_feishu_config_validation.py` ≥ 1
    - **不新增 `backend/tests/conftest.py`**：`ls backend/tests/conftest.py` 返回非 0
    - 4 个 test 函数定义均存在（grep 逐一验证）：
      - `def test_create_config_rejects_invalid_field_type_with_422`
      - `def test_update_config_rejects_invalid_field_type_with_422`
      - `def test_create_config_succeeds_when_validator_passes`
      - `def test_sync_logs_response_includes_leading_zero_fallback_count`
    - 运行 `python -m pytest backend/tests/test_api/test_feishu_config_validation.py -v` 退出码 0，**无** `fixture '...' not found` 错误，全部 PASS
    - 每个 test 至少一个 `assert`（grep `assert` 命中 ≥ 8）
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FeishuConfigValidationError.detail → HTTP 响应 body | detail 字段内容经由 API 直接透传，需确保不含敏感信息 |
| Axios response.data.detail → 前端 UI 文案 | 前端信任服务端 error/field/actual 字符串，渲染入 DOM 文本节点（不 innerHTML） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-10 | Information Disclosure | FeishuConfigValidationError.detail 透传 | mitigate | Plan 03 已约束 detail 仅含字段名 + 类型名，不含 token / secret；Plan 04 透传此结构不展开其他 server 信息 |
| T-30-11 | XSS | FeishuConfig.tsx setErrorMessage 渲染 detail.actual | mitigate | React 默认文本节点转义；字符串来自后端白名单（_FEISHU_FIELD_TYPE_NAMES），不可能含 `<script>` |
| T-30-12 | CSRF / Authz | PUT /feishu/config/{id} 422 响应 | accept | 既有 `require_roles('admin')` 守门 + JWT；本 plan 未新增入口 |
</threat_model>

<verification>
- [ ] POST /api/v1/feishu/config 在 FeishuConfigValidationError 时返回 HTTP 422，body 含 error=invalid_field_type / field=employee_no / expected=text / actual=number
- [ ] PUT /api/v1/feishu/config/{id} 同上
- [ ] GET /api/v1/feishu/sync-logs 返回 JSON 的每条 log 含 `leading_zero_fallback_count` 字段（integer，未设置的旧数据回读为 0）
- [ ] 前端 SyncStatusCard 在 leading_zero_fallback_count > 0 时渲染黄色文字「N 条记录通过前导零容忍匹配成功，建议排查飞书源数据格式」
- [ ] 前端 FeishuConfig handleSave 在 422 且 error=invalid_field_type 时显示「工号字段类型必须为文本（当前为 <actual>）...」
- [ ] `cd frontend && npm run lint && npm run build` 无错误
- [ ] 新增 4 个 API 测试 PASS（使用本地 fixture，无 `fixture not found` 错误）
- [ ] 既有 feishu 相关测试（pytest backend/tests/test_api/ pytest backend/tests/test_services/test_feishu_*.py）无回归
</verification>

<success_criteria>
- HR 在 FeishuConfig.tsx 页面保存一个「员工工号」映射到「数字」类型飞书字段的配置时，看到红色错误「工号字段类型必须为文本（当前为 number），请在飞书多维表格中将该字段改为「文本」类型后重试」，配置未持久化
- HR 在同步状态卡片（SyncStatusCard，AttendanceManagement 页面）能看到 `leading_zero_fallback_count > 0` 时的黄色提示。Phase 31 引入同步日志列表页时再迁移
- API 层结构化错误契约（422 + {error, field, expected, actual}）稳定，前端可按 error code 路由到具体文案
</success_criteria>

<output>
After completion, create `.planning/phases/30-employee-no-leading-zero/30-04-SUMMARY.md` including:
- backend/app/api/v1/feishu.py diff（exception handling 选择的路径 X/Y + SyncLogRead 透传）
- backend/app/schemas/feishu.py diff
- frontend/src/pages/FeishuConfig.tsx handleSave 新错误处理 diff
- frontend/src/components/attendance/SyncStatusCard.tsx 黄色提示 diff
- frontend/src/types/api.ts 字段新增
- pytest 输出（新 4 个测试 + 既有 feishu test_api 回归）
- npm run lint / npm run build 输出片段
- Phase 30 整体完成后的 4 条 Success Criteria 自验证（对应 ROADMAP.md § Phase 30）
</output>
</content>
</invoke>