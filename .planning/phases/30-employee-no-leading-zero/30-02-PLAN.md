---
phase: 30-employee-no-leading-zero
plan: 02
plan_number: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/import_service.py
  - backend/tests/test_services/test_import_leading_zero.py
autonomous: true
requirements: [EMPNO-01, EMPNO-02]
requirements_addressed: [EMPNO-01, EMPNO-02]

must_haves:
  truths:
    - "下载的 Excel 模板（employees / certifications / performance_grades / salary_adjustments 四种类型）的「员工工号」列 cell.number_format == '@' 且覆盖表头 + 示例行 + 其后至少 100 行预设空行"
    - "employees / salary_adjustments 模板的「直属上级工号」列同样 cell.number_format == '@'（100+ 行）"
    - "certifications 模板的「认证类型」、「认证阶段」列同样 cell.number_format == '@'（100+ 行）"
    - "employees 模板的示例行「员工工号」改为 '02651'；其他三种模板示例行「员工工号」也改为 '02651'"
    - "ImportService._dispatch_import 对 employee_no / manager_employee_no 列值做 format sanity check：匹配正则 ^\\d+\\.0$ 时该行进入 failed_rows，错误消息含「第 N 行员工工号列格式异常（疑似丢失前导零）」和「请在 Excel 中将该列改为「文本」格式后重新上传，或从系统重新下载最新模板」"
    - "sanity check 的 row_index 语义：pandas 0-based index 加 1 = 数据行号（不含 Excel 表头行），与既有 _import_* 的 row_index 口径完全一致"
    - "pytest 覆盖模板 cell.number_format、读入端 `'1234.0'` 模式检测、示例行工号字符串三类场景；测试文件顶部本地定义 db_session fixture（无 conftest.py）"
  artifacts:
    - path: "backend/app/services/import_service.py"
      provides: "扩展的 build_template_xlsx 与 _load_table / _dispatch_import 校验"
      min_lines: 50
    - path: "backend/tests/test_services/test_import_leading_zero.py"
      provides: "EMPNO-01/02 回归测试（含本地 fixture）"
      min_lines: 100
  key_links:
    - from: "build_template_xlsx"
      to: "cell.number_format = '@'"
      via: "openpyxl"
      pattern: "cell\\.number_format\\s*=\\s*'@'"
    - from: "_dispatch_import"
      to: "failed_rows with 格式异常 message"
      via: "正则 ^\\d+\\.0$ 检测"
      pattern: "格式异常（疑似丢失前导零）"
---

<objective>
在 `ImportService` 两端闭合工号前导零的 Excel 链路：

1. **模板下发端（EMPNO-01 / D-09 / D-10）** — `build_template_xlsx` 对所有导入类型的工号列（以及 certifications 的认证类型/阶段等短字符串列）统一设 `cell.number_format = '@'`，覆盖表头 + 示例行 + 至少 100 行预设空行；示例行工号改为 `'02651'` 以直观展示前导零。
2. **读入端（EMPNO-02 / D-06 / D-07）** — `_dispatch_import` 在 `pd.read_excel(dtype=str)` + `_normalize_columns` 之后，对 `employee_no` / `manager_employee_no` 等关键业务键列做 format sanity check：若该行值匹配 `^\d+\.0$` 模式（float 被 stringified），该行进入 `failed_rows` 且错误消息含可执行补救指引。**row_index 语义沿用既有约定 — pandas 0-based index 加 1 = 数据行号（不含 Excel 表头）**。

目的：HR 下次下载模板时，Excel 会把工号列识别为文本；即使 HR 用老模板或手动粘贴进来的文件工号列被错识别为数字，读入端也会直接把该行标红并告诉 HR「请改成文本格式或从系统重新下载最新模板」。

**row_index 语义说明（重要）：** 既有 `_import_employees` / `_import_certifications` / `_import_performance_grades` / `_import_salary_adjustments` 均使用 `int(index) + 1` 作为 row_index（即 pandas 0-based → 1-based，**不含 Excel 表头行**）。本 plan 的 sanity check 必须沿用**同一口径**以保持一致性。HR 在 Excel 对照时需加 1（例：row_index=2 对应 Excel 第 3 行）— 这是历史 UX 约定，本 phase 不扩大改动。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/30-employee-no-leading-zero/30-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.planning/codebase/CONVENTIONS.md

@backend/app/services/import_service.py

<interfaces>
<!-- 关键既定契约。Executor 直接使用，无需再去 codebase 翻找。 -->

ImportService 类常量（import_service.py `SUPPORTED_TYPES` / `REQUIRED_COLUMNS` / `COLUMN_ALIASES` 区域）：

```python
class ImportService:
    SUPPORTED_TYPES = {'employees', 'certifications', 'performance_grades', 'salary_adjustments'}
    MAX_ROWS = 5000
    REQUIRED_COLUMNS = {
        'employees': ['employee_no', 'name', 'department', 'job_family', 'job_level'],
        'certifications': ['employee_no', 'certification_type', 'certification_stage', 'bonus_rate', 'issued_at'],
        'performance_grades': ['employee_no', 'year', 'grade'],
        'salary_adjustments': ['employee_no', 'adjustment_date', 'adjustment_type'],
    }
    COLUMN_ALIASES = {
        'employees': {'员工工号': 'employee_no', ..., '直属上级工号': 'manager_employee_no'},
        'certifications': {'员工工号': 'employee_no', '认证类型': 'certification_type', '认证阶段': 'certification_stage', ...},
        'performance_grades': {'员工工号': 'employee_no', '年度': 'year', '绩效等级': 'grade'},
        'salary_adjustments': {'员工工号': 'employee_no', '调薪日期': 'adjustment_date', ...},
    }
```

`build_template_xlsx` 现有 headers 与示例行：

| 类型 | headers（列序即顺序） | 现示例行「员工工号」 |
|------|-----------------|--------------------|
| employees | ['员工工号','员工姓名','身份证号','所属部门','下属部门','所属公司','岗位族','岗位级别','在职状态','直属上级工号'] | 'EMP-1001' |
| certifications | ['员工工号','认证类型','认证阶段','补贴比例','发证时间','到期时间'] | 'EMP-1001' |
| performance_grades | ['员工工号','年度','绩效等级'] | 'EMP-1001' |
| salary_adjustments | ['员工工号','调薪日期','调薪类型','调薪金额'] | 'EMP-1001' |

**现有 `_load_table` 方法**（函数签名：`def _load_table(self, file_name: str, raw_bytes: bytes) -> pd.DataFrame`），xlsx 分支核心代码：
```python
if suffix in {'xlsx', 'xls'}:
    return pd.read_excel(io.BytesIO(raw_bytes), engine='openpyxl', dtype=str).fillna('')
```

**现有 `_dispatch_import` 方法**（签名：`def _dispatch_import(self, import_type: str, dataframe: pd.DataFrame) -> list[dict[str, object]]`），流程：
1. `self._normalize_columns(import_type, dataframe)` — 重命名中文列为系统字段名
2. 检查 `REQUIRED_COLUMNS[import_type]` 缺列 → raise `ValueError`
3. 根据 import_type 派发到 `_import_employees` / `_import_certifications` / `_import_performance_grades` / `_import_salary_adjustments`

**现有 `_import_*` 方法的 row_index 约定（从代码实际确认）：**
所有 `_import_*` 方法内部使用 `for index, row in dataframe.iterrows():` + `int(index) + 1` 作为 row_index。例如 `_import_employees` 早期片段形如：
```python
for index, row in dataframe.iterrows():
    try:
        with self.db.begin_nested():
            employee_no = str(row['employee_no']).strip()
            ...
    except Exception as exc:
        results.append({'row_index': int(index) + 1, 'status': 'failed', ...})
```
语义：`row_index = pandas_idx + 1`，表示「数据行号」（Excel 第 1 行是表头，数据从第 2 行起；pandas_idx=0 对应 Excel 第 2 行，row_index=1）。

**既有错误收集 dict 形状：** `{'row_index': int, 'status': 'success'|'failed', 'message': str, ...}`。

openpyxl 文本格式设置既定写法：
```python
from openpyxl.utils import get_column_letter
for row_idx in range(1, TEMPLATE_TEXT_PREFILL_ROWS + 1):  # 包含表头行
    cell = ws.cell(row=row_idx, column=text_col_idx)
    cell.number_format = '@'
```

正则（D-07）：`LEADING_ZERO_LOST_PATTERN = re.compile(r'^\d+\.0$')`

**重要环境事实：本项目 `backend/tests/` 目录下 *不存在* conftest.py**。新测试文件必须在文件顶部本地定义 `db_session` fixture —— **禁止新建 conftest.py**。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 改造 build_template_xlsx 统一工号列文本格式 + 示例行改为 '02651'</name>
  <files>backend/app/services/import_service.py</files>
  <read_first>
    - backend/app/services/import_service.py（整文件；重点看 `build_template_xlsx` 方法的完整实现 + `SUPPORTED_TYPES` / `COLUMN_ALIASES` 数据结构）
    - .planning/phases/30-employee-no-leading-zero/30-CONTEXT.md § D-09 / D-10（文本格式范围与示例行决策原文）
    - .planning/research/STACK.md § STACK-01（cell.number_format = '@' 技术方案）
  </read_first>
  <action>
    在 `backend/app/services/import_service.py` 的 `ImportService.build_template_xlsx` 方法中做以下修改（所有代码位置以 **类成员/方法边界** 描述，不依赖行号）：

    **1. 在 `ImportService` 类级别新增常量。**

    位置：放在现有 `COLUMN_ALIASES` 类变量的定义块之后、`__init__` 方法之前。

    ```python
    # D-09: 模板工号列预设文本格式的预填充行数（覆盖表头 + 示例 + 至少 100 行空行，
    # 避免 HR 新增数据时 Excel 重新识别列类型）。
    TEMPLATE_TEXT_PREFILL_ROWS = 105

    # D-09: 每种 import_type 下需要强制 cell.number_format = '@' 的列名（系统字段名）
    # D-10: employees 与 salary_adjustments 的 manager_employee_no 也属于工号类
    # D-09: certifications 的 certification_type / certification_stage 是短字符串，同样防误识别
    TEMPLATE_TEXT_COLUMNS = {
        'employees': ['employee_no', 'manager_employee_no'],
        'certifications': ['employee_no', 'certification_type', 'certification_stage'],
        'performance_grades': ['employee_no'],
        'salary_adjustments': ['employee_no'],
    }
    ```

    **2. 修改 `build_template_xlsx` 方法内每种类型的 `example` 行 — 所有工号示例改为 `'02651'`（D-10 要求）。**

    在方法内部的 `if normalized_type == 'employees'` / `elif ... 'certifications'` / `elif ... 'performance_grades'` / `elif ... 'salary_adjustments'` 分支内，把 `example` 列表中**工号位置的 `'EMP-1001'`** 替换为 `'02651'`。employees 类型的 `manager_employee_no`（最后一列）改为 `'02650'`。

    期望结果：
    ```python
    if normalized_type == 'employees':
        headers = ['员工工号', '员工姓名', '身份证号', '所属部门', '下属部门', '所属公司', '岗位族', '岗位级别', '在职状态', '直属上级工号']
        example = ['02651', '张小明', '310101199001010123', '产品技术中心', '后端平台组', '示例科技', '平台研发', 'P5', 'active', '02650']
    elif normalized_type == 'certifications':
        headers = ['员工工号', '认证类型', '认证阶段', '补贴比例', '发证时间', '到期时间']
        example = ['02651', 'ai_skill', 'advanced', '0.02', '2026-01-15T00:00:00+00:00', '']
    elif normalized_type == 'performance_grades':
        headers = ['员工工号', '年度', '绩效等级']
        example = ['02651', '2025', 'B']
    elif normalized_type == 'salary_adjustments':
        headers = ['员工工号', '调薪日期', '调薪类型', '调薪金额']
        example = ['02651', '2025-06-01', '年度调薪', '2000']
    ```

    **3. 在 `build_template_xlsx` 方法内、写完 headers 与 example 行之后、`buf = io.BytesIO()` 之前，新增一段将文本类列的 cell.number_format 设为 `'@'` 的循环（覆盖 1 ~ TEMPLATE_TEXT_PREFILL_ROWS 行）：**

    ```python
    # D-09: 对文本类列（工号、认证类型/阶段等）预填充 number_format='@'，
    # 覆盖表头 + 示例 + 103 行空行 = 105 行，确保 HR 录入时 Excel 不会重新识别列类型
    from openpyxl.utils import get_column_letter  # noqa: F401 (供需要时使用)
    text_column_system_names = self.TEMPLATE_TEXT_COLUMNS.get(normalized_type, [])
    alias_map = self.COLUMN_ALIASES.get(normalized_type, {})
    # system_field -> chinese_header 反向映射
    reverse_alias = {v: k for k, v in alias_map.items()}
    for system_name in text_column_system_names:
        header_label = reverse_alias.get(system_name)
        if header_label is None:
            continue
        try:
            col_idx = headers.index(header_label) + 1
        except ValueError:
            continue
        for row_idx in range(1, self.TEMPLATE_TEXT_PREFILL_ROWS + 1):
            cell = ws.cell(row=row_idx, column=col_idx)
            cell.number_format = '@'
    ```

    **4. 保留原有的列宽设置逻辑（`header_font`/`header_fill`/`header_alignment` 块）不变。**

    **严禁：**
    - 不要硬编码 105 为魔法数字到 for 循环里 — 必须引用 `self.TEMPLATE_TEXT_PREFILL_ROWS`
    - 不要删除既有的 header 样式逻辑
    - 不要改示例行以外的 headers 列表（仅示例值与模板前导零格式是 D-09/D-10 要求的改动）
    - 不要引入新依赖（openpyxl 已在 requirements.txt）
  </action>
  <acceptance_criteria>
    - `grep -c "TEMPLATE_TEXT_PREFILL_ROWS = 105" backend/app/services/import_service.py` 输出 `1`
    - `grep -c "TEMPLATE_TEXT_COLUMNS = {" backend/app/services/import_service.py` 输出 `1`
    - 文件含严格 4 处 example 起始值均为 `'02651'`：grep `"\['02651'," backend/app/services/import_service.py` 命中 ≥ 4 次
    - `grep "cell.number_format = '@'" backend/app/services/import_service.py` 命中 ≥ 1 次
    - Python 可 import：`python3 -c "from backend.app.services.import_service import ImportService; print(ImportService.TEMPLATE_TEXT_PREFILL_ROWS)"` 输出 `105`
    - Python 可实际生成四种模板：模板 xlsx 字节流 > 5000 字节且 openpyxl 能读回
    - 模板读回后，employees 模板 A1 行的 header 为 `'员工工号'` 且 A2 单元格值 = `'02651'` 且 `cell.number_format == '@'`；performance_grades / certifications / salary_adjustments 同样
    - employees 模板第 1-105 行 A 列（工号）所有 cell 的 number_format 都为 `'@'`；employees 模板 J 列（直属上级工号）同
    - certifications 模板第 1-105 行 B 列（认证类型）cell.number_format == `'@'`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: _dispatch_import 增加 format sanity check（row_index 沿用既有口径）</name>
  <files>backend/app/services/import_service.py</files>
  <read_first>
    - backend/app/services/import_service.py（整文件；重点阅读 `_load_table` / `_dispatch_import` / `_normalize_columns` / `_detect_cert_error_column` / `_import_employees`/`_import_certifications`/`_import_performance_grades`/`_import_salary_adjustments` 的完整实现 —— **确认所有 `_import_*` 使用 `int(index) + 1` 作为 row_index**）
    - backend/app/services/import_service.py 文件开头 import 区（确认 `re` 是否已 import）
    - .planning/phases/30-employee-no-leading-zero/30-CONTEXT.md § D-06 / D-07（sanity check 错误消息原文）
  </read_first>
  <action>
    在 `backend/app/services/import_service.py` 做以下修改。**所有代码位置以 类成员/方法边界 描述 —— 禁止引用绝对行号。**

    **1. 在文件开头 import 区加入 `re` 模块（如果还没 import）：**
    ```python
    import re
    ```
    位置：紧跟既有标准库 import 之后、第三方 import 之前。

    **2. 在 `ImportService` 类级别新增常量。**

    位置：放在 Task 1 新增的 `TEMPLATE_TEXT_COLUMNS` 类变量之后、`__init__` 方法之前。

    ```python
    # D-07: 检测 Excel 工号列被错识别为数字后 pandas dtype=str 得到 '1234.0' 形态
    _LEADING_ZERO_LOST_PATTERN = re.compile(r'^\d+\.0$')

    # D-07: 需要跑 format sanity check 的「关键业务键」列（系统字段名）
    # 跨所有 import_type 适用；per-type 通过 REQUIRED_COLUMNS 进一步过滤
    _EMPLOYEE_NO_KEY_COLUMNS = frozenset({'employee_no', 'manager_employee_no'})
    ```

    **3. 新增私有辅助方法 `_detect_leading_zero_loss_rows`。**

    位置：放在 `_detect_cert_error_column` 方法的 `return ''` 之后（该方法体闭合之外），在 `_import_employees` 方法定义之前。

    ```python
    @classmethod
    def _detect_leading_zero_loss_rows(cls, dataframe: pd.DataFrame) -> dict[int, tuple[int, str]]:
        """D-07: 扫描 dataframe 中的关键业务键列，返回 {pandas_idx: (row_index, column_name)} 映射。

        row_index 语义：pandas 0-based index 加 1 = 数据行号（不含 Excel 表头）。
        **与既有 _import_* 方法的 row_index 口径完全一致**（HR 对照 Excel 时需加 1 跳过表头行）。
        值匹配 '^\\d+\\.0$' 视为「工号列格式异常（疑似丢失前导零）」。
        """
        bad_rows: dict[int, tuple[int, str]] = {}
        for col_name in cls._EMPLOYEE_NO_KEY_COLUMNS:
            if col_name not in dataframe.columns:
                continue
            for pandas_idx, value in dataframe[col_name].items():
                if isinstance(value, str) and cls._LEADING_ZERO_LOST_PATTERN.match(value.strip()):
                    row_index = int(pandas_idx) + 1  # 与 _import_* 口径一致
                    if pandas_idx not in bad_rows:
                        bad_rows[pandas_idx] = (row_index, col_name)
        return bad_rows
    ```

    **4. 修改 `_dispatch_import` 方法。**

    位置：方法内部，在既有 `self._normalize_columns(...)` 调用之后、`missing = [column for column in required if column not in dataframe.columns]` 检查之后、派发到具体 `_import_*` 之前，插入 sanity check。

    **关键：过滤坏行时禁止 `reset_index(drop=True)` — 必须保留原 pandas index，这样后续 `_import_*` 内的 `int(index) + 1` 仍然对应原始 Excel 数据行号，不会偏移。**

    新版 `_dispatch_import` 完整实现（替换既有实现）：

    ```python
    def _dispatch_import(self, import_type: str, dataframe: pd.DataFrame) -> list[dict[str, object]]:
        # row_index 语义：pandas DataFrame 0-based index 加 1 = 数据行号（不含 Excel 表头）。
        # 所有 _import_* 方法均沿用此口径。HR 对照 Excel 时需加 1 跳过表头行。
        dataframe = self._normalize_columns(import_type, dataframe)
        required = self.REQUIRED_COLUMNS[import_type]
        missing = [column for column in required if column not in dataframe.columns]
        if missing:
            missing_labels = '、'.join(self._label_for_column(column) for column in missing)
            raise ValueError(f'缺少必填列：{missing_labels}。请重新下载最新模板后填写。')

        # D-07: format sanity check —— 在派发到具体 _import_* 前检测前导零丢失
        bad_rows = self._detect_leading_zero_loss_rows(dataframe)
        results: list[dict[str, object]] = []
        if bad_rows:
            for pandas_idx in sorted(bad_rows.keys()):
                row_index, col_name = bad_rows[pandas_idx]
                col_label = self._label_for_column(col_name)
                results.append({
                    'row_index': row_index,
                    'status': 'failed',
                    'error_column': col_label,
                    'message': (
                        f'第 {row_index} 行{col_label}列格式异常（疑似丢失前导零）。'
                        f'请在 Excel 中将该列改为「文本」格式后重新上传，或从系统重新下载最新模板。'
                    ),
                })
            # 用 bool mask 丢弃坏行，**保留原 pandas index**（禁止 reset_index）
            bad_mask = dataframe.index.isin(bad_rows.keys())
            dataframe = dataframe.loc[~bad_mask].copy()

        if import_type == 'employees':
            results.extend(self._import_employees(dataframe))
        elif import_type == 'performance_grades':
            results.extend(self._import_performance_grades(dataframe))
        elif import_type == 'salary_adjustments':
            results.extend(self._import_salary_adjustments(dataframe))
        else:
            results.extend(self._import_certifications(dataframe))
        return results
    ```

    **5. （可选但推荐）在 `_dispatch_import` 方法顶部或类级别 docstring 中加入 row_index 语义注释：**

    ```python
    # row_index 语义（全局约定）：pandas DataFrame 0-based index 加 1 = 数据行号（不含 Excel 表头）。
    # HR 对照 Excel 时需加 1（row_index=2 对应 Excel 第 3 行）。Phase 30 内不扩大改动。
    ```

    **关键点：**
    - `row_index = pandas_idx + 1`，与既有 `_import_employees/_import_certifications/_import_performance_grades/_import_salary_adjustments` 中 `int(index) + 1` 完全一致
    - `dataframe.loc[~bad_mask].copy()` **保留原 pandas index**，后续 `_import_*` 以 `dataframe.iterrows()` 遍历时拿到的 index 仍是原始值，`int(index) + 1` 与 sanity check 的 row_index 口径一致
    - 不可让 sanity check 抛异常；让坏行进 `failed_rows`，其他行继续处理（部分成功）— 和既有 audit / result_summary 模式一致
    - 消息文案字面必须含「格式异常（疑似丢失前导零）」 + 「请在 Excel 中将该列改为「文本」格式后重新上传」+「或从系统重新下载最新模板」三段（CONTEXT.md D-07 原文）
  </action>
  <acceptance_criteria>
    - `grep -c "_LEADING_ZERO_LOST_PATTERN = re.compile" backend/app/services/import_service.py` 输出 `1`
    - `grep -c "_EMPLOYEE_NO_KEY_COLUMNS = frozenset" backend/app/services/import_service.py` 输出 `1`
    - `grep -c "def _detect_leading_zero_loss_rows" backend/app/services/import_service.py` 输出 `1`
    - **`_dispatch_import` 方法内 sanity check 区域不使用 `reset_index(drop=True)`**：`grep -n 'reset_index' backend/app/services/import_service.py` 输出若存在，必须不在 `_dispatch_import` 方法内（可人工复核）；严格验证：`grep -A 40 "def _dispatch_import" backend/app/services/import_service.py | grep -c "reset_index(drop=True)"` 输出 `0`
    - sanity check 使用 bool mask 过滤：`grep "dataframe.loc\[~bad_mask\]" backend/app/services/import_service.py` 命中
    - 字面消息断言（grep）全部命中：
      - `grep "格式异常（疑似丢失前导零）" backend/app/services/import_service.py` 命中
      - `grep "请在 Excel 中将该列改为「文本」格式后重新上传" backend/app/services/import_service.py` 命中
      - `grep "或从系统重新下载最新模板" backend/app/services/import_service.py` 命中
    - 文件开头 import 区含 `import re`（grep `^import re$` 命中）
    - Python 可 import：`python3 -c "from backend.app.services.import_service import ImportService; print(ImportService._EMPLOYEE_NO_KEY_COLUMNS); print(bool(ImportService._LEADING_ZERO_LOST_PATTERN.match('1234.0')))"` 输出包含 `'employee_no'`, `'manager_employee_no'` 且正则匹配返回 `True`
    - **row_index 口径一致性断言（见 Task 3 的 test_row_index_matches_existing_import_contract 测试）**：sanity check 错误记录的 row_index 与现有 `_import_*` 使用的 row_index 语义完全一致（pandas_idx + 1）
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 新增 pytest 测试 test_import_leading_zero.py（含本地 db_session fixture）</name>
  <files>backend/tests/test_services/test_import_leading_zero.py</files>
  <read_first>
    - backend/app/services/import_service.py（Task 1/2 完成后的最新版本）
    - backend/tests/test_services/test_import_partial_success.py（整文件；复用 `_make_test_db()` + `load_model_modules()` 模式作为参考）
    - backend/tests/test_api/test_feishu_oauth_integration.py（整文件；复用 in-memory SQLite fixture 模式作为参考）
  </read_first>
  <behavior>
    - Test 1 `test_template_employee_no_cell_format_is_text_for_all_types`: 对四种 import_type 分别 build_template_xlsx，openpyxl 读回后断言 header 行的「员工工号」列 + 后续 1-105 行该列 cell.number_format == '@'
    - Test 2 `test_template_example_row_uses_leading_zero`: employees / certifications / performance_grades / salary_adjustments 模板的 A2 单元格值都等于 '02651'（字面字符串）
    - Test 3 `test_template_manager_employee_no_column_is_text`: employees 模板 J 列（直属上级工号）同样 cell.number_format == '@'，1-105 行覆盖
    - Test 4 `test_template_cert_type_and_stage_columns_are_text`: certifications 模板的 B 列（认证类型）和 C 列（认证阶段）同样 cell.number_format == '@'
    - Test 5 `test_dispatch_import_flags_leading_zero_lost_rows`: 构造一个 DataFrame 其中 employee_no 列在 pandas_idx=1（第二条数据）值为 '1234.0'，调用 _dispatch_import('performance_grades', df)，返回 results 中含一个 row_index=2（=pandas_idx 1 + 1）, status='failed', message 含「格式异常（疑似丢失前导零）」的 dict
    - Test 6 `test_dispatch_import_allows_valid_leading_zero_rows`: 构造一个 DataFrame employee_no = '02651'（合法），_dispatch_import 中该行不进入 failed_rows（即不出现 status='failed' + message 含「格式异常」的记录）
    - Test 7 `test_manager_employee_no_also_checked`: employees 类型下 manager_employee_no 列值为 '99.0' 的行同样被捕获为 failed
    - Test 8 `test_row_index_matches_existing_import_contract`: **row_index 口径一致性验证** —— 构造 3 条数据：pandas_idx=0 好行(emp_no='02651'）、pandas_idx=1 坏行(emp_no='1234.0')、pandas_idx=2 好行(emp_no='02652')。调用 `_dispatch_import('performance_grades', df)` 后，断言 sanity check 产生的 failed 记录 row_index=2；且后续好行进入 `_import_performance_grades` 时收到的 pandas index 仍为 0 和 2（未 reset）
  </behavior>
  <action>
    创建新文件 `backend/tests/test_services/test_import_leading_zero.py`。

    **本项目 `backend/tests/` 无 conftest.py；fixture 必须在本文件顶部本地定义**（参考 `test_import_partial_success.py` 的 `_make_test_db()` 模式）。

    文件必须以 `from __future__ import annotations` 开头。

    **完整文件内容（字面照抄，含本地 fixture）：**

    ```python
    from __future__ import annotations

    import io
    from pathlib import Path
    from uuid import uuid4

    import pandas as pd
    import pytest
    from openpyxl import load_workbook
    from sqlalchemy.orm import Session

    from backend.app.core.config import Settings
    from backend.app.core.database import create_db_engine, create_session_factory, init_database
    from backend.app.models import load_model_modules
    from backend.app.services.import_service import ImportService

    load_model_modules()


    # ---------------------------------------------------------------------------
    # Local fixtures (no conftest.py exists for this test tree — inline by design)
    # ---------------------------------------------------------------------------

    def _make_test_db():
        load_model_modules()
        temp_root = Path('.tmp').resolve()
        temp_root.mkdir(parents=True, exist_ok=True)
        database_path = (temp_root / f'lz-{uuid4().hex}.db').as_posix()
        settings = Settings(database_url=f'sqlite+pysqlite:///{database_path}')
        engine = create_db_engine(settings)
        init_database(engine)
        return create_session_factory(settings)


    @pytest.fixture()
    def db_session():
        session_factory = _make_test_db()
        session = session_factory()
        try:
            yield session
        finally:
            session.close()


    def _build_service(db_session: Session) -> ImportService:
        return ImportService(db_session)


    # ---------------------------------------------------------------------------
    # Template format tests (EMPNO-01 / D-09 / D-10)
    # ---------------------------------------------------------------------------

    @pytest.mark.parametrize('import_type', [
        'employees', 'certifications', 'performance_grades', 'salary_adjustments',
    ])
    def test_template_employee_no_cell_format_is_text_for_all_types(
        db_session: Session, import_type: str
    ) -> None:
        svc = _build_service(db_session)
        result = svc.build_template_xlsx(import_type)
        # build_template_xlsx 可能返回 (filename, bytes, media_type) 或 bytes；适配两种
        content = result[1] if isinstance(result, tuple) else result
        wb = load_workbook(io.BytesIO(content))
        ws = wb.active
        # 工号固定在第 1 列
        for row_idx in range(1, ImportService.TEMPLATE_TEXT_PREFILL_ROWS + 1):
            cell = ws.cell(row=row_idx, column=1)
            assert cell.number_format == '@', (
                f'{import_type} 模板第 {row_idx} 行第 1 列 (员工工号) cell.number_format '
                f"应为 '@' 但实际为 {cell.number_format!r}"
            )


    @pytest.mark.parametrize('import_type', [
        'employees', 'certifications', 'performance_grades', 'salary_adjustments',
    ])
    def test_template_example_row_uses_leading_zero(
        db_session: Session, import_type: str
    ) -> None:
        svc = _build_service(db_session)
        result = svc.build_template_xlsx(import_type)
        content = result[1] if isinstance(result, tuple) else result
        wb = load_workbook(io.BytesIO(content))
        ws = wb.active
        # 示例行是第 2 行，工号固定在第 1 列
        assert ws.cell(row=2, column=1).value == '02651'


    def test_template_manager_employee_no_column_is_text(db_session: Session) -> None:
        svc = _build_service(db_session)
        result = svc.build_template_xlsx('employees')
        content = result[1] if isinstance(result, tuple) else result
        wb = load_workbook(io.BytesIO(content))
        ws = wb.active
        # employees headers: 第 10 列是「直属上级工号」
        assert ws.cell(row=1, column=10).value == '直属上级工号'
        for row_idx in range(1, ImportService.TEMPLATE_TEXT_PREFILL_ROWS + 1):
            assert ws.cell(row=row_idx, column=10).number_format == '@'


    def test_template_cert_type_and_stage_columns_are_text(db_session: Session) -> None:
        svc = _build_service(db_session)
        result = svc.build_template_xlsx('certifications')
        content = result[1] if isinstance(result, tuple) else result
        wb = load_workbook(io.BytesIO(content))
        ws = wb.active
        # certifications headers: 第 2 列=认证类型，第 3 列=认证阶段
        assert ws.cell(row=1, column=2).value == '认证类型'
        assert ws.cell(row=1, column=3).value == '认证阶段'
        for col_idx in (2, 3):
            for row_idx in range(1, ImportService.TEMPLATE_TEXT_PREFILL_ROWS + 1):
                assert ws.cell(row=row_idx, column=col_idx).number_format == '@'


    # ---------------------------------------------------------------------------
    # Sanity check tests (EMPNO-02 / D-06 / D-07)
    # ---------------------------------------------------------------------------

    def test_dispatch_import_flags_leading_zero_lost_rows(db_session: Session) -> None:
        svc = _build_service(db_session)
        df = pd.DataFrame([
            {'employee_no': '02651', 'year': '2025', 'grade': 'A'},   # pandas_idx=0, row_index=1
            {'employee_no': '1234.0', 'year': '2025', 'grade': 'B'},  # pandas_idx=1, row_index=2
        ])
        results = svc._dispatch_import('performance_grades', df)
        failed = [r for r in results if r.get('status') == 'failed']
        bad = [r for r in failed if '格式异常（疑似丢失前导零）' in str(r.get('message', ''))]
        assert len(bad) >= 1
        # row_index 约定：pandas_idx + 1 = 2
        assert any(r.get('row_index') == 2 for r in bad)


    def test_dispatch_import_allows_valid_leading_zero_rows(db_session: Session) -> None:
        svc = _build_service(db_session)
        df = pd.DataFrame([
            {'employee_no': '02651', 'year': '2025', 'grade': 'A'},
        ])
        results = svc._dispatch_import('performance_grades', df)
        # 合法前导零行不应该因为 sanity check 被标 failed
        # （可能因员工不存在被标 failed，但 message 不应含「格式异常」）
        for r in results:
            assert '格式异常（疑似丢失前导零）' not in str(r.get('message', ''))


    def test_manager_employee_no_also_checked(db_session: Session) -> None:
        svc = _build_service(db_session)
        df = pd.DataFrame([
            {
                'employee_no': '02651', 'name': '张三', 'department': '产品',
                'job_family': '平台', 'job_level': 'P5', 'manager_employee_no': '99.0',
            },
        ])
        results = svc._dispatch_import('employees', df)
        bad = [r for r in results if r.get('status') == 'failed'
               and '格式异常（疑似丢失前导零）' in str(r.get('message', ''))]
        assert len(bad) >= 1
        # 应当检测到 manager_employee_no 列
        assert any(
            '直属上级工号' in str(r.get('error_column', ''))
            or '直属上级工号' in str(r.get('message', ''))
            for r in bad
        )


    def test_row_index_matches_existing_import_contract(db_session: Session) -> None:
        """row_index 口径一致性验证：sanity check 的 row_index 与既有 _import_* 的 row_index 一致。

        构造 3 条：idx=0 好行、idx=1 坏行、idx=2 好行。
        - sanity check 产生的 failed 记录：row_index=2（=idx 1 + 1）
        - 过滤坏行后，原 pandas index（0, 2）必须保留（不 reset_index），
          以保证下游 `_import_performance_grades` 拿到的 index 仍是原始值。
        """
        svc = _build_service(db_session)
        df = pd.DataFrame([
            {'employee_no': '02651', 'year': '2025', 'grade': 'A'},   # idx=0
            {'employee_no': '1234.0', 'year': '2025', 'grade': 'B'},  # idx=1 bad
            {'employee_no': '02652', 'year': '2025', 'grade': 'C'},   # idx=2
        ])

        # 直接验证 _detect_leading_zero_loss_rows 的 row_index 计算
        normalized = svc._normalize_columns('performance_grades', df)
        bad_map = ImportService._detect_leading_zero_loss_rows(normalized)
        # pandas_idx=1 对应 row_index=2
        assert 1 in bad_map
        row_index, col_name = bad_map[1]
        assert row_index == 2
        assert col_name == 'employee_no'

        # 整体 dispatch 也产生 row_index=2 的 failed 记录
        results = svc._dispatch_import('performance_grades', df)
        failed_rows = [r.get('row_index') for r in results
                       if r.get('status') == 'failed'
                       and '格式异常' in str(r.get('message', ''))]
        assert 2 in failed_rows

        # 反例：构造纯好行 3 条，验证 pandas index 保留不 reset
        df_good = pd.DataFrame([
            {'employee_no': '02651', 'year': '2025', 'grade': 'A'},
            {'employee_no': '02652', 'year': '2025', 'grade': 'B'},
            {'employee_no': '02653', 'year': '2025', 'grade': 'C'},
        ])
        normalized_good = svc._normalize_columns('performance_grades', df_good)
        bad_good = ImportService._detect_leading_zero_loss_rows(normalized_good)
        assert bad_good == {}  # 无坏行
    ```

    **关键点：**
    - `db_session` fixture 使用项目既定 `create_db_engine` + `init_database` 模式（与 test_import_partial_success.py 相同），用文件型 SQLite 避免 ORM 跨连接问题
    - `_dispatch_import` 会 DB 操作（查 Employee 表）— 合法行可能会因测试 DB 没有该员工而后续进入 failed，但本测试只验证 **sanity check 的行为**，断言字符串「格式异常」不要依赖行是否实际入库
    - 所有字符串断言使用字面字符串（避免间接引用使 grep 验收条件失效）
    - **禁止**新建 `backend/tests/conftest.py`
  </action>
  <acceptance_criteria>
    - 文件存在：`ls backend/tests/test_services/test_import_leading_zero.py` 返回 0
    - 文件首行字面含 `from __future__ import annotations`
    - **文件顶部含本地 `db_session` fixture 定义**：`grep -c "@pytest.fixture" backend/tests/test_services/test_import_leading_zero.py` ≥ 1 且 `grep -c "def db_session" backend/tests/test_services/test_import_leading_zero.py` ≥ 1
    - **不新增 `backend/tests/conftest.py`**：`ls backend/tests/conftest.py` 返回非 0
    - 文件含 8 个 test 函数（grep 字面匹配全部命中）：
      - `def test_template_employee_no_cell_format_is_text_for_all_types`
      - `def test_template_example_row_uses_leading_zero`
      - `def test_template_manager_employee_no_column_is_text`
      - `def test_template_cert_type_and_stage_columns_are_text`
      - `def test_dispatch_import_flags_leading_zero_lost_rows`
      - `def test_dispatch_import_allows_valid_leading_zero_rows`
      - `def test_manager_employee_no_also_checked`
      - `def test_row_index_matches_existing_import_contract`
    - 运行 `python -m pytest backend/tests/test_services/test_import_leading_zero.py -v` 退出码 0，**无** `fixture '...' not found` 错误
    - 执行 `python -m pytest backend/tests/test_services/test_import_partial_success.py -v` 仍然全通过（未引入回归）
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HR 用户上传 Excel | 不可信文件内容（含伪造单元格类型、极端长字符串、恶意 formula） |
| HR 用户下载模板 | 服务端输出二进制（无注入风险） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03 | Tampering | _detect_leading_zero_loss_rows 扫描逻辑 | mitigate | 正则 `^\d+\.0$` 仅匹配纯数字 + `.0` 结尾；不触发正则灾难回溯；对 >5000 行走既有 MAX_ROWS 上限截断 |
| T-30-04 | Denial of Service | build_template_xlsx 预填充 105 行 × 多列 | accept | 最大文本列数 = 3（certifications），单模板 <60KB；O(常数)开销，非攻击面 |
| T-30-05 | Information Disclosure | 错误消息含行号 | accept | 行号是 HR 自身上传文件的行号，不泄露第三方 PII |
</threat_model>

<verification>
- [ ] `build_template_xlsx` 对四种 import_type 都能生成 xlsx，所有工号类 + certifications 类型/阶段列的 cell.number_format == '@'（1-105 行）
- [ ] 四种模板示例行的员工工号均为 `'02651'`
- [ ] `_load_table` 返回的 DataFrame 仍是 `dtype=str`（未修改）
- [ ] `_dispatch_import` 对含 `'1234.0'` 格式的 employee_no / manager_employee_no 行返回 status='failed' + message 含 CONTEXT.md D-07 原文三段
- [ ] 合法行（`'02651'`）不会被 sanity check 误标
- [ ] sanity check 的 row_index 口径与既有 `_import_*` 完全一致（`pandas_idx + 1`）
- [ ] `_dispatch_import` 不使用 `reset_index(drop=True)`，原 pandas index 保留
- [ ] 新增 8 个测试全部 PASS
- [ ] `pytest backend/tests/test_services/test_import_service.py backend/tests/test_services/test_import_xlsx.py backend/tests/test_services/test_import_idempotency.py backend/tests/test_services/test_import_partial_success.py` 无回归
</verification>

<success_criteria>
- Excel 模板端：HR 从系统下载的 xlsx 文件中所有工号类列强制文本格式（1-105 行），Excel 录入 `01234` 保存后不会被转为 `1234`；示例行工号 `02651` 直观展示前导零
- 读入端：`1234.0` 形式的单元格值在入库前被 sanity check 拦截，HR 在结果 UI 中看到可执行的补救提示
- row_index 口径与既有 `_import_*` 完全一致（数据行号；不含 Excel 表头）
- 测试覆盖：模板格式、示例值、manager_employee_no、认证类型/阶段、合法行、异常行、manager 列异常、row_index 契约一致性 共 8 个场景
</success_criteria>

<output>
After completion, create `.planning/phases/30-employee-no-leading-zero/30-02-SUMMARY.md` including:
- build_template_xlsx diff 摘要（新增常量、示例行更新、文本格式循环）
- _dispatch_import diff 摘要（含 row_index 口径说明 + bool mask 过滤而非 reset_index）
- pytest 输出（8 个新测试 + 既有 import 测试集回归通过）
- 任何对 ImportService 其他方法的调整（如果不得不改）
</output>
</content>
</invoke>