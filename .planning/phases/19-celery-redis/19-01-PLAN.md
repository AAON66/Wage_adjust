---
phase: 19-celery-redis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - backend/app/celery_app.py
  - backend/app/tasks/__init__.py
  - backend/app/tasks/test_tasks.py
  - backend/tests/test_celery_app.py
autonomous: true
requirements:
  - ASYNC-01

must_haves:
  truths:
    - "celery_app 模块可导入且配置值正确（broker/backend 指向 redis_url, 序列化使用 json）"
    - "test_tasks.db_health_check 任务已注册到 celery app 并可通过 delay() 调用"
    - "worker_process_init signal 已注册，fork 后 dispose engine"
    - "celery 版本升级至 5.5.1，redis/hiredis 版本不变"
  artifacts:
    - path: "backend/app/celery_app.py"
      provides: "Celery app 实例 + worker_process_init signal"
      contains: "celery_app = Celery"
    - path: "backend/app/tasks/__init__.py"
      provides: "tasks 包初始化"
    - path: "backend/app/tasks/test_tasks.py"
      provides: "DB 健康检查测试任务"
      contains: "@celery_app.task"
    - path: "backend/tests/test_celery_app.py"
      provides: "Celery 配置单元测试"
      contains: "def test_"
  key_links:
    - from: "backend/app/celery_app.py"
      to: "backend/app/core/config.py"
      via: "get_settings().redis_url"
      pattern: "settings\\.redis_url"
    - from: "backend/app/tasks/test_tasks.py"
      to: "backend/app/celery_app.py"
      via: "celery_app.task decorator"
      pattern: "@celery_app\\.task"
    - from: "backend/app/celery_app.py"
      to: "backend/app/core/database.py"
      via: "worker_process_init signal disposes engine"
      pattern: "engine\\.dispose"
---

<objective>
创建 Celery app 实例、tasks 目录结构、测试 task，并将 celery 升级至 5.5.1。

Purpose: 为异步任务基础设施建立核心模块，使 worker 可独立启动并执行任务（ASYNC-01）。
Output: celery_app.py, tasks/ 目录, test_tasks.py, 单元测试, requirements.txt 更新
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-celery-redis/19-CONTEXT.md
@.planning/phases/19-celery-redis/19-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From backend/app/core/config.py:
```python
class Settings(BaseSettings):
    redis_url: str = "redis://localhost:6379/0"
    # ... other fields

@lru_cache
def get_settings() -> Settings:
    return Settings()
```

From backend/app/core/database.py:
```python
engine: Engine = create_db_engine()
SessionLocal: sessionmaker[Session] = create_session_factory()

def get_db_session() -> Generator[Session, None, None]:
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 升级 celery 版本 + 创建 celery_app.py + tasks 目录结构</name>
  <files>
    requirements.txt,
    backend/app/celery_app.py,
    backend/app/tasks/__init__.py,
    backend/app/tasks/test_tasks.py
  </files>
  <read_first>
    requirements.txt,
    backend/app/core/config.py,
    backend/app/core/database.py,
    backend/app/core/redis.py
  </read_first>
  <action>
1. 在 requirements.txt 中将 `celery==5.4.0` 修改为 `celery==5.5.1`（per D-11）。redis==5.2.1 和 hiredis==3.1.0 保持不变（per D-12）。

2. 创建 `backend/app/celery_app.py`（per D-01），内容：
   - 文件开头 `from __future__ import annotations`
   - 导入 `Celery` 和 `worker_process_init` signal
   - 通过 `get_settings()` 获取 `redis_url`
   - 创建 Celery 实例：`celery_app = Celery('wage_adjust', broker=settings.redis_url, backend=settings.redis_url, include=['backend.app.tasks.test_tasks'])`
   - 配置 `celery_app.conf.update()`，参数包括：
     - `task_serializer='json'`（per D-04）
     - `accept_content=['json']`（per D-04）
     - `result_serializer='json'`（per D-04）
     - `result_expires=3600`
     - `timezone='Asia/Shanghai'`
     - `enable_utc=True`
     - `task_track_started=True`
     - `task_acks_late=True`
     - `worker_prefetch_multiplier=1`
     - `worker_hijack_root_logger=False`
   - 注册 `@worker_process_init.connect` signal handler，内部延迟导入 `from backend.app.core.database import engine` 并调用 `engine.dispose()`

3. 创建 `backend/app/tasks/__init__.py`（per D-02），空文件或仅包含 docstring。

4. 创建 `backend/app/tasks/test_tasks.py`（per D-02），内容：
   - 文件开头 `from __future__ import annotations`
   - 导入 `from backend.app.celery_app import celery_app`
   - 导入 `from backend.app.core.database import SessionLocal`
   - 导入 `from sqlalchemy import text`
   - 定义 task `@celery_app.task(name='tasks.db_health_check')` 函数 `db_health_check() -> dict`
   - 函数内部创建独立 DB session（`db = SessionLocal()`），执行 `db.execute(text('SELECT 1')).scalar()`，在 finally 中 close session
   - 返回 `{'status': 'ok', 'db_check': result == 1}`

5. 运行 `pip install celery==5.5.1` 升级依赖。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust && python -c "from backend.app.celery_app import celery_app; print('broker:', celery_app.conf.broker_url); print('serializer:', celery_app.conf.task_serializer); print('tasks:', list(celery_app.tasks.keys()))" && python -c "from backend.app.tasks.test_tasks import db_health_check; print('task name:', db_health_check.name)"</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt 包含 `celery==5.5.1`（不再包含 `celery==5.4.0`）
    - requirements.txt 仍包含 `redis==5.2.1` 和 `hiredis==3.1.0`
    - backend/app/celery_app.py 包含 `celery_app = Celery(`
    - backend/app/celery_app.py 包含 `task_serializer='json'`
    - backend/app/celery_app.py 包含 `accept_content=['json']`
    - backend/app/celery_app.py 包含 `worker_hijack_root_logger=False`
    - backend/app/celery_app.py 包含 `engine.dispose()`
    - backend/app/celery_app.py 包含 `@worker_process_init.connect`
    - backend/app/tasks/__init__.py 存在
    - backend/app/tasks/test_tasks.py 包含 `@celery_app.task(name='tasks.db_health_check')`
    - backend/app/tasks/test_tasks.py 包含 `SessionLocal()`
    - backend/app/tasks/test_tasks.py 包含 `db.close()`
    - `python -c "from backend.app.celery_app import celery_app"` 退出码 0
  </acceptance_criteria>
  <done>
    celery_app 可正常导入，broker/backend 指向 redis_url，JSON 序列化配置生效，test task 已注册，worker_process_init signal 已挂载，celery 版本为 5.5.1
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Celery 配置单元测试</name>
  <files>
    backend/tests/test_celery_app.py
  </files>
  <read_first>
    backend/app/celery_app.py,
    backend/app/tasks/test_tasks.py,
    backend/tests/test_core/test_config.py
  </read_first>
  <behavior>
    - Test 1: celery_app 可导入且 broker_url 包含 redis://
    - Test 2: task_serializer 配置为 'json'
    - Test 3: accept_content 配置为 ['json']
    - Test 4: worker_hijack_root_logger 为 False
    - Test 5: tasks.db_health_check 已注册在 celery_app.tasks 中
    - Test 6: test_tasks.db_health_check.name == 'tasks.db_health_check'
    - Test 7: celery_app include 列表包含 'backend.app.tasks.test_tasks'
  </behavior>
  <action>
创建 `backend/tests/test_celery_app.py`，包含以下测试：

```python
from __future__ import annotations

from backend.app.celery_app import celery_app


def test_celery_broker_uses_redis():
    assert 'redis://' in celery_app.conf.broker_url


def test_celery_serializer_is_json():
    assert celery_app.conf.task_serializer == 'json'


def test_celery_accept_content_json_only():
    assert celery_app.conf.accept_content == ['json']


def test_celery_does_not_hijack_root_logger():
    assert celery_app.conf.worker_hijack_root_logger is False


def test_db_health_check_task_registered():
    assert 'tasks.db_health_check' in celery_app.tasks


def test_db_health_check_task_name():
    from backend.app.tasks.test_tasks import db_health_check
    assert db_health_check.name == 'tasks.db_health_check'


def test_celery_include_has_test_tasks():
    assert 'backend.app.tasks.test_tasks' in celery_app.conf.include
```

注意：测试文件放在 `backend/tests/` 根目录下（非 `test_core/` 子目录），因为 celery 是跨层概念。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust && python -m pytest backend/tests/test_celery_app.py -v --timeout=30</automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/test_celery_app.py 存在
    - backend/tests/test_celery_app.py 包含 `def test_celery_broker_uses_redis`
    - backend/tests/test_celery_app.py 包含 `def test_celery_serializer_is_json`
    - backend/tests/test_celery_app.py 包含 `def test_db_health_check_task_registered`
    - `python -m pytest backend/tests/test_celery_app.py -v` 退出码 0，所有 7 个测试通过
  </acceptance_criteria>
  <done>
    7 个单元测试全部通过，覆盖 celery app 配置和 task 注册
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker <-> Redis | Celery worker 通过 Redis 接收消息，需防止反序列化攻击 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | celery_app.py | mitigate | accept_content=['json'] 拒绝 pickle 反序列化（per D-04） |
| T-19-02 | Information Disclosure | celery_app.py | mitigate | worker_hijack_root_logger=False 防止日志泄露到非预期输出 |
</threat_model>

<verification>
1. `python -c "from backend.app.celery_app import celery_app"` 成功
2. `python -m pytest backend/tests/test_celery_app.py -v` 全部通过
3. requirements.txt 中 celery==5.5.1
</verification>

<success_criteria>
- celery_app 可正常导入，配置指向 redis_url
- 测试 task（db_health_check）已注册
- worker_process_init signal 已挂载
- 7 个单元测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/19-celery-redis/19-01-SUMMARY.md`
</output>
