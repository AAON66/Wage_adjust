---
phase: 07-dashboard-and-cache-layer
plan: 03
type: execute
wave: 2
depends_on: [07-01, 07-02]
files_modified:
  - frontend/src/pages/Dashboard.tsx
  - frontend/src/components/dashboard/DepartmentInsightTable.tsx
  - frontend/src/utils/roleAccess.ts
autonomous: false
requirements: [DASH-06, DASH-07]

must_haves:
  truths:
    - "看板页面使用双列网格布局（per D-09），KPI 卡片横排顶部，图表双列排列"
    - "周期选择器切换后所有图表联动刷新，且重置下钻展开状态和 loading 状态"
    - "部门表格行可展开查看该部门的等级分布和调薪平均值（per D-06）"
    - "KPI 待审批数每 30 秒轮询刷新，不刷新整页（per D-08）"
    - "DistributionChart 和 HeatmapChart 不再被引用（per D-02 全部替换）"
    - "员工角色看不到看板导航入口（per D-11，前端 ProtectedRoute 已配置）"
    - "后端 API 通过 require_roles 强制 employee 角色返回 403（per D-11，Plan 01 已实现）"
    - "Redis 不可用时图表区域显示 503 专用错误提示而非空白"
  artifacts:
    - path: "frontend/src/pages/Dashboard.tsx"
      provides: "重构后的看板页面"
      min_lines: 80
    - path: "frontend/src/components/dashboard/DepartmentInsightTable.tsx"
      provides: "带下钻展开的部门表格"
      contains: "DepartmentDrilldown"
  key_links:
    - from: "frontend/src/pages/Dashboard.tsx"
      to: "frontend/src/components/dashboard/KpiCards.tsx"
      via: "KpiCards 组件导入"
      pattern: "KpiCards"
    - from: "frontend/src/pages/Dashboard.tsx"
      to: "frontend/src/components/dashboard/AILevelChart.tsx"
      via: "AILevelChart 组件导入"
      pattern: "AILevelChart"
    - from: "frontend/src/components/dashboard/DepartmentInsightTable.tsx"
      to: "frontend/src/components/dashboard/DepartmentDrilldown.tsx"
      via: "行展开渲染 DepartmentDrilldown"
      pattern: "DepartmentDrilldown"
---

<objective>
Dashboard.tsx 全面重构 + 部门下钻 + 权限验证 + 503 错误处理

Purpose: 将看板页面从旧布局改造为双列网格布局，替换所有旧图表组件为 ECharts 新组件，实现部门行展开下钻，确认权限配置完整，处理 503 错误状态。

Output: 重构后的 Dashboard.tsx、支持下钻的 DepartmentInsightTable、权限配置验证

**评审修订说明（Codex GPT-5.4 反馈）：**
- [HIGH] 明确验证员工角色排除（前端 ProtectedRoute + 后端 require_roles 双重保护）
- [MEDIUM] 周期切换时重置所有依赖 UI 状态（下钻展开、loading、error、图表数据）
- [HIGH] 图表区域正确传递 isServiceUnavailable prop 处理 503 错误
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-dashboard-and-cache-layer/07-CONTEXT.md
@.planning/phases/07-dashboard-and-cache-layer/07-UI-SPEC.md
@.planning/phases/07-dashboard-and-cache-layer/07-01-SUMMARY.md
@.planning/phases/07-dashboard-and-cache-layer/07-02-SUMMARY.md

@frontend/src/pages/Dashboard.tsx
@frontend/src/components/dashboard/DepartmentInsightTable.tsx
@frontend/src/utils/roleAccess.ts
@frontend/src/App.tsx

<interfaces>
<!-- Plan 02 创建的组件接口 -->

From frontend/src/components/dashboard/AILevelChart.tsx:
```typescript
export function AILevelChart({ data, isServiceUnavailable }: {
  data: { label: string; value: number; percentage?: number }[];
  isServiceUnavailable?: boolean;
}): JSX.Element;
```

From frontend/src/components/dashboard/SalaryDistChart.tsx:
```typescript
export function SalaryDistChart({ data, isServiceUnavailable }: {
  data: { label: string; value: number }[];
  isServiceUnavailable?: boolean;
}): JSX.Element;
```

From frontend/src/components/dashboard/ApprovalPipelineChart.tsx:
```typescript
export function ApprovalPipelineChart({ data, isServiceUnavailable }: {
  data: { label: string; value: number }[];
  isServiceUnavailable?: boolean;
}): JSX.Element;
```

From frontend/src/components/dashboard/DepartmentDrilldown.tsx:
```typescript
export function DepartmentDrilldown({ department, levelData, avgAdjustment, employeeCount }: {
  department: string;
  levelData: { label: string; value: number }[];
  avgAdjustment: number;
  employeeCount: number;
}): JSX.Element;
```

From frontend/src/components/dashboard/KpiCards.tsx:
```typescript
export function KpiCards({ cycleId }: { cycleId: string | undefined }): JSX.Element;
```

From frontend/src/hooks/usePolling.ts:
```typescript
interface UsePollingResult<T> {
  data: T | null;
  error: Error | null;
  isServiceUnavailable: boolean;
}
export function usePolling<T>(fetcher: () => Promise<T>, intervalMs: number): UsePollingResult<T>;
```

From frontend/src/services/dashboardService.ts:
```typescript
export function fetchDashboardSnapshot(cycleId?: string): Promise<DashboardSnapshotResponse>;
export function fetchAiLevelDistribution(cycleId?: string): Promise<DashboardDistributionResponse>;
export function fetchSalaryDistribution(cycleId?: string): Promise<DashboardDistributionResponse>;
export function fetchApprovalPipeline(cycleId?: string): Promise<ApprovalPipelineResponse>;
export function fetchDepartmentDrilldown(department: string, cycleId?: string): Promise<DepartmentDrilldownResponse>;
```

From frontend/src/App.tsx (已验证的路由保护):
```typescript
// Line 418: <Route element={<ProtectedRoute allowedRoles={["admin", "hrbp", "manager"]} />}>
// Line 429:   <Route element={<DashboardPage />} path="/dashboard" />
// employee 角色已被 ProtectedRoute 排除
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard.tsx 重构 + DepartmentInsightTable 下钻 + 权限验证 + 503 错误传递 + 周期切换状态重置</name>
  <files>
    frontend/src/pages/Dashboard.tsx,
    frontend/src/components/dashboard/DepartmentInsightTable.tsx,
    frontend/src/utils/roleAccess.ts
  </files>
  <read_first>
    frontend/src/pages/Dashboard.tsx,
    frontend/src/components/dashboard/DepartmentInsightTable.tsx,
    frontend/src/utils/roleAccess.ts,
    frontend/src/App.tsx,
    frontend/src/components/dashboard/KpiCards.tsx,
    frontend/src/components/dashboard/AILevelChart.tsx,
    frontend/src/components/dashboard/SalaryDistChart.tsx,
    frontend/src/components/dashboard/ApprovalPipelineChart.tsx,
    frontend/src/components/dashboard/DepartmentDrilldown.tsx,
    frontend/src/services/dashboardService.ts,
    frontend/src/types/api.ts
  </read_first>
  <action>
    **1. 重构 `Dashboard.tsx`（per D-09 双列布局，D-10 周期选择器，D-02 全部替换旧图表）：**

    完全重写 Dashboard.tsx。保留 AppShell 包装和周期选择逻辑。

    新布局结构（per UI-SPEC 布局契约）：
    ```
    AppShell (title="组织洞察看板", description=per UI-SPEC)
      |- 周期选择器工具栏（保留现有 select，改文案 per UI-SPEC）
      |- KpiCards (cycleId)
      |- 双列网格:
      |    |- AILevelChart (左) + isServiceUnavailable prop
      |    |- SalaryDistChart (右) + isServiceUnavailable prop
      |- ApprovalPipelineChart (全宽) + isServiceUnavailable prop
      |- DepartmentInsightTable (全宽，带下钻)
    ```

    **移除的引用（per D-02 全部替换）：**
    - 删除 `import { DistributionChart }` -- 被 AILevelChart 替代
    - 删除 `import { HeatmapChart }` -- 被 DepartmentInsightTable 下钻替代
    - 删除 `import { OverviewCards }` -- 被 KpiCards 替代
    - 删除 `import { TalentSpotlightPanel }` -- 在新布局中不使用
    - 删除 `import { ActionSummaryPanel }` -- 功能整合到 KPI 卡片和审批流水线图中
    - 删除 `dashboard-hero` 整个 section -- 被 KPI 卡片替代
    - 删除 `dashboard-stage-grid` section -- 被新布局替代
    - 删除 `dashboard-analysis-grid` section -- 被双列网格替代

    **新增的引用：**
    - `import { KpiCards }` from dashboard/KpiCards
    - `import { AILevelChart }` from dashboard/AILevelChart
    - `import { SalaryDistChart }` from dashboard/SalaryDistChart
    - `import { ApprovalPipelineChart }` from dashboard/ApprovalPipelineChart

    **数据获取改造：**
    - 保留 `cycles` 和 `selectedCycleId` 状态
    - 不再使用 `/dashboard/snapshot` 单一 API
    - 改为独立获取各图表数据：
      - `aiLevelData` via `fetchAiLevelDistribution(selectedCycleId)`
      - `salaryDistData` via `fetchSalaryDistribution(selectedCycleId)`
      - `approvalData` via `fetchApprovalPipeline(selectedCycleId)`
      - `departmentInsights` 继续使用 `fetchDashboardSnapshot(selectedCycleId)` 获取，从返回的 snapshot 中提取 `department_insights` 字段
    - KPI 数据由 `KpiCards` 组件内部自行轮询获取，不在 Dashboard 层面管理
    - 使用独立的 useEffect 获取各图表数据，selectedCycleId 变化时联动刷新

    **[评审修订 #4 HIGH] 503 错误状态传递：**
    每个图表的数据获取需要检测 503 错误：
    ```typescript
    const [isRedisUnavailable, setIsRedisUnavailable] = useState(false);

    // 在各数据获取的 catch 中
    } catch (err) {
      if (axios.isAxiosError(err) && err.response?.status === 503) {
        setIsRedisUnavailable(true);
      } else {
        setErrorMessage('加载看板数据失败，请检查网络连接后刷新页面重试。');
      }
    }
    ```
    将 `isRedisUnavailable` 传递给 `AILevelChart`、`SalaryDistChart`、`ApprovalPipelineChart` 的 `isServiceUnavailable` prop。

    **[评审修订 #6 MEDIUM] 周期切换状态重置：**
    在 `selectedCycleId` 变化的 useEffect 中，重置所有依赖状态：
    ```typescript
    useEffect(() => {
      // 重置所有图表数据和状态
      setAiLevelData([]);
      setSalaryDistData([]);
      setApprovalData([]);
      setDepartmentInsights([]);
      setIsRedisUnavailable(false);
      setErrorMessage(null);
      setIsLoading(true);
      // ... 然后重新获取所有数据
    }, [selectedCycleId]);
    ```

    **状态管理：**
    ```typescript
    const [aiLevelData, setAiLevelData] = useState<DashboardDistributionItem[]>([]);
    const [salaryDistData, setSalaryDistData] = useState<DashboardDistributionItem[]>([]);
    const [approvalData, setApprovalData] = useState<DashboardDistributionItem[]>([]);
    const [departmentInsights, setDepartmentInsights] = useState<DashboardDepartmentInsight[]>([]);
    const [isLoading, setIsLoading] = useState(true);
    const [errorMessage, setErrorMessage] = useState<string | null>(null);
    const [isRedisUnavailable, setIsRedisUnavailable] = useState(false);
    ```

    **布局 CSS：**
    - 双列网格使用 CSS Grid: `display: grid; grid-template-columns: repeat(2, 1fr); gap: 16px;`
    - 响应式: `@media (max-width: 899px) { grid-template-columns: 1fr; }`
    - 使用现有 `.surface` CSS 类包裹每个图表区域
    - 每个图表区块使用 `<section className="surface">` + `<h3 className="section-title">` 标题

    **页面标题和描述（per UI-SPEC 文案契约）：**
    - title: "组织洞察看板"
    - description: "围绕评估周期查看 AI 等级分布、调薪幅度、审批进度与部门洞察，帮助管理者快速定位重点。"
    - 主操作按钮: "打开审批中心"（保留 Link to /approvals）

    **加载态和空状态（per UI-SPEC 状态契约）：**
    - 加载中: "正在加载组织洞察看板..."
    - 错误: 错误消息卡片，color: `var(--color-danger)`
    - Redis 不可用: "缓存服务暂时不可用，数据加载可能较慢。请联系管理员检查 Redis 服务状态。"（color: `var(--color-warning)`）
    - 空数据: "暂无看板数据" + "当前周期尚未产生评估或调薪记录。请先在工作台发起评估流程。"

    **2. 改造 `DepartmentInsightTable.tsx`（per D-06 页内展开下钻）：**

    保留现有表格结构。新增展开/收起功能：

    - 新增状态: `expandedDept: string | null`，`drilldownData: DepartmentDrilldownResponse | null`，`drilldownLoading: boolean`
    - 每行新增"查看详情"按钮（展开时变为"收起"）
    - 点击逻辑:
      - 如果已展开同一部门 -> 收起（`setExpandedDept(null)`）
      - 否则 -> 设置 expandedDept，异步调用 `fetchDepartmentDrilldown(department, cycleId)`
    - 展开区域: 在当前行 `<tr>` 后面渲染条件行 `<tr><td colSpan={全列}>`，内部渲染 `<DepartmentDrilldown />`
    - 同时只能展开一个部门（点击其他部门自动收起前一个）
    - 加载中显示 "加载中..."

    **[评审修订 #6 MEDIUM] 周期切换重置下钻状态：**
    Props 变更: 新增 `cycleId?: string` prop 用于下钻 API 调用。
    在 `useEffect` 中监听 `cycleId` 变化，变化时重置展开状态：
    ```typescript
    useEffect(() => {
      setExpandedDept(null);
      setDrilldownData(null);
    }, [cycleId]);
    ```

    **3. 验证 `frontend/src/utils/roleAccess.ts`（per D-11）：**

    **[评审修订 #3 HIGH] 确认员工角色排除（双重保护）：**
    - **前端 ProtectedRoute（已验证）：** `App.tsx` line 418 的 `<ProtectedRoute allowedRoles={["admin", "hrbp", "manager"]}>` 已包含 `/dashboard` 路由。employee 角色访问 `/dashboard` 会被重定向，无需修改。
    - **后端 require_roles（Plan 01 已实现）：** 所有看板 API 端点使用 `require_roles('admin', 'hrbp', 'manager')`，employee 角色收到 403。
    - **侧栏导航（需验证）：** 阅读 `roleAccess.ts` 中的 `getRoleModules` 函数，确认 employee 角色的模块列表不包含 dashboard/看板入口。如果已正确配置则无需修改；如果遗漏则添加排除。

    记录验证结果：在代码注释中标注 `// D-11: employee 角色已在 ProtectedRoute (App.tsx:418) + require_roles (dashboard.py) 双重排除`。
  </action>
  <verify>
    <automated>cd D:/wage_adjust/frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `Dashboard.tsx` 不再导入 `DistributionChart`、`HeatmapChart`、`OverviewCards`、`TalentSpotlightPanel`、`ActionSummaryPanel`
    - `Dashboard.tsx` 导入并渲染 `KpiCards`、`AILevelChart`、`SalaryDistChart`、`ApprovalPipelineChart`
    - `Dashboard.tsx` 向图表组件传递 `isServiceUnavailable` prop（grep 可验证: `grep "isServiceUnavailable" frontend/src/pages/Dashboard.tsx`）
    - `Dashboard.tsx` 在 selectedCycleId 变化的 useEffect 中重置 aiLevelData/salaryDistData/approvalData/isRedisUnavailable 等状态
    - 双列网格布局使用 CSS Grid `grid-template-columns: repeat(2, 1fr)`
    - `DepartmentInsightTable.tsx` 包含 `expandedDept` 状态和 `fetchDepartmentDrilldown` 调用
    - `DepartmentInsightTable.tsx` 在 cycleId 变化时重置展开状态（`setExpandedDept(null)`）
    - `DepartmentInsightTable.tsx` 渲染 `<DepartmentDrilldown />` 组件
    - `roleAccess.ts` 和 `App.tsx` 中 employee 角色不包含 dashboard 访问权限（已验证）
    - `npx tsc --noEmit` 无错误
  </acceptance_criteria>
  <done>Dashboard.tsx 使用双列网格布局和 ECharts 新组件，传递 503 错误状态，周期切换重置所有状态，DepartmentInsightTable 支持下钻展开，权限双重保护已验证</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 人工验证看板页面完整功能</name>
  <files>frontend/src/pages/Dashboard.tsx</files>
  <action>
    人工验证看板页面全部功能是否正常工作。启动前后端服务，使用浏览器访问看板页面进行交互验证。
  </action>
  <what-built>
    看板页面全面重构完成：
    - 双列网格布局（AI 等级分布 | 调薪幅度分布）
    - 4 个 KPI 卡片（待审批数 30 秒轮询、已评估/总人数、平均调薪幅度、等级概览）
    - 审批流水线状态柱状图
    - 部门洞察表格（行展开下钻）
    - 周期选择器切换联动（含状态重置）
    - Redis 缓存层（后端）-- Redis 不可用时缓存端点返回 503，前端显示专用错误提示
    - SQL 聚合替代全表扫描（后端）
    - 后端 require_roles 鉴权 -- employee 角色 API 返回 403
  </what-built>
  <how-to-verify>
    1. 确保 Redis 已启动: `redis-cli ping` 应返回 PONG
    2. 启动后端: `cd D:/wage_adjust && .venv/Scripts/activate && uvicorn backend.app.main:app --reload --port 8011`
    3. 启动前端: `cd D:/wage_adjust/frontend && npm run dev`
    4. 用 admin 账号登录，进入看板页面（/dashboard）
    5. 验证：
       - KPI 卡片横排显示 4 个指标
       - AI 等级分布柱状图使用 ECharts 渲染（有交互 tooltip）
       - 调薪幅度分布直方图使用 ECharts 渲染
       - 审批流水线状态柱状图正常显示
       - 部门洞察表格可点击"查看详情"展开下钻
       - 切换周期后所有图表刷新，下钻展开自动收起
    6. 检查 employee 角色登录后：
       - 侧栏无看板入口
       - 直接访问 /dashboard 被重定向
       - 直接调用看板 API 返回 403（可用 DevTools Network 验证）
    7. 停止 Redis 后刷新看板：
       - KPI 卡片仍正常显示（不依赖 Redis）
       - 其他图表区域显示"缓存服务暂时不可用"错误提示（per UI-SPEC 文案）
       - 不显示空白或 500 错误
  </how-to-verify>
  <verify>人工验证</verify>
  <done>用户确认看板页面功能正常，含 503 错误处理和权限验证</done>
  <resume-signal>输入 "approved" 确认看板功能正常，或描述问题</resume-signal>
</task>

</tasks>

<verification>
```bash
# 前端类型检查
cd D:/wage_adjust/frontend && npx tsc --noEmit

# 后端全套测试
cd D:/wage_adjust && python -m pytest backend/tests/ -x --timeout=60

# 前端构建
cd D:/wage_adjust/frontend && npm run build

# 验证 503 错误状态 UI
grep "isServiceUnavailable" D:/wage_adjust/frontend/src/pages/Dashboard.tsx
grep "isServiceUnavailable" D:/wage_adjust/frontend/src/components/dashboard/AILevelChart.tsx

# 验证旧组件不再引用
! grep "DistributionChart" D:/wage_adjust/frontend/src/pages/Dashboard.tsx
! grep "HeatmapChart" D:/wage_adjust/frontend/src/pages/Dashboard.tsx
```
</verification>

<success_criteria>
- Dashboard.tsx 完全使用 ECharts 新组件，旧 CSS 图表组件不再被引用
- 双列网格布局正确，响应式断点生效
- KPI 卡片 30 秒轮询正常工作
- 部门行展开下钻正常，同时只展开一个，周期切换时自动收起
- 周期选择器切换联动所有图表，且重置 loading/error/503 状态
- Redis 不可用时图表区域显示专用 503 错误提示（非空白非 500）
- KPI 端点不依赖 Redis，Redis 不可用时 KPI 仍正常
- 员工角色前后端双重排除（ProtectedRoute + require_roles 403）
- TypeScript 编译和前端 build 无错误
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard-and-cache-layer/07-03-SUMMARY.md`
</output>
