---
phase: 07-dashboard-and-cache-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/src/components/dashboard/AILevelChart.tsx
  - frontend/src/components/dashboard/SalaryDistChart.tsx
  - frontend/src/components/dashboard/ApprovalPipelineChart.tsx
  - frontend/src/components/dashboard/DepartmentDrilldown.tsx
  - frontend/src/components/dashboard/KpiCards.tsx
  - frontend/src/hooks/usePolling.ts
  - frontend/src/services/dashboardService.ts
  - frontend/src/types/api.ts
autonomous: true
requirements: [DASH-03, DASH-04]

must_haves:
  truths:
    - "echarts 和 echarts-for-react 已安装到 frontend/package.json"
    - "AI 等级分布图表组件使用 ECharts 柱状图渲染，中文标签（一级/二级/...）"
    - "调薪幅度分布组件使用 ECharts 柱状图渲染，区间标签（0-5%/5-10%/...）"
    - "审批流水线状态组件使用 ECharts 柱状图渲染，各状态计数"
    - "usePolling hook 支持 setInterval 轮询并在组件卸载时 clearInterval"
  artifacts:
    - path: "frontend/src/components/dashboard/AILevelChart.tsx"
      provides: "AI 等级分布 ECharts 柱状图"
      exports: ["AILevelChart"]
    - path: "frontend/src/components/dashboard/SalaryDistChart.tsx"
      provides: "调薪幅度分布 ECharts 直方图"
      exports: ["SalaryDistChart"]
    - path: "frontend/src/components/dashboard/ApprovalPipelineChart.tsx"
      provides: "审批流水线状态 ECharts 柱状图"
      exports: ["ApprovalPipelineChart"]
    - path: "frontend/src/components/dashboard/DepartmentDrilldown.tsx"
      provides: "部门下钻展开面板"
      exports: ["DepartmentDrilldown"]
    - path: "frontend/src/components/dashboard/KpiCards.tsx"
      provides: "4 个 KPI 卡片组件"
      exports: ["KpiCards"]
    - path: "frontend/src/hooks/usePolling.ts"
      provides: "可复用轮询 hook"
      exports: ["usePolling"]
  key_links:
    - from: "frontend/src/components/dashboard/AILevelChart.tsx"
      to: "echarts-for-react"
      via: "ReactECharts 组件"
      pattern: "ReactECharts"
    - from: "frontend/src/components/dashboard/KpiCards.tsx"
      to: "frontend/src/hooks/usePolling.ts"
      via: "usePolling hook 调用"
      pattern: "usePolling"
    - from: "frontend/src/services/dashboardService.ts"
      to: "/dashboard/kpi-summary"
      via: "axios GET"
      pattern: "kpi-summary"
---

<objective>
前端 ECharts 图表组件 + usePolling hook + 看板 API 客户端

Purpose: 安装 ECharts 依赖，创建所有新图表组件（AI 等级分布、调薪幅度、审批流水线、部门下钻、KPI 卡片），并新增前端 API 调用函数。这些组件将在 Plan 03 中集成到 Dashboard.tsx。

Output: 6 个新组件/hook + dashboardService 新增 API 函数 + TypeScript 类型
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-dashboard-and-cache-layer/07-CONTEXT.md
@.planning/phases/07-dashboard-and-cache-layer/07-RESEARCH.md
@.planning/phases/07-dashboard-and-cache-layer/07-UI-SPEC.md

@frontend/src/pages/Dashboard.tsx
@frontend/src/services/dashboardService.ts
@frontend/src/types/api.ts
@frontend/src/components/dashboard/DistributionChart.tsx
@frontend/src/components/dashboard/OverviewCards.tsx
@frontend/src/components/dashboard/DepartmentInsightTable.tsx
@frontend/package.json

<interfaces>
<!-- 执行器需要的关键类型 -->

From frontend/src/types/api.ts (现有类型):
```typescript
export interface DashboardDistributionItem {
  label: string;
  value: number;
}

export interface DashboardDistributionResponse {
  items: DashboardDistributionItem[];
  total: number;
}

export interface DashboardDepartmentInsight {
  department: string;
  employee_count: number;
  avg_score: number;
  high_potential_count: number;
  pending_review_count: number;
  approved_count: number;
  budget_used: string;
  avg_increase_ratio: number;
}
```

From UI-SPEC.md (ECharts 色板):
```
Level 1: #C9CDD4  Level 2: #BAE6FD  Level 3: #38BDF8
Level 4: #1456F0  Level 5: #00B42A
调薪幅度: #1456F0 alpha 递增
审批状态: draft=#C9CDD4, submitted=#38BDF8, manager_review=#1456F0,
         hr_review=#7C3AED, approved=#00B42A, rejected=#F53F3F
```

From UI-SPEC.md (ECharts 通用配置):
```typescript
const BASE_CHART_OPTION = {
  textStyle: {
    fontFamily: '"PingFang SC", "Microsoft YaHei", "Segoe UI", Inter, sans-serif',
    color: '#646A73',
  },
  tooltip: {
    trigger: 'axis',
    backgroundColor: '#FFFFFF',
    borderColor: '#E0E4EA',
    borderWidth: 1,
    textStyle: { color: '#1F2329', fontSize: 13 },
    padding: [8, 12],
    extraCssText: 'box-shadow: 0 4px 16px rgba(0,0,0,0.10);',
  },
  grid: { left: 16, right: 16, top: 24, bottom: 8, containLabel: true },
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 安装 ECharts + 新增 TypeScript 类型 + API 客户端函数 + usePolling hook</name>
  <files>
    frontend/package.json,
    frontend/src/types/api.ts,
    frontend/src/services/dashboardService.ts,
    frontend/src/hooks/usePolling.ts
  </files>
  <read_first>
    frontend/package.json,
    frontend/src/types/api.ts,
    frontend/src/services/dashboardService.ts,
    frontend/src/services/api.ts,
    frontend/src/hooks/useAuth.tsx
  </read_first>
  <action>
    **1. 安装 ECharts（per D-01）：**
    ```bash
    cd frontend && npm install echarts@5 echarts-for-react@3.0.6
    ```
    注意: 使用 echarts@5 而非 @6（echarts-for-react 3.0.6 的 peerDependency 是 echarts ^5.0.0）。

    **2. 更新 `frontend/src/types/api.ts`：**
    新增类型（追加到文件末尾，不修改现有类型）：

    ```typescript
    export interface KpiSummaryResponse {
      pending_approvals: number;
      total_employees: number;
      evaluated_employees: number;
      avg_adjustment_ratio: number;
      level_summary: DashboardDistributionItem[];
    }

    export interface ApprovalPipelineResponse {
      items: DashboardDistributionItem[];
      total: number;
    }

    export interface DepartmentDrilldownResponse {
      department: string;
      level_distribution: DashboardDistributionItem[];
      avg_adjustment_ratio: number;
      employee_count: number;
    }
    ```

    同时更新 `DashboardDistributionItem` 添加可选字段：
    ```typescript
    export interface DashboardDistributionItem {
      label: string;
      value: number;
      percentage?: number;  // 新增
    }
    ```

    **3. 更新 `frontend/src/services/dashboardService.ts`：**
    保留现有 `fetchDashboardSnapshot`，新增：

    ```typescript
    export async function fetchKpiSummary(cycleId?: string): Promise<KpiSummaryResponse> {
      const response = await api.get<KpiSummaryResponse>('/dashboard/kpi-summary', {
        params: cycleId ? { cycle_id: cycleId } : undefined,
      });
      return response.data;
    }

    export async function fetchSalaryDistribution(cycleId?: string): Promise<DashboardDistributionResponse> {
      const response = await api.get<DashboardDistributionResponse>('/dashboard/salary-distribution', {
        params: cycleId ? { cycle_id: cycleId } : undefined,
      });
      return response.data;
    }

    export async function fetchApprovalPipeline(cycleId?: string): Promise<ApprovalPipelineResponse> {
      const response = await api.get<ApprovalPipelineResponse>('/dashboard/approval-pipeline', {
        params: cycleId ? { cycle_id: cycleId } : undefined,
      });
      return response.data;
    }

    export async function fetchDepartmentDrilldown(department: string, cycleId?: string): Promise<DepartmentDrilldownResponse> {
      const response = await api.get<DepartmentDrilldownResponse>('/dashboard/department-drilldown', {
        params: { department, ...(cycleId ? { cycle_id: cycleId } : {}) },
      });
      return response.data;
    }

    export async function fetchAiLevelDistribution(cycleId?: string): Promise<DashboardDistributionResponse> {
      const response = await api.get<DashboardDistributionResponse>('/dashboard/ai-level-distribution', {
        params: cycleId ? { cycle_id: cycleId } : undefined,
      });
      return response.data;
    }
    ```

    **4. 创建 `frontend/src/hooks/usePolling.ts`（per D-08）：**
    ```typescript
    import { useCallback, useEffect, useState } from 'react';

    export function usePolling<T>(fetcher: () => Promise<T>, intervalMs: number): T | null {
      const [data, setData] = useState<T | null>(null);

      const stableFetcher = useCallback(fetcher, [fetcher]);

      useEffect(() => {
        let cancelled = false;

        async function poll() {
          try {
            const result = await stableFetcher();
            if (!cancelled) setData(result);
          } catch {
            // 静默失败，下次重试（per D-08）
          }
        }

        void poll();
        const id = setInterval(() => void poll(), intervalMs);
        return () => {
          cancelled = true;
          clearInterval(id);
        };
      }, [stableFetcher, intervalMs]);

      return data;
    }
    ```
  </action>
  <verify>
    <automated>cd D:/wage_adjust/frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `echarts` 和 `echarts-for-react` 出现在 `frontend/package.json` 的 dependencies 中
    - `KpiSummaryResponse`、`ApprovalPipelineResponse`、`DepartmentDrilldownResponse` 定义在 `api.ts`
    - `dashboardService.ts` 导出 `fetchKpiSummary`、`fetchSalaryDistribution`、`fetchApprovalPipeline`、`fetchDepartmentDrilldown`、`fetchAiLevelDistribution`
    - `usePolling.ts` 导出 `usePolling` hook，内部使用 `setInterval` + `clearInterval`
    - `npx tsc --noEmit` 无错误
  </acceptance_criteria>
  <done>ECharts 依赖安装，TypeScript 类型定义完整，API 客户端函数和 usePolling hook 可用，类型检查通过</done>
</task>

<task type="auto">
  <name>Task 2: 创建 ECharts 图表组件（AI 等级、调薪分布、审批流水线、部门下钻、KPI 卡片）</name>
  <files>
    frontend/src/components/dashboard/AILevelChart.tsx,
    frontend/src/components/dashboard/SalaryDistChart.tsx,
    frontend/src/components/dashboard/ApprovalPipelineChart.tsx,
    frontend/src/components/dashboard/DepartmentDrilldown.tsx,
    frontend/src/components/dashboard/KpiCards.tsx
  </files>
  <read_first>
    frontend/src/components/dashboard/DistributionChart.tsx,
    frontend/src/components/dashboard/OverviewCards.tsx,
    frontend/src/components/dashboard/DepartmentInsightTable.tsx,
    frontend/src/types/api.ts,
    frontend/src/hooks/usePolling.ts,
    frontend/src/services/dashboardService.ts,
    frontend/src/pages/Dashboard.tsx
  </read_first>
  <action>
    按照 UI-SPEC.md 的视觉规范创建所有组件。所有 ECharts 组件共享基础 option。

    **1. 创建共享常量文件（可选，也可内联到各组件中）。为简洁起见，在 AILevelChart.tsx 顶部定义 BASE_CHART_OPTION 并在其他组件中复制或提取。**

    **2. `AILevelChart.tsx`（per D-02, DASH-03）：**

    Props: `data: { label: string; value: number; percentage?: number }[]`

    - 中文标签映射（per UI-SPEC 和上下文）：
      `'Level 1' -> '一级'`, `'Level 2' -> '二级'`, `'Level 3' -> '三级'`, `'Level 4' -> '四级'`, `'Level 5' -> '五级'`
    - AI 等级色板（per UI-SPEC）：
      Level 1: `#C9CDD4`, Level 2: `#BAE6FD`, Level 3: `#38BDF8`, Level 4: `#1456F0`, Level 5: `#00B42A`
    - ECharts type: `'bar'`
    - 柱体圆角: `borderRadius: [4, 4, 0, 0]`
    - `barMaxWidth: 48`
    - Y 轴名称: `'人数'`
    - tooltip 显示人数和百分比
    - 容器高度 300px
    - 空数据时显示 "当前周期暂无数据"

    **3. `SalaryDistChart.tsx`（per DASH-04）：**

    Props: `data: { label: string; value: number }[]`

    - X 轴标签: 区间标签（'0-5%', '5-10%', '10-15%', '15-20%', '20%+'）直接使用
    - 单色渐变: `#1456F0` alpha 从 0.4 到 1.0 按区间递增（5 个区间，alpha 分别为 0.4, 0.55, 0.7, 0.85, 1.0）
    - ECharts type: `'bar'`
    - 柱体圆角: `borderRadius: [4, 4, 0, 0]`
    - Y 轴名称: `'人数'`
    - 容器高度 300px

    **4. `ApprovalPipelineChart.tsx`（per DASH-05）：**

    Props: `data: { label: string; value: number }[]`

    - 状态中文映射:
      `draft -> '草稿'`, `submitted -> '已提交'`, `pending_approval -> '审批中'`, `approved -> '已批准'`, `rejected -> '已拒绝'`, `recommended -> '已推荐'`, `adjusted -> '已调整'`, `locked -> '已锁定'`
    - 状态色（per UI-SPEC）:
      draft: `#C9CDD4`, submitted: `#38BDF8`, pending_approval: `#1456F0`, approved: `#00B42A`, rejected: `#F53F3F`
      其他状态: `#7C3AED`
    - ECharts type: `'bar'`
    - 容器高度 300px

    **5. `DepartmentDrilldown.tsx`（per D-06, DASH-06）：**

    Props: `department: string; levelData: { label: string; value: number }[]; avgAdjustment: number; employeeCount: number`

    - 展开面板布局: 双列，左侧 AI 等级分布迷你柱状图，右侧调薪平均值 + 员工数
    - 迷你图表容器高度: 200px
    - grid padding 缩小: `{ left: 8, right: 8, top: 16, bottom: 4, containLabel: true }`
    - 使用与 AILevelChart 相同的色板
    - padding: 20px（per UI-SPEC 间距规范）

    **6. `KpiCards.tsx`（per D-07, D-08, DASH-07）：**

    Props: `cycleId: string | undefined`

    - 使用 `usePolling(fetchKpiSummary, 30000)` 获取 KPI 数据
    - 4 个卡片（per D-07）:
      1. 待审批数: 值来自 `pending_approvals`。值 > 0 时数值颜色为 `#FF7D00`（per UI-SPEC 强调色规则）
      2. 已评估/总人数: 显示 `evaluated_employees / total_employees`
      3. 平均调薪幅度: 显示 `avg_adjustment_ratio` 转换为百分比格式
      4. AI 等级概览: 显示 `level_summary` 中 value 最大的等级名称
    - 沿用现有 `.metric-tile` CSS 类样式
    - 4 列网格布局（`grid-template-columns: repeat(4, 1fr)`），响应式: < 768px 单列，768-1279px 2列，>=1280px 4列（per UI-SPEC 断点）
    - 初始加载时 `usePolling` 返回 null，显示 '--' 占位
    - `cycleId` 变化时需要重新初始化轮询（将 cycleId 包含在 fetcher 的 useCallback deps 中）
  </action>
  <verify>
    <automated>cd D:/wage_adjust/frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `AILevelChart.tsx` 导出 `AILevelChart`，使用 `ReactECharts`，中文标签映射，5 色色板
    - `SalaryDistChart.tsx` 导出 `SalaryDistChart`，使用 `ReactECharts`，`#1456F0` alpha 渐变
    - `ApprovalPipelineChart.tsx` 导出 `ApprovalPipelineChart`，使用 `ReactECharts`，状态色映射
    - `DepartmentDrilldown.tsx` 导出 `DepartmentDrilldown`，200px 迷你图表
    - `KpiCards.tsx` 导出 `KpiCards`，内部调用 `usePolling`，4 个指标卡片
    - `npx tsc --noEmit` 无错误
  </acceptance_criteria>
  <done>5 个 ECharts 图表组件和 KPI 卡片组件创建完毕，TypeScript 类型检查通过</done>
</task>

</tasks>

<verification>
```bash
# TypeScript 编译检查
cd D:/wage_adjust/frontend && npx tsc --noEmit

# 检查 ECharts 依赖已安装
cd D:/wage_adjust/frontend && node -e "require('echarts'); require('echarts-for-react'); console.log('echarts OK')"

# 检查新组件文件存在
ls D:/wage_adjust/frontend/src/components/dashboard/AILevelChart.tsx D:/wage_adjust/frontend/src/components/dashboard/SalaryDistChart.tsx D:/wage_adjust/frontend/src/components/dashboard/ApprovalPipelineChart.tsx D:/wage_adjust/frontend/src/components/dashboard/DepartmentDrilldown.tsx D:/wage_adjust/frontend/src/components/dashboard/KpiCards.tsx D:/wage_adjust/frontend/src/hooks/usePolling.ts
```
</verification>

<success_criteria>
- ECharts 依赖正确安装且 TypeScript 类型可用
- 5 个图表组件 + 1 个 usePolling hook 创建完毕
- 所有组件遵循 UI-SPEC.md 的色彩、排版和间距规范
- KpiCards 使用 usePolling 实现 30 秒轮询
- DepartmentDrilldown 使用 200px 迷你图表
- 前端 TypeScript 编译无错误
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard-and-cache-layer/07-02-SUMMARY.md`
</output>
