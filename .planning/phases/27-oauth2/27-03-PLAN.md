---
phase: 27-oauth2
plan: 03
type: execute
wave: 3
depends_on: [27-01, 27-02]
files_modified:
  - frontend/src/components/auth/FeishuLoginPanel.tsx
  - frontend/src/pages/Login.tsx
autonomous: false
requirements: [FUI-01, FUI-03, FUI-04]
must_haves:
  truths:
    - "访问 /login 时右侧 section（LoginForm 下方）渲染 FeishuLoginPanel，页面加载后 2-3 秒内 QR 二维码 iframe 可见"
    - "QR 面板挂载后调用 GET /auth/feishu/authorize 取 authorize_url 与 state，传入 window.QRLogin({ id, goto: authorize_url, ... })"
    - "通过 window.addEventListener('message', handler) 监听飞书 iframe 的 postMessage；handler 校验 instance.matchOrigin(event.origin) 与 instance.matchData(event.data) 通过后取 event.data.tmp_code，拼接 authorize_url + '&tmp_code=' + encodeURIComponent(tmp_code) 并 window.location.href 跳转"
    - "挂载后启动 180 秒 setTimeout 计时；到期后面板覆盖毛玻璃蒙层 + 居中「二维码已过期，点击刷新」按钮"
    - "用户点击刷新：清空容器 innerHTML、移除旧 message listener、重新 fetch /authorize、调 window.QRLogin 重建实例、重置 180s 计时"
    - "SDK 脚本 load 失败 / authorize 请求失败时：面板内显示红色 inline banner（var(--color-danger)） + 「重试」按钮；不使用 Toast，不回跳 /login"
    - "Login.tsx 右侧 section 的现有 LoginForm、「账号由管理员开通」提示、「返回平台首页」链接完整保留（LOGIN-04）；邮箱密码登录仍可正常完成"
    - "组件卸载时清理 setTimeout、removeEventListener('message', handler)、不移除全局 <script> 节点（D-03 单例策略）"
    - "cd frontend && npm run lint 退出码 0；cd frontend && npm run build 退出码 0"
    - "Manual Test Plan #1 #5 #6 #10 #11 #13 全部通过（checkpoint 人工验证）"
  artifacts:
    - path: frontend/src/components/auth/FeishuLoginPanel.tsx
      provides: 自洽的飞书 QR 扫码登录面板（SDK 注入 + QRLogin 实例 + postMessage 监听 + 180s 过期 + 刷新 + 错误 banner）
      contains: "export function FeishuLoginPanel"
    - path: frontend/src/pages/Login.tsx
      provides: 右侧 section 引入 FeishuLoginPanel（不破坏现有 LoginForm）
      contains: "<FeishuLoginPanel"
  key_links:
    - from: frontend/src/components/auth/FeishuLoginPanel.tsx
      to: /api/v1/auth/feishu/authorize
      via: authorizeFeishu()
      pattern: "authorizeFeishu"
    - from: frontend/src/components/auth/FeishuLoginPanel.tsx
      to: 飞书 QRLogin SDK（CDN）
      via: 动态 <script src> 注入 + window.QRLogin
      pattern: "window.QRLogin"
    - from: frontend/src/components/auth/FeishuLoginPanel.tsx
      to: 浏览器 postMessage
      via: window.addEventListener('message', handler)，instance.matchOrigin + instance.matchData 双校验
      pattern: "matchOrigin"
    - from: frontend/src/components/auth/FeishuLoginPanel.tsx
      to: frontend/src/utils/feishuErrors.ts
      via: resolveFeishuError('backend'|'sdk', err)
      pattern: "resolveFeishuError"
    - from: frontend/src/pages/Login.tsx
      to: frontend/src/components/auth/FeishuLoginPanel.tsx
      via: JSX 渲染
      pattern: "FeishuLoginPanel"
---

<objective>
完成 Phase 27 最后一步：新建 FeishuLoginPanel 组件（完全自洽：SDK 注入 + authorize 请求 + QRLogin 实例 + postMessage 监听 + 180s 倒计时 + 点击刷新 + 分类错误 banner），并在 Login.tsx 右侧 section 的 LoginForm 下方引入。

Purpose: 用户在 /login 页可直接看到飞书 QR 扫码面板；扫码授权成功后浏览器自动重定向到 /auth/feishu/callback（由 Plan 02 处理）。不破坏现有邮箱密码登录路径（LOGIN-04）。

Output: FeishuLoginPanel.tsx 新建、Login.tsx 轻改。本 Plan 因含 SDK CDN + 真实飞书扫码，autonomous=false，必须通过 checkpoint:human-verify 完成 Manual Test Plan 中需要真实飞书账号的项。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/27-oauth2/27-CONTEXT.md
@.planning/phases/27-oauth2/27-RESEARCH.md
@.planning/phases/27-oauth2/27-01-PLAN.md
@.planning/phases/27-oauth2/27-02-PLAN.md
@frontend/src/pages/Login.tsx
@frontend/src/services/auth.ts
@frontend/src/utils/feishuErrors.ts
@frontend/src/hooks/usePolling.ts
@frontend/src/components/auth/LoginForm.tsx

<interfaces>
<!-- 来自 Plan 01（已交付）-->
// services/auth.ts
export function authorizeFeishu(): Promise<FeishuAuthorizeResponse>;
// types/api.ts
interface FeishuAuthorizeResponse { authorize_url: string; state: string; }
// utils/feishuErrors.ts
function resolveFeishuError(source: 'backend' | 'sdk', payload: unknown): FeishuError;

<!-- 飞书 QRLogin SDK 契约（来自 RESEARCH.md Pattern 2，验证过 5+ 社区实现）-->
CDN URL: https://lf-package-cn.feishucdn.com/obj/feishu-static/lark/passport/qrcode/LarkSSOSDKWebQRCode-1.0.3.js

declare global {
  interface Window {
    QRLogin?: (cfg: QRLoginConfig) => QRLoginInstance;
  }
}
interface QRLoginConfig {
  id: string;              // 容器元素 id
  goto: string;            // 完整 authorize_url（含 redirect_uri、state、scope）
  width?: string | number;
  height?: string | number;
  style?: string;
}
interface QRLoginInstance {
  matchOrigin: (origin: string) => boolean;
  matchData: (data: unknown) => boolean;
}

<!-- postMessage 契约（SDK 1.0.3）-->
// 扫码成功后，飞书 iframe 向宿主 window.postMessage({ tmp_code: string }, <飞书 origin>)
// 宿主必须：instance.matchOrigin(event.origin) && instance.matchData(event.data) 双通过
// 然后：window.location.href = authorize_url + '&tmp_code=' + encodeURIComponent(event.data.tmp_code)
// 浏览器请求该 URL → 飞书服务器授权 → 302 重定向到 redirect_uri（即 /auth/feishu/callback?code=...&state=...）
// D-10 勘误：SDK **没有** onSuccess/onReady/onErr 回调。不要去配这三个字段。

<!-- Login.tsx 右侧 section 现有结构（第 69-85 行）-->
// 在 <LoginForm /> 包裹 div 之后、「账号由管理员开通」提示之前插入 <FeishuLoginPanel />
// 用 marginTop: 20 与上方 LoginForm 保持视觉间距
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 实现 FeishuLoginPanel 组件（SDK 注入 + QRLogin + postMessage + 过期/刷新 + 错误 banner）</name>
  <files>frontend/src/components/auth/FeishuLoginPanel.tsx</files>
  <read_first>
    - frontend/src/services/auth.ts（Plan 01 Task 3 的 authorizeFeishu 导出）
    - frontend/src/utils/feishuErrors.ts（Plan 01 Task 2 的 resolveFeishuError）
    - frontend/src/hooks/usePolling.ts 第 56-64 行（项目 cleanup 模式）
    - frontend/src/components/auth/LoginForm.tsx（风格参考：CSS 类、color var、action-primary 按钮）
    - .planning/phases/27-oauth2/27-CONTEXT.md D-01 ~ D-14（全部，尤其 D-01/D-02/D-03/D-08/D-09/D-11/D-12/D-14）
    - .planning/phases/27-oauth2/27-RESEARCH.md 第 232-378 行（Pattern 1-3 的完整参考实现）
    - .planning/phases/27-oauth2/27-RESEARCH.md 第 476-546 行（Pitfall 1-6 全覆盖）
    - .planning/phases/27-oauth2/27-RESEARCH.md 第 803-828 行（Security Domain + 威胁清单）
  </read_first>
  <behavior>
    - 组件挂载时：
      1. 启动 SDK 脚本注入 hook（若 window.QRLogin 已存在则立即 ready；否则检查 DOM 是否已有 <script src=CDN>；否则插入新 <script>；success 事件 → setReady(true)；error 事件 → setSdkError(sdk_load_failed)）
      2. 并行发起 authorizeFeishu() 取 authorize_url + state
      3. ready && authorize_url 都就绪 → 清空容器 innerHTML → new instance = window.QRLogin({ id: 'feishu-qr-container', goto: authorize_url, width: '260', height: '260', style: 'width:260px;height:260px;border:none' })
      4. 挂 window.addEventListener('message', handler)；handler 校验 instance.matchOrigin + instance.matchData；event.data 必须是 object 且含 tmp_code 字符串；通过 → window.location.href = authorize_url + '&tmp_code=' + encodeURIComponent(tmp_code)
      5. 启动 180_000ms setTimeout → 到期 setIsExpired(true)
    - 面板视觉：
      - 标题 eyebrow "飞书扫码登录" + h2/h3 "打开飞书 App 扫码"
      - 容器 <div id="feishu-qr-container" style={{ width:260, height:260, margin:'0 auto' }} />
      - 加载态（!ready && !error）：容器位置显示 "二维码加载中…" 占位文本
      - 过期态（isExpired）：容器上方绝对定位毛玻璃蒙层（backdrop-filter: blur(4px); background: rgba(255,255,255,0.75)）+ 内部 action-primary 按钮 "二维码已过期，点击刷新"
      - 错误态（hasError 或 sdk_load_failed / authorize 失败）：用 className="surface-subtle" 渲染红色 inline banner（color: var(--color-danger)，background: 柔色）+ action-secondary 「重试」按钮（触发 refresh）
    - 刷新流程（点击「刷新」/「重试」）：
      1. setIsExpired(false); setHasError(false); setErrorMessage('')
      2. 清空旧 setTimeout 引用（如果已在）
      3. 移除旧 message listener（保存在 ref 中）
      4. 容器 innerHTML = ''
      5. 重新 authorizeFeishu() 获取新 authorize_url + state
      6. 调 window.QRLogin({...}) 重建实例 + 挂新 listener
      7. 重启 180s setTimeout
    - 组件卸载时：
      1. clearTimeout
      2. removeEventListener('message', handler)
      3. 不移除全局 <script>（D-03 单例策略）
      4. 不重置 window.QRLogin
    - 错误源分类：
      - SDK 脚本 load 失败 → setError(new Error('sdk_load_failed')) → resolveFeishuError('sdk', err)
      - authorizeFeishu 失败（network / 503）→ resolveFeishuError('backend', err)
    - 不用 Toast（D-12）；不 console.log / console.error（T-27-03）
  </behavior>
  <action>
新建文件 frontend/src/components/auth/FeishuLoginPanel.tsx。此组件较大（约 250 行），按以下结构实现 — 严格遵循 RESEARCH.md Pattern 1-3 + Pitfall 1-6 的所有要求：

```tsx
import { useCallback, useEffect, useRef, useState } from 'react';

import { authorizeFeishu } from '../../services/auth';
import { resolveFeishuError, type FeishuError } from '../../utils/feishuErrors';

const QR_SDK_URL =
  'https://lf-package-cn.feishucdn.com/obj/feishu-static/lark/passport/qrcode/LarkSSOSDKWebQRCode-1.0.3.js';
const QR_CONTAINER_ID = 'feishu-qr-container';
const QR_EXPIRY_MS = 180_000;

interface QRLoginConfig {
  id: string;
  goto: string;
  width?: string | number;
  height?: string | number;
  style?: string;
}
interface QRLoginInstance {
  matchOrigin: (origin: string) => boolean;
  matchData: (data: unknown) => boolean;
}
declare global {
  interface Window {
    QRLogin?: (cfg: QRLoginConfig) => QRLoginInstance;
  }
}

/** SDK 脚本单例注入 hook。返回 ready / error 状态；不在 cleanup 里移除脚本。 */
function useFeishuSdk(): { ready: boolean; error: Error | null; retry: () => void } {
  const [ready, setReady] = useState<boolean>(() => typeof window !== 'undefined' && !!window.QRLogin);
  const [error, setError] = useState<Error | null>(null);
  const [attempt, setAttempt] = useState(0);

  const retry = useCallback(() => {
    setError(null);
    setAttempt((n) => n + 1);
  }, []);

  useEffect(() => {
    if (window.QRLogin) {
      setReady(true);
      return;
    }

    const existing = document.querySelector<HTMLScriptElement>(
      `script[src="${QR_SDK_URL}"]`,
    );
    if (existing) {
      if (window.QRLogin) {
        setReady(true);
        return;
      }
      const handleLoad = () => setReady(true);
      const handleError = () => setError(new Error('sdk_load_failed'));
      existing.addEventListener('load', handleLoad, { once: true });
      existing.addEventListener('error', handleError, { once: true });
      return () => {
        existing.removeEventListener('load', handleLoad);
        existing.removeEventListener('error', handleError);
      };
    }

    const script = document.createElement('script');
    script.src = QR_SDK_URL;
    script.async = true;
    const handleLoad = () => setReady(true);
    const handleError = () => setError(new Error('sdk_load_failed'));
    script.addEventListener('load', handleLoad, { once: true });
    script.addEventListener('error', handleError, { once: true });
    document.head.appendChild(script);

    return () => {
      script.removeEventListener('load', handleLoad);
      script.removeEventListener('error', handleError);
      // D-03: 单例策略，不 script.remove()
    };
  }, [attempt]);

  return { ready, error, retry };
}

export function FeishuLoginPanel() {
  const { ready: sdkReady, error: sdkError, retry: retrySdk } = useFeishuSdk();
  const [authorizeUrl, setAuthorizeUrl] = useState<string | null>(null);
  const [isLoadingAuthorize, setIsLoadingAuthorize] = useState(false);
  const [authorizeError, setAuthorizeError] = useState<FeishuError | null>(null);
  const [isExpired, setIsExpired] = useState(false);
  const [refreshKey, setRefreshKey] = useState(0);

  const messageHandlerRef = useRef<((event: MessageEvent) => void) | null>(null);
  const instanceRef = useRef<QRLoginInstance | null>(null);
  const expiryTimerRef = useRef<number | null>(null);
  const authorizeUrlRef = useRef<string | null>(null);

  // Fetch authorize_url on mount + on manual refresh
  useEffect(() => {
    let cancelled = false;
    setIsLoadingAuthorize(true);
    setAuthorizeError(null);
    setAuthorizeUrl(null);

    void authorizeFeishu()
      .then((data) => {
        if (cancelled) return;
        setAuthorizeUrl(data.authorize_url);
        authorizeUrlRef.current = data.authorize_url;
      })
      .catch((err) => {
        if (cancelled) return;
        setAuthorizeError(resolveFeishuError('backend', err));
      })
      .finally(() => {
        if (!cancelled) setIsLoadingAuthorize(false);
      });

    return () => {
      cancelled = true;
    };
  }, [refreshKey]);

  // Render QR when SDK + authorize_url are both ready
  useEffect(() => {
    if (!sdkReady || !authorizeUrl || !window.QRLogin) return;

    // 清理容器，防止叠加（Pitfall 2）
    const container = document.getElementById(QR_CONTAINER_ID);
    if (container) container.innerHTML = '';

    // 移除上一轮的 message listener，避免残留
    if (messageHandlerRef.current) {
      window.removeEventListener('message', messageHandlerRef.current);
      messageHandlerRef.current = null;
    }

    const instance = window.QRLogin({
      id: QR_CONTAINER_ID,
      goto: authorizeUrl,
      width: '260',
      height: '260',
      style: 'width:260px;height:260px;border:none',
    });
    instanceRef.current = instance;

    const handler = (event: MessageEvent): void => {
      // T-27-02: origin + data 双校验
      if (!instance.matchOrigin(event.origin)) return;
      if (!instance.matchData(event.data)) return;
      // Pitfall 1: 1.0.3 event.data 为 object { tmp_code: string }
      if (typeof event.data !== 'object' || event.data === null) return;
      const tmpCode = (event.data as { tmp_code?: unknown }).tmp_code;
      if (typeof tmpCode !== 'string' || tmpCode.length === 0) return;
      const base = authorizeUrlRef.current ?? authorizeUrl;
      // 手动拼接 tmp_code 并导航 — SDK 1.0.3 不会自动重定向（D-10 勘误）
      window.location.href = base + '&tmp_code=' + encodeURIComponent(tmpCode);
    };
    window.addEventListener('message', handler);
    messageHandlerRef.current = handler;

    // 启动 180s 过期计时
    if (expiryTimerRef.current !== null) {
      window.clearTimeout(expiryTimerRef.current);
    }
    expiryTimerRef.current = window.setTimeout(() => {
      setIsExpired(true);
    }, QR_EXPIRY_MS);

    return () => {
      if (expiryTimerRef.current !== null) {
        window.clearTimeout(expiryTimerRef.current);
        expiryTimerRef.current = null;
      }
      if (messageHandlerRef.current) {
        window.removeEventListener('message', messageHandlerRef.current);
        messageHandlerRef.current = null;
      }
    };
  }, [sdkReady, authorizeUrl]);

  const handleRefresh = useCallback(() => {
    setIsExpired(false);
    setAuthorizeError(null);
    setRefreshKey((n) => n + 1);
  }, []);

  const handleRetrySdk = useCallback(() => {
    retrySdk();
    setRefreshKey((n) => n + 1);
  }, [retrySdk]);

  // 错误优先级：SDK 加载失败 > authorize 请求失败
  const displayError: FeishuError | null = sdkError
    ? resolveFeishuError('sdk', sdkError)
    : authorizeError;

  return (
    <section
      className="surface-subtle"
      style={{ marginTop: 20, padding: '20px 20px' }}
    >
      <p className="eyebrow">飞书扫码登录</p>
      <h3
        style={{
          marginTop: 8,
          fontSize: 16,
          fontWeight: 600,
          letterSpacing: '-0.02em',
          color: 'var(--color-ink)',
        }}
      >
        打开飞书 App 扫码
      </h3>
      <p style={{ marginTop: 4, fontSize: 13, color: 'var(--color-steel)' }}>
        使用已开通系统权限的工号扫描下方二维码。
      </p>

      {displayError ? (
        <div
          style={{
            marginTop: 16,
            padding: '12px 14px',
            borderRadius: 6,
            border: '1px solid var(--color-border)',
            background: 'var(--color-bg-subtle)',
          }}
        >
          <p style={{ fontSize: 13, lineHeight: 1.6, color: 'var(--color-danger)' }}>
            {displayError.message}
          </p>
          <div style={{ marginTop: 10 }}>
            <button
              className="action-secondary"
              onClick={displayError.code === 'sdk_load_failed' ? handleRetrySdk : handleRefresh}
              type="button"
            >
              重试
            </button>
          </div>
        </div>
      ) : (
        <div
          style={{
            marginTop: 16,
            position: 'relative',
            width: 260,
            height: 260,
            marginLeft: 'auto',
            marginRight: 'auto',
          }}
        >
          <div
            id={QR_CONTAINER_ID}
            style={{ width: 260, height: 260 }}
          />
          {(!sdkReady || isLoadingAuthorize || !authorizeUrl) && !isExpired ? (
            <div
              style={{
                position: 'absolute',
                inset: 0,
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
                color: 'var(--color-steel)',
                fontSize: 13,
              }}
            >
              二维码加载中…
            </div>
          ) : null}
          {isExpired ? (
            <div
              style={{
                position: 'absolute',
                inset: 0,
                display: 'flex',
                flexDirection: 'column',
                alignItems: 'center',
                justifyContent: 'center',
                gap: 12,
                background: 'rgba(255,255,255,0.75)',
                backdropFilter: 'blur(4px)',
                WebkitBackdropFilter: 'blur(4px)',
              }}
            >
              <p style={{ fontSize: 13, color: 'var(--color-ink)' }}>二维码已过期</p>
              <button className="action-primary" onClick={handleRefresh} type="button">
                点击刷新
              </button>
            </div>
          ) : null}
        </div>
      )}
    </section>
  );
}
```

实施关键点（照 RESEARCH.md 所有 pitfalls 复核）：
- Pitfall 1：取 tmp_code 时加 typeof 防御（object + string 双检）
- Pitfall 2：每次重建前 container.innerHTML = '' 且移除旧 listener
- Pitfall 3：authorizeUrlRef 保存最新值，handler 取 ref 而非 closure 变量，避免刷新后旧 handler 拼错 URL
- Pitfall 4：script.onerror 捕获到 sdk_load_failed 错误
- Pitfall 5：所有 setTimeout 在 cleanup 中 clearTimeout
- Pitfall 6：不适用（不触碰 localStorage；由回调页处理）
- T-27-02：matchOrigin + matchData 双校验
- T-27-03：不 console.log；不记录 tmp_code
- T-27-01：displayError.message 直接文本插值（React escape）
  </action>
  <verify>
    <automated>cd frontend && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/auth/FeishuLoginPanel.tsx 文件存在
    - grep -q "export function FeishuLoginPanel" frontend/src/components/auth/FeishuLoginPanel.tsx 返回 0
    - grep -q "LarkSSOSDKWebQRCode-1.0.3.js" frontend/src/components/auth/FeishuLoginPanel.tsx 返回 0
    - grep -q "window.QRLogin" frontend/src/components/auth/FeishuLoginPanel.tsx 返回 0
    - grep -q "matchOrigin" frontend/src/components/auth/FeishuLoginPanel.tsx 返回 0
    - grep -q "matchData" frontend/src/components/auth/FeishuLoginPanel.tsx 返回 0
    - grep -q "tmp_code" frontend/src/components/auth/FeishuLoginPanel.tsx 返回 0
    - grep -q "encodeURIComponent(tmpCode)" frontend/src/components/auth/FeishuLoginPanel.tsx 返回 0
    - grep -q "180_000\|QR_EXPIRY_MS = 180_000" frontend/src/components/auth/FeishuLoginPanel.tsx 返回 0
    - grep -q "removeEventListener('message'" frontend/src/components/auth/FeishuLoginPanel.tsx 返回 0
    - grep -q "clearTimeout" frontend/src/components/auth/FeishuLoginPanel.tsx 返回 0
    - grep -q "script.remove()" frontend/src/components/auth/FeishuLoginPanel.tsx 返回 1（无匹配 — 确认未违反 D-03 单例策略）
    - grep -q "console.log\|console.error\|console.warn" frontend/src/components/auth/FeishuLoginPanel.tsx 返回 1（无匹配 — T-27-03）
    - grep -q "dangerouslySetInnerHTML" frontend/src/components/auth/FeishuLoginPanel.tsx 返回 1（无匹配 — T-27-01）
    - grep -q "onSuccess\|onReady\|onErr" frontend/src/components/auth/FeishuLoginPanel.tsx 返回 1（无匹配 — D-10 勘误：这些回调不存在）
    - grep -q "resolveFeishuError" frontend/src/components/auth/FeishuLoginPanel.tsx 返回 0
    - cd frontend && npm run lint 退出码 0
  </acceptance_criteria>
  <done>
    组件文件存在、tsc 通过；SDK 单例注入 hook、QRLogin 实例管理、postMessage 双校验、180s 过期蒙层、刷新流程、错误 banner 全部就位。未使用 onSuccess/onReady/onErr 回调（D-10 勘误）；未 console.log；未 script.remove。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 在 Login.tsx 右侧 section 集成 FeishuLoginPanel</name>
  <files>frontend/src/pages/Login.tsx</files>
  <read_first>
    - frontend/src/pages/Login.tsx（完整读，重点第 69-85 行右侧 section 结构）
    - frontend/src/components/auth/FeishuLoginPanel.tsx（Task 1 完成后存在）
    - .planning/phases/27-oauth2/27-CONTEXT.md D-13（面板放右侧 section、LoginForm 下方，LOGIN-04 现有登录保留）
  </read_first>
  <behavior>
    - Login.tsx 渲染结构保持左右双 section 格局不变
    - 右侧 section：eyebrow + h2 + LoginForm + 「账号由管理员开通」提示 — 全部保留
    - 在 LoginForm 的包裹 div 之后、「账号由管理员开通」提示之前插入 <FeishuLoginPanel />
    - 不给 FeishuLoginPanel 传 props（D-14 自洽，无外部 props）
    - 邮箱密码登录 handleLogin 路径完全不变
  </behavior>
  <action>
在 frontend/src/pages/Login.tsx 做以下修改：

1. 顶部 imports（约第 1-7 行）在 LoginForm 导入之后追加 FeishuLoginPanel 导入：
```tsx
import { FeishuLoginPanel } from '../components/auth/FeishuLoginPanel';
import { LoginForm } from '../components/auth/LoginForm';
```
（注意：按字母序 Feishu 在 Login 之前。）

2. 右侧 section（约第 69-85 行）在 LoginForm 包裹 div 之后插入 `<FeishuLoginPanel />`：
原结构：
```tsx
          <div style={{ marginTop: 20 }}>
            <LoginForm errorMessage={errorMessage} isSubmitting={isSubmitting} onSubmit={handleLogin} />
          </div>
          <div style={{ marginTop: 16, background: 'var(--color-bg-subtle)', ...}}>
            账号由管理员开通。首次登录需先改密。
          </div>
```
改为：
```tsx
          <div style={{ marginTop: 20 }}>
            <LoginForm errorMessage={errorMessage} isSubmitting={isSubmitting} onSubmit={handleLogin} />
          </div>
          <FeishuLoginPanel />
          <div style={{ marginTop: 16, background: 'var(--color-bg-subtle)', ...}}>
            账号由管理员开通。首次登录需先改密。
          </div>
```

不要改动 handleLogin、resolveError、useAuth 消费、现有 section 布局、「返回平台首页」链接。
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "import { FeishuLoginPanel }" frontend/src/pages/Login.tsx 返回 0
    - grep -q "<FeishuLoginPanel />" frontend/src/pages/Login.tsx 返回 0
    - grep -q "<LoginForm" frontend/src/pages/Login.tsx 返回 0（保留邮箱密码表单）
    - grep -q "账号由管理员开通" frontend/src/pages/Login.tsx 返回 0（保留提示）
    - grep -q "返回平台首页" frontend/src/pages/Login.tsx 返回 0（保留链接）
    - cd frontend && npm run lint 退出码 0
    - cd frontend && npm run build 退出码 0
  </acceptance_criteria>
  <done>
    /login 页面右侧 section 同时显示 LoginForm 与 FeishuLoginPanel；tsc + vite build 通过；handleLogin 逻辑未变。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 人工验证 Manual Test Plan（本 Plan 可跑的全部 14 项）</name>
  <files>frontend/src/components/auth/FeishuLoginPanel.tsx, frontend/src/pages/Login.tsx, frontend/src/pages/FeishuCallbackPage.tsx</files>
  <read_first>
    - .planning/phases/27-oauth2/27-RESEARCH.md 第 771-788 行（Manual Test Plan 14 项源头清单）
    - .planning/phases/27-oauth2/27-CONTEXT.md 全文
  </read_first>
  <what-built>
    - FeishuLoginPanel.tsx：SDK 单例注入、authorize 请求、QRLogin + postMessage 双校验、180s 过期蒙层、刷新流程、分类错误 banner
    - Login.tsx：右侧 section 集成飞书面板，保留邮箱密码表单
    - 已联动 Plan 01 的 resolveFeishuError、Plan 02 的 FeishuCallbackPage
  </what-built>
  <action>
执行以下步骤对 Phase 27 端到端进行人工验证。任何一步未通过则停止并记录根因，修复后重验。

**前置条件：**
1. 启动后端：在项目根目录运行 `.venv\Scripts\activate` 然后 `uvicorn backend.app.main:app --reload`（默认端口 8011）
2. 启动前端：`cd frontend && npm run dev`（默认端口 5174）
3. 确保 `.env` 中 `FEISHU_APP_ID`、`FEISHU_APP_SECRET`、`FEISHU_REDIRECT_URI=http://localhost:5174/auth/feishu/callback` 三项已在飞书开放平台后台登记，且 Redis 可用

**验证步骤：**

FUI-01 + FUI-03 相关：
1. 访问 http://localhost:5174/login → 右侧 section 的 LoginForm 下方出现「飞书扫码登录」面板，二维码在 2-3 秒内渲染（iframe）
2. 用飞书 App 扫描 → 手机弹出授权页 → 授权后浏览器自动跳 http://localhost:5174/auth/feishu/callback?code=...&state=...
3. 回调页展示处理中骨架 → 成功 → 跳 workspace（admin/hrbp/manager）或 /my-review（employee）
4. 首次绑定用户登录成功后，检查 localStorage：wage_adjust.access_token / refresh_token / user 已写；数据库 users 表该账号的 feishu_open_id 已写入
5. 停留 3 分钟不扫码 → 面板出现毛玻璃蒙层 + 「二维码已过期，点击刷新」按钮
6. 点「刷新」→ 蒙层消失、二维码重新渲染、倒计时重置；后端日志 DEBUG 可看到新 state 生成并旧 state 已删除

FUI-04 相关（错误分类）：
7. 在飞书 App 授权页点「拒绝」→ 回调页显示「你已取消飞书授权」+「返回登录」
8. 手动把 /auth/feishu/callback 的 state query 改成随机字符串再访问 → 回调页显示「会话已过期，请刷新二维码重试」
9. 用一个**未绑定系统 User** 的飞书账号（或工号不在 Employee 表）扫码 → 回调页显示「工号未匹配，请联系管理员开通」
10. 在 /login 页按 F12 打开开发者工具 → Network 面板勾选 "Offline" → 点 FeishuLoginPanel 的「重试」→ 面板内出现红色 banner「网络错误，请检查连接」+「重试」按钮
11. 临时改 QR_SDK_URL constant 到不存在的域名 smoke test（测完回滚）→ 面板显示「飞书登录组件加载失败，请刷新重试」
12. 用**已绑定过 feishu_open_id** 的账号再次扫码 → 后端走 fast path（跳过 employee_no 匹配）→ 正常进入工作区

FUI-04 + LOGIN-04 保留约束：
13. 在 /login 用 admin/hrbp/manager/employee 四个角色分别做**邮箱密码登录** → 行为与 Phase 27 之前完全一致
14. 用 must_change_password=true 的账号扫码登录 → 回调页成功后跳 /settings 并携带 { forcePasswordChange: true, from: role home } state

**代码级回归：**
- `cd frontend && npm run lint` 退出码 0
- `cd frontend && npm run build` 退出码 0
- `cd backend && .venv/Scripts/python -m pytest backend/tests/test_api/test_feishu_oauth_integration.py -x` 全部通过
- `cd backend && .venv/Scripts/python -m pytest backend/tests/test_api/test_auth.py -x` 全部通过（邮箱密码登录回归）
  </action>
  <how-to-verify>
Checklist（逐项打钩；任何项失败需 STOP 修复）：

- [ ] 步骤 1（QR 面板渲染）通过
- [ ] 步骤 2（飞书扫码跳转）通过
- [ ] 步骤 3（回调页三状态）通过
- [ ] 步骤 4（localStorage + feishu_open_id）通过
- [ ] 步骤 5（180s 过期）通过
- [ ] 步骤 6（刷新流程）通过
- [ ] 步骤 7（授权取消）通过
- [ ] 步骤 8（state 篡改）通过
- [ ] 步骤 9（工号未匹配）通过
- [ ] 步骤 10（网络错误）通过
- [ ] 步骤 11（SDK 加载失败）通过
- [ ] 步骤 12（feishu_open_id fast path）通过
- [ ] 步骤 13（邮箱密码回归 LOGIN-04）通过
- [ ] 步骤 14（must_change_password）通过
- [ ] npm run lint 通过
- [ ] npm run build 通过
- [ ] pytest test_feishu_oauth_integration.py 通过
- [ ] pytest test_auth.py 通过
  </how-to-verify>
  <verify>
    <automated>cd frontend && npm run build && cd ../backend && python -m pytest backend/tests/test_api/test_feishu_oauth_integration.py backend/tests/test_api/test_auth.py -x</automated>
  </verify>
  <acceptance_criteria>
    - 上述 18 项 checklist 全部打钩
    - npm run build 退出码 0
    - pytest 两个测试文件全通过
    - 用户输入 "approved" 或类似确认字符串
  </acceptance_criteria>
  <done>
    所有 14 个手动验证步骤 + 4 个自动化回归命令 + 自动化 verify 命令全部通过，人工确认签字。FUI-01 / FUI-03 / FUI-04 均可声明 DONE；LOGIN-04 保留约束经回归验证未违反。
  </done>
  <resume-signal>输入 "approved" 或描述具体失败项（格式：步骤 N：<现象>），未通过项需分析根因并修复后重验</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 飞书 CDN ↔ 浏览器 | QRLogin SDK 脚本通过 HTTPS CDN 加载；信任飞书签名与 CDN 完整性（未启用 SRI — 官方不提供 hash） |
| 飞书 iframe ↔ 宿主 window | postMessage 跨域通信，必须通过 instance.matchOrigin + matchData 双校验 |
| URL query（tmp_code）↔ window.location.href | tmp_code 必须 encodeURIComponent；跳转前不写入 history stack（由浏览器 location.href 自然替换） |
| setTimeout ↔ React 卸载 | 定时器回调如在组件卸载后执行会报 state-on-unmounted 警告；必须 clearTimeout |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Injection (XSS via error message) | FeishuLoginPanel 错误 banner | mitigate | 错误文案来自 feishuErrors.ts COPY 表；React 文本插值 {displayError.message} 自动 HTML escape；禁止 innerHTML / dangerouslySetInnerHTML |
| T-27-02 | Spoofing (postMessage origin) | 监听 window.message 事件 | mitigate | 在 handler 中先 `if (!instance.matchOrigin(event.origin)) return;` 再 `if (!instance.matchData(event.data)) return;` 双通过才使用 tmp_code；禁止信任任意 origin；禁止硬编码 origin 字符串（由 SDK matchOrigin 做版本一致的白名单校验） |
| T-27-03 | Information Disclosure (tmp_code/state 泄漏) | postMessage handler、setTimeout、刷新流程 | mitigate | 不 console.log / console.error 任何 event.data / state / tmp_code；拼 URL 时 encodeURIComponent(tmp_code)；跳转用 window.location.href（替换当前历史记录，不 push） |
| T-27-04 | Spoofing (Session Fixation via stale state) | 刷新按钮 | mitigate | 每次刷新都重新 fetch /authorize 取新 state；旧 state 后端会在新 state 生成/消费时自然失效（Redis TTL 300s）；前端不缓存旧 authorize_url 给下轮使用 |
| T-27-05 | Tampering (CDN script 供应链) | QR_SDK_URL 外部脚本 | accept | 不启用 SRI（飞书官方不提供 hash）；接受"信任飞书 CDN"前提；CSP 配置（生产部署）需把 lf-package-cn.feishucdn.com 列入 script-src，否则展示 sdk_load_failed 错误 |
| T-27-06 | DoS (Timer leak / state-on-unmounted) | useEffect 中的 setTimeout + message listener | mitigate | 所有 setTimeout 在 useEffect cleanup 中 clearTimeout；所有 addEventListener 在 cleanup 中 removeEventListener；单例 <script> 节点按 D-03 **不**在 cleanup 中移除 |

说明：本 Plan 是 Phase 27 唯一接触 SDK / postMessage / setTimeout 的位置，故 T-27-02 / T-27-05 / T-27-06 集中于此。T-27-01 / T-27-03 / T-27-04 在 Plan 02 也有覆盖（分摊）。
</threat_model>

<verification>
本 Plan 通过需要全部达成：

1. 自动化检查
   - `cd frontend && npm run lint` 退出码 0
   - `cd frontend && npm run build` 退出码 0
   - `cd backend && pytest backend/tests/test_api/test_feishu_oauth_integration.py -x` 通过
   - `cd backend && pytest backend/tests/test_api/test_auth.py -x` 通过

2. grep 扫描确认代码合规：
   - FeishuLoginPanel.tsx 含 matchOrigin、matchData、tmp_code、encodeURIComponent
   - 无 onSuccess/onReady/onErr 字段（D-10 勘误）
   - 无 console.log / script.remove() / dangerouslySetInnerHTML / innerHTML（除容器清空 container.innerHTML='' 之外）

3. Manual Test Plan 14 项 + 代码级回归全部打钩
</verification>

<success_criteria>
- [ ] FeishuLoginPanel 在 /login 页右侧 section 渲染，二维码 2-3s 内可见
- [ ] 飞书扫码成功后浏览器跳到 /auth/feishu/callback 并完成 JWT 获取
- [ ] 180s 过期后显示毛玻璃蒙层 + 刷新按钮；点击刷新重建 QR 且 state 变化
- [ ] 6 类错误（授权取消 / state 无效 / 工号未匹配 / 网络错误 / SDK 加载失败 / feishu 500）均显示对应中文文案
- [ ] must_change_password=true 账号飞书登录后跳 /settings
- [ ] 邮箱密码登录（LOGIN-04）行为未改变
- [ ] 组件卸载后 setTimeout 与 message listener 被清理（无 console 警告）
- [ ] 后端 test_feishu_oauth_integration.py + test_auth.py pytest 通过
- [ ] FUI-01 / FUI-03 / FUI-04 全部验收
</success_criteria>

<output>
完成后生成 `.planning/phases/27-oauth2/27-03-SUMMARY.md`，记录：
- FeishuLoginPanel.tsx 与 Login.tsx 的最终结构（关键代码片段）
- Manual Test Plan 14 项逐项执行结果（通过 / 失败及根因）
- 生产部署 CSP 注意事项（script-src 需允许 lf-package-cn.feishucdn.com）
- 已知限制：SRI 未启用（飞书 CDN 无 hash）；本地 dev 需飞书开放平台后台登记 http://localhost:5174/auth/feishu/callback，否则 302 会被拒
- FUI-01 / FUI-03 / FUI-04 验收断言
</output>
