---
phase: 27-oauth2
plan: 02
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - frontend/src/hooks/useAuth.tsx
  - frontend/src/pages/FeishuCallbackPage.tsx
  - frontend/src/App.tsx
autonomous: true
requirements: [FUI-02, FUI-04]
must_haves:
  truths:
    - "useAuth() 返回对象新增 loginWithFeishu(code, state) 方法，调用它并拿到有效 code/state 后会写入 localStorage 并更新 user / accessToken 状态"
    - "访问 /auth/feishu/callback?code=X&state=Y 能看到处理中骨架卡片；成功后短暂停留并跳转到 getRoleHomePath(role)（或 must_change_password 时跳 /settings）"
    - "code/state 缺失或后端返回错误时，回调页显示居中错误卡片 + 「返回登录」按钮，按钮跳 /login"
    - "错误文案来自 resolveFeishuError('backend', err).message，与 feishuErrors.ts 的 COPY 表一致"
    - "/auth/feishu/callback 是公开路由（不受 ProtectedRoute 包裹），未登录状态下可直接访问"
    - "cd frontend && npm run lint 退出码 0；cd frontend && npm run build 退出码 0"
  artifacts:
    - path: frontend/src/hooks/useAuth.tsx
      provides: AuthContextValue.loginWithFeishu
      contains: "loginWithFeishu"
    - path: frontend/src/pages/FeishuCallbackPage.tsx
      provides: /auth/feishu/callback 的 React 组件
      contains: "export function FeishuCallbackPage"
    - path: frontend/src/App.tsx
      provides: /auth/feishu/callback 公开路由注册
      contains: "path=\"/auth/feishu/callback\""
  key_links:
    - from: frontend/src/pages/FeishuCallbackPage.tsx
      to: frontend/src/hooks/useAuth.tsx
      via: useAuth().loginWithFeishu
      pattern: "loginWithFeishu"
    - from: frontend/src/pages/FeishuCallbackPage.tsx
      to: frontend/src/utils/feishuErrors.ts
      via: resolveFeishuError('backend', err)
      pattern: "resolveFeishuError"
    - from: frontend/src/hooks/useAuth.tsx
      to: frontend/src/services/auth.ts
      via: feishuCallback(code, state)
      pattern: "feishuCallback"
    - from: frontend/src/App.tsx
      to: frontend/src/pages/FeishuCallbackPage.tsx
      via: <Route path="/auth/feishu/callback" element={<FeishuCallbackPage />} />（公开路由）
      pattern: "FeishuCallbackPage"
---

<objective>
扩展 useAuth context 与新增独立的 FeishuCallbackPage，完整实现 OAuth 回调链路：
1. useAuth() 补 loginWithFeishu(code, state)，内部调 services/auth.ts 的 feishuCallback 并同步 storeAuthSession + setUser + setAccessToken
2. 新建 FeishuCallbackPage：useSearchParams 解析 code/state → 调 loginWithFeishu → 三状态 UI（processing / success / failed）→ 成功后 navigate 到 role home（或 must_change_password 时到 /settings）
3. App.tsx 注册 /auth/feishu/callback 为公开路由（不进 ProtectedRoute）

Purpose: 让飞书授权后的浏览器 302 重定向能在前端完成 JWT 获取与工作区跳转，并在失败时展示分类中文错误。

Output: useAuth.tsx 扩展、FeishuCallbackPage.tsx 新建、App.tsx 路由表加一行。手动验证路径覆盖 Manual Test Plan 的 #2 #3 #7 #8 #9 #12 #14 共 7 项。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/27-oauth2/27-CONTEXT.md
@.planning/phases/27-oauth2/27-RESEARCH.md
@.planning/phases/27-oauth2/27-01-PLAN.md
@frontend/src/hooks/useAuth.tsx
@frontend/src/pages/Login.tsx
@frontend/src/App.tsx
@frontend/src/services/auth.ts
@frontend/src/types/api.ts
@frontend/src/utils/roleAccess.ts
@frontend/src/utils/feishuErrors.ts

<interfaces>
<!-- Plan 01 已交付：feishuCallback(code, state) → Promise<AuthResponse>，resolveFeishuError('backend', err) → { code, message } -->
<!-- 现有参考：useAuth.handleLogin（frontend/src/hooks/useAuth.tsx:112-129）揭示登录后 storeAuthSession + setUser + setAccessToken 的完整模式 -->
<!-- 现有参考：Login.tsx:28-38（must_change_password 跳转 /settings；否则跳 getRoleHomePath(profile.role)） -->
<!-- 现有参考：App.tsx:49-56（resolveError 读 detail || message 同兼容模式） -->

useAuth 需要扩展的接口（frontend/src/hooks/useAuth.tsx:19-28）:
```ts
type AuthContextValue = {
  user: UserProfile | null;
  accessToken: string | null;
  isAuthenticated: boolean;
  isBootstrapping: boolean;
  login: (payload: LoginPayload) => Promise<UserProfile>;
  loginWithFeishu: (code: string, state: string) => Promise<UserProfile>;  // NEW
  register: (payload: RegisterPayload) => Promise<UserProfile>;
  refreshProfile: () => Promise<UserProfile | null>;
  logout: () => void;
};
```

App.tsx 公开路由段（frontend/src/App.tsx:420-423）现状：
```tsx
<Route element={<HomePage />} path="/" />
<Route element={<ApiDocsPage />} path="/api-docs" />
<Route element={<LoginPage />} path="/login" />
<Route element={<Navigate replace to="/login" />} path="/register" />
```
新增路由插入位置：在 `<Route element={<LoginPage />} path="/login" />` 之后、`<Route element={<Navigate ... />} path="/register" />` 之前。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 在 useAuth 中新增 loginWithFeishu 方法</name>
  <files>frontend/src/hooks/useAuth.tsx</files>
  <read_first>
    - frontend/src/hooks/useAuth.tsx（完整读，重点第 3-17 行 import、第 19-28 行 AuthContextValue、第 112-129 行 handleLogin 模式、第 148-160 行 useMemo value）
    - frontend/src/services/auth.ts（Plan 01 Task 3 新增的 feishuCallback 导出）
    - frontend/src/types/api.ts（Plan 01 Task 1 新增的 UserProfile.feishu_open_id 字段）
    - .planning/phases/27-oauth2/27-CONTEXT.md D-07（loginWithFeishu 签名）
    - .planning/phases/27-oauth2/27-RESEARCH.md 第 642-660 行（Example 3 verbatim）
    - .planning/phases/27-oauth2/27-RESEARCH.md 第 538-546 行（Pitfall 6：必须同时 storeAuthSession + setUser + setAccessToken，否则 ProtectedRoute 不会认账）
  </read_first>
  <behavior>
    - loginWithFeishu('abc', 'xyz') 成功时：调用 feishuCallback('abc', 'xyz') 拿到 AuthResponse → storeAuthSession(response) → setUser(response.user) → setAccessToken(response.tokens.access_token) → 返回 response.user
    - loginWithFeishu 失败时：不写 localStorage、不动 context state，异常原样向上抛让调用方 catch
    - AuthContextValue 类型增加 loginWithFeishu 字段；AuthProvider 的 useMemo value 中加入该字段
    - useMemo 依赖数组不需要改（handleLoginWithFeishu 不闭包任何可变外部变量；setUser/setAccessToken 是 useState 稳定引用）
  </behavior>
  <action>
在 frontend/src/hooks/useAuth.tsx 做以下修改：

1. 顶部 import（第 4-17 行）的 services/auth 导入列表增加 feishuCallback：
原：
```ts
import {
  AUTH_SESSION_EVENT,
  clearAuthStorage,
  fetchCurrentUser,
  getStoredAccessToken,
  getStoredRefreshToken,
  getStoredUser,
  login as loginRequest,
  refresh as refreshRequest,
  register as registerRequest,
  storeAuthSession,
  updateStoredTokens,
  updateStoredUser,
} from '../services/auth';
```
改为：
```ts
import {
  AUTH_SESSION_EVENT,
  clearAuthStorage,
  feishuCallback as feishuCallbackRequest,
  fetchCurrentUser,
  getStoredAccessToken,
  getStoredRefreshToken,
  getStoredUser,
  login as loginRequest,
  refresh as refreshRequest,
  register as registerRequest,
  storeAuthSession,
  updateStoredTokens,
  updateStoredUser,
} from '../services/auth';
```

2. AuthContextValue 类型（第 19-28 行）增加 loginWithFeishu 字段，放在 login 后、register 前：
```ts
type AuthContextValue = {
  user: UserProfile | null;
  accessToken: string | null;
  isAuthenticated: boolean;
  isBootstrapping: boolean;
  login: (payload: LoginPayload) => Promise<UserProfile>;
  loginWithFeishu: (code: string, state: string) => Promise<UserProfile>;
  register: (payload: RegisterPayload) => Promise<UserProfile>;
  refreshProfile: () => Promise<UserProfile | null>;
  logout: () => void;
};
```

3. 在 handleLogin 函数（第 112-129 行）之后、refreshProfile 函数之前，新增 handleLoginWithFeishu 函数：
```ts
  async function handleLoginWithFeishu(code: string, state: string): Promise<UserProfile> {
    const response = await feishuCallbackRequest(code, state);
    storeAuthSession(response);
    setUser(response.user);
    setAccessToken(response.tokens.access_token);
    return response.user;
  }
```

4. useMemo value 对象（第 148-160 行）增加 loginWithFeishu 字段，与 login 紧邻：
```ts
  const value = useMemo<AuthContextValue>(
    () => ({
      user,
      accessToken,
      isAuthenticated: Boolean(user && accessToken),
      isBootstrapping,
      login: handleLogin,
      loginWithFeishu: handleLoginWithFeishu,
      register: handleRegister,
      refreshProfile,
      logout,
    }),
    [accessToken, isBootstrapping, user],
  );
```
依赖数组保持 `[accessToken, isBootstrapping, user]` 不变（handleLoginWithFeishu 与 handleLogin 的稳定性一样由 closure-over-setState 提供，不需入依赖）。

不要在 handleLoginWithFeishu 里 try/catch，不要记录任何 log。如果你发现需要改 handleLogin 或其他函数，请停止并说明理由（Plan 02 不应该触发 handleLogin 的任何修改）。
  </action>
  <verify>
    <automated>cd frontend && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "loginWithFeishu: (code: string, state: string)" frontend/src/hooks/useAuth.tsx 返回 0
    - grep -q "async function handleLoginWithFeishu" frontend/src/hooks/useAuth.tsx 返回 0
    - grep -q "feishuCallback as feishuCallbackRequest" frontend/src/hooks/useAuth.tsx 返回 0
    - grep -q "loginWithFeishu: handleLoginWithFeishu" frontend/src/hooks/useAuth.tsx 返回 0
    - grep -c "storeAuthSession(response)" frontend/src/hooks/useAuth.tsx 至少 1
    - cd frontend && npm run lint 退出码 0
  </acceptance_criteria>
  <done>
    useAuth() 消费方可直接 `const { loginWithFeishu } = useAuth()` 且 TypeScript 不报错；既有 login / register / refreshProfile / logout 行为未改变。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 新建 FeishuCallbackPage 组件（处理/成功/失败三状态）</name>
  <files>frontend/src/pages/FeishuCallbackPage.tsx</files>
  <read_first>
    - frontend/src/pages/Login.tsx（现有 must_change_password 跳转 /settings 的完整模式，第 28-44 行）
    - frontend/src/hooks/useAuth.tsx（Task 1 完成后 loginWithFeishu 可用）
    - frontend/src/utils/feishuErrors.ts（Plan 01 Task 2 的 resolveFeishuError）
    - frontend/src/utils/roleAccess.ts（getRoleHomePath 函数签名：接受 role string、返回 path string）
    - .planning/phases/27-oauth2/27-CONTEXT.md D-05 / D-06
    - .planning/phases/27-oauth2/27-RESEARCH.md 第 380-436 行（Pattern 4 完整参考实现）
    - .planning/phases/27-oauth2/27-RESEARCH.md 第 504-517 行（Pitfall 3：依赖数组与 useRef hasRun 策略，防止 code 被重放）
    - frontend/src/hooks/usePolling.ts 第 56-64 行（项目 cleanup 模式参考）
  </read_first>
  <behavior>
    - 组件挂载时先解析 URL query 的 code / state
    - 若 code 或 state 缺失：立即 setState('failed')，errorMessage 用 "缺少授权参数"（映射到 unknown_error 的 COPY）
    - 若 code/state 齐全：调 loginWithFeishu(code, state)；成功 → setState('success') → 根据 profile.must_change_password 选择跳 /settings（携带 { forcePasswordChange: true, from: getRoleHomePath(profile.role) } state）或 getRoleHomePath(profile.role)；失败 → setState('failed') + errorMessage = resolveFeishuError('backend', err).message
    - 导航使用 navigate(target, { replace: true })（T-27-03 防 state/code 进历史栈）
    - useEffect 含 cancelled flag + useRef hasRun 防止 StrictMode 双挂载导致 code 被后端拒绝（"授权码已使用"）
    - 三状态 UI：
      - processing：surface 类卡片居中，文本 "正在完成飞书登录…"
      - success：短暂显示 "登录成功，正在跳转…"（也可直接渲染 null，因为 navigate 立即发生）
      - failed：surface 类错误卡片，居中，显示 errorMessage + action-primary「返回登录」按钮（onClick → navigate('/login', { replace: true })）
    - 不展示 code / state 原文；不用 innerHTML
  </behavior>
  <action>
新建文件 frontend/src/pages/FeishuCallbackPage.tsx，内容如下（完全 verbatim）：

```tsx
import { useEffect, useRef, useState } from 'react';
import { useNavigate, useSearchParams } from 'react-router-dom';

import { useAuth } from '../hooks/useAuth';
import { resolveFeishuError } from '../utils/feishuErrors';
import { getRoleHomePath } from '../utils/roleAccess';

type CallbackState = 'processing' | 'success' | 'failed';

export function FeishuCallbackPage() {
  const [searchParams] = useSearchParams();
  const navigate = useNavigate();
  const { loginWithFeishu } = useAuth();
  const [state, setState] = useState<CallbackState>('processing');
  const [errorMessage, setErrorMessage] = useState<string>('');
  const hasRunRef = useRef(false);

  useEffect(() => {
    if (hasRunRef.current) return;
    hasRunRef.current = true;

    let cancelled = false;
    const code = searchParams.get('code');
    const stateParam = searchParams.get('state');

    if (!code || !stateParam) {
      setState('failed');
      setErrorMessage(resolveFeishuError('backend', new Error('缺少授权参数')).message);
      return () => {
        cancelled = true;
      };
    }

    async function run() {
      try {
        const profile = await loginWithFeishu(code!, stateParam!);
        if (cancelled) return;
        setState('success');
        if (profile.must_change_password) {
          navigate('/settings', {
            replace: true,
            state: { forcePasswordChange: true, from: getRoleHomePath(profile.role) },
          });
        } else {
          navigate(getRoleHomePath(profile.role), { replace: true });
        }
      } catch (err) {
        if (cancelled) return;
        setState('failed');
        setErrorMessage(resolveFeishuError('backend', err).message);
      }
    }
    void run();

    return () => {
      cancelled = true;
    };
  }, [searchParams, loginWithFeishu, navigate]);

  return (
    <main
      style={{
        minHeight: '100vh',
        background: 'var(--color-bg-page)',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        padding: '24px 20px',
      }}
    >
      <section
        className="surface animate-fade-up"
        style={{ padding: '32px 32px', maxWidth: 420, width: '100%', textAlign: 'center' }}
      >
        {state === 'processing' ? (
          <>
            <p className="eyebrow">飞书登录</p>
            <h1
              style={{
                marginTop: 8,
                fontSize: 22,
                fontWeight: 600,
                letterSpacing: '-0.02em',
                color: 'var(--color-ink)',
              }}
            >
              正在完成飞书登录…
            </h1>
            <p style={{ marginTop: 12, fontSize: 13, lineHeight: 1.7, color: 'var(--color-steel)' }}>
              正在校验授权，请稍候。
            </p>
          </>
        ) : null}

        {state === 'success' ? (
          <>
            <p className="eyebrow">飞书登录</p>
            <h1
              style={{
                marginTop: 8,
                fontSize: 22,
                fontWeight: 600,
                letterSpacing: '-0.02em',
                color: 'var(--color-ink)',
              }}
            >
              登录成功，正在跳转…
            </h1>
          </>
        ) : null}

        {state === 'failed' ? (
          <>
            <p className="eyebrow">飞书登录失败</p>
            <h1
              style={{
                marginTop: 8,
                fontSize: 22,
                fontWeight: 600,
                letterSpacing: '-0.02em',
                color: 'var(--color-ink)',
              }}
            >
              无法完成登录
            </h1>
            <p
              style={{
                marginTop: 12,
                fontSize: 13,
                lineHeight: 1.7,
                color: 'var(--color-danger)',
              }}
            >
              {errorMessage}
            </p>
            <div style={{ marginTop: 20 }}>
              <button
                className="action-primary"
                onClick={() => navigate('/login', { replace: true })}
                type="button"
              >
                返回登录
              </button>
            </div>
          </>
        ) : null}
      </section>
    </main>
  );
}
```

严格遵守：
- 用 {errorMessage} 注入文本节点（React 自动 HTML escape） — T-27-01 防 XSS
- navigate 永远带 { replace: true } — T-27-03 防 code/state 进历史栈
- 不 console.log 任何 code / state / err — T-27-03
- 不用 innerHTML / dangerouslySetInnerHTML
  </action>
  <verify>
    <automated>cd frontend && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/FeishuCallbackPage.tsx 文件存在
    - grep -q "export function FeishuCallbackPage" frontend/src/pages/FeishuCallbackPage.tsx 返回 0
    - grep -q "useSearchParams" frontend/src/pages/FeishuCallbackPage.tsx 返回 0
    - grep -q "loginWithFeishu" frontend/src/pages/FeishuCallbackPage.tsx 返回 0
    - grep -q "resolveFeishuError" frontend/src/pages/FeishuCallbackPage.tsx 返回 0
    - grep -c "replace: true" frontend/src/pages/FeishuCallbackPage.tsx 至少 2（登录成功 navigate + 返回登录按钮）
    - grep -q "dangerouslySetInnerHTML\|innerHTML" frontend/src/pages/FeishuCallbackPage.tsx 返回 1（无匹配）
    - grep -q "hasRunRef" frontend/src/pages/FeishuCallbackPage.tsx 返回 0（防 StrictMode 双提交）
    - cd frontend && npm run lint 退出码 0
  </acceptance_criteria>
  <done>
    文件存在、tsc 通过；三状态 UI（processing / success / failed）在 JSX 中存在且可由 setState 控制；无 innerHTML / console.log / 无未清理的 setTimeout。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 在 App.tsx 注册 /auth/feishu/callback 公开路由</name>
  <files>frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/App.tsx（完整读；重点第 1-32 行 imports、第 419-424 行公开路由段、第 425-464 行 ProtectedRoute 段）
    - frontend/src/pages/FeishuCallbackPage.tsx（Task 2 完成后存在）
    - .planning/phases/27-oauth2/27-CONTEXT.md D-04（路由必须是公开，不在 ProtectedRoute 下）
  </read_first>
  <behavior>
    - /auth/feishu/callback 可匿名访问（未登录状态下不被 ProtectedRoute 重定向到 /login）
    - 路由渲染 <FeishuCallbackPage />
    - 插入位置紧邻 /login、/register 等公开路由段
    - 其他路由保持不变
  </behavior>
  <action>
在 frontend/src/App.tsx 做两处修改：

1. 顶部 import 段（约第 19-28 行之间），按字母序插入 FeishuCallbackPage 导入。与 FeishuConfigPage（`import { FeishuConfigPage } from "./pages/FeishuConfig";`）相邻即可：
```tsx
import { FeishuCallbackPage } from "./pages/FeishuCallbackPage";
import { FeishuConfigPage } from "./pages/FeishuConfig";
```
（注意：保持项目既有的双引号字符串风格。）

2. 路由表（约第 420-423 行的公开路由段），在 `<Route element={<LoginPage />} path="/login" />` 之后、`<Route element={<Navigate replace to="/login" />} path="/register" />` 之前插入一行：
```tsx
          <Route element={<FeishuCallbackPage />} path="/auth/feishu/callback" />
```
最终该段形如：
```tsx
          <Route element={<HomePage />} path="/" />
          <Route element={<ApiDocsPage />} path="/api-docs" />
          <Route element={<LoginPage />} path="/login" />
          <Route element={<FeishuCallbackPage />} path="/auth/feishu/callback" />
          <Route element={<Navigate replace to="/login" />} path="/register" />
```

不要改任何 ProtectedRoute 包装，不要动其它路由。新路由**不在** ProtectedRoute 内 — 这就是 D-04 要求的"公开路由"实现。
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'import { FeishuCallbackPage }' frontend/src/App.tsx 返回 0
    - grep -q 'path="/auth/feishu/callback"' frontend/src/App.tsx 返回 0
    - grep -q 'element={<FeishuCallbackPage />}' frontend/src/App.tsx 返回 0
    - Verify the Route is NOT nested under ProtectedRoute: the FeishuCallbackPage route line number must be numerically smaller than the first `ProtectedRoute` element line — check with: `grep -n 'ProtectedRoute\|FeishuCallbackPage' frontend/src/App.tsx` then confirm FeishuCallbackPage line < first ProtectedRoute element line
    - cd frontend && npm run lint 退出码 0
    - cd frontend && npm run build 退出码 0（完整 tsc -b + vite build）
  </acceptance_criteria>
  <done>
    未登录用户可访问 /auth/feishu/callback；vite build 产出包含该路由声明；所有其他路由、ProtectedRoute 包装未变。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query ↔ React Router | 浏览器 URL 中的 code/state 不可信，需校验存在性后才交给后端 |
| FeishuCallbackPage ↔ 浏览器 History API | 使用 navigate 时必须 replace:true，防止 code/state 留在历史栈 |
| useAuth.loginWithFeishu ↔ localStorage | JWT 写入 localStorage（通过 storeAuthSession），XSS 场景下可被读取 — 与既有 handleLogin 风险同级 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Injection (XSS via error message) | FeishuCallbackPage 渲染 errorMessage | mitigate | errorMessage 仅来自 resolveFeishuError('backend', err).message，由前端 COPY 表映射；React 文本插值 {errorMessage} 自动 escape；不使用 innerHTML / dangerouslySetInnerHTML |
| T-27-03 | Information Disclosure (code/state 泄漏) | URL query 与浏览器历史栈 | mitigate | 所有 navigate 调用使用 { replace: true }；不 console.log code/state；回调页渲染不包含 code/state 明文 |
| T-27-04 | Spoofing (Session Fixation) | loginWithFeishu 接受任意 code/state | mitigate | 后端 Phase 26 已做 state Redis TTL 300s + 一次性消费 + code 防重放 600s；前端只负责原样转发，不复用旧 state |
| T-27-06 | DoS (定时器 / 状态更新在卸载组件上) | FeishuCallbackPage useEffect | mitigate | useEffect cleanup 设置 cancelled=true flag；setState 调用前检查 cancelled；useRef hasRunRef 防止 StrictMode 双挂载导致 code 被后端拒绝（"授权码已使用"）|
| T-27-02 | Spoofing (postMessage origin) | 不适用本 Plan | transfer | 由 Plan 03 FeishuLoginPanel 处理 postMessage 校验 |
| T-27-05 | Tampering (CDN script) | 不适用本 Plan | transfer | 由 Plan 03 处理 SDK CDN 注入 |
</threat_model>

<verification>
完成本 Plan 后，要求以下全部通过：

1. `cd frontend && npm run lint` → 退出码 0
2. `cd frontend && npm run build` → 退出码 0（完整 tsc -b + vite build）
3. 手动启动前后端（`uvicorn backend.app.main:app --reload` + `cd frontend && npm run dev`）后：
   - 手动访问 `http://localhost:5174/auth/feishu/callback?code=invalid&state=invalid` → 预期展示红色错误卡片 + 「返回登录」按钮；点击按钮跳回 `/login`
   - 手动访问 `http://localhost:5174/auth/feishu/callback`（无 query）→ 预期立即展示错误卡片
   - 邮箱密码登录回归：访问 `/login`、输入合法账号密码、验证仍能登录成功 → 确认未破坏 LOGIN-04
4. 后端回归：`pytest backend/tests/test_api/test_feishu_oauth_integration.py` 无失败
</verification>

<success_criteria>
- [ ] AuthContextValue.loginWithFeishu 可从 useAuth() 解构使用且 tsc 不报错
- [ ] /auth/feishu/callback 路由可匿名访问
- [ ] FeishuCallbackPage 三状态 UI（processing / success / failed）均有 JSX 分支
- [ ] 失败时错误文案来自 resolveFeishuError 且与 COPY 表一致（人工对照 4 类典型错误）
- [ ] 成功时 navigate 使用 { replace: true }
- [ ] 邮箱密码登录回归：/login 页面行为不变
- [ ] 后端 pytest test_feishu_oauth_integration.py 通过
- [ ] FUI-02 完整实现；FUI-04 在回调页场景实现
</success_criteria>

<output>
完成后生成 `.planning/phases/27-oauth2/27-02-SUMMARY.md`，记录：
- 3 个文件的 diff 摘要
- 手动验证 Manual Test Plan #2 #3 #7 #8 #9 #12 #14 中在本 Plan 可触发的项（#3 部分可验；#7/#8/#9 需要真实飞书登录，已在 Plan 03 完成后再跑完整链路）
- 依赖 Plan 03 完成才能完整验证的项（QR 面板 + 真实飞书扫码链路）
</output>
