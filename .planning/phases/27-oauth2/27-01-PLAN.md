---
phase: 27-oauth2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/api.ts
  - frontend/src/services/auth.ts
  - frontend/src/utils/feishuErrors.ts
autonomous: true
requirements: [FUI-04]
must_haves:
  truths:
    - "types/api.ts 存在 FeishuAuthorizeResponse、FeishuCallbackPayload 两个 interface 并被其他模块 import 不报错"
    - "UserProfile 接口包含 feishu_open_id: string | null 字段"
    - "services/auth.ts 暴露 authorizeFeishu() 与 feishuCallback(code, state) 两个导出函数"
    - "utils/feishuErrors.ts 暴露 resolveFeishuError(source, payload) 能根据后端 HTTP 400/403/503/网络错误返回 8 类错误码对应的中文文案"
    - "cd frontend && npm run lint 退出码为 0"
  artifacts:
    - path: frontend/src/types/api.ts
      provides: 飞书前端集成类型
      contains: "FeishuAuthorizeResponse"
    - path: frontend/src/services/auth.ts
      provides: authorizeFeishu + feishuCallback 导出
      contains: "export async function authorizeFeishu"
    - path: frontend/src/utils/feishuErrors.ts
      provides: resolveFeishuError 映射函数 + FeishuErrorCode 类型
      contains: "export function resolveFeishuError"
  key_links:
    - from: frontend/src/services/auth.ts
      to: /api/v1/auth/feishu/authorize
      via: api.get
      pattern: "api.get<FeishuAuthorizeResponse>\\('/auth/feishu/authorize'\\)"
    - from: frontend/src/services/auth.ts
      to: /api/v1/auth/feishu/callback
      via: api.post
      pattern: "api.post<AuthResponse>\\('/auth/feishu/callback'"
    - from: frontend/src/utils/feishuErrors.ts
      to: frontend/src/types/api.ts
      via: 读取 axios 错误 response.data.message（兼容 detail）
      pattern: "axios.isAxiosError"
---

<objective>
为 Phase 27 建立类型契约、服务封装与错误映射基础设施：types/api.ts 加 2 个 interface 并补齐 UserProfile.feishu_open_id 字段；services/auth.ts 新增 authorizeFeishu() 与 feishuCallback() 两个 HTTP 封装；utils/feishuErrors.ts 新增 resolveFeishuError() 集中管理 8 类中文错误文案。

Purpose: 所有下游 Plan 02（useAuth + 回调页）与 Plan 03（QR 面板）都依赖这三个文件。先建契约、再实现，避免执行时来回探索后端 shape。

Output: 3 个源文件的修改 / 新建。全部是纯类型 + 纯函数，无 React、无 DOM，无副作用。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/27-oauth2/27-CONTEXT.md
@.planning/phases/27-oauth2/27-RESEARCH.md
@backend/app/api/v1/auth.py
@backend/app/services/feishu_oauth_service.py
@backend/app/schemas/user.py
@backend/app/main.py
@frontend/src/services/auth.ts
@frontend/src/services/api.ts
@frontend/src/types/api.ts
@.env.example

<interfaces>
<!-- 后端已在 Phase 26 交付的契约。不再 grep 探索。 -->
<!-- Source: backend/app/schemas/user.py:37,92-95,154-156 + backend/app/services/feishu_oauth_service.py:21-26,89-138 -->

GET /api/v1/auth/feishu/authorize 响应（backend/app/api/v1/auth.py:269-277）:
```ts
// 后端 FastAPI 返回 plain dict，前端以 interface 约束
interface FeishuAuthorizeResponse {
  authorize_url: string;  // 完整飞书授权 URL，已含 client_id/redirect_uri/state
  state: string;          // CSRF token (base64url, 32 bytes)
}
```

POST /api/v1/auth/feishu/callback 请求与响应:
```ts
interface FeishuCallbackPayload {
  code: string;   // 飞书授权码，1..512
  state: string;  // CSRF state，1..128
}

interface AuthResponse {  // 已存在于 frontend/src/types/api.ts
  user: UserProfile;
  tokens: TokenPair;
}
```

错误响应 shape（已验证：backend/app/main.py:132-142 exception_handler 改写 HTTPException）:
```json
{ "error": "http_error", "message": "<中文 detail>" }
```
前端应优先读 response.data.message；兼容读 response.data.detail 作为回退（App.tsx:51 现有模式）。

后端错误 → 中文 detail → 前端分类（从 backend/app/services/feishu_oauth_service.py + _FEISHU_ERROR_MAP 推导）:

| HTTP | detail 原文 | 前端分类 |
|------|-------------|----------|
| 400 | `无效的 state 参数，请重新发起授权` | state_invalid_or_expired |
| 400 | `授权码已使用` | code_expired_or_replayed |
| 400 | `授权码已使用或已过期` | code_expired_or_replayed |
| 400 | `授权码已过期，请重新授权` | code_expired_or_replayed |
| 400 | `工号未匹配，请联系管理员开通` | employee_not_matched |
| 403 | `应用未获得用户授权，请联系管理员` | authorization_cancelled |
| 500 | `飞书应用配置错误，请联系管理员` | unknown_error |
| 502 | `飞书认证服务异常` / `获取飞书用户信息失败` | unknown_error |
| 503 | `认证服务暂不可用，请稍后重试` | redis_unavailable |
| 无 response | axios network 错误 | network_error |

UserProfile 当前状态（frontend/src/types/api.ts:7-18）缺 `feishu_open_id`（UserRead Pydantic 已有，见 backend/app/schemas/user.py:37）。
</interfaces>

<assumption_verification_outcome>
<!-- A4 + A5 已在 planning 阶段通过代码 grep 验证，不再作为 Wave 0 任务。 -->
<!-- A4: backend/app/main.py:132-142 确认 @app.exception_handler(HTTPException) 改写为 {error, message} 格式 -->
<!-- A5: .env.example:61 确认 FEISHU_REDIRECT_URI=http://localhost:5174/auth/feishu/callback 指向前端路由 -->
<!-- 若执行期间发现实际部署 .env 与 .env.example 不一致（redirect_uri 指向后端），应 BLOCK 并汇报。 -->
</assumption_verification_outcome>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 添加飞书前端类型定义</name>
  <files>frontend/src/types/api.ts</files>
  <read_first>
    - frontend/src/types/api.ts（完整读，定位 UserProfile 定义在第 7-18 行；AuthResponse 在第 52-55 行）
    - .planning/phases/27-oauth2/27-CONTEXT.md（D-07 确认 feishuCallback 返回 AuthResponse）
    - .planning/phases/27-oauth2/27-RESEARCH.md 第 122-206 行（后端契约 verbatim）
    - backend/app/schemas/user.py（UserRead.feishu_open_id 在第 37 行）
  </read_first>
  <behavior>
    - UserProfile.feishu_open_id 字段存在且类型为 string | null，与后端 UserRead.feishu_open_id: Optional[str] 对齐
    - FeishuAuthorizeResponse 含 authorize_url 与 state 两字段，均为 string
    - FeishuCallbackPayload 含 code 与 state 两字段，均为 string
    - 全部使用 interface（不使用 type alias），与项目现有风格一致
    - 添加位置紧邻 AuthResponse（第 52-55 行之后），保证相关类型归档相近
  </behavior>
  <action>
在 frontend/src/types/api.ts 做以下修改，不要改动任何其他已存在字段：

1. UserProfile（第 7-18 行）在 `employee_no: string | null;` 之后插入一行：
```ts
  feishu_open_id: string | null;
```
最终 UserProfile 形状：
```ts
export interface UserProfile {
  id: string;
  email: string;
  role: string;
  id_card_no: string | null;
  must_change_password: boolean;
  employee_id: string | null;
  employee_name: string | null;
  employee_no: string | null;
  feishu_open_id: string | null;
  departments: DepartmentRecord[];
  created_at: string;
}
```

2. 在 AuthResponse（第 52-55 行）之后、`LoginPayload` 之前插入两个新 interface：
```ts
export interface FeishuAuthorizeResponse {
  authorize_url: string;
  state: string;
}

export interface FeishuCallbackPayload {
  code: string;
  state: string;
}
```

不要引入 type alias、不要加 JSDoc、不要改动文件顶部的 ApiErrorPayload。保持 export interface 风格。
  </action>
  <verify>
    <automated>cd frontend && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "feishu_open_id: string | null" frontend/src/types/api.ts 返回 0
    - grep -q "export interface FeishuAuthorizeResponse" frontend/src/types/api.ts 返回 0
    - grep -q "export interface FeishuCallbackPayload" frontend/src/types/api.ts 返回 0
    - cd frontend && npm run lint 退出码为 0（tsc --noEmit 无错误）
  </acceptance_criteria>
  <done>
    UserProfile 有 feishu_open_id 字段；FeishuAuthorizeResponse 与 FeishuCallbackPayload 被导出；tsc 通过；文件其余部分未被修改（git diff 只影响上述三处）。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 实现 feishuErrors.ts 错误映射工具</name>
  <files>frontend/src/utils/feishuErrors.ts</files>
  <read_first>
    - frontend/src/services/api.ts（了解 axios baseURL 和 isAxiosError 使用模式）
    - frontend/src/App.tsx 第 49-56 行（现有 resolveError 同时兜底 detail 和 message 字段的模式）
    - .planning/phases/27-oauth2/27-CONTEXT.md 的 D-11 条款（签名精确要求）
    - .planning/phases/27-oauth2/27-RESEARCH.md 第 550-617 行（Example 1 的完整实现参考）
    - backend/app/services/feishu_oauth_service.py（错误码 _FEISHU_ERROR_MAP 在第 21-26 行；中文 detail 在 _validate_state / _check_code_replay / _exchange_code_for_token / _find_or_bind_user）
    - backend/app/main.py 第 132-142 行（确认错误响应 shape 为 {error, message}，非 {detail}）
  </read_first>
  <behavior>
    - resolveFeishuError('backend', axiosError_503) → { code: 'redis_unavailable', message: '登录服务暂不可用，请稍后重试' }
    - resolveFeishuError('backend', axiosError_403) → { code: 'authorization_cancelled', message: '你已取消飞书授权' }
    - resolveFeishuError('backend', axiosError_400_with_state_detail) → { code: 'state_invalid_or_expired', message: '会话已过期，请刷新二维码重试' }
    - resolveFeishuError('backend', axiosError_400_with_工号_detail) → { code: 'employee_not_matched', message: '工号未匹配，请联系管理员开通' }
    - resolveFeishuError('backend', axiosError_400_with_授权码_detail) → { code: 'code_expired_or_replayed', message: '授权码已失效，请重新扫码' }
    - resolveFeishuError('backend', axiosError_no_response) → { code: 'network_error', message: '网络错误，请检查连接' }
    - resolveFeishuError('backend', new Error('x')) → { code: 'unknown_error', message: '登录失败，请稍后重试' }
    - resolveFeishuError('sdk', new Error('sdk_load_failed')) → { code: 'sdk_load_failed', message: '飞书登录组件加载失败，请刷新重试' }
    - resolveFeishuError('sdk', 'anything else') → { code: 'unknown_error', message: '登录失败，请稍后重试' }
    - 对后端同时兼容 response.data.message（主路径，main.py 改写后的格式）与 response.data.detail（FastAPI 默认格式 fallback）
  </behavior>
  <action>
新建文件 frontend/src/utils/feishuErrors.ts，内容如下（完全 verbatim）：

```ts
import axios from 'axios';

export type FeishuErrorCode =
  | 'authorization_cancelled'
  | 'employee_not_matched'
  | 'state_invalid_or_expired'
  | 'code_expired_or_replayed'
  | 'redis_unavailable'
  | 'network_error'
  | 'sdk_load_failed'
  | 'unknown_error';

export interface FeishuError {
  code: FeishuErrorCode;
  message: string;
}

const COPY: Record<FeishuErrorCode, string> = {
  authorization_cancelled: '你已取消飞书授权',
  employee_not_matched: '工号未匹配，请联系管理员开通',
  state_invalid_or_expired: '会话已过期，请刷新二维码重试',
  code_expired_or_replayed: '授权码已失效，请重新扫码',
  redis_unavailable: '登录服务暂不可用，请稍后重试',
  network_error: '网络错误，请检查连接',
  sdk_load_failed: '飞书登录组件加载失败，请刷新重试',
  unknown_error: '登录失败，请稍后重试',
};

function extractDetail(data: unknown): string {
  if (!data || typeof data !== 'object') return '';
  const maybe = data as { message?: unknown; detail?: unknown };
  // 后端 main.py exception_handler 把 HTTPException.detail 改写为 { error, message } 格式
  if (typeof maybe.message === 'string') return maybe.message;
  if (typeof maybe.detail === 'string') return maybe.detail;
  return '';
}

function classifyBackend(err: unknown): FeishuErrorCode {
  if (!axios.isAxiosError(err)) return 'unknown_error';
  if (!err.response) return 'network_error';

  const status = err.response.status;
  const detail = extractDetail(err.response.data);

  if (status === 503) return 'redis_unavailable';
  if (status === 403) return 'authorization_cancelled';
  if (status === 400) {
    if (detail.includes('state')) return 'state_invalid_or_expired';
    if (detail.includes('工号')) return 'employee_not_matched';
    if (detail.includes('授权码')) return 'code_expired_or_replayed';
    return 'unknown_error';
  }
  return 'unknown_error';
}

function classifySdk(payload: unknown): FeishuErrorCode {
  if (payload instanceof Error && payload.message === 'sdk_load_failed') {
    return 'sdk_load_failed';
  }
  return 'unknown_error';
}

export function resolveFeishuError(
  source: 'backend' | 'sdk',
  payload: unknown,
): FeishuError {
  const code = source === 'backend' ? classifyBackend(payload) : classifySdk(payload);
  return { code, message: COPY[code] };
}
```

不要用 dangerouslySetInnerHTML、不要 template literal 拼接 detail 到 UI（T-27-01 XSS 防御）。不要 console.log 任何 payload（T-27-03）。
  </action>
  <verify>
    <automated>cd frontend && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/utils/feishuErrors.ts 文件存在
    - grep -q "export function resolveFeishuError" frontend/src/utils/feishuErrors.ts 返回 0
    - grep -q "export type FeishuErrorCode" frontend/src/utils/feishuErrors.ts 返回 0
    - grep -c "authorization_cancelled\|employee_not_matched\|state_invalid_or_expired\|code_expired_or_replayed\|redis_unavailable\|network_error\|sdk_load_failed\|unknown_error" frontend/src/utils/feishuErrors.ts 至少 9（8 类 + 至少 1 处 union 合计 9 行）
    - grep -q "data.message" frontend/src/utils/feishuErrors.ts 或 grep -q "maybe.message" 返回 0（确认优先读 message）
    - grep -q "data.detail" frontend/src/utils/feishuErrors.ts 或 grep -q "maybe.detail" 返回 0（确认兼容 detail fallback）
    - cd frontend && npm run lint 退出码为 0
  </acceptance_criteria>
  <done>
    文件存在且导出 resolveFeishuError / FeishuErrorCode / FeishuError；tsc 通过；不存在任何 innerHTML / dangerouslySetInnerHTML 用法。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 新增 authorizeFeishu + feishuCallback 服务函数</name>
  <files>frontend/src/services/auth.ts</files>
  <read_first>
    - frontend/src/services/auth.ts（完整读，了解现有 login/register/refresh/fetchCurrentUser 函数风格与 import 顺序）
    - frontend/src/types/api.ts（Task 1 完成后新增的 FeishuAuthorizeResponse / FeishuCallbackPayload / AuthResponse）
    - .planning/phases/27-oauth2/27-RESEARCH.md 第 619-640 行（Example 2 verbatim 实现参考）
    - backend/app/api/v1/auth.py 第 269-297 行（确认 endpoint path 为 /auth/feishu/authorize 与 /auth/feishu/callback）
  </read_first>
  <behavior>
    - authorizeFeishu() 发起 GET /auth/feishu/authorize 并返回 { authorize_url, state }，不带 Authorization header（公开端点，但 api.ts 拦截器若 localStorage 有 token 会带上；后端 endpoint 不校验 token，无害）
    - feishuCallback(code, state) 发起 POST /auth/feishu/callback body={code, state}，返回 AuthResponse（user + tokens）
    - 函数签名签：`authorizeFeishu(): Promise<FeishuAuthorizeResponse>` 与 `feishuCallback(code: string, state: string): Promise<AuthResponse>`
    - 失败时让 axios 错误自然抛出（Service 层不做 try/catch，与项目既有模式一致）
  </behavior>
  <action>
在 frontend/src/services/auth.ts 做以下修改：

1. 修改文件顶部的 type import 行（第 2 行），在 import 列表中加入 FeishuAuthorizeResponse 与 FeishuCallbackPayload：
```ts
import type { AuthResponse, ChangePasswordPayload, FeishuAuthorizeResponse, FeishuCallbackPayload, LoginPayload, RegisterPayload, SelfBindPreviewResult, TokenPair, UserProfile } from '../types/api';
```
（保持字母序，原位置是 `AuthResponse, ChangePasswordPayload, LoginPayload, ...`，插入两个新类型于 ChangePasswordPayload 之后、LoginPayload 之前。）

2. 在文件末尾（紧跟 selfBindConfirm 函数之后）追加两个新 async 函数：
```ts

export async function authorizeFeishu(): Promise<FeishuAuthorizeResponse> {
  const response = await api.get<FeishuAuthorizeResponse>('/auth/feishu/authorize');
  return response.data;
}

export async function feishuCallback(code: string, state: string): Promise<AuthResponse> {
  const payload: FeishuCallbackPayload = { code, state };
  const response = await api.post<AuthResponse>('/auth/feishu/callback', payload);
  return response.data;
}
```

不要添加 try/catch，不要手动设置 Authorization 头，不要引入新依赖。保持项目现有函数风格（async + await + return response.data）。
  </action>
  <verify>
    <automated>cd frontend && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export async function authorizeFeishu" frontend/src/services/auth.ts 返回 0
    - grep -q "export async function feishuCallback" frontend/src/services/auth.ts 返回 0
    - grep -q "/auth/feishu/authorize" frontend/src/services/auth.ts 返回 0
    - grep -q "/auth/feishu/callback" frontend/src/services/auth.ts 返回 0
    - grep -q "FeishuAuthorizeResponse\|FeishuCallbackPayload" frontend/src/services/auth.ts 返回 0
    - cd frontend && npm run lint 退出码为 0
    - cd frontend && npm run build 退出码为 0（完整 tsc -b + vite build）
  </acceptance_criteria>
  <done>
    auth.ts 导出了新的两个函数；类型 import 正确；tsc -b + vite build 双通过；未修改既有函数（git diff 只影响 import 行与文件末尾追加）。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器 JS ↔ 后端 FastAPI | 用户输入/篡改的 URL query（code/state）、localStorage 读取 |
| 前端代码 ↔ axios 错误对象 | 后端可返回 {error, message} 或 {detail}；不可信外部消息需要在展示前净化 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Injection (XSS via error message) | utils/feishuErrors.ts 返回的 message 被后续 Plan 02/03 直接渲染 | mitigate | COPY 映射表为前端静态定义，**不使用**后端 response 原文作为最终 UI 文案；仅用后端 detail 做字符串 includes 分类匹配。下游组件直接 {message} 注入文本节点（React 默认 HTML escape），禁止 innerHTML / dangerouslySetInnerHTML。 |
| T-27-03 | Information Disclosure (code/state 泄漏) | services/auth.ts 的请求与 feishuErrors.ts 的 classify 函数 | mitigate | 不 console.log / console.error payload；不把 error.response.data 写入日志。未来若加 telemetry，code/state 需脱敏。 |
| T-27-05 | Tampering (第三方 script 注入) | services 层本身不加载脚本，但 feishuErrors 可能在 SDK 故障时被触发 | accept | 本 Plan 无 script-src 风险；脚本注入在 Plan 03 做 SRI 讨论与 CSP 处置。 |

本 Plan 不涉及 postMessage（Plan 03）、不涉及路由跳转（Plan 02），故 T-27-02 / T-27-04 / T-27-06 暂不适用，由下游 Plan 覆盖。
</threat_model>

<verification>
整体 Plan 通过验证：
1. `cd frontend && npm run lint` 退出码 0（Task 1-3 全部满足）
2. `cd frontend && npm run build` 退出码 0（完整 tsc -b + vite build）
3. grep 验证每个 acceptance_criteria 表达式均能命中
4. 手动检查：打开 types/api.ts、services/auth.ts、utils/feishuErrors.ts 三文件对照 Task action 逐字核对
</verification>

<success_criteria>
- [ ] UserProfile.feishu_open_id: string | null 存在
- [ ] FeishuAuthorizeResponse / FeishuCallbackPayload interface 导出
- [ ] authorizeFeishu() 与 feishuCallback(code, state) 导出
- [ ] resolveFeishuError(source, payload) 能正确分类 8 类错误
- [ ] tsc --noEmit 与 vite build 双通过
- [ ] 未改动任何已有函数、未删除任何已有 export
- [ ] FUI-04 的"分类中文错误"基础表完成（Plan 02/03 消费）
</success_criteria>

<output>
完成后生成 `.planning/phases/27-oauth2/27-01-SUMMARY.md`，记录：
- 3 个文件的最终形态（diff 摘要）
- A4 / A5 假设验证结论（本 Plan 已在 planning 阶段 grep 验证，SUMMARY 复述一遍并引用代码行号）
- 单元测试策略：前端无 vitest，Plan 01 不跑单元测试；下游手动验证路径由 Plan 02/03 接棒
</output>
