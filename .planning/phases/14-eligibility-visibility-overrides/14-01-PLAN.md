---
phase: 14-eligibility-visibility-overrides
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/eligibility_override.py
  - backend/app/models/__init__.py
  - backend/app/schemas/eligibility.py
  - backend/app/services/eligibility_service.py
  - backend/app/api/v1/eligibility.py
  - alembic/versions/c14_add_eligibility_overrides.py
  - backend/tests/test_services/test_eligibility_batch.py
  - backend/tests/test_services/test_eligibility_override.py
  - backend/tests/test_api/test_eligibility_visibility.py
autonomous: true
requirements:
  - ELIG-05
  - ELIG-06
  - ELIG-07

must_haves:
  truths:
    - "Employee role receives 403 on all eligibility endpoints including /{employee_id}"
    - "Manager can only view eligibility for employees in their department (AccessScopeService enforced)"
    - "HR/admin can retrieve paginated eligibility list with department/status/rule filters applied BEFORE pagination"
    - "HR/admin can export full filtered eligibility dataset as Excel with max 5000 row cap"
    - "Only manager/HRBP can create override request (admin cannot per D-03)"
    - "Override creation validates: requester has AccessScope access to employee, employee is currently ineligible, selected rules are actually failing"
    - "HRBP approval then admin approval completes override lifecycle"
    - "decide_override enforces role-to-step binding: only hrbp can act on pending_hrbp, only admin on pending_admin"
    - "HRBP rejection terminates override without admin step"
    - "Approved override changes rule status to 'overridden' in eligibility results"
    - "DB-level unique index on (employee_id, year) for non-rejected overrides prevents duplicates"
  artifacts:
    - path: "backend/app/models/eligibility_override.py"
      provides: "EligibilityOverride ORM model with unique index"
      contains: "class EligibilityOverride"
    - path: "backend/app/schemas/eligibility.py"
      provides: "Batch + override Pydantic schemas with 'overridden' status"
      contains: "EligibilityBatchResponse"
    - path: "backend/app/services/eligibility_service.py"
      provides: "Batch query with filter-before-paginate, override CRUD with role-step binding and access validation"
      contains: "check_employees_batch"
    - path: "backend/app/api/v1/eligibility.py"
      provides: "All endpoints with role protection + AccessScopeService checks"
      contains: "require_roles"
    - path: "alembic/versions/c14_add_eligibility_overrides.py"
      provides: "Migration for eligibility_overrides table with unique index"
      contains: "eligibility_overrides"
  key_links:
    - from: "backend/app/api/v1/eligibility.py"
      to: "backend/app/services/eligibility_service.py"
      via: "service method calls"
      pattern: "service\\.check_employees_batch|service\\.create_override|service\\.decide_override"
    - from: "backend/app/services/eligibility_service.py"
      to: "backend/app/models/eligibility_override.py"
      via: "ORM queries"
      pattern: "EligibilityOverride"
    - from: "backend/app/api/v1/eligibility.py"
      to: "backend/app/services/access_scope_service.py"
      via: "scope checks on employee-specific endpoints"
      pattern: "AccessScopeService|ensure_employee_access"
    - from: "backend/app/api/v1/eligibility.py"
      to: "backend/app/dependencies.py"
      via: "role gating"
      pattern: "require_roles.*admin.*hrbp.*manager"
---

<objective>
Build the complete backend for eligibility visibility control, batch listing with multi-dimension filtering and Excel export, and the two-level override approval workflow -- with all authorization gaps from the Codex review addressed.

Purpose: Enable HR/managers to view eligibility status at scale, export for offline review, and handle exceptions through a structured approval process -- while hiding all eligibility data from employees. Addresses review concerns around AccessScopeService enforcement, filter-before-paginate, role-step binding, and override creation validation.

Output: EligibilityOverride model (with DB-level uniqueness), Alembic migration, extended EligibilityService with batch query and override methods, new API endpoints with role-based access control + AccessScopeService, unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-eligibility-visibility-overrides/14-CONTEXT.md
@.planning/phases/14-eligibility-visibility-overrides/14-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From backend/app/engines/eligibility_engine.py:
```python
@dataclass(frozen=True)
class RuleResult:
    rule_code: str       # 'TENURE', 'ADJUSTMENT_INTERVAL', 'PERFORMANCE', 'LEAVE'
    rule_label: str
    status: str          # 'eligible', 'ineligible', 'data_missing'
    detail: str

@dataclass
class EligibilityResult:
    overall_status: str  # 'eligible', 'ineligible', 'pending'
    rules: list[RuleResult]

class EligibilityEngine:
    def evaluate(self, *, hire_date, last_adjustment_date, performance_grade, non_statutory_leave_days, reference_date) -> EligibilityResult
```

From backend/app/schemas/eligibility.py:
```python
class RuleResultSchema(BaseModel):
    rule_code: str
    rule_label: str
    status: Literal['eligible', 'ineligible', 'data_missing']
    detail: str

class EligibilityResultSchema(BaseModel):
    overall_status: Literal['eligible', 'ineligible', 'pending']
    rules: list[RuleResultSchema]
```

From backend/app/services/eligibility_service.py:
```python
class EligibilityService:
    def __init__(self, db: Session, *, settings: Settings | None = None) -> None
    def check_employee(self, employee_id: str, *, reference_date=None, year=None) -> EligibilityResult
```

From backend/app/dependencies.py:
```python
def require_roles(*roles: str) -> Callable[[User], User]
def get_current_user(...) -> User
def get_db() -> Generator[Session, None, None]
```

From backend/app/services/access_scope_service.py:
```python
class AccessScopeService:
    def __init__(self, db: Session)
    def can_access_employee(self, current_user: User, employee: Employee | None) -> bool
    def ensure_employee_access(self, current_user: User, employee_id: str) -> Employee | None
    def _department_names(self, current_user: User) -> set[str]
    # IMPORTANT: hrbp is scoped by department (same as manager).
    # D-09 says HR/Admin see all -- AccessScopeService already handles this:
    #   admin -> True (all access)
    #   hrbp/manager -> department-scoped
    #   employee -> self only
```

From backend/app/models/mixins.py:
```python
class UUIDPrimaryKeyMixin   # id: Mapped[str] = mapped_column(String(36), primary_key=True)
class CreatedAtMixin         # created_at: Mapped[datetime]
class UpdatedAtMixin         # updated_at: Mapped[datetime]
```
</interfaces>

<review_concerns>
## Codex Review Concerns Addressed in This Plan

### HIGH concerns:
1. GET /eligibility/{employee_id} -- add AccessScopeService.ensure_employee_access() check (not just role gate)
2. status_filter and rule_filter BEFORE pagination -- filter-then-paginate, not paginate-then-filter
3. decide_override() must bind role to workflow step -- pending_hrbp only hrbp can decide, pending_admin only admin
4. Override creation must validate: (a) requester has AccessScope access to employee, (b) employee is currently ineligible, (c) selected rules are actually failing

### MEDIUM concerns:
5. Restrict override creation to manager/hrbp only (not admin) -- per D-03
6. HRBP scope clarification: AccessScopeService already scopes hrbp by department (same as manager); D-09 "HR/Admin see all" means admin role sees all, hrbp is department-scoped
7. DB-level unique index on (employee_id, year) WHERE status NOT IN ('rejected') for active override uniqueness
8. Export contract: full filtered dataset (not current page), max 5000 row cap
9. Existing sub-resource endpoints (/{employee_id}/performance-records, /{employee_id}/salary-adjustment-records) need AccessScopeService check too
</review_concerns>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EligibilityOverride model + migration + schemas + batch/override service methods</name>
  <files>
    backend/app/models/eligibility_override.py,
    backend/app/models/__init__.py,
    alembic/versions/c14_add_eligibility_overrides.py,
    backend/app/schemas/eligibility.py,
    backend/app/services/eligibility_service.py,
    backend/tests/test_services/test_eligibility_batch.py,
    backend/tests/test_services/test_eligibility_override.py
  </files>
  <read_first>
    backend/app/engines/eligibility_engine.py,
    backend/app/services/eligibility_service.py,
    backend/app/schemas/eligibility.py,
    backend/app/models/mixins.py,
    backend/app/models/employee.py,
    backend/app/models/performance_record.py,
    backend/app/models/salary_adjustment_record.py,
    backend/app/models/attendance_record.py,
    backend/app/services/access_scope_service.py,
    backend/app/core/database.py,
    backend/app/models/__init__.py
  </read_first>
  <behavior>
    - Test: check_employees_batch returns dict[str, EligibilityResult] for multiple employees with correct statuses
    - Test: check_employees_batch applies status_filter and rule_filter BEFORE pagination (addresses review HIGH concern #2) -- e.g., if 10 employees total, 3 ineligible, status_filter='ineligible', page_size=2 -> total=3 not 10
    - Test: check_employees_batch respects department scope for manager role via AccessScopeService
    - Test: check_employees_batch respects department scope for hrbp role (same as manager, addresses review MEDIUM #6)
    - Test: create_override_request creates record with status='pending_hrbp'
    - Test: create_override_request rejects invalid rule codes (not in TENURE/ADJUSTMENT_INTERVAL/PERFORMANCE/LEAVE)
    - Test: create_override_request validates requester has AccessScope access to employee (addresses review HIGH #4a)
    - Test: create_override_request validates employee is currently ineligible (addresses review HIGH #4b)
    - Test: create_override_request validates selected rules are actually failing (status='ineligible') for that employee (addresses review HIGH #4c)
    - Test: create_override_request rejects if active (non-rejected) override already exists for same employee+year
    - Test: decide_override with hrbp role on pending_hrbp status transitions to pending_admin
    - Test: decide_override with admin role on pending_admin status transitions to approved
    - Test: decide_override with admin role on pending_hrbp status raises error (role-step binding, addresses review HIGH #3)
    - Test: decide_override with hrbp role on pending_admin status raises error (role-step binding, addresses review HIGH #3)
    - Test: reject at hrbp step sets status to rejected (no admin step)
    - Test: reject at admin step sets status to rejected
    - Test: approved override causes _apply_overrides to change rule status to 'overridden'
    - Test: export_eligibility_excel returns BytesIO with correct columns, respects max 5000 row cap
  </behavior>
  <action>
    1. **Create EligibilityOverride model** in `backend/app/models/eligibility_override.py` (per D-03/D-04/D-05/D-06):
       - Inherits UUIDPrimaryKeyMixin, CreatedAtMixin, UpdatedAtMixin, Base
       - `__tablename__ = 'eligibility_overrides'`
       - Fields: employee_id (FK employees.id, index), requester_id (FK users.id, index), override_rules (JSON, list of rule codes), reason (Text), status (String(32), default 'pending_hrbp'), hrbp_approver_id (FK users.id, nullable), hrbp_decision (String(32), nullable), hrbp_comment (Text, nullable), hrbp_decided_at (DateTime(timezone=True), nullable), admin_approver_id (FK users.id, nullable), admin_decision (String(32), nullable), admin_comment (Text, nullable), admin_decided_at (DateTime(timezone=True), nullable), year (Integer, not null), reference_date (Date, nullable)
       - Status flow: pending_hrbp -> pending_admin -> approved | rejected (hrbp rejection short-circuits to rejected per CONTEXT specifics)
       - **DB-level uniqueness (addresses review MEDIUM #7):** Add a `UniqueConstraint('employee_id', 'year', name='uq_active_override_employee_year')`. At application level, enforce that only non-rejected overrides are checked. Note: SQLite does not support partial indexes, so uniqueness for non-rejected is enforced at application level with a pre-insert query; the DB constraint catches the edge case of two simultaneous inserts for the same employee+year.
       - Register model import in `backend/app/models/__init__.py`

    2. **Create Alembic migration** `alembic/versions/c14_add_eligibility_overrides.py`:
       - Create `eligibility_overrides` table with all columns from the model
       - Include the unique constraint on (employee_id, year)
       - Use `batch_alter_table` pattern for SQLite compatibility (per project convention)

    3. **Extend schemas** in `backend/app/schemas/eligibility.py`:
       - Add `'overridden'` to `RuleResultSchema.status` Literal: `Literal['eligible', 'ineligible', 'data_missing', 'overridden']`
       - Add `EligibilityBatchItemSchema`: employee_id, employee_no, name, department, job_family, job_level, overall_status, rules (list[RuleResultSchema])
       - Add `EligibilityBatchResponse`: items (list[EligibilityBatchItemSchema]), total (int), page (int), page_size (int)
       - Add `OverrideRequestCreate`: employee_id (str), override_rules (list[str]), reason (str), year (int | None = None), reference_date (date | None = None)
       - Add `OverrideRequestRead`: id, employee_id, employee_no, employee_name, requester_id, requester_name, override_rules, reason, status, hrbp_approver_id, hrbp_decision, hrbp_comment, hrbp_decided_at, admin_approver_id, admin_decision, admin_comment, admin_decided_at, year, reference_date, created_at, updated_at. Use ConfigDict(from_attributes=True).
       - Add `OverrideDecisionPayload`: decision (Literal['approve', 'reject']), comment (str | None = None)
       - Add `OverrideListResponse`: items (list[OverrideRequestRead]), total (int), page (int), page_size (int)

    4. **Extend EligibilityService** in `backend/app/services/eligibility_service.py`:
       - Add `_bulk_load_eligibility_data(self, employee_ids: list[str], year: int) -> tuple[dict, dict, dict]` -- 3 bulk queries for performance grades, max adjustment dates, leave totals (as shown in RESEARCH.md code example). This avoids N+1.
       - Add `check_employees_batch(self, *, department: str | None, status_filter: str | None, rule_filter: str | None, job_family: str | None, job_level: str | None, page: int, page_size: int, current_user: User, reference_date: date | None, year: int | None) -> tuple[list, int]`.
         **CRITICAL filter-before-paginate (addresses review HIGH #2):** Steps:
         (a) Query ALL employees matching DB-level filters (department, job_family, job_level) with AccessScopeService department scope for hrbp/manager roles.
         (b) Bulk load eligibility data for ALL matched employees.
         (c) Run engine per employee and apply overrides via `_apply_overrides`.
         (d) Apply status_filter and rule_filter in Python on the full result set.
         (e) Compute `total` from the filtered list length.
         (f) Apply pagination (slice) on the filtered list.
         (g) Return paginated items + total.
         Note: This means we evaluate all matching employees before paginating. For large datasets, this is acceptable since eligibility evaluation is CPU-only (no I/O per employee) after the bulk load.
       - Add `_apply_overrides(self, employee_id: str, result: EligibilityResult, year: int) -> EligibilityResult` -- query approved EligibilityOverride for (employee_id, year), create new RuleResult instances with status='overridden' and detail appended with '（已通过特殊审批覆盖）' for matching rule codes. Recalculate overall_status treating 'overridden' as 'eligible'. IMPORTANT: RuleResult is frozen dataclass -- create new instances, do not mutate.
       - Add `create_override_request(self, *, employee_id, requester: User, override_rules: list[str], reason: str, year: int | None, reference_date: date | None) -> EligibilityOverride`.
         **Validation (addresses review HIGH #4 and MEDIUM #5):**
         (a) Validate rule codes against {'TENURE', 'ADJUSTMENT_INTERVAL', 'PERFORMANCE', 'LEAVE'}.
         (b) Validate requester role is 'manager' or 'hrbp' (NOT admin, per D-03). Raise HTTPException(403) if admin tries.
         (c) Validate requester has AccessScope access to employee via `AccessScopeService.ensure_employee_access()`. Raises PermissionError -> 403 if manager/hrbp doesn't have department access.
         (d) Run `check_employee()` for the target employee to get current eligibility. Raise HTTPException(400, "Employee is currently eligible") if overall_status is 'eligible'.
         (e) Validate each rule in override_rules is actually 'ineligible' in the current result. Raise HTTPException(400, "Rule {code} is not currently failing") if a rule is eligible or data_missing.
         (f) Check no active (non-rejected) override exists for same employee+year. Raise HTTPException(409) if one exists.
         (g) Create record with status='pending_hrbp'.
       - Add `decide_override(self, *, override_id: str, approver: User, decision: str, comment: str | None) -> EligibilityOverride`.
         **Role-step binding (addresses review HIGH #3):**
         (a) Load override, raise 404 if not found.
         (b) If status is 'pending_hrbp': require `approver.role == 'hrbp'`. Raise HTTPException(403, "Only HRBP can approve/reject at this step") otherwise. Record hrbp fields. If approve -> status='pending_admin'. If reject -> status='rejected'.
         (c) If status is 'pending_admin': require `approver.role == 'admin'`. Raise HTTPException(403, "Only admin can approve/reject at this step") otherwise. Record admin fields. If approve -> status='approved'. If reject -> status='rejected'.
         (d) If status is 'approved' or 'rejected': raise HTTPException(400, "Override already finalized").
       - Add `list_overrides(self, *, page: int, page_size: int, status_filter: str | None, current_user: User) -> tuple[list[EligibilityOverride], int]`. Apply AccessScopeService scope for manager role (join to Employee to filter by department).
       - Add `export_eligibility_excel(self, items: list) -> io.BytesIO` -- use openpyxl Workbook to create Excel with columns: 工号, 姓名, 部门, 岗位族, 职级, 资格状态, 入职时长, 调薪间隔, 绩效等级, 假期天数. **Max 5000 rows (addresses review MEDIUM #8).** Return BytesIO buffer.

    5. **Write tests** in `backend/tests/test_services/test_eligibility_batch.py` and `backend/tests/test_services/test_eligibility_override.py`:
       - Use in-memory SQLite with test fixtures for employees, performance records, adjustment records, attendance records
       - Cover all behaviors listed above, especially:
         - Filter-before-paginate: verify total reflects filtered count, not total employee count
         - Role-step binding: verify hrbp cannot act on pending_admin, admin cannot act on pending_hrbp
         - Override creation validation: verify access check, ineligibility check, rule-specific check
         - Requester role restriction: verify admin gets 403 on override creation
       - Test `_apply_overrides` correctly creates new RuleResult instances (frozen dataclass)
       - Test export produces valid xlsx with expected columns, max 5000 row cap
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust && python -m pytest backend/tests/test_services/test_eligibility_batch.py backend/tests/test_services/test_eligibility_override.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class EligibilityOverride" backend/app/models/eligibility_override.py
    - grep -q "uq_active_override_employee_year\|UniqueConstraint.*employee_id.*year" backend/app/models/eligibility_override.py
    - grep -q "eligibility_overrides" alembic/versions/c14_add_eligibility_overrides.py
    - grep -q "overridden" backend/app/schemas/eligibility.py
    - grep -q "EligibilityBatchResponse" backend/app/schemas/eligibility.py
    - grep -q "check_employees_batch" backend/app/services/eligibility_service.py
    - grep -q "create_override_request" backend/app/services/eligibility_service.py
    - grep -q "decide_override" backend/app/services/eligibility_service.py
    - grep -q "_apply_overrides" backend/app/services/eligibility_service.py
    - grep -q "export_eligibility_excel" backend/app/services/eligibility_service.py
    - grep -q "ensure_employee_access\|can_access_employee" backend/app/services/eligibility_service.py
    - grep -q "pending_hrbp" backend/app/models/eligibility_override.py
    - grep -q "role.*hrbp\|hrbp.*role\|approver.role" backend/app/services/eligibility_service.py
    - grep -q "5000\|MAX_EXPORT_ROWS" backend/app/services/eligibility_service.py
    - python -m pytest backend/tests/test_services/test_eligibility_batch.py backend/tests/test_services/test_eligibility_override.py -x exits 0
  </acceptance_criteria>
  <done>
    EligibilityOverride model exists with DB-level unique constraint on (employee_id, year). Migration creates table. Schemas include batch response, override CRUD types, and 'overridden' status. Service has batch query with filter-before-paginate, override lifecycle with role-step binding and creation validation (access + ineligibility + rule check), and Excel export with 5000 row cap. All tests pass including filter-before-paginate, role-step binding, and override creation validation tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: API endpoints with role-based access control + AccessScopeService enforcement</name>
  <files>
    backend/app/api/v1/eligibility.py,
    backend/tests/test_api/test_eligibility_visibility.py
  </files>
  <read_first>
    backend/app/api/v1/eligibility.py,
    backend/app/dependencies.py,
    backend/app/services/access_scope_service.py,
    backend/app/schemas/eligibility.py,
    backend/app/api/v1/approvals.py
  </read_first>
  <action>
    1. **Harden existing eligibility endpoint** (per D-07 / ELIG-05, addresses review HIGH #1):
       - Change `GET /eligibility/{employee_id}` to use `require_roles('admin', 'hrbp', 'manager')` instead of `get_current_user`.
       - **Add AccessScopeService check**: After role gate, call `AccessScopeService(db).ensure_employee_access(current_user, employee_id)`. If returns None -> 404. If raises PermissionError -> catch and return 403. This prevents a manager from fetching eligibility for employees outside their department.

    2. **Harden sub-resource endpoints** (addresses review MEDIUM #9):
       - Change `GET /eligibility/{employee_id}/performance-records` to use `require_roles('admin', 'hrbp', 'manager')` + AccessScopeService check.
       - Change `GET /eligibility/{employee_id}/salary-adjustment-records` to use `require_roles('admin', 'hrbp', 'manager')` + AccessScopeService check.

    3. **Add batch list endpoint** `GET /eligibility/batch` (per D-01/D-08/D-09 / ELIG-06):
       - Depends: `require_roles('admin', 'hrbp', 'manager')`
       - Query params: department (str | None), status (str | None = None, values: eligible/ineligible/pending/all), rule (str | None = None), job_family (str | None), job_level (str | None), page (int = 1), page_size (int = 50), reference_date (date | None), year (int | None)
       - Response: `EligibilityBatchResponse`
       - Calls `service.check_employees_batch(...)` passing current_user for scope filtering
       - AccessScopeService is called INSIDE the service method (not at API layer) since it needs to filter the query, not check a single employee

    4. **Add batch export endpoint** `GET /eligibility/batch/export` (per D-02 / ELIG-06, addresses review MEDIUM #8):
       - Depends: `require_roles('admin', 'hrbp', 'manager')`
       - Same query params as batch list (minus pagination -- exports full filtered dataset up to 5000 rows)
       - Internally calls `service.check_employees_batch(...)` with page=1, page_size=5000 to get all filtered results
       - Then calls `service.export_eligibility_excel(items)` to generate workbook
       - Returns `StreamingResponse` with Excel file
       - Content-Disposition: `attachment; filename=eligibility_export_{date}.xlsx`
       - Media type: `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`

    5. **Add override request endpoints** (per D-03/D-04/D-05/D-06 / ELIG-07):
       - `POST /eligibility/overrides` -- Create override request.
         Depends: `require_roles('manager', 'hrbp')` (NOT admin, per D-03, addresses review MEDIUM #5).
         Body: `OverrideRequestCreate`. Returns `OverrideRequestRead` with 201 status. Adds AuditLog entry.
         Service layer handles all validation (access, ineligibility, rule check).
       - `GET /eligibility/overrides` -- List override requests.
         Depends: `require_roles('admin', 'hrbp', 'manager')`.
         Query params: status (str | None), page (int = 1), page_size (int = 50).
         Response: `OverrideListResponse`.
       - `POST /eligibility/overrides/{override_id}/decide` -- HRBP or admin decision.
         Depends: `require_roles('admin', 'hrbp')`.
         Body: `OverrideDecisionPayload`. Returns `OverrideRequestRead`.
         Service layer enforces role-step binding (hrbp on pending_hrbp, admin on pending_admin).
         Adds AuditLog entry.
       - `GET /eligibility/overrides/{override_id}` -- Get single override detail.
         Depends: `require_roles('admin', 'hrbp', 'manager')`. Returns `OverrideRequestRead`.

    6. **IMPORTANT: Route ordering** -- Place `/batch`, `/batch/export`, `/overrides`, and `/performance-records`, `/salary-adjustment-records` routes BEFORE `/{employee_id}` to prevent path parameter from capturing these as employee IDs. Move the existing `/{employee_id}` endpoint to the bottom of the router file.

    7. **Write API visibility tests** in `backend/tests/test_api/test_eligibility_visibility.py`:
       - Test employee role gets 403 on GET /eligibility/batch
       - Test employee role gets 403 on GET /eligibility/{employee_id}
       - Test employee role gets 403 on POST /eligibility/overrides
       - Test employee role gets 403 on GET /eligibility/{employee_id}/performance-records
       - Test admin/hrbp/manager roles get 200 on batch endpoint
       - Test manager gets 403 on GET /eligibility/{employee_id} for employee outside their department (AccessScopeService)
       - Test admin gets 403 on POST /eligibility/overrides (only manager/hrbp allowed per D-03)
       - Test override create returns 201 with correct fields
       - Test override decide enforces role-step binding (hrbp on pending_hrbp only, admin on pending_admin only)
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust && python -m pytest backend/tests/test_api/test_eligibility_visibility.py backend/tests/test_services/test_eligibility_batch.py backend/tests/test_services/test_eligibility_override.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "require_roles.*admin.*hrbp.*manager" backend/app/api/v1/eligibility.py
    - grep -q "AccessScopeService\|ensure_employee_access" backend/app/api/v1/eligibility.py
    - grep -q "batch" backend/app/api/v1/eligibility.py
    - grep -q "overrides" backend/app/api/v1/eligibility.py
    - grep -q "StreamingResponse" backend/app/api/v1/eligibility.py
    - grep -q "export" backend/app/api/v1/eligibility.py
    - grep -q "decide" backend/app/api/v1/eligibility.py
    - grep -q "require_roles.*manager.*hrbp" backend/app/api/v1/eligibility.py
    - grep -q "403\|employee.*role\|scope" backend/tests/test_api/test_eligibility_visibility.py
    - grep -q "role.*step\|pending_hrbp.*hrbp\|pending_admin.*admin" backend/tests/test_api/test_eligibility_visibility.py
    - python -m pytest backend/tests/test_api/test_eligibility_visibility.py -x exits 0
  </acceptance_criteria>
  <done>
    All eligibility endpoints protected with require_roles (employee gets 403 per ELIG-05). GET /{employee_id} and sub-resources have AccessScopeService check (manager can only see their department, addresses review HIGH #1). Batch list with filter-before-paginate works (ELIG-06). Excel export streams full filtered dataset up to 5000 rows (ELIG-06). Override creation restricted to manager/hrbp only (per D-03). Override decide enforces role-step binding (ELIG-07). Route ordering prevents path conflicts. Visibility tests pass.
  </done>
</task>

</tasks>

<verification>
```bash
# All backend tests pass
cd /Users/mac/PycharmProjects/Wage_adjust && python -m pytest backend/tests/ -x

# Specific phase tests
python -m pytest backend/tests/test_services/test_eligibility_batch.py backend/tests/test_services/test_eligibility_override.py backend/tests/test_api/test_eligibility_visibility.py -x -v

# Migration applies cleanly
cd /Users/mac/PycharmProjects/Wage_adjust && alembic upgrade head

# Server starts
cd /Users/mac/PycharmProjects/Wage_adjust && timeout 10 uvicorn backend.app.main:app --port 8011 || true
```
</verification>

<success_criteria>
- Employee role returns 403 on all eligibility endpoints including /{employee_id} and sub-resources (ELIG-05)
- Manager/HRBP scoped to their department via AccessScopeService on all employee-specific endpoints
- Batch endpoint returns paginated, filtered eligibility results with filters applied BEFORE pagination (ELIG-06)
- Excel export downloads full filtered dataset with max 5000 rows and correct columns (ELIG-06)
- Override creation restricted to manager/hrbp, validates access + ineligibility + rule status (ELIG-07)
- Override decide enforces role-step binding: hrbp on pending_hrbp, admin on pending_admin (ELIG-07)
- HRBP rejection terminates without admin step (ELIG-07)
- Approved override changes rule status to 'overridden' in subsequent eligibility checks (ELIG-07)
- DB-level unique constraint on (employee_id, year) prevents duplicate active overrides
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/14-eligibility-visibility-overrides/14-01-SUMMARY.md`
</output>
