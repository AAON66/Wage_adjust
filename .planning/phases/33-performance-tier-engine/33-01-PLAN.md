---
phase: 33-performance-tier-engine
plan: 01
type: tdd
wave: 1
autonomous: true
depends_on: []
files_modified:
  - backend/app/core/config.py
  - .env.example
  - backend/app/engines/performance_tier_engine.py
  - backend/app/engines/__init__.py
  - backend/tests/test_engines/test_performance_tier_engine.py
requirements: [PERF-03, PERF-04, PERF-06]
requirements_addressed: [PERF-03, PERF-04, PERF-06]
gap_closure: false

must_haves:
  truths:
    - "调用 PerformanceTierEngine.assign() 输入 (employee_id, grade) list 返回 TierAssignmentResult，tiers 字段是 {employee_id: 1|2|3|None} 映射"
    - "相同 grade 的员工被归入同一档次（ties 不被机械拆分），按 D-01 ties 首位向上扩张"
    - "样本量 < min_sample_size（默认 50）时全员 tier=None 且 insufficient_sample=true"
    - "样本量为 0/1/2/3 等边界时引擎不抛异常，行为与样本不足分支一致"
    - "实际分布偏离 20/70/10 超过 ±5% 时 distribution_warning=true（仅 insufficient_sample=false 时可能为 true）"
    - "异常 grade（None/空字符串/不在 GRADE_ORDER 内）的员工 tier=None 且 skipped_invalid_grades 计数 +1"
    - "单元测试覆盖 ≥ 20 用例，全部通过"
    - "Settings.performance_tier_min_sample_size 配置化，默认 50"
    - "PerformanceTierEngine 与 TierAssignmentResult 通过 backend.app.engines 包顶层可 import"
  artifacts:
    - path: "backend/app/engines/performance_tier_engine.py"
      provides: "PerformanceTierEngine 类 + TierAssignmentResult dataclass + PerformanceTierConfig dataclass"
      min_lines: 100
    - path: "backend/tests/test_engines/test_performance_tier_engine.py"
      provides: "≥ 20 个 pytest 用例，覆盖 ties / 边界样本 / 异常 grade / distribution warning / 配置注入"
      min_lines: 200
    - path: "backend/app/core/config.py"
      provides: "Settings.performance_tier_min_sample_size: int = 50 字段"
      contains: "performance_tier_min_sample_size"
    - path: ".env.example"
      provides: "PERFORMANCE_TIER_MIN_SAMPLE_SIZE 环境变量示例"
      contains: "PERFORMANCE_TIER_MIN_SAMPLE_SIZE"
    - path: "backend/app/engines/__init__.py"
      provides: "PerformanceTierEngine + TierAssignmentResult 顶层导出"
      contains: "PerformanceTierEngine"
  key_links:
    - from: "backend/app/engines/performance_tier_engine.py"
      to: "backend/app/engines/eligibility_engine.GRADE_ORDER"
      via: "import 复用，不重复定义"
      pattern: "from backend.app.engines.eligibility_engine import GRADE_ORDER"
    - from: "backend/app/engines/__init__.py"
      to: "PerformanceTierEngine 类"
      via: "包顶层 export，供 Phase 34 Service 层 import"
      pattern: "from .performance_tier_engine import PerformanceTierEngine, TierAssignmentResult"
    - from: "backend/app/core/config.py"
      to: ".env.example"
      via: "Settings 字段名 ↔ 环境变量名（pydantic-settings 大小写不敏感）"
      pattern: "PERFORMANCE_TIER_MIN_SAMPLE_SIZE"
---

<objective>
交付 `PerformanceTierEngine` 纯计算引擎：输入 `(employee_id, grade)` list，按 PERCENT_RANK 口径切 20/70/10 三档，ties 同档（D-01/D-02），样本不足/异常 grade 优雅降级（D-08/D-09），并输出分布偏离告警（D-03/D-04/D-05）。

**Purpose:** 为 Phase 34 Service 层提供分档计算的单一事实源（GRADE_ORDER 共用 EligibilityEngine 表，避免双源）。沿用现有 `eligibility_engine.py` / `salary_engine.py` 的「frozen dataclass 配置 + dataclass 结果 + 纯计算无 I/O」模式，确保 Phase 34 Service 可零负担注入 Engine 并消费结果。

**Output:**
- `backend/app/engines/performance_tier_engine.py`（新建，~120 行）
- `backend/tests/test_engines/test_performance_tier_engine.py`（新建，≥ 25 用例）
- `backend/app/core/config.py`（追加 1 字段）
- `.env.example`（追加 1 段落）
- `backend/app/engines/__init__.py`（追加 import + __all__）

**Why TDD plan type:** 五维分档算法的 ties 边界、distribution warning ±5% 边界、样本不足边界都是「输入→预期输出」清晰可验证的纯函数行为，符合 TDD 候选定义。每个 D-XX 决策对应可写出 `expect(engine.assign(...)).toEqual(...)` 的测试，先写测试反过来约束实现。
</objective>

<context>
@.planning/phases/33-performance-tier-engine/33-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@CLAUDE.md
@backend/app/engines/eligibility_engine.py
@backend/app/engines/salary_engine.py
@backend/app/core/config.py
@backend/tests/test_engines/test_eligibility_engine.py

## 关键 D-XX 决策快速索引

| 决策 ID | 内容 | 落地位置 |
|---------|------|----------|
| D-01 | Ties 首位扩张：按 ties 首位累计百分位归档 | `_assign_tiers` 算法 |
| D-02 | 横跨多档 ties 按中位数位次归档（极端：全员 grade=B） | `_assign_tiers` 算法 |
| D-03 | ±5% 是绝对百分点偏差：1档∈[15%,25%]、2档∈[65%,75%]、3档∈[5%,15%] | `_compute_distribution` 算法 |
| D-04 | `actual_distribution: dict[int, float]` 三档实际占比，分母为成功分档的人数（None 不计入） | `TierAssignmentResult.actual_distribution` |
| D-05 | distribution_warning 仅在 insufficient_sample=false 时计算 | `assign()` 主流程 |
| D-06 | 内部用 GRADE_ORDER 降序排序（A=5..E=1），调用方不需预排序 | `assign()` 入口 sort key |
| D-07 | `TierAssignmentResult` 字段：tiers / insufficient_sample / distribution_warning / actual_distribution / sample_size / skipped_invalid_grades | dataclass 定义 |
| D-08 | 异常 grade（None/''/'F'/'优' 等不在 GRADE_ORDER）跳过：tier=None + skipped_invalid_grades+1 | `assign()` 入口过滤 |
| D-09 | Engine 不抛异常，所有边界通过返回结构表达 | 全方法 try/except 自由 |
| D-10 | `Settings.performance_tier_min_sample_size: int = 50` 在 config.py | `config.py` line 80+ 之后 |
| D-11 | `tier_targets=(0.20, 0.70, 0.10)` + `distribution_tolerance=0.05` 硬编码在 PerformanceTierConfig 默认值 | dataclass 默认值 |

## 接口契约（供下游 Phase 34 Service 消费）

```python
# Phase 34 Service 调用方将这样使用：
from backend.app.engines import PerformanceTierEngine, TierAssignmentResult
from backend.app.core.config import get_settings

settings = get_settings()
engine = PerformanceTierEngine(
    config=PerformanceTierConfig(min_sample_size=settings.performance_tier_min_sample_size)
)
result: TierAssignmentResult = engine.assign(
    [('emp_001', 'A'), ('emp_002', 'B'), ('emp_003', None), ...]
)
# result.tiers           -> {'emp_001': 1, 'emp_002': 2, 'emp_003': None}
# result.insufficient_sample -> False（样本 ≥ 50 时）
# result.distribution_warning -> True/False
# result.actual_distribution -> {1: 0.22, 2: 0.68, 3: 0.10}
# result.sample_size     -> 99（已扣除 skipped_invalid_grades）
# result.skipped_invalid_grades -> 1
```

## 现有 GRADE_ORDER 复用约束（D-06）

`backend/app/engines/eligibility_engine.py:8` 已定义 `GRADE_ORDER: dict[str, int] = {'A': 5, 'B': 4, 'C': 3, 'D': 2, 'E': 1}`。Phase 33 新引擎**必须 import 复用**该常量，不允许重复定义。这是单一事实源原则：未来若新增 grade 等级（如 'A+'），只在一个文件改。

## 不在范围（防止超载）

- **不写**任何 Service / API / Model / Migration 代码（Phase 34 范围）
- **不读** `PerformanceRecord` 表（input list 由 Service 层组装）
- **不持久化**结果到 `PerformanceTierSnapshot`（Phase 34 范围）
- **不接触** frontend/ 任何文件（Phase 35 范围）
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 配置层 — Settings 字段 + .env.example 段落</name>
  <files>backend/app/core/config.py, .env.example</files>
  <read_first>
    - backend/app/core/config.py（确认现有 eligibility_min_* 阈值放置位置 line 79-83）
    - .env.example（确认现有段落分组风格）
    - .planning/phases/33-performance-tier-engine/33-CONTEXT.md（D-10 决策原文）
    - backend/app/engines/eligibility_engine.py（确认 EligibilityThresholds 注入模式）
  </read_first>
  <action>
    **改动 1：`backend/app/core/config.py`**

    在 line 83（`eligibility_max_non_statutory_leave_days: float = 30.0` 之后）追加新字段：

    ```python
    # Performance tier engine threshold (D-10 — Phase 33)
    performance_tier_min_sample_size: int = 50
    ```

    位置严格紧贴现有 `eligibility_*` 阈值之后、`log_level` 之前（保持业务阈值集中）。

    **改动 2：`.env.example`**

    在「LOGGING」段落（line 72 `# ============================================================`）之前插入新段落：

    ```
    # ============================================================
    # ELIGIBILITY / PERFORMANCE THRESHOLDS
    # ============================================================
    # Eligibility 阈值（已在 backend/app/core/config.py 配置默认值）
    # ELIGIBILITY_MIN_TENURE_MONTHS=6
    # ELIGIBILITY_MIN_ADJUSTMENT_INTERVAL_MONTHS=6
    # ELIGIBILITY_PERFORMANCE_FAIL_GRADES=C,D,E
    # ELIGIBILITY_MAX_NON_STATUTORY_LEAVE_DAYS=30.0

    # Performance tier engine 最小样本量（< 此值时引擎对全员返回 tier=null）
    PERFORMANCE_TIER_MIN_SAMPLE_SIZE=50
    ```

    **决策溯源：** 按 D-10「Settings.performance_tier_min_sample_size: int = 50 新增到 backend/app/core/config.py」与 CONTEXT canonical_refs「`backend/app/core/config.py` line 79-81 — `eligibility_min_*` 阈值放置示例」。

    **不要做的事：**
    - 不要在 `.env` 实际文件中写值（只改 `.env.example`，避免污染 dev 配置）
    - 不要改 `model_config` / `parse_cors_origins`（无关字段）
    - 不要新增 `field_validator`（int 默认值 pydantic 自动验证）
    - 不要把字段名改为 `perf_tier_min_sample_size` 或其它简称（D-10 锁定全名）
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust && python -c "from backend.app.core.config import Settings; s = Settings(); assert s.performance_tier_min_sample_size == 50, f'expected 50 got {s.performance_tier_min_sample_size}'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/core/config.py` 包含字符串 `performance_tier_min_sample_size: int = 50`
    - `backend/app/core/config.py` 中 `performance_tier_min_sample_size` 字段位于 `eligibility_max_non_statutory_leave_days` 之后、`log_level` 之前
    - `.env.example` 包含字符串 `PERFORMANCE_TIER_MIN_SAMPLE_SIZE=50`
    - `.env.example` 包含段落标题 `# ELIGIBILITY / PERFORMANCE THRESHOLDS`
    - `python -c "from backend.app.core.config import Settings; assert Settings().performance_tier_min_sample_size == 50"` 退出码 0
  </acceptance_criteria>
  <done>
    Settings 类暴露新字段，`.env.example` 文档化默认值，pydantic 加载默认值通过验证。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PerformanceTierEngine 实现 + 单测套件（TDD: RED → GREEN）</name>
  <files>backend/app/engines/performance_tier_engine.py, backend/tests/test_engines/test_performance_tier_engine.py</files>
  <read_first>
    - backend/app/engines/eligibility_engine.py（必读：GRADE_ORDER 来源 + frozen dataclass 风格 + RuleResult/EligibilityResult 模式）
    - backend/app/engines/salary_engine.py（必读：构造函数注入 config 模式）
    - backend/tests/test_engines/test_eligibility_engine.py（必读：pytest fixture + 测试组织风格 + 一行 assert + 边界值测试模式）
    - .planning/phases/33-performance-tier-engine/33-CONTEXT.md（必读：D-01 到 D-11 全部决策 + Specifics 节给出的 25 个建议测试用例）
    - .planning/REQUIREMENTS.md line 22-25（必读：PERF-03/04/06 原文）
    - CLAUDE.md「Coding Conventions」节（必读：from __future__ import annotations / PEP 604 union / 引擎纯计算无 I/O）
  </read_first>
  <behavior>
    **核心可测行为（按决策 ID 分组，22+ 用例最低线）：**

    **A. 基础正确性（5 用例）**
    - A1: 100 人均匀分布（每 grade 20 人）→ tiers 分布接近 20/70/10
    - A2: 50 人全员 grade='A' → 全员 tier=1（D-02 中位数归档：grade=A 中位数位次 ~50%，归 2 档；但全员同 grade 单 grade 横跨整 0-100% 区间，按 D-02 中位数 50% 落 2 档 — 测试需明确该极端 case 的预期结果，建议归 2 档因 50% 位次 ∈ [20%, 90%] 区间属 2 档）
    - A3: 50 人全员 grade='E' → 同 A2 逻辑，全员归 2 档（中位数 50%）
    - A4: 100 人 4 grade 混合（A=10, B=15, C=50, D=20, E=5）→ tiers 数量分布检查
    - A5: 5 人 5 grade（A/B/C/D/E 各 1 人）→ insufficient_sample=true，全员 tier=None

    **B. Ties 行为（4 用例 — D-01 + D-02）**
    - B1: 100 人，前 19 人 grade='A'，第 20 人 grade='A'（即 20 人都 A）→ 整 grade=A 入 1 档（首位累计百分位 1% ≤ 20%，按 D-01 向上扩张）
    - B2: 100 人，前 25 人 grade='A'（25 人都 A）→ 整 grade=A 入 1 档（首位 1% ≤ 20%，D-01 扩张到 25%；末位 25% > 20% 但 ties 不拆分）
    - B3: 100 人，21-25 名 grade='A'（即 5 人 grade=A，全部位次 21-25% 区间）→ ties 首位 21% > 20%，按 D-02 中位数 23% 归档 — 23% ∈ [20%, 90%] 落 2 档
    - B4: 100 人全员 grade='B' → D-02 中位数 50%，全员落 2 档（极端横跨 case）

    **C. 边界样本（6 用例 — D-09）**
    - C1: sample=0（空 list）→ tiers={}，insufficient_sample=true，sample_size=0，actual_distribution={}
    - C2: sample=1（1 人 grade='A'）→ tiers={'e1': None}，insufficient_sample=true
    - C3: sample=2 → tiers 全 None，insufficient_sample=true
    - C4: sample=3 → tiers 全 None，insufficient_sample=true
    - C5: sample=49 → tiers 全 None，insufficient_sample=true
    - C6: sample=50（含全部有效 grade）→ tiers 正常分档，insufficient_sample=false

    **D. 异常 grade（4 用例 — D-08）**
    - D1: 100 人，其中 1 人 grade=None → 该员工 tier=None，sample_size=99，skipped_invalid_grades=1
    - D2: 100 人，1 人 grade=''（空字符串）→ 同 D1
    - D3: 100 人，1 人 grade='F'（不在 GRADE_ORDER）→ 同 D1
    - D4: 100 人，3 人异常（None + '' + '优'）→ sample_size=97，skipped_invalid_grades=3

    **E. Distribution warning（5 用例 — D-03/D-04/D-05）**
    - E1: 1 档实际占比恰 15.0% → distribution_warning=false（边界含等）
    - E2: 1 档实际占比 14.99% → distribution_warning=true（< 15% 触发下界）
    - E3: 1 档实际占比恰 25.0% → distribution_warning=false
    - E4: 1 档实际占比 25.01% → distribution_warning=true（> 25% 触发上界）
    - E5: 三档全部在 [15-25, 65-75, 5-15] 区间 → distribution_warning=false
    - E6（额外）: insufficient_sample=true 时即使分布偏离也强制 distribution_warning=false（D-05）

    **F. 配置覆盖（2 用例 — D-10/D-11）**
    - F1: 自定义 PerformanceTierConfig(min_sample_size=10)，传入 10 人样本 → 不触发 insufficient_sample
    - F2: 自定义 PerformanceTierConfig(distribution_tolerance=0.10)，1 档 12% → 不触发 warning（10% 容差 ∈ [10%, 30%]）

    **G. 不变量 / 结构（2 用例）**
    - G1: TierAssignmentResult 是 dataclass，含 6 个字段（tiers/insufficient_sample/distribution_warning/actual_distribution/sample_size/skipped_invalid_grades）
    - G2: GRADE_ORDER 是从 eligibility_engine import 的同一对象（`is` identity check）

    **总计：5+4+6+4+6+2+2 = 29 用例**（远超 ROADMAP SC-5 的 20 用例最低线）
  </behavior>
  <action>
    **TDD RED → GREEN 流程：**

    **Step 1 (RED)：先写测试文件 `backend/tests/test_engines/test_performance_tier_engine.py`**

    文件骨架：

    ```python
    from __future__ import annotations

    import pytest

    from backend.app.engines.eligibility_engine import GRADE_ORDER as ELIGIBILITY_GRADE_ORDER
    from backend.app.engines.performance_tier_engine import (
        PerformanceTierConfig,
        PerformanceTierEngine,
        TierAssignmentResult,
    )


    @pytest.fixture
    def engine() -> PerformanceTierEngine:
        return PerformanceTierEngine()


    # --- A. 基础正确性 ---
    # --- B. Ties 行为 (D-01/D-02) ---
    # --- C. 边界样本 (D-09) ---
    # --- D. 异常 grade (D-08) ---
    # --- E. Distribution warning (D-03/D-04/D-05) ---
    # --- F. 配置覆盖 (D-10/D-11) ---
    # --- G. 不变量 / 结构 ---
    ```

    每节按 `<behavior>` 列出的 29 个用例编写，参考 `test_eligibility_engine.py` 风格：
    - 一行 assert
    - 函数名 `test_<行为>_<边界>` 格式
    - fixture 提供默认 engine
    - 自定义 config 测试就地构造

    构造 100 人样本的 helper：

    ```python
    def _make_employees(count: int, grade: str) -> list[tuple[str, str]]:
        return [(f'emp_{i:03d}', grade) for i in range(count)]
    ```

    运行 `pytest backend/tests/test_engines/test_performance_tier_engine.py -v` → 必须 ImportError（引擎尚未实现）→ TDD RED 通过。

    **Step 2 (GREEN)：实现 `backend/app/engines/performance_tier_engine.py`**

    ```python
    from __future__ import annotations

    from dataclasses import dataclass, field

    from backend.app.engines.eligibility_engine import GRADE_ORDER


    @dataclass(frozen=True)
    class PerformanceTierConfig:
        """Configurable tier engine parameters (D-10/D-11)."""
        min_sample_size: int = 50
        tier_targets: tuple[float, float, float] = (0.20, 0.70, 0.10)
        distribution_tolerance: float = 0.05


    @dataclass
    class TierAssignmentResult:
        """Tier assignment output (D-07)."""
        tiers: dict[str, int | None] = field(default_factory=dict)
        insufficient_sample: bool = False
        distribution_warning: bool = False
        actual_distribution: dict[int, float] = field(default_factory=dict)
        sample_size: int = 0
        skipped_invalid_grades: int = 0


    class PerformanceTierEngine:
        """Pure-computation performance tier engine (D-09: no exceptions, no I/O)."""

        def __init__(self, config: PerformanceTierConfig | None = None) -> None:
            self.config = config or PerformanceTierConfig()

        def assign(self, employees: list[tuple[str, str | None]]) -> TierAssignmentResult:
            # 1. 分离有效/无效 grade（D-08）
            valid: list[tuple[str, str]] = []
            invalid_ids: list[str] = []
            for emp_id, grade in employees:
                normalized = (grade or '').strip().upper() if grade else ''
                if normalized and normalized in GRADE_ORDER:
                    valid.append((emp_id, normalized))
                else:
                    invalid_ids.append(emp_id)

            sample_size = len(valid)
            tiers: dict[str, int | None] = {emp_id: None for emp_id in invalid_ids}

            # 2. 样本不足分支（D-09）
            if sample_size < self.config.min_sample_size:
                for emp_id, _ in valid:
                    tiers[emp_id] = None
                return TierAssignmentResult(
                    tiers=tiers,
                    insufficient_sample=True,
                    distribution_warning=False,
                    actual_distribution={},
                    sample_size=sample_size,
                    skipped_invalid_grades=len(invalid_ids),
                )

            # 3. 降序排序（D-06：高 grade 在前）
            sorted_emps = sorted(valid, key=lambda x: -GRADE_ORDER[x[1]])

            # 4. Ties 分档（D-01 首位扩张 + D-02 中位数归档）
            assigned = self._assign_tiers(sorted_emps)
            tiers.update(assigned)

            # 5. 实际分布（D-04：分母为成功分档人数）
            distribution = self._compute_distribution(assigned, sample_size)

            # 6. 偏离告警（D-03/D-05）
            warning = self._check_distribution_warning(distribution)

            return TierAssignmentResult(
                tiers=tiers,
                insufficient_sample=False,
                distribution_warning=warning,
                actual_distribution=distribution,
                sample_size=sample_size,
                skipped_invalid_grades=len(invalid_ids),
            )

        def _assign_tiers(self, sorted_emps: list[tuple[str, str]]) -> dict[str, int]:
            """按 D-01 首位扩张 + D-02 中位数归档 切档。

            算法：
            1. 按 grade 分组（已排序，相同 grade 必相邻）
            2. 对每组 ties，看首位累计百分位：
               - 首位 ≤ 20% → 整组 1 档（D-01 向上扩张到 1 档）
               - 首位 > 20% 但 < 90% → 看中位数（D-02）
                 - 中位数 ≤ 20% → 1 档
                 - 中位数 ≤ 90% → 2 档
                 - 中位数 > 90% → 3 档
               - 首位 ≥ 90% → 整组 3 档
            """
            n = len(sorted_emps)
            result: dict[str, int] = {}
            t1_cut = self.config.tier_targets[0]               # 0.20
            t2_cut = self.config.tier_targets[0] + self.config.tier_targets[1]  # 0.90

            # group by grade
            i = 0
            while i < n:
                grade = sorted_emps[i][1]
                j = i
                while j < n and sorted_emps[j][1] == grade:
                    j += 1
                # ties block: indices [i, j)
                first_pct = i / n                              # 首位累计百分位（0-based 即可，D-01 不强求 percentile rank 精确公式）
                last_pct = (j - 1) / n
                median_pct = ((i + j - 1) / 2) / n

                if first_pct < t1_cut:
                    tier = 1                                   # D-01 首位扩张到 1 档
                elif first_pct >= t2_cut:
                    tier = 3
                else:
                    # 跨档边界 — D-02 中位数归档
                    if median_pct < t1_cut:
                        tier = 1
                    elif median_pct < t2_cut:
                        tier = 2
                    else:
                        tier = 3

                for k in range(i, j):
                    result[sorted_emps[k][0]] = tier
                i = j

            return result

        def _compute_distribution(self, assigned: dict[str, int], sample_size: int) -> dict[int, float]:
            """三档实际占比（D-04，4 位小数，分母为已分档人数）。"""
            counts = {1: 0, 2: 0, 3: 0}
            for tier in assigned.values():
                counts[tier] += 1
            return {t: round(counts[t] / sample_size, 4) for t in (1, 2, 3)}

        def _check_distribution_warning(self, distribution: dict[int, float]) -> bool:
            """任一档超出 ±tolerance 区间即触发（D-03/D-05）。"""
            tol = self.config.distribution_tolerance
            targets = self.config.tier_targets
            for idx, tier in enumerate((1, 2, 3)):
                target = targets[idx]
                actual = distribution[tier]
                if actual < target - tol or actual > target + tol:
                    return True
            return False
    ```

    **关键算法注解：**
    - D-01「首位扩张」：当 ties 首位位次 ≤ 20% 时，整组归 1 档（即使末位超出 20%）。实现里 `if first_pct < t1_cut: tier = 1` 一行覆盖。
    - D-02「中位数归档」：当 ties 横跨档位边界时（首位 > 20% 且 < 90%），按中位数位次决定该 grade 的档位。「全员 grade=B」case 的 first_pct=0、median_pct=50% → first_pct < 20% 命中 D-01 → 全员入 1 档；但 behavior B4 期望全员入 2 档 — **需要重新审视**：
      - 重读 D-02 原文：「横跨多档的 ties（极端：某 grade 横跨 2 档边界，例如 25%-95% 百分位）按 ties 组中位数位次归档」
      - 「全员 grade=B」时 ties 占 0%-100%，跨越所有边界，属横跨多档 case → 按中位数 50% 归 2 档
      - 所以算法需先判断「横跨多档」（last_pct > t2_cut 时同时 first_pct < t1_cut → 跨 3 档）：

      ```python
      if first_pct < t1_cut and last_pct >= t2_cut:
          # 横跨多档（极端如全员同 grade）— 按 D-02 中位数归档
          if median_pct < t1_cut: tier = 1
          elif median_pct < t2_cut: tier = 2
          else: tier = 3
      elif first_pct < t1_cut:
          tier = 1                                   # D-01 首位扩张
      elif first_pct >= t2_cut:
          tier = 3
      else:
          # 跨 1↔2 或 2↔3 单档边界 — D-02 中位数归档
          if median_pct < t1_cut: tier = 1
          elif median_pct < t2_cut: tier = 2
          else: tier = 3
      ```

      这样「全员 grade=B」（first=0%、last=99%、median=50%）走第一分支 → 中位数 50% → 2 档 ✓
      「前 25 人都 A」（first=0%、last=24%、median=12%）走第二分支 → D-01 扩张到 1 档 ✓

    - D-04 分母约定：`sample_size`（即已成功分档人数，不含 skipped_invalid_grades）。
    - D-05：样本不足分支已 early return 且强制 `distribution_warning=False`，符合「样本不足时不计算分布告警」。
    - D-08 异常 grade 处理：`(grade or '').strip().upper()` 容错 None / ''；`if normalized in GRADE_ORDER` 过滤 'F'/'A+'/'优'。

    **Step 3：运行测试反复迭代直到全绿**
    ```
    pytest backend/tests/test_engines/test_performance_tier_engine.py -v
    ```
    必须达到「29 passed」。如有失败，**修代码不修测试**（除非测试本身误解了决策原文）。

    **不要做的事：**
    - 不要重新定义 `GRADE_ORDER`（必须从 eligibility_engine import）
    - 不要在 Engine 里读文件 / 调 DB / 调外部 API（CLAUDE.md 强制纯计算）
    - 不要 raise Exception（D-09 强制无异常）
    - 不要把 `tier_targets` / `distribution_tolerance` 暴露为 env 变量（D-11 明确「Phase 33 范围内不开放 env 配置」）
    - 不要写 docstring 之外的注释墙（与现有 engines 风格一致：单行 docstring + 类型注解承担文档）
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust && python -m pytest backend/tests/test_engines/test_performance_tier_engine.py -v --tb=short 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - 文件 `backend/app/engines/performance_tier_engine.py` 存在
    - 文件包含字符串 `class PerformanceTierEngine`
    - 文件包含字符串 `class TierAssignmentResult`
    - 文件包含字符串 `class PerformanceTierConfig`
    - 文件包含字符串 `from backend.app.engines.eligibility_engine import GRADE_ORDER`
    - 文件包含字符串 `from __future__ import annotations`
    - 文件 `backend/tests/test_engines/test_performance_tier_engine.py` 存在
    - `pytest backend/tests/test_engines/test_performance_tier_engine.py` 退出码 0
    - `pytest backend/tests/test_engines/test_performance_tier_engine.py -q` 输出包含 `passed` 且测试数 ≥ 20（即 `\d+ passed` 中数字 ≥ 20）
    - 文件中**不包含** `raise ` 关键字（D-09 无异常约束 — 用 grep 验证 `grep -c "raise " backend/app/engines/performance_tier_engine.py` == 0）
    - 文件中**不包含** `import requests` / `import httpx` / `from sqlalchemy` / `open(` / `Session` 字样（纯计算无 I/O 约束）
  </acceptance_criteria>
  <done>
    - PerformanceTierEngine 实现完成，所有 29 个测试用例通过
    - GRADE_ORDER 单一事实源（从 eligibility_engine import）
    - PerformanceTierConfig 通过构造函数注入，便于测试覆盖不同阈值
    - TierAssignmentResult 6 字段全齐
    - 所有边界样本 / ties / 异常 grade / distribution warning 用例覆盖
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 包导出 wiring — engines/__init__.py 追加 PerformanceTierEngine</name>
  <files>backend/app/engines/__init__.py</files>
  <read_first>
    - backend/app/engines/__init__.py（确认现有 import + __all__ 模式）
    - backend/app/engines/performance_tier_engine.py（任务 2 已创建，确认导出符号名）
  </read_first>
  <action>
    在 `backend/app/engines/__init__.py` 追加 `PerformanceTierEngine`、`TierAssignmentResult` 的 import 与 `__all__` 项。改造后文件应为：

    ```python
    from backend.app.engines.evaluation_engine import EvaluationEngine, EvaluationResult, EvaluatedDimension
    from backend.app.engines.performance_tier_engine import PerformanceTierEngine, PerformanceTierConfig, TierAssignmentResult
    from backend.app.engines.salary_engine import SalaryEngine, SalaryResult

    __all__ = [
        'EvaluationEngine', 'EvaluationResult', 'EvaluatedDimension',
        'PerformanceTierEngine', 'PerformanceTierConfig', 'TierAssignmentResult',
        'SalaryEngine', 'SalaryResult',
    ]
    ```

    保持原有 EvaluationEngine / SalaryEngine 导出不动，按字母序插入 performance_tier_engine import。

    **决策溯源：** CONTEXT「Integration Points」节「`backend/app/engines/__init__.py` — Phase 33 需要 export `PerformanceTierEngine` 与 `TierAssignmentResult` 供 Phase 34 Service 层 import」。同时导出 `PerformanceTierConfig` 是为 Phase 34 Service 显式注入自定义阈值（来自 `Settings.performance_tier_min_sample_size`）。

    **不要做的事：**
    - 不要删除现有 EvaluationEngine / SalaryEngine 导出
    - 不要把 import 改成 `from .performance_tier_engine import ...` 相对路径（与现有 `from backend.app.engines.evaluation_engine import ...` 风格保持一致）
    - 不要新增 `_helper` 等内部符号到 __all__
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust && python -c "from backend.app.engines import PerformanceTierEngine, PerformanceTierConfig, TierAssignmentResult, EvaluationEngine, SalaryEngine; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/engines/__init__.py` 包含字符串 `from backend.app.engines.performance_tier_engine import`
    - `backend/app/engines/__init__.py` 包含字符串 `PerformanceTierEngine`
    - `backend/app/engines/__init__.py` 包含字符串 `TierAssignmentResult`
    - `backend/app/engines/__init__.py` 包含字符串 `PerformanceTierConfig`
    - `backend/app/engines/__init__.py` 包含字符串 `EvaluationEngine`（旧导出未被破坏）
    - `backend/app/engines/__init__.py` 包含字符串 `SalaryEngine`（旧导出未被破坏）
    - `python -c "from backend.app.engines import PerformanceTierEngine, PerformanceTierConfig, TierAssignmentResult, EvaluationEngine, SalaryEngine"` 退出码 0
    - 整个 backend 测试套件未因 wiring 改动出现新失败（`pytest backend/tests/test_engines/ -q` 退出码 0）
  </acceptance_criteria>
  <done>
    PerformanceTierEngine 可通过 `from backend.app.engines import PerformanceTierEngine` 顶层 import，Phase 34 Service 层可零负担引用，旧 Engine 导出不受影响。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service → Engine | Phase 34 Service 层调用 `engine.assign(...)` 时传入的 `(employee_id, grade)` list 来源是已查 `PerformanceRecord` 表的内部数据，不直接来自 HTTP/外部输入；Engine 视为可信内部边界 |
| 配置文件读取 | `Settings.performance_tier_min_sample_size` 来自 `.env`（运维管理）或代码默认值 50；非用户可控输入 |

**不存在的攻击面（说明性）：**
- 无 HTTP 输入边界（Engine 不暴露端点；Phase 34 才接 API）
- 无网络 I/O / DB 访问（CLAUDE.md 强制纯计算）
- 无认证 / 鉴权（Engine 无身份概念，权限在 Service+API 层）
- 无文件 I/O / 反序列化不可信数据
- 无加密敏感数据处理

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering | `assign()` 输入 `(emp_id, grade)` list 中的 grade 字段 | mitigate | 通过 `GRADE_ORDER` 字典查找过滤合法 grade（D-08）；不在表中的字符串（如 'F'、'A+'、'优'、'<script>'）一律计入 `skipped_invalid_grades` 且对应 employee 的 tier 设为 None。**注入风险已消除**：grade 仅作 dict key 查询，从不用作 SQL/HTML/shell 字符串拼接 |
| T-33-02 | Denial of Service | `assign()` 收到空 list / 1-3 人小样本 / 异常 grade 比例极高的 list | mitigate | D-09 强制无异常：所有边界条件（sample=0, 全员 grade=None, sample < min_sample_size）通过返回结构表达（`insufficient_sample=true`、`tiers={}`、`actual_distribution={}`），永不抛 ZeroDivisionError / IndexError / KeyError 等异常导致服务崩溃 |
| T-33-03 | Information Disclosure | `_compute_distribution` 浮点除法精度漂移导致 distribution_warning 误报/漏报 | accept | 边界比较用绝对百分点偏差（D-03，比较 `actual < target - tol`），分母 `sample_size` 是整数，分子 `counts[tier]` 是整数 → 浮点结果可精确表示到 4 位小数。1e-15 级浮点误差不会跨越 ±5% 阈值。通过测试用例 E1-E5 显式覆盖 14.99%/15.0%/25.0%/25.01% 边界值确认 |

**注：** Phase 33 是纯计算引擎，不存在传统 STRIDE 中的 Spoofing / Repudiation / Elevation of Privilege 类别（无身份、无审计日志、无权限边界）。所有现实威胁向量收敛到「上游数据污染」一类，由 D-08 + D-09 完整覆盖。
</threat_model>

<verification>
## 整体 Phase 验证清单

**自动化测试：**
```bash
# 全部 Engine 测试套件通过
cd /Users/mac/PycharmProjects/Wage_adjust
python -m pytest backend/tests/test_engines/ -v

# Phase 33 新引擎单测 ≥ 20 用例全部通过
python -m pytest backend/tests/test_engines/test_performance_tier_engine.py -v
python -m pytest backend/tests/test_engines/test_performance_tier_engine.py -q | grep -E '\d+ passed'

# Settings 加载默认值正常
python -c "from backend.app.core.config import Settings; assert Settings().performance_tier_min_sample_size == 50; print('Settings OK')"

# 包顶层 import 通过
python -c "from backend.app.engines import PerformanceTierEngine, PerformanceTierConfig, TierAssignmentResult; print('Imports OK')"

# 烟测：50 人样本完整流程
python -c "
from backend.app.engines import PerformanceTierEngine
engine = PerformanceTierEngine()
emps = [(f'e{i}', 'A' if i < 10 else 'B' if i < 45 else 'D') for i in range(50)]
r = engine.assign(emps)
assert not r.insufficient_sample
assert r.sample_size == 50
assert r.skipped_invalid_grades == 0
assert all(v in (1, 2, 3) for v in r.tiers.values())
print(f'tiers={dict(list(r.tiers.items())[:3])}... distribution={r.actual_distribution} warning={r.distribution_warning}')
"
```

**手工 grep 检查（D-09 / 纯计算约束）：**
```bash
# Engine 文件内不含 raise 语句（D-09）
test 0 -eq $(grep -c "^[[:space:]]*raise " backend/app/engines/performance_tier_engine.py)

# Engine 不引入 I/O 依赖
! grep -E "^(import|from)" backend/app/engines/performance_tier_engine.py | grep -E "(httpx|requests|sqlalchemy|sqlite|open\(|Session|aiohttp|boto|minio)"

# 必须 import GRADE_ORDER（D-06 单一事实源）
grep -q "from backend.app.engines.eligibility_engine import GRADE_ORDER" backend/app/engines/performance_tier_engine.py
```

**配置/导出检查：**
```bash
# Settings 字段存在
grep -q "performance_tier_min_sample_size: int = 50" backend/app/core/config.py

# .env.example 已添加
grep -q "PERFORMANCE_TIER_MIN_SAMPLE_SIZE" .env.example

# 包导出齐全
grep -q "PerformanceTierEngine" backend/app/engines/__init__.py
grep -q "TierAssignmentResult" backend/app/engines/__init__.py
grep -q "PerformanceTierConfig" backend/app/engines/__init__.py
```

**回归保护：**
- `pytest backend/tests/` 整体退出码 0（不影响其它测试）
- `python -c "from backend.app.main import app"` 退出码 0（main 应用可正常 import，未因配置改动崩溃）
</verification>

<success_criteria>
对应 ROADMAP Phase 33 五条 Success Criteria：

| ROADMAP SC | 验证方法 | 落地位置 |
|------------|----------|----------|
| SC-1：调用 `PerformanceTierEngine.assign()` 输入排序后 `(employee_id, grade)` 列表，返回 `{employee_id: 1\|2\|3\|None}` 映射；ties 同档 | Task 2 测试组 A1+B1+B2+B3 | `assign()` + `_assign_tiers()` |
| SC-2：样本量 < `Settings.performance_tier_min_sample_size`（默认 50）时全员 `tier=null` + `insufficient_sample=true` | Task 2 测试组 C5（49 人）+ Task 1 Settings 加载 | `assign()` 早返回分支 + `Settings.performance_tier_min_sample_size = 50` |
| SC-3：人数 0 / 1 / 2 / 3 等边界样本不抛异常，行为与「样本不足」分支一致 | Task 2 测试组 C1-C4 | D-09 无异常约束 + `sample_size < min_sample_size` 早返回 |
| SC-4：实际分档分布偏离 20/70/10 ±5% 时 `distribution_warning=true` | Task 2 测试组 E1-E5 | `_compute_distribution()` + `_check_distribution_warning()` |
| SC-5：单元测试覆盖 20+ 用例（ties / 全员同分 / samples=0/1/2/3 / 分布偏离 / `min_sample_size` 配置化） | Task 2 共 29 用例覆盖 A-G 七组 | `test_performance_tier_engine.py` |

**所有 5 项 SC + 3 项 PERF 需求（PERF-03/04/06）落地完成。**
</success_criteria>

<output>
After completion, create `.planning/phases/33-performance-tier-engine/33-01-SUMMARY.md` documenting:

- 实现的字段/方法清单（PerformanceTierEngine / PerformanceTierConfig / TierAssignmentResult）
- 测试用例数与 ROADMAP SC 映射表
- D-01 到 D-11 决策落地点引用（哪个测试用例 / 哪段代码）
- 与 Phase 34 Service 层的接口契约（输入 list、输出 TierAssignmentResult 6 字段）
- 已知遗留：`tier_targets` / `distribution_tolerance` 未开放 env 覆盖（按 D-11 — Phase 34 / v1.5 再考虑）
</output>
