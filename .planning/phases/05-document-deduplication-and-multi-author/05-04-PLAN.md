---
phase: 05-document-deduplication-and-multi-author
plan: 04
type: execute
wave: 3
depends_on: ["05-02", "05-03"]
files_modified:
  - frontend/src/components/evaluation/FileUploadPanel.tsx
  - frontend/src/components/evaluation/ContributorPicker.tsx
  - frontend/src/components/approval/ContributorTags.tsx
  - frontend/src/pages/Approvals.tsx
  - frontend/src/services/fileService.ts
  - frontend/src/types/api.ts
autonomous: false
requirements:
  - SUB-01
  - SUB-02
  - SUB-03
  - SUB-05

must_haves:
  truths:
    - "上传重复文件时显示拒绝消息含已有记录引用 / Duplicate upload shows rejection message with existing record reference (per D-03)"
    - "上传时可选择协作者并输入贡献比例 / Upload UI allows selecting contributors with percentages (per D-05)"
    - "审批页面用内联标签展示协作者和贡献比例 / Approval page shows contributors as inline tags (per D-11)"
  artifacts:
    - path: "frontend/src/components/evaluation/ContributorPicker.tsx"
      provides: "协作者选择器组件 / Contributor picker component"
      contains: "ContributorPicker"
    - path: "frontend/src/components/approval/ContributorTags.tsx"
      provides: "协作者标签展示组件 / Contributor tags display component"
      contains: "ContributorTags"
  key_links:
    - from: "frontend/src/components/evaluation/FileUploadPanel.tsx"
      to: "frontend/src/services/fileService.ts"
      via: "上传请求含 contributors FormData / Upload request with contributors FormData"
      pattern: "contributors"
    - from: "frontend/src/pages/Approvals.tsx"
      to: "frontend/src/components/approval/ContributorTags.tsx"
      via: "project_contributors 数据渲染"
      pattern: "ContributorTags"
---

<objective>
实现前端的去重检测 UX、协作者选择器和审批页面协作者标签展示。

Implement frontend deduplication UX, contributor picker, and approval page contributor tag display.

Purpose: 完成用户可见的交互层，让员工可以分配协作者，审批人可以看到协作信息。
Output: ContributorPicker 组件、ContributorTags 组件、FileUploadPanel 扩展、Approvals 页面更新。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-document-deduplication-and-multi-author/05-CONTEXT.md
@.planning/phases/05-document-deduplication-and-multi-author/05-02-SUMMARY.md
@.planning/phases/05-document-deduplication-and-multi-author/05-03-SUMMARY.md

<interfaces>
From backend API (Plan 02):
- POST /api/v1/submissions/{id}/files — accepts `contributors` Form field (JSON string)
- HTTP 409 on duplicate with `{ error: "duplicate_file", existing_file_id, uploaded_by, uploaded_at, message }`

From backend API (Plan 03):
- GET /api/v1/approvals/ — ApprovalRecordRead now includes:
  ```typescript
  project_contributors: Array<{
    employee_id: string;
    employee_name: string;
    contribution_pct: number;
    file_name: string;
    is_owner: boolean;
  }>
  ```

From frontend/src/types/api.ts (current types to extend):
- ApprovalRecord interface
- UploadedFile interface (if exists)

From frontend/src/components/evaluation/FileUploadPanel.tsx:
```typescript
interface FileUploadPanelProps {
  isGithubImporting?: boolean;
  isUploading: boolean;
  onFilesSelected: (files: FileList | null) => void;
  onGitHubImport?: (url: string) => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript 类型 + fileService 更新 + ContributorPicker 组件 / Types, service, and contributor picker</name>
  <files>
    frontend/src/types/api.ts
    frontend/src/services/fileService.ts
    frontend/src/components/evaluation/ContributorPicker.tsx
    frontend/src/components/evaluation/FileUploadPanel.tsx
  </files>
  <read_first>
    frontend/src/types/api.ts
    frontend/src/services/fileService.ts
    frontend/src/components/evaluation/FileUploadPanel.tsx
    frontend/src/services/api.ts
    frontend/src/services/employeeService.ts
  </read_first>
  <action>
    1. **扩展 frontend/src/types/api.ts**:
       ```typescript
       export interface ContributorInput {
         employee_id: string;
         contribution_pct: number;
       }

       export interface ProjectContributorSummary {
         employee_id: string;
         employee_name: string;
         contribution_pct: number;
         file_name: string;
         is_owner: boolean;
       }

       export interface DuplicateFileError {
         error: 'duplicate_file';
         existing_file_id: string;
         uploaded_by: string;
         uploaded_at: string;
         message: string;
       }
       ```
       在 ApprovalRecord 接口中添加 `project_contributors: ProjectContributorSummary[]`。

    2. **创建或更新 frontend/src/services/fileService.ts**:
       - 导出 `uploadFiles(submissionId: string, files: FileList, contributors?: ContributorInput[]): Promise<UploadedFile[]>`
       - 构造 FormData: files 逐个 append，contributors 序列化为 JSON 字符串 append 为 'contributors' 字段
       - POST 到 `/api/v1/submissions/${submissionId}/files`
       - 处理 409 响应：解析 DuplicateFileError 并抛出带中文消息的错误

    3. **创建 ContributorPicker.tsx** (per D-05, D-06):
       ```typescript
       interface ContributorPickerProps {
         contributors: ContributorInput[];
         onChange: (contributors: ContributorInput[]) => void;
         disabled?: boolean;
       }
       ```
       - 使用 employeeService 加载员工列表（下拉选择）
       - 每行：员工选择下拉 + 贡献百分比数字输入 + 删除按钮
       - 底部显示"剩余比例（您的份额）: XX%"
       - 验证: 总和不超过 99%（至少给上传者留 1%），每个百分比 > 0
       - 添加按钮："+添加协作者"
       - Tailwind 样式，与现有上传面板视觉一致

    4. **修改 FileUploadPanel.tsx**:
       - Props 新增: `contributors?: ContributorInput[]`, `onContributorsChange?: (contributors: ContributorInput[]) => void`, `showContributorPicker?: boolean`
       - 在文件选择区域下方，条件渲染 ContributorPicker
       - onFilesSelected 回调携带 contributors 信息
       - 上传时如果收到 409 DuplicateFileError，显示 toast 或 inline 错误消息（per D-03: "此文件已由 XXX 在 YYYY-MM-DD 提交"）
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ContributorInput" frontend/src/types/api.ts
    - grep -q "ProjectContributorSummary" frontend/src/types/api.ts
    - grep -q "DuplicateFileError" frontend/src/types/api.ts
    - grep -q "ContributorPicker" frontend/src/components/evaluation/ContributorPicker.tsx
    - grep -q "contributors" frontend/src/components/evaluation/FileUploadPanel.tsx
    - tsc --noEmit 无类型错误
  </acceptance_criteria>
  <done>
    TypeScript 类型扩展完成，fileService 支持带 contributors 的 FormData 上传和 409 处理，ContributorPicker 组件可选择协作者和输入百分比，FileUploadPanel 集成 ContributorPicker 和去重错误展示。
  </done>
</task>

<task type="auto">
  <name>Task 2: 审批页面协作者标签展示 / Contributor tags in approval page</name>
  <files>
    frontend/src/components/approval/ContributorTags.tsx
    frontend/src/pages/Approvals.tsx
  </files>
  <read_first>
    frontend/src/pages/Approvals.tsx
    frontend/src/types/api.ts
    frontend/src/components/approval/ApprovalTable.tsx
  </read_first>
  <action>
    1. **创建 ContributorTags.tsx** (per D-11):
       ```typescript
       interface ContributorTagsProps {
         contributors: ProjectContributorSummary[];
       }
       ```
       - 每个协作者渲染为内联标签: `<span>` 含员工名 + 百分比 badge
       - 上传者（is_owner=true）用不同颜色/样式标记（如实心 badge vs 空心 badge）
       - 按贡献比例降序排列
       - 使用 Tailwind 的 flex-wrap gap-2 布局
       - 标签样式: 圆角、小字号、背景色区分 owner vs contributor
       - 示例: `[张三 40%] [李四 35%] [王五 25%]`（张三为 owner 用深色背景）

    2. **修改 Approvals.tsx**:
       - 在审批详情区域（展示评估维度分数的附近），添加"项目协作者"区块
       - 条件渲染: 仅当 `project_contributors.length > 0` 时显示
       - 使用 ContributorTags 组件渲染协作者列表
       - 标题: "项目协作者" + 文件名分组显示
       - 位置: 在 dimension_scores 展示区域之后、审批操作按钮之前
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ContributorTags" frontend/src/components/approval/ContributorTags.tsx
    - grep -q "is_owner" frontend/src/components/approval/ContributorTags.tsx
    - grep -q "project_contributors" frontend/src/pages/Approvals.tsx
    - grep -q "ContributorTags" frontend/src/pages/Approvals.tsx
    - tsc --noEmit 无类型错误
  </acceptance_criteria>
  <done>
    审批页面在有共享项目时显示协作者标签，上传者和协作者用不同视觉样式区分，审批人可一目了然看到项目的贡献分配。SUB-05 前端完成。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 人工验收 Phase 5 全部功能 / Human verify all Phase 5 features</name>
  <action>
    人工验证去重检测、协作者选择、评分折算、审批协作者展示全部功能正常。
    Manually verify dedup detection, contributor picker, score scaling, and approval contributor display.
  </action>
  <what-built>
    Phase 5 全部功能: 文件去重检测、协作者选择、评分折算、审批协作者展示。
    Phase 5 complete: file dedup detection, contributor picker, score scaling, approval contributor display.
  </what-built>
  <how-to-verify>
    1. 启动后端: `uvicorn backend.app.main:app --reload`
    2. 启动前端: `cd frontend && npm run dev`
    3. 测试去重 (SUB-01):
       - 上传一个文件到某员工的提交
       - 再次上传完全相同的文件 → 应看到拒绝消息，提示已存在
    4. 测试协作者 (SUB-02):
       - 上传文件时勾选"添加协作者"
       - 选择其他员工，输入贡献百分比
       - 确认总和提示正确，上传成功
    5. 测试共享查看 (SUB-03):
       - 切换到协作者账号
       - 在材料列表中应能看到共享项目
    6. 测试审批视图 (SUB-05):
       - 进入审批页面
       - 查看含共享项目的评估 → 应看到协作者标签和贡献比例
  </how-to-verify>
  <verify>人工验证 / Manual verification</verify>
  <done>用户确认全部功能正常 / User confirms all features working</done>
  <resume-signal>输入 "approved" 或描述发现的问题 / Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` — 无类型错误
- `cd frontend && npm run build` — 构建成功
- 视觉验证: 协作者选择器和审批标签按预期渲染
</verification>

<success_criteria>
- 上传重复文件显示中文拒绝消息含已有记录引用 (SUB-01 前端)
- 上传时可选择协作者、输入百分比、实时显示剩余比例 (SUB-02 前端)
- 审批页面用内联标签展示协作者，owner 视觉区分 (SUB-05 前端)
- TypeScript 编译无错误，前端构建成功
</success_criteria>

<output>
完成后创建 `.planning/phases/05-document-deduplication-and-multi-author/05-04-SUMMARY.md`
</output>
