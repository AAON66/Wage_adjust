---
phase: 05-document-deduplication-and-multi-author
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/project_contributor.py
  - backend/app/models/uploaded_file.py
  - backend/app/models/submission.py
  - backend/app/schemas/file.py
  - backend/app/schemas/approval.py
  - alembic/versions/005_phase05_content_hash_and_contributors.py
  - backend/tests/test_submission/__init__.py
  - backend/tests/test_submission/test_file_dedup.py
  - backend/tests/test_submission/test_contributor_service.py
  - backend/tests/test_submission/test_score_scaling.py
  - backend/tests/test_submission/test_approval_contributors.py
autonomous: true
requirements:
  - SUB-01
  - SUB-02
  - SUB-03
  - SUB-04
  - SUB-05

must_haves:
  truths:
    - "UploadedFile 模型包含 content_hash 字段 / UploadedFile model has content_hash column"
    - "ProjectContributor 关联表存在且有正确的外键和约束 / ProjectContributor association table exists with correct FKs and constraints"
    - "所有 5 个 SUB 需求都有对应的测试桩 / All 5 SUB requirements have test stubs"
  artifacts:
    - path: "backend/app/models/project_contributor.py"
      provides: "ProjectContributor 模型 / association table model"
      contains: "class ProjectContributor"
    - path: "alembic/versions/005_phase05_content_hash_and_contributors.py"
      provides: "Schema migration"
      contains: "content_hash"
    - path: "backend/tests/test_submission/test_file_dedup.py"
      provides: "SUB-01 test stubs"
      contains: "test_duplicate_upload_rejected"
  key_links:
    - from: "backend/app/models/project_contributor.py"
      to: "backend/app/models/uploaded_file.py"
      via: "ForeignKey uploaded_files.id"
      pattern: "ForeignKey.*uploaded_files"
    - from: "backend/app/models/project_contributor.py"
      to: "backend/app/models/submission.py"
      via: "ForeignKey employee_submissions.id"
      pattern: "ForeignKey.*employee_submissions"
---

<objective>
创建 Phase 5 的数据基础：Alembic 迁移、模型定义、Pydantic schema 扩展、以及所有需求的 RED 测试桩。

Create Phase 5 data foundation: Alembic migration, model definitions, Pydantic schema extensions, and RED test stubs for all requirements.

Purpose: 后续计划的所有后端逻辑和前端功能都依赖于此基础层。测试桩确保 Nyquist 合规。
Output: 新模型文件、迁移脚本、schema 扩展、5 个测试文件（全部 RED）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-document-deduplication-and-multi-author/05-CONTEXT.md
@.planning/phases/05-document-deduplication-and-multi-author/05-RESEARCH.md

<interfaces>
<!-- 执行者需要的关键类型和契约 / Key types and contracts the executor needs -->

From backend/app/models/mixins.py:
```python
class UUIDPrimaryKeyMixin:
    id: Mapped[str] = mapped_column(String(36), primary_key=True, default=generate_uuid)

class CreatedAtMixin:
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False, default=utc_now)
```

From backend/app/models/uploaded_file.py:
```python
class UploadedFile(UUIDPrimaryKeyMixin, CreatedAtMixin, Base):
    __tablename__ = "uploaded_files"
    submission_id: Mapped[str] = mapped_column(ForeignKey("employee_submissions.id"), nullable=False, index=True)
    file_name: Mapped[str] = mapped_column(String(255), nullable=False)
    file_type: Mapped[str] = mapped_column(String(32), nullable=False, index=True)
    storage_key: Mapped[str] = mapped_column(String(512), nullable=False, unique=True)
    parse_status: Mapped[str] = mapped_column(String(32), nullable=False, default="pending", index=True)
    submission = relationship("EmployeeSubmission", back_populates="uploaded_files")
```

From backend/app/models/submission.py:
```python
class EmployeeSubmission(UUIDPrimaryKeyMixin, CreatedAtMixin, Base):
    __tablename__ = "employee_submissions"
    __table_args__ = (UniqueConstraint("employee_id", "cycle_id"),)
    employee_id: Mapped[str] = mapped_column(ForeignKey("employees.id"), nullable=False, index=True)
    cycle_id: Mapped[str] = mapped_column(ForeignKey("evaluation_cycles.id"), nullable=False, index=True)
    uploaded_files = relationship("UploadedFile", back_populates="submission")
    evidence_items = relationship("EvidenceItem", back_populates="submission")
```

From backend/app/schemas/approval.py:
```python
class ApprovalRecordRead(BaseModel):
    # ... existing fields ...
    dimension_scores: list[DimensionScoreRead] = []
```

Existing Alembic migration naming pattern:
- `005_phase05_content_hash_and_contributors.py` (sequential number prefix)
- Uses `batch_alter_table` for SQLite compatibility
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 ProjectContributor 模型 + 扩展 UploadedFile + Alembic 迁移 / Create models and migration</name>
  <files>
    backend/app/models/project_contributor.py
    backend/app/models/uploaded_file.py
    backend/app/models/submission.py
    backend/app/schemas/file.py
    backend/app/schemas/approval.py
    alembic/versions/005_phase05_content_hash_and_contributors.py
  </files>
  <read_first>
    backend/app/models/uploaded_file.py
    backend/app/models/submission.py
    backend/app/models/mixins.py
    backend/app/models/__init__.py
    backend/app/schemas/file.py
    backend/app/schemas/approval.py
    backend/app/core/database.py
    alembic/versions/004_audit_log_operator_role_request_id.py
  </read_first>
  <action>
    1. 创建 `backend/app/models/project_contributor.py`（per D-05, D-06, D-08）:
       ```python
       class ProjectContributor(UUIDPrimaryKeyMixin, CreatedAtMixin, Base):
           __tablename__ = "project_contributors"
           __table_args__ = (
               UniqueConstraint("uploaded_file_id", "submission_id", name="uq_project_contributors_file_submission"),
               CheckConstraint("contribution_pct > 0 AND contribution_pct <= 100", name="ck_contribution_pct_range"),
           )
           uploaded_file_id: Mapped[str] = mapped_column(ForeignKey("uploaded_files.id", ondelete="CASCADE"), nullable=False, index=True)
           submission_id: Mapped[str] = mapped_column(ForeignKey("employee_submissions.id", ondelete="CASCADE"), nullable=False, index=True)
           contribution_pct: Mapped[float] = mapped_column(Float, nullable=False)
           status: Mapped[str] = mapped_column(String(32), nullable=False, default="accepted")  # accepted, disputed, resolved
           uploaded_file = relationship("UploadedFile", back_populates="contributors")
           submission = relationship("EmployeeSubmission", back_populates="contributed_projects")
       ```

    2. 扩展 `UploadedFile` 模型 — 添加:
       - `content_hash: Mapped[str] = mapped_column(String(64), nullable=False, default='', server_default='', index=True)`
       - `owner_contribution_pct: Mapped[float] = mapped_column(Float, nullable=False, default=100.0, server_default='100.0')`
       - `contributors = relationship("ProjectContributor", back_populates="uploaded_file", cascade="all, delete-orphan")`

    3. 扩展 `EmployeeSubmission` 模型 — 添加:
       - `contributed_projects = relationship("ProjectContributor", back_populates="submission")`

    4. 扩展 `backend/app/schemas/file.py` — 添加:
       ```python
       class ContributorInput(BaseModel):
           employee_id: str
           contribution_pct: float = Field(gt=0, le=100)

       class ContributorRead(BaseModel):
           model_config = ConfigDict(from_attributes=True)
           id: str
           uploaded_file_id: str
           submission_id: str
           employee_id: str
           employee_name: str
           contribution_pct: float
           status: str

       class DuplicateFileError(BaseModel):
           error: str = "duplicate_file"
           existing_file_id: str
           uploaded_by: str
           uploaded_at: str
           message: str
       ```
       同时在 `UploadedFileRead` 中添加 `content_hash: str` 和 `owner_contribution_pct: float = 100.0` 以及 `contributors: list[ContributorRead] = []`。

    5. 扩展 `backend/app/schemas/approval.py` — 在 `ApprovalRecordRead` 中添加:
       ```python
       project_contributors: list[ProjectContributorSummary] = []
       ```
       其中 `ProjectContributorSummary` 定义为:
       ```python
       class ProjectContributorSummary(BaseModel):
           employee_id: str
           employee_name: str
           contribution_pct: float
           file_name: str
           is_owner: bool = False
       ```

    6. 创建 Alembic 迁移 `005_phase05_content_hash_and_contributors.py`:
       - 使用 `batch_alter_table` 添加 `content_hash` (String(64), nullable=False, server_default='') 和 `owner_contribution_pct` (Float, nullable=False, server_default='100.0') 到 `uploaded_files`
       - 创建 `project_contributors` 表（所有字段如上所述）
       - 创建索引 `ix_uploaded_files_content_hash`
       - downgrade: drop `project_contributors` 表，用 `batch_alter_table` 移除两列
       - revision 链接到前一个迁移 `004_audit_log_operator_role_request_id` 的 revision ID
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m alembic upgrade head 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class ProjectContributor" backend/app/models/project_contributor.py
    - grep -q "content_hash" backend/app/models/uploaded_file.py
    - grep -q "contributed_projects" backend/app/models/submission.py
    - grep -q "ContributorInput" backend/app/schemas/file.py
    - grep -q "ProjectContributorSummary" backend/app/schemas/approval.py
    - grep -q "project_contributors" alembic/versions/005_phase05_content_hash_and_contributors.py
  </acceptance_criteria>
  <done>
    ProjectContributor 模型已创建，UploadedFile 和 EmployeeSubmission 已扩展 relationship，Pydantic schema 包含 ContributorInput/ContributorRead/DuplicateFileError/ProjectContributorSummary，Alembic 迁移可成功执行。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 创建 RED 测试桩覆盖全部 SUB 需求 / Create RED test stubs for all SUB requirements</name>
  <files>
    backend/tests/test_submission/__init__.py
    backend/tests/test_submission/test_file_dedup.py
    backend/tests/test_submission/test_contributor_service.py
    backend/tests/test_submission/test_score_scaling.py
    backend/tests/test_submission/test_approval_contributors.py
  </files>
  <read_first>
    backend/tests/test_services/test_approval_service.py
    backend/tests/test_services/test_evaluation_service.py
    backend/tests/test_api/test_file_api.py
    backend/app/models/project_contributor.py
    backend/app/schemas/file.py
  </read_first>
  <behavior>
    SUB-01 测试 (test_file_dedup.py):
    - test_duplicate_upload_rejected: 同一员工上传相同 file_name+content_hash 应返回错误引用已有记录
    - test_duplicate_different_employee_allowed: 不同员工上传相同文件应成功（per D-02 全局去重但 RESEARCH 建议 per-employee，采用 D-02 全局去重）
    - test_replace_file_dedup_check: 替换文件时也应检查去重（排除当前文件）
    - test_github_import_dedup_check: GitHub 导入也应检查去重

    SUB-02 测试 (test_contributor_service.py):
    - test_contributors_saved: 上传时指定协作者，project_contributors 表有记录
    - test_contributor_pct_sum_exceeds_100_rejected: 合作者比例总和 >=100 应拒绝
    - test_contributor_pct_zero_rejected: 0% 贡献应拒绝
    - test_owner_pct_computed: owner_contribution_pct = 100 - sum(contributor_pcts)
    - test_contribution_locked_after_evaluation: 评估提交后贡献比例不可修改（per D-07）

    SUB-03 测试 (test_contributor_service.py):
    - test_contributor_sees_shared_file: 协作者在 list_files 中可见共享项目
    - test_contributor_can_upload_supplementary: 协作者可上传补充材料到自己的 submission

    SUB-04 测试 (test_score_scaling.py):
    - test_shared_project_score_scaling: 80 分项目 × 60% 贡献 = 48 有效分（per D-08）
    - test_owner_score_scaling: 上传者有效分 = score × owner_pct/100
    - test_no_contributors_full_score: 无协作者时获得 100% 分数

    SUB-05 测试 (test_approval_contributors.py):
    - test_approval_shows_contributors: ApprovalRecordRead 包含 project_contributors 列表
    - test_approval_shows_owner_in_contributors: 上传者也出现在列表中（is_owner=True）
  </behavior>
  <action>
    1. 创建 `backend/tests/test_submission/__init__.py`（空文件）

    2. 创建 `test_file_dedup.py` — 4 个测试函数，全部使用 `pytest.mark.xfail(reason="RED: not implemented")`:
       - 每个测试设置 in-memory SQLite 数据库（复用项目的 conftest 模式），创建 EmployeeSubmission、UploadedFile
       - 测试调用 FileService 方法，断言 ValueError 或 HTTP 409

    3. 创建 `test_contributor_service.py` — 7 个测试函数，全部 xfail:
       - 测试 FileService 的 contributor CRUD 和验证逻辑
       - 测试 list_files 返回共享文件

    4. 创建 `test_score_scaling.py` — 3 个测试函数，全部 xfail:
       - 测试 EvaluationService 的分数折算逻辑

    5. 创建 `test_approval_contributors.py` — 2 个测试函数，全部 xfail:
       - 测试 ApprovalService 返回的记录包含 contributors

    所有测试用 `@pytest.mark.xfail(reason="RED: SUB-XX not implemented")` 标记。测试体写完整断言逻辑（不是空函数），但因功能未实现会失败。
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest backend/tests/test_submission/ -q --tb=short 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "test_duplicate_upload_rejected" backend/tests/test_submission/test_file_dedup.py
    - grep -q "test_contributors_saved" backend/tests/test_submission/test_contributor_service.py
    - grep -q "test_shared_project_score_scaling" backend/tests/test_submission/test_score_scaling.py
    - grep -q "test_approval_shows_contributors" backend/tests/test_submission/test_approval_contributors.py
    - grep -rq "xfail" backend/tests/test_submission/
  </acceptance_criteria>
  <done>
    16 个测试桩全部创建，全部标记 xfail，pytest 运行无 ERROR（只有 xfail）。覆盖 SUB-01 到 SUB-05 全部需求。
  </done>
</task>

</tasks>

<verification>
- `alembic upgrade head` 成功执行，无报错
- `pytest backend/tests/test_submission/ -q` 显示所有测试为 xfail（非 ERROR）
- ProjectContributor 模型包含 uploaded_file_id, submission_id, contribution_pct, status 字段
- UploadedFile 模型包含 content_hash, owner_contribution_pct 字段和 contributors relationship
</verification>

<success_criteria>
- Alembic 迁移可正向和反向执行
- 所有新模型和 schema 文件存在且可导入
- 16 个测试桩全部 RED（xfail），覆盖 5 个 SUB 需求
- 现有测试套件不受影响（`pytest backend/tests/ -q --ignore=backend/tests/test_submission` 通过）
</success_criteria>

<output>
完成后创建 `.planning/phases/05-document-deduplication-and-multi-author/05-01-SUMMARY.md`
</output>
