---
phase: 22-ai
plan: 03
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - frontend/src/services/taskService.ts
  - frontend/src/hooks/useTaskPolling.ts
  - frontend/src/services/evaluationService.ts
  - frontend/src/services/importService.ts
  - frontend/src/pages/EvaluationDetail.tsx
  - frontend/src/pages/ImportCenter.tsx
  - frontend/src/types/api.ts
autonomous: false
requirements:
  - ASYNC-02
  - ASYNC-03

must_haves:
  truths:
    - "触发 AI 评估后页面显示 'AI 评估中...' + Spinner，完成后自动刷新评估数据"
    - "触发批量导入后页面显示 '导入中 X/Y 行'，完成后自动刷新导入列表"
    - "轮询间隔固定 2 秒，组件卸载后 setInterval 被清理"
    - "任务失败时显示错误信息，用户可重新触发"
  artifacts:
    - path: "frontend/src/services/taskService.ts"
      provides: "fetchTaskStatus API 封装"
      contains: "fetchTaskStatus"
    - path: "frontend/src/hooks/useTaskPolling.ts"
      provides: "通用轮询 hook"
      contains: "useTaskPolling"
    - path: "frontend/src/services/evaluationService.ts"
      provides: "改造后的 generateEvaluation 返回 task_id"
      contains: "TaskTriggerResponse"
    - path: "frontend/src/services/importService.ts"
      provides: "改造后的 createImportJob 返回 task_id"
      contains: "TaskTriggerResponse"
  key_links:
    - from: "frontend/src/hooks/useTaskPolling.ts"
      to: "frontend/src/services/taskService.ts"
      via: "import fetchTaskStatus"
      pattern: "fetchTaskStatus"
    - from: "frontend/src/pages/EvaluationDetail.tsx"
      to: "frontend/src/hooks/useTaskPolling.ts"
      via: "import useTaskPolling"
      pattern: "useTaskPolling"
    - from: "frontend/src/pages/ImportCenter.tsx"
      to: "frontend/src/hooks/useTaskPolling.ts"
      via: "import useTaskPolling"
      pattern: "useTaskPolling"
---

<objective>
实现前端异步轮询集成：新建 taskService 和 useTaskPolling hook，改造 evaluationService/importService 适配新的 API 契约，更新 EvaluationDetail 和 ImportCenter 页面展示任务进度。

Purpose: 前端与后端异步 API 对接，用户在触发 AI 评估或批量导入后可实时看到任务状态。
Output: taskService.ts + useTaskPolling.ts + 改造后的 service 和页面文件
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-ai/22-CONTEXT.md
@.planning/phases/22-ai/22-RESEARCH.md
@.planning/phases/22-ai/22-01-SUMMARY.md

<interfaces>
<!-- Plan 01 后端 API 契约 -->

GET /api/v1/tasks/{task_id} 返回:
```json
{
  "task_id": "string",
  "status": "pending | running | completed | failed",
  "progress": { "processed": 0, "total": 100, "errors": 2 },  // optional
  "result": { ... },  // evaluation 或 import job 对象 (completed 时)
  "error": "string"   // failed 时
}
```

POST /api/v1/evaluations/generate 返回 202:
```json
{ "task_id": "string", "status": "pending" }
```

POST /api/v1/evaluations/regenerate 返回 202:
```json
{ "task_id": "string", "status": "pending" }
```

POST /api/v1/imports/jobs 返回 202:
```json
{ "task_id": "string", "status": "pending" }
```

<!-- 现有前端接口 -->

From frontend/src/types/api.ts:
```typescript
export interface EvaluationRecord { id: string; submission_id: string; overall_score: number; ... }
export interface ImportJobRecord { id: string; file_name: string; import_type: string; status: string; total_rows: number; success_rows: number; failed_rows: number; result_summary: object; ... }
```

From frontend/src/services/evaluationService.ts:
```typescript
export async function generateEvaluation(submissionId: string): Promise<EvaluationRecord> { ... }
export async function regenerateEvaluation(submissionId: string): Promise<EvaluationRecord> { ... }
```

From frontend/src/services/importService.ts:
```typescript
export async function createImportJob(importType: string, file: File): Promise<ImportJobRecord> { ... }
```

From frontend/src/pages/EvaluationDetail.tsx:
```typescript
// handleGenerateEvaluation() 调用 generateEvaluation/regenerateEvaluation
// 结果直接 setEvaluation(nextEvaluation)
```

From frontend/src/pages/ImportCenter.tsx:
```typescript
// handleUpload() 调用 createImportJob(selectedType, selectedFile)
// 结果直接 setLastImportResult(result) + loadJobs()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 taskService.ts + useTaskPolling.ts + 更新 types/api.ts</name>
  <files>
    frontend/src/services/taskService.ts,
    frontend/src/hooks/useTaskPolling.ts,
    frontend/src/types/api.ts
  </files>
  <read_first>
    frontend/src/services/api.ts,
    frontend/src/types/api.ts,
    frontend/src/hooks/useAuth.tsx
  </read_first>
  <action>

**1. 更新 `frontend/src/types/api.ts`**

在文件末尾添加以下类型定义：

```typescript
export interface TaskTriggerResponse {
  task_id: string;
  status: 'pending';
}

export interface TaskStatusResponse {
  task_id: string;
  status: 'pending' | 'running' | 'completed' | 'failed';
  progress?: { processed: number; total: number; errors: number };
  result?: unknown;
  error?: string;
}
```

**2. 创建 `frontend/src/services/taskService.ts`** (per D-05)

```typescript
import api from './api';
import type { TaskStatusResponse } from '../types/api';

export async function fetchTaskStatus(taskId: string): Promise<TaskStatusResponse> {
  const response = await api.get<TaskStatusResponse>(`/tasks/${taskId}`);
  return response.data;
}
```

**3. 创建 `frontend/src/hooks/useTaskPolling.ts`** (per D-01, D-02, D-03)

自定义 hook 接口：
```typescript
import { useEffect, useRef, useCallback, useState } from 'react';
import { fetchTaskStatus } from '../services/taskService';
import type { TaskStatusResponse } from '../types/api';

interface UseTaskPollingOptions {
  onComplete: (result: unknown) => void;
  onError: (error: string) => void;
  onProgress?: (progress: { processed: number; total: number; errors: number }) => void;
}

export function useTaskPolling(taskId: string | null, options: UseTaskPollingOptions) {
  const [status, setStatus] = useState<TaskStatusResponse | null>(null);
  const optionsRef = useRef(options);
  optionsRef.current = options;

  useEffect(() => {
    if (!taskId) {
      setStatus(null);
      return;
    }

    let cancelled = false;

    const interval = setInterval(async () => {
      try {
        const taskStatus = await fetchTaskStatus(taskId);
        if (cancelled) return;
        setStatus(taskStatus);

        if (taskStatus.status === 'completed') {
          clearInterval(interval);
          optionsRef.current.onComplete(taskStatus.result);
        } else if (taskStatus.status === 'failed') {
          clearInterval(interval);
          optionsRef.current.onError(taskStatus.error ?? '任务执行失败');
        } else if (taskStatus.progress) {
          optionsRef.current.onProgress?.(taskStatus.progress);
        }
      } catch {
        // 网络错误时不停止轮询，继续重试
      }
    }, 2000); // per D-03: 固定 2 秒间隔

    return () => {
      cancelled = true;
      clearInterval(interval);
    };
  }, [taskId]);

  return status;
}
```

关键设计点：
- `optionsRef` 避免 options 对象变化导致 effect 重新执行
- `cancelled` flag 防止组件卸载后 setState
- 网络错误时不停止轮询（短暂网络波动不应中断）
- 固定 2 秒间隔（per D-03，不使用递增退避）

  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust/frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/types/api.ts 包含 `interface TaskTriggerResponse` 和 `interface TaskStatusResponse`
    - frontend/src/services/taskService.ts 包含 `export async function fetchTaskStatus(taskId: string)`
    - frontend/src/hooks/useTaskPolling.ts 包含 `export function useTaskPolling(taskId: string | null`
    - frontend/src/hooks/useTaskPolling.ts 包含 `setInterval` 和 `2000`
    - frontend/src/hooks/useTaskPolling.ts 包含 `clearInterval(interval)` 在 cleanup 中
    - `npx tsc --noEmit` 退出码为 0
  </acceptance_criteria>
  <done>taskService、useTaskPolling hook 和 TypeScript 类型创建完成，tsc 编译通过</done>
</task>

<task type="auto">
  <name>Task 2: 改造 evaluationService + EvaluationDetail 页面集成轮询</name>
  <files>
    frontend/src/services/evaluationService.ts,
    frontend/src/pages/EvaluationDetail.tsx
  </files>
  <read_first>
    frontend/src/services/evaluationService.ts,
    frontend/src/pages/EvaluationDetail.tsx,
    frontend/src/hooks/useTaskPolling.ts,
    frontend/src/types/api.ts
  </read_first>
  <action>

**1. 修改 `frontend/src/services/evaluationService.ts`** (per D-04)

- 修改 `generateEvaluation` 和 `regenerateEvaluation` 返回类型：
  ```typescript
  import type { EvaluationRecord, TaskTriggerResponse } from '../types/api';

  export async function generateEvaluation(submissionId: string): Promise<TaskTriggerResponse> {
    const response = await api.post<TaskTriggerResponse>('/evaluations/generate', { submission_id: submissionId });
    return response.data;
  }

  export async function regenerateEvaluation(submissionId: string): Promise<TaskTriggerResponse> {
    const response = await api.post<TaskTriggerResponse>('/evaluations/regenerate', { submission_id: submissionId });
    return response.data;
  }
  ```
- 移除 `LONG_RUNNING_TIMEOUT` 常量（不再需要 120 秒超时）
- 移除 `{ timeout: LONG_RUNNING_TIMEOUT }` 参数
- 保持其他函数不变（fetchEvaluation, fetchEvaluationBySubmission, submitManualReview, submitHrReview, confirmEvaluation）

**2. 修改 `frontend/src/pages/EvaluationDetail.tsx`**

这是一个大文件（~2100 行），需要精准修改以下部分：

**新增导入：**
```typescript
import { useTaskPolling } from '../hooks/useTaskPolling';
```

**新增状态变量**（在组件函数开头的 useState 区域）：
```typescript
const [evaluationTaskId, setEvaluationTaskId] = useState<string | null>(null);
```

**添加 useTaskPolling 调用**（在 useEffect hooks 区域）：

```typescript
useTaskPolling(evaluationTaskId, {
  onComplete: (result) => {
    // result 是后端返回的 evaluation dict (per D-06)
    setEvaluation(result as EvaluationRecord);
    setDimensions(mapEvaluationToDrafts(result as EvaluationRecord));
    setReviewLevel((result as EvaluationRecord).ai_level);
    setReviewComment(localizeEvaluationNarrative((result as EvaluationRecord).explanation));
    setSalaryRecommendation(null);
    setEvaluationTaskId(null);
    setIsGeneratingEvaluation(false);
    void reloadCurrentCycleData();
    setSuccessMessage(evaluation ? 'AI 评分已按最新材料重新生成，可以继续主管复核。' : 'AI 评分已生成，可以继续主管复核。');
  },
  onError: (error) => {
    setEvaluationTaskId(null);
    setIsGeneratingEvaluation(false);
    setErrorMessage(error);
  },
});
```

**修改 `handleGenerateEvaluation` 函数**（约在第 1297 行附近）：

替换原有的同步调用：
```typescript
// 替换前:
// const nextEvaluation = evaluation ? await regenerateEvaluation(submission.id) : await generateEvaluation(submission.id);
// setEvaluation(nextEvaluation);
// ... 后续状态更新 ...

// 替换后:
const triggerResponse = evaluation
  ? await regenerateEvaluation(submission.id)
  : await generateEvaluation(submission.id);
setEvaluationTaskId(triggerResponse.task_id);
// 不再在这里 setIsGeneratingEvaluation(false)，轮询完成时才设置
```

修改 try/catch/finally 结构：
- `try` 块中：保留 `setIsGeneratingEvaluation(true)` 和 batch parse 逻辑
- `try` 块中：在触发 task 后只设置 taskId，不再设置评估结果
- `catch` 块中：保留错误处理
- `finally` 块中：移除 `setIsGeneratingEvaluation(false)`（改由轮询 onComplete/onError 设置）
- 保留 `setIsParsingAll(false)` 在 finally 中

**修改 UI 中的评估按钮区域**（per D-01）：

在评估按钮旁添加状态文字显示。找到 `handleGenerateEvaluation` 按钮（约第 1661 行），在按钮文字后或下方添加：
```tsx
{isGeneratingEvaluation && (
  <span className="ml-2 text-sm text-gray-500 animate-pulse">AI 评估中...</span>
)}
```

注意：按钮已有 `disabled={isGeneratingEvaluation || ...}` 条件，在任务执行期间按钮已禁用。

  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust/frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/services/evaluationService.ts 中 `generateEvaluation` 返回类型为 `Promise<TaskTriggerResponse>`
    - frontend/src/services/evaluationService.ts 中不再包含 `LONG_RUNNING_TIMEOUT`
    - frontend/src/pages/EvaluationDetail.tsx 包含 `import { useTaskPolling }` 
    - frontend/src/pages/EvaluationDetail.tsx 包含 `evaluationTaskId` 状态变量
    - frontend/src/pages/EvaluationDetail.tsx 包含 `useTaskPolling(evaluationTaskId`
    - frontend/src/pages/EvaluationDetail.tsx 包含 `setEvaluationTaskId(triggerResponse.task_id)`
    - frontend/src/pages/EvaluationDetail.tsx 包含 `AI 评估中...`
    - `npx tsc --noEmit` 退出码为 0
  </acceptance_criteria>
  <done>evaluationService 返回 task_id，EvaluationDetail 通过轮询获取评估结果，评估期间显示 "AI 评估中..." 状态文字</done>
</task>

<task type="auto">
  <name>Task 3: 改造 importService + ImportCenter 页面集成轮询</name>
  <files>
    frontend/src/services/importService.ts,
    frontend/src/pages/ImportCenter.tsx
  </files>
  <read_first>
    frontend/src/services/importService.ts,
    frontend/src/pages/ImportCenter.tsx,
    frontend/src/hooks/useTaskPolling.ts,
    frontend/src/types/api.ts
  </read_first>
  <action>

**1. 修改 `frontend/src/services/importService.ts`**

- 新增类型导入：
  ```typescript
  import type { ImportJobListResponse, ImportJobRecord, TaskTriggerResponse } from '../types/api';
  ```
- 修改 `createImportJob` 返回类型为 `TaskTriggerResponse`：
  ```typescript
  export async function createImportJob(importType: string, file: File): Promise<TaskTriggerResponse> {
    const formData = new FormData();
    formData.append('file', file);
    const response = await api.post<TaskTriggerResponse>(
      `/imports/jobs?import_type=${encodeURIComponent(importType)}`,
      formData,
      { headers: { 'Content-Type': 'multipart/form-data' } },
    );
    return response.data;
  }
  ```
- 保持其他函数不变

**2. 修改 `frontend/src/pages/ImportCenter.tsx`**

**新增导入：**
```typescript
import { useTaskPolling } from '../hooks/useTaskPolling';
import type { ImportJobRecord as ImportResult } from '../types/api';
```

**新增状态变量：**
```typescript
const [importTaskId, setImportTaskId] = useState<string | null>(null);
const [importProgress, setImportProgress] = useState<{ processed: number; total: number; errors: number } | null>(null);
```

**添加 useTaskPolling 调用：**
```typescript
useTaskPolling(importTaskId, {
  onComplete: (result) => {
    setImportTaskId(null);
    setImportProgress(null);
    setLastImportResult(result as ImportJobRecord);
    setIsUploading(false);
    void loadJobs();
  },
  onError: (error) => {
    setImportTaskId(null);
    setImportProgress(null);
    setIsUploading(false);
    setErrorMessage(error);
  },
  onProgress: (progress) => {
    setImportProgress(progress);
  },
});
```

**修改 `handleUpload` 函数**（约第 82 行）：

替换 try 块中的逻辑：
```typescript
async function handleUpload() {
  if (!selectedFile) {
    setErrorMessage('请先选择需要上传的文件。');
    return;
  }
  setIsUploading(true);
  setErrorMessage(null);
  setLastImportResult(null);
  setImportProgress(null);
  try {
    const triggerResponse = await createImportJob(selectedType, selectedFile);
    setImportTaskId(triggerResponse.task_id);
    setSelectedFile(null);
    const input = window.document.getElementById('import-file-input') as HTMLInputElement | null;
    if (input) {
      input.value = '';
    }
    // 不再在这里 setIsUploading(false) 和 loadJobs()
    // 轮询 onComplete 会处理
  } catch (error) {
    setErrorMessage(resolveError(error));
    setIsUploading(false);
  }
  // 移除 finally 块中的 setIsUploading(false)
}
```

**添加进度显示 UI**（per D-01, D-08）：

在上传按钮附近（约第 193 行），添加进度文字：
```tsx
{isUploading && (
  <div className="text-sm text-gray-500 animate-pulse mt-2">
    {importProgress
      ? `导入中 ${importProgress.processed}/${importProgress.total} 行${importProgress.errors > 0 ? `（${importProgress.errors} 行失败）` : ''}`
      : '导入中...'}
  </div>
)}
```

此文字在 isUploading 为 true 时显示，内容根据是否有 progress 数据动态切换：
- 无 progress 时：显示 "导入中..."
- 有 progress 时：显示 "导入中 45/120 行" 或 "导入中 45/120 行（3 行失败）"

  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust/frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/services/importService.ts 中 `createImportJob` 返回类型为 `Promise<TaskTriggerResponse>`
    - frontend/src/pages/ImportCenter.tsx 包含 `import { useTaskPolling }`
    - frontend/src/pages/ImportCenter.tsx 包含 `importTaskId` 和 `importProgress` 状态变量
    - frontend/src/pages/ImportCenter.tsx 包含 `useTaskPolling(importTaskId`
    - frontend/src/pages/ImportCenter.tsx 包含 `setImportTaskId(triggerResponse.task_id)`
    - frontend/src/pages/ImportCenter.tsx 包含 `导入中` 进度文字
    - `npx tsc --noEmit` 退出码为 0
  </acceptance_criteria>
  <done>importService 返回 task_id，ImportCenter 通过轮询获取导入进度和结果，导入期间显示 "导入中 X/Y 行" 进度文字</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> /tasks/{task_id} | 前端轮询后端任务状态 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-09 | Information Disclosure | useTaskPolling | mitigate | 后端轮询端点已有 get_current_user 认证；前端仅存储和查询当前用户触发的 task_id |
| T-22-10 | Denial of Service | setInterval 2s | accept | 固定 2 秒间隔对后端压力极小（企业内部用户量级），且组件卸载后自动清理 |
</threat_model>

<verification>
1. `cd frontend && npx tsc --noEmit` TypeScript 编译通过
2. `grep -r 'useTaskPolling' frontend/src/pages/` 返回 EvaluationDetail.tsx 和 ImportCenter.tsx
3. `grep 'LONG_RUNNING_TIMEOUT' frontend/src/services/evaluationService.ts` 无匹配（已移除）
4. `grep 'TaskTriggerResponse' frontend/src/services/evaluationService.ts frontend/src/services/importService.ts` 均有匹配
</verification>

<success_criteria>
- evaluationService 和 importService 返回 TaskTriggerResponse（task_id + status）
- EvaluationDetail 在评估期间显示 "AI 评估中..." + Spinner (per D-01)
- ImportCenter 在导入期间显示 "导入中 X/Y 行" (per D-01, D-08)
- 任务完成后自动刷新数据，无 Toast 通知 (per D-02)
- 轮询固定 2 秒间隔 (per D-03)
- TypeScript 编译通过
</success_criteria>

<output>
After completion, create `.planning/phases/22-ai/22-03-SUMMARY.md`
</output>
