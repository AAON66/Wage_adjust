---
phase: 23-eligibility-import
plan: 03
type: execute
wave: 2
depends_on: [23-01, 23-02]
files_modified:
  - frontend/src/pages/EligibilityManagementPage.tsx
  - frontend/src/components/eligibility-import/ImportTabContent.tsx
  - frontend/src/components/eligibility-import/ExcelImportPanel.tsx
  - frontend/src/components/eligibility-import/FeishuSyncPanel.tsx
  - frontend/src/components/eligibility-import/FeishuFieldMapper.tsx
  - frontend/src/services/eligibilityImportService.ts
  - frontend/src/utils/roleAccess.ts
autonomous: false
requirements: [ELIGIMP-01, ELIGIMP-03, ELIGIMP-04, FEISHU-01]

must_haves:
  truths:
    - "HR 可在调薪资格管理页面看到 6 个 Tab（原 2 + 新 4）"
    - "每个新 Tab 可通过 Excel 上传文件触发导入"
    - "每个新 Tab 可输入飞书多维表格 URL，获取字段列表并拖拽映射"
    - "拖拽映射建立连接后显示 SVG 连线"
    - "导入/同步完成后显示成功/失败/跳过统计和错误明细"
    - "侧边栏菜单包含调薪资格管理入口"
  artifacts:
    - path: "frontend/src/pages/EligibilityManagementPage.tsx"
      provides: "扩展后的 6 Tab 页面"
      contains: "performance_grades"
    - path: "frontend/src/components/eligibility-import/ImportTabContent.tsx"
      provides: "单个导入 Tab 内容组件"
      contains: "ExcelImportPanel"
    - path: "frontend/src/components/eligibility-import/ExcelImportPanel.tsx"
      provides: "Excel 文件上传面板"
      contains: "drop zone"
    - path: "frontend/src/components/eligibility-import/FeishuSyncPanel.tsx"
      provides: "飞书同步控制面板"
      contains: "FeishuFieldMapper"
    - path: "frontend/src/components/eligibility-import/FeishuFieldMapper.tsx"
      provides: "拖拽连线字段映射组件"
      contains: "SVG"
    - path: "frontend/src/services/eligibilityImportService.ts"
      provides: "API 调用服务"
      contains: "parsebitableUrl"
  key_links:
    - from: "EligibilityManagementPage.tsx"
      to: "ImportTabContent.tsx"
      via: "Tab 切换渲染"
      pattern: "ImportTabContent"
    - from: "ImportTabContent.tsx"
      to: "eligibilityImportService.ts"
      via: "API 调用"
      pattern: "eligibilityImportService"
    - from: "FeishuSyncPanel.tsx"
      to: "FeishuFieldMapper.tsx"
      via: "字段映射子组件"
      pattern: "FeishuFieldMapper"
---

<objective>
实现 Phase 23 完整前端：扩展 EligibilityManagementPage 为 6 Tab，每个新 Tab 包含 Excel 导入面板和飞书同步面板（含拖拽连线字段映射），导入结果复用 ImportResultPanel 展示。

Purpose: 让 HR 可在统一界面管理 4 类调薪资格数据的导入
Output: 完整的前端页面、组件、服务层
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-eligibility-import/23-CONTEXT.md
@.planning/phases/23-eligibility-import/23-RESEARCH.md
@.planning/phases/23-eligibility-import/23-UI-SPEC.md
@.planning/phases/23-eligibility-import/23-01-SUMMARY.md
@.planning/phases/23-eligibility-import/23-02-SUMMARY.md

<interfaces>
<!-- Plan 01/02 创建的 API 端点 -->

Backend API endpoints (from Plan 02):
```
POST   /api/v1/eligibility-import/excel           — Excel 文件导入，返回 TaskTriggerResponse {task_id, status}
POST   /api/v1/eligibility-import/feishu/parse-url — 解析飞书 URL，返回 {app_token, table_id}
POST   /api/v1/eligibility-import/feishu/fields    — 获取字段列表，返回 {fields: [{field_id, field_name, type, ui_type}]}
POST   /api/v1/eligibility-import/feishu/sync      — 触发飞书同步，返回 TaskTriggerResponse {task_id, status}
GET    /api/v1/eligibility-import/templates/{type}  — 下载 xlsx 模板
```

Import types: performance_grades, salary_adjustments, hire_info, non_statutory_leave

From frontend/src/hooks/useTaskPolling.ts:
```typescript
export function useTaskPolling(taskId: string | null, options?: { intervalMs?: number }): {
  status: string | null;
  progress: { processed: number; total: number; errors: number } | null;
  result: unknown;
  error: string | null;
  isPolling: boolean;
}
```

From frontend/src/services/api.ts:
```typescript
const api: AxiosInstance  // 已配置 JWT interceptor
```

From frontend/src/components/import/ImportResultPanel.tsx:
```typescript
interface ImportResultPanelProps {
  result: ImportJobResult | null;
  onExport?: () => void;
}
```

From frontend/src/pages/EligibilityManagementPage.tsx:
```typescript
type TabKey = 'list' | 'overrides';
const TABS: Array<{ key: TabKey; label: string }> = [
  { key: 'list', label: '调薪资格' },
  { key: 'overrides', label: '特殊申请' },
];
```

From frontend/src/utils/roleAccess.ts (sidebar module):
```typescript
// HR/admin groups contain module items with { title, description, href, icon }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: API 服务层 + 页面 Tab 扩展 + Excel 导入面板</name>
  <files>
    frontend/src/services/eligibilityImportService.ts
    frontend/src/pages/EligibilityManagementPage.tsx
    frontend/src/components/eligibility-import/ExcelImportPanel.tsx
    frontend/src/components/eligibility-import/ImportTabContent.tsx
  </files>
  <read_first>
    frontend/src/services/api.ts
    frontend/src/services/importService.ts
    frontend/src/pages/EligibilityManagementPage.tsx
    frontend/src/components/import/ImportResultPanel.tsx
    frontend/src/components/import/ImportErrorTable.tsx
    frontend/src/hooks/useTaskPolling.ts
    frontend/src/types/api.ts
    .planning/phases/23-eligibility-import/23-UI-SPEC.md
  </read_first>
  <action>
    1. **创建 eligibilityImportService.ts** (`frontend/src/services/eligibilityImportService.ts`):
       ```typescript
       import api from './api';

       export type EligibilityImportType = 'performance_grades' | 'salary_adjustments' | 'hire_info' | 'non_statutory_leave';

       export interface FeishuFieldInfo {
         field_id: string;
         field_name: string;
         type: number | null;
         ui_type: string | null;
       }

       export async function uploadEligibilityExcel(importType: EligibilityImportType, file: File) {
         const formData = new FormData();
         formData.append('file', file);
         return api.post(`/eligibility-import/excel?import_type=${importType}`, formData);
       }

       export async function parseBitableUrl(url: string) {
         return api.post<{ app_token: string; table_id: string }>('/eligibility-import/feishu/parse-url', { url });
       }

       export async function fetchBitableFields(appToken: string, tableId: string) {
         return api.post<{ fields: FeishuFieldInfo[] }>('/eligibility-import/feishu/fields', { app_token: appToken, table_id: tableId });
       }

       export async function triggerFeishuSync(syncType: EligibilityImportType, appToken: string, tableId: string, fieldMapping: Record<string, string>) {
         return api.post('/eligibility-import/feishu/sync', { sync_type: syncType, app_token: appToken, table_id: tableId, field_mapping: fieldMapping });
       }

       export function getTemplateUrl(importType: EligibilityImportType): string {
         return `/api/v1/eligibility-import/templates/${importType}?format=xlsx`;
       }
       ```

    2. **创建 ExcelImportPanel.tsx** (`frontend/src/components/eligibility-import/ExcelImportPanel.tsx`):
       - Props: `{ importType: EligibilityImportType; label: string }`
       - 遵循 UI-SPEC 中的 File Drop Zone 设计:
         - `border: 2px dashed var(--color-border)`, `border-radius: 8px`, `padding: 32px 24px`
         - 支持拖拽文件和点击选择，只接受 `.xlsx/.xls` 文件
         - Drag-over 状态: `border-color: var(--color-primary)`, `background: var(--color-primary-light)`
         - 文件选中后显示文件名 + 大小，带 [x] 清除按钮
       - 包含 "下载模板" 按钮（`.action-secondary`）: 直接链接到 `getTemplateUrl(importType)`
       - 包含 "开始导入" 按钮（`.action-primary`）:
         - 未选文件时 disabled
         - 点击后调用 `uploadEligibilityExcel(importType, file)`
         - 获得 task_id 后通过 `useTaskPolling` 轮询进度
         - 进度中显示 "正在导入数据... 已处理 X/Y 条"
       - 导入完成后通过 `onResult` 回调传递结果给父组件
       - 文件上传区域使用 `<input type="file" accept=".xlsx,.xls" hidden />` + ref
       - role="button" tabIndex={0} 用于无障碍

    3. **创建 ImportTabContent.tsx** (`frontend/src/components/eligibility-import/ImportTabContent.tsx`):
       - Props: `{ importType: EligibilityImportType; label: string }`
       - 垂直布局（上下分区，per UI-SPEC）:
         - 上方: `ExcelImportPanel` (包裹在 `.surface` 卡片中)
         - 中间: `FeishuSyncPanel` (包裹在 `.surface` 卡片中) — 此 task 先放占位，Task 2 实现
         - 下方: `ImportResultPanel` (条件渲染，导入/同步完成后显示)
       - 间距 16px（per UI-SPEC spacing md）
       - 管理 importResult 状态，Excel 和飞书都可更新它
       - 包含 "导出错误报告" 按钮（`.chip-button`，per D-10）

    4. **扩展 EligibilityManagementPage.tsx**:
       - 扩展 TabKey 类型: `'list' | 'overrides' | 'performance_grades' | 'salary_adjustments' | 'hire_info' | 'non_statutory_leave'`
       - TABS 数组添加 4 个新 Tab (per D-01, UI-SPEC Copywriting):
         - `{ key: 'performance_grades', label: '绩效等级' }`
         - `{ key: 'salary_adjustments', label: '调薪历史' }`
         - `{ key: 'hire_info', label: '入职信息' }`
         - `{ key: 'non_statutory_leave', label: '非法定假期' }`
       - 新 Tab 渲染 `<ImportTabContent importType={activeTab} label={当前tab label} />`
       - Tab 导航保持现有样式（per UI-SPEC Tab Navigation states）
       - 导入 ImportTabContent 组件

    所有组件使用项目已有的 CSS 类（.surface, .action-primary, .action-secondary, .chip-button, .eyebrow, .section-title）。不引入任何新 npm 依赖。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust/frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep "performance_grades" frontend/src/pages/EligibilityManagementPage.tsx` 匹配
    - `grep "salary_adjustments" frontend/src/pages/EligibilityManagementPage.tsx` 匹配
    - `grep "hire_info" frontend/src/pages/EligibilityManagementPage.tsx` 匹配
    - `grep "non_statutory_leave" frontend/src/pages/EligibilityManagementPage.tsx` 匹配
    - `grep "ImportTabContent" frontend/src/pages/EligibilityManagementPage.tsx` 匹配
    - `grep "ExcelImportPanel" frontend/src/components/eligibility-import/ImportTabContent.tsx` 匹配
    - `grep "uploadEligibilityExcel" frontend/src/services/eligibilityImportService.ts` 匹配
    - `grep "parseBitableUrl" frontend/src/services/eligibilityImportService.ts` 匹配
    - `grep "useTaskPolling" frontend/src/components/eligibility-import/ExcelImportPanel.tsx` 匹配
    - tsc --noEmit 通过
  </acceptance_criteria>
  <done>
    6 Tab 页面可切换，Excel 导入面板功能完整（文件拖拽、上传、进度、结果），API 服务层全部 typed
  </done>
</task>

<task type="auto">
  <name>Task 2: 飞书同步面板 + 拖拽连线字段映射组件 + 侧边栏入口</name>
  <files>
    frontend/src/components/eligibility-import/FeishuFieldMapper.tsx
    frontend/src/components/eligibility-import/FeishuSyncPanel.tsx
    frontend/src/components/eligibility-import/ImportTabContent.tsx
    frontend/src/utils/roleAccess.ts
  </files>
  <read_first>
    frontend/src/components/eligibility-import/ImportTabContent.tsx
    frontend/src/services/eligibilityImportService.ts
    frontend/src/hooks/useTaskPolling.ts
    frontend/src/utils/roleAccess.ts
    frontend/src/components/layout/AppShell.tsx
    .planning/phases/23-eligibility-import/23-UI-SPEC.md
  </read_first>
  <action>
    1. **创建 FeishuFieldMapper.tsx** (`frontend/src/components/eligibility-import/FeishuFieldMapper.tsx`):
       - Props:
         ```typescript
         interface FeishuFieldMapperProps {
           feishuFields: FeishuFieldInfo[];
           systemFields: string[];
           connections: Array<{ feishuField: string; systemField: string }>;
           onConnectionsChange: (connections: Array<{ feishuField: string; systemField: string }>) => void;
         }
         ```
       - **布局** (per UI-SPEC FeishuFieldMapper Layout):
         - 外层容器 `position: relative`，设置 `ref` 用于 SVG 坐标计算
         - 左栏 (48%): 标题 "飞书字段"，列出 feishuFields，每个 item `draggable="true"`
         - 右栏 (48%): 标题 "系统字段"，列出 systemFields，每个 item 是 drop target
         - 中间: SVG overlay (`absolute, inset-0, pointer-events-none`)

       - **拖拽交互** (per UI-SPEC Drag States, D-06):
         - 左栏 item: `onDragStart` 设置 `e.dataTransfer.setData('text/plain', fieldName)`，拖拽时 opacity: 0.5
         - 右栏 item: `onDragOver(e.preventDefault())`, `onDrop` 读取 field name 并创建 connection
         - 已连接的 item: 背景 `var(--color-primary-light)`, 边框 `var(--color-primary-border)`
         - Hover 已连接 item: 显示 "x" 移除图标
         - 点击 "x" 移除该连接

       - **SVG 连线**:
         - 使用 `getBoundingClientRect()` 计算左栏 item 右边缘到右栏 item 左边缘的坐标
         - 相对于容器 ref 计算偏移
         - `<line>` stroke: `var(--color-primary)`, strokeWidth: 2
         - 使用 `useRef` + `useEffect` + `ResizeObserver` 在窗口变化时重算坐标
         - 将坐标存储在 state 中，避免每次 render 都 query DOM

       - **键盘无障碍** (per UI-SPEC Accessibility):
         - 每个左栏 item 添加 "连接到..." 按钮，点击展开下拉选择可用的右栏字段
         - 已连接 item 添加 `aria-label="{feishuField} 已映射到 {systemField}"`

       - **底部状态栏**: "已建立 N 个映射关系" + "清除全部映射" 按钮（`.chip-button`）
         - 清除按钮使用行内确认: 点击后文本变为 "确认清除？"，3 秒后恢复，再次点击才执行清除

       - 系统字段名称映射（中文显示）:
         ```typescript
         const SYSTEM_FIELD_LABELS: Record<string, string> = {
           employee_no: '员工工号', year: '年度', grade: '绩效等级',
           adjustment_date: '调薪日期', adjustment_type: '调薪类型', amount: '调薪金额',
           hire_date: '入职日期', total_days: '假期天数', leave_type: '假期类型',
         };
         ```

    2. **创建 FeishuSyncPanel.tsx** (`frontend/src/components/eligibility-import/FeishuSyncPanel.tsx`):
       - Props: `{ importType: EligibilityImportType; label: string; onResult: (result: any) => void }`
       - **流程** (per UI-SPEC Feishu Sync Flow):
         1. URL 输入: `.toolbar-input` 全宽，"获取字段" 按钮（`.action-secondary`）
         2. 点击 "获取字段":
            - 调用 `parseBitableUrl(url)` 获取 app_token + table_id
            - 解析失败显示错误提示（per UI-SPEC Copywriting: "无法解析多维表格 URL..."）
            - 解析成功后调用 `fetchBitableFields(appToken, tableId)`
            - 获取中显示 skeleton placeholder（3 行灰条）
         3. 字段加载后: 渲染 `FeishuFieldMapper`
            - 左栏: 飞书返回的字段列表
            - 右栏: 根据 importType 确定系统字段列表:
              - performance_grades: ['employee_no', 'year', 'grade']
              - salary_adjustments: ['employee_no', 'adjustment_date', 'adjustment_type', 'amount']
              - hire_info: ['employee_no', 'hire_date']
              - non_statutory_leave: ['employee_no', 'year', 'total_days', 'leave_type']
         4. "开始同步" 按钮（`.action-primary`）:
            - 无映射时 disabled，显示 "请先建立至少一个字段映射关系..."
            - 点击后调用 `triggerFeishuSync(importType, appToken, tableId, fieldMapping)`
            - 获得 task_id 后通过 `useTaskPolling` 轮询进度
            - 进度中显示 "正在同步飞书数据... 已处理 X/Y 条"
         5. 同步完成后通过 `onResult` 回调传递结果

    3. **更新 ImportTabContent.tsx**:
       - 将 Task 1 中的占位替换为真正的 `FeishuSyncPanel` 组件
       - 确保 Excel 和飞书的结果都可以更新同一个 ImportResultPanel
       - 飞书同步区域包裹在 `.surface` 卡片中，带 `.eyebrow` "飞书多维表格同步" 和 `.section-title` "从飞书多维表格同步{label}数据"

    4. **更新 roleAccess.ts** (per D-03):
       - 在 admin 和 hrbp 的 module groups 中确认 "调薪资格" 已有菜单入口指向 `/eligibility`
       - 如果已存在（当前 grep 显示有 href='/eligibility'），则无需修改
       - 更新 description 为 "查看资格状态、特殊申请与数据导入管理。" 以反映新功能
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust/frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep "FeishuFieldMapper" frontend/src/components/eligibility-import/FeishuFieldMapper.tsx` 匹配
    - `grep "SVG" frontend/src/components/eligibility-import/FeishuFieldMapper.tsx` 匹配（大小写不敏感）
    - `grep "draggable" frontend/src/components/eligibility-import/FeishuFieldMapper.tsx` 匹配
    - `grep "getBoundingClientRect" frontend/src/components/eligibility-import/FeishuFieldMapper.tsx` 匹配
    - `grep "ResizeObserver" frontend/src/components/eligibility-import/FeishuFieldMapper.tsx` 匹配
    - `grep "FeishuSyncPanel" frontend/src/components/eligibility-import/FeishuSyncPanel.tsx` 匹配
    - `grep "parseBitableUrl" frontend/src/components/eligibility-import/FeishuSyncPanel.tsx` 匹配
    - `grep "fetchBitableFields" frontend/src/components/eligibility-import/FeishuSyncPanel.tsx` 匹配
    - `grep "triggerFeishuSync" frontend/src/components/eligibility-import/FeishuSyncPanel.tsx` 匹配
    - `grep "FeishuSyncPanel" frontend/src/components/eligibility-import/ImportTabContent.tsx` 匹配
    - tsc --noEmit 通过
  </acceptance_criteria>
  <done>
    飞书同步面板完整：URL 输入 → 字段获取 → 拖拽映射 → 同步触发 → 结果展示。FeishuFieldMapper 支持拖拽连线 + SVG 绘制 + 键盘无障碍。侧边栏菜单入口完整。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 完整功能验证</name>
  <files>frontend/src/pages/EligibilityManagementPage.tsx</files>
  <what-built>
    调薪资格统一导入管理完整前端：6 Tab 页面、Excel 文件上传导入、飞书多维表格字段映射（拖拽连线 UI）、同步触发与进度跟踪、导入结果统计展示。
  </what-built>
  <action>人工验证 Task 1 和 Task 2 构建的完整前端功能，确认所有交互流程正常。</action>
  <how-to-verify>
    1. 启动后端: `cd /Users/mac/PycharmProjects/Wage_adjust && uvicorn backend.app.main:app --reload`
    2. 启动前端: `cd /Users/mac/PycharmProjects/Wage_adjust/frontend && npm run dev`
    3. 以 admin 或 hrbp 角色登录

    **Tab 切换验证 (ELIGIMP-01):**
    4. 导航到 /eligibility 页面
    5. 验证页面显示 6 个 Tab: 调薪资格、特殊申请、绩效等级、调薪历史、入职信息、非法定假期
    6. 点击每个 Tab 确认内容区域切换正常

    **Excel 导入验证 (ELIGIMP-02):**
    7. 选择 "绩效等级" Tab
    8. 点击 "下载模板" 按钮，确认下载 xlsx 文件
    9. 将文件拖拽到上传区域或点击选择文件
    10. 确认文件名和大小显示，"开始导入" 按钮变为可点击
    11. 点击 "开始导入"，确认进度显示

    **飞书字段映射验证 (ELIGIMP-03, D-06):**
    12. 在飞书同步区域输入一个多维表格 URL
    13. 点击 "获取字段"，确认字段列表加载
    14. 从左侧拖拽一个飞书字段到右侧系统字段，确认:
        - SVG 连线正确绘制
        - 已连接字段高亮显示
        - hover 显示 "x" 删除按钮
    15. 验证 "已建立 N 个映射关系" 状态文本
    16. 验证 "清除全部映射" 确认流程

    **导入结果验证 (ELIGIMP-04):**
    17. 导入完成后验证显示成功/失败/跳过统计卡片
    18. 如有失败行，验证错误明细表格可展开

    **响应式验证:**
    19. 缩小浏览器窗口，确认拖拽连线位置重新计算正确
  </how-to-verify>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/Wage_adjust/frontend && npx tsc --noEmit</automated>
  </verify>
  <done>人工确认 6 Tab 切换、Excel 导入、飞书拖拽映射、同步、结果展示全部正常</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户文件上传 → 前端 | 前端限制 .xlsx/.xls 后缀，后端做实际验证 |
| 用户 URL 输入 → API | 前端传递到后端校验，不在前端直接调用飞书 API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-10 | Tampering | ExcelImportPanel file input | mitigate | accept 属性限制 .xlsx/.xls，后端 ImportService 二次验证 |
| T-23-11 | Information Disclosure | FeishuSyncPanel | mitigate | 飞书凭证不传前端，所有 API 调用通过后端转发 |
| T-23-12 | Spoofing | FeishuSyncPanel URL input | mitigate | 后端 parse_bitable_url 正则限制域名 |
</threat_model>

<verification>
1. `cd frontend && npx tsc --noEmit` 通过
2. 6 个 Tab 可切换
3. Excel 导入流程完整（选文件 → 上传 → 进度 → 结果）
4. 飞书映射流程完整（URL → 字段 → 拖拽 → 同步 → 结果）
5. SVG 连线渲染正确且窗口 resize 后重算
</verification>

<success_criteria>
- EligibilityManagementPage 显示 6 个 Tab
- 每个新 Tab 包含 Excel 导入面板 + 飞书同步面板
- FeishuFieldMapper 拖拽连线功能完整
- 导入结果复用 ImportResultPanel 展示
- tsc --noEmit 通过
- 人工验证通过
</success_criteria>

<output>
After completion, create `.planning/phases/23-eligibility-import/23-03-SUMMARY.md`
</output>
