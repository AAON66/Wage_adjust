## 2026-03-20 - Task: 项目基础配置

### What was done:
- 创建了 `backend/app/utils`、`backend/tests`、`uploads` 基础目录。
- 新增 `requirements.txt`，整理后端基础依赖清单。
- 新增 `.env.example`，补齐数据库、鉴权、对象存储、DeepSeek、Redis 与公共 API 配置模板。
- 新增 `backend/app/utils/helpers.py` 及必要的 `__init__.py`，提供时间、UUID、字典清理和金额四舍五入工具函数。
- 创建 `.venv` 虚拟环境并在受限环境下完成 `pip` 初始化。
- 安装 `requirements.txt` 中全部后端依赖。

### Testing:
- 已检查本机运行时入口：`py --version`、`node --version`、`cmd /c npm.cmd --version`。
- 已验证 `.venv` 中 `pip` 可用：`.\.venv\Scripts\python.exe -m pip --version`。
- 已验证工具模块可导入并运行：`.\.venv\Scripts\python.exe -c "from backend.app.utils.helpers import ..."`。
- 当前尚无法启动后端和前端开发服务器，因为 `backend/app/main.py` 与 `frontend/` 项目仍未实现，属于后续任务范围。

### Notes:
- 当前目录初始状态仅包含需求文档，尚未存在 FastAPI 或 React 可启动工程。
- PowerShell 直接执行 `npm` 会受执行策略限制，需使用 `npm.cmd`。
- PowerShell 中 `python` 不在 PATH，需要使用 `py`。
- 受沙箱影响，`venv` 和 `pip install` 需要在工作区内指定临时目录并放开权限执行。

## 2026-03-20 - Task: 数据库连接和配置

### What was done:
- 新增 `backend/app/core/config.py`，使用 `pydantic-settings` 建立统一配置管理和缓存读取入口。
- 新增 `backend/app/core/database.py`，实现 SQLAlchemy 引擎创建、连接池配置、`SessionLocal` 会话工厂、`Base` 声明基类和 `init_database()`。
- 新增 `backend/app/dependencies.py`，提供 `get_db()` 和 `get_app_settings()` 依赖注入函数。
- 新增 `backend/app/core/init_db.py`，提供数据库初始化脚本入口。
- 新增 `backend/app/models/__init__.py`，补充模型模块自动加载钩子，方便后续 schema 扩展。
- 新增 `backend/tests/test_core/test_config.py` 和 `backend/tests/test_core/test_database.py`，覆盖配置解析、SQLite 引擎创建、建表初始化和数据库依赖注入清理。
- 补充 `pytest` 到 `requirements.txt`，用于执行项目标准测试命令。

### Testing:
- `.\.venv\Scripts\python.exe -m pytest backend\tests\test_core -q -p no:cacheprovider`
- `.\.venv\Scripts\python.exe -m py_compile backend\app\core\config.py backend\app\core\database.py backend\app\core\init_db.py backend\app\dependencies.py backend\app\models\__init__.py backend\tests\test_core\test_config.py backend\tests\test_core\test_database.py`
- `.\.venv\Scripts\python.exe -m backend.app.core.init_db`

### Notes:
- 当前环境对系统临时目录和默认 pytest cache 目录有限制，测试时需避免依赖系统 Temp，并建议继续使用工作区内 `.tmp`。
- 当前数据库初始化脚本已可执行，但由于业务模型尚未实现，现阶段只负责加载已注册模型并执行 `create_all()`。

## 2026-03-20 - Task: PostgreSQL 数据库 Schema

### What was done:
- 新增核心模型文件：`user.py`、`employee.py`、`evaluation_cycle.py`、`submission.py`、`uploaded_file.py`、`evidence.py`、`evaluation.py`、`dimension_score.py`、`certification.py`、`salary_recommendation.py`、`approval.py`、`import_job.py`、`audit_log.py`。
- 新增 `backend/app/models/mixins.py` 统一 UUID 主键和时间戳字段定义，并在 `database.py` 中补充 metadata naming convention，稳定外键、唯一约束和索引命名。
- 为员工、评估周期、提交记录、AI 评估、调薪建议、审批记录等实体建立外键关系、唯一约束和关键索引。
- 初始化 Alembic：生成 `alembic/` 目录、`alembic.ini`、`alembic/env.py`，并接通 `Settings`、`Base.metadata` 与模型自动加载。
- 生成首个迁移脚本 `alembic/versions/d432371fb104_initial_schema.py`。
- 执行 `alembic upgrade head`，将数据库升级到初始 schema。
- 新增 `backend/tests/test_models/test_schema_models.py`，验证所有核心表可在 SQLite 环境下创建完成。

### Testing:
- `.\.venv\Scripts\python.exe -m pytest backend\tests -q -p no:cacheprovider`
- `.\.venv\Scripts\python.exe -m py_compile backend\app\core\config.py backend\app\core\database.py backend\app\core\init_db.py backend\app\dependencies.py backend\app\models\mixins.py backend\app\models\user.py backend\app\models\employee.py backend\app\models\evaluation_cycle.py backend\app\models\submission.py backend\app\models\uploaded_file.py backend\app\models\evidence.py backend\app\models\evaluation.py backend\app\models\dimension_score.py backend\app\models\certification.py backend\app\models\salary_recommendation.py backend\app\models\approval.py backend\app\models\import_job.py backend\app\models\audit_log.py backend\tests\test_models\test_schema_models.py alembic\env.py alembic\versions\d432371fb104_initial_schema.py`
- `.\.venv\Scripts\python.exe -m alembic revision --autogenerate -m "initial schema"`
- `.\.venv\Scripts\python.exe -m alembic upgrade head`
- `.\.venv\Scripts\python.exe -m alembic current`
- `.\.venv\Scripts\python.exe -c "from sqlalchemy import create_engine, inspect; ..."`

### Notes:
- 当前默认数据库使用 `sqlite+pysqlite:///./wage_adjust.db`，便于本地开发和迁移验证；后续切换 PostgreSQL 时可直接通过 `.env` 覆盖 `DATABASE_URL`。
- `alembic.ini` 需要保持无 BOM 编码，否则在当前 Windows 本地编码环境下会触发 Alembic 配置解析错误。

## 2026-03-20 - Task: FastAPI 应用初始化

### What was done:
- 新增 `backend/app/main.py`，实现 FastAPI 应用入口、应用工厂、`/health` 健康检查、v1 路由注册、全局异常处理与 CORS 配置。
- 新增 `backend/app/core/logging.py`，统一初始化控制台日志输出。
- 新增 `backend/app/api/__init__.py`、`backend/app/api/v1/__init__.py`、`backend/app/api/v1/router.py`、`backend/app/api/v1/system.py`，提供 v1 路由骨架和 `/api/v1/system/meta` 系统元信息接口。
- 新增 `scripts/start_backend.ps1` 作为本地开发启动脚本。
- 新增 `backend/tests/test_api/test_main.py`，覆盖健康检查、v1 路由注册、CORS 预检、统一异常处理与参数校验异常响应。

### Testing:
- `.\.venv\Scripts\python.exe -m py_compile backend\app\main.py backend\app\core\logging.py backend\app\api\__init__.py backend\app\api\v1\__init__.py backend\app\api\v1\router.py backend\app\api\v1\system.py backend\tests\test_api\test_main.py`
- `.\.venv\Scripts\python.exe -m pytest backend\tests -q -p no:cacheprovider`
- `.\.venv\Scripts\python.exe -c "from backend.app.main import app; ..."`
- 启动验证：短暂拉起 `uvicorn backend.app.main:app --host 127.0.0.1 --port 8011`，并成功请求 `GET /health`

### Notes:
- 当前日志格式先采用控制台可读格式，后续若需要严格 JSON 日志可在 `backend/app/core/logging.py` 上继续增强。
- 目前 v1 路由先放入系统元信息骨架，后续业务 API（认证、员工、周期等）可直接在 `backend/app/api/v1/` 下继续扩展并接入 `api_router`。

## 2026-03-20 - Task: 用户认证与权限 API

### What was done:
- 新增 `backend/app/core/security.py`，实现密码哈希、密码校验、access token / refresh token 生成与 JWT 解码校验。
- 新增 `backend/app/schemas/user.py`，定义用户注册、登录、刷新令牌、用户读取和认证响应 schema。
- 扩展 `backend/app/dependencies.py`，新增 `get_current_user()` 与 `require_roles()`，支持 Bearer Token 鉴权和基于角色的权限校验。
- 新增 `backend/app/api/v1/auth.py`，实现 `POST /api/v1/auth/register`、`POST /api/v1/auth/login`、`POST /api/v1/auth/refresh` 和 `GET /api/v1/auth/me`。
- 更新 `backend/app/api/v1/router.py`，将认证路由接入现有 v1 路由骨架。
- 新增 `backend/tests/test_api/test_auth.py`，覆盖注册、重复注册、登录、刷新 token、`/me` 鉴权和无效 token 场景。
- 为适配当前运行环境，将密码哈希方案切换为 `pbkdf2_sha256`，避免 `bcrypt` 在当前 Python/依赖组合下的兼容性问题。

### Testing:
- `.\.venv\Scripts\python.exe -m py_compile backend\app\core\security.py backend\app\schemas\__init__.py backend\app\schemas\user.py backend\app\dependencies.py backend\app\api\v1\auth.py backend\app\api\v1\router.py backend\tests\test_api\test_auth.py`
- `.\.venv\Scripts\python.exe -m pytest backend\tests -q -p no:cacheprovider`
- `.\.venv\Scripts\python.exe -c "from backend.app.main import app; ..."` 验证 `/api/v1/auth/register`、`/api/v1/auth/login`、`/api/v1/auth/refresh`、`/api/v1/auth/me` 已成功注册

### Notes:
- 当前认证接口使用 SQLite 本地测试数据库配合依赖覆盖完成验证，后续切换 PostgreSQL 时无需改动 API 契约。
- `require_roles()` 已可直接用于后续员工、周期、审批等业务路由的角色保护。

## 2026-03-20 - Task: 前端项目初始化

### What was done:
- 创建 `frontend/` 项目骨架，补齐 `src/components`、`src/pages`、`src/services`、`src/hooks`、`src/utils`、`src/types` 与 `public` 目录。
- 新增 `frontend/package.json`、`tsconfig.json`、`tsconfig.node.json`、`vite.config.ts`、`index.html`、`src/main.tsx`、`src/App.tsx`，完成 Vite + React + TypeScript 基础结构。
- 安装并配置基础前端依赖：`react`、`react-dom`、`react-router-dom`、`axios`、`vite`、`typescript`、`@vitejs/plugin-react`、`tailwindcss`、`postcss`、`autoprefixer`。
- 新增 `frontend/tailwind.config.js`、`frontend/postcss.config.js` 与 `frontend/src/index.css`，完成 Tailwind 初始接入。
- 新增 `frontend/src/services/api.ts` 与 `frontend/src/types/api.ts`，提供 API 服务封装和基础类型。
- 新增一个轻量首页/工作台路由骨架，便于后续继续接登录、员工列表、看板等页面。
- 生成 `frontend/package-lock.json` 并成功产出 `frontend/dist/` 构建结果。

### Testing:
- `cmd /c npm.cmd install`
- `cmd /c npm.cmd run build`
- `cmd /c npm.cmd run lint`

### Notes:
- 由于 `frontend/package.json` 使用 `\"type\": \"module\"`，`postcss.config.js` 需要使用 ESM 的 `export default` 写法。
- 当前 Windows 环境中部分文件如果带 BOM，Vite 读取配置时会报 JSON/配置解析错误，因此前端配置文件已统一处理为无 BOM UTF-8。

## 2026-03-20 - Task: 前端认证与权限功能

### What was done:
- 新增 `frontend/src/services/auth.ts`，封装注册、登录、刷新 token、获取当前用户与本地 token/session 存储逻辑。
- 新增 `frontend/src/hooks/useAuth.tsx`，实现 `AuthProvider`、认证状态恢复、登录/注册动作与退出登录能力。
- 新增 `frontend/src/components/ProtectedRoute.tsx`，实现受保护路由和登录态恢复中的过渡界面。
- 新增 `frontend/src/components/auth/LoginForm.tsx` 与 `frontend/src/components/auth/RegisterForm.tsx`。
- 新增 `frontend/src/pages/Login.tsx` 与 `frontend/src/pages/Register.tsx`，接入后端 auth API 并处理错误提示与跳转。
- 扩展 `frontend/src/types/api.ts`，补充认证相关类型定义。
- 更新 `frontend/src/App.tsx` 与 `frontend/src/main.tsx`，接入 `BrowserRouter`、认证 Provider、登录/注册路由和受保护的 `/workspace` 路由。

### Testing:
- `cmd /c npm.cmd run build`
- `cmd /c npm.cmd run lint`

### Notes:
- 当前前端认证依赖真实后端 `/api/v1/auth/*` 接口，默认通过 `VITE_API_BASE_URL` 或 `http://127.0.0.1:8000/api/v1` 访问。
- 认证状态恢复逻辑会优先尝试当前 access token，失败后再尝试 refresh token，便于后续继续扩展为长会话机制。

## 2026-03-20 - Task: 员工与评估周期服务层 / API

### What was done:
- 新增 `backend/app/schemas/employee.py` 与 `backend/app/schemas/cycle.py`，定义员工和评估周期的创建、更新、读取与列表响应 schema。
- 新增 `backend/app/services/employee_service.py` 与 `backend/app/services/cycle_service.py`，实现员工创建、列表筛选、详情查询，以及评估周期创建、列表、更新和状态发布能力。
- 新增 `backend/app/api/v1/employees.py` 与 `backend/app/api/v1/cycles.py`，实现员工和周期的 CRUD/发布接口。
- 更新 `backend/app/api/v1/router.py`，将 `employees` 和 `cycles` 路由注册到现有 v1 API。
- 新增 `backend/tests/test_services/test_employee_service.py`、`backend/tests/test_services/test_cycle_service.py` 与 `backend/tests/test_api/test_employee_cycle_api.py`，覆盖服务层和鉴权后的 API 全链路验证。

### Testing:
- `.\.venv\Scripts\python.exe -m py_compile backend\app\schemas\employee.py backend\app\schemas\cycle.py backend\app\services\__init__.py backend\app\services\employee_service.py backend\app\services\cycle_service.py backend\app\api\v1\employees.py backend\app\api\v1\cycles.py backend\app\api\v1\router.py backend\tests\test_services\test_employee_service.py backend\tests\test_services\test_cycle_service.py backend\tests\test_api\test_employee_cycle_api.py`
- `.\.venv\Scripts\python.exe -m pytest backend\tests -q -p no:cacheprovider`
- `.\.venv\Scripts\python.exe -c "from backend.app.main import app; ..."` 验证员工和周期路由已成功注册

### Notes:
- 员工创建当前对 `employee_no` 做唯一性校验，周期发布当前通过 `status='published'` 进行状态流转，后续可继续扩展更细的业务规则。
- 当前测试存在来自 `python-jose` 的 `datetime.utcnow()` 弃用告警，属于依赖库内部实现，不影响当前功能正确性，但后续可考虑升级或封装替代。

## 2026-03-20 - Task: 员工评估列表页 / 创建评估周期页 UI

### What was done:
- 新增 `frontend/src/services/employeeService.ts` 与 `frontend/src/services/cycleService.ts`，封装员工列表查询、周期列表读取和创建评估周期请求。
- 扩展 `frontend/src/types/api.ts`，补充员工列表、周期列表和创建周期表单相关类型定义。
- 新增 `frontend/src/pages/Employees.tsx`，实现员工评估列表页，支持按部门、岗位族、状态筛选，并展示员工档案卡片与评估入口信息。
- 新增 `frontend/src/pages/CreateCycle.tsx` 与 `frontend/src/components/cycle/CreateCycleForm.tsx`，实现创建评估周期页面、预算录入和历史周期侧边展示。
- 更新 `frontend/src/App.tsx`，将 `/employees` 与 `/cycles/create` 接入受保护路由，同时增强工作台卡片入口跳转。
- 页面已接入真实后端 `/api/v1/employees` 和 `/api/v1/cycles` API，可直接在当前认证体系下联调。

### Testing:
- `cmd /c npm.cmd run build`
- `cmd /c npm.cmd run lint`

### Notes:
- 当前员工列表页已覆盖任务要求中的主要筛选和业务入口，但员工详情页尚未实现，因此详情跳转将在后续任务 30 开始继续补齐。
- 创建评估周期页当前使用现有后端周期创建能力，默认将新建成功后的用户带回员工评估列表，便于继续后续评估工作流。

## 2026-03-20 - Task: 员工评估详情页 / 基础布局与状态展示

### What was done:
- 新增 `frontend/src/pages/EvaluationDetail.tsx`，实现员工评估详情页基础布局，支持通过路由参数读取员工 ID 并调用后端员工详情接口。
- 新增 `frontend/src/components/evaluation/StatusIndicator.tsx`，统一承接员工状态和评估生命周期状态的视觉展示。
- 扩展 `frontend/src/services/employeeService.ts`，新增 `fetchEmployee()` 单条员工详情查询方法。
- 更新 `frontend/src/pages/Employees.tsx`，将员工卡片接入详情页跳转，打通列表到详情的导航链路。
- 更新 `frontend/src/App.tsx`，注册 `/employees/:employeeId` 受保护路由，并将应用首页文案同步到当前已实现能力。
- 详情页当前展示员工基础信息、当前周期占位、评估流转状态和后续动作入口，为后续文件上传、解析、评级和调薪功能预留承接位置。

### Testing:
- `cmd /c npm.cmd run build`
- `cmd /c npm.cmd run lint`

### Notes:
- 当前详情页基于现有后端员工详情接口完成，评估数据、提交材料和实际评分结果将在后续任务中继续接入。
- 由于当前 shell 对中文源码写入存在编码风险，本轮新增和重写页面统一采用 ASCII 文案，后续如需再切回中文展示，可在稳定编辑链路下统一处理。

## 2026-03-20 - Task: 员工评估详情页 / 材料与证据区

### What was done:
- 新增 `frontend/src/components/evaluation/FileUploadPanel.tsx`，提供材料选择入口与支持格式提示。
- 新增 `frontend/src/components/evaluation/FileList.tsx`，展示材料列表、解析状态以及预留的重试解析和删除操作。
- 新增 `frontend/src/components/evaluation/EvidenceCard.tsx`，承接结构化证据展示、可信度标签和来源标签。
- 扩展 `frontend/src/types/api.ts`，补充 `UploadedFileRecord` 与 `EvidenceRecord` 类型定义，便于后续直接接入上传与解析 API。
- 更新 `frontend/src/pages/EvaluationDetail.tsx`，将材料上传区、文件列表和证据卡片集成到详情页，并补充本地 staged file 状态流转骨架。
- 当前详情页已经具备材料与证据区的 UI 承接能力，后续只需要将后端上传、预览、解析和证据接口接入现有组件即可。

### Testing:
- `cmd /c npm.cmd run build`
- `cmd /c npm.cmd run lint`

### Notes:
- 由于后端文件上传与解析 API 尚未实现，本轮材料区使用本地 staged state 和占位证据数据完成 UI 闭环，未伪造真实后端联调结果。
- `Preview` 按钮已预留位置但暂时禁用，等任务 20 的预览接口落地后可直接接通。

## 2026-03-20 - Task: 员工评估详情页 / 复核与校准区

### What was done:
- 新增 `frontend/src/components/review/ReviewPanel.tsx`，承接人工复核等级、复核备注和后续操作入口。
- 新增 `frontend/src/components/review/DimensionScoreEditor.tsx`，支持按维度修改分数并记录调整理由。
- 新增 `frontend/src/components/review/CalibrationCompareTable.tsx`，展示 AI 分数与人工分数的差异对比和校准说明。
- 更新 `frontend/src/pages/EvaluationDetail.tsx`，把复核区和校准区接入当前详情页，并补充本地维度分数、人工等级和复核备注状态。
- 当前详情页已经形成基础信息、材料与证据区、复核与校准区三段式结构，后续只需要把真实评估和审批 API 接上即可。

### Testing:
- `cmd /c npm.cmd run build`
- `cmd /c npm.cmd run lint`

### Notes:
- 本轮复核区使用本地状态和占位 AI/人工分数对比数据完成 UI 骨架，未伪造后端评估结果。
- `Submit review` 和 `Send to calibration` 按钮当前为预留入口，等任务 21 和 23 的 API 落地后可直接接通。

## 2026-03-20 - Task: 调薪模拟与审批中心 UI

### What was done:
- 新增 `frontend/src/components/salary/BudgetSimulationPanel.tsx`，提供预算金额、部门/岗位族筛选和推荐成本汇总展示。
- 新增 `frontend/src/components/approval/ApprovalTable.tsx`，承接审批记录表格、审批状态标签和审批操作入口。
- 新增 `frontend/src/pages/SalarySimulator.tsx`，实现调薪模拟页并接入预算模拟组件和推荐候选卡片。
- 新增 `frontend/src/pages/Approvals.tsx`，实现审批中心页并接入审批统计卡片和审批表格。
- 更新 `frontend/src/App.tsx`，注册 `/salary-simulator` 与 `/approvals` 受保护路由，并将工作台入口扩展为员工、周期、模拟和审批四个主入口。
- 当前前端已经具备从工作台进入调薪模拟和审批中心的完整路由与页面骨架，后续可直接对接真实调薪建议和审批 API。

### Testing:
- `cmd /c npm.cmd run build`
- `cmd /c npm.cmd run lint`

### Notes:
- 本轮使用本地 mock 数据完成预算模拟与审批中心的 UI 闭环，未伪造后端调薪或审批结果。
- `Approve` 和 `Reject` 按钮当前为占位入口，待任务 22 和任务 23 的 API 实现后可直接接入。

## 2026-03-20 - Task: 组织看板、导入中心与通用体验优化 UI

### What was done:
- 新增 `frontend/src/pages/Dashboard.tsx`，实现组织看板页面并接入概览卡片、热力图和分布图组件。
- 新增 `frontend/src/pages/ImportCenter.tsx`，实现导入中心页面并接入批量导入任务表格和模板入口。
- 新增 `frontend/src/components/dashboard/OverviewCards.tsx`、`HeatmapChart.tsx`、`DistributionChart.tsx`，承接组织概览、热力图和分布展示。
- 新增 `frontend/src/components/import/ImportJobTable.tsx`，承接导入任务列表展示。
- 新增 `frontend/src/components/ErrorBoundary.tsx`，为当前前端路由树提供基础错误兜底。
- 更新 `frontend/src/App.tsx`，注册 `/dashboard` 和 `/import-center` 受保护路由，并把工作台入口扩展为员工、周期、模拟、审批、看板和导入中心六个主模块。

### Testing:
- `cmd /c npm.cmd run build`
- `cmd /c npm.cmd run lint`

### Notes:
- 本轮看板和导入中心使用本地 mock 数据完成 UI 闭环，未伪造后端统计或导入 API 的真实联调结果。
- 当前仅补了基础 `ErrorBoundary` 这一类通用体验组件；如果后续需要更细的 Toast、Spinner、Skeleton 体系，可以在现有页面骨架上继续统一扩展。

## 2026-03-20 - Task: 文件上传、解析服务与材料 API

### What was done:
- 新增 `backend/app/core/storage.py`，实现本地文件系统存储服务，支持文件写入、读取、删除和预览 URL 生成。
- 扩展 `backend/app/core/config.py`，补充 `storage_base_dir` 与 `max_upload_size_mb` 配置，为上传链路提供本地默认值。
- 新增 `backend/app/parsers/base_parser.py`、`document_parser.py`、`ppt_parser.py`、`image_parser.py`、`code_parser.py`，实现多类型文件的基础解析能力。
- 新增 `backend/app/schemas/file.py`，定义上传文件、证据列表、预览响应和解析结果 schema。
- 新增 `backend/app/services/file_service.py`，实现文件类型校验、大小限制、落盘存储、材料列表与预览能力。
- 新增 `backend/app/services/parse_service.py`，实现按文件类型调度解析器、落库 `evidence_items`、更新文件解析状态和提交状态。
- 新增 `backend/app/api/v1/files.py`，实现以下接口：
  - `POST /api/v1/submissions/{id}/files`
  - `GET /api/v1/submissions/{id}/files`
  - `POST /api/v1/files/{id}/parse`
  - `POST /api/v1/submissions/{id}/parse-all`
  - `GET /api/v1/files/{id}/preview`
  - `GET /api/v1/submissions/{id}/evidence`
- 更新 `backend/app/api/v1/router.py` 与 `backend/app/services/__init__.py`，将文件路由和服务纳入现有应用骨架。
- 新增测试 `backend/tests/test_services/test_parse_service.py` 与 `backend/tests/test_api/test_file_api.py`，覆盖 markdown 文件解析、上传、预览、单文件解析、批量解析和证据读取链路。

### Testing:
- `.
\.venv\Scripts\python.exe -m pytest backend\tests\test_services\test_parse_service.py -q -p no:cacheprovider`
- `.
\.venv\Scripts\python.exe -m pytest backend\tests\test_api\test_file_api.py -q -p no:cacheprovider`
- `.
\.venv\Scripts\python.exe -m pytest backend\tests -q -p no:cacheprovider`
- `.
\.venv\Scripts\python.exe -m py_compile backend\app\core\config.py backend\app\core\storage.py backend\app\parsers\base_parser.py backend\app\parsers\document_parser.py backend\app\parsers\ppt_parser.py backend\app\parsers\image_parser.py backend\app\parsers\code_parser.py backend\app\schemas\file.py backend\app\services\file_service.py backend\app\services\parse_service.py backend\app\api\v1\files.py backend\app\api\v1\router.py backend\tests\test_services\test_parse_service.py backend\tests\test_api\test_file_api.py`

### Notes:
- 这一轮为了先打通可运行主链路，存储层采用本地文件系统实现，接口形态保持与后续 MinIO/S3 封装兼容，后面可平滑替换实现而不影响 API 契约。
- 当前多格式解析已覆盖 PDF、Markdown、TXT、PPTX、PNG/JPG、常见代码和配置文件，`.ppt`、Excel 深度解析与 OCR 仍可在后续任务中继续增强。
- 全量后端测试当前为 `21 passed`；警告仍然来自 `python-jose` 内部使用 `datetime.utcnow()`，不影响当前功能正确性。

## 2026-03-20 - Task: 自动评级 API

### What was done:
- 新增 `backend/app/engines/evaluation_engine.py`，实现基于证据数量、文本丰富度、来源多样性和平均可信度的基础自动评分逻辑，并映射到 `Level 1` 到 `Level 5`。
- 新增 `backend/app/schemas/evaluation.py`，定义评估详情、维度分数、人工复核请求和确认响应 schema。
- 新增 `backend/app/services/evaluation_service.py`，实现评估生成、重生成、人工复核、确认以及维度分数持久化能力。
- 新增 `backend/app/api/v1/evaluations.py`，实现以下接口：
  - `POST /api/v1/evaluations/generate`
  - `POST /api/v1/evaluations/regenerate`
  - `GET /api/v1/evaluations/{id}`
  - `PATCH /api/v1/evaluations/{id}/manual-review`
  - `POST /api/v1/evaluations/{id}/confirm`
- 更新 `backend/app/api/v1/router.py` 与 `backend/app/services/__init__.py`，将自动评级路由和服务接入现有应用。
- 新增测试 `backend/tests/test_services/test_evaluation_service.py` 与 `backend/tests/test_api/test_evaluation_api.py`，覆盖自动生成、重生成、人工复核和确认链路。

### Testing:
- `.
\.venv\Scripts\python.exe -m pytest backend\tests\test_services\test_evaluation_service.py -q -p no:cacheprovider`
- `.
\.venv\Scripts\python.exe -m pytest backend\tests\test_api\test_evaluation_api.py -q -p no:cacheprovider`
- `.
\.venv\Scripts\python.exe -m pytest backend\tests -q -p no:cacheprovider`
- `.
\.venv\Scripts\python.exe -m py_compile backend\app\engines\evaluation_engine.py backend\app\schemas\evaluation.py backend\app\services\evaluation_service.py backend\app\api\v1\evaluations.py backend\app\api\v1\router.py backend\tests\test_services\test_evaluation_service.py backend\tests\test_api\test_evaluation_api.py`

### Notes:
- 当前自动评级引擎采用可解释、可测试的本地规则实现，已经能输出五个维度分数、总分、等级、置信度和是否需要人工复核标记，后续可再替换为更复杂的 LLM 驱动实现。
- 人工复核接口已支持覆盖等级、总分、说明文本和单维度分数，为前端详情页复核区接入真实后端结果打下基础。
- 全量后端测试当前为 `23 passed`；告警仍来自 `python-jose` 内部的 `datetime.utcnow()` 用法，不影响当前功能正确性。

## 2026-03-20 - Task: 调薪建议 API

### What was done:
- 新增 `backend/app/engines/salary_engine.py`，实现基于 AI 等级、总分、岗位职级、部门和认证加成的确定性调薪计算逻辑。
- 新增 `backend/app/schemas/salary.py`，定义调薪建议详情、更新请求、预算模拟请求和锁定响应 schema。
- 新增 `backend/app/services/salary_service.py`，实现调薪建议生成、读取、人工调整、锁定以及按周期预算模拟能力。
- 新增 `backend/app/api/v1/salary.py`，实现以下接口：
  - `POST /api/v1/salary/recommend`
  - `GET /api/v1/salary/{id}`
  - `POST /api/v1/salary/simulate`
  - `PATCH /api/v1/salary/{id}`
  - `POST /api/v1/salary/{id}/lock`
- 更新 `backend/app/api/v1/router.py` 与 `backend/app/services/__init__.py`，将调薪建议路由和服务接入现有应用。
- 新增测试 `backend/tests/test_services/test_salary_service.py` 与 `backend/tests/test_api/test_salary_api.py`，覆盖调薪建议生成、预算模拟、人工调整和锁定链路。

### Testing:
- `.
\.venv\Scripts\python.exe -m pytest backend\tests\test_services\test_salary_service.py -q -p no:cacheprovider`
- `.
\.venv\Scripts\python.exe -m pytest backend\tests\test_api\test_salary_api.py -q -p no:cacheprovider`
- `.
\.venv\Scripts\python.exe -m pytest backend\tests -q -p no:cacheprovider`
- `.
\.venv\Scripts\python.exe -m py_compile backend\app\engines\salary_engine.py backend\app\schemas\salary.py backend\app\services\salary_service.py backend\app\api\v1\salary.py backend\app\api\v1\router.py backend\tests\test_services\test_salary_service.py backend\tests\test_api\test_salary_api.py`

### Notes:
- 当前调薪引擎采用可解释的本地规则，已经能输出推荐比例、推荐薪资、AI multiplier、认证加成和最终调整比例，后续可以在不改 API 契约的前提下升级为更复杂的预算优化策略。
- 预算模拟当前按周期下的现有评估结果和调薪建议汇总推荐增量成本，已能返回是否超预算标记，为前端调薪模拟页接真实数据做好准备。
- 全量后端测试当前为 `25 passed`；警告仍来自 `python-jose` 内部的 `datetime.utcnow()` 用法，不影响当前功能正确性。

## 2026-03-20 - Task: 前端接入真实评估与调薪 API

### What was done:
- 扩展 `backend/app/models/submission.py`、`backend/app/schemas/submission.py`、`backend/app/api/v1/submissions.py`，为前端详情页补充 submission ensure/list 入口和 `evaluation_id` 读取能力。
- 扩展 `backend/app/api/v1/evaluations.py`，新增按 submission 查询评估结果的 `GET /api/v1/evaluations/by-submission/{submission_id}`。
- 扩展 `backend/app/schemas/salary.py` 与 `backend/app/services/salary_service.py`，让薪酬模拟结果返回 `current_salary`，便于前端直接展示当前薪资、建议增量和目标薪资。
- 更新 `frontend/src/services/api.ts`，增加自动注入 Bearer Token 的 axios 请求拦截器。
- 新增 `frontend/src/services/submissionService.ts`、`fileService.ts`、`evaluationService.ts`、`salaryService.ts`，将 submission/files/evaluations/salary 真实 API 全部接入前端服务层。
- 重写 `frontend/src/pages/EvaluationDetail.tsx`，改为真实读取 cycle、submission、files、evidence、evaluation 和 salary recommendation 数据，并支持真实上传材料、触发解析、生成评估、提交人工复核、确认评估和生成调薪建议。
- 重写 `frontend/src/pages/SalarySimulator.tsx`，改为真实读取周期列表和预算模拟接口，而不再依赖本地 mock 数据。
- 更新 `frontend/src/components/review/ReviewPanel.tsx`、`frontend/src/components/salary/BudgetSimulationPanel.tsx` 与 `frontend/src/components/evaluation/FileList.tsx`，让组件契合真实接口与交互状态。
- 更新 `frontend/src/types/api.ts`，统一补齐 submission、evaluation、salary simulation 等前端类型定义。

### Testing:
- `cmd /c npm.cmd run build`
- `cmd /c npm.cmd run lint`
- `.
\.venv\Scripts\python.exe -m pytest backend\tests -q -p no:cacheprovider`

### Notes:
- 详情页现在已经不再依赖本地占位评估/调薪数据，真正接入了后端 submission/files/evaluations/salary API；唯一仍保留占位性质的是文件删除和预览按钮，后端尚未提供删除接口，预览入口仍为禁用态。
- 这次属于已完成任务之上的前后端联调增强，没有单独改 `task.json` 的新任务状态，以免混淆原始任务边界。

## 2026-03-20 - Task: 复核、校准与审批服务 / API

### What was done:
- 新增 `backend/app/services/approval_service.py`，实现送审、待办列表、审批决策、审批历史与校准队列查询。
- 新增 `backend/app/schemas/approval.py`，定义审批步骤、审批记录、审批状态返回与校准队列响应结构。
- 新增 `backend/app/api/v1/approvals.py`，开放 `/api/v1/approvals`、`/submit`、`/{id}`、`/recommendations/{id}/history`、`/calibration-queue` 接口，并注册到 v1 router。
- 调整 `backend/app/schemas/salary.py` 与 `backend/app/services/salary_service.py`，让调薪模拟结果补充 `current_salary`，与前端实时模拟页保持一致。
- 新增 `backend/tests/test_services/test_approval_service.py` 和 `backend/tests/test_api/test_approval_api.py`，覆盖审批链路、权限校验和校准队列。

### Testing:
- `\.\.venv\Scripts\python.exe -m pytest backend\tests\test_services\test_approval_service.py backend\tests\test_api\test_approval_api.py -q -p no:cacheprovider`
- `\.\.venv\Scripts\python.exe -m py_compile backend\app\schemas\approval.py backend\app\schemas\salary.py backend\app\services\approval_service.py backend\app\services\salary_service.py backend\app\api\v1\approvals.py backend\app\api\v1\router.py backend\tests\test_services\test_approval_service.py backend\tests\test_api\test_approval_api.py`
- `\.\.venv\Scripts\python.exe -m pytest backend\tests -q -p no:cacheprovider`

### Notes:
- 当前审批模型仍沿用 `recommendation_id + step_name` 唯一约束，因此送审接口按“多步骤、每步一个 approver”组织，而不是同一步多人并签。
- 校准中心目前提供真实待复核队列查询；更细粒度的校准会议编排和审计扩展可以继续在此基础上补充。

## 2026-03-20 - Task: 看板统计与洞察服务 / API

### What was done:
- 新增 `backend/app/schemas/dashboard.py`、`backend/app/services/dashboard_service.py` 和 `backend/app/api/v1/dashboard.py`，实现 overview、AI level distribution、department heatmap、ROI distribution 以及 `snapshot` 聚合接口。
- 将 dashboard 路由注册到 `backend/app/api/v1/router.py`，并把 `DashboardService` 暴露到服务导出入口。
- 新增 `backend/tests/test_services/test_dashboard_service.py` 和 `backend/tests/test_api/test_dashboard_api.py`，覆盖统计口径、路由结构与鉴权访问。
- 新增 `frontend/src/services/dashboardService.ts`，并更新 `frontend/src/pages/Dashboard.tsx` 与 `frontend/src/types/api.ts`，让组织看板页切换为真实后端数据源。

### Testing:
- `\.\.venv\Scripts\python.exe -m pytest backend\tests\test_services\test_dashboard_service.py backend\tests\test_api\test_dashboard_api.py -q -p no:cacheprovider`
- `\.\.venv\Scripts\python.exe -m py_compile backend\app\schemas\dashboard.py backend\app\services\dashboard_service.py backend\app\api\v1\dashboard.py backend\app\api\v1\router.py backend\tests\test_services\test_dashboard_service.py backend\tests\test_api\test_dashboard_api.py`
- `cmd /c npm.cmd run build`
- `cmd /c npm.cmd run lint`
- `\.\.venv\Scripts\python.exe -m pytest backend\tests -q -p no:cacheprovider`

### Notes:
- 当前 ROI 分布基于评估分数、AI multiplier、认证加成和调薪比例生成“estimated ROI” 分桶，属于可解释近似口径；后续如果接入真实业务收益数据，可以直接替换该计算。
- 前端看板页现在直接走 `/api/v1/dashboard/snapshot`，请求更少，也便于后续继续加周期筛选。

## 2026-03-20 - Task: 批量导入服务 / 导入导出 API

### What was done:
- 新增 `backend/app/schemas/import_job.py`、`backend/app/services/import_service.py` 和 `backend/app/api/v1/imports.py`，实现导入任务创建、任务列表、任务详情、模板下载和结果导出。
- 当前真实支持两类导入：`employees` 与 `certifications`，导入结果会写入 `import_jobs` 并保留逐行结果摘要。
- 新增 `backend/tests/test_services/test_import_service.py` 和 `backend/tests/test_api/test_import_api.py`，覆盖 CSV 导入成功、模板下载、结果导出以及 Excel 环境限制提示。
- 新增 `frontend/src/services/importService.ts`，并更新 `frontend/src/pages/ImportCenter.tsx`、`frontend/src/components/import/ImportJobTable.tsx` 与 `frontend/src/types/api.ts`，让导入中心页接入真实任务流。

### Testing:
- `\.\.venv\Scripts\python.exe -m pytest backend\tests\test_services\test_import_service.py backend\tests\test_api\test_import_api.py -q -p no:cacheprovider`
- `\.\.venv\Scripts\python.exe -m py_compile backend\app\schemas\import_job.py backend\app\services\import_service.py backend\app\api\v1\imports.py backend\app\api\v1\router.py backend\tests\test_services\test_import_service.py backend\tests\test_api\test_import_api.py`
- `cmd /c npm.cmd run build`
- `cmd /c npm.cmd run lint`
- `\.\.venv\Scripts\python.exe -m pytest backend\tests -q -p no:cacheprovider`

### Notes:
- 当前环境未安装 `openpyxl`，所以 `xlsx/xls` 上传会返回明确错误提示；稳定可用格式是 `csv`。
- 导入中心页已经接通真实模板下载、CSV 上传和结果导出，后续如果补上 `openpyxl`，可以直接扩展 Excel 支持而不需要改前端交互。

## 2026-03-20 - Task: 对外输出与系统集成 / 对外开放 API

### What was done:
- 新增 `backend/app/schemas/public.py`、`backend/app/services/integration_service.py` 和 `backend/app/api/v1/public.py`，实现对外只读集成接口。
- 新增公开接口：`/api/v1/public/employees/{employee_no}/latest-evaluation`、`/api/v1/public/cycles/{id}/salary-results`、`/api/v1/public/cycles/{id}/approval-status`、`/api/v1/public/dashboard/summary`。
- 为公开接口接入 `X-API-Key` 鉴权，并通过 `audit_logs` 记录外部读取行为。
- 调整 `backend/app/main.py`，让 `create_app(settings)` 时依赖注入真正使用传入的 `Settings`，修复测试环境和运行时配置不一致的问题。
- 新增 `backend/tests/test_services/test_integration_service.py` 和 `backend/tests/test_api/test_public_api.py`，覆盖导出结构、API Key 鉴权和访问审计。

### Testing:
- `\.\.venv\Scripts\python.exe -m pytest backend\tests\test_services\test_integration_service.py backend\tests\test_api\test_public_api.py -q -p no:cacheprovider`
- `\.\.venv\Scripts\python.exe -m py_compile backend\app\schemas\public.py backend\app\services\integration_service.py backend\app\api\v1\public.py backend\app\dependencies.py backend\app\main.py backend\app\api\v1\router.py backend\tests\test_services\test_integration_service.py backend\tests\test_api\test_public_api.py`
- `\.\.venv\Scripts\python.exe -m pytest backend\tests -q -p no:cacheprovider`

### Notes:
- 当前对外开放 API 是稳定的只读接口，适合 HR/绩效/薪酬系统拉取最新结果；尚未实现分页和增量同步游标，但返回结构已经为后续扩展预留了空间。
- `PUBLIC_API_RATE_LIMIT` 目前仍是配置预留，尚未接入真实限流中间件；这次先保证鉴权、结构和审计链路稳定可用。

## 2026-03-20 - Task: DeepSeek 封装 / 证据抽取 / AI评级引擎 / 调薪引擎

### What was done:
- 新增 `backend/app/services/llm_service.py`，封装 DeepSeek 调用、重试、分钟级内存限流、提示词模板与 JSON fallback 机制。
- 新增 `backend/app/services/evidence_service.py`，对解析后的文档做结构化证据抽取、标签归类、可信度估算，并把 LLM 调用结果与本地 heuristics 合并。
- 更新 `backend/app/services/parse_service.py`，让文件解析后的证据生成统一走 `EvidenceService`，并把 `llm_used_fallback`、`tags`、`credibility_notes` 等信息写入 evidence metadata。
- 重写 `backend/app/engines/evaluation_engine.py`，基于五维关键词命中、来源可靠度、证据丰富度与置信度做更正式的加权评分和等级映射。
- 重写 `backend/app/engines/salary_engine.py`，引入等级档位、职级/部门/岗位族调整、认证加成 clamp 和预算约束判断。
- 更新 `backend/app/services/salary_service.py`，把 `job_family` 传入调薪引擎并使用新的预算守卫逻辑。
- 新增测试：`backend/tests/test_services/test_llm_service.py`、`backend/tests/test_services/test_evidence_service.py`、`backend/tests/test_engines/test_rule_engines.py`。

### Testing:
- `\.\.venv\Scripts\python.exe -m pytest backend\tests -q -p no:cacheprovider`
- `\.\.venv\Scripts\python.exe -m py_compile backend\app\core\config.py backend\app\services\llm_service.py backend\app\services\evidence_service.py backend\app\services\parse_service.py backend\app\engines\evaluation_engine.py backend\app\engines\salary_engine.py backend\app\services\salary_service.py backend\app\services\__init__.py backend\tests\test_services\test_llm_service.py backend\tests\test_services\test_evidence_service.py backend\tests\test_engines\test_rule_engines.py`

### Notes:
- 当前环境没有真实 DeepSeek API Key，也不可外网联调，所以 `DeepSeekService` 默认会在未配置或调用失败时自动回落到本地 fallback；链路已经接好，后续只要配置真实 Key 即可切线上调用。
- 这次为了保持已完成 API 契约稳定，没有改动前端和现有评估/调薪接口的返回结构，只增强了内部评分逻辑和 evidence metadata。

## 2026-03-20 - Task: 首页与总览 UI

### What was done:
- 更新 `frontend/src/App.tsx`，把原本较薄的首页升级为真正的产品落地页，补充了 capabilities、workflow、module map 和认证态入口。
- 同时增强了 `/workspace` 工作台页，补充总览说明、当前项目足迹卡片和更完整的模块导航入口。
- 保持现有业务路由不变，让首页负责产品叙事与总览导流，工作台负责已登录后的操作入口。

### Testing:
- `cmd /c npm.cmd run build`
- `cmd /c npm.cmd run lint`

### Notes:
- 这轮主要是信息架构与视觉层升级，没有改动现有后端 API 契约。
- 当前项目只剩任务 35“最终测试、性能优化与部署准备”尚未完成。

## 2026-03-20 - Task: 最终测试、性能优化与部署准备

### What was done:
- 新增 `README.md`，补充项目说明、环境配置、启动方式、测试命令与当前交付边界。
- 新增 `DEPLOYMENT.md`，整理部署前检查项、迁移命令、运行命令和生产加固建议。
- 新增 `scripts/start_frontend.ps1`，补齐本地前端开发启动脚本，与后端启动脚本形成对称入口。
- 完成最终一轮后端与前端验证，确认当前代码、文档和运行说明一致。

### Testing:
- `\.\.venv\Scripts\python.exe -m pytest backend\tests -q -p no:cacheprovider`
- `cmd /c npm.cmd run build`
- `cmd /c npm.cmd run lint`

### Notes:
- 当前项目功能任务已全部完成；真实生产落地前仍建议补充公网限流、对象存储替换和后台任务队列。
- 当前工作区已经存在 `.git`，如果需要我下一步可以直接整理并提交一次完整 commit。
